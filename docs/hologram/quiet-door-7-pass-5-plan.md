# Quiet door #7 (Hologram) — Pass 5 PLAN

**Status:** EXECUTED — seam on `docs/hologram/quiet-door-7-ascii.md`. Door + HTML unchanged. Emission `[GAP]`.  
**Station:** hologram (HELD Grok-hologram).  
**Object:** Hologram seam — vs Hands, clock, frozen viewer.  
**Authority:** [`quiet-door-7-ascii.md`](quiet-door-7-ascii.md) passes 1–4. [`docs/hologram/README.md`](README.md). [`docs/graphics/README.md`](../graphics/README.md). [`docs/clock/README.md`](../clock/README.md). [`docs/shadow-clock-gearing.html`](../shadow-clock-gearing.html).  
**Not:** rewrite any of those. Not renderer claim. Not Graphic E. Not door #6 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door #6 stays CLOSED.

```
pull/ff → refresh hologram
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append seam table to quiet-door-7-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What does the hologram door name, what do the four graphics store, what does the clock door index, what does the frozen viewer show, and what does this reading add — without any of the five becoming another?**

Pass 5 is the seam. It does not start the receipt (pass 6).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1–4 | spine, fields, grain, walk: diagnose / suggest; do not store |

---

## 2. Rows execute will fill (empty until proceed)

| File | Names | Stores | Writes L? | Becomes the other if |
|---|---|---|---|---|
| `docs/hologram/README.md` | | | | |
| `docs/graphics/README.md` | | | | |
| `docs/clock/README.md` | | | | |
| `docs/shadow-clock-gearing.html` | | | | |
| `docs/hologram/quiet-door-7-ascii.md` | | | | |

Expected: hologram names touches; Hands stores the four graphics; clock is CLOSED Layer III index; frozen viewer is renderer HTML; this file reads the hologram door. None writes L. None claims renderer. `nostd-pipeline.html` is named on the hologram door and `which` = renderer — not rewritten.

---

## 3. What execute may write

- Append “Pass 5 — seam” to [`quiet-door-7-ascii.md`](quiet-door-7-ascii.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the hologram door, Hands, clock files, or any HTML.
- Claim renderer.
- Fill G2.
- Reopen door #6.
- Start pass 6 receipt.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote the five files as they stand.
2. Fill the seam. Five stay five.
3. Stop.

Success: this reading is not Hands; frozen HTML is not claimed.  
Failure: any of the five rewritten; lookrefs treated as Graphic E.

---

## Queued — later pass, not this one

| Pass | Act |
|---|---|
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Touches diagnose. Hands stores.
Lookrefs are not Graphic E.
Renderer stays unclaimed.
Door #6 stays CLOSED.
Φ stays [GAP].
```
