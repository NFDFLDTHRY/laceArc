# Quiet door #7 (Hologram) — Pass 3 PLAN

**Status:** PLAN only. Not run.  
**Station:** hologram (HELD Grok-hologram).  
**Object:** Hologram grain — touches vs Hands vs renderer vs primitives.  
**Authority:** [`quiet-door-7-ascii.md`](quiet-door-7-ascii.md) passes 1–2. Hologram door as written. `coord.sh which`.  
**Not:** HTML rewrite. Not renderer claim. Not Graphic E. Not door #6 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door + HTML stay. Door #6 stays CLOSED.

```
pull/ff → refresh hologram
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append four-kind grain to quiet-door-7-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What do hologram touches diagnose, what do Hands store, what does renderer own, what do primitives catalog — and what would it mean to collapse a lookref into Graphic E or rewrite HTML from this station?**

Pass 3 is grain. It does not start the diagnose/suggest walk (pass 4).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | Hg1–Hg8 fields |

---

## 2. Rows execute will fill (empty until proceed)

| Kind | Says | `which` | Collapse would look like |
|---|---|---|---|
| Hologram door | | | |
| visual-primitives catalog | | | |
| scene lookrefs (batch 2) | | | |
| ASTRA chrome (batch 1) | | | |
| `nostd-pipeline.html` | | | |
| Hands (four graphics) | | | |
| Clock door (CLOSED) | | | |
| Frozen viewer | | | |

Expected: hologram door, primitives, lookrefs `which` = hologram. `nostd-pipeline.html` and frozen viewer `which` = renderer. Four graphics `which` = graphics. Clock door `which` = hologram but CLOSED as door. Collapsing a lookref into Hands is the miss. Claiming renderer from hologram is the other miss.

---

## 3. What execute may write

- Append “Pass 3 — grain” to [`quiet-door-7-ascii.md`](quiet-door-7-ascii.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the hologram door, any PNG/JPG, any HTML, Hands, or clock files.
- Claim renderer.
- Fill G2.
- Start pass 4 walk.
- Reopen door #6.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote door, primitives, lookrefs, viewer, Hands. Fill `which` from coord.
2. Fill the grain. Four stay four.
3. Stop.

Success: a later reading cannot treat a lookref as Graphic E.  
Failure: HTML rewritten; renderer claimed; Hands amended.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 4 | walk: diagnose / suggest; do not store |
| 5 | seam vs Hands, clock, frozen viewer |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Touches diagnose. Hands stores.
Lookrefs are not Graphic E.
Renderer stays unclaimed.
Door #6 stays CLOSED.
Φ stays [GAP].
```
