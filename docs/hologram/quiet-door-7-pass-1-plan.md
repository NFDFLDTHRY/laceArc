# Quiet door #7 (Hologram) — Pass 1 PLAN

**Status:** EXECUTED — spine on `docs/hologram/quiet-door-7-ascii.md`. Door unchanged. HTML untouched. Emission `[GAP]`.  
**Station:** hologram (HELD Grok-hologram).  
**Object:** quiet door #7 — [`docs/hologram/README.md`](README.md).  
**Why hologram, not kit / not renderer:** hologram OWNS `docs/hologram/**`. The companion can live next to the door. Renderer owns `nostd-pipeline.html` (and two other viewers). This campaign does not claim renderer and does not rewrite HTML.  
**Not:** Core. Not Graphic E. Not Graphics A–D. Not door #6 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the hologram door. Do not rewrite HTML. Do not claim renderer. Do not reopen door #6.

```
pull/ff → refresh hologram
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/hologram/quiet-door-7-ascii.md spine
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the hologram door be one ASCII spine of what it names (touches that diagnose or suggest a projection; lookrefs; primitives catalog; CTC as anti-model) and what it refuses (Graphics A–D, store, cubes as WORD, ASTRA as runtime, CTC as Core) — without becoming a fifth graphic or a viewer?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Register row #7 | LIVE; `which` = hologram |
| Door #6 (Clock) | CLOSED 6/6; same station; not this iteration |
| Renderer | FREE; `nostd-pipeline.html` is renderer; HTML not this claim |
| Hands | four graphics still source of record |

This campaign makes #7 activity WORKED, then CLOSED at pass 6 of *this door iteration*. That does not flip LIVE. That does not reopen Clock.

---

## 2. Spine execute will draw (shape only)

Off the hologram door only. Do not paste PNGs. Do not paste HTML.

```
  HOLOGRAM DOOR
  docs/hologram/README.md
  touches (Shadow)
  not Graphics A–D; they do not store Lace
  they diagnose the holder or suggest a projection
  source of record remains docs/graphics/ A–D

  Touches
    story defense / overlap / why a true claim can fail
    clockwork views: lattice / crossing / traces
    visual-primitives.md — camera vocabulary

  Scene lookrefs (batch 2)
    canvas follows this set, not ASTRA white dashboards

  ASTRA chrome (batch 1)
    view instruments only
    not Lace runtime; not the canvas background
    proof counters are theater

  Primitive sheets
    lawful shape catalog beside lookrefs

  Live Layer III
    nostd-pipeline.html     renderer
    not shadow-clock-gearing.html
    not {A,B,C} holder machine

  Geometry metaphor
    worldline start→halt
    CTC is anti-model
    diagram φ ≠ emission Φ

  [X]  Graphics A–D / Graphic E
  [X]  this shelf as Hands
  [X]  cubes / orbs / arrows as D types
  [X]  ASTRA as runtime
  [X]  CTC as Core
  [X]  Personal Story / Perceived Reality / Story Defense as row types
  [X]  curation of exposure as Lace ingest
  [X]  fill Φ from "signal arrives; meaning does not"
  [X]  node-edge picture writes back
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

If a heading cannot be read off the door, `[GAP]`. Do not invent Graphic E.

---

## 3. What execute may write

Hologram only.

- Create [`docs/hologram/quiet-door-7-ascii.md`](quiet-door-7-ascii.md).
- Mark this plan EXECUTED.

Do not edit [`README.md`](README.md). The door is the object.  
Do not edit door #6 companions. Clock is CLOSED.

---

## 4. What execute must not do

- Edit the hologram door, any PNG/JPG, any HTML, or clock files.
- Claim renderer.
- Fill G2.
- Start pass 2 fields.
- Treat lookrefs as Hands.
- Open projection iter 5.
- Reopen door #6.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read `docs/hologram/README.md`. Quote headings, do not paste images.
2. Draw the spine on the hologram companion. Stop.

Success: one spine; door bytes unchanged; HTML untouched; G2 `[GAP]`.  
Failure: HTML rewritten; renderer claimed; lookrefs treated as Graphic E.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain: touches vs Hands vs renderer vs primitives |
| 4 | walk: diagnose / suggest; do not store |
| 5 | seam vs Hands, clock, frozen viewer |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Touches diagnose. Hands stores.
Lookrefs are not Graphic E.
Renderer stays unclaimed.
Door #6 stays CLOSED.
Φ stays [GAP].
```
