# Systems-manifest ASCII pass 7 plan — Update wave Pass 1 · Shadow SOURCE MACHINERY as Layer III cites

**Status:** EXECUTED on `docs/systems-manifest-ascii.md` (2026-09-18 PT). Emission `[GAP]`.  
**Series:** Pass **7** (linear executed-passes list) = **Update wave · Pass 1**. Subtitle: both names.  
**Station:** maps (catch-all for `docs/*.md` at court root). Holder: **reference-Mapper**. Keep maps **HELD**.  
**Authority:** `docs/systems-manifest.md` (Contracts I / II / Layer III; Pieces unchanged).  
**Companion:** `docs/systems-manifest-ascii.md` — companion, not a replacement.  
**Hands:** Graphics A–D. Shoe in hands.  
**Not this pass:** invent POINTER / Φ rules; fill G2; create `src/`; rewrite Passes 1–6 body; edit any `docs/clipboards/*-ascii-machinery.md` (cite-only).

---

## Why this update pass exists

Passes 1–6 already **EXECUTED** the global companion spine:

| Pass | Product (on ascii) |
|---|---|
| 1 | Spine + contract map |
| 2 | Piece board |
| 3 | Parameters / non-G2 |
| 4 | Source panels |
| 5 | Core mechanisms |
| 6 | Core-as-clock (impulse / pallet / tape / hands) |

Meanwhile Shadow shelves matured their own SOURCE MACHINERY ASCII (Pass 4 panels; several sealed through Pass 6 DIAGRAM SOUND / Order Proposal Shadow). Those shelves are **Shadow only · FORBID-as-Core-write · NON-binding**. The global spine has **no Layer III shelf map** yet that names them as outside-Core cites with a hard fence.

Without this update, a reader can mistake HOST/GPU · SHADER-LANG · HOST-NN · wasm64 · book Shadow panels for Contract I teeth, WORD/strand/store, or Φ fillers.

---

## What “update” means / does not mean

| Means (on execute) | Does **not** mean |
|---|---|
| Acknowledge Shadow SOURCE MACHINERY shelves as **Layer III / outside-Core** cites | Redraw Contract I / Graphic D / Core gearing |
| Hard fence: cite paths + roles; point at FM-* families | Import Device / Queue / dispatch / WGSL / wasm64 / MLGraph as Core ops |
| Thin Overview pointer + executed-passes header link for Pass 7 | Fill G2 / invent POINTER when / arity / adjacency |
| False-friend fence table (short) | Copy full FM tables from clipboards into the global ascii |
| Keep emission `[GAP]` | Promote Shadow Order Proposals to Core / Hands |
| Leave HCC / Cup / Water / train **outside** Core box (cite only) | Put train oil or clipboard glyphs inside I |

**Plan commit does not change** `docs/systems-manifest-ascii.md` body at all. Ascii file edit only on execute.

---

## Standing Lace law (this pass)

- graphics → systems-manifest; AGENTS.md; law-why.  
- One strand; emission `[GAP]`; no invent Φ; no `src/`.  
- Hands: Graphics A–D. Shoe in hands.  
- HCC / Cup / Water / train stay outside Core box (cite only).  
- Shadow clipboard ascii is Shadow only; **FORBID-as-Core-write**; **NON-binding**.  
- Do not edit webgpu / wgsl / webnn / wasm / book clipboard products in this pass (cite-only).  
- Do not rewrite Passes 1–6 content except thin pointers / new section / executed-passes link.

---

## Tickets (execute when human says Proceed)

### T1 — Shadow shelf inventory board

Draw a **cite-only** inventory board (paths + one-line role + one FORBID-bleed line). Do not paste panel bodies.

| Shelf | Path | Role (one line) | FORBID bleed (one line) |
|---|---|---|---|
| HOST/GPU | `docs/clipboards/webgpu-ascii-machinery.md` | WebGPU host/GPU API Shadow projection surface | GPUDevice/Queue/Buffer/Texture ≠ strand/store; submit/draw/dispatch ≠ Φ |
| SHADER-LANG | `docs/clipboards/wgsl-ascii-machinery.md` | WGSL shader-lang Shadow draw-path surface | WGSL ≠ WORD; module/entry ≠ strand/Φ; address space ≠ Lace store |
| HOST-NN | `docs/clipboards/webnn-ascii-machinery.md` | WebNN host-NN embed / projection surface | MLGraph/builder ≠ strand; MLTensor ≠ store; dispatch ≠ Φ |
| wasm64 compile-target | `docs/clipboards/wasm-spec-3-ascii-machinery.md` | Wasm Spec 3.0 compile-target ISA Shadow | linear mem/table/stack ≠ L; br/return ≠ Φ; trap ≠ cut |
| book · AgentScope | `docs/clipboards/agentscope-ascii-machinery.md` | Multi-agent / ReAG Shadow anti-model | ReAG/ISR ≠ Graphic D rows; LLM verdict ≠ Hands |
| book · Petersen–Zech | `docs/clipboards/petersen-zech-ascii-machinery.md` | Deep-learning math Shadow reading | neuron/width/depth ≠ WORD/STAR; NTK ↛ store |
| book · Xiao–Zhu | `docs/clipboards/xiao-zhu-foundations-ascii-machinery.md` | LLM pipeline Shadow anti-model of ingest | embed/KV ≠ store; next-token ≠ Core continue; tokenizer `[GAP]` |
| book · Kauffman 4ed | `docs/clipboards/kauffman-4ed-ascii-machinery.md` | Knots/physics Shadow reading | diagram ≠ rope; grab ≠ emission; YBE ↛ array law |
| book · Rowlands | `docs/clipboards/rowlands-zero-to-infinity-ascii-machinery.md` | Foundations-of-physics Shadow reading | rewrite ≠ mutate-WORD; no extra rooms; algebra ≠ Core ops |

Exact art on execute. This plan does not freeze glyph choices.

### T2 — Layer III placement (outside Contract I)

Place the inventory as **projection / host-embed / compile-target** under Layer III (or an adjacent “outside Core” cite band). Hard rules:

- Boxes sit **outside** the Contract I rectangle.  
- Arrows from I → III are **read / cite** only (no upward write).  
- Labels: host-embed · shader surface · compile-target · book Shadow — never “Core gear,” “second strand,” or “Φ rule.”  
- HCC / Cup / Water / train remain cited elsewhere outside I; this pass does not oil them into the new board.

### T3 — False-friend fence table (point at FM-* · do not copy full tables)

Short global fence. Point at shelf FM families; full tables stay on clipboards.

| Looks like Core | Is not | Point (cite shelf FM family) |
|---|---|---|
| MLGraph / MLContext / builder | strand / Graphic D | FM-Webnn* (`webnn-ascii-machinery.md`) |
| GPUBuffer / GPUTexture / bind-group | L / second store | FM-Wgpu1 · FM-Wgpu4 (`webgpu-ascii-machinery.md`) |
| GPUDevice / Adapter / Queue | Core door / cord | FM-Wgpu2 |
| WGSL / shader module / entry | WORD / Core operator / Φ | FM-Wgpu5 · FM-Wgsl* (`wgsl-ascii-machinery.md`) |
| encode / draw / dispatch / `queue.submit` | emission / Φ | FM-Wgpu3 · FM-Webnn* (dispatch) |
| wasm linear mem / table / stack | Lace store / strand | FM-W1…W3 (`wasm-spec-3-ascii-machinery.md`) |
| `br` / `return` / call / trap | POINTER when / cut / Φ | FM-W4 · FM-W8 |
| host import / canvas accept | fill emission `[GAP]` | FM-W7 · FM-Wgpu6 |
| book neuron / embed / ReAG / knot state-sum | WORD/STAR/store/history | FM-A* + book stamp legends (agentscope · petersen · xiao-zhu · kauffman · rowlands) |

Do **not** duplicate full FM tables into the global ascii.

### T4 — Executed-passes header + thin Overview pointer; keep G2 `[GAP]`

On execute only:

1. Add Pass **7** to the **Executed passes** header link list in `docs/systems-manifest-ascii.md` (once this pass is executed — not in the plan commit).  
2. Thin Overview / Layer III pointer to the new shelf board (one short paragraph or cite line).  
3. Leave **G2** (POINTER when / arity / adjacency) and emission Φ as **`[GAP]`**. No invent.

**Plan-only commit:** do not touch the ascii body. Plan file alone (+ maps.station if BASE refresh dirty).

### T5 — Sealed refuse list

Refuse on execute (and forever for this update wave Pass 1):

- No `src/`.  
- No Core gear rename (impulse/pallet/tape/hands stay Pass 6 meanings).  
- No train oil / HCC / Cup / Water inside Contract I.  
- No promoting Shadow Order Proposals to Core / Hands law.  
- No Device / Queue / dispatch / WGSL / wasm64 / MLGraph as Core ops.  
- No fill G2; no invent POINTER rules; no Φ filler from host submit or NN dispatch.  
- No rewrite of Passes 1–6 sections beyond thin pointers / new Layer III shelf section / executed-passes link.  
- No edits to any `docs/clipboards/*-ascii-machinery.md` in this pass.

---

## Execution steps (when human says Proceed)

1. `git pull --ff-only origin main` (or refresh maps BASE if clock turned).  
2. `./docs/coord/coord.sh check maps "reference-Mapper"` — must CHECK OK; keep HELD.  
3. Edit **only** `docs/systems-manifest-ascii.md`: T1 board · T2 Layer III placement · T3 false-friend fence · T4 header link + thin pointer · T5 refuse honored.  
4. Do **not** edit clipboard ascii products; do **not** create `src/`; do **not** fill G2.  
5. Mark this plan **EXECUTED** (status line).  
6. `./docs/coord/coord.sh check maps "reference-Mapper"` again.  
7. Commit. Push only if human asks. Leave maps HELD unless human releases.

**STOP** after the ascii companion update. No AGENTS.md restyle. No law-why rewrite.

---

## Steward test

- If a HOST/GPU · SHADER-LANG · HOST-NN · wasm64 · book glyph sits **inside** the Contract I box, delete it.  
- If the new board answers G2 or fills Φ, delete that answer.  
- If Device / Queue / dispatch / WGSL / MLGraph / wasm mem is drawn as a Core tooth, it failed the fence.  
- If you cannot still lace the shoe with only arrive → append → participate → continue, the update bled.

---

## Success criteria

- [x] New Layer III / outside-Core shelf inventory cites all nine paths above.  
- [x] Hard fence present: FORBID bleed + false-friend table pointing at FM-* families (no full table copy).  
- [x] Contract I / Passes 1–6 mechanism & clock art unchanged except thin pointers.  
- [x] Executed-passes header links Pass 7 once executed.  
- [x] Emission Φ and G2 remain `[GAP]`.  
- [x] No clipboard ascii edits; no `src/`; maps still HELD by reference-Mapper (unless human releases).

---

## Report shape (after execute)

Plan path + execute SHA(s); T1–T5 one-liners done; maps HELD check; ahead N; emission `[GAP]`; pushed or not.

---

## This plan commit (plan-only)

- Write this file **HELD**.  
- Include `docs/coord/stations/maps.station` if BASE was refreshed so the claim stays coherent.  
- **Do not** change `docs/systems-manifest-ascii.md`.  
- **Do not** push.
