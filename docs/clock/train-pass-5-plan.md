# Train pass 5 plan — ratio law from the clock sketch

**Status:** EXECUTED on the train clipboard. Emission `[GAP]`.  
**Station:** hologram  
**Clipboard:** `docs/clock/train-h-c-w-systems-manifest.md`  
**Cite:** `docs/clock/philosophy-map.md` ratio table + three gear clipboards.

Pass 4 audited cites and sealed `[GAP]`s. The train file still never restated the **ratios that must not slip**. Those are already in philosophy-map. Pass 5 binds them as train law. No new gear.

---

## Already done

Composition, 3-axis cells, trace, return pipes, ear, sealed board, anti-import.

Do not fill sealed cells. Do not write P0.

---

## Tickets

### T1 — Ratio register

From philosophy-map “Ratios that must not slip,” cited onto the train:

| Mesh | Ratio / law | Slip | Train name |
|---|---|---|---|
| H.RIC : H.PFC | 1:1 both turning | PFC-only | locked train |
| H.B : C | 1 impulse into the five-tooth wheel | Behavior as rewrite | πB as P4 |
| C wrap | tooth 5 drives next tooth 1 | aftermath files impact | C5 files C4 |
| W pallet : tape | 0 or 1 tick per attempt | grind counted as append | C3∧S1 counted as tick |
| tape : hands | many ticks : one cluster view | cluster stored as edges on WORD | label as Graphic D |

Philosophy-map says “5:5 wrap” on Gear C and “5:1 wrap” on the ratio table. Same recirculation (5 drives next 1). Pass 5 aliases them. Does not pick a third ratio.

### T2 — Tick definition restated

Tick = one legal append on Graphic D.  
W pallet : tape = 0 or 1 per attempt. Grind = 0. Pass 5 seats that next to legal-work = C3 ∧ Water S2|S3 ∧ no P0.

### T3 — Sealed stays sealed

G2, C4 vs S7, two next-C1 writers, Water §6. Ratios do not answer those.

---

## Execution (when you say go pass 5)

Edit only the train clipboard. Ratio register + tick restatement. STOP.

---

## Steward test

If a ratio is not in philosophy-map or a gear clipboard, it stays out.  
If a ratio is used to emit POINTER, delete it.
