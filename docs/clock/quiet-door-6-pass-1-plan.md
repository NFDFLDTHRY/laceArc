# Quiet door #6 (Clock) — Pass 1 PLAN

**Status:** EXECUTED — spine on `docs/clock/quiet-door-6-ascii.md`. Door unchanged. HTML untouched. Emission `[GAP]`.  
**Station:** hologram (HELD Grok-hologram). There is no clock station. `docs/clock/**` maps to hologram.  
**Object:** quiet door #6 — [`docs/clock/README.md`](README.md).  
**Why hologram, not kit / not renderer:** hologram OWNS `docs/clock/**`. The companion can live next to the door. Renderer owns three HTML viewers. This campaign does not claim renderer and does not rewrite HTML.  
**Not:** Core. Not Graphic D. Not a new extract. Not history pass 8. Not door #5 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the clock door. Do not rewrite HTML. Do not claim renderer.

```
pull/ff → refresh hologram
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/clock/quiet-door-6-ascii.md spine
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the clock door be one ASCII spine of what it names (Layer III; shadow prose; H/C/W/train manifests; viewer that shows and does not decide; provenance in `passes/`; things deliberately elsewhere) and what it refuses (Core, Graphic D, cubes as WORD, `src/`) — without becoming a viewer or a fifth graphic?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Register row #6 | LIVE; `which` = hologram |
| H/C/W/train campaigns | bound on this shelf |
| Projection HTML | Layer III viewers; some hologram-owned, some renderer-owned |
| Renderer | FREE; HTML not this claim |

This campaign makes #6 activity WORKED, then CLOSED at pass 6 of *this door iteration*. That does not flip LIVE. That is not a new Layer III pass.

---

## 2. Spine execute will draw (shape only)

Off the clock door only. Do not paste manifests. Do not paste HTML.

```
  CLOCK DOOR
  docs/clock/README.md
  Layer III — shadow holder machine
  not Core; not Graphic D; cubes are not WORD rows
  four graphics remain Hands
  nothing on this shelf amends them

  Shadow prose
    hologram / brief / contracts / 1:1 / philosophy-map
    math model — emission [GAP]

  Gear manifests
    H driver · C event time · W clutch · train H×C×W
    OBS ≠ POINTER

  Viewer app
    shows the strand; never decides what goes on it
    lace-projection.html     hologram
    hcc-a-projection.html    renderer
    frozen viewer            renderer (elsewhere)
    PWA: rawcdn + full SHA; never main, never Pages

  Provenance
    passes/ — executed pass plans
    a plan nobody links is provenance nobody can find

  Deliberately elsewhere
    frozen viewer · shafts · hologram templates

  [X]  cubes as WORD
  [X]  this shelf as Hands
  [X]  viewer as emitter
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

If a heading cannot be read off the door, `[GAP]`. Do not invent Graphic E.

---

## 3. What execute may write

Hologram only.

- Create [`docs/clock/quiet-door-6-ascii.md`](quiet-door-6-ascii.md).
- Mark this plan EXECUTED.

Do not edit [`README.md`](README.md). The door is the object.  
Do not put this plan in `passes/` this tick (that rule is grain for a later pass; this file is a quiet-door reading, not a Layer III campaign).

---

## 4. What execute must not do

- Edit the clock door, any HTML, sw.js, manifest, or wasm.
- Claim renderer.
- Fill G2.
- Start pass 2 fields.
- Treat cubes as WORD.
- Open projection iter 5.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read `docs/clock/README.md`. Quote headings, do not paste viewers.
2. Draw the spine on the hologram companion. Stop.

Success: one spine; door bytes unchanged; HTML untouched; G2 `[GAP]`.  
Failure: HTML rewritten; renderer claimed; cubes treated as WORD.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain: hologram vs renderer vs Hands |
| 4 | mechanism: show the strand; do not write it |
| 5 | seam vs Hands, live law, frozen viewer |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Layer III shows. Hands stores.
Cubes are not WORD.
Renderer stays unclaimed.
Φ stays [GAP].
```
