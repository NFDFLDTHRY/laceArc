# HCC-A pass 2 plan (clipboard vs attachment)

**Status:** EXECUTED on the clipboard. Projection HTML not this tick. Emission `[GAP]`.  
**Station:** hologram  
**Clipboard:** `docs/clock/hcc-a-systems-manifest.md`  
**Reference:** workspace attachment `Human Cognitive Compiler Architecture.txt` (HCC-A v1.0). Do not `git add` the txt.  
**Court:** Hands = graphics + Lace systems-manifest. Attachment wins over philosophy-map where they disagree.

Pass 1 rebound the clipboard onto the attachment and added SM / P / Acts. Pass 2 is a **second walk of leftover author structure**, not a new gearbox.

---

## What pass 1 already took

H1–H10 wires named. S1 exists. L event kinds. M priorities. B/ISL input lists. P1–P7. Two arbitration axes. Six-act as process layers. Closing “only structure crosses” in the overview.

Do not redo those rows.

---

## Pass 2 tickets (attachment still thicker than clipboard)

### T1 — Split Feedback from Behavior

Source §2.7 B and §2.8 Feedback are two headings. Clipboard fused them under H10.

Pass 2: add **H10b Feedback** *or* keep one piece and state the seam explicitly (B emits; Feedback is the R-write + S7 restart). Do not invent a second output shaft.

### T2 — Act diagnostic prompts

Each Act carries **diagnostic prompts** (forensics sentences). Clipboard has Act questions + outputs only.

Lift the prompts into Acts as a field. They are holder talk, not WORD rows.

### T3 — Act I scene vocabulary

Source PFC scene types: argument, test, rejection, game, lesson, betrayal, opportunity.  
Output: perceived stakes + perceived roles.

Clipboard says “stakes + roles” and omits the scene-type list.

### T4 — YAML as field contract, not a nickname

§6 fields still not itemized on H11:

- templates: romance / conflict / authority / self / others
- pruning: perceptual_level | prefilter_level | postcompile_level = low|medium|high
- arbitration.identity_vs_reality: identity_yields | reality_yields | split
- emotion_style.volatility: low|medium|high
- emotion_style.dominant_transition: reindex | reweight | rewrite | decide | ignore
- event_trace act_I … act_VI field names

Pass 2 writes those as H11 inputs. Still `[X]` if stored as Graphic D.

### T5 — Letter collision

Source Emotion Handling **Mode D / Mode C / Mode B** is not Compiler C and not Behavior B.

Clipboard already lists decide / rewrite / reweight / ignore. Pass 2 must stamp the collision so a later agent does not fuse Mode C into H5.

### T6 — SM ↔ piece interface coherence

Pass 2 table: each S* outputs vs next piece inputs. Flag any leftover observer-language (“recognition that…”) and replace with the handed thing, or `[GAP]`.

Known `[GAP]` that stays: numeric RIC/PFC switching procedure.

### T7 — Closing as system invariants

§7 four constraints become a **System invariants** block, not only overview prose:

- assumes objective reality
- respects individual subjective compilation
- forbids meaning transfer
- restricts communication to structure

Hands: kin of “no stored meaning / only structure crosses.” Not emission.

### T8 — Do not

- Fill POINTER emission
- Add `src/`
- Git-add the attachment
- Restyle the projection HTML as this ticket
- Rewrite philosophy-map in place (note lag; Hands still graphics)
- Treat Mode C as Compiler C
- Treat YAML `person_model` as a star

---

## Execution shape (when you say go pass 2)

1. Claim hologram (already held this plan tick).
2. Edit only `docs/clock/hcc-a-systems-manifest.md`.
3. Append a **Pass 2 clipboard deltas** table under Pass 1’s table. Do not rewrite Pass 1 history.
4. STOP. Projection HTML is a later hologram tick if you want the new fields on screen.

---

## Steward test

If it cannot be pointed at in the attachment, it does not enter the clipboard.  
If it would change WORD / POINTER / STAR, it is `[X]` or `[GAP]`, not a fill.
