# Projection iteration 5 / pass 2 plan — shaders to the three lookrefs

**Status:** PLAN. Not executed. Emission `[GAP]`.  
**Station:** hologram  
**Evidence:** `i5p1` shot, `ms 64`, `GPU on`, `t=0004 PTR`. Stage is a dark tank of empty teal boxes. Authority: traces `2378.jpg`, crossing `2379.png`, lattice `2377.jpg`.

i5p1 load path stays. This pass is **pixels**. Cubes stay glyphs. D1 stays 11 rows. Do not mint extra WORDs to fill the photo.

---

## Delta (shot vs lookref)

| Lookref | i5p1 now |
|---|---|
| Cracked circuit floor, cyan traces, orange ticks, pads | Black void. Floor shader too dark to read |
| Orange molten **inside** glass, face marks as glow | Empty teal CAD shells |
| Green plasma as wispy volume | Smooth lambert orbs |
| Gold ribbon (traces) + white hub beam (crossing) | One thin cyan stick |
| Camera up, looking down a dense field | Camera in the aisle, one chord |

D1 has eleven births. The photos show dozens of cubes. **That density is floor + time-fade of born rows, not new schema.** Inventing rows is Core.

---

## Tickets (this pass only)

### T1 — Floor reads at 8.4 / 6.2

`born < 0` fragment: lift cell albedo, thicken cyan coarse traces, orange tick dots, hairline cracks. Must be obvious in the i5p1 camera, not only at the origin. No JPEG.

### T2 — Ember occupies the shell

WORD = glass rim + **filled** orange core (size ~0.42 already) that actually lights the interior. Fake face glyphs = hash/SDF marks on the glass, emissive orange/red. Not an alphabet. Not a star table.

### T3 — Plasma is a volume, not a ball

PIE sphere: displace with hash, higher fresnel, green filaments in the fragment. Same mesh. Do not add a particle system.

### T4 — PTR chord = gold traces; live tick = cyan hub

Ribbon albedo → gold/yellow (`2378`). Current `floor(t)` node gets a short vertical cyan spike (`2379` crossing) as a **view**, not Φ. One spike. Not an emitter.

### T5 — Camera looks down the field

Eye y ≈ 9, look at floor y ≈ 0.2, orbit kept. Glyphs sit on the circuit, not flying in a tunnel.

### T6 — Title `i5p2`

New rawcdn SHA. Pin `LATEST.json`. `id lace-iii` / lazy lookrefs / `ms` stamp unchanged.

---

## Done means

`i5p2` at `t=0002` and `t=0004`: floor cells visible, orange inside at least one cube, gold chord, camera down. Still 11 D1 rows. `ms` still stamped.

---

## Steward test

If you added rows to match the photo, you left the shoe. If the floor is a JPEG, T1 missed. Emission `[GAP]`.
