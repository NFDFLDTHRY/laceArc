# Coffee Cup pass 3 plan — concurrent lanes, recirculation pipe, cluster-over-time

**Status:** EXECUTED on the Cup clipboard. Emission `[GAP]`.  
**Station:** hologram  
**Clipboard:** `docs/clock/coffee-cup-systems-manifest.md`  
**Reference:** `The Coffee Cup (1).txt` (not in git)  
**Cite only:** HCC packets; Water clutch; atomic map. Do not swallow Water. Do not amend P0–P4.

Pass 1 listed teeth. Pass 2 leftover sentences + C↔π. Pass 3 is the structure pass 1–2 still treat as gloss: **one event runs three lanes at once**, **C5→C1 is a pipe**, and **labels cluster as the wheel turns**.

---

## What is already done

C1–C5 stations, packets, loop paragraph, literacy, attention, quiet end, stake-removal invariant, C3 grades, C↔π table, C4 world vs C5 priors.

Do not redo those rows.

---

## Tickets

### T1 — Three lanes as concurrent pipes

Attachment puts social / language / lived on **every** stage. Pass 1 said “do not collapse them” and then wrote one station per stage.

Pass 3: one event is **three payloads in parallel**, same C-index.

| Lane | C2 specimen (source) | C4 specimen (source) |
|---|---|---|
| social | boundary crossed or refused; silence where response expected | rupture / block / severance |
| language | first charged utterance; label; accusation; framing | final statement; declaration; label that ends dialogue |
| lived | leaving; blocking; external authority; irreversible step | door closes; path ends |

Invariant: lanes are views of one fall, not three cups. Failure: treating a language C4 as if lived C3 were still open.

### T2 — Recirculation as a pipe, not a slogan

Source: Impact feeds narrative. Narrative rewrites conditions. Conditions make the next release likely.

Pass 3 register:

```
C1 possibility ──► C2 motion ──► C3 window ──► C4 ground truth
                                              │
                                              ▼
                         next C1 ◄── C5 narrative-that-rewrites-C1
```

Payload on C5→C1 is **rewritten thresholds** (source: higher threat, lower ambiguity, preemptive hardening) — not a copy of C4. Stamp: this pipe is holder time. It is not P4. It is not HCC S7 fused into one sink.

### T3 — Cluster labels over the wheel

Hands: objects do not have relationships; relationships cluster as gears turn; labels may be created, split, or merged to name what is observed.

Pass 3 table: which Cup stage may **observe** a cluster vs **rename** a label vs **forge** a WORD.

| Stage | May | Must not |
|---|---|---|
| C1 | notice existing wraps / eyelets | call them fate |
| C3 | intervene = more cord | merge labels as if merging L |
| C4 | ground-truth the observed cluster | file Graphic D smoother |
| C5 | new/split/merge **labels** for next looking | rewrite WORD; πID as star |

Layer III labels ≠ L merge ≠ ISL snapshot. Three “merge” words, already fenced on Core. Cup pass 3 seats them on teeth.

### T4 — Dual-clock schedule (Cup vs HCC)

Pass 2 said two clocks. Pass 3 writes coexistence, not a mapping that pretends they are one:

| Cup | May overlap HCC | Not equal to |
|---|---|---|
| C2 | πB (an act) | S6 only — an act can be freeze, speech, or let-go |
| C3 | πB while Water meshed | S0–S7 run *inside* a holder during a window |
| C4 | πFB | S7 world sink |
| C5 | πID | S7 identity sink |

Do not invent a scheduler that orders C4 and C5 against S7. Attachment is silent. `[GAP]` as procedure.

### T5 — Sealed

- Water clipboard (next gear, not this pass)
- POINTER from release/impact
- Cup stages as array enums
- Git-add the txt
- Philosophy-map rewrite in place

---

## Execution (when you say go pass 3)

Edit only `docs/clock/coffee-cup-systems-manifest.md`. Add T1–T4 tables. Pass 3 deltas under Pass 2. STOP.

---

## Steward test

If a lane example is not in the attachment, it does not enter.  
If cluster-label merge would write P0, it is `[X]`.
