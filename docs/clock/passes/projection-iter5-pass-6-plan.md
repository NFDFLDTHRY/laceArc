# Projection iteration 5 / pass 6 plan — the lace is geometry

**Status:** EXECUTED. Emission `[GAP]`. Title `i5p6`. Cyan trace ribbons y=0.35. Orange cubes, no shell. Gold bar 0.14.  
**Station:** hologram  
**Evidence:** stamp `i5p5`, `f=6`, `n=5514`, `t=0005 WHOLE`. Three flat teal cubes, spotted green ball, navy stage. Floor quad exists and still matches **clearValue**. Gold chords absent. The lace is invisible.

Fragment-louder has failed four passes. This pass bakes the circuit and the PTR chords as **meshes**.

---

## What failed

```
  f=6  →  40×40 quad IS in the draw
  navy →  the fragment grid never reads
  teal cubes →  orange core is not what the phone shows
  no gold  →  0.08 ribbons are lost in the void
```

Lookrefs are a **laced board**: traces, pads, orange occupancies, gold/cyan links. That is the shoe in Layer III. It is not a shader trick on a quad the eye cannot grade.

---

## Tickets (this pass only)

### T1 — Circuit as ribbons, y = 0.35

Bake a lattice of cyan ribbons on xz (cell 1.2, span ±12) at **y=0.35**, `born = -1`, drawn in the floor pass. If you can see a cube you can see a trace. Keep the 40×40 quad only as a dark well under them. No JPEG.

### T2 — WORD is an orange cube. No shell this pass.

Drop the glass shell from the bake. One ember cube `s=0.48`, color `(1, 0.28, 0.04)`. If the phone still shows teal, classification is wrong — fix the color gate, do not put glass back.

### T3 — PTR is a gold bar you cannot miss

Ribbon width **0.14**, y=0.55, solid, no fade after birth. Born 2 and 4 must show at `t=0005`. That bar **is** the lace in this view.

### T4 — Title `i5p6`

New rawcdn SHA. Pin `LATEST.json`. Kill the previous tab. Stamp and **h1** both `i5p6`. Still 11 D1 rows. `id lace-iii`.

---

## Done means

`i5p6` at `t=0005`: cyan traces under the cubes, cubes orange, at least one gold bar. Navy + teal boxes = fail.

---

## Steward test

If the lace is still only “three boxes in space,” T1/T3 missed. Cubes are glyphs. Emission `[GAP]`.
