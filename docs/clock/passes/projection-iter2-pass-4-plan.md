# Projection iteration 2 / pass 4 — rawcdn SHA is the install path

**Status:** EXECUTED. Emission `[GAP]`.  
**Station:** hologram

Steward template (StrawDummy-proven):

```
https://rawcdn.githack.com/NFDFLDTHRY/<REPO>/<FULL_COMMIT_SHA>/<APP_PATH>/index.html
```

This app's file is `docs/clock/lace-projection.html` (not `index.html`).

- `rawcdn.githack.com` + full SHA = install / WebAPK origin+path  
- `raw.githack.com` + `main` = preview only  
- New commit = new URL. SW Update does not float the app onto `main`.

Kept: `<link rel="manifest">`, split icon purpose, two-row dock.
