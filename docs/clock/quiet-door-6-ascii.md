# Quiet door #6 — Clock ASCII

**Object:** `docs/clock/README.md`  
**Station:** hologram. There is no clock station.  
**Executed:** [quiet-door-6-pass-1-plan.md](quiet-door-6-pass-1-plan.md).  
**Not:** Core. Not Graphic D. Not Hands. Not door #5 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Clock door bytes untouched. HTML untouched. Renderer unclaimed.

This diagram is a reading of the clock door. Layer III shows. Hands stores. Cubes are not WORD.

---

## Spine (pass 1)

```
                         CLOCK DOOR
                         docs/clock/README.md
                         Layer III — shadow holder machine
                         not Core; not Graphic D; cubes are not WORD rows
                         four graphics remain Hands
                         nothing on this shelf amends them
                         moved here in defrag pass 3


  Shadow prose
    hologram / brief / contracts / 1:1 / philosophy-map
    math model — Hands L + Shadow H; emission [GAP]

  Gear manifests
    H driver     stations H1–H11, phases S0–S7
    C event time stages C1–C5 + loop
    W clutch     OBS ≠ POINTER
    train        H × C × W composition

  Viewer app
    shows the strand; never decides what goes on it
    lace-projection.html     hologram
    hcc-a-projection.html    renderer
    frozen viewer            renderer (elsewhere)
    PWA: rawcdn + full SHA; never main, never Pages

  Provenance
    passes/ — executed pass plans
    a plan nobody links is provenance nobody can find

  Deliberately elsewhere
    frozen viewer · shafts · hologram templates

  [X]  cubes as WORD
  [X]  this shelf as Hands
  [X]  viewer as emitter
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

Layer III shows. Hands stores. Cubes are not WORD. Renderer stays unclaimed.

---

## Fence

| On this door | Not this spine |
|---|---|
| Layer III / shadow holder | a fifth graphic |
| cubes are glyphs | cubes as WORD rows |
| viewer shows, does not decide | viewer as emitter |
| lace-projection = hologram | rewrite HTML this pass |
| hcc-a-projection = renderer | claim renderer |
| PWA = rawcdn + SHA | install off `main` |
| no `src/` | open Rust |

---

## Pass 1 deltas

| Ticket | Action |
|---|---|
| T1 | Spine drawn from `docs/clock/README.md` headings |
| T2 | Viewer named, not rewritten |
| T3 | G2 pointed, not filled |
| T4 | Door + HTML untouched; renderer unclaimed |

Queued (later passes on this door): fields; hologram vs renderer vs Hands grain; show-the-strand walk; seam vs Hands / live law / frozen viewer; receipt.
