# Quiet door #6 (Clock) — Pass 4 PLAN

**Status:** PLAN only. Not run.  
**Station:** hologram (HELD Grok-hologram).  
**Object:** Clock **walk** — one mechanism on [`quiet-door-6-ascii.md`](quiet-door-6-ascii.md).  
**Authority:** Clock door passes 1–3. Clock door as a *named door*, not bytes to edit.  
**Not:** Hands verbs. Not HTML rewrite. Not renderer claim. Not Graphic E. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`.

```
pull/ff → refresh hologram
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append walk mechanism to quiet-door-6-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What single walk does the clock door run — show the strand; do not write it — how does every Cl-block apply it, and what does it refuse versus Hands verbs, versus cubes as WORD, versus a claimed renderer?**

The clock mechanism is not arrive / through-star / continue. Those three verbs belong to Hands. This walk **projects what is already on L and does not append**.

Pass 4 is the mechanism map. It does not start the seam (pass 5).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | Cl1–Cl8 fields |
| 3 | hologram ≠ renderer ≠ Hands |

---

## 2. Operator as the clock door states it (T1)

Working sentence (the door wins; execute quotes, does not invent a Core verb):

> Read-only projection. It shows the strand; it never decides what goes on it.

Walk:

```
 reader arrives at docs/clock/README.md
   if a viewer is asked        → show L; do not write L
   if Hands is asked           → send to the four graphics
   if frozen HTML is asked     → renderer; do not claim it
   if install is asked         → rawcdn + full SHA; never main
   never: cube ⇒ WORD
   never: viewer ⇒ emitter
   never: this shelf ⇒ Hands
   never: hologram ⇒ renderer
```

Execute draws **one walk box** on the clock companion. Not on the door. Not “emit POINTER.” Not A13.

---

## 3. Application register (T2)

Every Cl-block. `New machine?` must be `no`.

Rows execute will fill:

| # | Role | What arrives | What is walked | What continues | New machine? |
|---|---|---|---|---|---|
| Cl1 door | | | | | |
| Cl2 Layer III | | | | | |
| Cl3 cubes | | | | | |
| Cl4 shadow prose | | | | | |
| Cl5 gears | | | | | |
| Cl6 viewer | | | | | |
| Cl7 PWA | | | | | |
| Cl8 provenance | | | | | |

Expected: all `New machine?` = no. Cubes stay glyphs. Renderer stays unclaimed.

---

## 4. Clock versus Hands versus renderer (T3)

```
 quiet-door-6-ascii.md       clock door              Hands
 --------------------       ----------              -----
 Layer III reading          the index               the store
 names viewers              names viewers           four graphics

 wrapper-as-Core            [X]
 Hands verbs pasted here    [X]
 cubes as WORD              [X]
 viewer writes L            [X]
 renderer claimed           [X]
 G2                         [GAP]
```

---

## 5. What execute may write

- Append “Pass 4 — walk” to [`quiet-door-6-ascii.md`](quiet-door-6-ascii.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Edit the door, HTML, Hands PNGs, or live law.
- Copy A13 onto this walk.
- Claim renderer.
- Fill G2.
- Start pass 5 seam.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read spine + grain. Quote those, not A13.
2. Draw one walk box. Fill the register. Every `New machine?` = no.
3. Draw clock vs Hands vs renderer. Stop.

Success: one walk; viewer still does not write L.  
Failure: Hands verbs here; HTML rewritten; cubes as WORD.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 5 | seam vs Hands, live law, frozen viewer |
| 6 | receipt |

---

## 8. Governing check

```text
One pass, one act.
Layer III shows. Hands stores.
Cubes are not WORD.
Renderer stays unclaimed.
Φ stays [GAP].
```
