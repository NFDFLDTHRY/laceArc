# Train pass 6 plan — what’s missing

**Status:** EXECUTED. Stop stamp + two pointers. Sealed board untouched. Emission `[GAP]`.  
**Station:** hologram  
**Clipboard:** `docs/clock/train-h-c-w-systems-manifest.md`

---

## Honest remainder

**On the train file:** composition is done. Namespace, legal work, meshes, 3-axis cells, trace, return pipes, ear, audit, ratios. There is no sixth gear hiding in the attachments.

**Missing and must stay missing (sealed board):**

| Open | Why it is not pass-6 work |
|---|---|
| POINTER when / arity / adjacency | Hands G2 |
| C4 vs HCC S7 order | both attachments silent |
| Two writers into next C1 | no merge rule in sources |
| Water §6 tolerances | source left them unpinned |
| Empty L / numeric P4 | HCC internals |

Filling any of those would be invention.

**Missing off this file (pointers, not oil):**

| File | Gap |
|---|---|
| `docs/clock/philosophy-map.md` | still the sketch; does not point at the train clipboard |
| `docs/clock/system-mathematical-model.md` | Hands L + Shadow H=A×B×C; no cite of train-h-c-w |
| `docs/systems-manifest.md` | law station — **do not edit** this pass |

---

## Tickets

### T1 — Stop rule on the train clipboard

Stamp: no further *composition* passes against H×C×W unless a new remainder is pointed at. Sealed board is the remainder.

### T2 — Philosophy-map pointer (hologram)

One paragraph: train law lives at `docs/clock/train-h-c-w-systems-manifest.md`. Do not rewrite the map.

### T3 — Math-model pointer (hologram)

One cite in `docs/clock/system-mathematical-model.md`. Dual-universe algebra is not replaced by the train.

### T4 — Forbidden

Fill G2. Scheduler. Law-station Core manifest. Renderer pixels.

---

## Execution (when you say go pass 6)

Train clipboard T1. Philosophy-map T2. Math-model T3. STOP.

---

## Steward test

If pass 6 adds a wire, it failed.  
If it answers a sealed cell, delete the answer.
