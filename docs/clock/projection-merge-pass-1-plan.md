# Projection merge pass 1 — one Layer III shell

**Status:** EXECUTED as a first shell, not a finished visual. Emission `[GAP]`.  
**Station:** hologram (new `docs/clock/lace-projection.html`). Renderer HTML not edited this tick.  
**Map against:** `docs/systems-manifest-ascii.md`

---

## Why merge

Two live pages + one unbuilt D4 view. Docs moved; pixels did not. Visual sanity = one camera language, one layer law, no third store.

## Law of the merge

```
 Contract I   Graphic D tape     (D1 eleven rows only this pass)
 Contract II  arrival            (not a text box that emits)
 Layer III    this page          (read-only)
 holder       H×C×W              OUTSIDE the Core box
 G2           [GAP]              no emitter
```

Forbidden: type → new POINTER; fuse HCC cubes into WORD rows; CDN; rewrite shadow-clock-gearing.html without renderer claim.

## Pass 1 product

`docs/clock/lace-projection.html`

- HUD quotes the Core ASCII spine  
- Mode CORE: D1 tape as given data  
- Mode HOLDER: doors to existing clocks (not deleted; not fused)  
- Mode ASCII: the spine text on-page  

Later passes: restyle holder pages to the same tokens; optional fetch of clipboards; still no emitter.
