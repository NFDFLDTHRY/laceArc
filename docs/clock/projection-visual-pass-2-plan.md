# Projection visual pass 2 plan — leave the plastic blocks

**Status:** EXECUTED on `docs/clock/lace-projection.html`. Emission `[GAP]`.  
**Station:** hologram  
**Page:** `docs/clock/lace-projection.html`  
**Evidence:** mobile shot — WebGPU live, time plays, stage is toy cubes on a dark void.

Pass 1 killed the black rectangle. Pass 2 is **look language**, not a new dataset.

---

## Gap vs templates

| Template | Shot now |
|---|---|
| glass cube, ember core, glyph faces | opaque brown / blue boxes |
| green filament orb (volume) | one smooth plastic sphere |
| cyan traces on a lit lattice floor | two-segment tubes + a few floor lines |
| stacked density, hot points | 11 sparse toys |
| crossing pillar / cracked traces | none |

Data stays D1 eleven rows. Density is a **shader / floor / material** problem, not “add more WORDs.”

---

## Tickets

### T1 — Materials

WORD glyph: darker glass shell + orange core (two scales already exist; make the shell translucent).  
PTR glyph: smaller cyan glass, not a solid brick.  
PIE star: noisy / layered sphere, not a billiard ball.

### T2 — Floor as lattice

Replace the sparse line grid with a dense circuit floor (emissive crossings, not a CAD plane). Still a view. Not Graphic D.

### T3 — Traces

POINTER paths as thin glowing polylines that sit *on* the lattice, not flying plumber pipe. Height only where D6 participation needs lift.

### T4 — Camera / time

Keep orbit + t=0000…0010. Ease appear. Do not add rows.

### T5 — Sealed

No emitter. No CDN. Cubes remain glyphs. Photoreal match of the stills is pass 3+ if T1–T3 land.

---

## Execution (when you say go pass 2)

Edit `docs/clock/lace-projection.html` shaders + floor + trace geom. STOP.

---

## Steward test

If it still reads as Minecraft blocks on a void, T1–T2 failed.  
If you add invented indices to fill the floor, you left the shoe.
