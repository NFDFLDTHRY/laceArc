# Systems-manifest ASCII pass 6 plan — Core as clock, not a second gearbox

**Status:** EXECUTED on `docs/systems-manifest-ascii.md`. Emission `[GAP]`.  
**Station:** law  
**Authority:** `docs/systems-manifest.md` growth dynamics + Piece 2 + Layer III  
**Companion:** `docs/systems-manifest-ascii.md`  
**Do not import:** HCC S*, Cup C*, Water F* into Contract I.

The holder train is already a clock (`docs/clock/train-h-c-w-systems-manifest.md`). Pass 6 asks what **Graphic D** is when you insist on gear language. Answer: the **tape and the tick**, not a third going-train inside I.

---

## Clock reading that is legal on I

```
 impulse     arriving input          (II, thin)
 pallet      append section          (one tick recorded on L)
 tape        Graphic D               (history only grows)
 hands       Layer III / stars       (what you see after many ticks)
```

Ratios that already exist and must not be restated as new Core teeth:

| Mesh | Law | Slip |
|---|---|---|
| arrival : tape | growth; every arrival extends L | batch “build a graph” |
| POINTER : earlier index | backward only | forward / external ref |
| many ticks : one star view | star is passes | star table beside L |
| tape : hands | projection | hands write the tape |

W pallet : tape = 0 or 1 is **train** law (grind vs tick). It is not a Piece-16. Cite the train file outside the Core box.

---

## Tickets

### T1 — Core going-train (ASCII)

Draw impulse / pallet / tape / hands using only I/II/III names. No RIC, no C3, no OBS inside the boxes.

### T2 — What turns what

Arrival turns append. Append turns indices. Repeated variables turn a star *view*. Views do not turn the tape.

### T3 — Slip table

Graph-as-store, definition-as-collapse, tidy rewrite, dimension-as-meaning — already diagnostic tells. Pass 6 names them as **slipped gears**.

### T4 — Two clocks, one workpiece

One line: holder train times the steward; Core clock counts appends. They mesh at “legal work” only on the train clipboard. This file does not redraw that mesh.

### T5 — Sealed

G2 (when the pallet also writes POINTER). Fan-out per arrival (D1 is 0,1,1,2,2 — not a Core ratio).

---

## Execution (when you say go pass 6)

Edit only `docs/systems-manifest-ascii.md`. STOP.

---

## Steward test

If a Core gear is named HCC or Water, delete it.  
If the dial writes L, you left the shoe.
