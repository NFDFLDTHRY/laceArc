# Verification iteration 5, pass 3 — findings

**Status:** record. **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Method:** [the pass 3 plan](verification-iter5-pass-3-plan.md). **Applied to [the register](../namespace-register.md) only. No other file was touched.**

---

## 1. The falsifier fired on the half that mattered

The plan said `Φ` *"changed kind"* when the prefix-device campaign defined it. **Dated at source, the split is a day older than that campaign.**

| Term | First in the tree | By |
|---|---|---|
| `Φ scheduler` | **18 Sep 17:06** | Rowlands pass 5, in a FORBID clause |
| **`Φ-schedule`** | **18 Sep 17:58** | AgentScope pass 6 — *"no Core/AGENTS/Φ-schedule overclaim"* |
| `Φ-morphism` | 19 Sep 19:04 | prefix-device pass 5 |

**So the campaign did not redefine a token under the tree.** It named **the other half** of a distinction two clipboard campaigns were already using. The tree had a word for the part that stays `[GAP]` and none for the part that could be stated — **which is exactly why bare `Φ` kept standing for the whole.**

**The thesis was wrong about the cause and right about the effect.** Recorded that way rather than quietly reshaped.

## 2. The effect, measured

| | |
|---|---|
| Files carrying `Φ` | **244** |
| Occurrences | **2,505** |
| **Occurrences asserting `Φ` is `[GAP]`** | **~519** — `Φ [GAP]` 204 · `Φ stays [GAP]` 191 · `Φ = [GAP]` 50 · `Φ still [GAP]` 44 · `Φ / emission [GAP]` 30 |

**Every one of those ~519 is true of the schedule and false of the morphism, and a bare `Φ` cannot say which.**

**Why it is worth a row and not a rewrite:** the `src/` gate turns on emission being unanswered. `docs/pointer-emission.md` is careful — it splits the halves and says *"Not accepted"* ten times. The 244 files are not, because they were written when there was one Φ. **The register is where a token's change of state belongs**, and no dated artefact was touched.

## 3. What was registered, and what was declined

**Three families**, each read from its own defining document rather than grepped for:

| Family | Tokens | Why |
|---|---|---|
| **Emission morphism** | `Φ` · `Φ-morphism` · `Φ-schedule` | **A bare `Φ` written before 19 September 2026 means the schedule.** That sentence is the whole repair |
| **Emission open question** | `Q0`–`Q5` | Starts at `Q0`, which the manifest family has no member for |
| **Prefix-device numeral** | `I`, `II` | Collisions, sheets, ruling slots |

**One genuine collision, and it is the confusable kind:**

> **`Q1`–`Q5` — manifest open question vs emission open question.** Manifest `Q1` is *tokenization*; emission `Q1` is *when is a POINTER appended*. Manifest `Q2` is *the POINTER emission algorithm*; emission `Q2` is *what is the arity*. **Both families talk about pointers, so context does not reliably fix them.**

**One candidate collision declined.** The Roman numerals do **not** collide with the boundary stamps `[I]` `[II]` `[III]`: **two tells and both hold** — a stamp is bracketed, a prefix-device numeral is noun-prefixed and bare (`Collision I`, `Sheet II`, `slot I`). Nothing in the tree is ambiguous today. **Registered so it stays that way, and reported as a non-finding.**

**`G2` declined before the plan was written** — the fifth default-or-near-miss this campaign has checked and found correct. The prefix-device campaign cites maps' registered `Atomic gap G2` accurately from another shelf.

## 4. Headline, re-derived by the file's own method

| | before | after |
|---|---|---|
| Families | 27 | **30** |
| Distinct tokens | 264 | 268 |
| **Two-or-more owners** | **72** | **77** |

**The five are `Q1`–`Q5`, and the arithmetic is checkable rather than recalled** — which is the standard this register failed twice before iteration 4 pass 3 repaired it.

## 5. What pass 3 establishes

- **The `Φ` split predates the campaign that formalised it** by a day, in two clipboard campaigns' FORBID clauses.
- **~519 assertions that `Φ` is `[GAP]` are now half-true**, and the register carries the reading rather than 244 files being rewritten.
- **`Q1`–`Q5` is a real collision between two families that both discuss pointers** — the most confusable pair the register holds.
- **The Roman numerals are not a collision**, verified by two independent tells.
- **The register gained three families and the headline was re-derived, not recalled.**

**Referred up:** nothing new. §3's collision needs only the qualification rule the register already states.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

We thought someone had renamed a mark on the lace. They had named **the other end of it** — and the tree had been pointing at both ends with one word for a day before anyone noticed. Emission remains `[GAP]`.
