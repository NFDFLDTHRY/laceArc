# Miss-map — Pass 2 of 6 PLAN

**Status:** **`EXECUTED`** 2026-09-20 · [`miss-map-pass-2-sort.md`](miss-map-pass-2-sort.md). **Station:** maps (`Grok-maps`).
**Depends on:** [`miss-map-pass-1.md`](miss-map-pass-1.md).
**Emission:** `[GAP]`. No `src/`. Does not fill Slot II or tag D2.

Pass 1 found two NEW-MISS rows. Pass 2 **sorts**: in-calc miss vs out-with-cause vs already a \(\theta\) under another name.

---

## 0. One question

Are Slot II and D2 *Hands calculation misses*, or are they already covered by \(\theta_{\mathrm{G1}}\) / \(\theta_{\mathrm{WORD2}}\) / D1 tags under another name?

## 1. One result (on execute)

[`miss-map-pass-2-sort.md`](miss-map-pass-2-sort.md) — disposition of every pass-1 NEW-MISS and a reconfirm of OUT-WITH-CAUSE.

Marks: **IN-CALC-MISS** · **ALIAS** (already a named \(\theta\) or tag) · **OUT-WITH-CAUSE** · **SPECIMEN-MISS** (D2 walk not done).

No new parameter invented unless execute proves Slot II is not \(\theta_{\mathrm{G1}}\) and not \(\theta_{\mathrm{WORD2}}\). If it is distinct, name it as a miss, **do not assign a value**.

---

## 2. Must dispose

| Item | Test |
|---|---|
| Slot II | cluster-as-word vs G1 identity vs occurrence `0000`≠`0003` |
| D2 specimen | distinct drawing from D1? walk required? or Graphic D panel already in S1? |
| Q × \(\theta\) HALF | stays HALF until pass 5 of *this* campaign |
| Carrier/Hopper I-letters | stay NAMED-UNMAPPED |
| All pass-1 OUT-WITH-CAUSE | reconfirm or promote |

## 3. Forbidden this pass

- Fill Slot II with “cluster”
- Tag D2 rows (Hands miss walk is pass 3)
- Run G-gates
- Open `src/`
- Start pass 3

## 4. Accept pass 2 when

- Both NEW-MISS have a disposition.
- OUT-WITH-CAUSE list not silently flipped to IN-CALC.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 3 is immediately authorized.

## Steward test

Shoe in hands. Sorting a miss is not answering Slot II. **A cluster is a view. A view is not a word-row.**
