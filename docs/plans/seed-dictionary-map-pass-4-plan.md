# Seed dictionary × model — Pass 4 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `e8eb52d`).
**Depends on:** pass 3 definition-route *shape*.
**Human ruling:** the route **is** to wire the lace to each word a word is defined by. Those are our **first touches**.
**Hands:** Piece 9 route · Piece 11 Touch (“the newly created touch remains”).
**Emission:** `[GAP]`. First-touch *shape* ≠ when. No `src/`.

Pass 3 named the path. Pass 4 names the first **touches** on that path: participation into each defining-word star.

---

## 0. One question

What object is a first touch — a gloss, a Star table, or a POINTER-shaped participation that, *if written*, stays on \(L\)?

## 1. One result (on execute)

[`seed-dictionary-map-pass-4-touch.md`](seed-dictionary-map-pass-4-touch.md) — first touch vs gloss vs when.

---

## 2. Distinctions execute must keep

| Claim | Model |
|---|---|
| Route | wire \(L\) through each defining-word star |
| First touch | first Piece 11 participation on that route |
| Remains | if written, the POINTER section is permanent (Piece 11) |
| Not a gloss | Piece 9 / S6 — no definition field |
| Not a Star table | S3.1 |
| When the first touch is appended | \(\theta_{\mathrm{when}}\) free |
| Later document touches | Piece 10 / later Piece 11 — **not** first |

PIE→dessert→… is still the specimen path, still not emitted.

## 3. Forbidden this pass

- Append first-touch rows to “start the lace”
- Replace route with a stored gloss
- Fill \(\theta_{\mathrm{when}}\)
- Open `src/`
- Start pass 5 unless immediately authorized

## 4. Accept pass 4 when

- Touch / gloss / table / when each marked.
- First touch identified as L4 *shape*, not forced.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 5 is immediately authorized.

## Steward test

Shoe in hands. The first time the cord goes through dessert while defining PIE, that wrap can be felt later. **Feeling that it will be first is not writing the wrap.**
