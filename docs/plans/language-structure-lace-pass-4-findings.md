# Language structure / interaction loop — Pass 4 findings

**Status:** EXECUTED — structural retrieval / match / continuation ranking complete at one-word grain.  
**Plan:** [language-structure-lace-pass-4-plan.md](language-structure-lace-pass-4-plan.md).  
**Artifacts:** [mechanism map](language-structure-lace-pass-4-mechanism-map.md) · [retrieval court](language-structure-lace-pass-4-retrieval-court.md) · [retrieval ledger](language-structure-lace-pass-4-retrieval-ledger.md).  
**Station:** maps · `Grok-language-structure-pass4`.  
**Takeover:** human instruction to pick up stalled `ChatGPT-language-structure-pass4` after Product A.  
**Core / law / mathematical model / manifest / graphics / pointer draft:** unchanged.  
**Human acceptance:** none taken.  
**Pass 5:** not opened.

## 1. Executive result

"Best match by Star pattern" can be stated as a causal exact-suffix query cone plus empirical historical continuation modes.

Under that definition, on the frozen 20k-seed court:

> **exact structural matching predicts held-out next tokens above an order-destroyed control, and the useful prior is domain-conditioned.**

> **no resolver among R4-A..J is a universal winner. Selected scale collapses toward n=1. Cross-scale agreement abstains more than it lifts strict accuracy.**

Outcome class from the plan: **B + C**. Domain-conditioned seeds survive. Precision/coverage is a frontier, not a crown.

"Most likely Star pattern" as a multiword emitted object was **not** measured. One-word continuation is the scored court.

## 2. What "Star pattern" meant here

At the causal query boundary, a Star pattern is not a stored object. It is the endpoint-local suffix cone of the current prefix, exact historical occurrences of those suffixes in a frozen seed, and the continuation branches those occurrences actually took. SearchHandle/PATTERN notation is sufficient.

## 3. Instrument and takeover notes

Phase A: constructed `ANSWERS ONE QUESTION ONLY` stream produced a tied WHEN/WHAT mode set at N=5. C-DICT 20k vs C-SHUFFLE 20k under G1-B: n3 recurrent fraction 0.0953 vs 0.0075; n4 0.0358 vs 0.0004. Same direction as Pass 3.

Slice reconstruction reached 21,002 C-HEAD G1-B tokens at 7,100 entries, not Pass 3's published 9,444. Pins matched. Cardinality difference does not license a new tokenizer.

## 4. Held-out prediction, G1-B, R4-A one-word

| seed | Alice-final strict | OEWN-final strict | Frank-local strict |
|---|---:|---:|---:|
| C-DICT | 0.030 | 0.053 | 0.029 |
| C-SHUFFLE | 0.010 | 0.021 | 0.007 |
| C-HEAD | 0.010 | 0.014 | 0.012 |
| C-ENTRY | 0.033 | 0.076 | 0.035 |
| C-PROSE | 0.057 | 0.029 | 0.091 |

Coverage is high when the seed shares later vocabulary (C-PROSE × Alice 0.824, C-PROSE × Frank 0.883, C-DICT × Alice 0.732) and lower when it does not (C-HEAD; C-PROSE × OEWN 0.577).

## 5. Hypothesis dispositions

**H4-A SPLIT.** Sequence vs shuffle SUPPORTED (Alice-final R4-A 0.030 vs 0.010). Multi-scale resolver NOT a winner: mean selected n is 1.00 for R4-A/D and 1.18–1.49 for longest-suffix variants. F4-1 fired.

**H4-B WEAK.** R4-B/C do not lift precision over R4-A at this budget. R4-G at Alice t=10001 selected singleton n=2 `PARCEL` (N=1) against target `AND`. F4-2 fired.

**H4-C SUPPORTED.** R4-D/F ≈ R4-A. Witness t=10000 anchor `OF` target `IT` selected `A` N=783.

**H4-D WEAK as accuracy, real as abstention.** C-DICT × Alice-final R4-H coverage 0.622 vs R4-A 0.732; strict 0.028 vs 0.030.

**H4-E SUPPORTED bounded.** Dictionary seeds beat prose on held-out OEWN; prose beats dictionary on Alice and Frankenstein. Open #15 OPEN. C-ENTRY is a different 20k window, not a pure order control.

**H4-F SUPPORTED.** Frank-local × C-PROSE 0.091 vs C-DICT 0.029.

**H4-G ROBUST-TESTED** for unigram collapse, shuffle penalty, domain split, no unique crown. Global G1 OPEN.

**H4-H SUPPORTED** at Lab notation scope. No search index stored.

## 6. Witnesses

Seed C-DICT 20k, G1-B, Alice-final, fixed seed.

1. t=10000 `OF`→ selected `A` (N=783), target `IT`.
2. t=10001 `IT`→ R4-G `PARCEL` n=2 N=1, target `AND`.
3. t=10001 R4-H UNRESOLVED while R4-A selected `TO`.
4. Shuffle: same window R4-A 0.030 vs 0.010, coverage both 0.732.
5. C-HEAD Alice-final R4-A 0.010 / cov 0.574.
6. D1 later PIE@0003 RootTouches 0000. Not assigned to OEWN/Alice rows.

## 7. Falsifiers

F4-1 FIRED. F4-2 FIRED. F4-3 PARTIAL. F4-4 did not fire (shuffle loses). F4-5 through F4-12 did not fire.

## 8. What Pass 5 may consume

Query cone, selected/tied roots, UNRESOLVED/NO_MATCH/TIED, replay witnesses, resolver name. Not a crowned resolver/seed, not a scored multiword pattern, not RM-A, not a response string.

## 9. Gaps

G4 now has a measured exact-match court. G1/G3/RM-A unchanged. Multiword matrix NOT_PERFORMED. Pass 5 not opened.

## 10. Final sentence

The lace can be asked where it has been before. On these seeds the honest answer is usually: at this one word, many times, heading many ways. Longer exact shapes rarely have a seed continuation. Agreement across scales more often says it will not pick than that it knows. A dictionary helps later dictionary-shaped text. Nearby prose helps later prose. Destroying order hurts. None of that writes a POINTER, and none of it is a spoken reply.
