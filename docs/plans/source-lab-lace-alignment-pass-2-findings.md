# SOURCE → LAB → LACE ALIGNMENT — Restart Pass 2 findings

**Status:** EXECUTED 2026-09-21.  
**Campaign:** Source → Lab → Lace Alignment — restart.  
**Pass:** 2 — TRACE THE ARROWS.  
**Plan:** [source-lab-lace-alignment-pass-2-plan.md](source-lab-lace-alignment-pass-2-plan.md).  
**Trace artifact:** [source-lab-lace-alignment-pass-2-traceability.md](source-lab-lace-alignment-pass-2-traceability.md).  
**Execution baseline:** `444bcfad79ee5e53208b75ed2d31ce62a247aa61`.  
**Final semantic commit:** `1a17a44880d0ff01ec53f4388b34c23229ba74aa` — global ASCII Pass 34.  
**Implementation:** none.  
**Acceptance:** absent.  
**RM-A:** remains open / MISSING-A.

## 1. Result

Pass 1 aligned where repository surfaces belong.

Pass 2 established whether the arrows are operationally pointable:

```
source
  -> clipboard/provenance
  -> faithful Lab representation OR named representation debt
  -> experiment/derivation OR named first missing edge
  -> bounded result
  -> exact Lace consumer
  -> platform feasibility obligation when implementation-relevant
```

### Architecture-flow verdict

**TRACEABLE WITH DEBT.**

### Experiment-coverage verdict

**BOUNDED COVERAGE / NAMED DEBT.**

No inspected candidate-bearing result is floating without provenance or a Lace consumer. Not all source machinery is experimentally covered, and Pass 2 says so explicitly rather than converting instrument absence into source failure.

---

## 2. Lab reproduction

The execution environment had no outbound DNS for a Git clone. Exact committed tool/fixture bytes were therefore read through the connected GitHub source at the pinned revision and materialized locally.

Blob identity was verified with local Git blob hashing before execution:

| Product | GitHub blob | Local blob | Result |
|---|---|---|---|
| candidate runner | `7586d39ad56381b07cacdbe687e29aba984f1deb` | same | PASS |
| retention harness | `20c73806cc8e97041d545abb0a67e3b73504fcfe` | same | PASS |
| D1 fixture | `0a3e7e2cde0c87e574634b3da934ddebe5ec389f` | same | PASS |

### Candidate runner reproduced

- behavioral Pointability, RIC, F5 Reject, F3 Pair, I-A2: **0/6** derived;
- RootTouch: **1/6**, row 0004;
- AdjacentPairs: proposes all six expected pointer indices but **diverges at row 0004**.

No candidate was elected.

### Retention harness reproduced

- RootTouch: TP1/FN5, replay PASS, HUMAN calibration;
- WeightByRepetition: replay PASS, **LAB-PROXY**, no retain event;
- AftermathRecirculation: **34 discovered, 0 retained**, replay PASS;
- CheatingMemory: **replay FAIL determinism@0000**, REJECT;
- SidecarMemory: **TP6/FN0, replay PASS, ORACLE-CHEAT, REJECT**.

The strongest instrument result remains:

```
perfect D1 score
    != replay provenance
    != architecture authority
```

---

## 3. Behavioral machinery

Pass 2 preserves the distinction between source machinery and a Lace retention rule.

### Complete/pointable traces

- Water I-A1 Pointability: source → runner → 0/6 → admission discipline, not RM-A.
- Water F5 Reject: source → runner → 0/6 → admission/rejection behavior, not RM-A.
- HCC-A RIC: source → runner → 0/6 → input fidelity, not a relation-retention rule.
- Coffee Cup Aftermath/Loop: source → retention harness → 34 discovered / 0 retained → RM-A source silence at the exact retain edge.
- RootTouch: HUMAN H1/H4 → both instruments → bounded special-case calibration.

### Named debt rather than fake verdicts

- Water I-A2 requires two OBS inventories: **BLOCKED-INPUT** in one-strand D1.
- Water F3 Pair is a constructor with no source operand-selection path: **BLOCKED-INPUT / BLOCKED-ADAPTER**.
- HCC-A Story Ledger deletion/rewrite: **AUTHORITY-BLOCKED** as Core.
- HCC-A weights: source machinery exists, but no lawful Lace weight computation exists; A5 count remains LAB-PROXY.
- HCC-A pruning: sites/levels are not an executable retain predicate: **BLOCKED-OUTPUT**.
- broader HCC/Water/Cup state machines remain **SOURCE-ONLY / NOT-YET-INSTRUMENTED** unless a faithful behavioral hypothesis is explicitly posed.

This is experiment debt, not evidence that the behavioral sources are useless.

---

## 4. Research machinery

Research remains rich in selection/retention-shaped machinery but does not currently supply the missing Lace-domain correspondence.

- Kauffman hitch/capstan: real physical stability machinery; **BLOCKED-INPUT / BLOCKED-ADAPTER / wrong output** for RM-A.
- Rowlands nilpotency: real source validity predicate; **BLOCKED-ADAPTER** from sampled Lace evidence.
- Xiao argmax: real selector; needs learned/model state and outputs a token, not a retained relation: **BLOCKED-INPUT / AUTHORITY-BLOCKED / BLOCKED-OUTPUT**.
- Petersen–Zech interpolation/non-identification: **TRACE-COMPLETE as a falsifier**, not a retain rule. It prevents finite D1 fit from being called mechanism identification.
- AgentScope Stage 2: evidence-backed retention exists in its native domain but lacks a lawful Lace evidence adapter.
- AgentScope Stage 3: **PREFIX-BLOCKED** because decisive selection uses downstream/full-trajectory outcome.

No research candidate was smuggled into the retention harness.

---

## 5. RM-A / RM-B / RM-C

### RM-A

```
sampling-derived structural evidence
        |
        X MISSING-A
        |
authority-backed RETAIN decision
```

Pass 2 finds no new edge and creates none.

### RM-B

The existing bounded closure remains trace-complete:

```
selected finite contiguous sample
  -> source positions
  -> existing ordered WORD rows
  -> L-M19 repeated Join
  -> one lawful addressable representative
```

No claim is made for arbitrary pattern classes/ridges/cross-level structures.

### RM-C

Trace-complete classes:
- RootTouch: `(new occurrence, root)`;
- contiguous sample grounding: intrinsic sample order.

Other candidate classes remain candidate-specific/open.

---

## 6. Platform feasibility

Pass 2 explicitly keeps platform reality downstream of semantics.

### wasm64 / Rust

Current evidence supports a planned wasm64 target/toolchain route, but:
- practical memory64 scale on the Pixel remains UNESTABLISHED;
- P-64a/b/c remain NOT-RUN;
- actual wasm64 build remains NOT-RUN;
- implementation supported-domain proof therefore remains open.

### Cargo build-std

Source-mapped:
- core-only crate list syntax;
- bare build-std implications;
- build-std-features.

Still unestablished:
- exact compiler_builtins closure for the sealed plan;
- exact lockfile representation;
- actual build/artifact.

### WebGPU / WGSL

Current role is Layer-III/view realization. Existing Pixel GPU observations are HUMAN_REPORTED bounded evidence, not family-wide proof and not RM-A semantics.

### WebNN

Remains an optional non-authoritative projection proposal. It may be absent entirely. It has no Core role and no RM-A role.

### Browser/device

Install evidence is bounded. Production isolation/offline/shared-memory composition remains conditional/NOT-RUN where recorded.

A failed realization path does not automatically falsify the semantic mechanism.

---

## 7. Experiment debt

Pass 2 publishes debt in named classes rather than inventing new machinery:

- SOURCE-ONLY;
- NOT-YET-INSTRUMENTED;
- lawful adapter missing;
- input unavailable;
- wrong output domain;
- authority-forbidden state;
- prefix/future violation;
- platform evidence missing;
- specimen coverage too narrow;
- no current Lace consumer.

The full register is in the traceability artifact.

---

## 8. Global ASCII Pass 34

Pass 34 is the final semantic edit.

It preserves:
- Pass 32 as canonical mechanism catalog;
- Pass 33 as vertical authority/context overlay;
- systems-manifest as contract registry.

It adds:
- edge-state model;
- byte-exact Lab reproduction;
- source-family trace outcomes;
- experiment-debt taxonomy;
- RM face trace;
- platform feasibility boundary;
- Pass-2 verdicts.

No Core mechanism or authority class was created.

---

## 9. Station disposition

### Read-only / KEEP

No semantic edit was required in:
- behavioral-goal clock/source clipboards;
- research/platform clipboard bodies;
- law;
- prompt/context entrance;
- kit controls.

Pass 1 had already aligned their authority story, and Pass-2 reproduction did not contradict it.

### Maps

Pass-2 authored:
- operational traceability register;
- global ASCII Pass 34;
- findings/index/receipt.

No gearing shaft was required.

---

## 10. Falsifier board

| Falsifier | Result |
|---|---|
| orphan source mechanism silently treated as architecture | PASS |
| LAB-PROXY laundered as source | PASS |
| unrun candidate called tested | PASS |
| instrument incapacity called source failure | PASS |
| orphan Lab result called architecture | PASS |
| authority upgraded by score | PASS |
| D1 fit called mechanism identity | PASS |
| false positives hidden | PASS |
| editor adapter hidden | PASS |
| platform supplies semantics | PASS |
| platform evidence states collapsed | PASS |
| hardware ignored on implementation-relevant path | PASS |
| failed realization path called semantic refutation | PASS |
| bounded result universalized | PASS |
| RM-A design begins | PASS |
| implementation gate moves | PASS |

---

## 11. Completion board

| Gate | Result |
|---|---|
| G1 current main/tree/stations pinned | PASS |
| G2 traceability artifact created | PASS |
| G3 behavioral candidate-bearing machinery traced | PASS |
| G4 research candidate-bearing machinery traced | PASS |
| G5 Lab proxies/controls traced separately | PASS |
| G6 human/derived calibration machinery traced | PASS |
| G7 every executed Lab result names a Lace consumer | PASS |
| G8 every untested mechanism has named debt | PASS |
| G9 instrument incapacity not converted into source verdict | PASS |
| G10 candidate runner reproduced | **PASS, byte-exact** |
| G11 retention harness reproduced | **PASS, byte-exact** |
| G12 D1 oracle/provenance controls preserved | PASS |
| G13 platform feasibility obligations mapped | PASS |
| G14 platform evidence strength preserved | PASS |
| G15 no platform fact becomes Lace semantics | PASS |
| G16 RM-A remains un-designed | PASS |
| G17 RM-B/RM-C scopes remain bounded | PASS |
| G18 Pass 32 and Pass 33 preserved | PASS |
| G19 global ASCII changed last | PASS |
| G20 companion delta is linear Pass 34 | PASS |
| G21 experiment-debt register present | PASS |
| G22 all claims released | **PENDING final maps release** |

---

## 12. Verdict

```
ARCHITECTURE FLOW:
    TRACEABLE WITH DEBT

EXPERIMENT COVERAGE:
    BOUNDED COVERAGE / NAMED DEBT
```

The repository does not currently suffer from an unpointable “mystery machine” problem at the inspected candidate-bearing edges.

Its actual frontier is narrower:
- RM-A is one explicit missing semantic edge;
- some behavioral machinery has not yet been faithfully instrumented;
- research candidates stop at named cross-domain boundaries;
- future physical realization still carries specific platform debt.

Pass 2 creates no new candidate to soothe any of those debts.

**STOP. Pass 3 is not opened automatically.**
