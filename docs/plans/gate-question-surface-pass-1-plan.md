# The gate's question surface — Pass 1 PLAN · the census

**Status:** **`EXECUTED`** 2026-09-21 → [`gate-question-surface-pass-1-census.md`](gate-question-surface-pass-1-census.md).
**`F2` and `F3` fired.** `F3` nine times: **nine of the manifest's eleven open questions are absent from the law file's question table**, five of them `[GAP]`-marked. `F2` once, on **arity**. And the census corrected its own planner — this plan said the manifest carried **18** open questions; it carries **11**.
**Station:** maps (`Claude-Projection` · BASE `94517a2`). **Law station untouched: `pointer-emission.md` and `systems-manifest.md` are byte-unchanged by this pass.**
**Emission:** `[GAP]`. No `src/`.

---

## 0. Subject

**The thing the human is asked to accept is [`pointer-emission.md`](../pointer-emission.md). It does not state the whole of what is open.**

Four inventories bear on the gate and live in three files:

| Inventory | Where |
|---|---|
| "Current question table" | `pointer-emission.md` — **the file acceptance would cover** |
| numbered open questions | `systems-manifest.md` |
| *"Open — the author decides, and the graphics cannot help"* | `graphics-close-reading.md` §6 |
| the historical Pass-7 question surface | `pointer-emission.md`, retained as dated |

**The law file's only mention of the manifest is its Authority line** — *"Four graphics … and `docs/systems-manifest.md` derived from them."* It never points at the manifest's open-question list.

## 1. Scope

**A census and nothing else.** One table: every question that bears on the gate → where it lives → its status there → its evidence → whether the sources agree.

## 2. Thesis

*The gate's real question surface is larger than the file that gates it, and the difference is not noise — it contains at least one live open question and at least one question already settled by the picture.*

## 3. Falsifiers

| | Fired? |
|---|---|
| **F1** — the four sources deduplicate to the law table, statuses agree, nothing missing → thesis wrong, campaign shrinks to pass 1 | **no** |
| **F2** — a question carries **different statuses** in two sources | **yes, once** — arity |
| **F3** — a question exists in one source and not at all in another | **yes, nine times** |
| **F4** — the census needs a question no source states → *I invented it; drop it* | **no** — and one near-miss went the other way: the census caught **this plan** miscounting its own source |

## 4. This pass must not

Edit `pointer-emission.md` or `systems-manifest.md`; promote anything to `CLOSED`; fill a `[GAP]`; take a ruling; add code or `src/`. **All held.**

## 5. Accept when

Every question from all four sources appears exactly once with its home, status and evidence; every cross-source disagreement is listed whatever the count; the four falsifiers are reported; the law file is byte-unchanged. **All met — see [the census](gate-question-surface-pass-1-census.md).**

## 6. Passes 2–6, named not designed

2 — test whether D1 settles slot assignment (`ref_A` is the described subject, 6 of 6, and the **later** index in 4 of 6).
3 — whether any other question is half-closed the same way.
4 — the `F2`/`F3` findings, worked.
5 — assemble the proposal.
6 — receipt.

**The whole campaign proposes; it does not close.** Every candidate closure goes to the human as `[PROPOSAL]`, because the law file is theirs and a six-of-six pattern is evidence, not a ruling.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

**A census does not add a single fact to the tree.** It puts what is already there in one column and counts it. That is the whole of pass 1, and it was enough to find nine questions the gate file does not list and one it closes on evidence its own source says does not close it.
