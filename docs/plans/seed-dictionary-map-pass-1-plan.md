# Seed dictionary × model — Pass 1 of 6 PLAN

**Status:** **`EXECUTED`** 2026-09-20 · [`seed-dictionary-map-pass-1.md`](seed-dictionary-map-pass-1.md). **Station:** maps (`Grok-maps`).
**Human charge:** the dictionary is the lace **seed**. It is wired first. It is what later text is wired *up to*.
**Hands:** Piece 9 Dictionary Wiring · Piece 3 ingest · Piece 10 document feed.
**Emission:** `[GAP]`. Spoken “dictionary wired first” is **not** \(\Phi_{\mathrm{schedule}}\). No `src/`.

A seed is a **prefix of Arrive** (and, if Φ were known, of participation into that prefix). A dictionary is not a second store, not a lexicon API, not a solved when.

---

## 0. One question

If the first life of \(L\) is dictionary ingest, what does the partial model already force, and what still has to fire before “the future has something to attach to”?

## 1. One result (on execute)

[`seed-dictionary-map-pass-1.md`](seed-dictionary-map-pass-1.md) — seed vs document vs Φ, on the equations.

---

## 2. Distinctions execute must keep

| Claim | Hands | Must not become |
|---|---|---|
| Seed enters as WORD variables | Arrive · Slot II Hands side | intern table |
| Dictionary wiring = same governing rule on definition text | Piece 9 | a special Core mode |
| Later documents reuse the same stars | Piece 10 | a second lace |
| “Wired first” (spoken Q1) | order of *campaign ingest* | calculated schedule |
| Seed corpus shipped? | ASCII OPEN #15 | assumption that a file is on \(L\) |
| Defining pointer cluster | spoken Slot II | the WORD row itself |

Forced-legal seed life = L1 on the dictionary stream. That prefix is *something* later Arrive can name — **if** a POINTER is later written. The seed prefix alone does not write those POINTERs.

## 3. Forbidden this pass

- Fill \(\theta_{\mathrm{when}}\) because “dictionary first”
- Load or invent a word list as Core
- Treat #15 as CLOSED
- Answer Slot II
- Open `src/`
- Start pass 2

## 4. Later passes (named only)

| Pass | Job |
|---|---|
| 2 | Seed as L1-only prefix (what exists before any Φ) |
| 3 | Spoken “pay into seed” vs Hands D1 `0003` |
| 4 | Piece 9 route ≠ stored gloss |
| 5 | #15 / G1 / Hopper as ingest face |
| 6 | Soundness + STOP |

## 5. Accept pass 1 when

- Seed, wiring, document feed, spoken-first, #15 each have a row on Arrive / shape / hole / companion.
- \(\theta_{\mathrm{when}}\) still free.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 2 is immediately authorized.

## Steward test

Shoe in hands. First you thread the dictionary eyelets. **Having eyelets is not the same as grabbing them from a later sentence.**
