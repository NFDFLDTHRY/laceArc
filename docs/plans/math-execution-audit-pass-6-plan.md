# Mathematical execution model audit — pass 6 plan: propagation, namespace gate, tick, receipt, rulings

**Status:** EXECUTED — CLOSED · receipt [math-execution-audit-pass-6-findings.md](math-execution-audit-pass-6-findings.md). Campaign closed; no pass 7.  
**Station:** maps.  
**Board:** [math-execution-audit-iteration-plan.md](math-execution-audit-iteration-plan.md) · findings MA-X07 · rulings MA-H01–MA-H05.  
**Pinned:** `5ffe01156047df01e8cd0823a286b53e89082905`.  
**Editor (declared):** whoever holds `maps`. The agent that wrote the board takes no ruling.  
**Emission:** untouched.

## Question (one act)

Can the campaign close with every surface that cites the model checked against what passes 1–5 changed, the namespace register updated under its reproduction gate, one appended coherence tick, a receipt with `n/m` for every pass, and every ruling left with the human?

## Measured before proposing

- **Surfaces that cite S0–S12 or the model's results:** `AGENTS.md` (behavioral-machine court), `README.md` (current court table), `docs/README.md` (court row 44), `docs/prompts/context-pass.md` (§3 table, §9), `docs/namespace-register.md` (S0–S12 family; theorem families), `docs/systems-manifest-ascii.md` Pass 39, `docs/systems-manifest.md` (SM-X integration contracts name S9–S12), `docs/pointer-emission.md` (*Current mathematical construction*), `atomic-primitives-map.md`, the three clock clipboards' Pass-5/6 blocks. Passes 1–5 add companions, labels, lemmas, S4.7 and a receipt; they change no section number and no verdict, so most of these need nothing. The check is what this pass does; edits outside maps are handoffs.
- **Namespace.** Current headline 62 / 456 / 127 / 677 (70×2 / 37×3 / 8×4 / 7×5 / 5×6). Candidates to register after passes 1–3: the model's ruling family `H1`–`H12` (collides with the HCC clipboard's `H1`–`H11` — a real cross-shelf collision the register should route); `STAR-1`, `VIEW-1` (Pass-4 theorem family, alongside `AUTH-1 PATH-1`); audit lemma labels `L1`–`L5` only if pass 2 keeps those names (they collide with nothing registered but *L-M01*… are different literals; the rule is literal tokens). *Theorem G* is a name, not a token. Every addition changes the headline; reproduce first, then recompute.
- **Coherence log.** Last tick: the reference-audit closeout at `b87de00`. Append-only; byte-prefix check.
- **Rulings prepared on the board:** MA-H01 (what "solve" may mean beyond this campaign — the human-facing RM-A design/ruling pass), MA-H02 (promote the rulings register into law), MA-H03 (manifest cites the theorem companion), MA-H04 (shelve the satellites), MA-H05 (no new section numbers — assumed).

## Thesis / falsifier

**Thesis.** One maps claim closes the campaign: a propagation table (surface · cites · still true? · handoff if not), the namespace register updated with the headline reproduced before and after, one tick, the receipt, seven index rows, rulings listed as not taken.

**Falsifier.** A surface found stale is edited from maps though it is law/kit/hologram-owned → reverted, handed off. The namespace headline is guessed → the pass stops until reproduced. The tick rewrites an earlier tick or the header → reverted. A ruling is taken. A pass 7 is opened.

## Steps

1. Pull ff-only · `claim maps "<Identity>"`.
2. Propagation table over the surfaces listed; for each, quote the sentence that cites the model and say whether passes 1–5 changed what it relies on. Expected: no law-owned surface needs a change; `docs/README.md` row 44 may want the companion named (law handoff, MA-H03); the namespace register is maps and is updated here.
3. Namespace: reproduce 62 / 456 / 127 / 677 with the register's counting rule; add the `H1`–`H12` model-ruling family (defined in the pass-1 register), `STAR-1` / `VIEW-1` into the graphics-fidelity family, and the pass-2 lemma labels if they are literal tokens; recompute; regenerate the affected collision rows (`H1`–`H11` gains the HCC-phase owner); preserve the old headline as history.
4. Append one tick in the standing form; byte-prefix check.
5. Write `math-execution-audit-pass-6-findings.md`: verdict; per-pass `n/m`; findings register dispositioned (MA-M01–M14, MA-X01–X07); coverage board → closeout; what did not move (equations, verdicts, gates, law blobs); rulings MA-H01–H05 not taken; next board — leftovers as the next iteration's pass 1, never pass 7 (the human-facing RM-A design/ruling pass is the obvious candidate and is the human's to convene).
6. `docs/plans/README.md` rows for every campaign file not yet indexed; plan status lines → EXECUTED.
7. `check-docs` (orphans 0) · gate · commit · release. Say *proceed is the human's word* and stop.

## May write / must not

**May write (maps):** `docs/namespace-register.md` (gated) · `docs/coherence-audit-log.md` (append only) · `docs/plans/README.md` · `math-execution-audit-pass-6-findings.md` · the six plan files' status lines and the board's status line.  
**Must not:** edit a law-, kit-, hologram-, graphics- or coord-owned surface · guess the namespace headline · rewrite a tick · take a ruling · open a pass 7.

## Expected vs actual

| | Expected | Actual |
|---|---|---|
| Surfaces checked | 11 | NOT_TESTED |
| Surfaces needing a change outside maps | 0–1 (court row 44, handoff) | NOT_TESTED |
| Namespace headline before | 62 / 456 / 127 / 677 reproduced | NOT_TESTED |
| Namespace headline after | recomputed, not guessed | NOT_TESTED |
| Coherence-log prefix check | exact prefix preserved | NOT_TESTED |
| Rulings taken | 0 of 5 | NOT_TESTED |
| Human acceptance | ABSENT | — |

## Stop

Stop when the register, the tick, the receipt and the index rows are committed and the station is released. The next event is the human's: a ruling, a new convening, or nothing.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc. Putting the lace down and writing what you checked, what you tied, and what you left for the owner to decide is the last act. RM-A remains `[GAP]`.
