# Repo lag cleanup — Pass 4 clock / hologram target-court reconciliation plan

**Status:** EXECUTED — CLOCK / WASM TARGET COURT RECONCILED; HISTORICAL VIEWER EVIDENCE PRESERVED.  
**Station sequence:** maps persists this plan, then releases; hologram executes the target-court repair; maps writes closeout findings and the narrowed remainder docket.  
**Base before plan persistence:** `b589e6fa549f635e9722dcc8ec5c115eff0253f1` (repo-lag Pass 3 closeout).  
**No new theory. No Core change. No target selection. No acceptance. No implementation.**

## Question

Can a reader enter through the clock / Wasm surfaces without confusing historical viewer experiments or wasm64 source/capability evidence with an accepted Core compilation target?

## Scope

Primary live target surfaces:

- `docs/clock/wasm/README.md`
- `docs/clock/README.md`

Audit-only unless a current target contradiction is discovered:

- `docs/clock/system-mathematical-model.md`
- `docs/clock/philosophy-map.md`
- `docs/clock/shadow-clock-hologram.md`
- `docs/clock/hcc-a-mechanisms-plan.md`
- `docs/clock/passes/**`
- `docs/hologram/**`

Runtime / release files are out of scope:

- `docs/clock/LATEST.json`
- viewer HTML
- worker / SW / manifest
- WebGPU / WGSL runtime
- browser/device probe state
- release promotion

## Governing target court

Current source/Lab target evidence establishes:

- **TARGET UNDECIDED**;
- permanent logical positions do not imply native 64-bit pointers;
- total persistent Lace history does not imply all history must be simultaneously resident in Wasm memory;
- wasm64 remains a conditional Tier-3 / higher-burden candidate;
- wasm32 remains a lawful lower-burden candidate but is not proved sufficient;
- actual build / browser / Pixel evidence remains distinct from source-specified capability;
- R3 carrier/lifetime remains OPEN;
- RM-A remains OPEN / MISSING-A;
- GAP-HOST remains OPEN;
- pointer acceptance remains ABSENT;
- no `src/`.

## Required distinctions

The live clock entrance must preserve:

```
HISTORICAL VIEWER ATTEMPT
  !=
SOURCE / TOOLCHAIN CAPABILITY
  !=
TARGET CANDIDATE
  !=
TARGET SELECTION
  !=
EXECUTED BUILD / DEVICE PROOF
```

## Primary repairs

### docs/clock/wasm/README.md

Replace the live `Separate Core target` section that currently says the repository's proposed Core target is `wasm64-unknown-unknown`.

The current target section must state:

- compilation target = **TARGET UNDECIDED**;
- wasm64 is conditional on an established requirement plus actual toolchain/device proof;
- wasm32 is a lawful lower-burden candidate, not selected and not yet proved sufficient;
- historical wasm32 bake evidence remains historical;
- the current viewer remains JavaScript and reports `wasm:false`;
- no source capability or viewer behavior becomes Core execution evidence;
- pointer acceptance remains absent and implementation closed.

### docs/clock/README.md

Repair the live `wasm/` viewer row so it no longer advertises a “separate proposed wasm64 Core target.”

Narrow the broad live terminal wording `POINTER emission stays [GAP]` to the current actual boundary:
- pointer draft reconciled but unaccepted;
- RM-A retain-when OPEN / MISSING-A;
- no `src/`.

Update the `system-mathematical-model.md` index row if needed so it reflects the file's already-present historical qualification and current route to `math-execution-model.md` S0–S12.

Do not alter current HCC/Cup/Water routing or Water semantics.

## Historical evidence preservation

Preserve as dated facts:

- 2026-09-18 wasm32 libcore unavailable in that session;
- deferred `rustup target add`;
- SIMD Wasm discussed as later work;
- current viewer loads JavaScript bake and no `.wasm` module;
- current pinned ixp2 release metadata and URL state.

Historical evidence may contain `wasm32` or `wasm64` without being stale if it is explicitly tied to its time/evidence class.

## No probe / implementation expansion

Do not run or add:

- `rustup target add`;
- Cargo/rustc builds;
- wasm32 or wasm64 compilation;
- custom target JSON;
- Pixel / browser Memory64 probes;
- WebGPU or WebNN probes;
- `src/`.

Any such work requires its own evidence/probe plan.

## Post-edit sweep

Search all `docs/clock/**` for:

- `wasm64-unknown-unknown`;
- `proposed wasm64`;
- `Core target`;
- `TARGET UNDECIDED`;
- `wasm32-unknown-unknown`.

Classify each remaining hit as:

- CURRENT + CORRECT;
- HISTORICAL + CORRECT;
- SOURCE/CAPABILITY + CORRECT;
- STALE CURRENT CLAIM.

Stop only when there are zero stale-current target claims.

Also inspect `docs/hologram/**` for hidden target-selection language.

## Cross-station remainder

Expected survivors after this pass:

- maps: `docs/namespace-register.md`;
- maps: `docs/coherence-audit-log.md`;
- coord: `docs/coord/stations/README.md` clipboard-station description.

Record only; do not consume them from hologram.

## Falsifiers

F1 historical wasm32 bake evidence rewritten away.  
F2 wasm64 capability evidence deleted because target is undecided.  
F3 wasm64 remains presented as selected/proposed current Core target.  
F4 wasm32 silently becomes selected instead.  
F5 TARGET UNDECIDED weakened to “probably wasm32/64.”  
F6 logical index width equated with native pointer width.  
F7 total Lace history equated with resident Wasm memory.  
F8 S11 technical realization treated as semantic authority.  
F9 viewer behavior treated as Core execution evidence.  
F10 source capability treated as device execution.  
F11 release pin changed.  
F12 viewer/worker/SW changed.  
F13 toolchain/build probe run without a probe plan.  
F14 pointer contract treated as accepted.  
F15 RM-A closed.  
F16 R3 closed.  
F17 GAP-HOST closed.  
F18 historical clock pass records rewritten.  
F19 system-math historical body modernized.  
F20 foreign-station backlog consumed.  
F21 `src/` or implementation opened.

## Completion gates

G1 plan persisted before execution.  
G2 hologram claimed from current main.  
G3 all live `docs/clock` target claims inventoried.  
G4 Wasm README says TARGET UNDECIDED.  
G5 Wasm README keeps wasm64 conditional.  
G6 Wasm README keeps wasm32 candidate/non-selected.  
G7 historical wasm32 attempt preserved.  
G8 JavaScript viewer path preserved.  
G9 source capability != execution distinction preserved.  
G10 clock README target row reconciled.  
G11 clock README broad emission wording narrowed.  
G12 system-math index row reflects historical status.  
G13 current S9–S12 route preserved.  
G14 current Water/source-device routing untouched.  
G15 no viewer/runtime/release files changed.  
G16 LATEST pin untouched.  
G17 no build/device probe performed.  
G18 pointer acceptance remains absent.  
G19 RM-A/G1/R3/GAP-HOST unchanged.  
G20 post-edit target sweep has zero stale-current hits.  
G21 historical pass records unchanged.  
G22 next-backlog docket updated.

## Execution order

1. persist this plan and release maps;
2. claim hologram;
3. pin current main;
4. inventory current clock target claims;
5. repair `docs/clock/wasm/README.md`;
6. repair `docs/clock/README.md`;
7. audit historical/system/behavioral clock surfaces;
8. run target stale-phrase sweep over `docs/clock/**` and `docs/hologram/**`;
9. release hologram;
10. claim maps;
11. write Pass-4 findings + remainder docket;
12. mark plan executed and index receipt;
13. release maps.

## Stop

Do not automatically begin namespace/audit-log or coord cleanup after this pass.


## Execution closeout

**Executed 2026-09-22.** The plan was persisted before hologram execution. Only `docs/clock/wasm/README.md` and `docs/clock/README.md` were semantically edited. The former selected/proposed-wasm64 current wording was replaced by **TARGET UNDECIDED** with wasm64 conditional/high-burden and wasm32 lower-burden but unproved. The historical wasm32 bake attempt, current JavaScript `wasm:false` path, viewer/runtime/release state, and clock pass history were preserved.

The post-edit direct-file sweep found no stale current target-selection claim. GitHub code-search indexing lagged the just-written bytes, so final classification used fetched current file contents plus unchanged historical blobs. See [Pass-4 findings](repo-lag-pass-4-clock-target-findings.md).

Verdict: **CLOCK TARGET COURT CURRENT / TARGET UNDECIDED / HISTORICAL WASM EVIDENCE PRESERVED / RUNTIME AND RELEASE UNCHANGED.**
