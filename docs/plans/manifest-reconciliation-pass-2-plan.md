# MANIFEST RECONCILIATION — Pass 2 PLAN · propagate the canonical foundation

**Status:** **EXECUTED** 2026-09-21 → [findings](manifest-reconciliation-pass-2-findings.md). **Verdict: FOUNDATION SOUND WITH DATED ARTIFACTS.**  
**Campaign:** Manifest Reconciliation. **Pass:** 2.  
**Baseline:** Pass 1 canonical pair at `b0d26f98fefb755edec6243f456f9ee49a45d8e7`.  
**Primary question:** do all **current** repository consumers agree with the Pass-32 global mechanism catalog and the matching mechanism contract registry?  
**Lab:** Lab Source Architecture Pass 4 remains **PAUSED** throughout this pass.

## 0. Foundation under test

```
systems-manifest-ascii.md
    = current mechanism catalog
      L-M01 … L-M21 + L-GAP-RM

systems-manifest.md
    = current contract registry
      SM-C01 … SM-C21 + SM-GAP-RM
      SM-X-BEH · SM-X-RES · SM-X-PLAT · SM-X-L3
```

If Pass 2 discovers a defect in either canonical document, propagation stops at that defect and the canonical pair is repaired before downstream work continues.

## 1. Consumer classes

Every relevant document is classified before editing:

- **CURRENT-CANONICAL**
- **CURRENT-CONSUMER**
- **CURRENT-PROPOSAL**
- **DATED-BUT-ACTIVE-EVIDENCE**
- **HISTORICAL**
- **SUPERSEDED**

Historical files are evidence of what the repository believed then. They are not rewritten merely to make history look current.

## 2. Five foundation tests

**FND-1 — provenance**
- A6 = PIE-in-context, not the explicit definition route.
- B4 = explicit PIE → dessert → slice → whole → food definition route.
- C9 = dictionary-wiring analogue.
- A11/A12 = dictionary/document integration + definition traffic.
- B10 = no-collapse anti-model.
- B11 = projection boundary.

**FND-2 — participation semantics**
- persistence ≠ retention;
- re-entry ≠ materialization.

**FND-3 — two different 2D objects**
- Graphic-C 2D = projection/view;
- H7–H12 position×scale 2D = derived sampling working machinery.

**FND-4 — D1 evidence**
- D1 = state/shape/bounded witness;
- D1 ≠ universal selection/materialization algorithm.

**FND-5 — current open**
- exact sampling-derived retention/materialization remains `SM-GAP-RM`.

## 3. Wave A — maps/current proposals

Repair current maps/plans where Pass 1 found known drift:
- `docs/atomic-primitives-map.md`;
- `docs/plans/fixtures/README.md` and `docs/plans/hologram-ast-ir.md` so sealed Hologram IR v0.2.0 is unmistakably a dated semantic fixture, not the current mechanism map;
- `docs/plans/rust-nostd-branch-contracts.md` / crate map only where current prose still implies an arbitrary semantic selector/retention owner or collapses sampling into a view;
- `docs/plans/lab-source-architecture-pass-4-plan.md`: repair its false premise and WeightByRepetition provenance **without resuming it**;
- `docs/plans/README.md`: current routing and any malformed literal newline artifact.

Do **not** modify `hologram-ir-golden-v0.2.0.json` in place. It is sealed; semantic currency is corrected by classification, not fossil repainting.

## 4. Wave B — current control surfaces

Repair stale active framing in:
- `docs/prompts/context-pass.md`;
- `docs/kit/agent-control.md`;
- `docs/kit/READ_ME_FIRST.md`.

Current wording must identify exact sampling-derived retention/materialization as the active Core-adjacent gap. Selector-era language may remain only where explicitly historical.

## 5. Wave C — historical qualification

Preserve executed history bodies. Add a current reader qualification at `docs/history/README.md` noting the later A6/B4 source correction and directing readers to Pass 32 / the contract registry.

## 6. Read-only confirmation surfaces

Audit without editing unless a contradiction appears:
- `AGENTS.md`, `CLAUDE.md`, root `README.md`, `CONTRIBUTING.md`, `docs/README.md`;
- `docs/pointer-emission.md`, live law;
- graphics companions and close reading;
- current math model;
- current behavioral/research/platform clipboard boundaries against SM-X-*.

## 7. Falsifiers

| ID | Fires when |
|---|---|
| F1 | a current entrance still calls arbitrary non-root-touch selection the active GAP |
| F2 | a current consumer cites A6 as the explicit definition path |
| F3 | a current projection contract uses B10 as projection evidence |
| F4 | B8 persistence is promoted to general retain-when |
| F5 | H7–H12 sampling is attributed to Graphic C |
| F6 | D1 is treated as a complete selection/materialization algorithm |
| F7 | sealed Hologram IR v0.2.0 is presented as the current semantic mechanism map |
| F8 | historical files are rewritten to look retrospectively correct |
| F9 | Pass 2 invents SM-GAP-RM |
| F10 | foundation repair authorizes implementation |

## 8. Station order

`maps → prompts → kit → history → read-only audit of law/graphics/clipboards → maps receipt`.

## 9. Verdicts

Pass 2 ends with exactly one:
- **FOUNDATION SOUND**
- **FOUNDATION SOUND WITH DATED ARTIFACTS**
- **FOUNDATION NOT SOUND**

The Lab remains paused until the verdict is recorded.
