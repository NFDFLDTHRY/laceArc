# Mathematical execution model audit — pass 4 findings: the satellites and the update witness under current law

**Status:** EXECUTED — CLOSED.  
**Plan:** [math-execution-audit-pass-4-plan.md](math-execution-audit-pass-4-plan.md) · board [math-execution-audit-iteration-plan.md](math-execution-audit-iteration-plan.md) · findings MA-X01, MA-X02.  
**Proceed:** the human, 2026-09-22: *"Proceed with pass 4"*. Read as MA-H00 for pass 4 and nothing else; MA-H01–MA-H05 remain untaken, MA-H04 (shelving the satellites) in particular.  
**Pre-pass baseline:** `5ffe01156047df01e8cd0823a286b53e89082905` (`origin/main`); maps claim `559860b` above the pass-1 to pass-3 commits.  
**Products:** one dated header block in each of five files · this receipt. One maps commit, then release.  
**`git diff --numstat`: 5 added / 0 removed in each of the five files. Each file's original text is byte-identical after the inserted block is removed (hashes in §3). No model, law, manifest, pointer draft, graphic, fixture, register or `docs/README.md` change. No acceptance claimed. No retain-when written.**

## 1. Verdict

**ALL FIVE FILES NOW CARRY CURRENT LAW IN ONE DATED HEADER EACH — BINARY `Join` CLOSED, ROOTTOUCH CLOSED, Q3 CLOSED, Q5 = WORD + ROOTTOUCH, Φ HISTORICAL, RM-A THE OPEN — WITH EVERY SUPERSEDED CELL NAMED AND EVERY REFUSAL THAT STILL HOLDS SAID TO HOLD. THEIR BODIES ARE UNCHANGED.**

**THE FALSIFIERS DID NOT FIRE: NO BODY LINE CHANGED; NO HEADER SAYS THE CONSTRUCTOR IS OPEN OR RM-A CLOSED; NO HEADER CLAIMS ACCEPTANCE.**

## 2. Superseded cells, per file

Each header carries the same five current facts, then names what it supersedes and what stands. The table below is the supersession list as written.

| File | Superseded (dated history) | Stands (still the law of the file) |
|---|---|---|
| [`math-execution-model-pass-3-witness.md`](../math-execution-model-pass-3-witness.md) | Status *Pass 2 partial algebra*; W0 *audits under Pass 2 partial algebra*; W2 `Φ [GAP]` on 0002, 0004, 0006, 0007, 0009, 0010; W6 **Q2**, **Q3**, **Q5**; W8 `Φ [GAP]` → `RM-A [GAP]` | W1; W3 (Star set is a view — STAR-1); W4; W5; W6 Q1, Q4; W7 in full, W7.2 still a forbidden *D1 ⇒ arity law* inference; D1 a specimen, not a scheduler |
| [`math-execution-model-pass-4-adversarial.md`](../math-execution-model-pass-4-adversarial.md) | A0 *open Q1–Q5* (beyond the 2026-09-20 Q4 qualification); A1 C6; A4 **Q2**, **Q3**, **Q5**; A5 *W6 repeated open*; A7 `Φ [GAP]` → `RM-A [GAP]` | N01–N15 in full — N09 still refused as an inference, N08 refused and now also closed by Q3, N11–N13 unchanged; A3 P0/P1; A6; A0's law sentence (non-examples are not inverted into *emit when*) |
| [`math-execution-model-pass-5-projections.md`](../math-execution-model-pass-5-projections.md) | P6 **Q2**, **Q3**, **Q5**; P4's *whether N-D appearance changes arity*; P8 `Φ [GAP]` → `RM-A [GAP]`; Status *Pass 2 π* (now the family π_κ / Π(L)) | P1; P2–P4; P5; P6 Q1, Q4; P7 in full; P8; sharpened, not contradicted, by VIEW-1 and PATH-1 |
| [`math-execution-model-pass-6-arrival.md`](../math-execution-model-pass-6-arrival.md) | R5 **Q2**, **Q3**, **Q5**; R4 *II → Φ does not fill* (reads as: Arrival does not fill RM-A; RootTouch is Core's append); R7 `Φ [GAP]` → `RM-A [GAP]`; Status *Pass 2 Contract II* | R0 law (feed ≠ store ≠ emit ≠ projection); R1; R2; R3; R5 Q1, Q4; R6; R7; *G1 tokenization still open*; refusal now pointed from the model, never denied here |
| [`math-execution-update-pass-5-witness.md`](math-execution-update-pass-5-witness.md) | *drawings do not settle Q2* (alone); stamp cells `H-PTR empty; Witness-only` ×4, `Q0 SPOKEN + HANDS CONFLICT` (0003), `Q5 SPOKEN + HANDS CONFLICT` (0004), `required-shape still OPEN` (0007); emission-questions rows Q0, Q1, Q2, Q4, Q5; `P5-R` *Q2 picked: no*, *Next: pass 6 readiness* | the specimen; every *Calculated why? no* (RM-A); *no cell reads "emitted because"* for every row but the RootTouch; U6; *old witness edited: no* — bytes unchanged; the **Q3** row, unchanged and current |

**The one reading every header keeps.** *"Emitted because …"* is still not readable off D1 or off a picture — because retain-when (RM-A) is open, not because the constructor is. Each header says this in its own words; none qualifies a refusal away.

## 3. Falsifiers and byte receipts

| Falsifier | Check | Result |
|---|---|---|
| a body line changed | `git diff` removed lines across the five files | **0**; 5 added lines per file, all one contiguous block under the header |
| the original text survives byte-for-byte | new file with the inserted block removed, compared to `HEAD` | **identical** in all five: `5212f01909b5` · `680426e99a59` · `3c5f97bf589c` · `1f8faa5164fd` · `ba7fe3bc51de` (SHA-256 prefixes of the `HEAD` blobs) |
| a header says the constructor is open, or RM-A closed | read back | **none** — each names Q2 CLOSED at 2 and RM-A OPEN |
| a header claims acceptance | read back | **none** — each ends *not accepted; this header is not acceptance* |

Placement: under the existing header, before the steward line (witness, projections, arrival); between the 2026-09-20 qualification and *Original pass record follows* (adversarial); before *Same eleven rows as D1* (update witness). The adversarial file thus carries two dated qualifications in order, 2026-09-20 (Q4) and 2026-09-22 (the rest), neither rewriting the other.

## 4. Expected vs actual

| | Expected | Actual |
|---|---|---|
| Files with a current-law header | 5/5 (from 1 partial) | **5/5** |
| Body lines changed | 0 | **0** |
| Superseded cells named | every Q2/Q5 cell in the five files | **every Q2, Q3, Q5 cell**, plus the Φ / H-PTR / required-shape / CONFLICT cells and the stale Status lines |
| Human acceptance | ABSENT | ABSENT |

## 5. Evidence at its strength

| Claim | State |
|---|---|
| Additions only; bodies identical | OBSERVED (`git diff --numstat`; blob comparison) |
| The five current facts | as stated by the model (S1.3, S2, S3.2, S3.3, S4.1, S4.4, the current steward receipt) and the pointer draft's current question table, both re-read this pass; not re-derived |
| Each superseded cell | OBSERVED — every file was re-read line by line for Q-cells, Φ lines, arity lines and status lines before the list was written |
| Graphics | NOT_PERFORMED |

## 6. Handoffs

- **`docs/README.md` rows 45–48** describe the four satellites neutrally (*witness*, *adversarial reading*, *projection readings*, *arrival reading*) and need no change for this pass; whether the four move to a shelf is MA-H04, the human's. Untouched.
- **Pass 5:** the dated S4.7 with the RM-B / RM-C lemmas and the docket's five questions.
- **Pass 6:** the propagation table now has five more surfaces that cite the model by *Pass 2*-era section names; the headers point them at the current sections.

## 7. Beyond the plan's *May write*, and why

The pass-4 plan's Status line was flipped to executed and one row was added to [`docs/plans/README.md`](README.md), as passes 1–3 did.

## 8. Stop

Five headers and this receipt committed under one maps claim; maps released. Pass 5 needs its own claim and the human's *proceed*.

## 9. Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc. Today's date is written on the old notes, with the lines later settled named; the notes themselves are as they were. RM-A remains `[GAP]`.
