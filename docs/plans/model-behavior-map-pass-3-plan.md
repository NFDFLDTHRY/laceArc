# Model behaviors × equations — Pass 3 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `52fb895`).
**Depends on:** pass 1 B-Refuse · pass 2 L0–L5 · model S6 · WallCalc · U12Calc.
**Emission:** `[GAP]`. No `src/`. Does not fill B-When.

Pass 2 listed legal lives. Pass 3 lists **refused traces** — acts that look like work and are not `Step`.

---

## 0. One question

Which proposed acts does the partial model already reject, and which filter (Hands S6, wall, or U12) rejects them?

## 1. One result (on execute)

[`model-behavior-map-pass-3-refuse.md`](model-behavior-map-pass-3-refuse.md) — refuse catalog.

Each row: act · filter · cite · not a when.

---

## 2. Acts execute must refuse

From S6 + prior campaigns (do not invent new morals):

| Act |
|---|
| delete / rewrite / reset / cut / reuse index |
| intern / collapse occurrences |
| Star table or meaning table beside \(L\) |
| adjacency of WORDs as emit |
| forward or off-line POINTER |
| \(\pi\) write-back |
| Shadow / H×C×W / ReAG as rows |
| rustc receipt / wasm64 “opens Implementation Law” as a stitch |
| Reidemeister / slide as replacement of history |
| next-token / NTK / MASK as Arrive |
| spoken Q1 installed as `Step` |

If an act is only “not forced” (L4 POINTER), it is **not** in this catalog.

## 3. Forbidden this pass

- Treat refuse as a schedule
- Walk D1 (pass 4)
- Open `src/`
- Start pass 4

## 4. Accept pass 3 when

- Every act in §2 has a filter + cite.
- B-When still free.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 4 is immediately authorized.

## Steward test

Shoe in hands. A refused act is not a missing stitch. **No is not when.**
