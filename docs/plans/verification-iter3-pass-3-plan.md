# Verification iteration 3, pass 3 — the dating conventions, and who owns them

**Status:** `[PLAN]`. Execution NOT_RUN. **Station:** maps. Read-only on kit.
**Emission:** `[GAP]`. No `src/`. **Pinned at `93033a0`.**
**Iteration:** [iteration 3](verification-iteration-3-plan.md), pass 3 of six. Passes 1 and 2 EXECUTED.

## 0. The question has an owner, and it is not this campaign

The iteration plan set pass 3 as: *"the fourteen dating conventions — are they one convention or fourteen?"*

**Probing found a live campaign already answering it.** `docs/kit/cold-doors.md` is a steward register on the **kit** station, five passes deep, last touched **today**. It carries:

- **Five stamps** — `LIVE` routes to live law or tip · `DATED` is an honest pin · `STALE` is a pin that outlives the tree · `PAUSED` is a human halt · `SHUT`
- **A THEN/NOW rule** — *"THEN/NOW on the package door is a reading, not a retarget."*
- **Ten doors**, each with a stamp, a pin and its evidence

**This campaign does not take that question.** It has declined the atomic map, RESYNC and the audit prompt on the same grounds, and cold-doors is further along than pass 3 would have been.

## 1. What is measured, and it is about us

The convention is real and in use. Eleven documents use `THEN` as a dating device, and across the whole tree **exactly one** use is ordinary English.

**And the verification campaign uses none of it:**

| Stamp | In verification documents | Tree-wide |
|---|---|---|
| `LIVE` | **0** | 10 |
| `DATED` | **0** | 6 |
| `PAUSED` | **0** | 4 |
| `SHUT` | **0** | 4 |
| `STALE` | 1 (a quotation) | 9 |

What this campaign does instead: **9 of its 22 documents carry "Pinned at", 8 carry "measured at", and 13 carry neither.**

**So there are two dating vocabularies in this tree and this campaign wrote the second one without checking whether a first existed.**

## 2. That is the third instance of one pattern

| Instance | Where | How it was settled |
|---|---|---|
| `C01` against `C10` — two evidence vocabularies twelve clauses apart | [iteration 1 pass 3](verification-iter1-pass-3-findings.md) | the **task template** decided it, not the law |
| Seven receipts, six slots, **seven names** | [iteration 2 pass 4](verification-iter2-pass-4-findings.md) | nothing decided it |
| **cold-doors stamps against verification pins** | here | undecided |

**Every time, a second vocabulary grew because nobody was handed the first.** Twice this campaign diagnosed it in someone else's work. This is the first time it is the one holding the second vocabulary.

## 3. One overlap, to hand up once rather than twice

cold-doors' row 4 carries **the same finding as [iteration 1's D11](verification-iter1-pass-5-plan.md)**: `law-why-these-documents.md` against its own IMPLEMENTATION LAW block.

Two campaigns found it independently. **It should reach the human once.** Pass 3 names the duplication so the ruling is not requested twice with different words.

## 4. What pass 3 does

1. **Audit this campaign's own dating** against cold-doors' five stamps — which of the 22 verification documents would be `DATED`, which `LIVE`, which have quietly gone `STALE`.
2. **Propose adoption**, with the cost stated: 22 documents, a header line each, and the risk that a stamp applied to a plan means something different than a stamp applied to a door.
3. **Report the D11 overlap** so the human rules once.
4. **Coordinate, not merge.** A note that reaches cold-doors' station without editing it.

## 5. What pass 3 must not do

- **Not edit `docs/kit/cold-doors.md` or any kit file.** It is another campaign's, mid-pass-5.
- **Not invent a third vocabulary.** The whole finding is that this campaign already invented a second.
- **Not stamp 22 documents before the adoption is ruled.** §4.2 proposes; it does not apply.
- **Not raise D11 as new.** It is iteration 1's, and cold-doors found it too.
- **Not fill `[GAP]`.**

## 6. Accept pass 3 when

Every verification document has a stamp it *would* carry under cold-doors' vocabulary, with the ones already stale named; the adoption proposal states its cost and its one real risk; the D11 duplication is recorded once; and cold-doors' ownership of the convention is stated plainly enough that iteration 4 does not re-open it.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

Twice this campaign found someone keeping two lists for one thing and said so. **This time the second list is ours.** Emission remains `[GAP]`.
