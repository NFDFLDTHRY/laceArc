# Mathematical execution model - ROOT / TOUCH / FOLD campaign - Pass 5 PLAN

**Status:** **EXECUTED 2026-09-20 — PASS 5 COMPLETE.**
**Planning station:** maps (Astra-propagation).
**Pinned main before claim:** 7654c06bf72ea0f1d7ba9ae397b2461b316e7c25.
**Maps claim:** 6992dfeed3fc53b0c4c565a33cd346fb10ff2955.
**Depends on:** Pass 4 findings, reconciled pointer-emission law, reconciled systems manifest, and the Pass-4 propagation docket.
**Primary job:** propagate the smaller, current gap through all active lower-authority surfaces without reopening settled constructor mechanics or fabricating acceptance.
**Implementation:** no src/. No Cargo. No branch births.
**Acceptance:** ABSENT. Pass 5 must preserve the explicit acceptance gate.
**Current remaining gap:** arbitrary non-root-touch relation/sample selection only.

Pass 5 is the propagation campaign. It is not another mathematical derivation pass and it is not an implementation pass.

---

## 0. Current authority chain

Read in this order:

1. Graphics A-D — source corpus.
2. docs/systems-manifest.md — current derived contract map, reconciled in Pass 4.
3. docs/pointer-emission.md — current reconciled draft law surface, not accepted.
4. docs/math-execution-model.md — current mathematical construction and proofs.
5. docs/systems-manifest-ascii.md — global systems ASCII companion, currently stale.
6. graphics ASCII companions, maps, environment, reduction and Rust planning surfaces — lower-authority dependent readings.

Settled current mechanics:

\[
\mathsf{Arrive}(v)
\Rightarrow
\mathsf{WORD}(v)
\]

\[
\neg\mathrm{Seen}(v)
\Rightarrow
\text{new WORD becomes Root}(v)
\]

\[
\mathrm{Seen}(v)
\Rightarrow
\mathsf{Join}(\text{new occurrence},\mathrm{Root}(v))
\]

\[
\mathsf{Join}(a,b)
=
(\mathsf{POINTER},a,b)
\]

with two selected ordered earlier same-line operands.

For finite selected sample:

\[
P_1=s_1,
\qquad
P_{n+1}
=
\mathsf{Join}(P_n,s_{n+1}).
\]

Remaining current gap:

\[
\boxed{
\text{what upstream process selects an arbitrary non-root-touch ordered relation/sample?}
}
\]

No downstream surface may enlarge that gap back into "POINTER construction is unknown."

---

## 1. Pass 5 objective

**Make the repository's active current-facing documentation agree on the size and location of the remaining gap.**

The campaign is complete when:

- global systems ASCII carries the reconciled mechanics as Pass 29;
- Graphic B/C ASCII companions point to current law without rewriting PNG authority;
- active maps split constructor facts from the selection gap;
- environment/reduction models shrink broad sigma/schedule language to the selection residual;
- Rust planning surfaces no longer treat arity, target kind, repeated-WORD admission or POINTER construction as open premises;
- current-facing law/README entrypoints do not advertise the old broader blocker;
- historical receipts remain historical and unchanged;
- acceptance remains absent;
- no src/ exists.

---

## 2. Station choreography

Pass 5 execution is sequential. One station at a time.

### Phase A — maps: global systems ASCII first

Claim maps.

Primary target:
- docs/systems-manifest-ascii.md

Required output:
- append **Pass 29 = Iteration-4 Pass 4**;
- update current-tip qualification / open-board wording;
- do not rewrite Passes 1-28.

May also write:
- Pass-5 findings/receipt;
- plans index.

Must not yet edit:
- graphics companions;
- law files;
- environment/reduction/Rust maps, except a phase receipt if required.

Release maps.

### Phase B — graphics: B/C companion qualification

Claim graphics.

Targets:
- docs/graphics/graphic-b-ascii.md
- docs/graphics/graphic-c-ascii.md
- docs/graphics/README.md only if navigation requires it.

Purpose:
- qualify stale G2/POINTER-gap statements against current law;
- preserve PNG authority;
- do not claim the PNGs literally say Join.

Release graphics.

### Phase C — maps: active maps and proof surfaces

Reclaim maps.

Targets, in this order:
1. docs/atomic-primitives-map.md
2. docs/reason-model-map.md if stale current claims are found
3. docs/plans/lace-context-iter6-pass-4-environment.md
4. docs/plans/reduction-pass-2-execution-model.md
5. docs/plans/rust-nostd-crate-map.md
6. current rust-nostd branch contracts / maps that still expose the old blocker
7. any current maps-owned navigation index that points readers to superseded status.

Historical plan receipts are append-qualified, not rewritten.

Release maps.

### Phase D — law: current-facing entrypoint hygiene

Claim law only after Phase C.

Targets to audit:
- README.md
- docs/README.md
- CONTRIBUTING.md
- AGENTS.md
- CLAUDE.md
- docs/staking-the-workspace.md
- any current law-owned sentence that still says pointer emission/arity/target kind are wholly open.

Edit only current-facing status/gate wording that is actually stale.

Must preserve:
- explicit human acceptance requirement;
- permanent no-second-store rules;
- no src/ until gate + task authorization;
- historical references.

Release law.

### Phase E — maps closeout

Reclaim maps.

Record:
- exact commits by station;
- stale-surface count before/after;
- unresolved selection gap;
- remaining non-Pass-5 work, if any.

Release maps and STOP.

---

## 3. Phase A — global systems ASCII Pass 29

### 3.1 Identity

Append:

**Pass 29 — Iteration-4 Pass 4 — ROOT / TOUCH / JOIN propagation**

Pass 29 is a current-law incorporation into the companion.

It does not alter the authority chain:

\[
\text{Graphics}
>
\text{systems-manifest}
>
\text{systems-manifest-ascii}.
\]

### 3.2 Current-tip qualification

The file currently says, in several current-tip locations:

    Emission remains [GAP].
    G2 / Φ stay [GAP].
    POINTER when / arity / adjacency = [GAP].

Pass 29 must not edit those historical Pass-1-28 records.

Instead add a current qualification:

    Historical Passes 1-28 preserve the then-current G2/Φ gap.
    Current law after ROOT / TOUCH / FOLD Pass 4:
      LAND                     settled
      unseen -> ROOT           settled
      seen -> RootTouch Join   settled
      binary Join              settled
      WORD/POINTER operands    settled
      finite-sample n+1        settled
      adjacency trigger        refused
      arbitrary relation/sample selection  [GAP]
      G1/tokenization          [GAP]

### 3.3 Core flow

Pass 29 must draw:

    Contract II
    raw word arrival
          |
          v
       WORD row
          |
      +---+---+
      |       |
    unseen   seen
      |       |
     ROOT   Join(new occurrence, Root)
      |       |
      +---+---+
          |
    selected arbitrary relation/sample?
          |
         yes
          |
       binary Join
          |
     repeated n+1 Join
          |
     grounded point
          |
       Continue

And a separate fence:

    selection source = [GAP]
    Join does not discover meaning
    Contract II does not secretly carry S
    Projection does not write selection back

### 3.4 N-D line

Add:

    WORD / JOIN point
          |
    Join may name Join
          |
    point-of-points
          |
    relation among relations
          |
    dot-to-dot N-D projection
          |
       read only

No coordinates. No N-D store.

### 3.5 Open-board delta

Current global open board should distinguish:

**Closed from old G2 bundle**
- repeated WORD admission;
- unseen-root structural branch;
- root-touch;
- physical binary arity;
- WORD/POINTER operand permission;
- backward same-line legality;
- finite-sample grounding;
- POINTER-to-POINTER participation;
- adjacency-as-trigger false.

**Still open**
- arbitrary relation/sample selector;
- G1/tokenization;
- document/conversation/provenance boundaries;
- read/query/traversal;
- concurrency;
- C6 family-label representation;
- eventual-reference rule;
- unrelated device/runtime opens.

### 3.6 Pass-29 falsifiers

| ID | Falsifier | Action |
|---|---|---|
| A29-1 | Pass 29 rewrites Passes 1-28 history | STOP |
| A29-2 | companion claims Graphics literally say Join | STOP |
| A29-3 | selection gap disappears without new ruling | STOP |
| A29-4 | Contract II gains hidden sample input | STOP |
| A29-5 | N-D becomes a stored dimension schema | STOP |
| A29-6 | acceptance is implied by law reconciliation | STOP |
| A29-7 | current law commits are not cited | STOP |
| A29-8 | clipboards are promoted to Core authority | STOP |

---

## 4. Phase B — Graphic B qualification

Graphic B ASCII is a source-reading companion, not law.

### 4.1 B8 touch remains

Keep Graphic-B source statement:
- touch remains;
- later wire can re-enter.

Add qualification outside the PNG transcription:

> Current law maps the retained seen-word touch to RootTouch Join(new occurrence, root). Graphic B itself does not specify the array constructor.

### 4.2 B12 governing rule

Do not rewrite the source box.

The existing "no emit POINTER" wording is valid as a statement about the PNG.

Add:

> Array-layer constructor is supplied by current law / Graphic-D reconciliation, not by B12 itself.

### 4.3 Gap split

Replace only current companion-status summaries:

Old:
- when POINTER appended = [GAP].

Current:
- root-touch selection = closed;
- binary Join construction = closed;
- arbitrary non-root-touch operand selection = [GAP].

### 4.4 B falsifiers

- B PNG text must not be rewritten.
- No new panel source quote.
- No claim that every touch is automatically arbitrary Join selection.
- No document-boundary selector.

---

## 5. Phase B — Graphic C qualification

### 5.1 C7 5D+ touches-of-touches

Current law/storage correspondent:

\[
\mathsf{Join}(\text{point},\text{point})
\]

with POINTER operands allowed.

Add qualification:
- relation-among-relations now has a known storage constructor;
- arbitrary relation choice remains open.

### 5.2 C6 family seam

Do **not** close:
- how dictionary/document/conversation/provenance families are labelled or discriminated.

The current law settles relation storage, not route-family metadata.

### 5.3 C11 projection fence

Strengthen cross-reference only:

- recursive grounded points are real Lace rows;
- N-D drawing remains projection;
- a UI-selected edge cannot write Core unless a separately authorized input/interface exists.

### 5.4 C gap board

Old:
- G2 when / arity / adjacency = [GAP].

New current companion qualification:
- binary Join / arity = current law;
- adjacency trigger = closed false;
- RootTouch = current law;
- arbitrary relation/sample selector = [GAP];
- C6 labels = [GAP].

---

## 6. Phase C — atomic primitives map

Current P3/P5/P11 language bundles too much under G2.

Required split:

### Closed constructor facts

- P2 WORD occurrence per successful arrival.
- first occurrence = root role.
- later occurrence remains distinct.
- P3 POINTER = binary Join.
- refs earlier same line.
- operand kind = WORD or POINTER.
- RootTouch = Join(new occurrence, root).
- finite sample = repeated Join.
- relation-to-relation = Join may name Join.

### Open selection facts

- arbitrary ordered relation/sample selection.
- automatic subject/operand chooser.
- G1 equality/tokenization.
- route-family boundary/labeling.

No new primitive for selection.

---

## 7. Phase C — reason-model map

Audit current sections that still say:
- emission broadly open;
- arity open;
- target kind open;
- repeated occurrence POINTER requirement open.

Only current-facing summaries should be updated.

Historical source analogies and anti-import boundaries remain unchanged.

Expected current statement:

> Storage construction is known once ordered operands are selected. The unresolved issue is origin/selection of an arbitrary relation assertion.

Do not import a selection solution from an adjacent paper/model.

---

## 8. Phase C — execution environment E1-E10

This is a dated proposal and must retain its original candidate ledger.

### 8.1 No rewrite of E5 history

Keep all 17 candidate dispositions.

Add a current qualification section after the dated environment:

The broad old unknown:

\[
\sigma:\mathcal L\times V\rightarrow Step^*
\]

has been narrowed by later human rulings.

Deterministic/current:
- LAND;
- unseen root;
- seen RootTouch;
- binary Join;
- finite-sample grounding.

Remaining unconstrained object:

\[
\mathrm{Select}
\]

or an equivalent explicitly unimplemented relation-selection input.

### 8.2 Do not define Select

Pass 5 must not invent:

\[
\mathrm{Select}(L,v)=...
\]

No semantic heuristic, similarity, adjacency, window, route boundary or learned state is admitted.

### 8.3 Environment current readiness

Change only current qualification:

Old:
- no accepted schedule/emitter.

Current:
- constructor law reconciled but **not human accepted for implementation**;
- arbitrary selection remains GAP;
- implementation gate remains closed.

---

## 9. Phase C — reduction whole-app model

Current broad STEP must be qualified.

Desired conceptual split:

\[
\mathrm{STEP}(L,v,\rho?)
\]

where \(\rho?\) is **not** silently added as a new input type.

Instead state:

1. arrival mechanics are deterministic under current law;
2. arbitrary relation grounding is deterministic once an ordered relation/sample is supplied;
3. the current whole-app model does not say where arbitrary selection originates;
4. hopper \(h\) remains word arrival custody and must not silently become sample-selection custody.

### Reduction falsifiers

- do not mutate \(\Sigma=(L,h,K)\) merely to hide the gap;
- do not add \(S\) to state without ruling;
- do not invent a Ground command;
- do not claim OPEN/PERSIST semantics changed.

---

## 10. Phase C — Rust no_std maps and branch contracts

This is documentation premise reconciliation only.

### 10.1 rust-nostd-crate-map

Known stale statements:
- arity open;
- target selection/timing/arity bundled;
- pointer layout conditional on accepted arity;
- "EMISSION SETTLED? NO / [GAP]" as broad status.

Required current split:

**Constructor premise available in reconciled draft law**
- WORD;
- binary POINTER layout;
- backward same-line refs;
- WORD/POINTER targets;
- Join rows reusable.

**Still open / gate**
- arbitrary relation/sample selector;
- G1 identity;
- acceptance of pointer-emission law;
- task-specific proof/branch authorization.

### 10.2 Branch contracts

Search all current rust-nostd branch cards for:
- "arity unresolved";
- "POINTER target kind unresolved";
- "later WORD optional";
- broad sigma/emission blocker;
- required POINTER→POINTER permission still open.

Update only current premise summaries.

Do not:
- create directories;
- create code;
- create Cargo;
- mark proofs complete;
- make branch 1 eligible solely because constructor mechanics are reconciled.

### 10.3 Acceptance remains a hard gate

Pass 5 must preserve:

\[
\text{reconciled law}
\neq
\text{human accepted law}
\]

and:

\[
\text{known constructor}
\neq
\text{authorized implementation task}.
\]

---

## 11. Phase D — law-facing entrypoint audit

After lower surfaces are current, claim law.

Search:
- README.md
- docs/README.md
- CONTRIBUTING.md
- AGENTS.md
- CLAUDE.md
- docs/staking-the-workspace.md

for current-facing statements equivalent to:

- pointer emission wholly unanswered;
- arity still open;
- POINTER-to-POINTER permission open;
- later WORD may be absent;
- Q2 conflict is current;
- existence of pointer-emission file alone is the implementation gate;
- implementation blocked only because constructor unknown.

### Allowed edits

Only update stale **current status**.

### Must preserve

- exact acceptance requirement;
- no src/ before acceptance/task authorization;
- no second store;
- Rust no_std / zero foreign dependency law;
- one branch/task/proof discipline;
- historical receipts and old commit descriptions.

### Phase-D falsifiers

- do not loosen gate language because constructor is known;
- do not call draft law accepted;
- do not rewrite permanent bans as temporary;
- do not erase historical "emission GAP" references that are date-qualified.

---

## 12. Global stale-statement census

Pass 5 findings must include counts by class before and after:

1. BROAD-GAP — says pointer emission / Φ wholly unknown.
2. ARITY-OPEN — says binary shape still unresolved.
3. TARGET-KIND-OPEN — says POINTER→POINTER/WORD permission unresolved.
4. LATER-WORD-OPEN — says repeated arrival may omit WORD.
5. ROOT-OPEN — unseen/root behavior open.
6. GROUND-OPEN — finite-sample construction open.
7. SELECTION-GAP — correctly names only arbitrary relation/sample selection.
8. ACCEPTANCE-GATE — correctly says reconciled draft is not accepted.
9. HISTORICAL — dated old status, must remain.

Pass 5 must reduce classes 1-6 only on active current-facing surfaces.

Do not "fix" class 9.

---

## 13. Pass 5 proof board

Execution findings must score these:

| ID | Requirement |
|---|---|
| P5-1 | Global ASCII Pass 29 added without rewriting Passes 1-28 |
| P5-2 | Current ASCII open board names selection gap, not broad G2 |
| P5-3 | B companion qualified without rewriting PNG authority |
| P5-4 | C companion qualifies C7 storage while keeping C6 labels open |
| P5-5 | Atomic map splits constructor facts from selection gap |
| P5-6 | Reason map has no active broad-emission claim |
| P5-7 | Environment keeps historical candidate ledger and narrows only current qualification |
| P5-8 | Reduction does not smuggle selection into \(h\) or \(\Sigma\) |
| P5-9 | Rust map/branch cards use current constructor premises |
| P5-10 | No Rust proof/eligibility claim is upgraded by documentation alone |
| P5-11 | Law entrypoints reflect reconciled draft but preserve acceptance gate |
| P5-12 | Historical receipts retained |
| P5-13 | N-D remains projection-only |
| P5-14 | No hidden sample channel added to Contract II |
| P5-15 | No selection heuristic invented |
| P5-16 | No src/ / Cargo / implementation artifact created |

Target: 16 PASS. Any failure stops the affected phase.

---

## 14. Drift handling

Main is active and other agents are working.

Before every phase:
1. fetch main;
2. verify target station is FREE;
3. verify target files have not materially changed since the planning read;
4. if target drift changes the stale statement or authority relationship, refresh the phase plan/findings before editing;
5. never overwrite another agent's work merely because Pass 5 was planned first.

The two commits after Pass 4 currently touch only the Rust-target clipboard shelf and do not alter Pass-5 targets. This is an observation at planning time, not a guarantee for execution time.

---

## 15. What Pass 5 does not do

Pass 5 does not:

- solve arbitrary relation/sample selection;
- accept pointer-emission.md;
- implement Join;
- create a selection API;
- define query/traversal;
- resolve G1/tokenization;
- define document boundaries;
- define C6 route-family labels;
- alter the canonical PNGs;
- import HCC-A/Coffee Cup/Water state into Core;
- create src/, Cargo, crates or branches;
- mark Rust proofs complete;
- start Pass 30 automatically.

---

## 16. Expected result

Expected after execution:

\[
\text{repo active current surfaces}
\approx
\text{current law}
\]

with one clean Core-adjacent residual:

\[
\boxed{
\text{arbitrary relation/sample selection}
}
\]

The repository should stop presenting this as:

\[
\text{unknown POINTER representation/emission/arity}
\]

and instead present:

\[
\text{known append mechanics}
+
\text{unknown arbitrary relation chooser}.
\]

Expected next question after Pass 5:

> Is arbitrary selection actually a required automatic Core behavior, or is relation selection supplied by some later explicit interaction/interface not yet defined by the source?

Pass 5 must not answer that question.

---

## 17. Stop condition

Pass 5 is complete only when:

- Pass 29 is appended to global systems ASCII;
- graphics B/C companions are current-qualified;
- maps/environment/reduction/Rust current surfaces are reconciled;
- law-facing entrypoints are audited and stale current status corrected;
- 16-point proof board is complete;
- stale-statement census is recorded;
- all station claims are released;
- pointer-emission acceptance remains absent;
- arbitrary selection remains named [GAP];
- no src/ or Cargo exists;
- then STOP.

## Steward test

Do not let old documentation make the hole larger than it is. Do not let new documentation make the hole smaller than it is. The constructor is known. The arbitrary chooser is not. Pass 5 exists to make every current-facing surface say exactly that.


---

## 18. Execution receipt

Pass 5 executed as a staged propagation campaign.

### Phase A — maps
- Global systems ASCII Pass 29: a7a7180f31b5c3b2483c928d766d38d8648281a3
- Phase-A findings: b073c8c8589c196def6a648799edf365eb4618eb

### Phase B — graphics
- Graphic B ASCII: f6c68ba105bb2aed7ed284e350d8fea422ab8810
- Graphic C ASCII: 17edb9f6306413671f0710a4463278dddd8b6de6
- graphics release: 3c11cc7c98e074f0a56f52cb8f5f5be3b56d00b7

### Phase C — maps/proof/Rust surfaces
- atomic map: 84ceeac21ab7a89ca2f9d418cf0b47bbc1de97e2
- reason map: d12820abb716a4c3d8836aa266174ef290e70240
- environment qualification: ee62a700f4e29fbce7f88604ba58fb914f50e8ee
- reduction qualification: f848a508e465cb43a84c91d59ebcc779cc55d14c
- Rust crate map: 1c0c44285e1565b8f06a88ac2ed109064d898d5a
- Rust branch contracts: a66b58690733e5e6040258f05ba00f7baabdfd8e + 18d60e5bb378108976e87a03a77b033ddc525e97
- Rust second reading: 9f07e477241c5854d34cee3df96ea653ef023e78
- Phase B/C findings receipt: d84726b2c16f077de8a3b4a3aea17e2e8a7daef3

### Phase D — law entrypoints
- README: 223b77648d5579b9fd76da3600f098c33e91bdb6
- docs/README: 9b6a3abc439dd6525f9b42b965bb41ecac921a4c
- CONTRIBUTING: bae2d717b7f971273bb0c3cf3d767c9ceab6dc6b
- AGENTS: 58ccbf3c11833c1d9d0da6b1779159392edb5e37
- CLAUDE: 996a2ce55f61aacb4ad0dbc4c0b1f26c80828df9
- law release: 73e76fe961880c7f342e5e531ba02f057536734c

### Census-triggered bounded control extension

The planned closeout census found mandatory future-agent control surfaces outside the initial target list. Rather than claim a false green result, Pass 5 paused closeout and propagated current status through those owning stations:

- context-pass: aed51f60c0a54f67df4223071c1774df5ca984ce
- prompts release: c5586627042c69bc13daf8a31d0cd1a8043481db
- agent-control steward layer: 9f8517759b80e7e46b5a091a0b3a71245168311f
- roles: e58742deec16f81344e65637fc0308e5b4c6ae91
- kit READ_ME_FIRST: 1b8ea27983f566319ff146abc72227285face028
- kit release: a3c075bacd95ea17d3bcc9d49ce8d54dde20d2fc

The verbatim transported human-law block and dated audit were preserved.

### Final proof board

**P5-1 through P5-16: 16/16 PASS.**

The bounded 19-surface active-status census and exact methodology are in:
[math-execution-root-touch-fold-pass-5-findings.md](math-execution-root-touch-fold-pass-5-findings.md).

### Final current result

Settled/current:
- WORD landing;
- unseen root;
- seen RootTouch;
- binary Join;
- earlier same-line WORD/POINTER operands;
- repeated finite-sample grounding;
- relation-among-relation storage;
- N-D projection reading.

Still open:

\[
\boxed{
\text{arbitrary non-root-touch relation/sample selection}
}
\]

Implementation status:
- pointer-emission draft reconciled but **not human-accepted**;
- no branch proof/eligibility upgrade;
- no src/;
- no Cargo;
- no implementation artifact.

The census extension changed documentation scope only. It did not add Core machinery.

**STOP. No Pass 6 opened automatically.**
