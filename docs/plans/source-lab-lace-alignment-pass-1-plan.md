# SOURCE → LAB → LACE ALIGNMENT — Restart Pass 1 PLAN

**Status:** PLAN — NOT EXECUTED.  
**Campaign:** Source → Lab → Lace Alignment — **restart**.  
**Pass:** 1.  
**Planning station:** maps · `Astra-alignment-restart`.  
**Pinned current main before planning claim:** `80b0346fdca5fdd928d9285bf8bb4f6e8b228b11`.  
**Planning claim commit:** `e9b21e2bd15c677aa85e265d2f6a96e91b6c58b6`.  
**Prior campaigns are evidence, not this campaign's numbering:** Lab Source Architecture Passes 1–6, Manifest Reconciliation Passes 1–4, global ASCII Passes 30–32, Sampling/Density/Emergence, ROOT/TOUCH/FOLD, and the older Structural Alignment plan remain distinct.  
**Implementation:** none. No `src/`, Cargo implementation, branch birth, runtime authorization, pointer acceptance, or new Core mechanism follows from this plan.  
**Human acceptance:** unchanged / absent for the current pointer draft.

Pass 1 restarts alignment from the human's vertical architecture rather than from the repository's accumulated campaign taxonomy.

---

## 0. Target architecture — exact alignment court

```
                 BEHAVIORAL GOALS
        HCC-A · Coffee Cup · Water
                    |
              source clipboards
                    |
                    | possible machinery,
                    | behavioral constraints,
                    | hypotheses
                    v

        BOOKS / PAPERS / RESEARCH
                    |
              source clipboards
                    |
                    | possible machinery,
                    | algorithms, mathematics,
                    | known constraints
                    v

              +-----------+
              | laceArc   |
              |    LAB    |
              +-----------+
                    |
             propose / attack
             reproduce / test
             falsify / retain
                    |
                    v
                  LACE
                    |
          substrate / machine under
              experimental growth
                    |
                    v

          HARDWARE / PLATFORM REALITY
       CPU · GPU · WASM · WebGPU · WebNN
       Rust · no_std · browser · device
                    |
             hardware/spec/reference
                 clipboards
                    |
                    v
       physical execution constraints
```

This diagram is the **alignment model for this campaign**.

It is not a claim that every arrow is a runtime call edge. It defines role, evidence flow, and authority.

---

## 1. Pass-1 question

> **Can every current, active repository surface be placed unambiguously into this vertical model without collapsing behavioral goals, source machinery, Lab instruments, Lace substrate law, or physical platform constraints into one another?**

Pass 1 is an **alignment census + reconciliation pass**.

It is not:
- a new source harvest;
- an RM-A design pass;
- a retention-rule search;
- an implementation pass;
- a mechanism-ranking pass;
- a rewrite of historical receipts.

---

## 2. Current baseline at the restart

### 2.1 Global companion

Current `docs/systems-manifest-ascii.md` says:

- Pass 30 = sampling reconciliation;
- Pass 31 = prior Lab Source Architecture source/lab overlay;
- Pass 32 = current canonical mechanism catalog ↔ manifest contract registry.

Pass 32 is useful and remains current for **mechanism identity / connections**, but it is not itself the organizing ontology for this restarted alignment campaign.

### 2.2 Canonical mechanism / contract pair

Current canonical pair:

```
docs/systems-manifest-ascii.md
    = mechanism catalog / typed connections / authority classes

docs/systems-manifest.md
    = behavioral contracts / integration boundaries / open interfaces
```

Current catalog:
- `L-M01 … L-M21`;
- `L-GAP-RM`.

Current manifest:
- `SM-C01 … SM-C21`;
- `SM-GAP-RM`;
- `SM-X-BEH`;
- `SM-X-RES`;
- `SM-X-PLAT`;
- `SM-X-L3`.

Pass 1 must **preserve** this useful decomposition while placing it correctly inside the larger vertical model.

### 2.3 Latest Lab evidence

Lab Source Architecture Passes 2–6 materially advanced the experimental evidence.

Current latest Lab finding:

```
RM-A RETAIN
    OPEN
    evidence insufficiency certified as MISSING-A

RM-B MATERIALIZE
    PARTIAL / CLASS-SPECIFIC CLOSURE
    selected finite contiguous sample occurrence
      -> existing ordered WORD rows
      -> L-M19 repeated Join
      -> one lawful addressable representative

RM-C ROLE / ORDER
    CLASS-SPECIFIC CLOSURES
    RootTouch
    contiguous ordered sample grounding
    generic classes remain open
```

This is **Lab evidence**.

It is not automatically canonical architecture merely because the experiments executed.

### 2.4 Current authority mismatch to audit

The canonical surfaces still conservatively expose one `L-GAP-RM / SM-GAP-RM` with RM-A/RM-B/RM-C question faces.

That is not automatically wrong.

Pass 1 must explicitly distinguish:

```
canonical open interface
        !=
latest bounded Lab evidence about parts of that interface
```

The campaign must not accidentally promote bounded closure into universal law or, conversely, hide real experimental progress behind stale broad wording.

### 2.5 Known historical bookkeeping defect

Current tree still has:

- `docs/plans/sampling-density-emergence-pass-6-plan.md` → **PLAN - NOT EXECUTED**;
- no `docs/plans/sampling-density-emergence-pass-6-findings.md`;
- global ASCII still records Pass 30 as “SAMPLING / DENSITY / EMERGENCE Pass 6 reconciliation.”

Pass 1 must classify this as a **provenance/receipt alignment issue**, not silently infer execution history.

---

## 3. Alignment layers and authority

Every current surface must resolve to one primary layer.

### A — Behavioral-goal sources

Examples:
- HCC-A;
- Coffee Cup;
- Where the Water Is Loud / Dual Proofing.

Role:
- target behavior families;
- behavioral constraints;
- source-described possible machinery;
- hypotheses;
- falsifiers.

Clipboard route:
- existing `docs/clock/*-systems-manifest.md` products and provenance.

May feed:
- laceArc Lab.

Must not automatically become:
- Lace Core;
- required runtime decomposition;
- retention semantics;
- accepted implementation.

### B — Research / books / papers

Examples:
- Kauffman;
- Rowlands;
- Xiao–Zhu;
- Petersen–Zech;
- AgentScope.

Role:
- algorithms;
- mathematics;
- mechanisms;
- known limits;
- counterexamples;
- falsifiers;
- source-shaped candidate machinery.

Clipboard route:
- `docs/clipboards/`.

May feed:
- laceArc Lab.

Must not automatically become:
- behavioral goals;
- Core law;
- legal adapters into Lace.

### C — laceArc LAB

Includes:
- candidate plans;
- proposer / attacker / resolver work;
- bounded fixtures;
- `lace-candidate-run.py`;
- `lace-retention-harness.py`;
- D1 golden fixture;
- candidate admission envelopes;
- replay/oracle-independence machinery;
- findings, falsifiers, negative results, bounded survivors.

Role:
- convert source ideas into experimental evidence;
- attack both candidates and instruments;
- preserve failures;
- distinguish source mechanism from Lab proxy;
- compare only after authority/provenance is explicit.

Must not become:
- second authoritative store;
- accepted Core by score;
- silent law writer.

**Important:** “retain” in the Lab loop means retain **evidence / candidate status**, not append a POINTER to Lace. The two meanings must stay explicitly separate.

### D — LACE

Role:
- substrate / machine under experimental growth.

Authority:
- Graphics A–D;
- explicit human rulings;
- admitted DERIVED consequences;
- canonical mechanism/contract pair at current scope.

Current known substrate includes:
- one append-only Graphic-D Lace;
- WORD / POINTER;
- root / RootTouch;
- binary Join;
- finite grounding;
- derived sampling/search working machinery under current authority;
- current open interfaces.

Lace does not equal:
- the Lab;
- the source corpus;
- hardware APIs;
- the viewers;
- implementation packaging.

### E — Hardware / platform reality

Examples:
- CPU;
- GPU;
- WebAssembly;
- WebGPU;
- WGSL;
- WebNN;
- rustc;
- Cargo;
- Rust `no_std`;
- browser/Chrome;
- Android/device;
- actual memory/resource constraints.

Role:
- physical feasibility;
- executable primitives;
- resource ceilings;
- compiler/target/toolchain truth;
- runtime observations.

Clipboard route:
- existing platform/toolchain clipboards;
- future hardware manuals/specs/observations use the same evidence discipline.

May:
- kill an impossible candidate;
- enable an implementation experiment;
- constrain architecture where physical feasibility is relevant.

Must not:
- manufacture missing Lace semantics;
- decide RM-A by convenience;
- become Core because a platform exposes an API.

---

## 4. Pass-1 thesis

[
oxed{
	ext{Every active repo surface has one primary layer and explicit cross-layer authority.}
}
]

[
oxed{
	ext{All external/source machinery enters architecture through the Lab, not by citation.}
}
]

[
oxed{
	ext{Lab evidence may narrow an open without silently becoming canonical law.}
}
]

[
oxed{
	ext{Hardware constrains realizability; it does not define Lace semantics.}
}
]

---

## 5. Pass-1 execution product: vertical alignment register

Execution should create:

`docs/plans/source-lab-lace-alignment-pass-1-findings.md`

Its primary table must be a **current-tree vertical alignment register**.

Minimum columns:

| Field | Meaning |
|---|---|
| Surface | exact file / mechanism / product |
| Primary layer | GOAL / RESEARCH / LAB / LACE / PLATFORM |
| Authority class | SOURCE / HUMAN / DERIVED / LAB / CANDIDATE / PLATFORM / VIEW / GAP |
| Inputs | what may lawfully feed this surface |
| Outputs | what the surface may lawfully produce |
| May affect | exact downstream layer(s) |
| Must not affect | authority fence |
| Current status | current / dated / proposal / sealed / superseded / open |
| Drift | none / wording / topology / authority / provenance / receipt |
| Action | KEEP / QUALIFY / MOVE-REFERENCE / RECONCILE / HISTORY-ONLY |

Do not create a second canonical ontology file unless execution proves one is required. The findings register is campaign evidence.

---

## 6. Mandatory census surfaces

Pass 1 must inspect at least these **current** surfaces.

### Governing / entrance

- `AGENTS.md`
- `README.md`
- `docs/README.md`
- `docs/law-why-these-documents.md`
- `docs/references.md`
- `docs/prompts/context-pass.md`
- `docs/kit/agent-control.md`
- `docs/kit/READ_ME_FIRST.md`

### Behavioral-goal clipboard family

- `docs/clock/README.md`
- `docs/clock/hcc-a-systems-manifest.md`
- `docs/clock/coffee-cup-systems-manifest.md`
- `docs/clock/water-systems-manifest.md`
- `docs/clock/train-h-c-w-systems-manifest.md`
- current H/C/W mechanism/composition products that remain live.

### Research clipboard family

- `docs/clipboards/README.md`
- current clipboard + mechanisms + ASCII products for:
  - Kauffman;
  - Rowlands;
  - Xiao–Zhu;
  - Petersen–Zech;
  - AgentScope.

### Platform / toolchain clipboard family

- Wasm 3;
- WebGPU;
- WGSL;
- WebNN;
- rustc;
- Cargo build-std;
- current browser/device observations referenced by active proposals.

### Lab instruments / evidence

- `docs/plans/tools/lace-candidate-run.py`
- `docs/plans/tools/lace-retention-harness.py`
- `docs/plans/fixtures/d1-golden-v0.1.0.json`
- Lab Source Architecture Passes 2–6;
- Manifest Reconciliation Pass 4 Lab admission envelope;
- current synthesis and RM-A design docket.

### Lace substrate / canonical

- four canonical PNGs;
- `docs/systems-manifest.md`;
- `docs/systems-manifest-ascii.md`;
- `docs/pointer-emission.md`;
- `docs/math-execution-model.md`;
- `docs/atomic-primitives-map.md`;
- current Rust no_std planning maps only as **future implementation proposals**, not current substrate law.

### Coordination / proposal apparatus

Classify, do not automatically promote:
- Carrier;
- Hopper;
- Seed corpus;
- Frontier;
- refusal edge;
- Runtime Layer III;
- `Σ=(L,h,K)`;
- R3/R4/R9/R10;
- Open #15.

---

## 7. What Pass 1 should reconcile

### R1 — source-flow visibility

Every current entrance must make this route recoverable:

```
behavioral goals ─┐
research ----------┼──> source clipboards ──> LAB
platform specs ----┘                           |
                                               v
                                              Lace
```

Platform also constrains Lace from below:

```
Lace
  |
  v
physical execution reality
  |
constraints / observations
  |
  +--------------------> Lab
```

### R2 — Lab vs Lace

Repair any surface where:
- harness output reads like Core truth;
- a candidate score reads like acceptance;
- a Lab proxy is attributed to a source;
- a negative result is treated as source silence;
- “retain evidence” can be confused with “retain POINTER.”

### R3 — behavioral goal vs candidate machinery

Preserve both:
- H/C/W define behavioral goals;
- H/C/W also contain possible machinery.

Reject both bad simplifications:
- “they are the required machine”;
- “they are only goals and their machinery is irrelevant.”

### R4 — research machinery

Preserve research mechanisms as testable candidate inputs.

Reject:
- metaphor-only demotion;
- direct Core import;
- editor adapters hidden as source-defined correspondences.

### R5 — hardware/platform placement

Platform/toolchain sources must be shown as:
- implementation primitives;
- physical constraints;
- observed/runtime facts;
- candidate enablers/falsifiers.

They must not be shown as:
- Lace semantic authority;
- RM-A fallback;
- mere Layer-III decoration when they can matter to implementation.

### R6 — canonical vs latest Lab evidence

Explicitly align:

```
SM-GAP-RM / L-GAP-RM
    canonical open interface
```

with latest bounded evidence:

```
RM-A OPEN / MISSING-A
RM-B partial class-specific closure
RM-C class-specific closure
```

Pass 1 must decide only **how to state that relationship honestly**.

It must not:
- universally close RM-B;
- universally close RM-C;
- invent RM-A.

### R7 — history vs current

Historical plans/findings remain historical even when current wording changes.

Current entrances must route readers around stale premises without rewriting old evidence.

### R8 — receipt integrity

Classify the Sampling Pass-6 / Pass-30 mismatch.

Execution may:
- add a qualification / provenance receipt;
- repair a current index claim;
- point Pass 30 at the commits that actually produced it.

Execution must not:
- fabricate a missing historical findings file as though it existed then;
- flip the old plan to EXECUTED without pointable execution evidence.

---

## 8. Station order for execution

```
PRE-FLIGHT
current main + all stations + recursive tree
        |
        v
LAW
authority / source roles / entrance language
        |
        v
HOLOGRAM / CLOCK
behavioral-goal clipboards + candidate machinery
        |
        v
CLIPBOARDS
research + platform source routes
        |
        v
PROMPTS
mandatory one-story entrance
        |
        v
KIT
active agent controls / package entrance
        |
        v
MAPS
Lab instruments + canonical/Lab distinction
        |
        v
GLOBAL ASCII LAST
only if current companion requires a new alignment overlay
        |
        v
FINDINGS / INDEX / RELEASE
```

Graphics source bytes are read-only.

No gearing shaft should be required unless execution discovers an actual owned current surface that cannot be aligned without one. A diagram mention of gearing does not justify a shaft claim.

---

## 9. Global ASCII rule

Current global mechanism catalog is **linear Pass 32**.

If Pass 1 execution materially changes `docs/systems-manifest-ascii.md`, record the next global companion delta as:

**linear Pass 33 — Source → Lab → Lace Alignment Restart Pass 1**

Pass 33 must be an **alignment overlay / qualification**, not a replacement for Pass 32's mechanism catalog.

Required relationship:

```
Pass 32
    canonical mechanism catalog

Pass 33, if needed
    current vertical authority / source / Lab / platform alignment
```

Do not renumber old passes.

---

## 10. Falsifiers

Pass 1 fails if any of these fire.

### F1 — source becomes architecture by citation
A clipboard mechanism becomes Lace merely because it exists.

### F2 — behavioral machinery erased
H/C/W machinery is dropped because the documents are behavioral-goal sources.

### F3 — research becomes decoration
Research mechanisms are fenced so strongly that the Lab cannot test them.

### F4 — Lab becomes Core
Harness state, candidate output, score, fixture, or findings become authoritative Lace.

### F5 — Lab evidence is ignored
Bounded RM-B/RM-C progress is hidden behind a broad GAP statement with no current qualification.

### F6 — bounded result becomes universal law
Partial/class-specific RM-B/RM-C closure is promoted beyond its tested class.

### F7 — platform becomes semantics
WebGPU/WebNN/Wasm/Rust/browser/device capability supplies RM-A or another missing semantic rule.

### F8 — platform becomes irrelevant
The repo treats platform/hardware references as presentation-only despite physical execution constraints.

### F9 — viewer becomes store
Projection/viewer/cache state gains Lace authority.

### F10 — old campaign numbering is reused
The restart is called Pass 7 or otherwise flattened into the prior Lab Source Architecture campaign.

### F11 — history is rewritten
Old receipts are cosmetically edited to look current.

### F12 — receipt is fabricated
Sampling Pass-6 history is “fixed” by inventing execution evidence.

### F13 — Pass 32 is discarded
The useful mechanism/contract catalog is replaced rather than placed inside the vertical model.

### F14 — RM-A design begins
Pass 1 invents, ranks, selects, or recommends a retain predicate.

### F15 — implementation gate moves
Any Core implementation authorization follows from alignment.

---

## 11. Completion board

Pass 1 execution is complete only when:

| Gate | Required |
|---|---|
| G1 | current main pin and full current-tree census recorded |
| G2 | every mandatory surface assigned a primary layer |
| G3 | H/C/W goals + candidate machinery both preserved |
| G4 | research machinery remains Lab-eligible and non-Core |
| G5 | platform/toolchain role includes physical feasibility and primitives |
| G6 | Lab instruments explicitly non-canonical |
| G7 | Lab “retain evidence” distinguished from Lace retention |
| G8 | Lace canonical substrate boundaries preserved |
| G9 | Pass 32 mechanism/contract pair preserved |
| G10 | canonical GAP vs latest RM-A/B/C evidence relationship stated |
| G11 | no partial result promoted universally |
| G12 | current entrances tell the same vertical story |
| G13 | historical receipts remain historical |
| G14 | Sampling Pass-6 / Pass-30 provenance mismatch explicitly classified |
| G15 | global ASCII updated last only if necessary |
| G16 | if changed, global ASCII uses linear Pass 33 |
| G17 | no RM-A rule proposed or selected |
| G18 | no implementation/acceptance gate moved |
| G19 | all claims released |

---

## 12. Expected Pass-1 verdict form

Pass 1 should end with one of:

### ALIGNED
All current active surfaces fit the vertical architecture without material authority drift.

### ALIGNED WITH OPENS
The architecture is coherent, but named surfaces remain intentionally open / candidate / bounded.

### MISALIGNED
At least one active surface grants the wrong layer authority or contradicts the target architecture.

Do not use “aligned” merely because prose can be paraphrased into agreement. The register and falsifier board must support it.

---

## 13. What Pass 1 deliberately does not do

Pass 1 does not:
- resume the old Lab Source Architecture numbering;
- design RM-A;
- rank retention candidates;
- search more books for a retain predicate;
- universally close RM-B or RM-C;
- choose implementation crates;
- build Rust;
- benchmark hardware;
- rewrite the graphics;
- accept pointer-emission;
- promote platform APIs;
- turn H/C/W into required runtime parts;
- demote H/C/W to goal-only prose.

The campaign restarts by making the repository answer the architecture correctly **before** another mechanism/design pass is allowed.

**STOP after the Pass-1 alignment verdict.**
