# CLIPBOARD — laceArc ← Rust `#![no_std]` crate / branch map

**PLAN / PROPOSAL. Nine sheets, A–H and X. No implementation acceptance. Current constructor premise: binary Join is reconciled in draft law; arbitrary non-root-touch relation/sample selection remains [GAP].**

Shelf note (defrag pass 2): this file is the **crate tree**. Serial birth cards live in `docs/plans/rust-nostd-branch-contracts.md`. Neither file is an accepted emission rule.

Execution of [the crate-map prompt](../prompts/rust-nostd-crate-map-prompt.md), checking and completing its existing draft. The sheets use rendered mapping tables. The seven planned names, birth order, and direct dependencies are retained alongside [the branch contracts](rust-nostd-branch-contracts.md). No proposed directory is created.

**Authority:** [graphics A–D](../graphics/README.md), [systems manifest](../systems-manifest.md), the complete [staking law](../staking-the-workspace.md), the complete [live law](../law-why-these-documents.md), [AGENTS.md](../../AGENTS.md), and [agent controls](../kit/agent-control.md). The [reason model](../reason-model-map.md) supplies the recorded anti-import boundaries. This map replaces none of those sources.

**Implementation Law:** Rust; `#![no_std]`; zero external dependencies; every dependency is our first-party branch; one branch, one task; state and prove that task before another branch exists. Allocation requires its own proven first-party task under the full law. The posture names, including seL4 and Coq, are the bar, not dependencies to import.

**Gate:** [pointer-emission.md](../pointer-emission.md) is now a reconciled but still unaccepted draft. It supplies current WORD/root/RootTouch/binary Join/finite-sample constructor premises, but **does not supply an arbitrary relation/sample selector and is not human-accepted**. Its filename, commits, or reconciliation do not open implementation. Exact human acceptance and every premise needed by the particular task remain required.


## Current premise qualification — ROOT / TOUCH / FOLD Pass 5

This map predates the Pass-4 law reconciliation. Read its dated "emission [GAP]" and arity-open rows as historical unless this section or a later current row qualifies them.

**Current constructor facts available as draft-law premises:**
- every successful arrival appends one WORD occurrence;
- unseen occurrence becomes its root;
- seen occurrence RootTouches that root;
- POINTER is binary Join(a,b);
- both operands are earlier same-line entries;
- WORD and POINTER are both legal operand kinds;
- Join rows are reusable by later Join;
- repeated Join grounds finite selected samples;
- adjacency alone does not emit.

**Still open / not owned by these seven doors:**
- arbitrary non-root-touch relation/sample selection;
- semantic subject/operand choice for an arbitrary asserted relation;
- G1 identity/tokenization;
- acceptance of pointer-emission.md;
- task-specific implementation/proof premises.

Therefore:

\[
\text{known constructor}
\neq
\text{accepted implementation authorization}.
\]

No branch birth, proof status, dependency edge, directory, source file, or Cargo artifact is upgraded by this qualification.

## PAGE A — INTUITION TEST

The YES entries are the editor's proposed association between a name and its cited source. **A stranger's recognition test and a physical-cord test are NOT_PERFORMED.** A plausible name is not evidence of a safe implementation.

| Planned name | Editor association | What the name must mean | Hands citation |
|---|---|---|---|
| strand/ | YES | The one retained line, including WORD and POINTER sections. | Pieces 1/6; B1, C1, D1. |
| word/ | YES | The value of an individual WORD occurrence, without interning. | Piece 4; D1–D2, D5. |
| pointer/ | YES | Constraints on an already-admitted POINTER section, not when to create it. | Piece 5; D2–D3, D6. |
| route/ | YES | Continue by extending that same line under accepted prose. | Pieces 2/8; B5, B12, D footer. |
| view_star/ | YES | Read retained passes as a star; do not store a Star object. | Pieces 7/15; A12, B3/B9, D4–D5. |
| view_proj/ | YES | Show a projection of the line without writing it. | Piece 13; B11, C11, D4. |
| core/ | YES, as the project's composition label | Connect proved work to one strand, without another engine or store. | Piece 2; manifest composition and Implementation Law. |

**KEEP:** shoe-readable names and one task per planned door. **FORBID:** names that conceal a graph, vocabulary, embedding, meaning store, or emission policy.

The [AgentScope clipboard](../clipboards/agentscope-clipboard.md) is a separate tracked document about the holder, not part of this crate map.

The absence of an arrival/ folder is a layout choice, not rejection of **Contract II, the Arrival Surface**. Arrival remains necessary as a thin surface. It must not own a vocabulary or second history. This revision preserves the seven-door plan rather than adding an eighth door.

## PAGE B — PROPOSED WORKSPACE SKETCH

All paths in this table are **future candidates after the gate**, not existing files or commands. A slash denotes a proposed directory, not an existing Git branch. A task branch and a crate directory are distinct things.

| Future root-relative door | One-task sentence | Authoritative state boundary |
|---|---|---|
| strand/ | Maintain the append-only 1D log of retained sections. | Implements the sole retained store; Pieces 1 and 6 name the same object. |
| word/ | Describe the preserved WORD value for one occurrence. | Describes content held in that strand; no intern table or independent occurrence store. |
| pointer/ | Describe POINTER layout constraints once the necessary contract is accepted. | Describes a strand section; no edge store, target finder, or append authority. |
| route/ | Continue by appending an independently admitted section through strand/. | No separate route representation; no decision that a word must emit a POINTER. |
| view_star/ | Read stars from distinct retained occurrences and recorded participation. | No Star table or authoritative membership cache. |
| view_proj/ | Present the retained strand as a read-only 1D, 2D, or 3D shadow. | No coordinate, graph, or per-dimension store claiming Lace authority. |
| core/ | Orchestrate only the proved first-party doors at their reviewed revisions. | No second store, duplicate branch logic, or policy hidden in composition. |

**One Core contract remains irreducible.** These are proposed proof/work boundaries, not independently owned strands or seven Core engines. WORD, POINTER, route, touch, and star formation remain coupled through one retained history. The two view doors expose Layer III readings; listing them as callees of the core/ facade does not promote them into authoritative Core state.

**Current unassigned responsibility (ROOT / TOUCH / FOLD Pass 5):** the branch cards let route/ append independently admitted sections and core/ delegate. The physical Join constructor is now a draft-law premise, but **no listed door owns arbitrary non-root-touch relation/sample selection**. The seven-door map therefore remains an incomplete implementation decomposition. This note adds no branch or dependency edge and changes no card's task.

The statement “one folder per Piece” is therefore not adopted as a law. Several Pieces describe the same store or operation. Conversely, a Piece can have both a Core formation and a read-only presentation. Any later physical packaging must preserve these distinctions.

**vs Hands:** CONFIRMS one strand and one store; REFINES the former draft's folder-to-Piece equivalence. The concrete section representation and inter-crate composition remain unproved.

## PAGE C — PIECE → PLANNED WORK

These mappings identify responsibilities, not implemented types or executable interfaces.

| Piece / source | Planned locus | One-task contribution and unresolved boundary |
|---|---|---|
| 1 Strand; A1, B1, C1 | strand/ | Retain one continuous history; no second array for the other end. |
| 2 Governing Rule; A13, B12, C12, D footer | route/ with thin core/ composition | Realize accepted continuation through the one strand. LAND/root/RootTouch/binary Join mechanics are current draft-law premises; arbitrary relation/sample selection remains unowned. |
| 3 Raw Input / Arrival Order; C10, B6 | Contract II surface into the core/route path | Preserve supplied occurrences and order. Segmentation, case, and input-custody details are not selected; no new arrival/ crate is proposed here. |
| 4 WORD; D1–D2, D5 | word/ content held by strand/ | Preserve each occurrence at its distinct position. Encoding and unresolved word-identity criteria remain open. |
| 5 POINTER; D1–D3, D6 | pointer/ constraints, composed with strand/ | Current draft law fixes binary Join with two earlier same-line WORD/POINTER operands. pointer/ still must not choose arbitrary relation operands, own append timing, or infer semantic subject/order. |
| 6 Append-only 1D Array; D1 and key properties | strand/ | The same store as Piece 1, with permanent positions and no rewritten prefix. |
| 7 Star; A5/A12, B2–B3, D5 | Core formation; view_star/ reads it | Distinct passes remain on the strand. Reading requires stated identity and traversal premises; it does not construct a stored Star. |
| 8 Sequence as Physical Route; B5, C2 | route/ and strand/ | Retain exact supplied order; no sentence object, reordering, or parallel route log. |
| 9 Dictionary Wiring; B4, C9 | Dictionary feed uses the same Arrival Surface and Core; view_star/ may inspect the result | Feeding definition words extends the strand. Viewing that wiring is a separate read, not the feed itself. |
| 10 Document Feed; B6, C10 | Document feed uses that same Arrival Surface and Core; view_proj/ may display retained order | Preserve each arriving occurrence. A projection does not ingest the document or create a DOCUMENT entry. |
| 11 Touch / Participation; B7–B8, C7, D6 | Existing Core participation; no separate touch crate | RootTouch and Join-of-Join are current constructor premises. Arbitrary non-root-touch relation/sample selection remains outside this map's answers. |
| 12 Dimension; C5–C8 | Reading/configuration of Core, exposed by view_proj/ | Independent participation is not a new store, row type, or numerical width rule. How C6's four relation families correspond to D's schema remains open; a view label does not settle it. |
| 13 Projection; B11, C11, D4 | view_proj/ | Present recorded facts and explicit view conventions without writing back or inferring unrecorded structure. |
| 14 No Stored Meaning / No Collapse; A13, B10, C12 | Constraint on every door | Exclude stored definitions, extra authority, and merged occurrences; not a meaning module. |
| 15 Star-Internal Traffic; A12, B9 | Core formation; view_star/ reads it | Keep internal passages distinct; no interior-state table or separate engine. |

**Correction to the prior map:** Pieces 9 and 10 are not solely view responsibilities. The companion contracts' citations of these Pieces on view cards are valid for inspecting already-retained routes; they do not transfer feed ownership into a view. The manifest explicitly places dictionary/document feeds on the common Arrival Surface as uses of Core.

## PAGE D — DEPENDENCY EDGES AND CAPABILITIES

Retain the companion plan's direct edges. All are conditional on exact proved revisions and the independent implementation gate.

| Caller | Direct first-party callees | Required boundary |
|---|---|---|
| strand/ | none | Retention must not depend on layouts implemented in a later birth. |
| word/ | none | Value/layout description; no storage or append authority. |
| pointer/ | none | Layout/constraint description; same-line reference evidence remains a composition obligation. |
| route/ | strand/, word/, pointer/ | Compose earlier work for one independently admitted section; no hidden emission helper. |
| view_star/ | strand/ | Read-only access, including all transitively reachable capabilities. |
| view_proj/ | strand/, view_star/ | Read-only paths to the same retained source; no call to route/. |
| core/ | route/, view_star/, view_proj/ | Separate continuation from observation; no bypass into a raw append surface. |

This is **nine proposed direct edges**, each toward an earlier birth in Page G. That table has no dependency cycle [INFERENCE from the listed ordering], but it is not proof that future code obeys it.

There are convergent read paths, such as core/ reaching view_star/ directly and through view_proj/. Preserve the explicitly recorded dependencies; do not pretend the map is a flat tree. Every such path must refer to the same retained strand and exact proved revisions. Convergence does not authorize duplicated stores or a convenience dependency. No utils/ hub, unlisted edge, or foreign tree is added.

**Composition obligations remain open:** how immutable sections are represented before later layout births; how a layout-only POINTER can carry valid same-line/earlier-reference evidence and an accepted slot convention; how views receive values without duplicating word identity; and how retained content remains stable without mutable aliases. If a chosen representation needs a later callee or another task, it cannot graduate on the strength of this diagram. Revise both plans explicitly before using a different dependency structure.

## PAGE E — ANTI-CRATES

“Named-as-forbid” below is a planning classification. “Leak-risk” marks a responsibility that could cross the boundary; it is not a report that such a store currently exists.

| Temptation | Classification | Hands restriction |
|---|---|---|
| tokenizer/ that masks, replaces, normalizes, or interns | Named-as-forbid | Piece 3 preserves arrivals. The required word-boundary question is not permission to import an NLP pipeline. |
| embed/, vocab/ | Named-as-forbid | D1/D4 and Piece 14: no authoritative side store or occurrence collapse. |
| graph/, gnn/ | Named-as-forbid | B11/C11: node-edge pictures are projections, not Core storage. |
| ntk/, train/ | Named-as-forbid | Staking: function ≠ construction; trained parameters are not the retained strand. |
| reag/, agent_errata/ | Named-as-forbid as Core doors | Holder diagnosis remains outside WORD/POINTER and the planned Core dependency set. |
| reidemeister/ | Named-as-forbid | Rule Zero prohibits rewriting recorded history. |
| star_table/ | Named-as-forbid | A12/D5 preserve every pass; the star is read from the strand. |
| emit_pointer/, emit stub | Named-as-forbid in this plan | Binary Join mechanics are already a draft-law premise; a folder must not invent the still-open arbitrary selector, bypass acceptance, or become a convenience emission owner. |
| codec/, weld/, utils/ | Named-as-forbid as convenience doors | Do not revive rejected history machinery or hide a second task under a generic name. |
| Arrival Surface that owns a vocabulary or document history | Leak-risk | Contract II depends on I; it is not a second authority. |
| view cache that owns participation facts | Leak-risk | A disposable label does not admit a forbidden graph, definition, or Star store. |
| HCC-A / Cup / Water / trainA as Core dependencies | Named-as-forbid | HCC-A, Cup and Water are conceptual reference documents; trainA and the HTML renderer interpret concepts. They establish no required holder/runtime components and do not decide Graphic D rows ([live-law correction](../law-why-these-documents.md#conceptual-reference-documents)). |

These are exclusions from this planned Core workspace, not claims that external books contain no such machinery. No attached PDF or adjacent framework is imported.

## PAGE F — INTUITIVE STRUCTURE CHECKLIST, WITH EVIDENCE STATES

| Prompt requirement | Current result |
|---|---|
| Names map to the shoe | Editor association recorded on Page A; human recognition/physical tests NOT_PERFORMED. |
| One folder ↔ one Piece or one proved task | REFINED: preserve the seven one-task doors, while Pieces remain coupled within one Core contract. No task is yet proved. |
| Each branch README states its one task | Task sentences are written in this plan and the companion cards; future branch READMEs do not exist. |
| Forbidden states unrepresentable through types | UNESTABLISHED. Prose exclusions are obligations; no admitted Rust types or capability boundaries have been implemented. |
| Every crate is no_std; allocation only under its lawful gate | Required, NOT_BUILT. No crate or effective dependency closure has been verified. |
| Views cannot write the strand | Required, NOT_IMPLEMENTED. A read-looking method name is insufficient evidence. |
| core/ delegates without hidden branch logic | Required, NOT_IMPLEMENTED. Exact composition proof remains open. |
| Source graphics and law remain authoritative | OBSERVED for this documentation change; every Piece is cited on Page C. |
| Emission module absent | OBSERVED. The law draft is reconciled but unaccepted; arbitrary relation/sample selection remains open. No implementation module is authorized. |
| No foreign Cargo dependencies | No Cargo workspace or proposed crate directories exist. Future dependency compliance is UNESTABLISHED, not a completed build check. |

Two language facts prevent false green boxes. Rust's no_std attribute changes automatic library/prelude behavior; it does not itself prevent explicit or transitive linkage of std. The future effective build must be inspected under the repository's stricter law. [Rust Reference: no_std](https://doc.rust-lang.org/reference/names/preludes.html#the-no_std-attribute).

Shared references can also permit mutation through interior-mutability types. Therefore, a read-only view requires a proof about all reachable mutation paths, not merely a shared-reference signature. This is a counterexample to an insufficient check, not a proposal to use those types for Lace history. [Rust core::cell documentation](https://doc.rust-lang.org/core/cell/index.html).

Allocation stays absent from the seven births. A demonstrated need does not automatically admit a stock allocation library or std. It would require the law's separately stated and proven first-party allocation task and an explicit revision of the plan.

## PAGE G — BRANCH BIRTH ORDER

The following is a **serial proposal**, matching the companion contracts. All seven proof statuses are **UNESTABLISHED**, and none is currently eligible. Each row requires the global gate and discharge of the preceding task before another branch exists.

| Birth / door | One task | Required proof shape, not a claimed proof |
|---|---|---|
| 1 strand/ | Retain the append-only line. | For an independently admissible immutable section and sufficient declared resources, successful extension adds one fresh position and preserves every earlier index/content pair. Include payload stability, empty start, index bounds, and failure scope. Depend on no later birth. |
| 2 word/ | Preserve the WORD value of one occurrence. | Show preserved content and distinct occurrence positions even when values compare equal. State the representation/equality domain; choose no normalization or interning default. |
| 3 pointer/ | Constrain the binary POINTER / Join layout. | Under eventual human acceptance, every admitted Join has exactly two ordered earlier same-line operands and both WORD/POINTER kinds remain expressible. The card must not choose arbitrary operands or infer semantic subject/order. An unusable empty type is not a successful proof. |
| 4 route/ | Continue through the one append door. | Given an independently admitted section, refine one authorized extension while preserving prefix and supplied order. Compose exact predecessor contracts; storage extension does not prove the missing emission decision. |
| 5 view_star/ | Read retained passes as stars. | Establish non-writing behavior, soundness, and coverage over an explicitly stated identity/read domain. Preserve distinct indices; removing the view loses no authoritative fact. |
| 6 view_proj/ | Present a read-only shadow. | Trace displayed facts to retained indices or declared view conventions; show no write-back through any exposed path. A lossy picture is not a round-trip or topology proof. |
| 7 core/ | Compose only the proved doors. | Establish every callee's preconditions and preserve postconditions, resource bounds, and capability limits at exact proved revisions, with no second store or hidden policy. |

A storage proof must establish more than increasing a length counter. For a prefix L of length n and an independently authorized new section e, the intended successful result is L followed by e, with every earlier L[i] unchanged for i < n. This states the preservation obligation; it neither proves an implementation nor selects e, its kind, or an emission event.

The finite resource domain must be explicit. Overflow, exhaustion, interruption, restart, input custody, and concurrent ordering cannot be answered by deleting history, silently losing an arrival, wrapping an index, or declaring false success. This plan chooses no allocator, queue, persistence scheme, atomicity rule, or numerical capacity.

Before graduation, each task needs its exact contract and revision, implementation within that one task, evidence that forbidden states cannot occur through its admitted surface, and a proof covering the claimed domain. Tests, examples, model agreement, and a compiling build are distinct evidence. Any additional human requirement for a written proof note must be identified and met. A moving branch name is not a proved dependency.

**No parallel branch fan-out. No scaffold branch as a placeholder. Core composition is last.** If a necessary premise remains unresolved, that birth remains ineligible; do not satisfy the ordering by assuming a later branch has already solved it.

## PAGE H — ANYTHING THAT LOOKS LIKE “WHEN TO POINT”

| Boundary | What is established | What remains open |
|---|---|---|
| Backward references | D2/D3: targets are earlier existing positions on the same line. | The evidence/admission mechanism and choice of targets. |
| Target kinds | D3/D6, key properties, AGENTS.md, and Piece 5 allow earlier WORD or POINTER targets. | Permission does not select a particular target or cause an append. |
| POINTER representation | Current draft law: POINTER is binary Join(a,b), itself a retained section; both operands are earlier same-line WORD/POINTER points. | Concrete Rust representation/proof remains unimplemented; arbitrary operand selection remains open. |
| Ordered operands / semantic subject | D1's reference order is not uniformly chronological; current Join consumes an ordered selected pair. | What upstream relation assertion/selector supplies semantic operand order remains part of arbitrary selection. |
| Arrival and adjacency | Every successful arrival appends WORD; seen occurrence RootTouches root; adjacency alone is not a relation trigger. | Arbitrary non-root-touch relation/sample selection remains open. |
| Repetition / star reading | Repeated occurrences remain distinct; current law requires seen occurrence + RootTouch Join. | G1 identity policy remains open; arbitrary additional relation selection remains open. |
| Eventual participation | In the illustrated D1 prefix, all entries except the newest are referenced later. | Whether every entry must eventually be referenced; an example pattern is not a requirement. |
| C6's four relation families | C6 names dictionary, document, conversation, and provenance routes; D shows no family field. | The relation between those families and the retained schema; no discriminator, extra row kind, or separate engine is added. |
| route/ or core/ composition | May eventually realize independently accepted continuation and Join construction. | No arbitrary selector may be hidden in glue, an input constructor, or a type-only helper. |
| Read/identity/resource contracts | Their obligations are identified by the manifest and agent controls. | Their unresolved premises are not closed by accepting an unrelated filename. |

**Current constructor qualification, ROOT / TOUCH / FOLD Pass 5:** pointer-emission now reconciles binary Join, WORD/POINTER operands, backward same-line refs, RootTouch, and finite-sample grounding. Earlier target-kind and arity conflicts are no longer current blockers. **Arbitrary relation/sample selection, human acceptance, G1, and implementation proof remain separate obligations.**

The updated manifest also records a counterexample to the proposed rule that every consecutive WORD pair emits a POINTER. In D1's WORD subsequence, pairs 0001/0003 and 0005/0008 have no corresponding POINTER in the illustrated prefix. This bounds a candidate against the recorded example; it does not establish a replacement emission rule or settle whether adjacency itself is a relation. “Consecutive” here means consecutive WORD occurrences, not adjacent array indices. [Row-level reading, §3](../graphics-close-reading.md#3-emission-is-underdetermined-and-the-rows-prove-it).

The companion plan records a required outline at [pointer-emission-prompt.md](../prompts/pointer-emission-prompt.md). It was absent at the original audit revision and is present at the commit-integration base 6fb2dac. Its header explicitly calls for unanswered questions, keeps the output DRAFT, and states that writing it is not acceptance. The outline remains distinct from an exact accepted emission contract; this commit task does not execute it.

**DELTA, current:** physical Join construction is a reconciled draft-law premise; FALSE FRIEND if a type/folder is mistaken for acceptance or for the arbitrary selector. **SELECTION SETTLED? NO / [GAP]. IMPLEMENTATION AUTHORIZED? NO.**

## PAGE X — CROSS-CUT AND BOUNDED VERDICT

| Temptation | Required restriction |
|---|---|
| Seven folders imply seven stores | One strand authority; first-party proof boundaries do not split the physical model. |
| Dictionary/document processing is only a view | The same Arrival Surface feeds Core; views inspect what has already been retained. |
| Type-only POINTER means emission is settled | Layout validity and permission to append are different obligations. |
| Checked planning boxes count as guarantees | Mark requirements, observations, implementation, proof, and acceptance separately. |
| no_std guarantees no std or hidden mutation | Inspect the actual effective build and capability closure; the attribute alone is insufficient. |
| A proved prefix extension proves Lace | It proves only its stated storage property; emission, identity, and other applicable premises remain necessary. |
| A leaf needs an unlisted helper | Revisit both plans explicitly; do not add a convenience dependency or second task. |
| First-party label or a commit means accepted | Require the exact proof and applicable human acceptance evidence. |
| Allocator need permits an eighth concurrent birth | Preserve serial proof order and the full allocation gate; revise the plan before such work. |
| A future branch directory is harmless preparation | No src/, Cargo manifest, crate directory, code/type stub, or branch birth before the gate. |

**Propose:** retain the seven names/order/dependency edges, with strand/ as the one retained-state implementation and views as dependent readings.

**Attack:** try a mutable alias to old WORD content, a reused index, an external reference with an in-range number, a fixed pair inferred from D2, chronological slot assignment, a view that can reach a writer, a second history behind a route, or an admission helper that chooses emission. Also challenge a claimed proof that depends on an unborn callee or succeeds only because no valid POINTER can exist.

**Resolve:** these remain explicit proof obligations and exclusions. The reading corrects the feed/view mapping and unjustified passed boxes; it does not establish that a concrete Rust design enforces the obligations. Review roles were performed by one agent, **not independent reviewers**.

## Source and task receipt

| Field | Observed scope |
|---|---|
| Task | Execute the Rust no_std crate-map prompt; one documentation output: `docs/plans/rust-nostd-crate-map.md`. |
| Initial base | `f995e4429ffc202be06dc6230423f3288edbb620`, branch main. |
| Re-anchor before editing | Fetched and fast-forwarded to `7cd385f1debd88c4eeb2fe847279ba91851cb927`. Only the branch-contracts document was added; the map baseline, prompt, graphics, manifest, laws, controls, reason map, and emission draft are unchanged. The new companion was read in full before editing. |
| Completion re-anchor | A final fetch advanced main to `490473e7e3d3e1579da7ea0361d5c1654360e648`. Fast-forwarded while preserving local drafts. Reopened all four replacement lossless graphics, read the full new close-reading document, and reviewed manifest and companion-contract changes. Incorporated slot assignment, eventual-reference and C6-family gaps, plus the bounded adjacency counterexample. The assigned prompt and map baseline remain unchanged. |
| Commit re-anchor | The human's later instruction to commit pending work brought main to `6fb2dacc4d25460bd000cf43e4700cb9949e6b77`. The controlling sources, companion contracts, and emission draft are unchanged since the completed audit. The newly present emission-outline prompt's header was checked and its status updated above; no new prompt or implementation task was run. |
| Prior map identity | Pre-edit SHA-256: `db348c0cb6d5de88256a43dd9dd395e8bd984be25275de7941ae76ae4f92303b`. |
| Existing work | Preserve modified AgentScope/five-stake clipboards and untracked atomic-primitives/Petersen–Zech drafts. The companion branch contracts are separately committed work and are not edited here. |
| Source coverage | Full prompt and map; graphics index and all four canonical PNGs; manifest Pieces 1–15, composition, I/II/III seams and open questions 1–11; standing implementation law and controls; reason-map anti-import boundaries; full companion contracts and emission draft; full row-level close reading, with its historical-report claims kept separate from source authority. |
| Physical / visual evidence | Graphics A/B/C/D VISUALLY_OBSERVED during this walk. Physical shoe-lace operation and stranger-name test NOT_PERFORMED. |
| Adjacent sources | No PDF is imported or used to settle a Core rule. Rust reference pages support only the bounded language cautions on Page F, not Lace semantics. |
| Repository inspection | Proposed seven directories, src/, and root Cargo.toml are absent before this edit; no implementation or manifest is authored by the task. |
| Permission / proof / enforcement | Documentation work authorized. Exact emission acceptance is not established; all future task proofs UNESTABLISHED. No compiler, runtime, physical test, or proof checker was run. These plans remain ADVISORY controls. |
| Open differences retained at the original receipt | Emission draft's target-kind question versus D3/D6; manifest Piece 13's disposable-database wording versus the stricter live-law prohibition. No forbidden store is admitted by calling it a view. The target-kind item is superseded by the current Q4 qualification above. |
| Completion boundary | All nine prompt sheets and all fifteen Pieces are mapped, with seven conditional births and their proof obligations. This is a completed planning artifact, not permission to implement. |

**Next narrow question:** after explicit human acceptance of the reconciled law, what task-specific premises and ownership rule for the still-open arbitrary relation/sample selector are required before any proposed birth can become eligible? This map does not answer that question.

**STOP.** Commit only on the human's instruction. This run does not start another prompt or create a branch.
