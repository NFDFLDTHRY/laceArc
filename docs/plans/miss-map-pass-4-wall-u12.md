# Miss-map — Pass 4 · wall / U12 leftovers

**Status:** **EXECUTED** 2026-09-20. **Station:** maps (`Grok-maps`).
**Ticket:** [`miss-map-pass-4-plan.md`](miss-map-pass-4-plan.md).
**Cite:** W1–W6 · U1–U7 · rust-target G-rows.
**Emission:** `[GAP]`. G-gates still `NOT_RUN`. No `src/`.

Unrun checks are missing carrier facts. Not missing stitches.

---

| Item | Mark |
|---|---|
| W1–W6 predicates | **ALREADY-MAPPED** |
| U1–U6 predicates | **ALREADY-MAPPED** |
| U7 Cargo silences (`compiler_builtins`, lockfile, `build-std-features`) | **U12 hole** — not H-PTR |
| rustc G1–G10 `NOT_RUN` | **U12 incomplete** — still not I-EQ |
| OPEN #13 law custody vs W5 \(h\) | **OUT-WITH-CAUSE** as I-EQ · **distinct** from session \(h\) · law docket |
| U6 Runtime vs renderer PAUSED | **ALIAS** of U6 predicate + **kit miss** (PAUSED is station, not a new equation) |
| OPEN #15 seed corpus | **HOLE** / G1 companion — **not U12** |
| “wasm64 opens Implementation Law” | **ALREADY-MAPPED** refused |

No G-gate flipped to RUN. No WallCalc/U12Calc enlarged.

## `P4-R`

| | |
|---|---|
| Builds | **0** |
| H-PTR filled | **no** |
| Next | pass 5 Q × \(\theta\); **not opened** |

**Emission `[GAP]`. Shoe in hands.**
