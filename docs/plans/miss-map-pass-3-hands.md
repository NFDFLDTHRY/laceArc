# Miss-map — Pass 3 · Hands

**Status:** **EXECUTED** 2026-09-20. **Station:** maps (`Grok-maps`).
**Ticket:** [`miss-map-pass-3-plan.md`](miss-map-pass-3-plan.md).
**Cite:** prefix-device D2 = WORD/POINTER shape; `pointer-emission.md` Slot II.
**Emission:** `[GAP]`. Slot II unanswered. No fake D2 array. No `src/`.

D2 is a **panel**, not an 11-row specimen.

---

| D2 claim | Equation | Mark |
|---|---|---|
| Two kinds: WORD \| POINTER | S1.3 Kind | **ALREADY-MAPPED** |
| WORD carries a variable / value | Arrive / Piece 4 | **ALREADY-MAPPED** |
| “The variable is the word” | Slot II Hands side | **IN-CALC-MISS** — CONFLICT with spoken cluster |
| POINTER = refs to earlier cells | \(\Phi_{\mathrm{morphism}}\) shape | **ALREADY-MAPPED** |
| Minimal structure; no third type | S1.3 | **ALREADY-MAPPED** |

Spoken Slot II (word = defining pointer cluster) stays **IN-CALC-MISS**. Not assigned. Not \(\theta_{\mathrm{G1}}\). Not \(\theta_{\mathrm{WORD2}}\).

Remaining Hands holes already named: \(\theta_{\mathrm{WORD2}}\), \(\theta_{\mathrm{G1}}\), \(\theta_{\mathrm{reqPTR}}\), \(\theta_{\mathrm{left}}\), Carrier/Hopper I-letters.

SPECIMEN-MISS on D2 is **closed as a misread**: nothing to L-tag. The miss that remains is Slot II CONFLICT, not an untagged array.

## `P3-R`

| | |
|---|---|
| Fake D2 walk | **0** |
| Slot II value | **not chosen** |
| Next | pass 4 wall/U12 misses; **not opened** |

**Emission `[GAP]`. Shoe in hands. A cluster you see later is not that variable row.**
