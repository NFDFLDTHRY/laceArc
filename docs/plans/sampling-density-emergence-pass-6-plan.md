# SAMPLING / DENSITY / EMERGENCE campaign - Pass 6 PLAN

**Status:** PLAN - NOT EXECUTED.
**Name:** SAMPLING CAMPAIGN RECONCILIATION / PROPAGATION
**Planning station:** maps (Astra-sampling-reconcile).
**Pinned main before claim:** 17236ca14c8278e384b0ac8fa5d3f0762523c1c7.
**Maps planning claim:** 77741513cc78fcabc32f72ec8661872cb49efaab.
**Depends on:** Sampling / Density / Emergence Passes 1-5 EXECUTED; ROOT / TOUCH / FOLD Passes 1-6 retained as dated history; current coordination/staking law.
**Pass shape:** one complete pass. No subpasses.
**Implementation:** none. No src/. No Cargo. No Rust.
**Acceptance:** absent unless the human separately supplies an exact acceptance sentence during execution.

Pass 6 is not another sampler/search experiment.

Passes 1-5 have already established the active model strongly enough that the remaining problem is repository inconsistency.

The repository currently has two incompatible active stories.

OLD active wording:

\[
\operatorname{Origin}(\rho)=[GAP]
\]

with:
- arbitrary non-root relation assertion;
- selector / chooser provenance;
- selected sample treated as an upstream unexplained input.

NEW human-ruled and tested architecture:

\[
\boxed{
\text{1D Lace}
\to
\text{derived 2D sampling}
\to
\text{recurring / cross-scale structural evidence}
\to
\text{sparse retained POINTER topology}
}
\]

with star/search as disposable derived reads.

Pass 6 reconciles the active repository to the second model while retaining the first as historical evidence.

---

## 0. Current campaign facts that Pass 6 may propagate

### Human rulings

Pass 6 must preserve the exact human amendment chain already recorded in Passes 1-5.

Key direct human rulings include:

> “The sampling itself. You don't choose the relationships, you find them over time as lace grows.”

> “It allows a pointer to be place between any size of relationship so a short paragraph or sentence can link to the corpus of priors say apples. They always occur in certain patterns by usage patterns and these patterns can link to any layer of the n+1 samplings as it's the pointers that are the relational map.”

> “Entries and pointers get laid down on the 1D array, the sampling creates the 2D array enabling pointers at any level.”

Pass 6 may quote these exact sentences or cite the campaign findings that preserve them.

It must not manufacture stronger human wording.

### Proven campaign architecture

Passes 1-5 establish at maps level:

1. WORD and POINTER retained history stays on one append-only 1D Lace.
2. Sampling creates a derived 2D position-by-scale working surface.
3. The 2D sampling surface is not a second authoritative store and is not merely a Layer-III picture.
4. POINTERs can relate addressable structures across different sampling levels.
5. Dense 2D sampling can coexist with sparse 1D retention.
6. Exact contiguous recurrence alone is not the complete retention rule.
7. Star search is a read-only layered view:
   - lexical;
   - anchored repeated patterns;
   - retained POINTER paths;
   - explicit neighboring-star expansion.
8. Flat transitive star closure saturates and loses anchor shape.
9. Search should preserve explicit structural frontiers and witness paths.
10. Search results can be disposable replay handles.
11. Minimal selected-result handle:
   \[
   SearchHandle=(through,anchor,item).
   \]
12. Local anchor-cone reconstruction can reproduce search pattern frontiers without materializing the full 2D surface.
13. Retained pointer participation can be reconstructed by a forward Lace scan without a correctness-critical reverse index.

These are reconciliation inputs, not implementation authorization.

---

## 1. What is still OPEN after Pass 5

Pass 6 must not erase real gaps while removing the wrong chooser gap.

### R6-G1 - exact sampling-derived retention rule

The campaign does not establish a complete universal rule:

\[
\boxed{
\text{When exactly does observed sampling structure cause a new retained 1D POINTER?}
}
\]

Refuted universal rules include:
- every sampled cell retained;
- every adjacent pair retained;
- exact-repeat-only retention;
- first-seen-every-cell materialization;
- cheapest contiguous grounding.

Therefore the current active gap must become:

> **sampling-derived retention / materialization condition**

not:

> arbitrary semantic relation-assertion provenance.

### R6-G2 - ephemeral sample to addressable operand

If a relationship to retain involves a 2D sample not already represented by a Lace point:

\[
\boxed{
\text{how is that sample grounded / resolved into an addressable 1D operand when retention requires it?}
}
\]

D1 never exhibits a POINTER referencing an unaddressed cell, so this remains OPEN.

### R6-G3 - G1 word equality / tokenization

Still OPEN.

Bounded campaign tokenization for Coffee Cup was analytical only.

### R6-G4 - general traversal / geometric presentation

Star-search structural reads are now mapped.

That does not automatically close every older generic traversal, geometry, coordinate, route-family or UI question.

Do not convert specific star-search results into a universal traversal law.

### R6-G5 - human acceptance

Still ABSENT.

Reconciliation is not acceptance.

No src/Cargo/Rust implementation gate opens merely because active docs are repaired.

---

## 2. Central reconciliation statement

Every active current-law/model/control surface should converge on this distinction:

\[
\boxed{
\text{Relationships are not semantically chosen first.}
}
\]

\[
\boxed{
\text{Growing Lace is sampled across scale; recurrent/overlapping structure becomes discoverable.}
}
\]

\[
\boxed{
\text{POINTER topology is the retained relational map.}
}
\]

\[
\boxed{
\text{The exact general sampling-derived retention/materialization rule remains OPEN.}
}
\]

This replaces the current active framing:

\[
Origin(\rho)=?
\]

without deleting the historical work that produced it.

---

## 3. One-pass station choreography

Pass 6 is one pass with sequential station ownership.

No station may be edited without its own claim.

Execution order:

    live main / station census
              |
              v
            law
              |
              v
          graphics
              |
              v
          prompts
              |
              v
            kit
              |
              v
        clipboards
              |
              v
            maps
              |
              +--> math/model maps
              +--> Rust planning maps
              +--> global systems ASCII LAST
              +--> findings / index / final sweep
              |
              v
          release maps

The order is deliberate.

The global systems ASCII is updated only after the subordinate source/control/clipboard surfaces have been reconciled so it does not summarize stale state.

---

## 4. Pre-write census

Before the first content edit, execution must refresh main and search active current surfaces for at least:

- Origin(rho)
- Origin(ρ)
- arbitrary non-root relation
- arbitrary relation/sample selection
- selector
- chooser
- selection remains [GAP]
- selection remains open
- emission [GAP]
- 2D plus view / projection
- star plus view
- query/write
- read/traversal open

Every hit must be classified:

### CURRENT-STALE
Active current statement that contradicts the sampling campaign.

### CURRENT-STILL-TRUE
Active current statement that remains valid.

### HISTORICAL-RETAIN
Dated receipt / old campaign body.

### SOURCE-SILENCE-RETAIN
Clipboard/source finding saying a particular external source does not supply Lace machinery.

### SEPARATE-GAP
Valid unrelated open question such as G1, route-family boundaries, generic geometry.

No global search-replace is allowed.

---

## 5. Law station target

Claim law first.

Primary files:

- docs/pointer-emission.md
- docs/systems-manifest.md
- AGENTS.md
- CLAUDE.md
- docs/law-why-these-documents.md
- README.md
- docs/README.md

Other law-owned files only if the live census proves stale current wording.

### 5.1 pointer-emission.md

Update the current reconciliation surface only.

Required changes:

1. authority section gains the sampling campaign and direct human amendments;
2. current remaining gap is reclassified from arbitrary relation-assertion provenance / Origin(rho) to exact sampling-derived retention/materialization rule;
3. state explicitly:
   - no arbitrary semantic chooser is intended;
   - 1D rows are authoritative;
   - sampling creates a derived 2D working surface;
   - cross-scale pointering is allowed;
4. distinguish:
   - sampling observation;
   - retained pointer append;
   - read/search projection;
5. preserve RootTouch as the closed special case;
6. keep binary Join constructor unchanged;
7. preserve no hidden Contract-II relation payload;
8. preserve no external write/query interface as an established Core mechanism;
9. qualify any no-query wording so maps-level derived read/search operations are not mistaken for a Core write API;
10. preserve acceptance = ABSENT.

### Historical fence

Do not rewrite:
- historical Pass-7 Answer lines;
- ROOT / TOUCH / FOLD Pass-6 findings;
- old propagation dockets;
- dated chooser/provenance reasoning.

Instead mark the old Origin(rho) result as:

> HISTORICAL - correct narrowing of the then-assumed arbitrary-assertion question, superseded as the active problem by later human sampling rulings.

### 5.2 systems-manifest.md

Reconcile the current pieces without renumbering them unless absolutely forced.

Required model distinctions:

#### Star

Update Piece 7 current description to include:
- star as accumulated root/occurrence/retained-participation formation;
- star as a derived search anchor;
- not a stored Star row/table.

#### Touch / participation

Replace current arbitrary relation assertion provenance [GAP] coupling with:
- sampling-derived relationship discovery;
- exact retention/materialization rule OPEN.

#### 2D sampling vs dimensional views

Do not rewrite Piece 12 into a new store.

Add an explicit cross-piece distinction.

**Derived 2D sampling working surface**
- position x scale;
- reconstructible;
- may supply structural evidence used by retention machinery;
- not authoritative persistent storage;
- not a Layer-III viewer write-back.

**Graphic C / N-D presentation**
- still a read/projection of retained participation;
- still not a coordinate store;
- still not meaning.

Do not claim Graphic C itself specifies the sampling algorithm.

H11 is a human amendment, not a recovered Graphic C quote.

#### Search

Record only derived search behavior:
- star entry;
- recurrence DAG/frontier;
- pointer witness paths;
- neighboring-star expansion;
- SearchHandle replay concept.

Do not define a public API or persistent index.

### 5.3 law entrypoints / controls

AGENTS / CLAUDE / law explanation / README entrances must stop telling agents:

> arbitrary relation/sample selection is the active current gap.

Replace with:
- sampling architecture is current;
- retention/materialization rule remains open;
- G1 remains open;
- search is derived/read-only;
- acceptance remains absent.

No entrypoint may imply Pass 6 reconciliation equals human acceptance.

Release law.

---

## 6. Graphics station target

Claim graphics.

Do not edit PNG/source images.

Reconcile:
- docs/graphics-close-reading.md
- current ASCII companion qualifications where they contain active current-gap summaries.

Required distinction:

### Source reading

Graphic C still shows:
- 1D presentation;
- 2D crossings/adjacency;
- 3D looping/stars;
- N-D participation views;
- projection is not Lace.

### Human sampling amendment

The campaign's 2D sampling surface is:
- later human-ruling machinery;
- compatible with one-store constraints;
- not claimed as text recovered from C3.

The graphics surface must not say:
> every 2D object is only Layer III visualization

in a way that erases the sampling working surface.

It also must not say:
> Graphic C proves the sampler.

Preserve source authority honestly.

Release graphics.

---

## 7. Prompts station target

Claim prompts.

Update current mandatory/control prompts that agents actually enter through, especially:
- docs/prompts/context-pass.md
- live pointer-emission/reconciliation prompts if they remain current entrypoints.

Current story must become:

    word arrival -> root / RootTouch
        |
        v
    append-only 1D Lace
        |
        v
    derived 2D sampling across position x scale
        |
        v
    recurring / overlapping structure becomes discoverable
        |
        v
    exact general retention/materialization rule OPEN
        |
        v
    retained POINTER topology
        |
        v
    derived star/search frontiers

Prompt fences:
- do not invent the retention rule;
- do not reintroduce arbitrary semantic chooser;
- do not call search results authoritative state;
- do not treat reconciliation as acceptance.

Dated prompt-creation instructions remain dated if clearly historical.

Release prompts.

---

## 8. Kit station target

Claim kit.

Update current steward qualifications / READ_ME_FIRST / role controls that currently say:
- arbitrary relation/sample selection remains the active gap.

Preserve:
- verbatim transported human-law blocks;
- historical commit observations;
- source receipts;
- acceptance gate language.

New current qualification must say:
- chooser framing superseded by sampling rulings;
- exact retention/materialization rule remains open;
- star/search is derived and disposable;
- implementation remains forbidden absent exact acceptance.

No kit text becomes Core machinery.

Release kit.

---

## 9. Clipboards station target

Claim clipboards.

This is a selective qualification pass, not source rewriting.

### Preserve external-source silence

If a book clipboard says:

> this book does not supply POINTER timing / arity / adjacency

that may remain true and must remain as a source finding.

Do not rewrite silence into a Lace rule.

### Update stale current-Lace qualifications

Where a clipboard uses the old source silence to summarize the repository's current state as:
- emission [GAP]
- arbitrary selection [GAP]
- POINTER when unresolved

update the qualification to:

> source remains silent; current Lace separately uses human-ruled sampling architecture, while the exact sampling-derived retention/materialization rule remains open.

### Rust / toolchain clipboards

Any current implementation-readiness note must preserve:
- no implementation gate opened;
- binary Join / root-touch settled;
- sampling architecture current;
- retention/materialization open;
- SearchHandle is derived maps-level analysis, not accepted ABI/API;
- no second store;
- no foreign dependencies;
- no src/Cargo.

Do not add search crates, indexes or caches.

Release clipboards.

---

## 10. Maps station target

Claim maps last.

Primary active surfaces:

- docs/math-execution-model.md
- docs/reason-model-map.md
- docs/atomic-primitives-map.md
- docs/plans/rust-nostd-crate-map.md
- docs/plans/rust-nostd-second-reading.md
- docs/plans/rust-nostd-branch-contracts.md
- docs/systems-manifest-ascii.md
- docs/plans/README.md
- this Pass-6 plan/findings

Other maps-owned active hits only if census proves stale.

### 10.1 math-execution-model.md

Replace active Origin(rho) [GAP] with the new active mathematical seam.

Add / reconcile:

**authoritative 1D history**

\[
L=(WORD|POINTER)^*
\]

append-only.

**derived sampling surface**

\[
S[n,t]
\]

with:
- exact bounded candidate from Pass 2;
- clear stamp that exact universal sample identity/tokenization remains constrained by G1;
- dense working surface need not be stored.

**sampling vs retention**

Explicitly separate:
- sampled cell;
- recurrence / overlap evidence;
- retained relation.

Mark retention/materialization rule OPEN.

**cross-scale addressability**

POINTER operands need not have equal n.

No n field is required in POINTER by current evidence.

**star/search read**

Record:
- layered StarSearch;
- frontier/path preservation;
- local cone reconstruction;
- forward-scan pointer read;
- minimal SearchHandle replay concept.

These are derived reads, not Core row types.

**historical Pass-6 note**

Preserve the old Origin(rho) analysis as dated history with a pointer to the new reconciliation.

### 10.2 reason-model-map.md

Update the current-law qualification.

OLD:
> remaining Core-adjacent GAP is arbitrary non-root relation/sample selection.

NEW:
> source books still do not supply Lace's sampling/retention machinery; current human law separately establishes sampling as the discovery mechanism; exact retention/materialization remains open.

Preserve book-source conclusions.

### 10.3 atomic-primitives-map.md

Update current star-internal-traffic and relation-gap lines:
- sampling discovery current;
- retained POINTER overlay sparse;
- retention/materialization open;
- star search derived;
- no stored Star/index.

Do not promote search to primitive storage.

### 10.4 Rust no_std planning

Update crate map / second reading / branch contracts.

Remove as active blocker:
- arbitrary semantic relation/sample selector ownership.

Replace with:
- exact sampling-derived retention/materialization premise not yet accepted/closed;
- G1 identity still open;
- SearchHandle/search expansion is maps-level derived read analysis, not an implementation contract;
- no Core crate/card authorized by this reconciliation.

Do not create new crate cards in Pass 6.

Do not reinterpret existing view_star/ or view_proj/ cards as accepted implementation.

### 10.5 global systems ASCII - LAST content edit

docs/systems-manifest-ascii.md is updated after all station-owned consumers above.

It must show the reconciled whole-system flow:

    ARRIVAL
       |
       v
    1D LACE
    WORD / POINTER
       |
       +--> root / RootTouch
       |
       v
    DERIVED 2D SAMPLING
    position x scale
       |
       +--> recurrence / overlap / multi-scale ridges
       |
       v
    [OPEN] retention / materialization condition
       |
       v
    retained POINTER topology
       |
       +--> POINTER -> POINTER / cross-scale participation
       |
       v
    DERIVED STAR SEARCH
       |
       +--> lexical frontier
       +--> repeated-pattern frontier
       +--> pointer witness paths
       +--> neighboring-star frontier
       |
       v
    disposable SearchHandle
    through + anchor + item

Hard fences visibly present:
- 2D sampler is not a second authoritative store;
- sampler is not Graphic C proof;
- Layer III visualization cannot write Lace;
- search cannot write Lace;
- SearchHandle is not stored memory;
- exact retention rule OPEN;
- acceptance ABSENT;
- no src/Cargo.

The global ASCII must distinguish:

1. derived working 2D sampling surface; and
2. read-only 2D/3D/N-D visualization/projection.

Do not collapse both under the word view.

---

## 11. HCC-A / Coffee Cup / Water conceptual-reference fence

Pass 6 must preserve the project correction:

- HCC-A;
- The Coffee Cup;
- Where the Water Is Loud

are conceptual reference documents / behavioral-goal sources.

They are not:
- holder machinery;
- runtime dependencies;
- literal Core modules.

The Coffee Cup was used in Passes 2-5 as a prose stress corpus.

That use does not demote or promote its conceptual-reference role.

Pass 6 may cite it as:
- conceptual behavioral goal source;
- bounded prose test specimen.

Do not make it a Lace dataset dependency.

Global ASCII / control surfaces must keep those roles separate.

---

## 12. Source authority stamps

Every Pass-6 change must identify claim class.

### R - direct human ruling
H7-H12 and current explicit corrections.

### P - graphics / D1
What A-D actually show.

### M - maps-level mathematical result
Passes 1-5 derivations/tests:
- 2D candidate lattice;
- recurrence ridges;
- star-search frontier;
- SearchHandle;
- local reconstruction.

### HIST
Old chooser/provenance campaigns.

### SOURCE-SILENT
External reference was checked and does not supply mechanism.

### OPEN
Retention/materialization; G1; general unresolved implementation premises.

### ACCEPTANCE
ABSENT unless separately provided by human.

No M result may be rewritten as a graphic quote.

No SOURCE-SILENT result may be rewritten as current Lace ignorance if human rulings supersede that ignorance.

---

## 13. Current-gap replacement table

Pass-6 findings must carry this cross-surface mapping:

| Old active phrase | New active phrase | Historical treatment |
|---|---|---|
| arbitrary relation/sample selection [GAP] | sampling discovery current; retention/materialization [GAP] | old phrase retained in dated receipts |
| Origin(rho) [GAP] | exact sampling-derived retain/materialize condition [GAP] | Pass-6 provenance finding retained |
| Core chooser not established | semantic chooser rejected as intended model; sampler discovers structure | old negative remains historical |
| external assertion interface not established | still no Core relation write API; derived search is read-only | preserve |
| 2D is only projection | Graphic-C 2D projection remains view; human-ruled 2D sampling surface is derived working machinery | distinguish |
| star = view only | star remains derived formation/view and now has mapped search behavior | no stored Star |
| query/write not invented | no Core write/query API; derived read expansion operations exist at maps level | distinguish |
| read/traversal behavior open | general traversal may remain open; star-search structural reads mapped | narrow |

This table is a reconciliation receipt, not law by itself.

---

## 14. Stale-wording post-sweep

After all content edits, search again.

### Must disappear from active current surfaces

Unqualified current statements that:
- arbitrary non-root relation/sample selection is the active primary gap;
- Origin(rho) is the active current campaign question;
- every 2D representation is merely a UI projection;
- star search behavior is wholly open;
- search requires a stored Star table;
- SearchHandle is authoritative storage.

### May remain

- historical plans/receipts;
- explicit old-wording examples;
- external-source silence findings;
- implementation acceptance blocks;
- exact retention/materialization OPEN;
- G1 OPEN;
- general geometry/traversal OPEN where not closed by star-search work.

Every remaining stale-looking hit must be classified in findings.

---

## 15. Acceptance / implementation gate

Pass 6 is reconciliation only.

Even after successful propagation:

\[
\boxed{
\text{pointer-emission draft is still NOT human-accepted unless explicitly accepted.}
}
\]

No:
- src/;
- Cargo.toml;
- Rust;
- executable sampler;
- search index;
- cache implementation;
- ABI;
- crate branch;
- implementation-law promotion.

Rust planning may become less wrong, not more authorized.

---

## 16. Pass-6 falsifier board

| ID | Falsifier | Consequence |
|---|---|---|
| **F6-1** | execution claims retention rule is fully solved | reject reconciliation |
| **F6-2** | Origin(rho) remains active current gap after law update | incomplete |
| **F6-3** | semantic chooser reappears under new name | reject |
| **F6-4** | every sample is implied to become retained | reject |
| **F6-5** | exact recurrence becomes universal retention trigger | reject |
| **F6-6** | 2D sampler becomes authoritative second store | reject |
| **F6-7** | 2D sampler is demoted to mere Layer-III picture | reject H11 |
| **F6-8** | Graphic C is claimed to specify the sampler | source overclaim |
| **F6-9** | N-D projection is promoted to storage | reject |
| **F6-10** | star becomes stored object/index | reject |
| **F6-11** | derived search operations are described as Core write API | reject |
| **F6-12** | SearchHandle becomes persistent truth | reject |
| **F6-13** | source-silence clipboard text is rewritten as if source taught sampler | reject source corruption |
| **F6-14** | external book silence is used to reopen human-ruled sampling architecture | reject stale qualification |
| **F6-15** | HCC/Coffee/Water become holder/runtime machinery | reject |
| **F6-16** | global ASCII updated before subordinate current surfaces and drifts from them | reject ordering |
| **F6-17** | Rust planning treats reconciliation as implementation authorization | reject |
| **F6-18** | acceptance inferred from commits/pass status | reject |
| **F6-19** | historical chooser/provenance receipts are deleted/rewritten | reject history loss |
| **F6-20** | code/src/Cargo/Rust appears | STOP |

---

## 17. Proof board

Execution must produce:

1. pre-write current/stale/history/source-silence census.
2. exact H7-H12 ruling record in findings.
3. law station reconciliation.
4. pointer-emission current gap replaced correctly.
5. systems-manifest sampler vs projection distinction.
6. acceptance remains absent.
7. graphics source/human-amendment distinction.
8. prompts current story reconciled.
9. kit current controls reconciled without touching verbatim law block.
10. clipboard source-silence/current-law distinction preserved.
11. math-execution-model reconciled.
12. reason-model current qualification reconciled.
13. atomic primitives current qualification reconciled.
14. Rust no_std planning reconciled without new cards/code.
15. global systems ASCII updated last.
16. global ASCII shows sampler vs projection distinction.
17. HCC/Coffee/Water roles remain conceptual behavioral-goal references.
18. post-write stale-wording sweep classified.
19. 20 falsifiers reported.
20. final tree has no src/Cargo/Rust and all stations released.

Target:

\[
20/20.
\]

---

## 18. Required findings product

Create:

docs/plans/sampling-density-emergence-pass-6-findings.md

It must contain:

1. live-base/station receipt;
2. human ruling record;
3. active old-vs-new gap table;
4. pre-write census;
5. law reconciliation summary;
6. pointer-emission exact current rule after reconciliation;
7. manifest piece/cross-piece reconciliation;
8. graphics qualification;
9. prompts/kit/clipboards propagation;
10. math/model/map reconciliation;
11. Rust planning reconciliation;
12. global systems ASCII reconciliation;
13. conceptual-reference role check;
14. acceptance/implementation gate check;
15. post-write stale wording census;
16. falsifier board;
17. proof board;
18. exact residual OPENs;
19. changed-file/station ledger;
20. recommendation for next work, without opening another pass automatically.

---

## 19. Expected post-Pass-6 current story

If execution succeeds, every active current surface should tell the same story:

    words arrive
        |
        v
    1D append-only Lace
    WORD + POINTER
        |
        +--> unseen -> root
        +--> seen -> RootTouch
        |
        v
    derived 2D sampling
    position x scale
        |
        v
    recurring / overlapping structure
        |
        v
    [OPEN]
    exact retention / materialization rule
        |
        v
    sparse retained POINTER relational map
        |
        v
    cross-scale / POINTER-to-POINTER participation
        |
        v
    derived star search
        |
        +--> structural frontiers
        +--> witness paths
        +--> local reconstruction
        |
        v
    disposable replay handle
    through + anchor + item

And separately:

    Graphic C / Layer III / N-D projection
        |
        v
    read-only presentation
        |
        X
    no write-back

---

## 20. What Pass 6 does not do

Pass 6 does not:

- solve the retention/materialization rule;
- solve G1;
- accept pointer-emission.md;
- implement the sampler;
- implement search;
- add cache/index machinery;
- add semantic ranking;
- add a second store;
- add a Lace ID for cross-Lace portability;
- create Rust crates/cards/branches;
- rewrite the graphics;
- rewrite external source findings;
- erase ROOT / TOUCH / FOLD Pass 6;
- create subpasses;
- automatically start another campaign.

---

## 21. Stop condition

Pass 6 stops only when:

- the current repo no longer frames arbitrary semantic selection/provenance as the active central gap;
- the exact real gap is retention/materialization;
- 1D authority / 2D sampling / cross-scale pointering are consistent everywhere current;
- sampling 2D is clearly distinct from projection 2D;
- star/search is clearly derived and disposable;
- source silence is preserved honestly;
- historical chooser/provenance work remains intact as history;
- law acceptance remains absent;
- global systems ASCII is updated after subordinate surfaces;
- stale-wording sweep is clean/classified;
- all stations are released;
- no implementation artifact exists;
- then STOP.

## Steward test

Do not erase the wrong turn.

Move it behind us.

The repository should be able to tell a new reader:

> We once asked who chooses the relation. The human clarified that the sampling finds structure as Lace grows. What remains open is not a chooser. It is the exact rule that turns sampled structural evidence into retained pointer structure.
