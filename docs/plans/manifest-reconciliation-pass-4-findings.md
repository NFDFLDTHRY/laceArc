# MANIFEST RECONCILIATION — Pass 4 findings

**Status:** **EXECUTED** 2026-09-21.  
**Campaign:** Manifest Reconciliation. **Pass:** 4.  
**Baseline:** Pass 3 verdict `FOUNDATION SOUND WITH EXPLICIT OPEN INTERFACES` at `02487dd1ada9761ed601f7f63c972325e561de72`.  
**Lab admission artifact:** [manifest-reconciliation-pass-4-lab-admission.md](manifest-reconciliation-pass-4-lab-admission.md).  
**Verdict:** **LAB ADMISSION ENVELOPE SOUND WITH CONDITIONALS.**  
**Lab:** inherited Lab Source Architecture Pass 4 remains **PAUSED** and unexecuted.

## 1. Result

The retention/materialization experiment can be isolated from the repository's other open questions without pretending those opens are solved.

The base bounded Lab needs:

```
D1 fixed token specimen
serial prefix-only execution
authoritative Graphic-D Lace
current L-M20 / SM-C20 sampling
one explicit target: SM-GAP-RM
```

It does **not** require global answers to:
- tokenization/normalization;
- concurrency/publication;
- projection geometry;
- residence/recovery;
- capacity/refusal custody;
- seed-corpus packaging;
- WebNN;
- multi-Lace/export;
- eventual reference.

Those questions remain globally open where they were open.

A candidate that actually depends on G1 normalization, route-family labels, L-M21 search, or ordered role semantics pulls that premise into its own admission record. The base Lab does not pay that debt on behalf of every candidate.

## 2. SM-GAP-RM is now a pointable three-face question

The manifest now exposes one GAP with three subquestions:

### RM-A — RETAIN?

What authority-backed condition says sampling-derived structural evidence should survive as retained Lace?

### RM-B — MATERIALIZE?

If a selected structure exists only as an ephemeral sample, how does it become an addressable 1D Lace operand so current L-M19/L-M05 machinery can use it?

### RM-C — ROLE / ORDER?

When a retained relation needs ordered Join operands, where do those roles come from without importing the D1 oracle's answer labels?

These are **not new mechanisms**.

No `L-M22`, `L-M23`, or new row kind was created.

A candidate may cover only some faces. Partial coverage is now a first-class result rather than an automatic failure or an excuse to fill the missing faces by editor judgment.

## 3. Current open-interface dependency classification

### LAB TARGET

- **SM-GAP-RM** only.

### LAB CONDITIONAL

- G1/tokenization: exact D1 token identity may be frozen; candidate-specific normalization pulls G1 into scope.
- document/conversation/provenance / C6 route-family labels: absent from base specimen; candidate use requires an explicit premise.
- traversal/query remainder: only pulled in by a candidate consuming SM-C21 search.
- operand role/order: candidate-specific RM-C debt where ordered roles are required.

### ORTHOGONAL TO THE BASE EXPERIMENT

- multi-writer/concurrent arrival ordering;
- projection geometry;
- eventual future reference;
- multi-Lace/snapshot/export.

### IMPLEMENTATION / APPLICATION

- R3 residence/recovery / Carrier;
- R4 capacity/refusal custody;
- R9 publication/frontier memory ordering;
- R10 WebNN projection proposal;
- Open #15 seed corpus;
- concrete encoding/resource/failure proof.

### ACCEPTANCE

Human acceptance of `pointer-emission.md` remains required before Core implementation. It is not a prerequisite for a documentation/Lab experiment that does not implement Core.

## 4. Bounded-freeze rule

Pass 4 establishes:

> An open does not block the bounded Lab when holding it fixed to the exact specimen leaves the validity of the retention experiment unchanged.

For D1:

```
tokens = exactly PIE / DESSERT / PIE / WHOLE / CUSTOMER
execution = serial
future = unavailable
projection coordinates = absent
route-family metadata = absent
residence/capacity = irrelevant to semantics
seed corpus = D1 itself
```

This is **experimental isolation**, not closure.

G1 remains open globally.

## 5. Candidate authority is now independent from candidate score

The admission envelope defines:

- `[SOURCE]`
- `[HUMAN]`
- `[DERIVED]`
- `[LAB-PROXY]`
- `[EXTERNAL-CANDIDATE]`
- `[ADVERSARIAL-CONTROL]`

A candidate does not change class because it scores well.

Important corrected examples:

### WeightByRepetition

A5 source:
- repetition strengthens/adds passage.

Executable representation:
- integer occurrence count.

Classification:
- **[LAB-PROXY]**, not “Graphic A's weight rule.”

### SidecarMemory

Classification:
- **[ADVERSARIAL-CONTROL]**.

It can never become a legitimate retention candidate by scoring 6/6.

## 6. D1 oracle-independence is separate from replay legality

Pass 4 defines:

- **D1-BLIND**
- **D1-SCHEMA-ONLY**
- **D1-WITNESSED**
- **D1-DERIVED**
- **ORACLE-CHEAT**

This resolves the instrument problem exposed by SidecarMemory without inventing a code-inspection theorem.

A rule may be:
- deterministic;
- prefix-pure;
- perfectly replayable;

and still be invalid as independent evidence because its code contains the answers.

Therefore:

```
replayable from prefix + candidate code
        !=
derived from Lace evidence
```

RootTouch is **D1-WITNESSED** at row 0004: the row is evidence for the ruled special case, not independent discovery of a universal rule.

## 7. The inherited 6/6 scoreboard is insufficient

A candidate that emits:

- all six expected D1 POINTERs;
- plus thirty invented POINTERs;

must not receive a perfect result.

Future Lab scoring must account for:

- TP expected matches;
- FN missed expected events;
- FP invented retention events;
- ordered-pair mismatches;
- event/index mismatches;
- oracle-independence class.

This is a direct repair to the experimental envelope, not to Core semantics.

## 8. Human rulings are walls, not contestants

H7–H12 are existing architecture constraints:

- H7/H8: sampling discovers; no arbitrary semantic choice.
- H9: star/search is derived.
- H10: retained relational truth is laid down on 1D Lace.
- H11: sampling creates the derived 2D working surface.
- H12: eventual retained participation may span sampling levels.

A replacement Lab plan may use human-ruled examples as calibration/witnesses, but must not score H7–H12 as rival hypotheses against source/research candidates.

## 9. Search-order correction

The inherited paused Lab plan contained a real logical conflict:

- it reserved the research family for a later pass;
- its old F1 also said that if the graphics were silent, “it is the human's to write.”

Those cannot both govern the same search.

Pass 4 corrected the paused plan.

Current evidence order is:

```
behavioral family      already harvested
        ↓
source of record A-D   next eligible harvest
        ↓
research family        later eligible candidates through SM-X-RES
        ↓
bounded comparison if useful
        ↓
evidence insufficient
        ↓
human may later rule/design
```

The platform family remains SM-X-PLAT feasibility/primitive evidence and is not a source of Lace retention semantics.

Graphics silence therefore means:

> the source-of-record harvest supplied no complete RM-A/RM-B/RM-C rule.

It does **not** automatically mean:

> invent one now.

## 10. Source silence is classified

The replacement Lab must distinguish:

- **SILENT-A:** no RM-A retention predicate.
- **SILENT-B:** retention decision exists but no RM-B materialization path.
- **SILENT-C:** relevant structural language exists but no mechanical predicate.

Examples:
- A5 repetition/strength may be structurally relevant yet SILENT-A.
- B8 persistence/re-entry is strong SM-C12 source evidence yet can remain SILENT-A for general sampled retention.

Silence is evidence about the edge of a source, not permission to add a threshold.

## 11. Candidate admission contract

Before execution a future candidate must declare:

- ID;
- authority class;
- exact provenance;
- RM-A/B/C coverage;
- inputs;
- derived state;
- reconstructibility;
- D1 oracle relationship;
- expected output;
- explicit non-claims;
- any conditional opens pulled into scope.

If the admission form cannot be completed without inventing the missing retention predicate, the candidate is not admitted.

## 12. SidecarMemory becomes a permanent adversarial control

Expected result remains:

```
replay          PASS
prefix purity   PASS
oracle score    perfect
authority       ADVERSARIAL-CONTROL
oracle class    ORACLE-CHEAT
admission       REJECT
```

That result demonstrates provenance blindness of replay. It no longer poses a paradox for candidate admission because provenance/oracle independence is an independent axis.

## 13. Canonical changes

### systems-manifest.md

Added:
- RM-A/RM-B/RM-C under **one** SM-GAP-RM;
- Current Open Interface Dependency Classification;
- bounded-freeze rule;
- explicit statement that specimen freezing is not global closure.

No mechanism behavior or acceptance changed.

### systems-manifest-ascii.md

The existing `L-GAP-RM` socket now displays:

```
RM-A RETAIN?
RM-B MATERIALIZE?
RM-C ROLE / ORDER?
```

No new mechanism box or ID was created.

The catalog also states that bounded Lab freezes may isolate unrelated opens without closing them.

## 14. Read-only audit result

The following current surfaces were checked against the admission envelope and required no semantic repair:

- `pointer-emission.md` — current gap and G1 remain separate; no hidden external relation-write interface.
- `math-execution-model.md` — G1 stays open; sampling vs retained append stays separate; slot semantics are not inferred chronologically.
- `prompts/context-pass.md` — current GAP is retention/materialization; platform/source boundaries remain fenced.
- `kit/agent-control.md` — exact retention GAP stays open; sampling/search remains non-writing.
- sampling campaign findings — H7–H12 and bounded D1 analyses remain evidence, not a retention-rule answer.
- Lab Source Architecture Pass 1 source-role findings — SM-X boundaries remain intact.
- `reason-model-map.md` — the mapped research books are source-silent for Lace's retention rule, while their other machinery remains eligible candidate machinery rather than Core authority.

The **paused inherited Lab Pass 4** did require correction to its search-order fallback; that correction was made while the plan remains paused.

## 15. Falsifier results

| F | Result |
|---|---|
| **F1** unrelated open falsely blocks bounded D1 Lab | **did not fire** — all tested unrelated opens can be frozen or excluded |
| **F2** SM-GAP-RM cannot be meaningfully partitioned | **did not fire** — RM-A/B/C are distinct question faces |
| **F3** D1-derived rule can present score as independent evidence | **FIRED against inherited protocol** — repaired by oracle-independence classes |
| **F4** false positives can hide behind expected-row score | **FIRED against inherited 6/6 scoreboard** — future scoring requires FP/pair/event accounting |
| **F5** HUMAN ruling treated as candidate contestant | **risk present in inherited baseline style; repaired by admission classes** |
| **F6** G1 silently solved in bounded experiment | **did not fire** — exact specimen freeze is explicit and non-global |
| **F7** search/projection/sampling gains write authority | **did not fire** |
| **F8** SidecarMemory becomes legitimate because replay/oracle pass | **did not fire after admission classification** — permanent adversarial control |
| **F9** external machinery bypasses SM-X | **did not fire** |
| **F10** graphics silence jumps directly to human invention | **FIRED in paused inherited plan** — search order corrected |
| **F11** RM-A/B/C become new mechanisms | **did not fire** |
| **F12** Pass 4 answers SM-GAP-RM | **did not fire** |

## 16. Final verdict

```
LAB ADMISSION ENVELOPE SOUND WITH CONDITIONALS
```

The base bounded D1 retention experiment can run without resolving unrelated repository opens.

The condition is candidate-local:

> If a candidate depends on an otherwise-frozen open, it must declare and justify that premise before admission.

This is the final foundation step before designing a **replacement** Lab Source Architecture Pass 4.

The inherited Lab Pass 4 remains paused and must not be executed verbatim.
