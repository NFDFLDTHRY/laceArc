# Miss-map — Pass 5 · Q × θ

**Status:** **EXECUTED** 2026-09-20. **Station:** maps (`Grok-maps`).
**Ticket:** [`miss-map-pass-5-plan.md`](miss-map-pass-5-plan.md).
**Cite:** `pointer-emission.md` Q table · [`model-behavior-map-pass-5-params.md`](model-behavior-map-pass-5-params.md).
**Emission:** `[GAP]`. Values **not chosen**. No `src/`.

Alignment is not an answer. HALF from pass 1 is now **ALIGNED** as a map.

---

| Q / other | Stamp | \(\theta\) | Same hole? | Value |
|---|---|---|---|---|
| Q0 later WORD | SPOKEN + HANDS CONFLICT `0003` | \(\theta_{\mathrm{WORD2}}\) | yes | **not chosen** |
| Q1 when POINTER | SPOKEN ≠ calculated | \(\theta_{\mathrm{when}}\) | yes | **not chosen** |
| Q2 arity | CONFLICT | \(\theta_k\) | yes | **not chosen** |
| Q3 leftover adjacency | CLOSED no-emit; leftover SPOKEN ≠ store | \(\theta_{\mathrm{left}}\) | leftover only | **not chosen** |
| Q4 backward | CLOSED | — | constraint, not a \(\theta\) | n/a |
| Q4 required PTR→PTR | OPEN | \(\theta_{\mathrm{reqPTR}}\) | yes | **not chosen** |
| Q5 later pass requires POINTER | SPOKEN + HANDS CONFLICT `0003`+`0004` | \(\theta_{\mathrm{when}}\) **and** \(\theta_{\mathrm{WORD2}}\) | overlap, not a third axis | **not chosen** |
| Slot II word-to-machine | SPOKEN cluster + CONFLICT D2 | **none** | IN-CALC-MISS | **not chosen** |
| G1 identity | OPEN | \(\theta_{\mathrm{G1}}\) | yes; not a Q number | **not chosen** |
| extra arrival groups | P27-F only `0005–0007` | \(\theta_{\mathrm{group}}\) | yes; not a Q number | **not chosen** |

Slot II was **not** merged into \(\theta_{\mathrm{G1}}\).

## `P5-R`

| | |
|---|---|
| Pairs | 10 rows |
| Values | **0** |
| Next | pass 6 miss-map STOP; **not opened** |

**Emission `[GAP]`. Shoe in hands.**
