# Reference audit — pass 2 plan: A / B / C companion fidelity against the PNGs

**Status:** PLAN · not executed.  
**Station:** graphics (execution). This plan file is maps-owned.  
**Board:** [reference-audit-iteration-plan.md](reference-audit-iteration-plan.md) · findings RA-G01, RA-G02, RA-G03, RA-G04, RA-G06, RA-G09, RA-G10.  
**Pinned:** `21652f463b3178075cc9d4568847502766c0304a`.  
**Editor (declared):** whoever holds `graphics`.  
**Emission:** RM-A `[GAP]` · G2 stamps in the companions are dated history and stay as written · no `src/`.

## Question (one act)

Can the three existing companions be made 1:1 with what the PNGs print — numerals, titles, panel lists, labels, the header spelling — by addition and by their own *PNG wins* rule, without rewriting one dated delta table, receipt or G2 stamp?

## Measured before proposing

All readings below are `VISUALLY_OBSERVED` at reduced resolution this session; execution re-sights at 1672×941 (B, C) and 1536×1024 (A).

**RA-G01 — numerals are printed.** A prints `1.`–`13.`, B `1.`–`12.`, C `1.`–`12.`, D `1.`–`6.`; only the letter prefix is assigned. Surfaces that say otherwise, exact lines: `graphic-b-ascii.md:41`, `:183` (B2 *printed title on PNG `[GAP]`*); `graphic-c-ascii.md:42`, `:171` (C3), `:310` (correctness board row); `graphics-close-reading.md:445`; `ascii-iterations.md:61` (i3 pass 3 queue row).

**RA-G02 — printed titles.**

| Panel | Companion working title | Printed title | Printed subtitle |
|---|---|---|---|
| B1 | Continuous wire | Continuous wire | A single strand is fed continuously. |
| B2 | Wire through star / arrival | **A word occurrence arrives** | New wire passes through the word's star. |
| B3 | Star | **Repetition builds the star** | Later occurrences pass through the same star. |
| B4 | Dictionary | Dictionary wiring (a definition is a route) | A word's definition is a path through other stars. |
| B5 | Sequence is physical | Sequence is physical | A sentence becomes a continuous route in order. |
| B6 | Document feed | Document feed | A longer text routes through many stars. |
| B7 | Touch (thread, not attach) | **Higher-dimensional touch** | New wire threads through an existing 3D star. |
| B8 | Touch remains | **Touch participates later** | A touch becomes material for future Lace. |
| B9 | Star-inside-star traffic | Star inside star traffic | Zoom inside a word star. |
| B10 | [X] Collapse | **No collapse** | Lace is not a flattened abstraction. |
| B11 | [X] Graph as thing | **Projection vs Lace** | A 2D view is only a shadow of the full structure. |
| B12 | Governing rule | Governing rule | The entire mechanism in one simple rule. |
| C1 | Rule Zero | Rule zero | One continuous strand. Always growing. |
| C2 | 1D presentation | 1D presentation | Seen as a line in time and order. |
| C3 | 2D crossings / adjacency | **2D presentation** | A flat projection on paper. |
| C4 | 3D looping / stars | **3D presentation** | The true structure begins to appear. |
| C5 | More dimensions ≠ more meaning | **What a dimension adds** | Each new dimension is another independent way to participate. |
| C6 | Four named families (views) | **4D presentation** | The same star in multiple independent relation families. |
| C7 | 5D+ touches of touches | **5D and beyond** | Touches can participate in later touches. |
| C8 | Symptoms of N-D (view) | **How N-dimensionality presents itself** | Many simultaneous routes through the same star. |
| C9 | Dictionary / no collapse | **Dictionary wiring example** | A definition is a route through other stars. |
| C10 | Input treatment | Input treatment | Raw input is fed directly into Lace, word by word. |
| C11 | [X] Projection ≠ Lace | **Projection vs actual Lace** | Lower-dimensional views are shadows, not the thing itself. |
| C12 | Governing rule | Governing rule | The entire mechanism in one simple rule. |

A's thirteen working titles already match the print (A1 *Start* … A13 *What this model gives you*); A's subtitles are the feeds (*Feed the word "The"*, …) and are already in its Pass-2 fields table.

**RA-G03 — B9 labels and the grouping gap.** Printed on B9: *Incoming passes (from many contexts)* · *Outgoing passes to other stars* · *Internal threading in higher dimensions (many crossings)*. No wire/star/touch column headers exist on B; the `[GAP]` closes as *not printed*.

**RA-G04 — printed lists not on the shelf.**
- A header: *One continuous strand. Every word a centimeter of wire. Every word's star a persistent 3D formation. Meaning does not exist. Only structure, sequence, and participation.* A footer: *Lace is not a database. It is a growing 3D history of language in motion.* A13: ten check-marks (the companion's checklist carries nine; the tenth is *Everything builds on the same simple rule*, followed by the rule).
- B header: *One continuous strand. Every word occurrence is more wire. Each word's star is its persistent 3D identity.* Tagline: *Same simple rule. Extraordinary structure. A single growing strand weaves a 3D universe of connections.* B10: four ✗ rows — *Not a database lookup (Word → stored entry): Loses the structure* · *Not one flat edge (A—B): Ignores 3D structure* · *Not a stored definition (PIE = "a dessert …"): No stored meanings* · *Not semantic collapse: Destroys the real behavior* — and one ✓: *It is a continuously growing 3D structure. Real wire. Real structure. Real behavior.* B11: left column *2D projection (simplified view)* five bullets; right column *True 3D Lace (actual structure)* five bullets. B12: eight numbered points — *1 Sequence is preserved (one continuous strand) · 2 Every occurrence is kept (no deletions, no edits) · 3 Each word has a persistent 3D star · 4 New wire threads through existing structure · 5 Touches remain and can participate later · 6 A definition is a route, not a stored meaning · 7 The Lace grows without bound · 8 No collapse, no second store, no separate mechanisms* — footer *Same strand. More words. A richer universe.*
- C header: *More dimensions do not add meaning. They add independent ways the same continuous wire can participate in existing structure.* Tagline: *One strand. Infinite participation. A richer universe.* C8 *Visible symptoms*: six bullets (already listed in the manifest's diagnostic section; not on the shelf). C10 specimen: RAW INPUT (ARRIVAL ORDER) *"The customer paid the restaurant for the dessert slice."* fed word by word; the route is drawn as **nine occurrence nodes, `the` three times as three nodes** — occurrence-per-node, consistent with D5. C11: ✗ five / ✓ five. C12: seven bullets and footer *Same strand. More dimensions. A richer universe.*

**RA-G06 — `centimetre`.** `graphic-a-ascii.md` carries it at the header block, the note line beneath it, the Fence table row and the Pass-4 operator box (four lines). The PNG prints `centimeter`. Close reading §5/§5a already holds the corrected spelling and the author's unit ruling; iteration-4's receipt left these four lines because the graphics station was not held.

**RA-G09 — the graphics door's table.** `docs/graphics/README.md:5–6`: three header cells, two delimiter cells.

**RA-G10 — door coverage.** 17/36; the 19 unlinked files are the pass plans behind the companions and the prefix-device sheets.

## Thesis / falsifier

**Thesis.** Each companion gains (a) a *Printed on the PNG* block — numerals confirmed, printed title and subtitle per panel, working title kept as alias, (b) the missing printed lists, (c) B9's labels, (d) the negative closure of the grouping gap, and the A file's four `centimetre` lines become `centimeter` with a one-line pointer to close reading §5a. Every historical Pass-N section, delta table and G2 stamp stays byte-identical. The close reading's §7 sentence is qualified in place (numerals printed; letters assigned) without touching §1–§6.

**Falsifier.** (a) A full-resolution re-sight contradicts a title or list above — then the PNG wins and this plan is corrected in the open before any companion changes. (b) Any dated section is rewritten. (c) A list transcription smuggles a mechanism not printed (for instance reading B12.7 *grows without bound* as a capacity ruling; it is a source sentence, and R4 stays open).

## Steps

1. Pull ff-only · `which` · `claim graphics "<Identity>"` with an expected hold.
2. Re-sight A, B, C at full resolution against the tables above; record `n/m` matches; correct this plan's table in the open where it was wrong.
3. `graphic-b-ascii.md` and `graphic-c-ascii.md`: replace the *not recovered* sentence with a dated *Printed on the PNG* block; add the printed-title table; append the missing lists as a new dated section (*Reference audit pass 2 — printed lists*); B9 labels into the existing B9 table (the `—` row becomes three rows); close the grouping gap negative; the correctness-board row *Printed panel numbers* gains a dated `Pass` line beneath the historical `[GAP]` line, not over it.
4. `graphic-a-ascii.md`: A13's tenth check; header and footer lines as printed; the four `centimetre` → `centimeter` with the pointer to §5a; everything else untouched.
5. `graphics-close-reading.md` §7: qualify *"They are not printed in the graphics"* in place — *the numerals are printed on all four; the A/B/C/D letters are assigned* — dated, one sentence, §1–§6 untouched.
6. `graphics/README.md`: fix the delimiter row; add one sentence *panel numerals are printed on the artwork; the letter is this repository's*; add a *Provenance* table linking the 19 pass plans and the prefix-device sheets (door → 36/36 or state the fraction reached).
7. `ascii-iterations.md`: iteration-3 pass-3 row (printed numbers) recorded as executed by this pass; the iteration-3 table shows which of its six rows this campaign used.
8. Gate · `check-docs` · commit with an actor trailer · release · findings file with the `n/m` re-sight counts and the falsifier disposition.

## May write / must not

**May write (graphics):** `docs/graphics/graphic-a-ascii.md` · `graphic-b-ascii.md` · `graphic-c-ascii.md` · `docs/graphics/README.md` · `docs/graphics/ascii-iterations.md` · `docs/graphics-close-reading.md` (§7 one sentence).  
**Must not:** touch a PNG · rewrite any Pass-N / i2pN section, delta table or receipt · edit the manifest, the namespace register, the pointer draft (those are passes 3 and 5) · turn a printed sentence into a Core rule · answer G2 · touch the fixture.

## Expected vs actual

| | Expected | Actual |
|---|---|---|
| Titles re-sighted matching this plan | 37/37 (A 13, B 12, C 12) | NOT_TESTED |
| Companion panels carrying the printed title after the pass | B 12/12 · C 12/12 (from 6/12 · 7/12) | NOT_TESTED |
| Live surfaces still saying numerals are not printed | 0 of 5 (namespace register is pass 5) | NOT_TESTED |
| `centimetre` on the graphics shelf | 0 (from 4; plan files excluded as dated) | NOT_TESTED |
| Dated sections byte-changed | 0 | NOT_TESTED |
| Graphics door | 36/36 or the fraction reached | NOT_TESTED |
| Physical | NOT_PERFORMED | — |
| Human acceptance | ABSENT | — |

## Stop

Stop when the three companions, the close reading §7 sentence, the door and the iteration table are committed under one graphics claim and released. Pass 3 is the law station's and needs its own claim.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc. Reading the numbers printed on the four pictures is allowed. Renumbering them is not. RM-A remains `[GAP]`.
