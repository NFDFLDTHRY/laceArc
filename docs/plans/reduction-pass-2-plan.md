# The reduction — pass 2 plan: the mathematical model of execution

**Status:** **`EXECUTED`** → [execution model](reduction-pass-2-execution-model.md). **Thesis held: authoritative state is `L` alone (M1); six transitions; parameters = the rulings list.** **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Campaign:** [the reduction](reduction-pass-1-plan.md) — six passes inside the human's added budget (2026-09-19 22:11 PT), weighted to his three results: **(1) the mathematical model of execution — the #1 result; (2) the global ASCII systems diagram updated; (3) the collaborative agentic apparatus.** **Pinned:** `e2e8155` + branch tip. **Editor:** Claude Fable 5.1; one hand; declared.

## Question (one act)

**What is the execution model of the whole deliverable — state, transitions, invariants — stated formally, parametric exactly in the human's rulings, and aligned transition-by-transition to the crate doors, the fences, the proof obligations and the device probes?**

## Measured before proposing

The sealed algebra (S1–S6) models **Core growth** only: `L`, `Arrive`, `Φ_morphism`, `Star`, `π`; schedule `[GAP]`. The environment (iteration 6 pass 4) typed σ and fenced it. **Neither models the running app**: no hopper, no refusal, no carrier, no reload, no frontier, no observer. The companion's pass-2 line says *"Shared state = L. One line."* — asserted, never proved for the deliverable.

## Thesis, stated so it can lose

**The deliverable's authoritative state is `L` alone.** Every other component — frontier, views, carrier bytes, the pending arrival — is a function of `L` or is custody, not history; the transitions are six (SUBMIT · STEP · REFUSE · PERSIST · PROJECT · OPEN); the model is parametric in exactly (σ, encoding, R3, R4) and in nothing an agent may choose. **Optimal** is defined, not asserted: minimal authoritative state, minimal transition set, parametric only in the human's rulings.

**Falsifier:** an observable of the running app that is not a function of `L` and the parameters — if one exists the state is not `L` alone, and the model says so.

## Steps
2a state space Σ · 2b transitions as small-step rules · 2c invariants I1–I6 · 2d theorems M1 (minimal state) · M2 (determinism modulo σ; reload identity) · M3 (D1 trace) · 2e the alignment table transition → door/sheet → fences → proof obligation → probe → ruling · 2f what the model refuses to decide.

## May write / must not
`docs/plans/reduction-pass-2-execution-model.md`; README row; this status line. **Must not:** edit `math-execution-model.md` (sealed) or the emission file; choose σ, an encoding, a carrier, a capacity policy; add a state that is not a function of `L`.

## Expected vs actual
Expected: six transitions; state Σ = (L, h, K) with only `L` authoritative; M1 proved within scope; the alignment table has no empty cell. **Actual:** `NOT_TESTED`. Physical: `NOT_PERFORMED`.

## Stop
The three theorems stated with premises; the alignment table complete; the parameter list equals the rulings list. Then stop.
