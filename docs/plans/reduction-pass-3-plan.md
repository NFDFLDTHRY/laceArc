# The reduction — pass 3 plan: the global ASCII systems diagram, updated (Pass 28)

**Status:** **`EXECUTED`** → [findings](reduction-pass-3-findings.md); Pass 28 on the companion. Thesis held. **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Campaign:** [the reduction](reduction-pass-1-plan.md) — six passes inside the human's added budget (2026-09-19 22:11 PT), weighted to his three results: **(1) the mathematical model of execution — the #1 result; (2) the global ASCII systems diagram updated; (3) the collaborative agentic apparatus.** **Pinned:** `e2e8155` + branch tip. **Editor:** Claude Fable 5.1; one hand; declared.

## Question (one act)

**Does the companion carry the execution model, the door, and the roles — as readings and cite-only boxes that change no Core sentence — and does the execution model CONFIRM or contradict the companion's own *shared state = L, one line*?**

## Measured before proposing
Pass 27 tip: DIAGRAM SOUND WITH OPENS (Core); deliverable boxes drawn cite-only. The companion has no execution-state diagram: its Core mechanisms (pass 5) are per-tick pictures; its clock (pass 6) is a metaphor. `Shared state = L` (pass 2) is a sentence without a proof.

## Thesis / falsifier
M1 (pass 2) **CONFIRMs** the pass-2 sentence and adds nothing to Core; the six transitions map onto existing boxes (Contract II · I · III · P27-D) without a new box in Contract I. Falsifier: a transition that needs a Core box the companion lacks — then Pass 28 must draw one and say why.

## Steps
3a Pass 28 = Iteration-4 Pass 3: execution-model reading (Σ · six transitions · I1–I6 · M1 CONFIRM) · 3b door pointer (S) · 3c roles board (steward, cite-only) · 3d Iteration-4 banner + pointer + executed-passes link · 3e sealed refuse honored.

## May write / must not
`docs/systems-manifest-ascii.md` (append + thin pointers only); `docs/plans/reduction-pass-3-findings.md`; README row. **Must not:** redraw Pass 1–6 art; edit Passes 7–27 beyond pointers; add a Contract IV; fill Φ.

## Expected vs actual
Pass 28 ≤ 120 lines; 0 Core changes; M1 as CONFIRM row. **Actual:** `NOT_TESTED`.

## Stop
Pass 28 appended; check-docs green; findings written.
