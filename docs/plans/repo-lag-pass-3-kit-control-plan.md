# Repo lag cleanup — Pass 3 kit control / transport reconciliation plan

**Status:** PLAN — human approved in conversation by “Proceed with pass 3.”  
**Station sequence:** maps persists this plan, then releases; kit executes the control/transport reconciliation; maps writes closeout findings and remainder docket.  
**Base before plan persistence:** `fee484821a09cff8b03259b22bc1d600a4c4cb3f` (repo-lag Pass 2 closeout).  
**No new law. No Core change. No target selection. No acceptance. No implementation.**

## Question

Can an agent routed through the portable kit receive the same current Pass-39-era story as an agent entering through `README → AGENTS → context-pass`, without rewriting the human law block or erasing historical transport evidence?

## Scope

Kit-owned live/control surfaces:

- `docs/kit/READ_ME_FIRST.md`
- `docs/kit/agent-control.md`
- `docs/kit/grok-bot-profile.md`
- `docs/kit/README.md`
- `docs/kit/cold-doors.md`
- `docs/kit/agent-task-template.md`
- `.claude/README.md`
- `docs/kit/scanner.md`

Historical pass/receipt bodies under `docs/kit/**` remain provenance and are not rewritten unless a live register row embedded in a mixed current/history file is objectively wrong.

No hook/enforcement work:
- no `.claude/hooks/**` changes;
- no CI / GitHub Actions;
- no repository permission changes;
- no claim that prompt obedience is enforced.

## Required current court

Portable controls must route current work through:

- Global ASCII Pass 39 as current non-Core integration overlay;
- S9 read-device algebra / RD-1;
- S10 observability / provenance / non-identifiability;
- S11 realization/evidence planes with T0–T6 and GAP-HOST;
- S12 optional device-local operators; fixed common chassis rejected;
- HCC-A / Coffee Cup / Water mathematical blueprints, not implementations;
- source desired-behavior mechanisms distinct from concrete implementation mechanisms;
- Water current target: UNK nonblocking predicted evidence shape; INVALID later evidence-backed refutation;
- RM-A OPEN / MISSING-A;
- G1 OPEN;
- R3 OPEN;
- GAP-HOST OPEN;
- exact H×C×W runtime coupling OPEN;
- TARGET UNDECIDED;
- pointer acceptance ABSENT;
- no `src/`.

## Transport integrity gate

Before wrapper edits, re-measure the human-law transport blocks in:

- `docs/law-why-these-documents.md` live fenced block;
- `docs/kit/agent-control.md` transported staking block;
- `docs/kit/grok-bot-profile.md` Explanation transport block.

Disposition:
- IDENTICAL → preserve block bytes; repair wrapper/current control prose only.
- DIFFERENT → identify the exact difference; repair only as a lawful transport synchronization, never by paraphrase or invented merge.

## Primary repairs

### agent-control.md

Preserve:
- transported human-law block;
- C01 dated `4fe984b` observation;
- C01–C12 structure;
- Lab-vs-Lace-retain distinction;
- gate/acceptance discipline.

Replace the stale “Current steward qualification — Alignment Restart Pass 1” wrapper with a current Pass-39-era steward overlay carrying S9–S12, current behavioral-device status, current Water law, open gates, and TARGET UNDECIDED.

Do not turn the overlay into a second law block.

### grok-bot-profile.md

Preserve:
- Name / Job / Description structure;
- human-law Explanation block if transport-integrity check holds;
- live-law-wins rule;
- permanent implementation prohibitions.

Replace the obsolete First Task that asks Grok to create/propose `docs/pointer-emission.md`.

New first task is grounding:
1. pull current main and report HEAD;
2. read AGENTS, context-pass, live diagram prompt and live law;
3. open Graphics A–D;
4. return one-page steward brief with current substrate, S9–S12 roles, RM-A/G1/R3/GAP-HOST, TARGET UNDECIDED, pointer draft/acceptance state, HCC/Cup/Water role and assigned task boundary;
5. propose no new file unless the human assigned one;
6. stop.

The profile must not autonomously choose the next repository task.

### READ_ME_FIRST.md

Preserve THEN / earlier reread historical pins.

Replace the stale “Current reading, Alignment Restart Pass 1” package reading with current Pass-39 / Prompt-Pass-1 / Root-Pass-2 routing.

Provider/external routing should explicitly include:
- AGENTS;
- context-pass;
- agent-control;
- task template.

### docs/kit/README.md

Update current-status rows for package door / agent-control / Grok profile so September-20 synchronization provenance is not mistaken for current control state.

Preserve dated synchronization history where useful.

### cold-doors.md

Do not rewrite Passes 1–6 history.

Repair the live ten-door table:
- Graphics door current station `law` → `graphics`.

Reconsider the DATED transport companion descriptions after current wrapper reconciliation. A DATED law-copy pin may remain DATED even when its wrapper is current.

## Audit-only surfaces unless a current contradiction is found

### agent-task-template.md
Expected unchanged. It is a form, not an architecture dashboard.

### .claude/README.md
Expected unchanged. Tooling scope/hook semantics, not a current-machine summary.

### scanner.md
Expected unchanged. Historical methodological correction: LIVE != idle; stamp != activity; ownership != activity.

## Cross-station remainder

Expected survivors after kit:
- hologram: `docs/clock/wasm/README.md` and wasm64 wording in `docs/clock/README.md`;
- maps: `docs/namespace-register.md`, `docs/coherence-audit-log.md`;
- coord: `docs/coord/stations/README.md` clipboard-station description.

Record, do not repair from kit.

## Falsifiers

F1 transported human law paraphrased instead of preserved.  
F2 dated C01 observation rewritten as current history.  
F3 Grok first task still asks for new `docs/pointer-emission.md`.  
F4 Grok profile autonomously chooses the next repo task.  
F5 agent-control still calls Alignment Restart Pass 1 its current steward state.  
F6 S9–S12 represented as Core.  
F7 source desired-behavior mechanisms flattened into optional implementation ideas.  
F8 fixed common S12 chassis resurrected.  
F9 old Water blocking-UNK target revived.  
F10 lexical INVALID revived.  
F11 RM-A closed.  
F12 target selected.  
F13 GAP-HOST silently closed.  
F14 human acceptance inferred.  
F15 cold-door historical passes rewritten.  
F16 DATED transport relabeled LIVE without evidence.  
F17 scanner history rewritten.  
F18 task template bloated with architecture it does not need.  
F19 hooks/enforcement changed.  
F20 foreign-station debt repaired from kit.  
F21 `src/` / implementation opened.

## Completion gates

G1 relevant kit surfaces classified.  
G2 current law transport blocks compared at current tip.  
G3 transport differences explicitly dispositioned.  
G4 agent-control current wrapper routes through Pass 39.  
G5 agent-control carries S9–S12 correctly.  
G6 agent-control carries current Water law.  
G7 agent-control preserves RM-A/G1/R3/GAP-HOST.  
G8 agent-control says TARGET UNDECIDED.  
G9 Grok obsolete pointer-file creation task removed.  
G10 Grok first task becomes current-state grounding.  
G11 Grok cannot self-assign next repo work.  
G12 READ_ME_FIRST current reading updated.  
G13 READ_ME_FIRST historical pins preserved.  
G14 kit README current statuses repaired.  
G15 cold-door Graphics ownership repaired.  
G16 cold-door historical receipts preserved.  
G17 agent-task-template audited.  
G18 .claude README audited.  
G19 scanner audited/preserved.  
G20 no hook/enforcement changes.  
G21 no law/Core/target/acceptance movement.  
G22 foreign-station remainder docket updated.

## Execution order

1. persist this plan and release maps;
2. claim kit;
3. pin current main;
4. exact-compare transported human-law blocks;
5. classify kit live/history surfaces;
6. repair `agent-control.md` current wrapper;
7. repair `grok-bot-profile.md` first task/current wrapper;
8. update `READ_ME_FIRST.md` current reading;
9. update `docs/kit/README.md` status rows;
10. repair `cold-doors.md` live Graphics ownership row;
11. audit task template / .claude README / scanner;
12. run stale-control sweep;
13. release kit;
14. claim maps;
15. write Pass-3 findings + new remainder docket;
16. mark plan executed and index receipt;
17. release maps.

## Stop

Do not automatically begin hologram, namespace/audit-log or coord cleanup after this pass.
