# Golden IR fixtures

Sealed `HologramIr` documents for the Layer III nostd pipeline viewer. **Plan-shaped, not parsed from any Rust source** — there is no `src/` to parse. `mode` is `"fixture"`, `items[]` is empty, `emission_gate` is `"blocked"`, and every bind is `authority: "PROPOSAL"`.

| File | Version | Use |
|---|---|---|
| [behavioral-goals-assay-controls-v0.1.0.json](behavioral-goals-assay-controls-v0.1.0.json) | 0.1.0 | **Synthetic assay self-test controls**, not Lace/holder output. Twelve goal IDs × PASS/FAIL polarity = 24 cases. Unlabeled correct behavior must pass; label-mimic anti-goals must fail. `expected_pass` is harness-self-test metadata and must never be exposed to a future candidate under test. |
| [hologram-ir-golden-v0.2.0.json](hologram-ir-golden-v0.2.0.json) | 0.2.0 | **Current viewer default; dated semantic fixture.** Adds `pieces` (P1–P15), `seams` and `binds` to the 0.1.0 envelope. It predates Manifest Reconciliation Pass 1: its P9 cite bundle still carries A6 and its P13 bundle still groups B10–B11, and its three seam bands do not represent the later derived sampling/search working-machinery layer. **Sealed bytes remain unchanged. Do not use v0.2.0 as the current mechanism/contract authority.** |
| [hologram-ir-golden-v0.1.0.json](hologram-ir-golden-v0.1.0.json) | 0.1.0 | Historical. Doors only |
| [d1-golden-v0.1.0.json](d1-golden-v0.1.0.json) | **Graphic D panel 1 as a test oracle** — eleven rows, five word arrivals, six pointers, transcribed from [the close reading](../../graphics-close-reading.md) and verified against the panel by sighted read. Carries the three candidate rules the rows themselves rule out. **Re-sighted 2026-09-22** against the PNG at 2× by [reference audit pass 1](../reference-audit-pass-1-findings.md): 11/11 rows match the panel and the close reading; the [Graphic D companion](../../graphics/graphic-d-ascii.md) is a third agreeing transcription. **The oracle at the third arrow of [Lab Source Architecture](../lab-source-architecture-pass-1-findings.md)** — `source → clipboard → lab evidence → candidate systems`. **Not Graphic D**; it settles nothing the PNG does not |

Schema: [../hologram-ast-ir.md](../hologram-ast-ir.md). Binding to the picture: [../hologram-ast-ir-viewer.md](../hologram-ast-ir-viewer.md). Validate: [../tools/hologram-ir-shape-check.py](../tools/hologram-ir-shape-check.py).

A fixture is not a proved birth. The current semantic authority is the Pass-32 mechanism catalog plus the systems-manifest contract registry, not this sealed snapshot. `hologram-ir-golden-v0.2.0.json` remains useful for viewer/shape regression at its dated schema; semantic modernization would require a new fixture/schema revision rather than an in-place rewrite. Exact sampling-derived retention/materialization stays `[GAP]`. No `src/`.
