# Seed dictionary × model — Pass 3 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `a3141da`).
**Depends on:** pass 2 WORD-only prefix.
**Human ruling:** what is wired is **each dictionary word to the words used to define that word**.
**Hands:** Piece 9 — “A word’s definition is a route through other word stars.” Example path: PIE → dessert → slice → whole → food.
**Emission:** `[GAP]`. Route *shape* ≠ when. No `src/`.

Pass 2 left unused eyelets. Pass 3 says what “used” means for seed: participation of the headword’s definition stream into the stars of the defining words.

---

## 0. One question

If CAT is defined with ANIMAL, … — which objects on \(L\) does that wiring name, and which act still has no schedule?

## 1. One result (on execute)

[`seed-dictionary-map-pass-3-defroute.md`](seed-dictionary-map-pass-3-defroute.md) — definition-route shape vs Arrive vs Φ-when.

---

## 2. Shape execute must record

| Claim | On the model |
|---|---|
| Headword enters | Arrive (WORD) |
| Defining words enter if unseen | Arrive (Piece 9 precondition) |
| Wiring | POINTER *shape* from the definition feed into earlier defining-word indices / later participation into those stars |
| Not stored | no gloss field on the headword |
| Example PIE→dessert→… | Hands specimen path, **not** a computed schedule |
| When those POINTER ticks fire | \(\theta_{\mathrm{when}}\) still free |
| Repeat headword as extra WORD | D1 `0003` CONFLICT remains named |

Spoken “pay into seed” is this shape *talked as a when*. Execute keeps the shape and leaves the when.

## 3. Forbidden this pass

- Write the PIE path as emitted rows
- Fill \(\theta_{\mathrm{when}}\) because “definition is a route”
- A definition field / lexicon table
- Open `src/`
- Start pass 4 unless immediately authorized

## 4. Accept pass 3 when

- Headword / defining-words / route-shape / not-a-gloss / when-still-free each have a row.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 4 is immediately authorized.

## Steward test

Shoe in hands. PIE’s definition path goes through dessert, slice, whole, food. **Seeing the path is not writing the grabs.**
