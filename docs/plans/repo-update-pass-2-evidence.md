# Repository update — pass 2 evidence and probes

**Status:** planning evidence plus current EXECUTION RECEIPT, 2026-09-20. The human's “Proceed with pass 2” authorizes the reviewed local scope. See §8 for actual dispositions; §§1–7 retain the planning baseline and its limits. **Station:** maps.
**Base:** `10fe4947c4450453e7aa1c256cdea294d6981e48`. **Plan:** [pass 2](repo-update-pass-2-plan.md).
**Meaning of locators:** paths and named sections below identify exact content at this base. Line ranges are discovery aids, not future reading instructions. A source sighting, synthetic result, browser result, human acceptance and release identity are separate fields.

## 1. Reanchor and what arrived

Fetch and ff-only pull found main unchanged and clean. Pass 1 was published after its execution receipt, in these eight station commits:

| Station | Commit |
|---|---|
| law | `e458697b3c483c98e100838029dbc041b32e10fd` |
| kit | `b32db5fc1c273df95521759d1ac51ecbc69def68` |
| history | `29a38724ae61e4b5d868be82109ab26837879e9c` |
| clipboards | `3ae9409284f8d6b1ddb02e5157e64a39f216405e` |
| hologram | `05b3f05bb1d178675d0492238049837f5a4c96a1` |
| prompts | `f037a0c7edd664ef960edbaa53cf0915a578ff02` |
| coord | `bb7386f7f423c7504e259f2b3f28e1fe224f5a3d` |
| maps | `10fe4947c4450453e7aa1c256cdea294d6981e48` |

The chain changes exactly the 47 paths in the [pass 1 manifest](repo-update-pass-1-evidence.md#actual-file-manifest): 44 modified Markdown files and three additions. Current census: **561 paths, 486 Markdown**, versus the preserved base inventory's 558/484. No new Rust source, Core implementation, runtime modification or release promotion arrived in that chain. The earlier sixteen-branch content dispositions are not recomputed here; this task does not create a missing-content backlog from eight new descendants on main.

All eleven doc stations and eight gear shafts were FREE before planning. Only maps is claimed for the three planning paths; expected hold about twenty minutes, then release. No other station or shaft is claimed for reconnaissance.

## 2. Current finding register

Every P2 row below was OPEN at planning. OBSERVED/SYNTHETIC/INFERENCE describes the discovery evidence, not today's repair status; use [§8](#8-execution-dispositions) for execution dispositions. Prior U references retain continuity without rewriting the pass 1 record.

| ID / carry | Current evidence and exact source/consumer | Proposed closure |
|---|---|---|
| **P2-01 / U08,U23** | **OBSERVED prose.** `math-execution-model-pass-4-adversarial.md` Authority/A0/A1-C6/A4-Q4 ranks model/witness above manifest and retains blanket Q1–Q5/target-generality gaps. Crosswalk Pass 1 Authority/M1/M3-Q4 and Pass 3 Authority/A1-C14/A4-Q4 repeat the roles/GAP conflation. The pending `atomic-construction-pass-1-plan.md` §5 row “Emission draft §4 vs D3/D6 and AGENTS.md” correctly affirms permission but still carries the draft's former uncertainty as a current conflict; §2 and the paragraph after the §5 table retain holder-text/mechanism labels. Early problem-map K15/K17 are dated historical claims | Qualify the current entrances and precise permission/proof distinction; preserve historical catalog/receipt bodies and the pending plan's NOT_RUN status. N12's commit-as-acceptance refusal and crosswalk W10's host-ingest refusal remain valid |
| **P2-02** | **OBSERVED mislabel.** Arrival R3 (`docs/math-execution-model-pass-6-arrival.md`, discovery lines 63–64) calls Piece 9 document feed and Piece 10 dictionary feed. Manifest Piece 9 is Dictionary Wiring; Piece 10 is Document Feed | Correct both label/description pairs against the manifest; no input behavior chosen. Literal carry search found these two direct reversals |
| **P2-03 / U03,U07,U26** | **OBSERVED overclaim.** Compute §1/§3 calls proposed one-writer/concurrency behavior law or consequence while R9 remains open; G2 calls empty imports a binary proof of F-S3; G5 still says finite testing proves “offline forever”; the domain path is labeled Pages production without the current conflict. Shape Sheet H says host independence “by construction”; Sheet S selects one-submit/one-value as the only source-compatible reading; Sheet E says no encoding chosen but prescribes length-prefixing without a blocker | Distinguish source constraints, proposed design, structural check and remaining R5/R6/R9/G1 obligations. No replacement ABI, framing or scheduling decision |
| **P2-04 / U16 follow-up** | **VISUALLY_OBSERVED source plus derivative mismatch.** Kauffman printed 324/PDF 343 says `κ=exp(θ/(2π))`, `θ` total angle in radians, one turn `exp(1)`, n turns `exp(n)`. Mechanisms M-A2 mixes that miniature with `exp(μθ)` at μ=1; ASCII P-A2 prints `exp(2πn)`; reason-map K7 and its sample retain `exp(θ)`. Clipboard Page A has the printed normalization | Correct the current formula chain, units, worked example and source attribution. Pass 1 repaired ≤ and other source claims, not this next-page normalization; its U16 closure must not be read as a complete formula audit |
| **P2-05 / U16 residual** | **OBSERVED prose.** Xiao clipboard X1/X2 still says order kin dies at interning and equates one ID at many positions with one collapsed node; its coupling table repeats that implication. `clipboard-five-stakes.md` repeats it despite its later correction, and retains “reason-map ≤ fix OPEN” routing after the fix landed | Carry the pass 1 indexed-occurrence distinction into current shelf interpretations and correct actual fix routing. Vocabulary prohibitions and dated original receipt counts remain |
| **P2-06 / U14,U18** | **OBSERVED navigation.** Seven direct-index advisories, but only five entrances have the bounded missing prose routes described in §3 | Add grouped routes to four closed quiet-door campaigns and graphics Pass 7; preserve intentional grouping, assets and history |
| **P2-07 / U17** | **OBSERVED hook messages.** `.claude/hooks/session-start.sh` still says no runtime; `check-docs.sh` comments/status prose describe the whole repo as no-code/absence instead of no Core and the exact acceptance boundary | Wording-only edits plus isolated hook behavior checks; no new detector, metric, enforcement or exit-code change |
| **P2-08 / U21** | **OBSERVED dispatch chain.** `coord.sh::delegate_gear` excludes force-free; `cmd_force_free` also drops the required reason. `gearing/claim.sh::cmd_force_free` accepts no reason and clears NOTE. Current direct contract is human-only, with reason in the commit | Restore the umbrella's dispatch and reason-retention promise through both owners. Preserve direct backend semantics and human-only authority; test synthetic claims, never live holds |
| **P2-09 / U19,U25** | **OBSERVED text/parser/bindings; visual behavior unobserved.** HCC parser starts at `###`, omitting the corrected preamble; responsive description hides below 820px. HCC/nostd/gearing current labels retain required-holder framing. Gearing's two source links point to moved docs-root paths on main. `contracts-gears.js` says AB/BC/CA have no pickTarget while renderer defines them. Lace Holder doors inherit `.hud` pointer-events:none | Visible persistent qualification and correct routes/binding prose; frozen gearing requires designated editor. Browser/touch/phone inspection remains a separate closure field |
| **P2-10 / U20,U24** | **STATIC INFERENCE.** Lace HTML binds one uniform buffer to passes then overwrites flag 0 with 1 before submission; “GPU on” precedes submission. `keepFallback` writes inline display:block and `hideFallback` is empty | Stable per-pass data; bounded setup/submission/error claims; failure→recovery correctness. Controlled stubs plus actual images/overlay checks, never submission=render proof |
| **P2-11 / U24 extension** | **SYNTHETICALLY_OBSERVED.** Actual worker bake emits 22 filament triangles, all zero area; actual inline fallback emits none. D1 rows agree; tessellation differs. Details in §5 | Nondegenerate finite filament geometry for all 11 birth IDs in both paths, with retained row/material semantics; separately inspect browser appearance |
| **P2-12** | **OBSERVED update control flow.** `lace-projection.html::resolveLatestSha` falls back to GitHub commits/main if release-pointer providers fail; `goLatest` navigates to it. Pointer id/html fields are ignored. Update handler can replace “tip fail” with “fresh” when no SW waits | Release-pointer-only selection; invalid/mismatched/failed selection cannot navigate or imply freshness. Actual pin-to-pin installation remains untested; no LATEST edit |
| **P2-13 / U20** | **OBSERVED source; browser cache behavior unobserved.** SW activation deletes every origin cache except its own, including unrelated and other-install caches. PRE has only two icons; first client deliberately unclaimed; cache puts do not extend fetch event lifetime | Preserve foreign and other-pin sentinels. Report cold first-install, controlled warmed offline and optional worker/REF availability separately. Do not equate purge repair with an offline carrier |
| **P2-14 / U12** | **OBSERVED source availability.** Twelve attachments are present and hashed; supplied WebAssembly PDF can be inspected for the rust-target Page G requests. Cargo A4 and the other identified target texts are absent | A bounded source-sighting addendum stating exactly which requested facts are supported, absent or require another source. No Cargo/build/validator/device or ruling claim follows |

**Addresses on the existing system map:** P2-01–03 are ALGEBRA/ENVIRONMENT/SHAPE and Contracts I–III, with existing Q4/G1/R5/R6/R9 obligations. P2-04–05 are source-reference mappings, not new Core Pieces. P2-06–08 are reading/TEAM steward work. P2-09–11 are Contract III VIEW; P2-12–13 are SHIP/shell S and its evidence boundary. P2-14 is TARGET, with R1/R11/R12 and G8′/G9/G10 remaining distinct. No new box or Core operator is introduced.

## 3. Navigation probe, not a warning-count target

The existing checker counts only links directly in each README. Reviewers also followed intended same-shelf Markdown routes. That second measurement is bounded shelf reachability, not proof of global graph reachability or a full content review.

| Entrance | Direct coverage | Actual route finding |
|---|---:|---|
| `docs/README.md` | 24/43 | All 43 same-shelf files reachable within two Markdown links, through the companion; no bulk additions proposed |
| `docs/clock/README.md` | 19/29 | Closed quiet-door #6 ASCII/receipt route missing; six plans follow its ASCII header. Two icons are manifest-wired assets, not omitted prose |
| `docs/clock/passes/README.md` | 31/61 | All 61 reachable within two links through `projection-iterations.md`; retain grouping |
| `docs/gearing/README.md` | 13/21 | Missing closed quiet-door #8 group: ASCII, receipt, six linked plans |
| `docs/graphics/README.md` | 16/36 | Missing `prefix-device-pass-7.md`; nineteen omitted direct plan links already have grouped routes. New label must state historical disputed SPOKEN custody, not acceptance |
| `docs/history/README.md` | 25/33 | Missing closed quiet-door #5 group: ASCII, receipt, six linked plans |
| `docs/hologram/README.md` | 16/31 | Missing closed quiet-door #7 group; seven primitive images already route through `visual-primitives.md` |

The four quiet-door groups contain 32 files; one additional graphics receipt lacks its intended shelf route. This is the repair set, not 110 independently broken links. Images and frozen history bodies do not need modification.

The checker's non-Markdown path scan still treats the pass 1 inventory JSON as references. Its unsigned count counts trailer lines rather than commits; in this actual window the independently checked 83 is correct, with no multi-trailer inflation. Neither algorithm is changed by P2-07. Any later metric repair needs its own counterexamples, including inventory-only mentions and legitimate script/manifest references.

## 4. Source probe and supplied-file limits

**Kauffman:** root rendered and visually inspected printed p.324, one-based PDF p.343. SHA-256 `5bc2465525431f2a944cfb1af1ee51e24d1bba242bdcb40d277faa2d37581697`. The displayed expression is `κ = exp(θ/(2π))`; text identifies radians and gives the one-turn and n-turn forms described above. The source's printed model is what the derivative must attribute. Its prose/model normalization must not be silently generalized or replaced. `pdftotext` returned an empty page because this page is scanned; the finding comes from the rendered image, not OCR inference.

The earlier Xiao sighting remains bounded to printed p.2/PDF p.9 Eq.1.1 and printed p.38/PDF p.45 §2.1.1, recorded with its hash in pass 1. No new full book reading is claimed here.

**Available corpus:** nine PDFs and three conceptual-reference TXT files, checked by the source reviewer. Cargo A4, rustc A1's original text, JS-API A3, rustup A10 and tool-conventions A11 are not supplied. The A4 queue remains unchanged. The WebAssembly PDF hash is `687e73641a7eec8692273fdf048e1578bf2f379f507c36ec284a39d9e7f70fce`.

**WebAssembly availability probe:** local text extraction identifies Release 3.0 (2026-09-11), 323 PDF pages. These are candidate source anchors for F, not a completed visual sighting, accepted check or configured-validator result. PDF page numbers are one-based.

| Requested proposition | Available section | PDF / printed page | Qualification to carry into the sighting |
|---|---|---|---|
| Memory64 address-type encoding | §5.3.9 Limits; §5.3.12 Memory Types; §5.5.8 Memory Section | 188 / 184; 189 / 185; 206 / 202 | i32 flags `0x00`/`0x01` differ from i64 `0x04`/`0x05`; a parser must identify their grammatical positions, not search arbitrary bytes |
| Memory validation bounds | §3.2.15 Memory Types | 42 / 38 | Source rule available; a selected validator's enforcement is still unobserved |
| Custom-section carrier | §5.5.3 Custom Section | 205–206 / 201–202 | Section id 0; custom payloads are ignored by WebAssembly semantics |
| Name metadata | §7.7.1 Name Section, Name Maps, Function Names | 282–284 / 278–280 | Names may be ignored and need not be unique. Missing names do not prove absence of a crate's code; present names do not prove complete provenance |
| EH tags and operators | §5.5.16 Tag Section; §5.3.10 Tag Types; §5.4.2 Control Instructions | 209 / 205; 188 / 184; 189–190 / 185–186 | Include `throw`, `throw_ref`, `try_table`. Local tag-section absence does not exclude imported tags (§5.3.14 / §5.5.5) |
| Feature-subset premises | §7.2 Profiles and its Conventions/Defined Profiles; §7.3 Implementation Limitations | 253–255 / 249–251 | Defined profiles FUL/DET do not supply the proposed G8′ validator mode. Distinguish an artifact-subset policy from spec conformance; §7.3 does not permit conforming implementations to omit individual features |
| Validation algorithm | §7.6 Validation Algorithm | starts 274 / 270 | The algorithm is available; the chosen tool's feature switches and G8′ policy require separate evidence |

Text search found no `target_features`, `build-std`, `compiler_builtins` or `panic=abort` material. This negative text search does not replace the missing target documents or prove those topics absent from every source. The bounded source campaign can proceed from the positive anchors while its visual, carrier-adequacy, tool and build fields remain pending.

No uploaded source is modified or copied into Git. HCC-A, Coffee Cup and Water remain conceptual references. None of these attachments supplies exact human acceptance of pointer emission, a hosting amendment, fixture scope or the open architecture rulings.

## 5. Runtime probe and release boundary

The projection reviewer executed the **actual current worker and extracted inline bake** in an isolated Node VM with stub message endpoints. No browser, network, GPU, install, source edit or claim was used.

| Observation | Worker | Inline fallback |
|---|---:|---:|
| Total vertices | 5,592 | 3,798 |
| Floor vertices | 6 | 6 |
| Filament triangles | 22 | 0 |
| Filament triangles with exactly zero cross-product area | 22 | not applicable |
| Filament birth IDs requested | 0–10 | no filament triangles |
| Sphere tessellation | 20 | 16 |

The defect is localized: worker `ribbon` derives width only from dx/dz; the filament endpoints share x/z and differ only in y, so both width offsets are zero. Inline `bakeMain` has no corresponding filament addition. D1 rows agree. Reproduction measures the actual position arrays and triangle cross products; it does not infer invisible geometry merely from a label. This supports adding the worker to the previously deferred U24 repair scope. It does not justify demanding byte-identical fallback tessellation or assert any rendered result.

**Release closure inspected from Git:** current `LATEST.json` points to `3b619929182e98ccaa348a2fc696b98c2e416a2e`, title `ixp2`. Nine launch assets and twelve linked-view assets exist at both that revision and main. Twenty of the 21 blobs are identical; only `docs/clock/hcc-a-systems-manifest.md` differs (release blob `4c97a9bd…`, main `106bcc0b…`). Thus documentation publication did not repair the current viewer runtime or automatically advance the pinned interpretation.

Exact inspected closure:

```text
docs/clock/lace-projection.html
docs/clock/manifest.webmanifest
docs/clock/icon-192.png
docs/clock/icon-512.png
docs/clock/sw.js
docs/clock/projection-bake-worker.js
docs/hologram/clockwork-view-lattice.jpg
docs/hologram/clockwork-view-traces.jpg
docs/hologram/clockwork-view-crossing.png
docs/shadow-clock-gearing.html
docs/clock/hcc-a-projection.html
docs/clock/hcc-a-systems-manifest.md
docs/hologram/lookref-lattice-hub-uplink.png
docs/gearing/contracts-axle.js
docs/gearing/contracts-gears.js
docs/gearing/contracts-trainA.js
docs/gearing/contracts-trainB.js
docs/gearing/contracts-trainC.js
docs/gearing/contracts-cells.js
docs/gearing/contracts-escape.js
docs/gearing/contracts-audits.js
```

This is the inspected relative closure for those surfaces, not a claim that external APIs, update-pointer providers or every network dependency are captured by 21 paths. Re-derive it for a later candidate. The independent nostd viewer additionally consumes `docs/plans/fixtures/hologram-ir-golden-v0.2.0.json`, which remains read-only.

The manifest's declared GitHub identity and its SHA-relative rawcdn start/scope do not demonstrate same-icon migration between pins. Neither a new commit nor a passing Node fixture establishes an installed update. LATEST promotion, manifest migration and deployment remain outside pass 2's proposed local repair scope.

**Execution capability check:** Node is present. Chromium/Chrome binaries were not found on PATH; Python Playwright is absent; no callable browser/playwright tool was exposed by the registry search. No usable browser was verified. This is a bounded environment observation, not proof that no browser can ever be provided. First-install, warmed offline, GPU images, computed visibility, touch selection and Android installation are all NOT_RUN.

## 6. Held decisions and review coverage

- U01's five SPOKEN locations are already registered. No new exact human-statement locator was recovered by this planning audit. Missing custody and Q0/Q5/slot-II source conflicts remain separate; do not re-stamp or accept answers.
- U02's reserved law pair, U03's hosting acceptance and U07's unassigned σ implementation owner remain explicit. No source/runtime cleanup takes those decisions.
- R1/R3/R4/R5/R6/R7/R9/R10/R11/R12 and the proposed build gates retain their documented scope. A finite source or browser check is not an affirmative ruling.
- The frozen gearing renderer needs its human-designated session. Other free station work need not wait on that designation.

Independent planning work: architecture reviewer read the omitted model catalogs and targeted shape/compute clauses; controls reviewer read the hooks, dispatch/backend and seven indexes, with a second navigation reviewer checking graphics/history/hologram; reference reviewer examined current shelves, consumer carries and supplied-file identity; projection reviewer inspected runtime source and ran the isolated geometry probe; branch reviewer checked the release closure and update selection. Root reanchored, ran the checker, sighted the Kauffman page and integrates the plan. Reviewer agreement is not acceptance.

Limits: historical campaign bodies were sampled for status/routing, not all reread; no fresh full-repository semantic census; no external web freshness claims about targets; no browser/device execution; no physical operation. The current plan deliberately preserves those limits instead of treating them as repaired work.

## 7. Planning receipt

Planning baseline and final checker both exit 0 with **90 advisories** (7 direct-index, 83 unsigned), no stale absence and no law-copy drift. The six-count reduction from pass 1's pre-publication 96 is a rolling commit-window effect, not six new repairs.

Three independent final reviews challenged proof/scope, controls/ownership and source/history. Corrections integrated before handoff:

- Replaced mistaken P2-01 locator shorthand (N12, M2, W10 and nonexistent R2/R3) with the actual Authority/M1/M3/A4/§5 passages. Preserved the valid N12 and W10 refusals. The pending construction plan affirms POINTER-target permission; its stale claim is that the draft's former uncertainty remains a current conflict.
- Made the required later executable-work instruction explicit, separately from document review, publication and frozen-renderer designation. No executable change is authorized or performed by this planning record.
- Specified distinct already-FREE fixture expectations for doc override, direct gear override and the proposed umbrella gear path; a unified protocol is not assumed.

| Planning check | Actual result |
|---|---|
| Scoped finding coverage | 14/14 P2 findings have evidence class, candidate owner/path scope and proposed closure; this is not a fresh semantic audit of all 561 paths |
| Relative links, including new untracked files | 144/144 targets in the three planning/index files resolve; linked Markdown anchors checked |
| Ownership | 3/3 changed planning/index paths resolve to maps; maps gate passes for Codex at the unchanged origin/main base |
| Whitespace | `git diff --check` passes; final new-file whitespace check is part of handoff |
| Commit identity | HEAD and origin/main remain `10fe4947c4450453e7aa1c256cdea294d6981e48`; no commit or push in this turn |
| Repair execution | NOT_RUN, including proposed regression-file creation |
| Browser/device/installation/release | NOT_RUN; source and Node probes do not fill these fields |

Exact planning artifact delta: new `docs/plans/repo-update-pass-2-plan.md`, new `docs/plans/repo-update-pass-2-evidence.md`, and two index rows in `docs/plans/README.md`. The maps claim is transient coordination metadata, not a fourth deliverable. Final handoff requires releasing it, verifying all eleven doc stations and eight gear shafts FREE, and confirming only these three unstaged planning paths remain. No other source, runtime, law, historical receipt, inventory or release path is changed.

## 8. Execution dispositions

**Instruction and base:** the human said “Proceed with pass 2” after the reviewed plan. Fetch and ff-only pull again found `10fe4947c4450453e7aa1c256cdea294d6981e48` unchanged. The three existing planning files were the only incoming local changes. The instruction covers the plan's bounded local document/executable work; it does not publish code, promote a release, designate the frozen renderer editor, or accept Core/emission.

| Finding | Actual local work | Completion and remaining evidence |
|---|---|---|
| P2-01 | Four dated catalog/problem-map entrances now state current source roles and Q4 permission. The pending construction plan's current premises use conceptual-reference roles and no longer carry the old permission conflict | **DOCUMENT CORRECTED.** Four historical bodies are byte-identical after excluding added entrances; valid refusals and receipts remain. Construction execution and emission acceptance remain absent |
| P2-02 | Arrival R3 now maps Piece 9 to dictionary wiring and Piece 10 to document feed | **DOCUMENT CORRECTED**, checked against the manifest; no stream behavior changed |
| P2-03 | Compute/shape distinguish proposed concurrency, import structure versus F-S3, R5 trust, submit boundaries, R6 framing, finite offline tests and unaccepted hosting. G1/G4/G7 were additionally narrowed after independent challenge | **DOCUMENT CORRECTED.** No new gate, ABI, encoding, scheduling choice or ruling. Dependency records, final-row equality and commit ancestry each retain their actual proof limits |
| P2-04 | Kauffman's three clipboard consumers and both reason-map passages now use the printed normalization, angle/turn conventions and inclusive bound. Algorithm and crosswalk routing agree; failure of the bound remains MAY_SLIP | **SOURCE CORRECTION VERIFIED** within printed pp.323–324. Source pages, historical receipts and other mechanism regions are preserved; no physical material calibration or Core field follows |
| P2-05 | Xiao/five-stakes current panels and crosswalks distinguish indexed occurrences from shared token identity; stale fix routes now point to landed corrections | **DOCUMENT CORRECTED** from the earlier bounded source sightings. Vocabulary/count/embedding substitution prohibitions remain; no tokenizer chosen |
| P2-06 | Five shelf entrances link the four closed quiet-door groups and the historical graphics Pass 7 receipt. New runtime tests receive a reader route | **NAVIGATION REPAIRED** for the named shelves. Grouped reachability, direct-index counts and global semantic review are distinct; historical campaign bodies/images unchanged |
| P2-07 | Hook comments/status strings now name existing Layer III code, absent Lace Core and human acceptance. Predicates, registration, executable control flow and advisory algorithms are unchanged | **WORDING AND SYNTHETIC BEHAVIOR VERIFIED.** Both original and revised hooks skip local invocations and call the checker once remotely; startup remains advisory, exit 0 |
| P2-08 | Umbrella gear override dispatch forwards the required reason; backend retains an optional supplied reason. Direct no-reason clearing and doc/gear FREE-state differences remain intact | **SYNTHETICALLY VERIFIED: 15/15.** The same fixture catches both original dispatch failure and wrapper-only NOTE loss. Ordinary operations unchanged; no live override performed |
| P2-09 | HCC header/cards and nostd footer carry the conceptual-reference classification. AB/BC/CA binding descriptions match picker/HUD keys. Holder links opt into pointer events. A concrete frozen-renderer patch is prepared in §10 | **PARTIAL.** Parser/data/key and card-generation checks pass. Frozen HTML remains untouched pending human-named editor; rendered legibility, pointer/touch activation and old pinned displays remain unverified |
| P2-10 | Opaque/floor and transparent draws use distinct uniform buffers; GPU status names submission after it occurs. Failure restores fallback; recovery releases its forced display while preserving REF choice; uncaptured errors stop the failed session | **LOCAL REPAIR / SYNTHETIC CHECKS.** Actual-source stubs cover distinct submitted flags and failure/error→reboot→recovery. GPU pixels, computed visibility and real device loss remain pending |
| P2-11 | Worker ribbons retain width for vertical filaments; inline bake includes corresponding filaments. Fixed D1 values/refs, floor and material/birth semantics are checked | **LOCAL REPAIR / SYNTHETIC GEOMETRY.** Both paths now emit 22 nonzero-area filament triangles covering all 11 birth IDs. Worker/inline sphere tessellation remains 20/16. Visible pixels remain pending |
| P2-12 | Release selection requires matching pointer identity/path and a full SHA; failed selection cannot use main or report fresh. SW request failure, pending installation and installation failure are separate states | **LOCAL REPAIR / SYNTHETICALLY VERIFIED.** Final lifecycle regressions and independent timing counterexamples pass. Actual installed updates and release promotion remain unperformed |
| P2-13 | Removed automatic origin-wide cache deletion on activation; retained existing fetch/precache policy | **SYNTHETIC ISOLATION VERIFIED.** Foreign and other-pin sentinels survive. First-install offline, warmed offline and optional worker/REF availability remain distinct NOT_RUN browser scenarios; unbounded old-cache retention is the deliberate cost of not deleting unowned data |
| P2-14 | Added six bounded Wasm source-sighting rows and their rust-target carries, with 14 visually inspected PDF pages | **BOUNDED SOURCE REQUEST COMPLETED.** Name-based crate absence, configured validator enforcement, real builds and missing target attachments remain unestablished. Cargo A4 stays next |

**Review-driven corrections:** the first runtime suite passed 21 cases but omitted installation still in progress after `update()` returned. An independent counterexample exposed the false “fresh” state. The pending/failure lifecycle was added to E4 before final closure. This behavior follows the distinction between promise resolution and later installation outcomes in the [Service Workers Install algorithm](https://w3c.github.io/ServiceWorker/#installation-algorithm), inspected 2026-09-20 (editor draft dated 2026-09-17): steps 4–7 expose the installing worker and resolve the job; later steps finish or fail installation. A successful request alone is not an installed update.

Other review corrections were the compute G1/G4/G7 limits, two remaining Kauffman crosswalk OPEN cells, M-A2's MAY_SLIP wording and preservation of changed ASCII border positions. These stayed inside the approved file pools. No new source-of-record document or unresolved human decision was rewritten.

**Thesis disposition:** supported for the executed local source/consumer repairs within their evidence fields. A blanket claim of completed rendering, installation, permanent offline operation or human acceptance is not established. The frozen renderer remains conditional; its prepared patch makes the remaining edit reviewable without taking the designation.

## 9. Execution checks and exact file manifest

### Evidence actually obtained

| Check | Result and limit |
|---|---|
| Hook behavior before/after | Unset/false remote flag: 0 checker calls and startup 0. Remote true with checker pass/fail: exactly 1 call and startup 0; only failure reports a failed check. Syntax and normalized executable-form parity pass |
| Hook checker on one pinned fixture tree | Before/after both exit 0 with identical categories: 0 orphans, 7 thin indexes, 0 stale absence, 0 law drift, 83 unsigned. This verifies unchanged wording-only behavior, not universal repo compliance |
| Coordination | `bash docs/coord/tests/force-free-dispatch.sh`: 15/15 current cases; independent isolated baseline and wrapper-only variants each fail at their corresponding original defect. Reason quoting, target fields and untouched sentinels checked; no network or real claims in fixtures |
| Runtime | `node --test docs/clock/tests/runtime-regressions.mjs`: **23/23 pass**. The same final fixture against `LACE_SOURCE_REV=10fe4947c4450453e7aa1c256cdea294d6981e48` passes 5 and fails 18, including the original unawaited-update rejection. Two additional independent in-memory timing counterexamples pass: an already-pending install and a redundant failure before the update continuation. These extra two are separate from the 23 checked-in cases |
| Geometry | Actual worker remains 5,592 vertices; inline becomes 3,864 after adding 66 filament vertices. Both have floor 6, 22 nondegenerate filament triangles and birth coverage 0–10. The fixed D1 oracle checks all WORD values and POINTER refs |
| HCC parser/card generation | Unchanged parsed records: 12 pieces, 8 stages, 7 parameters, 6 acts and 4 edges. All 48 piece×tab renders include the qualification; inline scripts compile. This does not observe browser layout |
| Gear binding join | Six keys and all fields except the three BIND descriptions preserved; AB/BC/CA match existing picker/HUD definitions |
| Wasm sightings | Codex-References visually inspected PDF pages 42, 188–190, 205–206, 209, 253–255, 274 and 282–284. A second reviewer independently spot-checked 42, 188, 254, 255 and 283; other claims were reviewed against the first reader's stated limits |
| Kauffman sighting | Printed p.324 / PDF p.343 visually checked by root and source reviewers. The new normalization sighting complements the earlier inclusive-bound check; no new Xiao full-source pass was performed |
| Browser capability | Bundled Node Playwright exists. Its Chromium/Firefox/WebKit executable paths are absent; an actual headless Chromium launch failed because the executable is missing. No package/browser download or installation attempted. Python Playwright absence did not establish Node-package absence |
| Independent review | Separate reviewers challenged model proofs, source carries, coordination, runtime and prompt updates. Reviewer agreement is not human acceptance |

The lifecycle refinement is complete and independently reviewed. Final repository/link checks and the handoff conditions are recorded in §11.

### Actual file manifest

The following is the exact execution pool, including the three incoming planning files. New files are marked **new**. Claim files are transient coordination state and excluded from deliverables.

| Owner | Changed paths |
|---|---|
| maps: record | **new** `docs/plans/repo-update-pass-2-plan.md`; **new** `docs/plans/repo-update-pass-2-evidence.md`; `docs/plans/README.md`; `docs/coherence-audit-log.md` |
| maps: models | `docs/math-execution-model-pass-6-arrival.md`; `docs/math-execution-model-pass-4-adversarial.md`; `docs/plans/math-execution-wasm-crosswalk-pass-1.md`; `docs/plans/math-execution-wasm-crosswalk-pass-3-adversarial.md`; `docs/plans/math-execution-problem-map-pass-1.md`; `docs/plans/atomic-construction-pass-1-plan.md`; `docs/plans/lace-context-iter7-pass-2-compute-and-inventory.md`; `docs/plans/lace-context-iter6-pass-5-crate-proposal.md`; `docs/reason-model-map.md` |
| clipboards | `docs/clipboards/kauffman-4ed-mechanisms.md`; `docs/clipboards/kauffman-4ed-ascii-machinery.md`; `docs/clipboards/kauffman-4ed-clipboard.md`; `docs/clipboards/xiao-zhu-foundations-clipboard.md`; `docs/clipboards/clipboard-five-stakes.md`; `docs/clipboards/wasm-spec-3-clipboard.md`; `docs/clipboards/rust-target-clipboard.md` |
| kit | `.claude/hooks/session-start.sh`; `.claude/hooks/check-docs.sh`; `.claude/README.md` |
| navigation | `docs/graphics/README.md` (graphics); `docs/history/README.md` (history); `docs/clock/README.md` and `docs/hologram/README.md` (hologram); `docs/gearing/README.md` (gearing-meta) |
| coord | `docs/coord/coord.sh`; `docs/coord/README.md`; **new** `docs/coord/tests/force-free-dispatch.sh` |
| gearing-meta | `docs/gearing/claim.sh`; `docs/gearing/CLAIMS.md` |
| prompts | `docs/prompts/coord-protocol-prompt.md`; `docs/prompts/context-pass.md` |
| renderer | `docs/clock/hcc-a-projection.html`; `docs/hologram/nostd-pipeline.html` |
| gear:gears | `docs/gearing/contracts-gears.js` |
| hologram runtime | `docs/clock/lace-projection.html`; `docs/clock/sw.js`; `docs/clock/projection-bake-worker.js`; **new** `docs/clock/tests/runtime-regressions.mjs`; **new** `docs/clock/tests/README.md` |

No canonical PNG, attached source, law/staking/manifest/emission file, pass 1 receipt/inventory, frozen renderer, other contract shaft, LATEST pointer, web app manifest, icon, lookref or IR fixture changes. No branch, commit, push, deployment or installed-client result is created by this execution turn.

## 10. Prepared frozen-renderer edit

**NOT_APPLIED.** `docs/gearing/CLAIMS.md` requires a human-named designated editor session for `docs/shadow-clock-gearing.html`; a renderer claim or the general Proceed does not supply that designation under this plan. The patch below was prepared and passed `git apply --check` against the unchanged file. Browser layout/touch checks remain necessary after it is applied. The two relative documentation links retain the viewed revision's path context.

```diff
--- a/docs/shadow-clock-gearing.html
+++ b/docs/shadow-clock-gearing.html
@@ -4,7 +4,7 @@
 <meta charset="utf-8"/>
 <meta name="viewport" content="width=device-width, initial-scale=1"/>
 <title>LaceArc / Shadow clock — Gearing Interaction Contracts (WebGPU 3D)</title>
-<meta name="description" content="Projection view: holder machine {A,B,C} as physical clock gearing of lace. WebGPU 3D only. Not Core. Not a sixth graphic."/>
+<meta name="description" content="Conceptual-reference gearing view for HCC-A, Coffee Cup and Water. No required holder machinery or pointer emission. WebGPU 3D only. Not Core."/>
 <style>
   :root {
     --bg: #06080c;
@@ -49,6 +49,7 @@
   }
   header .sub { color: var(--muted); font-size: 0.85rem; max-width: 52rem; margin: 0.35rem 0 0; }
   header .sub-wrap { max-width: 52rem; }
+  .reference-note { color: var(--ink); font-size: 0.8rem; line-height: 1.4; margin: 0.5rem 0; max-width: 52rem; }
   header .sub-wrap summary {
     display: none;
     color: var(--muted);
@@ -424,10 +425,11 @@
 <body>
 <header>
   <div>
-    <h1>LaceArc / Shadow clock — Physical Gearing</h1>
+    <h1>LaceArc / Shadow clock — Conceptual Gearing View</h1>
+    <p class="reference-note">HCC-A, Coffee Cup and Water are conceptual reference documents. This gearing view does not define required holder machinery, runtime dependencies or conditions for legal append.</p>
     <details class="sub-wrap">
       <summary>Layer III · emission [GAP] · WebGPU 3D</summary>
-      <p class="sub">Holder machine {A, B, C} as clock gearing on one lace axle. Projection only — not Core, not a sixth graphic, not WORD/POINTER rows. POINTER emission stays [GAP]. <strong>3D WebGPU required</strong> (no 2D primary view).</p>
+      <p class="sub">Concepts from HCC-A, Coffee Cup and Water illustrated as {A, B, C} gearing on one lace axle. Projection only — not Core, not a sixth graphic, not WORD/POINTER rows. POINTER emission stays [GAP]. <strong>3D WebGPU required</strong> (no 2D primary view).</p>
     </details>
   </div>
   <span class="badge">REQUIRES WebGPU · 3D only · Layer III</span>
@@ -492,7 +494,8 @@

     <div id="chips"></div>

-    <h3>Interaction contract</h3>
+    <h3>Recorded interaction contract</h3>
+    <p class="reference-note" id="contractQualification">This selected contract preserves an earlier conceptual interpretation. Its gears, requirements and [H]/[I]/[X] stamps do not establish required holder components or supply pointer emission. Graphic D and the current law govern Lace.</p>
     <div id="contract">Click a hot target on the clock…</div>

     <div class="wall-note">
@@ -507,8 +510,8 @@

     <h3>Docs</h3>
     <div class="links">
-      <a href="https://github.com/NFDFLDTHRY/laceArc/blob/main/docs/shadow-clock-hologram.md" target="_blank" rel="noopener">shadow-clock-hologram.md</a>
-      <a href="https://github.com/NFDFLDTHRY/laceArc/blob/main/docs/shadow-clock-agent-brief.md" target="_blank" rel="noopener">shadow-clock-agent-brief.md</a>
+      <a href="clock/shadow-clock-hologram.md" target="_blank" rel="noopener">shadow-clock-hologram.md</a>
+      <a href="clock/shadow-clock-agent-brief.md" target="_blank" rel="noopener">shadow-clock-agent-brief.md</a>
       <a href="https://github.com/NFDFLDTHRY/laceArc/blob/main/docs/staking-the-workspace.md" target="_blank" rel="noopener">staking-the-workspace.md</a>
       <a href="https://github.com/NFDFLDTHRY/laceArc/blob/main/docs/systems-manifest.md" target="_blank" rel="noopener">systems-manifest.md</a>
       <a href="https://github.com/NFDFLDTHRY/laceArc" target="_blank" rel="noopener">NFDFLDTHRY/laceArc</a>
```

## 11. Final local validation and handoff

- **Scope:** all 43 deliverable paths match the exact manifest: 38 tracked modifications and 5 new files. No staged paths or new commits. HEAD and origin/main remain `10fe4947c4450453e7aa1c256cdea294d6981e48`.
- **Repository checker:** exit 0, still 90 advisories: 7 direct-index warnings and 83 unsigned commits in the unchanged history window. Zero reported orphans, stale absence and law-copy drift. The inventory-reference and direct-index limitations recorded in §3 remain; warning-count stability does not erase the navigation repairs.
- **Changed-document validation:** all relative targets in 31/31 changed Markdown files resolve; new/changed anchors were checked separately. The 33/33 named historical route targets are reachable through their intended grouped entrances, and both new runtime-test routes resolve. File scope, diff whitespace and new-file whitespace pass. The prepared frozen patch passes `git apply --check` without changing its target.
- **Aggregate synthetic checks:** fresh runs of the final working tree pass 23/23 runtime checks and 15/15 coordination checks. Independent source/model/control/runtime/prompt reviews have no outstanding material correction. The final receipt review corrected a dating ambiguity by labeling the plan's original pre-Proceed authorization paragraphs.
- **Remaining evidence:** no usable browser executable, rendered GPU image, computed CSS visibility, touch result, physical/device/installation or offline session was obtained. No Core or human decision was supplied. The frozen renderer's exact edit is prepared but not applied; release metadata remains unchanged.
- **Handoff condition:** after this final receipt write, release maps, verify all eleven doc stations and eight gear shafts FREE, and verify only the 43 unstaged deliverable paths remain. All other editing stations/shafts have already passed their gates and been released. The maps gate and final working-tree check are the last coordination actions; no commit, push or deployment is part of this turn.
