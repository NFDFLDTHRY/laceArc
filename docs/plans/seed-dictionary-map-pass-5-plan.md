# Seed dictionary × model — Pass 5 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `11faa50`).
**Depends on:** passes 1–4 EXECUTED (seed L1 · def-route shape · first-touch L4).
**Emission:** `[GAP]`. No corpus loaded. No `src/`.

Pass 4 named first touches. Pass 5 sits the **ingest face** of seed: Hopper, G1 identity, OPEN #15 “is a seed corpus shipped?”

---

## 0. One question

Where does a dictionary stream *enter*, how does a spelling count as the same \(v\), and is any seed file already on the lace?

## 1. One result (on execute)

[`seed-dictionary-map-pass-5-ingest.md`](seed-dictionary-map-pass-5-ingest.md) — Hopper / G1 / #15.

---

## 2. Items execute must mark

| Item | Expected (test) |
|---|---|
| Hopper (P27) | ingest face of Contract II; **NAMED-UNMAPPED** as own I-letter; Arrive is the equation |
| G1 word identity | \(\theta_{\mathrm{G1}}\) still unpicked; seed does not intern |
| OPEN #15 seed shipped? | **HOLE** — no file on \(L\); workspace refs ≠ Graphic D |
| Seed as L1 given a stream | forced-legal **if** a stream is fed; feeding is not this pass |
| Tokenizer / MASK | still CONFLICT-WRITE |

Do not ship a dictionary in this file.

## 3. Forbidden this pass

- Commit a word list
- Fill \(\theta_{\mathrm{G1}}\) or \(\theta_{\mathrm{when}}\)
- Close #15 by fiat
- Open `src/`
- Start pass 6 unless immediately authorized

## 4. Accept pass 5 when

- Hopper, G1, #15 each marked.
- No corpus bytes on main.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 6 is immediately authorized.

## Steward test

Shoe in hands. The hopper is the hole you feed the dictionary through. **An empty hopper is not an empty theory. It is also not a loaded lace.**
