# The reduction — pass 4 plan: the collaborative agentic apparatus, applied

**Status:** **`EXECUTED`** → [roles.md](../kit/roles.md), advisory check, prompt line. Thesis held: three of seven applied; four to the brief. **Station:** kit · prompts. **Emission:** `[GAP]`. No `src/`.
**Campaign:** [the reduction](reduction-pass-1-plan.md) — six passes inside the human's added budget (2026-09-19 22:11 PT), weighted to his three results: **(1) the mathematical model of execution — the #1 result; (2) the global ASCII systems diagram updated; (3) the collaborative agentic apparatus.** **Pinned:** `e2e8155` + branch tip. **Editor:** Claude Fable 5.1; one hand; declared.

## Question (one act)

**Which of F1–F7 can be applied now as artifacts on stations this campaign may hold, without a human ruling, and which must wait?**

## Measured before proposing
[Iteration 7 pass 1](lace-context-iter7-pass-1-actors.md): 810 of 1,035 commits unsigned; 4 force-frees in 48 h; 20 task-as-identity claims; the law station edited by an anonymous campaign. F1–F7 proposed, none applied. `CONTRIBUTING.md` is law station (locked to the human by F3 as proposed); `coord.sh` is code on coord.

## Thesis / falsifier
Three of seven can land now as kit/prompts artifacts (role cards; the trailer convention; an **advisory** unsigned-commit count in `check-docs.sh`); four need the human (CONTRIBUTING line, claim duration in `coord.sh`, law allow-list, push for cloud sessions). Falsifier: an artifact here that changes what any agent is *permitted* to do — that would be a control change disguised as documentation, and it must be pulled.

## Steps
4a `docs/kit/roles.md`: one card per actor group — role · stations · must not · evidence locator · join steps · trailer format · claim-duration convention (voluntary until coord rules) · 4b `check-docs.sh`: one advisory line, *N of the last 200 commits carry no actor trailer*; run it; keep `exit 0` semantics · 4c the join prompt already exists (diagram-prompt); add the roles pointer · 4d the four human items to the pass 5 brief.

## May write / must not
`docs/kit/roles.md`, `docs/kit/README.md` row, `.claude/hooks/check-docs.sh` (advisory only) — kit; `docs/prompts/lace-context-diagram-prompt.md` one line — prompts. **Must not:** edit CONTRIBUTING / AGENTS / coord.sh / any `.station` OWNS; add a gate; name a designated editor.

## Expected vs actual
Advisory check reports ~78% unsigned at the pin; roles.md cites pass 1 for every claim. **Actual:** `NOT_TESTED`.

## Stop
Artifacts written; check-docs runs green with the new advisory; nothing enforced.
