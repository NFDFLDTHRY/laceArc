# Atomic construction mapping: Pass 1 plan

**Status: PLAN / PROPOSAL. Planning complete; Pass 1 execution NOT_RUN.**
**Station: maps. Editor: Codex-atomic-pass1. Documentation only. POINTER emission remains [GAP].**

The human authorized taking the maps station and planning Pass 1 after proposing a connection between the existing atomic primitives, all fifteen manifest pieces, and the seven planned branch contracts. This document defines that pass. Its numbering belongs to the construction mapping; it does not reopen the reason-model map's completed three academic mapping passes.

Initial source and claim base: [b7d4891545de712832dece67163d69ac276996d3](https://github.com/NFDFLDTHRY/laceArc/tree/b7d4891545de712832dece67163d69ac276996d3). Its parent, 24c6348e5bec4918b61846360cd28bca4a52937d, is the substantive source baseline. The plan was re-anchored and maps reclaimed at [a793aa95149b92ff2be1466c600cfb4891510d92](https://github.com/NFDFLDTHRY/laceArc/tree/a793aa95149b92ff2be1466c600cfb4891510d92); its controlling source contents are unchanged. Recheck live main and relevant source identities before execution.

## 1. One question and one result

**Question:** Can every proposed branch guarantee be traced through the existing primitives and manifest mechanisms to an explicit source, an identified provider of each required premise, and a bounded proof obligation, while preserving one strand and every unresolved construction rule?

The planned result is a construction crosswalk inside [atomic-primitives-map.md](../atomic-primitives-map.md). It must expose where the seven branch cards rely on a premise that no earlier task or accepted source has established. Merely drawing an acyclic call diagram cannot establish that the proposed construction works.

Success means complete accounting of the assigned claims, including precise open obligations. It does not mean that those obligations have been discharged. A row can be fully mapped and still be blocked for implementation.

| Work boundary | Output and permitted effect |
|---|---|
| This planning task | This plan only, plus this editor's maps claim/check/release metadata through the coordination protocol. |
| Planned Pass 1 | Extend `docs/atomic-primitives-map.md` with the construction crosswalk, source differences, counterexamples, and dated receipt. Preserve its existing identifiers and historical evidence. |
| Reference plans | Read the crate map and branch cards. Record any required amendment as a finding; this pass does not silently change their tasks, birth order, or allowed callees. |
| Authority and other stations | Read graphics, manifest, full laws, controls, and emission draft. An amendment belongs to its owning station and exact authorized task. |

No Core code, type definition, constructor, executable emitter, test implementation of an invented rule, Cargo file, branch directory, or implementation branch is an output. No renderer, gearing shaft, academic clipboard, or historical recovery is edited by this pass.

## 2. Authority and source order

Keep the complete [staking law](../staking-the-workspace.md) and [live law](../law-why-these-documents.md) binding in full. This plan points to them; its tables do not replace them.

1. Ground the review in one open, two-ended cord whose earlier passes remain. Physical manipulation by this agent is **NOT_PERFORMED**. A source-grounded shoe analogy is not an observation or emission algorithm.
2. Open the [graphics index](../graphics/README.md) and canonical [A](../graphics/from-words-to-worlds.png), [B](../graphics/mechanisms-in-3d.png), [C](../graphics/n-dimensional-relationships.png), and [D](../graphics/data-structure-1d-array.png). Graphic D remains the sole store; A–C describe the same mechanism.
3. Read the [manifest](../systems-manifest.md): Pieces 1–15, interface-coherence check, composition, I/II/III seams, and open questions 1–11. Cross-check its claims against the panels.
4. Read [AGENTS.md](../../AGENTS.md), both full laws, [agent controls](../kit/agent-control.md), and the [task/proof forms](../kit/agent-task-template.md). Use [reason-model RM1–RM10](../reason-model-map.md#puzzle-pieces-of-the-physical-object-reason-model-only) and its rejected imports as boundary checks.
5. Read the [atomic map](../atomic-primitives-map.md), [crate map](rust-nostd-crate-map.md), and all seven [branch cards](rust-nostd-branch-contracts.md). These are proposals and explanatory maps, not amendments to the sources.
6. Read the [emission draft](../pointer-emission.md), and [close reading §§1–4](../graphics-close-reading.md#the-evidence) against D1. Keep the draft's lack of acceptance and any disagreement with the graphics explicit.

The five academic texts and three holder texts supply no new Core primitive in this pass. Their established boundaries remain in the full laws and existing maps. This task does not require another book pass or importing PDFs into git.

## 3. Three dependency relations, recorded separately

| Relation | What it records | Required check |
|---|---|---|
| Proposed direct call | The named first-party callee a branch is permitted to use. | Preserve the nine existing edges in crate-map Page D / branch-card Page F. |
| Serial birth prerequisite | Which task must be proven before the next branch may exist. | Preserve the seven births, hence six adjacent graduation gates. Birth order does not add a call edge. |
| Required premise | A fact a particular guarantee needs, such as immutable retained content or an existing same-line target. | Name its source/provider, scope, evidence, and acceptance where required. A premise can remain open even when the call graph is acyclic. |

A branch label is not evidence that its task is implemented or proved. A required premise with no established provider is an **OPEN obligation**, not an implicit helper, extra crate, new row kind, or permission to start a later birth.

Record the global implementation gate separately from the premise audit. The inspected emission draft has no human acceptance, so branch births remain ineligible. That fact does not answer where an individual storage, identity, layout, or read guarantee obtains its premises. Pass 1 must trace each such premise instead of marking every row only “blocked by emission.” A conditional documentation analysis does not open the implementation gate.

Each future crosswalk record must contain the following fields. These are documentation fields, not a Lace data schema.

| Field | Required content |
|---|---|
| Identity and role | Existing P, K, manifest Piece, branch, or edge identifier; carrier, occurrence identity, entry form, effect, formation, constraint, arrival surface, or view. |
| Authority | Exact panel or prose section and source revision; distinguish LAW, SOURCE, INFERENCE, and PROPOSAL. |
| Inputs | The stated domain and representation assumptions; no default for an unanswered input question. |
| Preconditions | Each condition needed before the claimed operation or read. |
| Premise provider | Exact source or earlier task that must establish each condition. Mark missing or unproved providers OPEN; do not turn an obligation into an implemented API. |
| Output and permitted effects | What the task promises; which retained object, if any, may grow. State read-only effects explicitly. |
| Preserved constraints | Applicable K1–K6, occurrence distinctions, and one-strand authority. |
| Failure and resources | Defined guarantees, excluded claims, and unresolved bounds, input custody, interruption, or failure premises. |
| Proof obligation | Claim, assumptions, scope, and connection to the source. Separate statement, proof sketch, reviewed proof, and test. |
| Composition | Callee precondition required, caller evidence offered, and exact proved revision if one exists. Identify absence honestly. |
| Counterexample | A concrete way the guarantee could fail or depend on an unrecorded operation. |
| Status | Separate claim origin, evidence state, human acceptance, and enforcement as required by control C10. |

## 4. Pass 1 work sequence

### Step 1: pin the evidence and scope

Pull current main with fast-forward only, resolve ownership, claim maps, and record the clean/dirty starting state. Pin the source commit and identities of the graphics, laws, manifest, atomic map, and both branch plans. If the source changes during the pass, identify the affected claims and reread them before refreshing the claim. Preserve other agents' work.

Record the existing output's dated observations separately from the new pass. The original atomic-map receipt describes its own earlier revision; do not silently turn it into evidence about the current tree.

### Step 2: expand the five existing primitive obligations

Retain P0–P4 and K1–K6. Do not recast all five P items as callable operators or five stores.

| Existing item | Role to preserve | First construction question to trace |
|---|---|---|
| P0, one strand / one array | Carrier; Pieces 1 and 6 describe the same retained object. | What assumptions let the first planned birth retain immutable sections without depending on a later layout birth? |
| P1, occurrence at a position | Identity and order intrinsic to the carrier. | Which obligation establishes a fresh permanent position, preserves its content association, and prevents a foreign or display-only number from becoming authority? |
| P2, WORD | Entry form for a distinct occurrence. | Which guarantees preserve the supplied value, and which still need accepted word-identity and representation premises? |
| P3, POINTER | Entry form referring to earlier entries on the same line, including POINTER entries. | Where are existence, same-line identity, and earlier-position evidence established, separately from target selection, timing, arity, and slot assignment? |
| P4, APPEND | Permitted growth effect. | Given an independently admitted section, what preserves the old prefix, and who establishes that admission? |

For P4, use the existing conditional obligation: for a valid finite retained prefix L and an independently admissible immutable section s, a successful extension yields L followed by s and preserves every earlier index/content pair. Trace every premise. This statement does not select s, prove its admission, determine how many sections an arrival produces, or promise hardware atomicity or crash consistency.

The carrier, identity, and constraints may have no independent operation or output. State that fact instead of inventing one to fill a column. Preserve the existing meaning of “atomic”: small specification obligations, with mathematical minimality and full construction still UNESTABLISHED.

### Step 3: cover all fifteen manifest pieces

Extend the existing fifteen-row composition table or add a linked companion table in the same atomic-map file. Each piece appears once as the primary row; many-to-many references to primitives and planned branches are expected.

| Piece | Required distinction in the execution result |
|---|---|
| 1, Continuous strand | Same carrier as Piece 6; no second strand contract or second end-store. |
| 2, Governing rule | Full arrival/routing/participation obligation exceeds a conditional storage append. |
| 3, Arrival | Contract II remains a thin dependent surface; the seven-door list has no arrival crate. Record its required premises without adding one. |
| 4, WORD | Preserved value and distinct occurrence address remain separate obligations. |
| 5, POINTER | Reference constraints do not supply selection, arity, timing, adjacency, or slot assignment. |
| 6, Sole array | P0 and the retained effects of P4, shared by every relevant path. |
| 7, Star | Formation in Core; `view_star/` reads it. Membership alone is not the full formation. |
| 8, Sequence as route | WORD arrival order and total array order remain distinct when POINTER sections intervene. |
| 9, Dictionary wiring | Feed through Contract II and I; a view may inspect the retained result. No lexicon engine. |
| 10, Document feed | Same Arrival/Core path; projection does not perform ingest or own documents. |
| 11, Touch / re-entry | Later participation can name earlier POINTER sections. Permission does not establish an emission event. |
| 12, Dimension | Configuration/reading of I and dependent projection; no Contract IV, dimension field, or depth metric by default. |
| 13, Projection | III reads the same retained structure; no write-back or inference of missing participation from pixels. |
| 14, No meaning / no collapse | Constraints across the construction, not another operator or stored object. |
| 15, Star-internal traffic | Distinct retained passages and participation; no interior-state table or new traversal algorithm. |

Every row must cite the relevant A/B/C/D panels, existing P/K items, contributing proposed branch responsibilities or boundary surface, applicable gaps, and one failure case. A source gap does not justify leaving the row unmapped or assigning it a convenience engine.

### Step 4: audit seven branch promises and nine calls

Preserve the existing tasks and proposed direct callees. The focus column is the question to investigate, not a concluded defect or a proposed implementation.

| Birth | Branch | Proposed direct callees | First premise to investigate |
|---|---|---|---|
| 1 | strand/ | none | Immutable admissible section content before word/ and pointer/ exist; retained payload stability, fresh positions, resources and failure scope. |
| 2 | word/ | none | Value preservation and comparison under explicit identity premises; occurrence addresses remain a strand/composition responsibility. |
| 3 | pointer/ | none | Inhabited valid references with existing/same-line/earlier evidence, while the layout leaf has no strand callee. |
| 4 | route/ | strand/, word/, pointer/ | The provider of section admission and transfer of the three callee preconditions; no missing emission rule hidden in Continue. |
| 5 | view_star/ | strand/ | Retained WORD-value and identity evidence through its read surface, without a second equality policy or unlisted word/ dependency. |
| 6 | view_proj/ | strand/, view_star/ | Source-grounded output and non-writing capabilities through both paths to the same strand. |
| 7 | core/ | route/, view_star/, view_proj/ | Establishing all called preconditions, separating append and read capabilities, and preserving one history across convergent calls. |

For each of the nine calls, write a distinct caller-to-callee record: required input facts, offered facts, permitted capabilities, returned guarantee, open obligation, and source citation. Do not infer an extra direct edge from a transitive path or a birth prerequisite.

Then inspect **all remaining branch premises**, including leaves with no callees and the shared Arrival Surface. A complete nine-edge table would still miss admission or representation assumptions supplied by no edge. Group these in a premise ledger and classify their proposed provider as source contract, earlier task, Arrival Surface, view convention, or UNESTABLISHED. Those are review classifications, not new runtime components.

If a promise appears to require an unborn task, circular assumption, unlisted dependency, or hidden second task, record the exact conflicting sentences and affected claim. Keep the current plan intact and specify the amendment needed for later review. Do not repair the seam by inventing a type, unchecked constructor, raw-byte escape hatch, allocator, or emitter.

### Step 5: attack the proposed crosswalk

Use the following as **planned document-review challenges**. None has been executed against a new crosswalk by this planning task. They are not Core tests or a permitted test implementation.

| Challenge | Required distinction or failure report |
|---|---|
| Start with A1's empty strand | Identify which obligations have an empty case; do not pretend an earlier POINTER target exists before any entry. |
| D1: PIE at 0000 and 0003 | Both positions remain distinct. Equal values do not establish one occurrence or complete word-identity policy. |
| D1: POINTERs 0007, 0009, 0010 | Preserve participation in earlier participation; no second edge store and no WORD-only restriction. |
| D1: 0002 uses 0000 → 0001; 0004 uses 0003 → 0000 | Each operand is earlier than its containing POINTER. Operand order itself is neither uniformly ascending nor descending. |
| D1: consecutive WORD occurrences 0001/0003 and 0005/0008 lack a linking POINTER in the depicted prefix | Challenge an automatic pointer-per-consecutive-word rule. Do not infer a replacement emitter, that adjacency is never a relation, or that a later reference is impossible. |
| A repeated word or holder's “touch” is offered as admission | Require the missing source rule; neither equality nor analogy supplies it. |
| A numeric target is in range but belongs to a different hypothetical strand | Index inequality alone cannot establish same-line membership. This is an adversarial input, not permission to implement multiple Lace stores. |
| A layout type has no lawful inhabitants | Exclusion of bad states alone does not prove the task can represent the permitted WORD/POINTER participation. |
| An old payload changes through an alias, callback, or another reachable capability | Stable indices alone do not establish K1. Trace every admitted mutation path in the proposed guarantee. |
| A view is handed a route/append capability | The call name “read” does not prove non-writing behavior. Both direct and transitive paths matter. |
| A star is replaced by a set of matching WORD indices | Check the omitted route and participation evidence in A12/B9/D1. Membership coverage is not full structural fidelity. |
| C6's four route families become a field or four stores | Preserve the schema gap and the one-strand constraint; a view label is not evidence that the family is encoded. |
| A finite capacity is exhausted or an operation is interrupted | Record the exact promised failure scope and remaining resource/input-custody premises. Do not assume unlimited storage, all-or-nothing execution, index reuse, or silent loss. |
| The global emission gate is assumed open | Require exact accepted content and applicable task premises. A plan, commit, successful check, or claimed proof is not human acceptance. |

D1 is a fixed source specimen: eleven depicted rows, five WORDs and six POINTERs. It may witness or refute a bounded claim. It is not generated output, a training set from which to infer emission, or proof of arbitrary-arrival completeness.

### Step 6: resolve and issue the bounded result

Perform propose, attack, and resolve against the same source revision. One agent may perform all three and must record that independence limit. Resolve only what the evidence supports; retain remaining objections with affected claims and necessary evidence.

The execution result must separately report:

- Coverage of five P items, six K constraints, fifteen pieces, seven branch cards, nine calls, and six serial graduation gates.
- Source-supported obligations, conditional deductions, proposal-only boundaries, and exact source conflicts.
- Every required premise with an established source/provider or a precise OPEN status.
- Actual document inspections and any failures, distinct from future proof, physical, implementation, and runtime evidence.
- One next narrow question selected from the most consequential unresolved dependency. Selecting it does not execute another task or alter the emission draft.

## 5. Questions and conflicts that must survive the pass

Preserve G1–G7 from the atomic map. Add subordinate questions or source-linked notes where needed; do not renumber the existing gaps or hide several questions behind one assumed solution.

| Existing boundary | Required treatment |
|---|---|
| G1: arrival and word identity | Keep representation/value preservation, occurrence identity, and the decision that two values name the same variable distinct. |
| G2: POINTER admission/emission | Keep timing, target selection, arity, WORD/POINTER interleaving, adjacency, and repeated-word behavior separate. Add explicit linkage to manifest Q9's slot-assignment question. |
| G3 and G5: route families | Carry manifest Q11 and C6's four named families without adding a discriminator, new row kind, or new store. |
| G4 and G5: reads and fidelity | Distinguish existing participation from a view's layout choices, and finite backward-reference descent from an unspecified general traversal. |
| G6: concurrency | Repository station coordination is steward machinery, not a Core scheduling or atomicity rule. |
| G7: concrete realization | Preserve capacity, encoding, stable payload, failure, interruption, input-custody and composition premises. No resource or persistence strategy is selected. |
| Manifest Q10: eventual participation | The D1 pattern does not require that every entry eventually be referenced. The last depicted entry does not establish an exception rule either. |
| Emission draft §4 vs D3/D6 and AGENTS.md | Earlier POINTER targets are explicitly permitted by the sources. The draft's uncertainty about that permission is a conflict, not a reason to erase it. Timing and selection remain open. |
| Manifest Piece 13 vs full store prohibitions | Do not admit a forbidden database or table by calling it a disposable view. Refer any needed wording correction to the law station. |
| Mathematical-model sufficiency wording flagged by the atomic map | Emission acceptance alone cannot discharge unrelated identity, read, representation, or resource obligations. Follow control C03's task-specific gate. |

The holder's HCC-A, Cup, and Water mechanisms remain [H], and their displays remain [III]. Their permission, phase, ledger, or talk-pointer operations cannot become a premise provider for a Graphic D append decision. This boundary is inherited from the full laws; the pass creates no new interpretation of those texts.

## 6. Completion criteria for executing Pass 1

Pass 1 is complete as a **documentation audit** when all of the following are evidenced:

1. Every required item in the coverage inventory is present, uniquely identified in its primary table, and traced to exact sources. K1–K6 constrain the whole composition; they do not become six operators.
2. Every claim needing a premise names its provider and evidence or stays OPEN with a precise missing obligation. No row earns a success label from a placeholder such as “valid input” or “earlier work handles this.”
3. The seven task names, their order, and nine direct edges match both reference plans. A discrepancy is reported with citations and remains unresolved unless separately authorized for correction.
4. All fifteen mechanism rows keep Core formation, Arrival feeding, and Projection reading distinct. Pieces 1/6 retain one carrier; dictionary/document feeds and the two views retain their different effects.
5. Every listed challenge receives an actual bounded assessment against the produced crosswalk. Expected and observed outcomes are separate. Failures and open objections remain in the output.
6. D1 retains all eleven depicted rows and their ordering wherever used. No case is rewritten to make a proposed rule fit, and no generalized emitter is inferred.
7. Claim origin, evidence, human acceptance, and enforcement remain separate. No implementation proof, minimality proof, physical-cord experiment, or emission acceptance is claimed by this prose exercise.
8. All relative links in the edited artifact resolve; existing repository documentation checks are run and their actual result recorded. A failed check is not changed or bypassed to obtain a pass.
9. The final diff contains only the assigned atomic-map output and this editor's coordination metadata. Current main and relevant source identities are checked before the drop; refresh and reread if they moved.
10. The result ends with the bounded verdict and next question, then stops. No branch birth or automatic Pass 2 follows.

These are exit criteria for the planned pass. They are not a claim that the pass has run or that all implementation premises can presently be satisfied.

## 7. Planning review and evidence receipt

**Propose:** Make the new pass account for inputs, effects, invariants, and proof premises across the existing source map and branch plans. Use separate records for calls, birth gates, and premise providers.

**Attack:** An acyclic graph could conceal an unborn layout dependency; “admissible section” could conceal the entire missing emitter; a non-writing view could still return invented participation; an empty valid-reference type could make a safety statement vacuous. A complete table could be mistaken for completed implementation proof.

**Resolve:** Require explicit provider/evidence fields, leaf and Arrival premise review, legal-behavior witnesses alongside forbidden-state checks, and separate coverage/proof/acceptance statuses. The identified seams remain questions for Pass 1. This planning review was performed by **one agent**, not independent reviewers.

| Field | Observed scope |
|---|---|
| User authorization | “That is an extremely important direction. Take and plan pass 1,” following the proposed maps/atomic-construction scope and the supplied coordination workflow. |
| Repository and base | NFDFLDTHRY/laceArc; initial source/claim base b7d4891545de712832dece67163d69ac276996d3; re-anchored to a793aa95149b92ff2be1466c600cfb4891510d92 on existing main. |
| Initial working state | Clean at 1fa7da9; fast-forwarded through the folder/protocol updates and renderer restoration before the published claim. No pre-existing local draft was overwritten. |
| Coordination | `which` assigns both the atomic map and this plan to maps. Claim published as Codex-atomic-pass1 at b7d4891; the protocol's refresh/check passed against that tip before authoring. Native HTTPS push lacked credentials; the connected GitHub Git-data tools published the claim with an exact matching tree and a non-force ref update. |
| Graphics | A/B/C/D opened at original resolution during planning: VISUALLY_OBSERVED. Physical-cord manipulation: NOT_PERFORMED. |
| Prose read | Full standing orders, staking/live laws, manifest, atomic map, crate map, seven branch cards, control C01–C12, task/proof forms and emission draft; reason-model RM1–RM10, relational mapping/seams and open questions; close-reading §§1–4. |
| Adjacent sources | No new full reading of the supplied academic or holder texts. Their existing exclusion boundaries are preserved through the full laws and source maps. No PDF added. |
| Source pin and changes during reading | The intervening e3719ef → 24c6348 changes concerned renderer/audit restoration and station releases. The cited primitive, manifest, law, graphics, crate-map and branch-card source contents were unchanged. The later claim commit changes only station metadata. |
| Failed gate and recovery | The pre-commit gate correctly rejected the stale b7d4891 base after main reached 5aaf101. That upstream commit had released this editor's maps claim. The draft was preserved, main fast-forwarded, FREE ownership confirmed, and maps reacquired through the protocol. Only the identical plan was restored; stale claim metadata was not restored. |
| Final source re-anchor | Main then advanced to a793aa9 through renderer changes and a dated path sweep in prompts/clipboards/defrag notes. Those changes were inspected. The graphics, manifest, laws, controls, atomic map, reason map, close reading, emission draft, crate map, and branch cards remain byte-identical to the initial source base. Maps was reclaimed at that tip before the final plan edit. The atomic map stays at the docs root. |
| Planning evidence | Ownership and claim checks passed; all four graphics were inspected; five primitive roles, fifteen pieces, seven proposed branches and nine allowed direct calls were located in the sources. With the new plan staged, the existing documentation check passed, as did staged and working-tree whitespace checks. These checks establish documentation consistency, not construction proof. |
| Plan status | The plan, coverage requirements, review-record fields, challenge set, source conflicts, and stop condition are written. This is planning evidence only. |
| Pass 1 execution | NOT_RUN. No new construction crosswalk, completed challenge results, implementation, runtime test, or proof is claimed. |
| Acceptance and enforcement | Exact emission acceptance is ABSENT in the inspected state. These documents are ADVISORY; no claim of enforced runtime safety. |
| Authored product | `docs/plans/atomic-construction-pass-1-plan.md`; coordination metadata is maintained only through the assigned station workflow. |
| Next permitted task | Execute this bounded documentation pass when instructed, beginning with current-source/claim verification and the five primitive obligation records. |

**Next narrow question:** Beyond the global implementation gate, which claimed guarantee in the seven-card construction first depends on a premise whose provider is missing, circular, or later in the required birth order?

**Stop boundary:** deliver this plan and its verification. Pass 1 execution and Core implementation remain separate tasks.
