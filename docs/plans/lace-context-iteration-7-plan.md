# Iteration 7 — the team, the distribution model, and the component inventory

**Status:** `[PROPOSAL]` iteration. Passes 1–3 executed; passes 4–6 scoped. **Freeze:** the human stated 2026-09-19 21:23 PT that all other agents are paused for the duration of this work and the repo will not change underneath it; §3's local-claim caveat is therefore moot for this sitting and the bundle lands on `e2e8155` clean. **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Pinned base:** `e2e8155942b6ef084d2ef9f4a05fcf8fb1dd829f`; **stacked on** branch `lace-context-iteration-6` (its receipt is the board this iteration inherits). **Editor:** one agent (Claude Fable 5.1, cloud, no push credential); propose / attack / resolve by the same hand — declared.
**Convened by:** the human, 2026-09-19 21:12 PT, in this session. The subject is his, in his words, and each clause is carried to the pass that owns it:

| The human said | Pass |
|---|---|
| *"The agents are not playing together as a team … identify every single contribution actor by source model and utilize that evidence to align the repo with individual agent capabilities"* | **1** |
| *"the target distribution model is a GitHub custom domain … locked to a glacier I can update on a 3 month basis … must never once require an android 17 pixel 9a and family reconnect to the server after install … Githack is our development install"* | **kit amendment** ([distribution-model.md](../kit/distribution-model.md)) |
| *"a combined heterogeneous compute model across wasm64, WebGPU, WebNN, and WebGPU shading language parallel / concurrent execution model"* | **2** |
| *"describe the entire global systems manifest of components consistent with the global ASCII systems diagram"* | **2** (inventory) |
| *"[the ASCII diagram] itself is in need of a critical systems level audit"* | **3** |
| *"construct the environment whose execution will simulate lace against all relevant included and missing external references"* | [iteration 6 pass 4](lace-context-iter6-pass-4-environment.md) already; **4** extends it to *simulate* (run the D1 witness and any candidate σ mechanically, Layer III) once R7 gives it a σ |
| *"I will link to my business domain only what I can prove under a show your work mathematical model"* | **5** (the promotion rule, prepared for the human) |
| *"Make your reference material requests one item at a time"* | the receipt carries the queue; each reply asks for exactly one |

## 1. Thesis, stated so it can lose

**The team failure is an attribution failure first and a capability failure second: the tree cannot say who did 78% of its own commits, and once it can, the demonstrated behaviours sort cleanly into roles that the existing station protocol can enforce with two additions (a required actor trailer and a claim-duration flag) and one lock (the law station).**

**Falsifier:** if the anonymous 773 commits, read by style and by station-claim names in the same windows, cannot be assigned to at most a handful of actors with consistent behaviours, the thesis loses — the failure would then be noise, not roles. **Pass 1 tests this.**

## 2. The six passes

| Pass | Question | Product | Station |
|---|---|---|---|
| **1** | Who has touched this tree, by model, and how did each behave? | [actors and roles](lace-context-iter7-pass-1-actors.md) — census with git evidence; capability map; role assignment; protocol fixes F1–F7 | maps |
| **2** | What is the law-clean heterogeneous compute model, and what is the complete component inventory it sits in? | [compute model and inventory](lace-context-iter7-pass-2-compute-and-inventory.md) | maps |
| **3** | Does the global ASCII companion survive a *systems-level* audit — read as one diagram rather than as a 26-pass log? | [systems audit](lace-context-iter7-pass-3-audit.md) — **executed**: 0 contradictions; 3 literal-reading defects captioned; 6 missing boxes admitted cite-only as Pass 27; OPEN #14, #15 | maps |
| **4** | Can the environment *execute* — a Layer III checker that runs E7 mechanically on the D1 witness and on any candidate σ? | planned: a checker specification (not Core, not `src/`), then the checker as a Layer III page | maps → renderer |
| **5** | The promotion rule from githack to the business domain — what must be proven, in what form, before anything moves | planned: the rulings brief, prepared for the human | maps |
| **6** | Receipt; coverage as fractions; the reference queue | planned | maps |

Kit amendment (this sitting, kit station): [distribution-model.md](../kit/distribution-model.md) — the human's production constraint landed as an amendment with his words, source and date, and the three things it forces (a precache-everything service worker, R3 no longer optional, releases pinned by SHA on the domain too).

## 3. Rules this iteration adds

| Rule | Why |
|---|---|
| **Every actor claim cites a commit hash or a station-file line.** A behaviour without a locator is an opinion | pass 1 is evidence, not gossip |
| **Model is stated only where the tree states it.** An untrailed commit is *untrailed*; "consistent with Grok's signature" is an inference and is labelled so | C10 |
| **The kit amendment is the human's.** His words are quoted; the agent's reading of what they force is `[INFERENCE]` beneath them | C02 amendment rule |
| **One reference per reply.** The queue is ordered by what each unblocks | the human's instruction |

## 4. Pass 3 method (run — see the audit record)

Read `systems-manifest-ascii.md` **top-down as a diagram**: every box on the Spine, Contract map, Fence, G-III cards and Pass-26 additions becomes a row; each row gets (component · owner contract or layer · what it depends on · what depends on it · evidence state · the pass that introduced it · whether anything after that pass contradicts it). Then the two questions a systems audit asks and a log audit never does: **is every dependency edge drawn** (or is a component used by something that does not cite it), and **is any component drawn twice under two names** (the Φ / Φ-morphism / Φ-schedule split was exactly that, found by iteration 5). Output: a dependency table, a duplicate list, a missing-edge list, and a verdict in the companion's own vocabulary — extend Pass 26; do not redo Pass 25.

## 5. Accept the iteration when

Pass 1's census covers every commit (attributed or explicitly unattributed) and every station claim; the role table names a station, a role and an evidence locator per actor; the kit amendment quotes the human and changes no Core sentence; pass 2's inventory has no component without an owner and a gate; pass 3's audit has run; the receipt carries the reference queue with exactly one item marked *next*.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

A team that does not sign its work cannot be assigned work. Emission remains `[GAP]`.
