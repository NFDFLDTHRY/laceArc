# ROOT / TOUCH / FOLD Pass 4 — propagation docket

**Status:** DATED HANDOFF. **Not executed here.**
**Source state:** Pass 4 law reconciliation complete.
**Law commits:** pointer-emission current reconciliation d2df9fd5269c07d3dcba2e0a2ca250dd9ef2ea67 + historical-scope cleanup 3454891c97d1d058cceeae36146a8ca2bf8f6a84; systems-manifest reconciliation 9f899cc29fe10cf2151e96d6d26f42c6bbf15f0e + stale-language sweep fc2cc5b861d73f54054c08b85c0638b6b17b2004.
**Acceptance:** absent. No src/.

This docket names downstream surfaces that are now stale because the law/model no longer treat POINTER construction, arity, root-touch, or finite-sample grounding as one undifferentiated [GAP].

It does **not** authorize editing those surfaces in Pass 4.

---

## 1. Current settled spine to propagate

    arrival
      ↓
    append WORD
      ↓
    unseen?
      ├─ yes → this WORD becomes Root
      └─ no  → append Join(new occurrence, Root)
      ↓
    selected arbitrary relation/sample?
      ├─ no → continue
      └─ yes
           ↓
         binary Join over selected ordered earlier points
           ↓
         repeat Join for finite sample / n+1 grounding
           ↓
         one grounded point may represent a larger Lace portion
      ↓
    continue

Physical constructor:

\[
\mathsf{Join}(a,b)
=
(\mathsf{POINTER},a,b).
\]

Remaining current gap:

\[
\boxed{
\text{what upstream process selects an arbitrary non-root-touch ordered relation/sample?}
}
\]

N-D remains a read-only projection of recursive WORD/Join participation.

---

## 2. Global systems ASCII — required next incorporation

**Target:** docs/systems-manifest-ascii.md  
**Current authority:** below docs/systems-manifest.md.  
**Do not overwrite Passes 1–28.** Append the next linear pass.

Recommended next identity:

- **Pass 29**
- **Iteration-4 Pass 4** if the existing iteration numbering is continued.

### Pass-29 scope

Update the current-tip reading so it no longer says:

    G2 POINTER when / arity / adjacency = [GAP]
    Hands silent. This diagram does not fill it.

Current replacement must distinguish:

    LAND / ROOT / ROOT-TOUCH      settled
    binary JOIN constructor       settled
    finite-sample n+1 grounding   settled
    adjacency-as-trigger          refused
    arbitrary relation/sample
    selection                     [GAP]
    G1/tokenization               [GAP]

### Core flow to add

    Contract II raw word arrival
                |
                v
             WORD row
                |
         +------+------+
         |             |
       unseen        seen
         |             |
       ROOT      RootTouch Join
         |             |
         +------+------+
                |
          selected relation/sample?
                |
           binary Join ladder
                |
                v
            grounded point
                |
            Continue

### N-D relationship to add

    WORD / JOIN points
           ↓
    Join may name prior Join
           ↓
    points-of-points
           ↓
    relations among relations
           ↓
    read-only dot-to-dot N-D projection

### Must preserve

- one store;
- no dimension columns;
- no graph DB;
- no sample registry;
- no UI/projection write-back;
- no automatic semantic chooser;
- no claim of acceptance;
- no src/.

### Open-board delta

Global ASCII current Open board should:
- remove broad G2 arity/construction gap as current;
- add/narrow to **arbitrary relation/sample selection**;
- retain G1/tokenization;
- retain document/provenance boundary gaps;
- retain read/query/traversal gap;
- retain concurrency and other unrelated opens.

---

## 3. Graphic B ASCII — graphics-station handoff

**Target:** docs/graphics/graphic-b-ascii.md.

Known stale current reading:
- B8 says when a POINTER is appended remains [GAP].
- B12 operator box says no "emit POINTER."

Required future clarification must remain subordinate to Graphic B PNG:

- Graphic B itself still does not spell out array mechanics.
- Current law now maps the geometric touch to storage:
  - unseen → root;
  - seen → RootTouch Join;
  - touch remains a Join point;
  - later Join may include prior touch.
- Do not rewrite Graphic B as though the PNG literally printed Join.
- Add a law/model cross-reference rather than retroactively claiming new source text.

---

## 4. Graphic C ASCII — graphics-station handoff

**Target:** docs/graphics/graphic-c-ascii.md.

Known stale current readings:
- C7 "when a POINTER is appended [GAP] (G2)";
- global C spine says G2 when / arity / adjacency [GAP];
- C6-to-D seam still treats POINTER mechanics as absent.

Required future clarification:

- C7 relations-among-relations have a current storage correspondent:
  - binary Join whose operands may be POINTER points.
- H6 / law reconciliation identifies recursive Join points as machinery behind the dot-to-dot N-D projection.
- C11 remains absolute: projection is not storage.
- C6 family labels remain **views / labels [GAP]**; no family field is invented.
- arbitrary relation/sample selection remains open.

Do not change the PNG's authority.

---

## 5. Atomic primitives / reason-model maps

### docs/atomic-primitives-map.md

Current stale grouping:
- POINTER trigger, selection, arity, adjacency and interleaving all bundled under G2.
- Touch admission/writing broadly open.

Later maps reconciliation should split:

    closed:
      WORD occurrence landing
      unseen root role
      seen RootTouch
      binary Join
      backward same-line
      WORD/POINTER operands
      finite-sample repeated Join

    open:
      arbitrary relation/sample selector
      G1 identity
      route-family labeling/boundaries

### docs/reason-model-map.md

Check any wording that still treats pointer construction/arity as open.
Do not change external-reference mappings unless directly affected.

---

## 6. Environment E1–E10 — later reconciliation

**Target:** docs/plans/lace-context-iter6-pass-4-environment.md.

This file currently types a broad:

\[
\sigma:\mathcal L\times V\rightarrow Step^*
\]

and its E5 candidate inventory predates H1–H6.

Later work should not rewrite its dated evidence. Instead append/current-qualify:

- arrival WORD is fixed;
- root handling is fixed structurally;
- RootTouch is fixed;
- physical POINTER constructor is binary Join;
- finite-sample grounding construction is fixed;
- the remaining free object is **selection**, not the entire append sequence.

A possible new type to investigate:

\[
\mathrm{Select}:
(\text{current Lace},\text{current context})
\rightarrow
\text{ordered relation/sample}
\]

but **do not define this function without a source ruling**.

The old E5 "campaign Q0/Q5" refutation remains historical evidence and should not be deleted.

---

## 7. Whole-app reduction model — later reconciliation

**Target:** docs/plans/reduction-pass-2-execution-model.md.

Current STEP uses:

\[
s=\sigma(L,v).
\]

Later qualification should split:
- deterministic LAND/root/RootTouch mechanics;
- deterministic Join construction given selected operands/sample;
- remaining selection input.

Potential whole-app interface question:
- does selection arrive through an existing input surface?
- is it a future explicit operator?
- is it only a precondition of a relation assertion?

Pass 4 found:
- Contract-II (v,S) expansion is refused under current source;
- no general query/write operator exists;
- route boundaries do not currently select samples;
- relation assertion supplying ordered operands is admitted only as constructor precondition.

Therefore reduction must **not** silently place selection into hopper h.

---

## 8. Rust crate / branch contracts — later reconciliation

Known stale surfaces include:
- docs/plans/rust-nostd-crate-map.md;
- branch contracts that say arity/admission/target kind are all open;
- route/core cards excluding scheduling under the old broad sigma.

Required future split:

**Core constructor facts**
- WORD append;
- binary POINTER / Join;
- refs earlier same line;
- operand may WORD/POINTER;
- no second store.

**Still unowned**
- arbitrary relation/sample selection.

Do not create src/ merely because constructor law is now reconciled.
Implementation still waits on the project's explicit acceptance gate and later branch/task authorization.

---

## 9. README / open-gate wording — law/steward later check

Search current root/docs README and contributor guidance for claims equivalent to:

- "pointer emission is wholly unanswered";
- "arity open";
- "later WORD may not be required";
- "Q2 conflict blocks all Core";
- "existence of pointer-emission file is the gate."

Update only under their owning station and without fabricating human acceptance.

The correct present state is:

- reconciled draft law exists;
- binary Join/current mechanics are specified;
- arbitrary selector remains [GAP];
- human acceptance of pointer-emission.md is still absent;
- no src/.

---

## 10. Suggested global incorporation order

    1  global ASCII Pass 29
       current law → systems topology reading

    2  Graphic B/C ASCII qualification
       source-reading companions point to current law
       without rewriting PNG claims

    3  atomic/reason maps
       split constructor facts from selection gap

    4  environment
       shrink broad sigma hole to selection hole

    5  reduction execution model
       place deterministic mechanics vs unresolved selection

    6  Rust crate/branch contracts
       update proof obligations only
       no src until gate/branch authorization

This order prevents old lower-authority companions from continuing to project a larger gap than current law while preserving the authority chain.

---

## 11. Propagation acceptance tests

Each later pass should check:

- Does it keep arbitrary selection distinct from Join?
- Does it preserve raw Contract-II input without a hidden sample channel?
- Does it avoid a query/write API unless separately ruled?
- Does it keep N-D as projection only?
- Does it avoid sample/cardinality registry state?
- Does it preserve historical receipts?
- Does it avoid claiming pointer-emission acceptance?
- Does it leave src/ absent unless separately authorized?

## Steward test

Propagate the smaller gap, not the old bigger one. The machine knows how to append the relation once its two points are chosen. Nothing downstream should keep pretending the physical POINTER constructor is still unknown, and nothing should pretend the unresolved chooser has been solved.
