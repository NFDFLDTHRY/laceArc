# LAB SOURCE ARCHITECTURE — Pass 6 PLAN · cross-family synthesis / insufficiency proof

**Status:** **EXECUTING** 2026-09-21.  
**Campaign:** Lab Source Architecture. **Pass:** 6.  
**Baseline:** Pass 5 verdict `RESEARCH SUPPLIES RM-SHAPED MACHINERY BUT NO LEGAL LACE INPUT PATH` at `ba2325e04cdbaa0bad02d2bafb99ccb29e6318d9`.  
**Primary question:** can already-harvested behavioral, source, human-ruled, derived-sampling/search, and research machinery compose across RM-A/RM-B/RM-C without an editor-created correspondence?  
**Implementation:** no Core implementation, no `src/`, Cargo, build, acceptance, or new source harvest.

## 0. Socket signatures

### RM-A
Input: lawful sampling-derived structural evidence.  
Output: authority-backed retain decision / retained-relation description.

### RM-B
Input: selected ephemeral sampled structure.  
Output: addressable existing Lace operands or a lawfully grounded Lace point.

### RM-C
Input: candidate retained relation / operand set plus lawful role evidence.  
Output: ordered Lace operand roles.

## 1. Legal adapter stamps

Every synthesis edge is one of:
- `IDENTITY`;
- `DERIVED`;
- `FROZEN-PREMISE`;
- `SOURCE-DEFINED`;
- `EDITOR-ADAPTER`;
- `UNAVAILABLE`;
- `AUTHORITY-FORBIDDEN`.

Only the first four can appear in an admissible composition.

A chain's authority cannot become stronger by composition. A single editor adapter makes the composed rule an editor construction.

## 2. Surviving fragments

Behavioral:
- Aftermath / Loop: recirculation discovery, no retain predicate;
- HCC-A weights/pruning: retention-shaped vocabulary but missing derivation/predicate;
- Water pointability / F3 Pair: admission/constructor-shaped, no chooser.

Source/current law:
- persistence/re-entry after recording;
- binary Join + POINTER→POINTER;
- one-store/no-collapse;
- H5/H6 repeated-Join grounding of an already-selected ordered finite sample;
- H7–H12 sampling discovery;
- L-M21 reconstructible search/witness machinery.

Research:
- Kauffman capstan threshold;
- Rowlands nilpotency predicate;
- Xiao selectors;
- AgentScope evidence-backed candidate retention/selection;
- Petersen interpolation/non-identification warning.

## 3. Strongest-chain attacks

Attack at least:
1. sampling → Aftermath → B8 → Join;
2. A5 repetition → HCC-A weight/prune → Join;
3. sampling → Kauffman threshold → Join;
4. sampling → Rowlands nilpotency → retain;
5. sampling → Xiao selector → retain;
6. sampling → AgentScope Stage 2 → Join;
7. AgentScope Stage 3 selection;
8. Water F3 Pair → L-M19.

Record the **first illegal/missing edge** in each chain.

## 4. RM-B independent proof

Do not let RM-A failure hide materialization machinery.

Test the class:

```
selected contiguous sample cell S[n,t]
```

Current sampling defines:
[
S[n,t]=(r_{t-n+1},ldots,r_t).
]

Current SearchHandle/WORD-row discipline says a sample occurrence can be reconstructed by starting at a WORD row and walking the immutable Lace prefix while counting WORD rows and ignoring interleaved POINTER rows.

H3 guarantees every arrival lands as a WORD row. H5/H6 + L-M19 prove any finite ordered sample of already-existing Lace points can be grounded by repeated binary Join.

Therefore test the derived chain:

```
selected S[n,t]
  --DERIVED--> ordered constituent WORD rows
  --DERIVED--> finite ordered addressable sample
  --DERIVED / L-M19--> one later addressable Lace point
```

Test concrete D1 controls:
- S[2,1] PIE DESSERT → rows (0000,0001) → Join(0000,0001) = D1 0002;
- S[2,3] PIE WHOLE → rows (0003,0005) → Join(0003,0005) = D1 0006;
- unretained S[2,2] DESSERT PIE would be materially groundable as (0001,0003) **if RM-A selected it**, without claiming D1 should retain it.

This is a materialization-capability proof, not a retain-when rule.

Also attack limits:
- pattern class vs one occurrence;
- noncontiguous/cross-level sampled structures;
- topology non-uniqueness for larger samples.

## 5. RM-C independent proof

For the contiguous-sample grounding class, the sample itself is an ordered tuple. Test whether RM-C is therefore locally closed as:

```
source-position order of selected S[n,t]
      → ordered WORD-row list
      → L-M19 fold order
```

This must **not** be generalized to arbitrary semantic relations or D1 `ref_A/ref_B` roles.

Existing class-specific closure remains:
- RootTouch: (new occurrence, root).

Potential new class-specific closure:
- contiguous sampled occurrence grounding: source order of sample constituents.

Generic relation RM-C may remain open.

## 6. Composition matrix + missing-edge register

Publish:
```
producer | output | consumer | required input | adapter | legal? | RM face
```

For every failed chain, record only the first illegal edge, then deduplicate.

## 7. Insufficiency certificate

If no lawful RM-A composition survives, prove:
1. eligible families harvested;
2. strongest fragments typed;
3. strongest compositions attacked;
4. every failed RM-A chain stops at a named editor/unavailable/forbidden edge;
5. RM-B/RM-C partial closures are separated from RM-A;
6. platform semantics remain out of scope.

Only after that may a design-decision docket be written.

## 8. Design docket

If insufficiency is certified, create a non-ruling docket defining:
- what is fixed;
- what remains unspecified;
- exact future design input/output type;
- forbidden answers;
- reusable partial machinery;
- falsifiers.

Do not propose, rank, or take a design.

## 9. Falsifiers

| ID | Fires when |
|---|---|
| F1 | sources joined by English vocabulary instead of type-compatible edges |
| F2 | composed candidate authority is treated as stronger than its weakest edge |
| F3 | editor adapter hidden as synthesis |
| F4 | B8 persistence is used as the missing retain decision |
| F5 | A5 count equated to HCC-A weight without a source-defined edge |
| F6 | Kauffman no-slip / Rowlands nilpotency renamed retain |
| F7 | ReAG/ISR/neural evidence invented from Lace |
| F8 | AgentScope Stage 3 uses future outcome inside prefix retention |
| F9 | Xiao model probabilities/embeddings become Lace state |
| F10 | RM-A failure prevents independent RM-B test |
| F11 | universal RM-C failure hides a relation-class-specific closure |
| F12 | cross-family composition tunes against or changes D1 |
| F13 | finite D1 fit is treated as mechanism identification |
| F14 | human design declared necessary before missing adapters are deduplicated |
| F15 | design docket proposes/ranks/takes a ruling |
| F16 | platform machinery bridges semantic gap |

## 10. Frozen surfaces

No edits by default to:
- systems-manifest;
- global systems ASCII;
- pointer-emission;
- AGENTS/CLAUDE;
- graphics;
- clipboards;
- D1 fixture;
- executed Passes 1–5.

If this pass proves a canonical open is genuinely narrower than current wording, record the proof first. Reconciliation is a separate authority action unless explicitly required to keep the canonical contract truthful.

## 11. Verdicts

- **CROSS-FAMILY SYNTHESIS SUPPLIES ADMISSIBLE RM MACHINERY**
- **CROSS-FAMILY SYNTHESIS NARROWS RM-B / RM-C BUT RM-A REMAINS OPEN**
- **CROSS-FAMILY SYNTHESIS EXPOSES A SINGLE IRREDUCIBLE ADAPTER**
- **EVIDENCE INSUFFICIENCY CERTIFIED — NEW DESIGN/RULING REQUIRED**

No verdict itself takes a ruling.
