# Systems × equations map — Pass 6 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `68f12ef`).
**Depends on:** passes 1–5 EXECUTED.
**Emission:** `[GAP]`. No `src/`. Does not accept `pointer-emission.md`.

Pass 5 walked D1. Pass 6 judges whether the **map** is sound, then **STOP**.

This is not a judgment that Φ has been calculated.

---

## 0. One question

Does the six-pass map still say only what Hands, the wall, and U12 allow — and does it stay silent where the calculation is still a hole?

## 1. One result (on execute)

[`systems-eq-map-pass-6-soundness.md`](systems-eq-map-pass-6-soundness.md) — soundness board + STOP. Campaign **CLOSED 6/6**.

---

## 2. Soundness board (execute marks each)

| Cell | Must hold | Fail if |
|---|---|---|
| S1 I-EQ | Arrive / \(L\) / Star / \(\pi\) / morphism *shape* match Graphic D | second store, Arrive=Φ |
| S2 WallCalc | \(\mathrm{WallCalc}\cap\{\mathsf{WORD},\mathsf{POINTER},\Phi_{\mathrm{schedule}}\}=\emptyset\) | Shadow row on \(L\) |
| S3 U12Calc | same intersection empty | rustc writes a section; wasm64 opens Implementation Law |
| S4 Holes | H-PTR / H-k / G1 / required PTR→PTR / Carrier-Hopper I-letters empty or named-unmapped | any hole filled |
| S5 D1 | 0005–0007 CONFIRM; 6/6 Witness-only | D1 used as emitter; other groups invented |
| S6 #14 | recorded CONFLICT kept | smoothed |
| S7 Ruling | wall + U12 **in** the calculation | either forbidden from the map again |
| S8 Acceptance | draft remains draft | this file as acceptance |

A FAIL is a finding. Execute does not soothe it by writing Φ.

Allowed surgical: one sentence on [`systems-eq-map-pass-1.md`](systems-eq-map-pass-1.md) header *Campaign CLOSED 6/6* if the board is all PASS. No row edits on passes 1–5 products otherwise.

## 3. Forbidden this pass

- Fill G2 / H-PTR / H-k
- Accept emission
- Open `src/`
- Start a seventh pass of *this* campaign

## 4. Accept pass 6 when

- S1–S8 marked.
- STOP sentence published; campaign closed 6/6.
- U6-from-math-update (SPOKEN ≠ calculated) still held.
- Plan `PLAN` → `EXECUTED`. maps released.

## Steward test

Shoe in hands. A sound map still has holes. **Closing the iteration is not closing Φ.**
