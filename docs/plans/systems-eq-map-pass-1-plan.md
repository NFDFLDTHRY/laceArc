# Systems × equations map — Pass 1 of 6 PLAN

**Status:** **`EXECUTED`** 2026-09-20 · [`systems-eq-map-pass-1.md`](systems-eq-map-pass-1.md). **Station:** maps (`Grok-maps`).
**Iteration:** new, after math-execution UPDATE **CLOSED 6/6**.
**Emission:** `[GAP]`. No `src/`. Does not accept `pointer-emission.md`.
**Human ruling this iteration sits on:** U12 (rustc / Cargo shelf 10) and the Hands/Shadow wall **are part of the calculations**. They cannot be forbidden from the map. They still are **not** rows of \(L\).

---

## 0. One question

What does every named box and edge on the **global systems ASCII spine** correspond to in the **equation set**, once wall-objects and U12-objects are allowed *in the calculation* without being allowed *on the strand*?

## 1. One result (on execute)

[`systems-eq-map-pass-1.md`](systems-eq-map-pass-1.md) — a row-per-surface map.

No new equation. No filled G2. No rustc-as-Arrive. No Shadow-as-WORD.

---

## 2. Sources (read-only)

| Corpus | Path | Role |
|---|---|---|
| Global ASCII | [`../systems-manifest-ascii.md`](../systems-manifest-ascii.md) | companion spine, passes 1–28, DIAGRAM SOUND WITH OPENS |
| Hands contracts | [`../systems-manifest.md`](../systems-manifest.md) | outranks the ASCII |
| Equations (execution) | [`../math-execution-model.md`](../math-execution-model.md) | Arrive, Star view, \(\pi\), \(\Phi_{\mathrm{morphism}}\), \(\Phi_{\mathrm{schedule}}\) stamp |
| Equations (dual-universe) | [`../clock/system-mathematical-model.md`](../clock/system-mathematical-model.md) Hands §2 + wall \(W : \mathcal{S} \not\hookrightarrow \mathcal{H}\) |
| Interface holes | [`math-execution-update-pass-3-inputs.md`](math-execution-update-pass-3-inputs.md) | H-WORD / H-PTR / H-k / H-left — to be *reclassified*, not filled |
| U12 | [`../clipboards/rust-target-clipboard.md`](../clipboards/rust-target-clipboard.md) P6-R + Cargo A4 extract | toolchain side of the calculation |
| D1 refresh | [`math-execution-update-pass-5-witness.md`](math-execution-update-pass-5-witness.md) | specimen only |

Graphics A–D remain Hands. ASCII does not outrank them.

## 3. ASCII surfaces execute **must** walk

Not all 2883 lines of pass-chat. Every **named mechanism figure** and every **admitted extra box**:

1. Spine (Contract II → I rectangle → Layer III)
2. One-tick mechanism (pass 5) + P27-F caption (arrival ≠ tick)
3. Participation mechanism (pass 5)
4. Star mechanism (pass 5)
5. Dual presentation, one mechanism (pass 5)
6. Not-a-Core-mechanism fence (pass 5)
7. Core as clock + slip + two clocks (pass 6)
8. Layer III Shadow SOURCE MACHINERY board (pass 7) — **in the map as WALL-CALC / III-READ**, not as FORBID-from-calculation
9. Open board items that name G2 / Φ
10. Pass 27 six admitted boxes: Carrier · Hopper · Seed corpus · Frontier · refuse edge · Layer III Runtime
11. Pass 28 Σ = (L, h, K) and the six transitions SUBMIT · STEP · REFUSE · PERSIST · PROJECT · OPEN

Quiet-door commentary, kit-door notes, and Order Proposals get one row each as COMPANION unless they name a mechanism edge.

## 4. Equation surfaces execute **must** walk

- \(L\), Kind, \(\mathsf{Arrive}(v)\), \(\mathrm{Star}(v)\), \(\pi\), \(\Phi_{\mathrm{morphism}}\), \(\Phi_{\mathrm{schedule}}\) stamp
- \(\mathsf{Step}_{\mathrm{partial}}\)
- Dual-universe \(\mathsf{Participate}=[GAP]\), \(W : \mathcal{S} \not\hookrightarrow \mathcal{H}\)
- U12: target `wasm64-unknown-unknown`, `build-std` / `core`, 64-bit memories, no-unwind — as **calculation objects**, not as Core morphisms

## 5. Row marks

| Mark | Means |
|---|---|
| **I-EQ** | this ASCII edge *is* an equation on \(L\) |
| **WALL-CALC** | wall / Shadow object; **in the calculation**; not a row of \(L\) |
| **U12-CALC** | toolchain object; **in the calculation**; not Arrive / Φ |
| **III-READ** | projection / cite; read-only toward I |
| **COMPANION** | ASCII or Layer III hygiene; no equation yet |
| **HOLE** | G2 / H-PTR / H-k / leftover / G1 / required PTR→PTR |
| **UNMAPPED** | named on one side, silent on the other |
| **CONFLICT** | ASCII and an equation disagree; both kept |

Human ruling: **WALL-CALC** and **U12-CALC** are legal marks. A row that would have been “MUST-NOT take” under closed UPDATE pass 3 is now one of those two, or CONFLICT if it was also being smuggled onto \(L\).

## 6. Forbidden this pass

- Fill G2 / H-PTR / H-k
- Put rustc receipt or Shadow state into \(L\)
- Redraw the ASCII spine
- Rewrite the sealed Arrive equation
- Accept emission
- Open `src/`
- Start pass 2

## 7. Later passes (named only)

| Pass | Job |
|---|---|
| 2 | Enumerate WALL-CALC objects and what they may compute *without* writing \(L\) |
| 3 | Enumerate U12-CALC objects and what they constrain *without* becoming Arrive |
| 4 | Close UNMAPPED / CONFLICT rows, or leave them named |
| 5 | Walk D1 + ASCII one-tick against the equations (arrival ≠ tick) |
| 6 | Soundness of the *map* (not of Φ) + STOP |

## 8. Accept pass 1 when

- Every surface in §3 has ≥1 row.
- Every equation in §4 is cited from ≥1 row.
- At least one WALL-CALC row and one U12-CALC row exist (the ruling is visible).
- Zero HOLE rows filled.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 2 is immediately authorized.

## Steward test

Shoe in hands. The ASCII is a reading of the contracts. The equations are a reading of Graphic D. The wall and the compiler are in the *calculation*, not on the *tape*. **Mapping is not emitting.**
