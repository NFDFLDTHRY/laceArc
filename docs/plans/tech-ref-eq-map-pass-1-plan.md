# Technician references × mathematical model — Pass 1 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `22e0cf9`).
**Emission:** `[GAP]`. No `src/`. Does not accept `pointer-emission.md`.
**Does not import adjacent axioms into Core.**

**Technician references** = the five staked adjacent texts in [`../references.md`](../references.md), already felt on the shoe in [`../reason-model-map.md`](../reason-model-map.md) and clipped in [`../clipboards/clipboard-five-stakes.md`](../clipboards/clipboard-five-stakes.md).

This campaign does **not** redo KEEP/FORBID as reason-model pieces. It maps those texts onto the **equations and calculation sides** now closed: Arrive, Star-as-view, \(\pi\), \(\Phi_{\mathrm{morphism}}\) shape, H-PTR hole, WallCalc, U12Calc.

PDFs stay off `main`. Execute cites in-repo clipboards + reason-map + references catalog. Do not paste book bodies.

---

## 0. One question

For each of the five technician texts, which mathematical-model objects does it **kin**, **limit**, **conflict-as-write**, or **miss** — and does any of them close H-PTR?

## 1. One result (on execute)

[`tech-ref-eq-map-pass-1.md`](tech-ref-eq-map-pass-1.md) — five cards × model objects.

No new morphism. No imported axiom as `Step`. Clipboards and `pointer-emission.md` **unread-write**.

---

## 2. The five

| # | Text | In-repo readings (cite, don’t rewrite) |
|---|---|---|
| T1 | Kauffman *Knots and Physics* 4ed | reason-map · kauffman-4ed-clipboard · five-stakes p1 |
| T2 | Rowlands *Zero to Infinity* | reason-map · rowlands clipboard · five-stakes p2 |
| T3 | Xiao & Zhu *Foundations of LLMs* | reason-map · xiao-zhu clipboard · five-stakes p3 |
| T4 | Petersen & Zech *Mathematical theory of DL* | reason-map · petersen-zech clipboard · five-stakes p4 |
| T5 | Bi et al. AgentScope | reason-map · agentscope clipboard · five-stakes p5 |

Rustc / Cargo books are **U12**, already carded. Not this corpus.

## 3. Model objects each card must mark

Arrive · Star view · \(\Phi_{\mathrm{morphism}}\) shape · H-PTR / schedule hole · leftover adjacency · G1 · \(\pi\) · WallCalc · U12Calc · \(W\)

Marks:

| Mark | Means |
|---|---|
| **KIN-EQ** | the equation already is that Hands fact; the text names a kin |
| **ADJ-CALC** | may enter the *calculation* as kin-read / limit (same discipline as WallCalc) |
| **CONFLICT-WRITE** | importing it as a write would put a row on \(L\) or fill H-PTR from habit |
| **OFF-SHOE** | no contact with cord / arrival / index |
| **HOLE** | text is silent on this object; does not close Φ |

A row that would have said “adjacent so forbid from calculation” is wrong after the wall/U12 ruling. **ADJ-CALC** is legal. **CONFLICT-WRITE** is still illegal.

## 4. Forbidden this pass

- Fill H-PTR from clove hitch, NTK, next-token, ReAG, or nilpotent rewrite
- Treat Kauffman hitch as Φ-schedule (old map: hitch is kin of participation, not the when)
- Rewrite reason-model-map or five-stakes
- Commit PDFs
- Open `src/`
- Start pass 2 (per-book deepen) unless immediately authorized

## 5. Later passes (named only)

| Pass | Job |
|---|---|
| 2 | T1 Kauffman × equations (hitch, Reidemeister vs Rule Zero) |
| 3 | T2 Rowlands |
| 4 | T3 Xiao–Zhu (tokenizer / intern / next-token) |
| 5 | T4 Petersen–Zech (NTK ≠ star) |
| 6 | T5 AgentScope + STOP (ReAG ≠ POINTER) |

## 6. Accept pass 1 when

- Five cards exist; every model object in §3 is marked on each card or explicitly bundled.
- Zero H-PTR fills.
- Existing KEEP/FORBID morals cited, not silently flipped.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 2 is immediately authorized.

## Steward test

Shoe in hands. A technician book may sit next to the lace. **Kin is not a stitch. A hitch is not a when.**
