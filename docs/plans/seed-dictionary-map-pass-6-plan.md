# Seed dictionary × model — Pass 6 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `bb5de62`).
**Depends on:** passes 1–5 EXECUTED.
**Emission:** `[GAP]`. No `src/`. Does not accept `pointer-emission.md`.

Pass 5 left #15 open. Pass 6 judges whether the **seed map** is sound, then **STOP**. Campaign closes 6/6. Seed prefix is not a solved Φ.

---

## 0. One question

Does the six-pass seed map still say: dictionary enters as Arrive, definition route is first-touch *shape*, hopper is empty of rows, when is free?

## 1. One result (on execute)

[`seed-dictionary-map-pass-6-soundness.md`](seed-dictionary-map-pass-6-soundness.md) — board + STOP.

---

## 2. Board execute must mark

| Cell | Must hold |
|---|---|
| S1 | Seed life = L1 given a stream |
| S2 | WORD-only prefix is legal unused lace |
| S3 | Wiring shape = headword → defining-word stars |
| S4 | First touch = L4 shape, not forced |
| S5 | No gloss / no Star table / no corpus on main |
| S6 | #15 HOLE |
| S7 | \(\theta_{\mathrm{when}}\) free; spoken wired-first ≠ schedule |
| S8 | this file ≠ acceptance |

## 3. Forbidden this pass

- Load a dictionary
- Pick \(\theta_{\mathrm{when}}\)
- Accept emission
- Open `src/`
- Pass 7 of *this* series

## 4. Accept pass 6 when

- S1–S8 marked.
- STOP published; campaign CLOSED 6/6.
- Plan `PLAN` → `EXECUTED`. maps released.

## Steward test

Shoe in hands. First eyelets, first path, first possible wraps. **Possible is not written.**
