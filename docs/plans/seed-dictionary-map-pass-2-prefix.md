# Seed dictionary × model — Pass 2 · L1-only prefix

**Status:** **EXECUTED** 2026-09-20. **Station:** maps (`Grok-maps`).
**Ticket:** [`seed-dictionary-map-pass-2-plan.md`](seed-dictionary-map-pass-2-plan.md).
**Parent:** [`seed-dictionary-map-pass-1.md`](seed-dictionary-map-pass-1.md).
**Emission:** `[GAP]`. No seed POINTER invented. No corpus loaded. No `src/`.

Unused is not un-lace. Unused is not grabbed.

---

Let the dictionary stream be \(v_1,\ldots,v_n\) (including repeats). L1-only seed:

\[
L = (\mathsf{WORD},v_1){}^\smallfrown\cdots{}^\smallfrown(\mathsf{WORD},v_n).
\]

| Fact | Status |
|---|---|
| \(L\) is WORD-only | **forced-legal** |
| Indices \(0..n-1\) exist | later L4 *could* name them |
| \(\mathrm{Star}(v)\) = WORD-pass view | **readable** |
| Definition route as POINTER chain | **absent** |
| Spoken “wired” | **not this state** |
| Repeat spelling | new WORD (Hands); Q0 CONFLICT **named**, not resolved |
| Attach-point | **possible**, not written |

This prefix is a lace. It is not a wired dictionary in the spoken-Q1 sense.

## `P2-R`

| | |
|---|---|
| POINTER rows added | **0** |
| Next | pass 3 spoken pay-in vs `0003`; **not opened** |

**Emission `[GAP]`. Shoe in hands.**
