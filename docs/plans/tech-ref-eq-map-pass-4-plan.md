# Technician references × mathematical model — Pass 4 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `df8b5cf`).
**Depends on:** pass 1 T3 · passes 2–3 EXECUTED.
**Text:** Xiao & Zhu *Foundations of Large Language Models*.
**Cite-only:** reason-map §3 X1–X7 · xiao-zhu clipboard · five-stakes page 3.
**Emission:** `[GAP]`. No `src/`. PDF off git. Clipboards unread-write.

Pass 1: next-token is ADJ-CALC projection / CONFLICT-WRITE as Piece 2; intern CONFLICT-WRITE; G1 HOLE with no-mask limit. Pass 4 stamps X1–X7 onto the equations. It does not close G1 or H-PTR.

---

## 0. One question

Which Xiao–Zhu loci kin arrival-order, and which would replace distinct WORD occurrences with a vocabulary object, a mask, an embedding store, or \(\arg\max\mathrm{Pr}(y\mid x)\)?

## 1. One result (on execute)

[`tech-ref-eq-map-pass-4-xiao.md`](tech-ref-eq-map-pass-4-xiao.md) — X1–X7 stamped.

No tokenizer as Arrive. No next-token as schedule.

---

## 2. Loci execute must mark

| Locus | Expected (test) |
|---|---|
| X1 token sequence / arrival order | ADJ-CALC / weak KIN-EQ of order only; not a tokenizer |
| X2 shared vocab vs indexed occurrences | CONFLICT-WRITE if collapse passages; sequence-with-IDs does not itself intern |
| X3 next-token loss | ADJ-CALC \(\pi\) / CONFLICT-WRITE as Piece 2 output |
| X4 MASK / BERT delete-replace | CONFLICT-WRITE vs G1 / Piece 4 |
| X5 embeddings | CONFLICT-WRITE as second store |
| X6 prompts / RLHF | ADJ-CALC WallCalc steward; CONFLICT-WRITE as Core rows |
| X7 inference / decode | OFF-SHOE as Arrive; ADJ-CALC as holder talk |

H-PTR and G1 stay HOLE. X4 constrains G1; it does not fill it.

## 3. Forbidden this pass

- Fill H-PTR from next-token or prefilling
- Intern PIE
- Start Petersen–Zech
- Edit xiao clipboard or reason-map
- Open `src/`

## 4. Accept pass 4 when

- X1–X7 stamped + equation cite.
- T3 morals not flipped.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 5 is immediately authorized.

## Steward test

Shoe in hands. Two passes of “the” are two wraps. **A likelihood is not a section.**
