# Mathematical execution model — UPDATE campaign · Pass 6 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `8d1a42d`).
**Emission:** `[GAP]`. No `src/`. Not an emitter. Does not accept `pointer-emission.md`.
**Depends on:** passes 1–5 EXECUTED.
**Charge (human):** ensure the model is **accurate** — accurate to Hands and to this campaign, not complete, not accepted.

Pass 5 showed D1 still answers none of Q0–Q5. Pass 6 is readiness + an accuracy board + **STOP**.

---

## 0. One question

After five update passes, what in the sealed partial model is **true to the strand**, what is **labeled but uncomputable**, and what would make the model *inaccurate* if we pretended it were finished?

## 1. One result (on execute)

[`math-execution-update-pass-6-readiness.md`](math-execution-update-pass-6-readiness.md) — accuracy board · calculable / uncomputable · STOP.

The model stays PARTIAL. Ready-partial ∧ blocked-complete is confirmed or amended with cites. This campaign then **closes 6/6**.

---

## 2. Accuracy board (execute must mark each)

Accuracy here means: the model does not say more than Graphics A–D + the emission file’s *stamps* + the interface holes. It does not mean the holes are filled.

| Cell | Must match | Fail if |
|---|---|---|
| A1 Store | Graphic D / S1.2 one line | second store, Shadow-in-\(L\) |
| A2 Arrive | S2 WORD append only | Arrive merged into Φ |
| A3 Star | S3.1 view | Star table |
| A4 Wall | \(W : \mathcal{S} \not\hookrightarrow \mathcal{H}\) | H/C/W as Core |
| A5 Φ-morphism | S4.2 shape, \(k\ge 1\) | \(k=2\) as law |
| A6 Φ-schedule | SPOKEN ≠ calculated; H-PTR empty | spoken when installed as `Step` |
| A7 Q0 | SPOKEN + HANDS CONFLICT on `0003` | conflict silenced |
| A8 Q2 | CONFLICT | arity picked from D1 |
| A9 Q3 | CLOSED no-emit; leftover not a store | adjacency fires Φ |
| A10 D1 | pass 5: 6/6 POINTER Witness-only | D1 used as emitter |
| A11 G1 | still OPEN | tokenizer invented |
| A12 Q4 required PTR→PTR | still OPEN | 0007 treated as required |
| A13 rustc shelf | U12 out | toolchain in `Arrive` |
| A14 Acceptance | draft remains draft | this pass as acceptance |

A fail is a finding. Execute does **not** soothe it by writing Φ.

## 3. Calculable now / still uncomputable

**Calculable** (already defined, may be used as input later):

- \(L\), kinds, \(\mathsf{Arrive}(v)\), \(\mathrm{Star}(v)\) as view, backward-shape check, prefix freeze

**Uncomputable** (holes stay empty; saying otherwise is inaccurate):

- H-WORD later-arrival (CONFLICT with D1 `0003`)
- H-PTR / \(\Phi_{\mathrm{schedule}}\)
- H-k
- leftover adjacency as a stored relation
- G1 identity
- required POINTER→POINTER on every later participation

Pass 6 may **name** these. It may not close them.

## 4. Allowed surgical edits

- Header / S8: campaign stamp “UPDATE 6/6 readiness”; add pin `8d1a42d` (pass 5 execute) next to the existing pins.
- If the accuracy board finds a **false sentence** in S0–S8 (says OPEN where pass 2 already retied, or says calculated where U6 forbids), retie that sentence only. No new morphism.

Old campaign products, emission file, dual-universe §3: **untouched**.

## 5. Forbidden this pass

- Fill any H-hole
- Accept `pointer-emission.md`
- Open `src/`
- Claim a total `Step`
- Start a seventh pass of *this* campaign
- Import rustc or Shadow to “finish” accuracy

## 6. Accept pass 6 when

- Readiness file exists; A1–A14 marked PASS / FAIL / N/A with cites.
- Zero FAIL left untreated: either a surgical retie (allowed) or a named leftover that is *honest silence*, not a false OPEN/calculated mix.
- Calculable / uncomputable lists published.
- STOP: this UPDATE campaign is closed 6/6. Next work is a **new** ticket if you want one.
- U6 held. Emission `[GAP]`.
- Plan `PLAN` → `EXECUTED`. maps released.

## Steward test

Shoe in hands. An accurate partial model still has holes. **Filling a hole to look finished is the inaccuracy.**
