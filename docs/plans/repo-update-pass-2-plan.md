# Repository update — pass 2 plan

**Status:** EXECUTED for the authorized local work. Source/document corrections and synthetic repairs are independently reviewed. Frozen-renderer and browser/device completion remain pending; see the execution receipt's separate evidence fields.
**Instructions:** “Plan pass 2,” then “Proceed with pass 2.” The latter authorizes the bounded local execution below, including its specified executable repairs and fixtures. **Station / editor:** maps / Codex, session `95fe3444c11b`. **Date:** 2026-09-20.
**Base:** `10fe4947c4450453e7aa1c256cdea294d6981e48`, clean and equal to origin/main after fetch and ff-only pull.
**One outcome:** reconcile the remaining current document and executable consumers with their sources and measured behavior, while preserving unresolved decisions.
**Read with:** [pass 2 evidence and probes](repo-update-pass-2-evidence.md), [pass 1 dispositions](repo-update-pass-1-evidence.md#8-execution-dispositions), [current context](../prompts/context-pass.md), [task/adjudication form](../kit/agent-task-template.md).

## 1. What this pass inherits

Pass 1 is published: eight station commits, 47 changed/new paths, ending at this base. Its receipt was finalized before the later “Push all”; statements there about an uncommitted handoff describe that earlier stage. They do not mean publication is still pending. The original 558-file inventory remains a base snapshot. Main now has **561 tracked files, 486 Markdown**. No branch integration or Core acceptance occurred.

Pass 1's I1/M1/I6/M3 qualifications and explicit unassigned σ implementation edge survived independent rereading. Do not repeat that work or rewrite dated receipts to make them look contemporary. This pass follows actual remaining consumers: omitted live model documents, reference-shelf copies, agent hooks, viewer text and behavior, update selection, coordination dispatch, and a few missing shelf routes.

**Question:** can those consumers state and demonstrate only what their sources and tests support, without selecting an emission rule, architectural ruling, installation policy or release on the human's behalf?

**Thesis `[PROPOSAL]`:** the specified source/consumer defects can be repaired through bounded documentation and existing Layer III maintenance. Source correction, structural checks, successful submission, rendered pixels, installation, and acceptance will remain separate claims.

**Falsifiers:** a proposed fix needs a new Core operation or human ruling; a source correction changes the source rather than its interpretation; a synthetic check passes while the stated behavior remains unobserved; a supposedly missing navigation route already exists; or a repair can modify another installation's cache, claim, or release. Narrow or stop the affected batch when one occurs; independent authorized batches continue.

## 2. Evidence established before scoping

The [evidence record](repo-update-pass-2-evidence.md) supplies exact paths, named functions/headings, current discrepancies and review limits. In particular:

- Arrival R3 reverses manifest Pieces 9 and 10. Untouched adversarial/crosswalk consumers still rank derivations as authority or reopen Q4's closed permission.
- The compute/shape proposals contain additional unqualified concurrency, host-independence, submit-boundary and encoding claims. These are separate from the proof scopes repaired in pass 1.
- A new visual sighting of Kauffman printed p.324 / PDF p.343 confirms the printed normalization `κ = exp(θ/(2π))`, with `θ` the total angle in radians and `exp(n)` for `n` revolutions. Several current derivatives still print `exp(θ)` or `exp(2πn)`. Pass 1's inequality correction did not verify this next-page exponent.
- A scratch Node execution of the actual geometry sources found **22/22 worker filament triangles with zero area**, and **no corresponding filaments in the inline fallback**. This is synthetic geometry evidence, not a GPU image.
- The release selector can fall back from failed release-pointer providers to `commits/main`, then report “fresh” after failed comparison. A full SHA alone does not prove that revision was promoted.
- The checker passes with **90 advisories: 7 direct-index warnings and 83 unsigned commits in its moving 200-commit window**. Five shelf entrances need grouped routes; two already reach all their files through existing groups. Zero orphans is partly an inventory-reference artifact, not navigation proof.

All four canonical PNGs were visually inspected earlier in this session and their bytes are unchanged. This planning step does not claim a fresh four-image inspection or any physical cord operation. The source/law hierarchy and accepted HCC-A/Coffee Cup/Water conceptual-reference classification remain unchanged.

## 3. Scope and authorization

**Original planning authorization record, before Proceed:** the next three paragraphs state the limits presented for review at the planning handoff. The execution update after them records the subsequent instruction.

**This planning turn writes only:** this plan, `repo-update-pass-2-evidence.md`, and `docs/plans/README.md`, under maps. No runtime repair, fixture installation, commit or push is part of this turn.

**Proposed scope for a later Proceed:** the exact documentation corrections and existing Layer III/steward maintenance enumerated below, with isolated synthetic checks. The listed new test files are first-party regression fixtures for those repairs, not a new application, Core implementation, dependency framework or CI service. No external package installation is assumed. One station per editor at a time and one station per commit; dependent summaries follow their source correction.

**Bounded executable scope to put to the human:** shell changes in B/C, HTML/JavaScript/WGSL changes in D/E, and the three new shell/Node/test-document paths in §5. The later execution instruction must cover this scope, for example by directing execution of this plan in full. Planning, a station claim or continuation of document review alone does not authorize executable edits. This distinction is required by AGENTS.md's standing language restriction and code-commit rule; it does not imply Core or emission acceptance.

**Execution update:** the human subsequently said “Proceed with pass 2.” That instruction has now exercised the bounded local scope above, as recorded in [the execution receipt](repo-update-pass-2-evidence.md#8-execution-dispositions). The distinct publication, renderer-designation and acceptance boundaries below remain.

**Distinct remaining boundaries:**

- `docs/shadow-clock-gearing.html` is explicitly frozen in `docs/gearing/CLAIMS.md`. Its repair requires a human-named designated editor session; a renderer claim alone does not grant that designation. Plan its exact changes now; execute other free batches if it remains unavailable.
- Document execution, the bounded executable scope above, code commit/publication and frozen-renderer designation are distinct. An instruction covering local execution is not itself a publication instruction. Prior publication of pass 1 is not publication of pass 2.
- No `LATEST.json`, manifest identity/scope/start URL, hosting policy, deployment, actual installation, Core/type/schema/encoding implementation, branch-birth change, or reserved ruling is selected by this plan.
- `AGENTS.md`, live law, staking, the manifest, `pointer-emission.md`, canonical images, attached sources and historical evidence captures stay read-only. A new human statement is recorded with its locator and exact scope; it is not silently converted into an accepted contract.

## 4. Work batches and completion tests

### A. Finish source and proposal qualifications

**Findings:** P2-01–P2-05. **Owners:** maps, then the specifically affected clipboards.

1. Correct the Piece 9/10 reversal against the manifest. Carry current source-role and Q4 distinctions into the omitted current catalogs/crosswalks and the pending construction-plan entrance. Preserve the early problem-map table and completed campaign records; qualify their current entrance rather than rewriting their past observations.
2. Distinguish proposed one-writer/concurrency premises from law; empty imports from a proof of σ's permitted inputs; one-submit behavior from the unresolved word-boundary question; and proposed framing from R6. Remove no obligation and choose no replacement protocol, ABI or encoding. Carry the existing hosting/offline qualification into compute G5 and its production label.
   Execution review also found overclaims in the same current compute table's G1/G4/G7. Their bounded source carries distinguish dependency records from complete build provenance, final D1 equality from full E7/intermediate-frontier proof, and ancestry from accepted-content/source/artifact correspondence. This refinement adds no file, gate or ruling.
3. Reconcile Kauffman's current miniature, algorithm, diagram and reason-map formula with the newly sighted page. State angle/turn conventions explicitly and preserve the inclusive inequality. Attribute the printed model exactly; do not silently replace it with a preferred general physical formula.
4. Carry the indexed-occurrence distinction into the Xiao and five-stakes current readings. Sharing vocabulary identity does not by itself erase distinct sequence positions. Retain restrictions against substituting a vocabulary object, count or embedding store for Lace history. Update stale “map fix OPEN” routing only where the referenced fix actually landed.

**Completion:** every changed claim has a source locator and every active consumer in this batch either agrees or visibly records the remaining conflict. A second reader checks the exact corrected rows. Substituting `θ = 2πn` in the source-attributed expression yields `exp(n)`; no parameter becomes a Core field. No Q4 permission is presented as an emission schedule or acceptance.

### B. Restore reader routes and truthful hook messages

**Findings:** P2-06–P2-07. **Owners:** graphics, history, hologram, gearing-meta; kit for hooks.

Add one historical Pass 7 link in the graphics entrance and grouped ASCII/receipt links for closed quiet-door campaigns #5–#8 in their respective entrances. Existing companion headers already link the six plans in each group. Keep statuses dated. Do not copy 110 direct links merely to silence the checker or require every asset to be a prose entry.

Scope hook absence messages to Lace Core and retain the human-acceptance gate. Change wording/comments only: preserve hook registration, command predicates, exit behavior and advisory semantics. Do not piggyback changes to the orphan, direct-index or unsigned-commit algorithms.

**Completion:** the previously missing reader routes resolve through intended grouped links; unchanged source/history bodies remain exact. In an isolated hook fixture, local/non-remote invocation skips the checker; remote invocation calls it once; checker success/failure remains reported while session startup still exits 0. Compare checker category counts on the same fixture revision before/after the wording edit. A direct-index warning may remain after navigation is repaired.

### C. Repair coordination override dispatch and reason custody

**Finding:** P2-08 (U21). **Owners:** coord, gearing-meta, then prompts for the verified behavior description.

Repair the entire umbrella-to-backend path, including forwarding and retaining the required reason. Merely allowing `force-free` in the dispatch switch is insufficient. Preserve the backend's direct human-only override contract, including existing no-reason invocation behavior; an optional supplied reason may be retained without inventing expiry, identity enforcement or automatic override permission. Align the help and derivative instructions with observed behavior only after verification.

**Completion:** a disposable directory tree contains copied scripts, synthetic HELD doc/gear claims and untouched sentinel claims. Reproduce the original rejected umbrella path before repair. Afterward, verify exact reason retention, expected target clearing and untouched sentinels; missing reason on the umbrella, unknown station/shaft and wrong-holder ordinary release reject without writes. Explicitly cover both HELD and already-FREE fixtures and the preserved direct backend call. Never run an override against this repository's real claims or use a live hold as the fixture.

Already-FREE outcomes stay entrypoint-specific: doc `force-free` rejects without writes; direct gear `force-free` without a reason succeeds and clears fields/NOTE as before. The proposed umbrella gear path delegates that gear behavior and retains its required supplied reason, including on an already-FREE fixture. Do not silently unify the doc and gear protocols.

### D. Make conceptual-reference qualifications visible

**Finding:** P2-09 (U19/U25). **Owners:** renderer; gear:gears for its binding text; hologram for the Lace viewer's link hit testing.

Give the HCC viewer a visible qualification that survives its Markdown parser and narrow-screen layout. Correct the nostd viewer's current holder-machine wording. For the frozen gearing viewer, prepare a persistent qualification visible with each selected contract and repair its two moved documentation links, but wait for the designated editor before applying it. This shared qualification can preserve historical contract bodies; it does not require opening all eight shafts. Separately correct AB/BC/CA's stale “no pickTarget” descriptions in `contracts-gears.js` without changing the six shaft keys. Restore pointer/touch access to the Lace viewer's two reference links while preserving keyboard access.

**Completion:** verify text delivery/parser behavior and static key/picker joins, then inspect narrow/wide rendering with a contract open, including A8/B2/E1/E2. Actual pointer/touch activation and visual legibility require browser observations. No claim that old pinned installs display a main-branch wording fix.

### E. Repair the bounded projection/runtime defects

**Findings:** P2-10–P2-13. **Owner:** one hologram editor for the shared HTML/SW/worker set; sequence these tickets within that claim to avoid concurrent edits to the same HTML.

| Ticket | Minimum behavior to restore | Synthetic check | Evidence still requiring a browser |
|---|---|---|---|
| E1: pass data | Stable distinct uniform data for opaque/floor and transparent passes through submission | Record buffer bindings/writes against a controlled WebGPU stub; distinguish both passes and prove neither reads the other's final flag | Actual D1 images at fixed `t=2` and `t=10`; correct material classes and no validation errors |
| E2: geometry/fallback | Finite nonzero-area filaments for all 11 row birth IDs in both worker and inline paths; retained D1/material/floor semantics | Execute the actual sources under Node; check triangle area and birth coverage. Tessellation 20 versus 16 may remain if explicitly documented; byte equality is not required | Visible filaments and meaningful fallback image; source geometry alone does not establish pixels |
| E3: recovery/evidence | Clear forced fallback state after successful recovery; truthful setup/submission/error reporting | Drive failure→recovery and REF off/on through actual handlers in a controlled fixture | Computed overlay visibility, image recovery, device-loss/validation behavior. Do not equate queue completion with painted geometry |
| E4: release selection | Only an explicitly selected release pointer can choose an update; unavailable/malformed/mismatched pointers fail visibly and retain the current pin | Mock provider success/failure, matching/different pins and waiting-SW states; no fallback to main, false “fresh”, or navigation after failed selection | Actual selected pin and installed-client behavior; no pointer promotion in this pass |
| E5: cache isolation | Candidate activation retains unrelated and other-pinned-install caches; any cleanup is confined to provably owned data | Seed same-origin sentinels and run the actual activate handler with isolated CacheStorage doubles | First-install offline, controlled warmed offline, and worker/REF/linked-view availability reported separately |

**E4 execution-review refinement:** a fulfilled service-worker `update()` request can precede installation completion. Cover the existing `installing` lifecycle and failure to `redundant`; neither a pending worker nor its failed installation may be called “fresh.” This stays within the same update handler and test fixture, without changing release selection, manifest identity or offline architecture.

Use built-in Node facilities and the actual runtime source in the fixtures. A mock must test an externally meaningful invariant, not merely repeat the chosen implementation. Cache isolation does not imply cold-offline readiness. This pass does not add a new offline carrier or promise perpetual offline operation. If current cache-write lifetime or precache coverage prevents a requested scenario, record the exact failure; do not silently expand the architecture to make the test green.

### F. Sight available target evidence and close the pass

**Finding:** P2-14. Use the supplied WebAssembly PDF for the specific rust-target Page G source requests. Record printed/PDF page identities, precise propositions and normative/proposal limits; do not treat an occurrence of a feature name as proof that a validator enforces it. Carry only established sightings into the existing wasm/rust-target records, with a dated addendum preserving prior receipts.

The [availability table](repo-update-pass-2-evidence.md#4-source-probe-and-supplied-file-limits) gives candidate sections for limits/address types, custom/name sections, EH and validation. The sighting must distinguish missing name metadata from absent crate code, local tag-section absence from imported tags, and the actual `throw`/`throw_ref`/`try_table` operators from a literal “try/throw” scan. G8′ is a proposed artifact-subset check; the PDF's FUL/DET profiles do not establish a selected validator mode or its switches.

Cargo A4 remains the next attachment. It, rustc A1's original text, A3, A10 and A11 are not among the twelve supplied attachments. A Wasm sighting cannot resolve Cargo flags, toolchain trust, R12 fixture permission or a real build. Keep the five SPOKEN custody locations and the Q0/Q5/slot-II source conflicts separate; no new human locator was recovered by planning.

Finish with before/after claim pairs, exact file changes and check results, source/synthetic/browser/device fields, dispositions for every P2 finding, and independent challenge of the touched dependency sets. Append one dated coherence-audit tick. Do not change the pass 1 census or rewrite its pre-publication receipt.

## 5. Exact candidate pools on execution

These are maximum file boundaries, not instructions to edit every file. Select the required passages for each batch; any new dependency needs a recorded reason and updated scope before editing it.

| Owner | Candidate paths |
|---|---|
| maps: record | this plan; `repo-update-pass-2-evidence.md`; `docs/plans/README.md`; `docs/coherence-audit-log.md` |
| maps: current models | `docs/math-execution-model-pass-6-arrival.md`; `docs/math-execution-model-pass-4-adversarial.md`; `docs/plans/math-execution-wasm-crosswalk-pass-1.md`; `docs/plans/math-execution-wasm-crosswalk-pass-3-adversarial.md`; `docs/plans/math-execution-problem-map-pass-1.md`; `docs/plans/atomic-construction-pass-1-plan.md`; `docs/plans/lace-context-iter7-pass-2-compute-and-inventory.md`; `docs/plans/lace-context-iter6-pass-5-crate-proposal.md`; `docs/reason-model-map.md` |
| clipboards: source corrections | `docs/clipboards/kauffman-4ed-mechanisms.md`; `docs/clipboards/kauffman-4ed-ascii-machinery.md`; `docs/clipboards/kauffman-4ed-clipboard.md`; `docs/clipboards/xiao-zhu-foundations-clipboard.md`; `docs/clipboards/clipboard-five-stakes.md` |
| clipboards: target sightings | `docs/clipboards/wasm-spec-3-clipboard.md`; `docs/clipboards/rust-target-clipboard.md`; dated evidence addenda only, no restamping the whole mapping or executing other campaigns |
| five navigation entrances | `docs/graphics/README.md` (graphics); `docs/history/README.md` (history); `docs/clock/README.md` and `docs/hologram/README.md` (hologram); `docs/gearing/README.md` (gearing-meta) |
| kit: hook wording | `.claude/hooks/session-start.sh`; `.claude/hooks/check-docs.sh`; `.claude/README.md` |
| coord: override | `docs/coord/coord.sh`; `docs/coord/README.md`; proposed new `docs/coord/tests/force-free-dispatch.sh` |
| gearing-meta: override backend | `docs/gearing/claim.sh`; `docs/gearing/CLAIMS.md` |
| prompts: verified carries | `docs/prompts/coord-protocol-prompt.md`; `docs/prompts/context-pass.md` (only changed source/result routing) |
| renderer: ordinary | `docs/clock/hcc-a-projection.html`; `docs/hologram/nostd-pipeline.html` |
| renderer: conditional | `docs/shadow-clock-gearing.html`, only after the human names the designated editor session |
| gear:gears | `docs/gearing/contracts-gears.js`, AB/BC/CA binding descriptions only; no other shaft is opened by this plan |
| hologram: runtime and evidence instructions | `docs/clock/lace-projection.html`; `docs/clock/sw.js`; `docs/clock/projection-bake-worker.js`; proposed new `docs/clock/tests/runtime-regressions.mjs` and `docs/clock/tests/README.md` |

`LATEST.json`, manifests/icons/lookrefs, fixture JSON, other contract shafts, canonical sources and reserved control sentences are read-only. No broad renderer redesign, performance campaign, new library or GitHub Actions workflow is proposed.

## 6. Execution order, evidence limits and stop

1. Reanchor, compare delta against this base, confirm actual owners/holds and inspect any changed premise. Do not rerun the complete branch survey merely because main has new descendants.
2. Perform A/B and the available source work in F; independent stations may run concurrently. Same-file work is serialized. Source correction precedes dependent summary/status changes.
3. Perform C with isolated claims; D's ordinary files and gear binding correction can proceed separately. Frozen gearing work stays visibly conditional.
4. Perform E with each original defect reproduced before repair; run bounded synthetic regressions after the corresponding change. Do not broaden testing without a concrete remaining risk.
5. If a suitable browser is available, perform the named visual/interaction/cache observations and record its exact environment. Planning found Node, but no Chromium/Chrome executable on PATH, Python Playwright, or exposed browser tool; it did not prove that every possible browser route is unavailable. Browser/GPU/device checks remain NOT_RUN until actually obtained. Missing browser evidence leaves those closure fields pending, while source and synthetic work can be completed and reviewed.
6. Record the actual receipt, run the existing checker and changed-link/scope checks, independently review and release all claims. Commit/push only under the applicable instruction, one station per commit.

**Publication is not deployment.** Before any later release promotion, inspect the exact candidate commit's dependency set, run its required browser/install evidence and obtain promotion scope. Promotion is a later commit pointing to the tested candidate SHA; do not require a commit to contain its own hash. The present release remains `3b619929182e98ccaa348a2fc696b98c2e416a2e` / `ixp2`.

**Completion is a vector:** source corrected; synthetic behavior verified; rendered/interaction behavior observed or pending; device/install behavior observed or pending; human decision present or still missing; released revision unchanged or separately promoted. No single green status replaces those fields. Pass 2 may finish its authorized local work with explicitly pending frozen-renderer, browser/device and human-decision items; it may not label those underlying defects closed.

**Planning actual, before Proceed:** the evidence record preserves the probes, review coverage and planning checks. Three independent planning reviews produced locator, authorization and fixture-contract corrections. Repairs, new regression files and publication were NOT_RUN at that handoff.

**Execution actual:** the [current execution receipt](repo-update-pass-2-evidence.md#8-execution-dispositions) records each finding's local result and remaining evidence obligations. Local execution does not change the pinned release or assert human acceptance.
