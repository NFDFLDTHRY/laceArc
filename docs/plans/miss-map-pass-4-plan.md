# Miss-map — Pass 4 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `30a14fc`).
**Depends on:** pass 3 Hands EXECUTED · W1–W6 · U1–U7.
**Emission:** `[GAP]`. No `src/`. No rustc run.

Pass 3 closed the fake D2 walk. Pass 4 lists what is still missing on the **wall** and **U12** sides of the calculation — not on \(L\).

---

## 0. One question

Which wall/U12 objects are already carded as predicates, and which checks or silences are still incomplete *as calculation objects*?

## 1. One result (on execute)

[`miss-map-pass-4-wall-u12.md`](miss-map-pass-4-wall-u12.md) — leftover wall/U12 miss list.

---

## 2. Must dispose

| Item | Expected (test) |
|---|---|
| W1–W6 cards | ALREADY-MAPPED predicates |
| U1–U6 predicates | ALREADY-MAPPED |
| U7 Cargo silences | U12 hole, not H-PTR |
| rustc G1–G10 `NOT_RUN` | U12 incomplete checks; still not I-EQ |
| OPEN #13 law custody vs W5 \(h\) | OUT or distinct custody miss |
| U6 Layer III Runtime vs renderer PAUSED | same predicate? or kit miss |
| OPEN #15 seed corpus | companion / G1, not U12 |
| “wasm64 opens Implementation Law” | already refused |

Do not run a G-gate to “complete” U12. Naming `NOT_RUN` is the miss.

## 3. Forbidden this pass

- `build-std` / rustc
- Fill H-PTR
- Pair Q×θ (pass 5)
- Open `src/`
- Start pass 5

## 4. Accept pass 4 when

- Every item in §2 marked.
- Zero G-gates flipped to RUN by this pass.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 5 is immediately authorized.

## Steward test

Shoe in hands. An unrun compile check is a missing *carrier* fact. **It is not a missing stitch.**
