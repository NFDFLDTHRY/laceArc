# Mathematical execution model — UPDATE campaign · Pass 5 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `13379e4`).
**Emission:** `[GAP]`. No `src/`. Not an emitter. Does not accept `pointer-emission.md`.
**Depends on:** pass 1–4 EXECUTED. Align: [`math-execution-update-pass-4-align.md`](math-execution-update-pass-4-align.md).
**Specimen:** Graphic D / D1. **Old transcript (frozen):** [`../math-execution-model-pass-3-witness.md`](../math-execution-model-pass-3-witness.md).

Pass 4 found 0 DIVERGE on Hands. Pass 5 walks D1 again under the **updated labels**. It does not grow a new lace.

---

## 0. One question

Does the published D1 specimen still sit inside \(\mathsf{Arrive}\) + Φ-morphism *shape* + the pass-3 interface, once OPEN cells have been retied to SPOKEN / CONFLICT / CLOSED — without turning any POINTER row into a calculated why?

## 1. One result (on execute)

[`math-execution-update-pass-5-witness.md`](math-execution-update-pass-5-witness.md) — a refresh audit. The old pass-3 witness file is **not rewritten**.

Same eleven rows. Same two-reference drawings. New stamp column only.

---

## 2. What the refresh must do

| Row | Old W2 | After execute |
|---|---|---|
| WORD 0000 / 0001 / 0003 / 0005 / 0008 | Arrive | still Arrive |
| POINTER 0002,0004,0006,0007,0009,0010 | Witness-only; Φ `[GAP]` | Witness-only; H-PTR **empty**; shape Q4 CLOSED |
| 0003 PIE after 0000 PIE | new occurrence | Q0 SPOKEN + HANDS CONFLICT (second WORD exists on D1) |
| 0004 after 0003 | shape OK | Q5 SPOKEN + HANDS CONFLICT (`0003` then `0004`); not a proof that POINTER was required |
| 0007 POINTER→POINTER | appears | Q4 allowed CLOSED; required still OPEN |
| Binary refs | illustrative | Q2 CONFLICT; **do not promote \(k=2\)** |

W3 identities and W4 orders stand. Restate them; do not invent a fourth identity.

W6 checklist: replace “still open” with the emission *stamps*, and keep the **No** column — D1 still does not *answer* Q1–Q5. SPOKEN is not a D1 derivation.

## 3. Forbidden this pass

- A transcript that “decides” Q2 from the picture
- A why for any POINTER row (that is H-PTR filled)
- Treating D1 as a training set or scheduler
- Silencing HANDS CONFLICT on `0003` / `0004`
- Editing the old witness file, emission file, or dual-universe §3
- Writing \(\Phi_{\mathrm{schedule}}\)
- Opening `src/`
- Starting pass 6

## 4. Accept pass 5 when

- Refresh file exists; all eleven D1 rows audited.
- Every POINTER remains Witness-only.
- Q2 still CONFLICT. H-PTR / H-k still empty.
- Old pass-3 witness untouched.
- U6 held. Emission `[GAP]`.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 6 is immediately authorized.

## Steward test

Shoe in hands. D1 is a specimen on the table. **A labeled specimen is not an emitter.**
