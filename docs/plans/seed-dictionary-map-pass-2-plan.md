# Seed dictionary × model — Pass 2 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `a419290`).
**Depends on:** [`seed-dictionary-map-pass-1.md`](seed-dictionary-map-pass-1.md).
**Emission:** `[GAP]`. No `src/`. No word list loaded.

Pass 1: seed = L1 prefix. Pass 2 describes that prefix **before any POINTER** — what exists to attach to when Φ has not fired.

---

## 0. One question

If the dictionary stream is only Arrive, what is on \(L\), what can \(\mathrm{Star}(v)\) already see, and what still cannot happen?

## 1. One result (on execute)

[`seed-dictionary-map-pass-2-prefix.md`](seed-dictionary-map-pass-2-prefix.md) — L1-only seed life.

---

## 2. What execute must record

| Fact | Status |
|---|---|
| \(L\) is a finite WORD-only line | forced-legal L1 |
| Indices exist; later POINTER *could* name them | attach-point **possible** |
| \(\mathrm{Star}(v)\) as WORD-pass view | readable |
| Definition route as POINTER chain | **not present** (no L4) |
| Spoken “wired” | **not this prefix** |
| Repeats of a spelling | new WORD rows (Hands) / Q0 CONFLICT named, not resolved |

Do not emit seed POINTERs to “complete” wiring.

## 3. Forbidden this pass

- Fill \(\theta_{\mathrm{when}}\)
- Invent a seed corpus
- Start pass 3 (spoken pay-in vs `0003`)
- Open `src/`

## 4. Accept pass 2 when

- WORD-only seed prefix is described as a legal \(L\).
- “Wired” is explicitly **not** this state.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 3 is immediately authorized.

## Steward test

Shoe in hands. A row of unused eyelets is still a lace. **Unused is not un-lace. Unused is also not yet grabbed.**
