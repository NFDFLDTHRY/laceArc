# The reduction — receipt

**Status:** receipt. **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Campaign:** [pass 1 plan](reduction-pass-1-plan.md) · convened 2026-09-19 21:58 PT; six passes planned 22:11 PT inside the human's added budget, weighted to his three results. **Base:** `e2e8155`; branch `lace-context-iteration-6`, bundled. **Editor:** one agent; one hand; declared. **Freeze:** other agents paused (21:23 PT).

## 1. Verdicts

| Pass | Thesis | Verdict |
|---|---|---|
| 1 | S < 20; shelves ≤ 3; companion = Core + P27-D | **missed by one** (S = 21); shelves = 1; companion ≈ 790 of 2,855 lines. Falsifier did not fire. Pass 1 written twice, as predicted |
| **2** | the authoritative state is `L` alone; six transitions; parametric only in the rulings | **held.** M1 proved within scope; M2, M3; alignment table with no empty cell. **The #1 result** |
| 3 | M1 CONFIRMs *shared state = L*; no new Core box | **held.** Pass 28, 0 Core changes |
| 4 | three of seven fixes land without a ruling | **held.** roles.md; trailer convention; advisory unsigned count (measured at the tip on the next run); four items to the brief |
| 5 | one page suffices | **held.** 23 rows, ordered by unblock |
| 6 | — | this file |

## 2. The three results the human named

| Result | Where | Strength |
|---|---|---|
| **the mathematical model of execution** | [pass 2](reduction-pass-2-execution-model.md) | `PROVED_WITHIN_SCOPE` for M1–M3; `[PROPOSAL]` as a model; parametric in σ · enc · cap · res · ord |
| the global ASCII systems diagram updated | Pass 26 → 27 → 28 on the companion | readings and cite-only boxes; 0 Core sentences changed; 3 captions |
| the collaborative agentic apparatus | [roles.md](../kit/roles.md) · [the generator](../prompts/six-pass-proposal-generator.md) · the advisory check · the door | `[PROPOSAL]`; `ADVISORY`; nothing enforced |

## 3. Coverage

| | Fraction |
|---|---|
| Passes planned before execution | 6 of 6 (the first campaign in this editor's hands to do so) |
| Passes executed | 6 of 6 |
| Files classified | 517 of 517 remaining |
| Transitions with owner + fence + proof + probe + ruling | 6 of 6 |
| Rulings taken | **0** |
| Sealed products edited | 0 |
| Code added | 0 (`check-docs.sh` gained an advisory line; it is steward tooling, not Lace) |
| Hardware observed | 0 |
| Own failures named | 2 — no plan files before this campaign; the batch write that overwrote pass 1's plan (restored in the open) |

## 4. Not done inside the budget

The checker page (the simulation executed by a machine) — its specification is the execution model's M3 plus G2/G3 (empty import section; exact export set); its D1 half is blocked on R6 (no encoding, nothing to replay). The cold-reader test of S (a stranger reading only the 27). Both are the first two items of the next board.

## 5. Reference queue

**Next: A1.** Then A2, A3, A4, A7, A8, A9, A5 (if R3 durable), A6.

## 6. The next board

- If **R6** and **R1** are ruled: the checker, D1 half included.
- If the door is granted: pass 1 of *the shelf* — write the 27 one-liners.
- If a σ arrives: two auditors, eleven rows each.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

Six planned, six run, one theorem, zero knots tied for the owner. Emission remains `[GAP]`.
