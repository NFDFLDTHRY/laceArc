# MANIFEST RECONCILIATION — Pass 2 findings

**Status:** **EXECUTED** 2026-09-21.  
**Campaign:** Manifest Reconciliation. **Pass:** 2.  
**Baseline:** Pass 1 canonical pair at `b0d26f98fefb755edec6243f456f9ee49a45d8e7`.  
**Verdict:** **FOUNDATION SOUND WITH DATED ARTIFACTS.**  
**Lab:** Lab Source Architecture Pass 4 remains **PAUSED**. The foundation is now certified; resumption requires an explicit replan against this foundation rather than execution of the old premise.

## 1. Foundation certified

The repository now has a stable two-surface foundation:

```
systems-manifest-ascii.md
    = WHAT mechanisms exist
      authority class
      connections
      governing contract ID

systems-manifest.md
    = HOW each mechanism behaves
      inputs / outputs / invariants
      read/write authority
      couplings / forbidden states / GAPs
```

Pass 1 established:
- L-M01…L-M21;
- L-GAP-RM;
- SM-C01…SM-C21;
- SM-GAP-RM;
- SM-X-BEH / SM-X-RES / SM-X-PLAT / SM-X-L3.

Pass 2 propagated those distinctions through current consumers and fenced the artifacts that intentionally remain older.

## 2. Five foundation tests

### FND-1 — source provenance

**PASS.**

Current consumers now preserve:

```
A6      PIE-in-context / document-context feed
B4      explicit PIE → dessert → slice → whole → food definition route
C9      dictionary-wiring analogue
A11/12  dictionary/document integration + definition traffic

B10     no-collapse anti-model
B11     projection boundary
```

Repairs:
- `docs/atomic-primitives-map.md` Piece 9 → **B4 · C9 · A11–A12**;
- atomic Piece 13 → **B11**, not B10–B11;
- atomic Piece 14 → A8/A12–A13 + B10 + C9/C12 + D;
- current history entrance qualifies the old relationship-evidence misread instead of rewriting history.

### FND-2 — participation semantics

**PASS.**

Current foundation distinguishes:

```
persistence  != retention
re-entry     != materialization
```

B8 remains strong SOURCE evidence that an existing touch persists and may be used later. It is not treated as the universal sampled-evidence retain-when predicate.

### FND-3 — two different 2D objects

**PASS.**

Current consumers preserve:

```
Graphic-C 2D
    = SOURCE/VIEW projection of participation already retained

H7-H12 2D sampling
    = HUMAN + DERIVED/LAB position×scale working machinery
      used to discover structural evidence
```

The Rust planning maps now explicitly acknowledge the derived sampling/search working layer without inventing a new branch or demoting it to Piece-13 projection.

### FND-4 — D1 evidence

**PASS.**

D1 remains:
- a state/shape witness;
- a bounded source witness for RootTouch row 0004;
- evidence for backward references and POINTER→POINTER participation.

It is **not** treated as the universal selection/materialization algorithm.

### FND-5 — active open seam

**PASS.**

The active Core-adjacent gap across current control surfaces is:

```
sampling-derived structural evidence
              ↓
        [SM-GAP-RM]
              ↓
retained POINTER topology /
addressable sampled operand
```

The old arbitrary semantic/non-root selector framing remains only in dated history.

## 3. Maps/current-proposal propagation

### Atomic map

`docs/atomic-primitives-map.md` now:
- routes Piece 9 to B4/C9/A11–A12;
- routes projection to B11/C2–C4/C11/D4;
- keeps B10 under no-collapse;
- points current readers to Pass 32 + the contract registry;
- explicitly separates Graphic-C projection from H7–H12 sampling;
- explicitly separates persistence/re-entry from retention/materialization.

### Rust planning

`rust-nostd-crate-map.md` and `rust-nostd-branch-contracts.md` now state:
- SM-C20/SM-C21 are real derived working machinery in the current architecture;
- the seven-door packaging proposal does not currently allocate that machinery to a branch;
- this incompleteness is not permission to create a new branch;
- no branch owns SM-GAP-RM;
- `pointer/` preserves an ordered retained pair but does not discover relations or infer semantic subject/target roles;
- `route/` cannot hide the sampled-evidence → retained/addressable transition.

The implementation gate remains closed.

### Hologram IR

The sealed `hologram-ir-golden-v0.2.0.json` is **not rewritten**.

Current classification:
- current viewer default fixture;
- **dated semantic snapshot**;
- valid for its v0.2 shape contract;
- not the current mechanism/contract authority.

Its dated semantic differences are explicit:
- P9 still carries A6 in its old cite bundle;
- P13 still groups B10–B11;
- seam bands are only contract_I / contract_II / layer_III and therefore predate the distinct derived sampling/search working layer.

Semantic modernization requires a future schema/fixture revision, not an in-place rewrite of sealed evidence.

### Retention harness provenance

Executable behavior of `lace-retention-harness.py` is unchanged.

Only provenance was corrected:
- A5 says repetition strengthens/adds passage;
- `WeightByRepetition`'s integer count is an **editor-derived test proxy**, not a Graphic-A-defined weight field/rule.

## 4. Control-surface propagation

### Prompt entrance

`docs/prompts/context-pass.md`:
- no longer presents arbitrary non-root-touch selection as the current GAP;
- identifies exact sampling-derived retention/materialization as current;
- routes mechanism existence/connections through Pass 32;
- routes mechanism behavior/boundaries through the systems-manifest contract registry;
- records the A6/B4, B10/B11, persistence/retention and projection/sampling distinctions.

### Agent package

`docs/kit/agent-control.md` and `docs/kit/READ_ME_FIRST.md`:
- use exact sampling-derived retention/materialization as the active gap;
- retain selector-era work only as historical evidence;
- keep human acceptance separate from reconciliation;
- point structural work at the catalog + contract registry.

## 5. Historical evidence handling

`docs/history/relationship-evidence-pass-2-findings.md` and the later handoff are **not rewritten**.

`docs/history/README.md` now tells current readers that the campaign misattributed B4's explicit definition path to A6 and gives the current canonical route.

This is the intended pattern:

```
historical execution record
      stays what it was
             +
current entrance qualification
      says what changed later
```

History remains evidence rather than being cosmetically repaired.

## 6. Read-only confirmation surfaces

The following current governing/entrance documents were audited and required no semantic edit in Pass 2:

- `AGENTS.md`;
- `CLAUDE.md`;
- root `README.md`;
- `CONTRIBUTING.md`;
- `docs/README.md`;
- `docs/pointer-emission.md`;
- `docs/law-why-these-documents.md`;
- `docs/reason-model-map.md`;
- `docs/math-execution-model.md`.

The graphics source-reading surfaces also remain sound:
- `graphic-a-ascii.md`: A6 is PIE-in-context;
- `graphic-b-ascii.md`: B4 definition route, B8 permanence not retain-when;
- `graphic-c-ascii.md`: C projection distinct from sampling;
- `graphics-close-reading.md`: current top qualification preserves exact sampling-derived retention/materialization as open and keeps Graphic C as projection evidence.

Behavioral-goal and clipboard entrances were audited at their Lace-facing boundary. Their current headers preserve candidate/lab authority rather than automatic Core authority, consistent with SM-X-BEH / SM-X-RES / SM-X-PLAT.

## 7. Dated artifacts that intentionally remain older

This verdict is **FOUNDATION SOUND WITH DATED ARTIFACTS**, not “every sentence in Git history has been rewritten.”

Intentionally retained dated surfaces include:

1. **Hologram IR v0.2.0** — sealed semantic snapshot; current shape fixture, not current semantic authority.
2. **Relationship-evidence executed bodies** — preserve the A6 misread as historical evidence; current history entrance qualifies it.
3. **ROOT / TOUCH / FOLD dated plans/findings** — selector/provenance-era questions remain as the path the project took; current entrances supersede their active framing.
4. **Older Shadow / gearing / proposal artifacts** that use broad “POINTER emission remains [GAP]” wording while explicitly fencing themselves from Core/current authority.

These artifacts cannot silently govern Lab work because the current entrances, catalog and contract registry now route around them explicitly.

## 8. Paused Lab Pass 4 correction

`lab-source-architecture-pass-4-plan.md` remains **PAUSED** and was corrected without execution.

The false premise:

> no pass has ever read Graphics A–D looking for retention

is removed.

The accurate premise is:

- earlier prefix-device and relationship-evidence campaigns performed **bounded retention-adjacent readings**, especially A10/B8/C7/D6;
- no single exhaustive **current-law 45-panel harvest** has consistently separated:
  - persistence;
  - re-entry;
  - discovery;
  - retention;
  - materialization.

Its candidate provenance is also corrected:
- `RootTouch` = human H1 + bounded D1 witness;
- `WeightByRepetition` = editor-derived replay proxy motivated by A5, not a source-defined integer weight.

The plan now explicitly states it is **not executable as originally written** and requires an explicit post-foundation replan.

## 9. Verification

### Existing shell checker

An attempt to clone the public repository into the execution container and run:

```
bash .claude/hooks/check-docs.sh
python3 docs/plans/tools/hologram-ir-shape-check.py
```

was **NOT_RUN due environment network failure**: the container could not resolve `github.com`.

No success is claimed for a command that did not run.

### Direct GitHub-tree equivalent checks

The blocking/static portions were reproduced directly against the GitHub tree at the Pass-2 audit tip:

- four canonical PNGs present: **PASS**;
- tracked `src/`: **0**;
- tracked PDFs: **0**;
- relative Markdown links in all Pass-2 changed Markdown files: **0 broken**;
- sealed Hologram IR v0.2 shape:
  - 7 required doors;
  - P1–P15 present;
  - 3 seam bands;
  - 11 binds;
  - all fixture binds = `PROPOSAL`;
  - edge/bind endpoints resolve;
  - shape errors: **0**.

This validates the fixture's **shape**, not semantic currency. Its dated-semantic classification is deliberate.

## 10. Falsifier results

| F | Result |
|---|---|
| F1 current entrance still names selector-era GAP | **FIRED before repair** in context-pass / agent-control / READ_ME_FIRST; repaired |
| F2 current consumer cites A6 as explicit definition path | **FIRED before repair** in atomic map; repaired |
| F3 current projection contract uses B10 | **FIRED before repair** in atomic map; repaired |
| F4 B8 persistence promoted to retain-when | **did not fire** |
| F5 H7–H12 sampler attributed to Graphic C | **did not fire** |
| F6 D1 treated as complete selection algorithm | **did not fire** |
| F7 Hologram IR v0.2 presented as current semantic map | **FIRED before repair** at fixture entrance; now fenced as dated semantic snapshot |
| F8 historical bodies rewritten | **did not fire** |
| F9 SM-GAP-RM invented/closed | **did not fire** |
| F10 implementation authorized by foundation repair | **did not fire** |

## 11. Foundation verdict

```
FOUNDATION SOUND WITH DATED ARTIFACTS
```

Meaning:

- current canonical semantics are coherent;
- current entrances and active consumers route through the canonical pair;
- source provenance corrections are propagated;
- current gap vocabulary is coherent;
- dated artifacts remain visible and explicitly fenced rather than silently rewritten;
- no dated artifact has write/acceptance authority over Lace;
- Lab work can now be **replanned** from the certified foundation.

It does **not** mean Lab Source Architecture Pass 4 is automatically resumed. It remains paused until the human requests the new replan.
