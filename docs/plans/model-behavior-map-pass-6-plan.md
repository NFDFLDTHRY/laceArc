# Model behaviors × equations — Pass 6 of 6 PLAN

**Status:** **`PLAN`** · not executed. **Station:** maps (HELD `Grok-maps` · BASE `3e740cf`).
**Depends on:** passes 1–5 EXECUTED.
**Emission:** `[GAP]`. No `src/`. Does not accept `pointer-emission.md`.

Pass 5 named free coordinates. Pass 6 judges whether the **behavior map** is sound, then **STOP**. Campaign closes 6/6. This is not a calculated schedule.

---

## 0. One question

Does the six-pass behavior map still separate forced Arrive, shape-legal POINTER, refused acts, one D1 specimen, and seven unpicked parameters — without smuggling a when?

## 1. One result (on execute)

[`model-behavior-map-pass-6-soundness.md`](model-behavior-map-pass-6-soundness.md) — board + STOP.

---

## 2. Board execute must mark

| Cell | Must hold |
|---|---|
| S1 | L1 Arrive forced-legal given ingest |
| S2 | L4 POINTER shape-legal, not forced |
| S3 | 11 refused acts; none is a when |
| S4 | D1 6/6 L4; one L5 group only |
| S5 | seven \(\theta\) with 0 values |
| S6 | spoken Q1 still not `Step` |
| S7 | wall/U12 filters ≠ stitches |
| S8 | this file ≠ acceptance |

FAIL is a finding. Do not soothe by writing Φ.

Allowed surgical: CLOSED stamp on pass 1 header only.

## 3. Forbidden this pass

- Pick any \(\theta\)
- Accept emission
- Open `src/`
- Pass 7 of *this* series

## 4. Accept pass 6 when

- S1–S8 marked.
- STOP published; campaign CLOSED 6/6.
- Plan `PLAN` → `EXECUTED`. maps released.

## Steward test

Shoe in hands. A sound behavior map still has free axes. **Closing the list is not tying the bow.**
