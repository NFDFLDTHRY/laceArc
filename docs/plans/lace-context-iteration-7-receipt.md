# Iteration 7 receipt — the team, the distribution model, the component inventory

**Status:** receipt. **Station:** maps. **Emission:** `[GAP]`. No `src/`.
**Iteration:** [plan](lace-context-iteration-7-plan.md). **Base:** `e2e8155`, branch `lace-context-iteration-6`. **Editor:** one agent (Claude Fable 5.1); no independent review.
**Closed early, by the human's redirection.** On 2026-09-19 21:58 PT the human named the run's campaign — *the reduction* — and this iteration's remaining passes fold into it (§4). Three of six passes ran.

## 1. Thesis — did it lose?

**Thesis:** the team failure is an attribution failure first and a capability failure second; once attribution is possible the behaviours sort into roles the station protocol can enforce with two additions and one lock.

**Held on its first half, untested on its second.** The 773 anonymous commits sorted into three signatures ([pass 1 §3](lace-context-iter7-pass-1-actors.md)); the falsifier did not fire. Whether the roles *can be enforced* is untested — F1–F7 are proposals on kit/coord and no one has run them.

## 2. Established

| | Where | Strength |
|---|---|---|
| 225 of 1,035 commits signed (one Claude Code session, Opus 5 → Fable 5.1 → Opus 5); 810 unsigned; Grok Bot is "LaceArc" by its profile; Codex, Astra, reference-Mapper by handle; the prefix-device campaign unattributed | pass 1 | INFERENCE from git; locators on every row |
| Roles per station from behaviour; law station locked to the human + one named editor; fixes F1–F7 | pass 1 §5–6 | `[PROPOSAL]` |
| The production constraint landed as a kit amendment in the human's words; what it forces beneath as inference | [distribution-model.md](../kit/distribution-model.md) | HUMAN_REPORTED · acceptance `ABSENT` until affirmed in-repo |
| Heterogeneous compute under the law: one writer; WebGPU/WGSL as π; **WebNN no Core role**, one Layer III role on OPEN #14; frontier protocol R9; isolation only via SW-synthesised headers R11 | [pass 2](lace-context-iter7-pass-2-compute-and-inventory.md) | `[PROPOSAL]` |
| 32-component global inventory, every row with owner and gate, five holes named | pass 2 §6 | `OBSERVED` as a composition of cited documents |
| The companion audited edge-by-edge: 0 contradictions; 3 literal-reading defects captioned; 6 missing boxes admitted cite-only (Pass 27); OPEN #14, #15 | [pass 3](lace-context-iter7-pass-3-audit.md) | `OBSERVED` |

## 3. Not established; deferred

| | Why | Goes to |
|---|---|---|
| The checker — the environment executed by a machine (pass 4) | not run | **the reduction**, as the reduction's executable form |
| The promotion rule as a rulings brief (pass 5) | prepared in outline only (G1–G7) | the reduction, pass 5 |
| Any of R1–R11, #14, #15 | the human's | the reduction's rulings queue |
| F1–F7 as enforced controls | proposals only | kit / coord, when the human rules |

## 4. My own tooling failure, named

**No pass had a plan file.** Eight passes across iterations 6 and 7 were executed straight into findings, with the pass-level planning folded into the two iteration boards. The tree's measured cadence is *Plan → Proceed → Commit*, one plan file per pass, measured before proposing, with allowed files and a stop condition stated before the first edit. I did not do that, and the iteration 6 receipt did not say so. The human caught it. From the reduction's pass 1 onward every pass has its plan first, status `PLAN` until the human says proceed.

## 5. Coverage

| | Fraction |
|---|---|
| Passes run | 3 of 6 |
| Commits classified | 1,035 of 1,035 |
| Companion surfaces audited as edges | 28 of 28 |
| Inventory rows with owner + gate | 32 of 32 |
| Pass-plan files written before execution | **0 of 3** |
| Rulings taken | 0 |
| Hardware observed by this session | 0 |

## 6. Reference queue (one per reply)

**Next: A1** — rustc *Platform Support → `wasm64-unknown-unknown`*, printed to PDF with the date. Then A2 (Memory64 intent + SpiderMonkey post), A3 (WebAssembly JS Interface, memory limit), A4 (Cargo `build-std`), A7 (GitHub Pages custom domain — headers), A8 (HTML *cross-origin isolation*), A9 (WASM threads/atomics status), A5 (WHATWG File System) if R3 goes durable, A6 (Manifest + Service Workers).

## 7. The board for what follows

The human named it: **the reduction** — [pass 1 plan](reduction-pass-1-plan.md), status `PLAN`.

## Steward test

If you cannot do it to a shoe lace in your hands, you may not do it to laceArc.

Three knots tied, three left on the bench, one bad habit written down. Emission remains `[GAP]`.
