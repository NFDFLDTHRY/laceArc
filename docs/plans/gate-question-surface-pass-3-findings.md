# The gate's question surface — Pass 3 findings · `F1` fired, and the thesis with it

**Status:** **EXECUTED** 2026-09-21. **Station:** maps. **Plan:** [`gate-question-surface-pass-3-plan.md`](gate-question-surface-pass-3-plan.md).
**Verdict: the thesis is false, and false in the useful direction.** **Emission:** `[GAP]`. No `src/`. **Law station untouched.**
**New this pass:** the four graphics were supplied to the executing session directly. **Panel D was read with sight for the first time in this campaign.**

---

## 1. `F1` fired. Graphic D is carried

[The plan](gate-question-surface-pass-3-plan.md) required `F1` be checked *"before any finding is claimed"*: **does a D companion exist under another name?**

**It does.** [`systems-manifest-ascii.md`](../systems-manifest-ascii.md) carries:

| | |
|---|---|
| **the D1 tape, exact** — all eleven rows, headed *"D1 tape (close-reading, exact)"* | lines ~342–352 |
| **D-panel summaries** — `D3` pointer participates · `D4` 3D view is not stored · `D5` repeated variable → star · `D6` pointer may ref a pointer · Key props | above it |
| **Graphic A, B and C spines** in the same file | below it |

**My premise confused two different things**: that no file is *named* `graphic-d-ascii.md`, and that D is *uncarried*. Only the first is true.

**And the repository told me.** [`graphic-a-ascii.md`](../graphics/graphic-a-ascii.md)'s own header lists what it is not: *"**Not this file:** B, C, **D1 tape, Hands-manifest spine (`docs/systems-manifest-ascii.md`)**"* — a pointer straight at the carry, in a file this campaign had already opened. **I read the header and did not follow the pointer.**

**The pass shrinks to saying so**, as the plan required. What survives is below.

## 2. The reach audit, run on the corrected premise — and it inverts

| Closure kind | Evidence | Reachable by a reader who cannot open the PNGs? |
|---|---|---|
| **picture-closed** | `D1` rows · `D2` schema · `D6` | **`CARRIED`** — the tape and the panel summaries are in `systems-manifest-ascii.md` |
| **ruling-closed** | `H1`–`H6` | **`TEXT`** — quoted verbatim in [the root-touch-fold pass 3 findings](math-execution-root-touch-fold-pass-3-findings.md) §0 |
| **inference-closed** | `D1` 6/6 + `D2` | **`CARRIED`** — the evidence is reachable; the *inference* is the open issue, not the reach |

**Every closure on the gate is reachable in-repo by a text-only reader.** The thesis said the gate's picture-closures were its least checkable evidence and that this inverted the source-of-record order. **It is the reverse: the graphics shelf had already solved the problem, under a name I did not look for.**

**This is the better outcome and it should be said plainly.** A campaign that only reports the findings flattering to its own premise is not measuring.

## 3. What a sighted reading of D adds

The four graphics were put in front of the executing session. **Panel D was compared to the transcription every claim in this campaign rests on.**

| Check | Result |
|---|---|
| D1's eleven rows vs [the close reading's table](../graphics-close-reading.md) | **match, row for row** — index, type, references and Explanation |
| `ref_A` / `ref_B` — whose vocabulary? | **the graphic's own.** D2 draws the POINTER entry as `index · type · ref_A · ref_B`. Not a reader's invention |
| D2's slot count | **exactly two slots drawn**, as the close reading says |
| Does D state a slot rule anywhere? | **no.** Panels 1, 2, 3 and 6 label the relations; none gives a rule for which operand is `ref_A` |
| Does D state when a pointer is emitted? | **no.** The footer is *"Input arrives. Append a new section to the Lace. Use pointers to participate in what already exists."* — no trigger, no chooser |

**[Pass 2](gate-question-surface-pass-2-findings.md) is confirmed by a second reader, and it is the first second reader D has had in this campaign.** Its fourteen refutations, its four-signature argument and its 6/6 subject pattern stand on a transcription that has now been checked against the panel.

## 4. Pass 1's census missed a fifth inventory

`systems-manifest-ascii.md` also carries **a twelve-item open-question table** — headed *"From `docs/systems-manifest.md`. Not answers."*

[Pass 1](gate-question-surface-pass-1-census.md) enumerated **four** sources and mapped 22 distinct questions. **This is a fifth**, and it is not a duplicate of the manifest's eleven:

| | |
|---|---|
| **item 12** — *"emission still unanswered; HCC is not piece 16"* `[GAP]` | **appears in no source pass 1 counted** |
| **item 9** — *"slot assignment `ref_A` vs `ref_B`"* `[GAP]` | the same question the law file's table omits — **absent there, present in two other files** |
| **item 2** — *"POINTER when / arity / implicit adjacent edge"* `[GAP]` | **fuses** what the law file splits into three rows, two of them `CLOSED` |

**Item 2 is the sharper form of pass 1's `F2`.** The ascii spine states the open question as *"whether arity is **exactly** two forever"*, and the law file answers a narrower question — *"CLOSED current constructor: 2"* — without saying the wider one is still open.

**So the count is worse than pass 1 reported: five inventories, not four.**

## 5. And one of pass 2's findings was already in the tree

The same file records, under the D1 tape:

> *"Still `[GAP]`: when to emit; whether arity is exactly two forever; **slot A vs B (not chronological)**."*
> *"Ruled out: every adjacent WORD pair emits a POINTER (0001–0003 and 0005–0008 have none)."*

**Chronology's refutation was recorded in two places, not one.** [Pass 2's `F4`](gate-question-surface-pass-2-findings.md) said two of fourteen were known; the count holds, and the second location is now named. **Twelve refutations, the signature argument and the sighted confirmation remain new.**

## 6. Falsifiers

| | Result |
|---|---|
| **`F1`** a D companion exists elsewhere | **FIRED.** `systems-manifest-ascii.md`. The thesis collapses and the pass shrinks, as planned |
| **`F2`** the picture-closures do not cite D | **did not fire** — they do |
| **`F3`** the A/B/C companions are not transcriptions | **did not fire** — `graphic-a-ascii.md` reads *"This is an ASCII reading … and settles nothing the PNG does not"* |
| **`F4`** reach is the wrong measure here | **moot.** `F1` fired first. Had it not, `F4` would still have applied: the PNGs are tracked, so the limit was always narrow |

## 7. What this pass did not do

- **Did not write a D transcription.** One exists. It should be **linked from the graphics door**, which is graphics-station work and not this campaign's.
- **Did not edit the law file or the manifest.** Byte-unchanged.
- **Did not close, reopen or propose anything.**

**Pass-3 receipt:** `F1` run first as the plan required, and it fired — Graphic D is carried in `systems-manifest-ascii.md`, under a name the campaign never searched for and a pointer it had already read past. The reach audit re-run on the corrected premise **inverts the thesis**: every closure on the gate is reachable by a text-only reader. Panel D read with sight and the campaign's transcription verified row for row; `ref_A`/`ref_B` confirmed as the graphic's own vocabulary; D confirmed to state **no** slot rule and **no** emission trigger. **Two corrections to pass 1**: a fifth inventory exists, and it carries a question (`item 12`) that appears in none of the four counted. No `[GAP]` closed, no ruling taken. Emission `[GAP]`. Shoe in hands, or no.
