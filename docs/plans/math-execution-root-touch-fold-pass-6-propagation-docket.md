# ROOT / TOUCH / FOLD Pass 6 — provenance propagation docket

**Status:** DATED HANDOFF. **Not executed in Pass 6.**
**Source state:** Pass-6 V-C verdict + law reconciliation complete.
**Acceptance:** absent.
**No src/. No Cargo.**

Pass 6 reclassifies the remaining Core-adjacent hole.

Old propagated shorthand from Pass 5:

\[
\text{arbitrary relation/sample selection}=[GAP]
\]

Current law after Pass 6:

\[
\boxed{
\operatorname{Origin}(\rho)=\text{[GAP]}
}
\]

where rho is an already-specified arbitrary non-root ordered relation/sample.

In prose:

> **What authoritatively originates / asserts / owns the ordered operands or sample for an arbitrary non-root-touch relation?**

Current law does **not** establish:
- an automatic Core chooser;
- an external assertion/write interface;
- a hidden Contract-II relation payload;
- a Layer-III write path;
- a route/document boundary selector.

RootTouch remains the closed provenance special case.

---

## 1. Why propagation is deferred

Pass 5 already performed a large authority-to-companion/control propagation wave.

Pass 6 is a classification/provenance pass.

Changing every lower-authority surface again inside Pass 6 would:
- blur the source-necessity result with a repository sweep;
- risk colliding with the gate-question-surface campaign;
- overwrite useful dated Pass-5 receipts;
- make it harder to see that only the **name/location of the gap** changed.

Therefore Pass 6 updates only:
- pointer-emission.md;
- systems-manifest.md;
- math-execution-model.md;
- Pass-6 records.

Everything below is a later handoff.

---

## 2. Global systems ASCII

**Target:** docs/systems-manifest-ascii.md.

Current Pass 29 says:
- arbitrary relation/sample selection = [GAP].

Later qualification should say:

    arbitrary non-root relation assertion
          |
          | origin / authority / ownership [GAP]
          v
    ordered relation/sample rho
          |
          v
       Join / Ground

Important:
- do not delete Pass 29;
- append/current-qualify;
- do not imply the assertion is external;
- do not imply Core automatically chooses it;
- preserve RootTouch as ruled provenance;
- acceptance remains absent.

---

## 3. Graphic B/C ASCII companions

### Graphic B

Current qualification says arbitrary non-root-touch operand/sample selection remains [GAP].

Later wording:
- B still names retained touch/threading;
- current storage constructor remains Join;
- arbitrary non-root relation **provenance** remains [GAP];
- B itself does not say who originates arbitrary relation assertions.

### Graphic C

Current qualification says arbitrary relation/sample selection remains [GAP].

Later wording:
- Join-of-Join storage remains current;
- C6 labels remain a separate [GAP];
- arbitrary relation **origin/provenance** remains [GAP];
- N-D stays projection-only.

Do not touch PNG authority.

---

## 4. Active maps

### atomic-primitives-map.md

Current Pass-5 qualification says "arbitrary relation/sample selection."

Later split:

**constructor**
- unchanged;

**provenance**
- RootTouch provenance: ruled;
- arbitrary non-root relation assertion origin: OPEN;
- automatic Core chooser: not established;
- external assertion interface: not established.

Do not add a new primitive for rho or Origin(rho).

### reason-model-map.md

Current adjacent-source conclusion can become:

> adjacent references supply neither the constructor law nor arbitrary relation-assertion provenance.

Do not import provenance from Kauffman / LLM / GNN / AgentScope / HCC-A / Coffee Cup / Water.

---

## 5. Dated environment

**Target:** docs/plans/lace-context-iter6-pass-4-environment.md.

Pass-5 qualification currently writes:

\[
Select\;[GAP]\to(a,b)\text{ or }S\to Join/Ground.
\]

Later qualification should not rewrite E1-E10/E5.

Replace only current qualification conceptually with:

\[
Origin(\rho)\;[GAP]
\to
\rho
\to
Join/Ground.
\]

State:
- no Select(L,v) function is established;
- old candidate ledger remains dated scheduler evidence;
- Pass 6 refuted three automatic punch-card interpretations as universal D1 schedules;
- no external relation interface established.

---

## 6. Whole-app reduction model

Current Pass-5 qualification says arbitrary selection remains [GAP].

Later wording:

- Sigma=(L,h,K) unchanged;
- hopper h remains word-arrival custody;
- no rho or selector state added;
- deterministic construction begins once rho is specified;
- Origin(rho) remains [GAP];
- no claim whether provenance is internal or external.

---

## 7. Rust planning surfaces

Targets:
- rust-nostd-crate-map.md
- rust-nostd-branch-contracts.md
- rust-nostd-second-reading.md

Later premise wording should say:

**known**
- binary Join mechanics;

**still unowned**
- arbitrary non-root relation-assertion provenance / authority.

Do not create:
- selector crate;
- rho type;
- relation command;
- input channel;
- branch ownership assignment.

Branch/proof eligibility remains unchanged.

---

## 8. Control surfaces

Targets:
- README.md
- docs/README.md
- CONTRIBUTING.md
- AGENTS.md
- CLAUDE.md
- docs/prompts/context-pass.md
- docs/kit/agent-control.md steward layer
- docs/kit/roles.md
- docs/kit/READ_ME_FIRST.md

Current Pass-5 language consistently says "arbitrary relation/sample selection remains [GAP]."

Later propagation should refine it to:

> arbitrary non-root relation-assertion provenance / authority remains [GAP]; an automatic Core chooser is not established, and an external assertion interface is not established.

Preserve:
- acceptance gate;
- frozen stake;
- verbatim transported human-law block;
- historical Pass-7/E5 receipts;
- no-src law.

---

## 9. Gate-question-surface campaign coordination

Pass 6 intentionally does not modify that campaign.

Its Pass 2 owns:
- ref_A / described-subject evidence;
- operand semantic ordering question.

A future provenance propagation pass must:
- read the gate campaign's current tip first;
- not overwrite its closure-authority work;
- distinguish semantic slot ordering from relation assertion origin.

---

## 10. Propagation acceptance tests

A later pass should prove:

1. no surface still implies Select(L,v) is known to be the missing Core function;
2. no surface claims provenance is external unless newly ruled;
3. no surface claims automatic Core choice is required unless newly ruled;
4. RootTouch provenance remains the closed special case;
5. D1 five arbitrary non-root relation origins remain open;
6. PC-2 whole-prefix-per-arrival remains refuted as universal;
7. PC-3 one-active-sample remains refuted as complete rule;
8. PC-4 all-portions-auto remains refuted as obligation;
9. constructor law remains unchanged;
10. acceptance remains absent;
11. no rho row/type/interface is introduced;
12. no src/Cargo/Rust implementation appears.

## Steward test

Propagate the smaller, more accurate hole.

Do not teach future agents that the missing thing is a chooser unless the source first proves that the machine is supposed to choose.

The open question is who or what authoritatively originates the relation assertion.
