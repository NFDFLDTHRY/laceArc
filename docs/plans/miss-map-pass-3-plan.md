# Miss-map — Pass 3 of 6 PLAN

**Status:** **`EXECUTED`** 2026-09-20 · [`miss-map-pass-3-hands.md`](miss-map-pass-3-hands.md). **Station:** maps (`Grok-maps`).
**Depends on:** [`miss-map-pass-2-sort.md`](miss-map-pass-2-sort.md).
**Emission:** `[GAP]`. No `src/`. Slot II stays unanswered.

Pass 2: Slot II is IN-CALC-MISS; D2 is SPECIMEN-MISS. Prefix-device already records **D2 as the WORD/POINTER shape panel**, not a second 11-row array. Pass 3 maps that **Hands panel** onto the equations and lists remaining Hands misses. Wall/U12 is pass 4. Q×θ is pass 5.

---

## 0. One question

What does Graphic D panel D2 actually claim in Hands, and which of those claims are already S1 Kind / Arrive / Star-view — versus Slot II still sitting in CONFLICT?

## 1. One result (on execute)

[`miss-map-pass-3-hands.md`](miss-map-pass-3-hands.md) — D2 panel × equations + remaining Hands-miss list.

Do not invent a D2 row-walk that D2 does not draw. Do not pick cluster vs variable.

---

## 2. Hands misses execute must treat

| Item | Job |
|---|---|
| D2 kinds WORD \| POINTER | map to S1.3 Kind — likely ALREADY-MAPPED |
| D2 “variable is the word” | Hands side of Slot II CONFLICT — keep both sides |
| D2 POINTER = refs earlier | map to Φ-morphism shape — ALREADY-MAPPED |
| Slot II spoken cluster | IN-CALC-MISS remains; not \(\theta_{\mathrm{G1}}\) |
| Carrier / Hopper I-letters | Hands-adjacent named-unmapped; no new letters |
| \(\theta_{\mathrm{WORD2}}\), \(\theta_{\mathrm{G1}}\), \(\theta_{\mathrm{reqPTR}}\), \(\theta_{\mathrm{left}}\) | Hands holes already named |

## 3. Forbidden this pass

- Answer Slot II
- Draw a fake D2 array
- Enumerate U12 (pass 4)
- Pair Q×θ (pass 5)
- Open `src/`

## 4. Accept pass 3 when

- D2 panel claims each have a mark.
- Slot II still CONFLICT, no value.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 4 is immediately authorized.

## Steward test

Shoe in hands. D2 shows two kinds and a variable. **A cluster you see later is not that variable row.**
