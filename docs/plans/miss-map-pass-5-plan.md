# Miss-map — Pass 5 of 6 PLAN

**Status:** **`EXECUTED`** 2026-09-20 · [`miss-map-pass-5-q-theta.md`](miss-map-pass-5-q-theta.md). **Station:** maps (`Grok-maps`).
**Depends on:** emission Q0–Q5 + Slot II · seven \(\theta\) · Slot II IN-CALC-MISS.
**Emission:** `[GAP]`. No `src/`. Pairing is not answering.

Pass 4 finished wall/U12 leftovers. Pass 5 aligns the **draft questions** to the **free parameters**. HALF from pass 1 closes as a *map*, not as Φ.

---

## 0. One question

Which emission Q is which \(\theta\), and which Q has **no** \(\theta\) (Slot II)?

## 1. One result (on execute)

[`miss-map-pass-5-q-theta.md`](miss-map-pass-5-q-theta.md) — one row per Q / Slot II.

Columns: Q · stamp · \(\theta\) or none · same hole? · value still unpicked?

---

## 2. Expected pairs (test, don’t rubber-stamp)

| Q | Candidate \(\theta\) |
|---|---|
| Q0 later WORD | \(\theta_{\mathrm{WORD2}}\) |
| Q1 when POINTER | \(\theta_{\mathrm{when}}\) |
| Q2 arity | \(\theta_k\) |
| Q3 leftover adjacency | \(\theta_{\mathrm{left}}\) |
| Q4 required PTR→PTR | \(\theta_{\mathrm{reqPTR}}\) (backward CLOSED is not a \(\theta\)) |
| Q5 later pass requires POINTER | overlaps \(\theta_{\mathrm{when}}\) + \(\theta_{\mathrm{WORD2}}\) |
| Slot II | **no \(\theta\)** — own IN-CALC-MISS |
| G1 identity | \(\theta_{\mathrm{G1}}\) (not a Q number) |
| extra L5 groups | \(\theta_{\mathrm{group}}\) (not a Q number) |

A pair that would pick a value is a breach.

## 3. Forbidden this pass

- Answer any Q
- Merge Slot II into \(\theta_{\mathrm{G1}}\)
- Open `src/`
- Start pass 6 unless immediately authorized

## 4. Accept pass 5 when

- Every Q + Slot II + the two extra \(\theta\) have a row.
- Zero values picked.
- HALF from pass 1 is now **ALIGNED** (map) or a named leftover pair.
- Plan `PLAN` → `EXECUTED`. maps released unless pass 6 is immediately authorized.

## Steward test

Shoe in hands. Two names for one hole are still one hole. **Alignment is not an answer.**
