# MATHEMATICAL EXECUTION MODEL — Behavioral Read Devices Pass 2 descriptive composition map

**Status:** EXECUTED PASS-2 ARTIFACT.
**Campaign:** Mathematical Execution Model — Behavioral Read Devices.
**Pass:** 2 — MAP WHAT THE EYE CAN SEE.
**Execution base:** 0f81a798f773d5b97c8860d10f17e0649e632168.
**Inputs:** direct source reread; Pass-2 observability map; current S9 generic device algebra.
**Purpose:** map the minimum faithful descriptive architecture required by each source after observability has been separated from Lace omniscience.
**Not:** a full source-specific runtime implementation; not cross-source composition; not Core.

---

## 1. S9 chassis result

Pass 2 tested whether the generic S9 device

\[
\mathcal D_\theta=(Q,G,\Omega,\Delta,\omega)
\]

must grow a new runtime field before any source-specific device can be expressed.

### Verdict

**S9 CHASSIS SUFFICIENT WITH SOURCE-SPECIFIC FACTORIZATION.**

No new generic field is required.

The apparent missing distinctions can be expressed by:
- factoring \(Q\);
- typing \(G\);
- constraining \(\Omega\);
- typing \(\Delta\)'s output;
- strengthening \(\omega\)'s provenance obligations.

The generic chassis remains minimal.

---

## 2. Diagnostic/readout versus causal transition

Pass 1 folded output into \(\Delta\).

Pass 2 tests whether a pure diagnostic/readout requires a separate \(\lambda\) map.

It does not.

A pure descriptive step can be represented as:

\[
\Delta(q,o,g)
=
(q,y),
\]

so:

\[
q'=q.
\]

The state is unchanged while a typed readout \(y\) is returned.

Therefore:

\[
\text{description}
\neq
\text{state mutation}
\]

even though both use the same generic transition/output map.

### Consequence

No separate generic output function is needed.

Source-specific devices may define:
- mutating local transitions;
- identity-state descriptive transitions;
- external action/request outputs

using the same \(\Delta\) codomain.

**Verdict:** S9 does not require re-expansion.

---

# PART A — HCC-A SHAPE

## 3. HCC-A is a forked recurrent compiler with a diagnostic overlay

A future HCC device should preserve at least three mathematically distinct layers.

### 3.1 Layer H-A — source architecture components

Candidate factorization:

\[
Q_H
=
Q_{RIC}
\times
Q_{PFC}
\times
Q_C
\times
Q_{story}
\times
Q_M
\times
Q_E
\times
Q_{ISL}
\times
Q_B
\times
Q_{\phi}.
\]

This is **not** a claim that each factor must be a separate runtime object.

It is a descriptive factorization preserving source distinctions.

### 3.2 Layer H-B — parallel input fork

For admitted source packet \(x_t\):

\[
r_t
=
F_{RIC}(x_t),
\]

\[
p_t
=
F_{PFC}(x_t;\theta_H,q_t).
\]

The source requires:

\[
x_t
\to
\begin{cases}
r_t\\
p_t
\end{cases}
\]

in parallel/coexistence before compiler integration.

The evidence path may not be defined as:

\[
r_t:=p_t.
\]

### 3.3 Layer H-C — recurrent compiler loop

A later HCC-specific transition may have source-shaped composition:

\[
(r_t,p_t)
\to
C
\to
L_{story}
\to
M
\to
E
\to
ISL
\to
B
\to
R' / \text{next input}.
\]

S0–S7 is an ordering/phase description over that recurrent architecture.

It is not the same object as the component set.

### 3.4 Layer H-D — parameter family

The source says same architecture, different parameters.

Therefore parameterization belongs naturally in:

\[
\theta_H
=
(\theta_{templates},
\theta_{priority},
\theta_{pruning},
\theta_{arbitration},
\theta_M,
\theta_E,
\theta_{identity}).
\]

These parameters affect local transitions.

They are not observable from Lace by type alone.

### 3.5 Layer H-E — Six-Act diagnostic overlay

The Six-Act stack asks descriptive questions over an event/interaction.

Pass 2 models it as a readout over a runtime trace:

\[
\mathsf{Diag}_H:
\mathsf{Trace}_H
\to
\mathsf{Acts}_{I..VI}.
\]

This may be realized inside S9 as an identity-state transition:

\[
\Delta_H(q,o,g)
=
(q,\mathsf{Diag}_H(\cdot)).
\]

Therefore:

\[
\text{Six-Act diagnostic}
\neq
\text{S0--S7 runtime state machine}.
\]

### 3.6 Layer H-F — YAML

The YAML template is a representation/configuration example.

It belongs to a serialization map:

\[
\mathsf{Encode}_H:
(\theta_H,\mathsf{Trace}_H)
\to
\mathsf{YAML}.
\]

It is not source evidence that \(\theta_H\) or trace state must literally be stored in YAML.

---

## 4. HCC evidence-preservation obligation

HCC makes one stronger demand than generic pointability.

The same source packet feeds both RIC and PFC, and only structure is transmissible.

For any Lace-grounded source packet:

\[
o=(v,\nu),
\]

let:

\[
\nu_L
=
\mathsf{LacePart}(\nu).
\]

A future HCC device must preserve a replayable evidence branch \(r_t\) whose Lace support is not replaced by prior-conditioned interpretation.

Candidate invariant:

\[
\boxed{
\mathsf{ReplayableBasis}(r_t,\nu_L)
}
\]

and for any HCC output \(y\) that claims Lace-grounded support:

\[
\boxed{
\mathsf{support}_L(y)
\subseteq
\nu_L
\quad\text{and}\quad
\mathsf{support}_L(y)\neq\varnothing.
}
\]

This is weaker and more precise than requiring every interpretation to carry the entirety of \(\nu_L\).

### HCC anti-mind-reading corollary

If:

\[
z\in
\{L_{story},M,E,ISL,\theta_H\},
\]

then a claim about another holder's \(z\) must not be stamped SEE-L unless independent evidence establishes that exact quantity.

At most, a future device may emit a model/inference with:
- explicit assumptions;
- external/local provenance;
- uncertainty.

---

# PART B — COFFEE CUP SHAPE

## 5. The five roles are not a one-hot state machine

The source's strategic requirement is to hold all five stages at once.

Therefore a faithful descriptive shape cannot be only:

\[
\chi_t
\in
\{
Conditions,
Release,
Trajectory,
Impact,
Aftermath
\}.
\]

That one-hot variable may be useful for "where is the process now?" but is insufficient as the whole model.

### Candidate causal tableau

For an event \(e\), define a descriptive tableau:

\[
\mathcal C(e,t)
=
(C_e,R_e,T_e(t),I_e,A_e(t)).
\]

These coordinates are heterogeneous.

- \(C_e\): enabling-condition field;
- \(R_e\): initiating boundary/event;
- \(T_e(t)\): trajectory/control description at relevant time \(t\);
- \(I_e\): realized impact/outcome when resolved;
- \(A_e(t)\): aftermath state/effect at later time.

This is a **device lift**, not a source-supplied equation.

---

## 6. Coffee Cup role typing

### Conditions

A set/field of enabling constraints:

\[
C_e
=
\{c_1,\ldots,c_k\}
\]

where membership does not imply causation of the final outcome.

### Release

A boundary/event:

\[
R_e
\]

which changes the system from pre-initiation possibility to active process.

It is not equivalent to Impact.

### Trajectory

A time/context dependent control description:

\[
T_e(t)
=
(X_t,\mathcal A_t,\mathcal M_t),
\]

where:
- \(X_t\) is current observed/context state;
- \(\mathcal A_t\) is the admitted intervention/action set;
- \(\mathcal M_t\) is the model used to evaluate consequences/mitigation.

The source only requires the intervention window to shrink mechanically over time.

A future device may model that as:

\[
\mathcal A_{t'}
\subseteq
\mathcal A_t
\quad
(t\le t')
\]

for a chosen event/model.

Pass 2 does not assert this monotonicity across every possible real-world system without a device-specific model.

### Impact

A resolved outcome claim:

\[
I_e
=
(\text{outcome},w_I).
\]

Its witness \(w_I\) must establish actual outcome evidence.

"Latest Lace row" is not sufficient by definition.

### Aftermath

A later transform on local/system state:

\[
A_e:
(q,g)\mapsto(q',g')
\]

that may alter future thresholds/conditions.

It does not modify \(I_e\)'s historical witness.

---

## 7. Counterfactual provenance discipline

Trajectory contains claims of the form:

> action \(a\) could still alter outcome \(O\).

These are not pure historical observations.

Define a counterfactual claim record:

\[
\mathsf{CFClaim}
=
(X,w_X,\mathcal A,\mathcal M,\varphi),
\]

where:
- \(X\) is observed state;
- \(w_X\) is its witness;
- \(\mathcal A\) is the admitted intervention/action set;
- \(\mathcal M\) is the model/assumption set;
- \(\varphi\) is the resulting counterfactual proposition.

A valid counterfactual witness must expose:

\[
w_{CF}
=
w_X
\cup
\mathsf{ExternalAssumptions}
\cup
\mathsf{ModelAssumptions}.
\]

It may not collapse to:

\[
w_{CF}=\mathsf{Lace}(i)
\]

merely because Lace supplied some historical input.

### Consequence

The Coffee Cup device will need to be **model-explicit** about intervention windows.

Pass 2 does not choose that model.

---

## 8. Coffee Cup strategic literacy

The source's strategic-literacy requirement is naturally expressed as availability of a simultaneous description:

\[
\mathsf{View}_C(e,t)
=
(C_e,R_e,T_e(t),I_e,A_e(t)).
\]

Some coordinates may be:
- unresolved;
- not yet applicable;
- partially known;
- counterfactual.

That is acceptable.

"Hold all five" does not mean hallucinate unknown future Impact.

It means preserve the five causal roles without collapsing the event into one privileged moment.

---

# PART C — WATER SHAPE

## 9. Water must retain three mathematical objects

A faithful Water device must preserve:

\[
\mathcal W
=
(\mathcal P,\mathcal R,\mathcal B).
\]

Where:

- \(\mathcal P\): Proof Ledger;
- \(\mathcal R\): River Proofing;
- \(\mathcal B\): Delta Bridge interface.

This is a descriptive factorization.

The source itself says they are two systems with an interface.

---

## 10. Proof Ledger factor

For holder set \(H\), define local inventories:

\[
O_h
=
\{\text{holder }h\text{'s admitted OBS}\}.
\]

OBS is pointable.

But:

\[
OBS
\neq
\mathsf{POINTER}.
\]

A source-specific OBS object later needs at least:
- holder identity;
- pointable claim;
- witness metadata once the source-open pointer format is ruled.

### DELTA

A DELTA requires paired observation material:

\[
\Delta_{h,k}
=
\mathsf{Compare}(o_h,o_k),
\]

with:

\[
o_h\in O_h,
\qquad
o_k\in O_k.
\]

Sharedness is therefore relational, not implied by both holders reading the same Lace.

### UNK

An UNK is an explicit dependency:

\[
u
=
(\text{missing evidence requirement},
\text{affected DELTA(s)}).
\]

Pass 2 keeps the source's operating rule open:
- halt at any UNK;
- bounded UNK budget;
- other.

### INVALID

INVALID is a routing classification for non-computable/unanchored input.

The rigorous classifier remains source-open.

---

## 11. River factor

The River system has its own native location/topology states:

\[
\mathcal R
\in
\{
Banks,
Rapids,
Delta/Crossing
\}
\]

at the plain-language grain.

Its invariants include:

\[
Pressure\neq Transfer.
\]

and:

\[
Crossing
\Rightarrow
Delta\ conditions.
\]

This state depends on:
- holder relation;
- live pressure/context;
- shared footing.

It is not equivalent to a Proof Ledger node.

---

## 12. Delta Bridge factor

The Bridge translates:

\[
\mathcal B:
(\mathcal P,\mathcal R,G,H)
\to
(\text{shared interface state},\text{routing output}).
\]

At the Specs grain it recognizes:

\[
S0,S1,S2,S3,S4
\]

and functions:

\[
Locate,
Anchor,
Pair,
Unknowns,
Reject.
\]

The Bridge is therefore not merely another variable inside Proof Ledger or River.

It is the interop relation that lets the two native systems remain native.

### Important source-layer distinction

The plain-language middle layer names four sayable shared states.

The Specs layer names S0–S4 including Bank Rebuild.

Pass 2 preserves both source surfaces rather than silently rewriting one into the other.

---

## 13. Holder-indexing discipline

Water requires separate local inventories.

HCC independently requires local meaning/non-transfer.

Define a multi-holder descriptive state only where needed:

\[
Q
=
Q_{shared}
\times
\prod_{h\in H} Q_h.
\]

Each holder may have:

\[
G_h,
\quad
Q_h,
\quad
\omega_h.
\]

All may read one common Lace prefix:

\[
L_n.
\]

This does not create:

\[
L_h.
\]

### Shared-state admission rule

A proposition may enter \(Q_{shared}\) only by an explicit source/device rule that establishes sharedness.

For Water:
- one holder's OBS does not become shared merely because it cites Lace;
- pairing/acknowledgement is still required where the source requires it.

For HCC:
- shared structural evidence does not transfer Meaning/Emotion/Identity.

---

## 14. Evidence-preservation theorem candidate EP-1

Pass 2 refines the plan's proposed invariant.

### Problem with the original candidate

The planned:

\[
\mathsf{basis}(z)\supseteq\nu_L
\]

is too strong.

An output may legitimately rely on only part of a larger input provenance set.

### EP-1 — support traceability

For a provenance-bearing observation:

\[
o=(v,\nu),
\]

and derived output \(y\), if the device claims a Lace-grounded proposition \(\varphi_y\), then there must exist a finite Lace support:

\[
S_L(y)
\subseteq
\mathsf{LacePart}(\nu)
\]

such that:
- \(S_L(y)\neq\varnothing\);
- the cited Lace claim can be replayed/checked from \(S_L(y)\) and the declared read rule;
- any additional dependence on local/external/counterfactual state remains separately tagged.

Formally:

\[
\boxed{
\mathsf{ClaimsLace}(y)
\Rightarrow
\exists S_L(y)\neq\varnothing:
S_L(y)\subseteq\mathsf{LacePart}(\nu)
}
\]

plus provenance separation:

\[
w(y)
=
S_L(y)
\uplus
S_G(y)
\uplus
S_Q(y)
\uplus
S_H(y)
\uplus
S_{CF}(y)
\uplus
S_U(y).
\]

This is a bookkeeping/direct-sum notation for evidence classes, not a storage partition.

### Stronger HCC RIC rule

When a future HCC device claims to preserve the raw structural path, it must keep a replayable representation of the structural observation independent of PFC interpretation.

That is source-specific and stronger than generic EP-1.

---

## 15. Description versus transition verdict

A descriptive reference may produce readout without changing local state.

S9 already supports:

\[
\Delta(q,o,g)=(q,y).
\]

Therefore:
- HCC Six-Act diagnostic readout;
- Cup strategic tableau;
- Water Locate/readout

can be represented without forced local mutation.

**Verdict: S9 CHASSIS SUFFICIENT.**

No generic \(\lambda\) field is reintroduced.

---

## 16. Cross-source recurrence discipline

The old Shadow model used:

\[
After
\equiv
Rebuild
\equiv
ActVI.
\]

Pass 2 rejects that as identity.

The valid commonality is only:

\[
\mathsf{NextStartEffect}(x)
\]

for three different source mechanisms:

\[
Aftermath
\to
\text{later Conditions},
\]

\[
Water\ S4
\to
\text{later interaction state},
\]

\[
HCC\ ActVI
\to
\text{later templates/priors}.
\]

Thus:

\[
\boxed{
\text{same feedback shape}
\neq
\text{same state}.
}
\]

No cross-source product state is built.

---

## 17. Source-open preservation

Pass 2 leaves Water's source-open degrees untouched:

\[
OBS\ pointer\ format=[OPEN],
\]

\[
INVALID\ matcher=[OPEN],
\]

\[
UNK\ operating\ rule=[OPEN].
\]

It also does not invent:
- HCC hidden-state inference rules;
- Coffee Cup intervention model;
- Coffee Cup universal stage detector;
- cross-source coupling rules.

---

## 18. Descriptive composition verdict

### HCC-A

**MAPPED WITHOUT LAYER COLLAPSE.**

Minimum shape:

\[
\text{parallel fork}
+
\text{recurrent compiler}
+
\text{parameter family}
+
\text{diagnostic overlay}
+
\text{representation layer}.
\]

### Coffee Cup

**MAPPED WITHOUT ONE-HOT COLLAPSE.**

Minimum descriptive shape is a heterogeneous simultaneous causal tableau with explicit counterfactual/control provenance.

### Water

**MAPPED WITHOUT SYSTEM FUSION.**

Minimum shape:

\[
ProofLedger
+
River
+
DeltaBridge
+
holder-indexed\ state.
\]

### Generic chassis

**S9 CHASSIS SUFFICIENT WITH SOURCE-SPECIFIC FACTORIZATION.**

No minimal amendment to the runtime tuple is required.

---

## 19. Stop

This composition map prepares source-specific device work.

It does not create:
- \(D_H\);
- \(D_C\);
- \(D_W\);
- H×C×W runtime;
- RM-A;
- Core types;
- implementation code.

A bounded S10 may now record the observability/composition court if the falsifier board holds.
