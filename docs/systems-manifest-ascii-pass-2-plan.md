# Systems-manifest ASCII pass 2 plan

**Status:** EXECUTED on `docs/systems-manifest-ascii.md`. Emission `[GAP]`.  
**Station:** law  
**Authority:** `docs/systems-manifest.md`  
**Companion:** `docs/systems-manifest-ascii.md` (pass 1 spine exists)

---

## What pass 1 flattened

The spine has I / II / III and the operator. It does **not** still letter:

- Pieces 1–15 as a board (kind + one source job)
- Four interfaces the source actually gives (write / identity / participation / view)
- Two-layer presentation table (1D store vs 1D view vs 2D vs 3D vs N-D)
- Relational mapping: one shared state, one control flow
- Open-questions list as labeled `[GAP]`s already in the manifest (not new answers)

Pass 2 hangs those on the existing spine. It does not redraw the Core box. It does not import H×C×W.

---

## Tickets

### T1 — Piece board

One line per piece. Kind + job. Box membership already on pass 1.

| # | Job (from manifest) |
|---|---|
| 1 | one strand; Rule Zero |
| 2 | one operator; two sentences |
| 3 | arrival order / punch-card |
| 4 | WORD section |
| 5 | POINTER section |
| 6 | 1D array is the lace stored |
| 7 | star = persistent word identity as passes |
| 8 | sequence as physical route |
| 9 | dictionary = wiring (use of I) |
| 10 | document feed (use of I) |
| 11 | touch / later participation |
| 12 | dimension = independent axis of reading I |
| 13 | projection ≠ lace |
| 14 | no stored meaning / no collapse |
| 15 | star-internal traffic (still the same wire) |

### T2 — Interface four

Write, identity, participation, view. Explicitly: no query / delete / transaction / multi-Lace.

### T3 — Presentation ladder

1D array / 1D sequence view / 2D paper / 3D star-thread / N-D families — each with “must not become.”

### T4 — Shared state / control flow

One line: shared state = L. Control = arrive → append → participate → continue. No second clock inside I.

### T5 — Sealed

G2. Train oil. `src/`.

---

## Execution (when you say go pass 2)

Edit only `docs/systems-manifest-ascii.md`. Pass 2 sections. STOP.

---

## Steward test

If a piece line invents a type not in the manifest, delete it.  
If H oil enters the piece board, delete it.
