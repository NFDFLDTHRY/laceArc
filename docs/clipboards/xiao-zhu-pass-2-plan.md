# Xiao & Zhu pass 2 — leftovers / failure modes / coherence (PDF present)

**Status:** EXECUTED — 2026-09-18 ~19:13 PT · agent `reference-Mapper`. Emission still `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Tong Xiao & Jingbo Zhu, *Foundations of Large Language Models*  
**arXiv:** `2501.09223v2` [cs.CL] · 15/17 Jun 2025  
**License:** CC BY-NC 4.0 — cite; do not fold body into Apache Core  
**Clipboard under test:** `docs/clipboards/xiao-zhu-foundations-clipboard.md`  
**Ancestor:** [`xiao-zhu-pass-1-pdf-plan.md`](xiao-zhu-pass-1-pdf-plan.md) (EXECUTED · PDF-sighted A–F, X)  
**Sibling (read-only unless T5 opens):** `docs/clipboards/clipboard-five-stakes.md` PAGE 3/5  
**Hands / map:** shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §3 (X1–X7)  

**PDF (this machine):**

| Field | Value |
|---|---|
| Working link (gitignored) | `refs/local/xiao-zhu-foundations-llm.pdf` |
| Attachment SHA | `0f5d145d298a90bbe1648b365d97b8833e86be3c31bedd7db900a932ba50bc1a` |
| Pages | **277** · text layer yes · printed ≈ PDF − 7 (Pass 1 certified) |
| Git | **Never `git add` the PDF** |

Law: arrival order kin only · intern / MASK / embed / next-token ≠ Core · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add)
        → do NOT redo A–X fill or invent emission
        → Pass 2 deltas only: D1–D6 (+ optional D7)
        → receipt on book clipboard
        → check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 (PDF) | **Admission + locus bridges** — A–F, X stamped against this attachment |
| **2** | **Leftovers** — what the clipboard is not; KEEP/FORBID strip; failure modes with PDF pins; interface coherence; source gate cite; sealed |

Same shape as Petersen / AgentScope / Rowlands pass 2 (D1–D6), now that the PDF is already present.

Still **Shadow / Layer III**. Not Core. Not `src/`. Not other shelves. This book remains the **anti-model of ingest**.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–F, X morals | Filled; Pass 1 CONFIRM/REFINE/SILENT stamps |
| PDF gate | Present; 277 pp.; text layer; offset certified; gitignored; SHA matches |
| Per-page KEEP/FORBID | Already on A–E (and X cross-cut) |
| Five-stakes PAGE 3/5 | Agrees; T6 skipped in Pass 1 |
| reason-model-map §3 X1–X7 | Untouched (stand) |

Pass 2 does **not** re-walk every page unless a delta hunt needs a fresh pin.

---

## What is still flat / missing

1. **No “what this clipboard is not” strip** — readers can mistake it for Core tokenizer law, emission rules, or permission to import LLM ingest into Lace.  
2. **No compact ON-SHOE / KEEP / FORBID / MAP strip** mirroring five-stakes PAGE 3/5 on the book clipboard itself (per-page cards exist; spine card does not).  
3. **No numbered failure-mode table (FM-X\*)** with PDF pins (false friends are narrative only on F/X).  
4. **No interface-coherence paragraph** stating which PAGE outputs may touch Hands inputs (X1 weak read-only kin; nothing writes Graphic D).  
5. **Source gate** is in Pass 1 receipt — Pass 2 should **cite** it, not claim PDF absent.  
6. **Optional:** one Shadow constructive miniature (e.g. next-token sample step or MASK recipe) stamped FORBID-as-Core-write / SILENT for Lace append.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Soften X1 KEEP or X2/X4/X5/X3/X7 FORBIDs | Pass 1 confirmed |
| Invent a Lace tokenizer to “close” PAGE F gap | opening ≠ filling |
| Fold CC BY-NC body into Apache Core | license |
| Amend reason-model-map / law / graphics | wrong stations |
| Rewrite five-stakes PAGE 3 morals | default skip |
| Add `src/` or edit other reference shelves | wrong Shadow shelf |
| Git-add PDF · push without ask | standing rules |

---

## Pass 2 product (deltas D1–D6)

Edit **only** `docs/clipboards/xiao-zhu-foundations-clipboard.md` unless T5 opens five-stakes.

### D1 — What this clipboard is not

Short sealed list:

- Not Core / not a sixth graphic  
- Not `pointer-emission.md` / not a Φ fill  
- Not a rewrite of reason-model-map §3  
- Not a Lace tokenizer specification (PAGE F opens a gap; does not close it)  
- Not permission to treat vocab ids as WORD identity  
- Not permission to treat next-token / MASK / embeds as Core ops  
- Not license to paste CC BY-NC body into Apache tree  

### D2 — ON-SHOE / KEEP / FORBID / MAP strip

Mirror five-stakes PAGE 3/5 on the book clipboard (compact card). Must match Pass 1 morals:

| KEEP | FORBID |
|---|---|
| arrival order (X1) — weak kin; dies at intern | intern token as identity (X2) |
| | [MASK] / replace / delete as Lace write (X4) |
| | embed / position store as Core (X5) |
| | next-token / causal LM as Core continue (X3, X7) |

Map arrows: X1 ──weak kin──► Piece 3 · X2 ──[X]──► Piece 4/7 · X3/X7 ──[X]──► Piece 2 · X4 ──[X]──► Piece 3 · emission `[GAP]`.

### D3 — Failure modes (FM-X1…X9)

Table: ID · Misread · Hands deadbolt · PDF pin (Pass 1 reuse or fresh `pdftotext`).

| ID | Misread seed |
|---|---|
| FM-X1 | token sequence order ⇒ Lace WORD identity (collapse before intern dies) |
| FM-X2 | shared vocab id ⇒ same Lace occurrence / star node |
| FM-X3 | next-token / argmax ⇒ Core continue / ingest operator |
| FM-X4 | [MASK] / replace / delete ⇒ Lace cut or authorized rewrite |
| FM-X5 | token+pos(+seg) embed table ⇒ Core store / second world |
| FM-X6 | soft prompt / RLHF / alignment ⇒ Core write (steward-only at best) |
| FM-X7 | decoding / prefilling / KV-cache ⇒ POINTER when-rule or Φ fill |
| FM-X8 | “tokenizer” discussion ⇒ Lace tokenizer delivered (close the gap) |
| FM-X9 | corpus / dataset ⇒ DOCUMENT append / punch-card Lace fuel |

Each row needs a PDF/printed pin (reuse Pass 1 bridges where valid).

### D4 — Interface coherence

One short paragraph: only X1 (arrival order) is kin to Hands inputs, and only as a **weak read** that dies when tokens are interned ids. X2–X5, X3/X7 outputs do not match any Core write. Soft prompts / alignment (X6) stay steward-side if used at all. Nothing in the book supplies POINTER when/arity/adjacency or a Lace tokenizer — `[GAP]` remains. Prediction ↛ putting cord through a hole.

### D5 — Source gate (cite Pass 1)

Do **not** narrate PDF-absent. Cite Pass 1 receipt: PDF present, 277 pp., text layer, printed≈PDF−7, path `refs/local/xiao-zhu-foundations-llm.pdf`, SHA `0f5d145d…ba50bc1a`, never git-add.

### D6 — Sealed

Repeat sealed list: invent Φ · invent Lace tokenizer · amend map/law/graphics · `src/` · git-add PDF · paste CC BY-NC into Core · treat next-token/MASK/embeds as Lace Core · pretend OCR without the file.

### Optional D7 — Constructive miniature (Shadow only)

If time: one numbered paper algorithm for (a) causal next-token train/sample step or (b) BERT MASK recipe (§1.2–1.3), stamped **FORBID-as-Core-write** and **SILENT for Lace append**. Not a Graphic D scheduler. Mark choice points `[OPEN]`. Skip entirely rather than invent steps the book does not state.

---

## Tickets

### T0 — Claim / base

Held at plan by `reference-Mapper`. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → reason-model-map §3 → Pass 1 clipboard → five-stakes PAGE 3/5.

### T2 — PDF gate

Confirm `refs/local/xiao-zhu-foundations-llm.pdf` resolves; `git check-ignore`; abort if PDF staged.

### T3 — Write D1–D2–D4–D5–D6

Prose deltas; no A–X moral rewrite. Prefer append after Pass 1 receipt.

### T4 — FM-X\* hunt (D3)

Reuse Pass 1 pins; `pdftotext` only where a row lacks a pin. ≤2-line eye-quote per new pin.

### T5 — Five-stakes (default skip)

Only if human asks: one receipt line “book clipboard Pass 2 deltas landed”; do not rewrite PAGE 3 KEEP/FORBID.

### T6 — Optional D7 miniature

Default **include if ≤30 min**; else defer with note under Pass 2 receipt.

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: clipboard D1–D6 (+ D7 if any) + this plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Pass 2 receipt on the Xiao–Zhu clipboard.  
2. D1–D6 present; D5 says PDF **present** (not absent).  
3. FM-X1…X9 each have Hands deadbolt + PDF/printed pin (or `[OPEN]` with cause).  
4. No stamp invents POINTER when/arity/adjacency or a Lace tokenizer.  
5. X1 KEEP and X2/X4/X5/X3/X7 FORBID not softened.  
6. reason-model-map / law / graphics / systems-manifest untouched; PDF never staged; CC BY-NC not pasted into Core.  
7. If D7 present: miniature is Shadow-only with FORBID-as-Core-write.  
8. Steward test: shoe in hands — refuse “intern vocab id as Lace WORD” using only D2+D3.

---

## Execution (when you say go pass 2)

1. Refresh claim.  
2. T1–T4 (+ T6 if time; T5 only if asked).  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
Order of holes can look like arrival — until the atom is interned.  
A vocab id is not two wraps of the same word.  
Next-token prediction is not putting cord through a hole.  
MASK/replace is not a Lace edit.  
Opening a tokenizer gap is not filling Φ.  
Φ stays `[GAP]`.

---

## Execution receipt

| Field | Value |
|---|---|
| Status | EXECUTED |
| When | 2026-09-18 ~19:13 PT |
| Agent | `reference-Mapper` |
| Product | D1–D6 + D7 (BERT MASK miniature) on `xiao-zhu-foundations-clipboard.md` |
| T5 five-stakes | skip |
| FM-X1…X9 | all pinned; no `[OPEN]` |
| Amended map/law/graphics/src | No |
| PDF staged | No |
| Emission | `[GAP]` |
