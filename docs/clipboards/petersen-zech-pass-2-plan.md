# Petersen & Zech pass 2 — leftovers / failure modes / coherence (PDF present)

**Status:** PLANNED — not executed. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Philipp Petersen & Jakob Zech, *Mathematical theory of deep learning*  
**arXiv:** `2407.18384v4` [cs.LG] · cover 16 Jan 2026  
**Clipboard under test:** `docs/clipboards/petersen-zech-clipboard.md`  
**Ancestor:** [`petersen-zech-pass-1-pdf-plan.md`](petersen-zech-pass-1-pdf-plan.md) (EXECUTED · PDF-sighted A–F, X)  
**Sibling (read-only unless T5 opens):** `docs/clipboards/clipboard-five-stakes.md` PAGE 4/5  
**Hands / map:** shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §4 (P1–P7)  

**PDF (this machine):**

| Field | Value |
|---|---|
| Working link (gitignored) | `refs/local/petersen-zech-mathematical-theory-deep-learning.pdf` |
| Attachment SHA | `a8505f3de280cb1c6f9742161471fadc52c21340afc05efe4549bd58c84383fa` |
| Pages | **333** · text layer yes · printed ≈ PDF − 1 at cited loci |
| Git | **Never `git add` the PDF** |

Law: function ≠ construction · star ≠ neuron · train-w ≠ Lace history · GNN/NTK ↛ Core store · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add)
        → do NOT redo A–X fill or invent emission
        → Pass 2 deltas only: D1–D6 (+ optional D7)
        → receipt on book clipboard
        → check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 (PDF) | **Admission + locus bridges** — A–F, X stamped against this attachment |
| **2** | **Leftovers** — what the clipboard is not; KEEP/FORBID strip; failure modes with PDF pins; interface coherence; source gate cite; sealed |

Same shape as AgentScope / Rowlands / Kauffman pass 2 (D1–D6), now that the PDF is already present.

Still **Shadow / Layer III**. Not Core. Not `src/`. Not other shelves.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–F, X morals | Filled; Pass 1 CONFIRM/REFINE/SILENT stamps |
| PDF gate | Present; 333 pp.; text layer; gitignored; SHA matches |
| Per-page KEEP/FORBID | Already on A–E (and X cross-cut) |
| Five-stakes PAGE 4/5 | Agrees; T5 skipped in Pass 1 |
| reason-model-map §4 P1–P7 | Untouched (stand) |

Pass 2 does **not** re-walk every page unless a delta hunt needs a fresh pin.

---

## What is still flat / missing

1. **No “what this clipboard is not” strip** — readers can mistake it for Core / emission law / sixth graphic / license to replace Lace with nets.  
2. **No compact ON-SHOE / KEEP / FORBID / MAP strip** mirroring five-stakes PAGE 4/5 on the book clipboard itself (per-page cards exist; spine card does not).  
3. **No numbered failure-mode table (FM-P\*)** with PDF pins (false friends are narrative only on F/X).  
4. **No interface-coherence paragraph** stating which PAGE outputs may touch Hands inputs (read-beside only; nothing writes Graphic D).  
5. **Source gate** is in Pass 1 receipt — Pass 2 should **cite** it, not claim PDF absent.  
6. **Optional:** one Shadow constructive miniature (e.g. GD step or NTK linearization) stamped FORBID-as-Core-write / SILENT for Lace append.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Soften P5 KEEP or P1/P4/P6/P7 FORBIDs | Pass 1 confirmed |
| Import nets / NTK / GNN as Core schema | off-shoe |
| Amend reason-model-map / law / graphics | wrong stations |
| Rewrite five-stakes PAGE 4 morals | default skip; optional one-line receipt only if T5 opens |
| Add `src/` or edit other reference shelves | wrong Shadow shelf |
| Git-add PDF · push without ask | standing rules |

---

## Pass 2 product (deltas D1–D6)

Edit **only** `docs/clipboards/petersen-zech-clipboard.md` unless T5 opens five-stakes.

### D1 — What this clipboard is not

Short sealed list:

- Not Core / not a sixth graphic  
- Not `pointer-emission.md` / not a Φ fill  
- Not a rewrite of reason-model-map §4  
- Not permission to replace Lace history with trained weights  
- Not permission to treat Φ-sameness as Lace identity  
- Not an import of GNN/NTK/architecture graphs into Graphic D  

### D2 — ON-SHOE / KEEP / FORBID / MAP strip

Mirror five-stakes PAGE 4/5 on the book clipboard (compact card). Must match Pass 1 morals:

| KEEP | FORBID |
|---|---|
| function ≠ construction (P5 / Remark 2.2) | star = hidden unit (P1) |
| | train w as history (P4) |
| | GNN as Core (P6) |
| | NTK as store (P7) |

Map arrows: P5 → Piece 13 / projection · P1 ──[X]──► Piece 7 · P4 ──[X]──► Piece 6 · P7 ──[X]──► Piece 6 (second world) · emission `[GAP]`.

### D3 — Failure modes (FM-P1…P9)

Table: ID · Misread · Hands deadbolt · PDF pin (Pass 1 reuse or fresh `pdftotext`).

| ID | Misread seed |
|---|---|
| FM-P1 | neuron / hidden unit ⇒ Lace star |
| FM-P2 | architecture graph ⇒ Graphic D / POINTER edges |
| FM-P3 | universal approximation ⇒ Lace identity / possession |
| FM-P4 | train / GD / backprop ⇒ rewrite Lace history |
| FM-P5 | same Φ ⇒ same Lace (collapse construction) |
| FM-P6 | GNN / message-passing ⇒ Core touch (from omission or analogy) |
| FM-P7 | NTK / infinite width ⇒ second store / participation axis |
| FM-P8 | depth / width hyperparameters ⇒ Lace dimension / rooms |
| FM-P9 | interpolation / reconstruction theorems ⇒ POINTER when-rule / Φ fill |

Each row needs a PDF/printed pin (reuse Pass 1 bridges where valid).

### D4 — Interface coherence

One short paragraph: only P5 (function≠construction) is kin to Hands inputs as a **negation/read** (projection ≠ store). P1–P4, P6–P7 outputs do not match any Core write. Nothing in the book supplies POINTER when/arity/adjacency — `[GAP]` remains. Approximation ↛ strand operation.

### D5 — Source gate (cite Pass 1)

Do **not** narrate PDF-absent. Cite Pass 1 receipt: PDF present, 333 pp., text layer, printed≈PDF−1, path `refs/local/petersen-zech-mathematical-theory-deep-learning.pdf`, SHA `a8505f3d…c84383fa`, never git-add.

### D6 — Sealed

Repeat sealed list: invent Φ · amend map/law/graphics · `src/` · git-add PDF · treat nets/NTK as Lace Core · pretend OCR without the file.

### Optional D7 — Constructive miniature (Shadow only)

If time: one numbered paper algorithm for a GD/SGD step (§10.1–10.2) **or** NTK linearization (§11.3), stamped **FORBID-as-Core-write** and **SILENT for Lace append**. Not a Graphic D scheduler. Mark choice points `[OPEN]`. Skip entirely rather than invent steps the book does not state.

---

## Tickets

### T0 — Claim / base

Held at plan by `reference-Mapper`. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → reason-model-map §4 → Pass 1 clipboard → five-stakes PAGE 4/5.

### T2 — PDF gate

Confirm `refs/local/petersen-zech-mathematical-theory-deep-learning.pdf` resolves; `git check-ignore`; abort if PDF staged.

### T3 — Write D1–D2–D4–D5–D6

Prose deltas; no A–X moral rewrite. Prefer append after Pass 1 receipt.

### T4 — FM-P\* hunt (D3)

Reuse Pass 1 pins; `pdftotext` only where a row lacks a pin. ≤2-line eye-quote per new pin.

### T5 — Five-stakes (default skip)

Only if human asks: one receipt line “book clipboard Pass 2 deltas landed”; do not rewrite PAGE 4 KEEP/FORBID.

### T6 — Optional D7 miniature

Default **include if ≤30 min**; else defer with note under Pass 2 receipt.

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: clipboard D1–D6 (+ D7 if any) + this plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Pass 2 receipt on the Petersen–Zech clipboard.  
2. D1–D6 present; D5 says PDF **present** (not absent).  
3. FM-P1…P9 each have Hands deadbolt + PDF/printed pin (or `[OPEN]` with cause).  
4. No stamp invents POINTER when/arity/adjacency.  
5. P5 KEEP and P1/P4/P6/P7 FORBID not softened.  
6. reason-model-map / law / graphics / systems-manifest untouched; PDF never staged.  
7. If D7 present: miniature is Shadow-only with FORBID-as-Core-write.  
8. Steward test: shoe in hands — refuse “replace Lace history with trained w” using only D2+D3.

---

## Execution (when you say go pass 2)

1. Refresh claim.  
2. T1–T4 (+ T6 if time; T5 only if asked).  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
A net that matches a readout is not the lace.  
Training updates parameters — not occurrence history.  
Stars are accumulated passes — not neurons.  
NTK is analysis — not a second store.  
Φ stays `[GAP]`.
