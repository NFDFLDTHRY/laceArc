# Xiao & Zhu pass 1 (PDF-sighted) — clipboard vs attached *Foundations of Large Language Models*

**Status:** EXECUTED — 2026-09-18 ~19:06 PT. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Tong Xiao & Jingbo Zhu, *Foundations of Large Language Models*  
**arXiv:** `2501.09223v2` [cs.CL] (15 Jun 2025 / book date 17 Jun 2025)  
**License:** CC BY-NC 4.0 — cite; do not fold body text into Apache Core  
**Clipboard under test:** `docs/clipboards/xiao-zhu-foundations-clipboard.md`  
**Sibling (read-only unless T5 opens):** `docs/clipboards/clipboard-five-stakes.md` PAGE 3/5  
**Hands / map:** shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §3 (X1–X7)  

**PDF (this machine):**

| Field | Value |
|---|---|
| Attachment | `0f5d145d298a90bbe1648b365d97b8833e86be3c31bedd7db900a932ba50bc1a.pdf` (≈2.6 MB) |
| Working link (gitignored) | `refs/local/xiao-zhu-foundations-llm.pdf` |
| Pages | **277** (`pdfinfo`) |
| Text layer | **Present** |
| Printed ↔ PDF | printed \(p\) ≈ PDF \(p−7\) (ch.1 starts PDF 8 = printed p.1) — **CERTIFIED** at execute |
| SHA-256 | `0f5d145d298a90bbe1648b365d97b8833e86be3c31bedd7db900a932ba50bc1a` |
| Git | **Never `git add` the PDF** |

This is **not** a greenfield invent. A Xiao–Zhu clipboard and five-stakes PAGE 3/5 already exist.  
Pass 1 **sights this attachment** and verify-and-tightens the book clipboard against it (same job class as Petersen / AgentScope / Rowlands PDF pass 1).

Law: arrival order kin only · intern / MASK / embed / next-token ≠ Core · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add)
        → for each clipboard PAGE A–F, X: find locus on THIS PDF
        → stamp CONFIRM / REFINE / CONFLICT / SILENT vs map X1–X7
        → patch clipboard receipt + loci / eye-quotes only as needed
        → check · commit · release · STOP
```

---

## Spot-check already done at plan time (not full execute)

| PDF p. | Content (`pdftotext`) | vs prior clipboard |
|---|---|---|
| 1–2 | Title / Xiao & Zhu / arXiv:2501.09223v2 / Jun 2025 / CC BY-NC | Identity **CONFIRM** |
| 9 | Eq 1.1 indexed input sequence \(x_0,\ldots,x_m\) | five-stakes / map X1 seed **CONFIRM** |
| 14 | Decoder-only “predicts … given preceding tokens” | PAGE A seed **CONFIRM** |
| 30 | BERT mask / replace discussion region | PAGE B/D seed **CONFIRM** |

Full A–X walk **DONE** at execute. Clipboard header said 277 pp. — matches `pdfinfo`.

---

## What already exists (do not pretend blank)

| Artifact | State |
|---|---|
| `xiao-zhu-foundations-clipboard.md` | PAGE A–F, X filled; KEEP arrival order; FORBID intern/MASK/embed/next-token as Core |
| five-stakes PAGE 3/5 | Agrees; KEEP X1; FORBID X2–X5,X7 as Core |
| reason-model-map §3 X1–X7 | Filled |
| This attachment | Present; linked under `refs/local/` (gitignored) |

Pass 1 product is **verify-and-tighten**, not a second mythology.

---

## Pass 1 (PDF) product

Edit **only** `docs/clipboards/xiao-zhu-foundations-clipboard.md` unless T5 opens five-stakes:

1. **New receipt block** — `Pass 1 (PDF-sighted)` with attachment id / `refs/local/xiao-zhu-foundations-llm.pdf`, 277 pp., text layer yes, offset certificate, gitignored, SHA.  
2. **Per-page locus bridges** — for A–F, X: § / printed p. **and** PDF one-based page from **this** file. Method: `pdftotext` (primary).  
3. **Stamp each page** vs reason-model-map X1–X7: CONFIRM / REFINE / CONFLICT / SILENT.  
4. **Re-verify** Eq 1.1 / decoder-only prefix LM / vocab+position embeds / MASK-replace / prompting / alignment / Ch.5 inference against PAGE morals.  
5. **PAGE F / X** — confirm still **no** POINTER when/arity/adjacency; refresh false friends with PDF pins if needed.  
6. Emission stays `[GAP]`. No `src/`. No PDF in git. Do not invent Φ.  
7. **License note** — CC BY-NC body stays out of Apache Core (cite only).

Out of scope: rewriting reason-model-map, law, graphics; five-stakes rewrite (optional one-line “PDF re-sighted” only if T5 opens); other shelves; treating tokenizer/vocab/next-token as Lace Core.

---

## Tickets

### T0 — Claim / base

Held at plan by `reference-Mapper`. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → reason-model-map §3 → current Xiao–Zhu clipboard → five-stakes PAGE 3/5.

### T2 — PDF gate

Confirm `refs/local/xiao-zhu-foundations-llm.pdf` resolves to attachment `0f5d145d…ba50bc1a`. Confirm `git check-ignore`. Abort if a PDF is staged.

### T3 — Offset certificate

Confirm printed ↔ PDF relation (header claim: printed ≈ PDF − 7). Record in Pass 1 receipt.

### T4 — Page walk (core)

| Clipboard page | Seed (prior clipboard) | Must verify |
|---|---|---|
| A Token sequence / arrival order | §1.2.1 decoder-only · PDF ~14 | KEEP arrival order kin; FORBID vocab atoms as WORD identity (X1) |
| B Interned vocabulary token | §1.3 / ch.2.1 · PDF ~30, 44–45 | FORBID intern id as Lace occurrence identity (X2) |
| C MASK / replace / corrupt | BERT §1.3 · PDF ~30 | FORBID MASK/replace as Lace cut (X4) |
| D Embed / position store | §2.1.1 · PDF ~45 | FORBID embed store as Core (X5) |
| E Next-token / generative objective | ch.2 generative · PDF ~45+ | FORBID next-token as Core continue (X3/X7) |
| F When-to-point / false friends | prompting/alignment/inference lookalikes | still silent on Φ; list false friends with pins |
| X Cross-cut | derived | temptations vs Hands; tokenizer `[GAP]`; Φ `[GAP]` |

### T5 — Scope honesty (required)

Any retained equation numbers / “opens tokenizer gap” claims must pin exact PDF pages; mark `[OPEN]` if a prior paraphrase overstates the book. Do not invent a Lace tokenizer.

### T6 — Five-stakes (optional)

Default **skip**. If PAGE 3/5 eye-quotes disagree with this PDF, add one receipt line only — do not rewrite PAGE 3 KEEP/FORBID.

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: xiao-zhu clipboard PDF-sighted receipt + locus stamps + this plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Pass 1 (PDF-sighted) receipt present on the Xiao–Zhu clipboard.  
2. Every PAGE A–F, X has PDF pin(s) from **this** attachment (or explicit `[OPEN]` with cause).  
3. X1–X7 placement unchanged in spirit: arrival-order KEEP-read (weak kin); intern/MASK/embed/next-token FORBID as Core; Φ `[GAP]`.  
4. No card invents POINTER when/arity/adjacency or a Lace tokenizer.  
5. PDF never staged; systems-manifest / law / graphics / reason-model-map untouched (report OPEN only).  
6. Steward test: re-find Eq 1.1 (PDF ~9) and decoder-only prefix LM (PDF ~14) with `pdftotext -f N` on the linked PDF.

---

## Execution receipt

**EXECUTED** 2026-09-18 ~19:06 PT · station clipboards · `reference-Mapper`.

| Ticket | Result |
|---|---|
| T0 Claim / base | REFRESHED clipboards; local pass-1 plan commit kept (ahead of origin) |
| T1 Hands skim | shoe → graphics A–D → manifest → law → reason-model-map §3 X1–X7 → clipboard → five-stakes PAGE 3/5 |
| T2 PDF gate | `refs/local/xiao-zhu-foundations-llm.pdf` → `0f5d145d…ba50bc1a`; SHA match; 277 pp.; text layer; `git check-ignore` OK |
| T3 Offset | **printed ≈ PDF − 7** CERTIFIED (ch.1 PDF 8 = printed 1; pairs in clipboard receipt) |
| T4 Page walk | A–F, X all PDF-pinned; stamps CONFIRM×6 · REFINE×1 (B) · SILENT×1 (F) · CONFLICT×0 |
| T5 Scope | PAGE B eye-quote refined to book wording; no Lace tokenizer / Φ invent; CC BY-NC cite-only |
| T6 Five-stakes | **skip** — PAGE 3/5 pins agree (no eye-quote conflict) |
| T7 Commit / release | pending this commit |

Amended: clipboard + this plan only. Emission `[GAP]`. PDF never staged.

---

## Steward test

Shoe in hands, or no.  
Order of holes can look like arrival — until the atom is interned.  
A vocab id is not two wraps of the same word.  
Next-token prediction is not putting cord through a hole.  
MASK/replace is not a Lace edit.  
Φ stays `[GAP]`.
