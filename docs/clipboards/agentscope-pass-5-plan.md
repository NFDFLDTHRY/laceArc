# AgentScope pass 5 — ASCII as order proposal + accuracy steward

**Status:** PLANNED — not executed. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Paper:** Jiayi Bi et al., *Diagnosing with Insights…* · arXiv `2609.02371v1` · **AGENTSCOPE**  
**ASCII (Pass 4):** `docs/clipboards/agentscope-ascii-machinery.md` — SOURCE MACHINERY + P-A1…P-A9  
**Mechanisms (Pass 3):** `docs/clipboards/agentscope-mechanisms.md` — M-A1…M-A9  
**Clipboard:** `docs/clipboards/agentscope-clipboard.md`  
**Sibling (read-only default):** five-stakes PAGE 5/5 · reason-model-map §5 · `systems-manifest-ascii.md` (Core — **do not conflate**)  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → map §5 → Wave A cards + ASCII → Pass 1–4 receipts  

**PDF (gitignored):** `refs/local/bi-et-al-agentscope-diagnosing-insights.pdf` · **never `git add`**

Law: ASCII SOURCE MACHINERY is a **Shadow proposal** for shelf/steward order · not Core · not Φ · ReAG ↛ 1D array · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → treat Pass 4 SOURCE MACHINERY as ORDER PROPOSAL (Shadow)
        → P5-X accuracy matrix every M-A* ↔ P-A* ↔ overview node
        → P5-F fix drift only where matrix requires
        → P5-O one canonical SOURCE MACHINERY (if duplicates/drift)
        → P5-P write Order Proposal section (proposal stamps; FORBID-as-Core)
        → P5-R receipt · plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1–2 | Admit + leftover fences |
| 3 | Wave A mechanism cards |
| 4 | ASCII panels of those mechanisms |
| **5** | **Steward pass:** (1) keep M ↔ P ↔ overview **in order** (accuracy); (2) adopt the ASCII diagram **as a proposal** for how this shelf (and steward diagnosis of repo work) stays ordered — **without** promoting it to Core or Φ |

Sibling shelves used pass 5 for Wave B and pass 6 for accuracy.  
This shelf’s ask is **pass 5 with the ASCII as an order proposal**, so Pass 5 **combines** accuracy stewardship with an explicit **Shadow order proposal** derived from SOURCE MACHINERY.

Still **Shadow / Layer III**. Not Core. Not `src/`. Not a rewrite of `systems-manifest-ascii.md`.

---

## What “ASCII as a proposal to keep the repo in order” means

The Pass 4 SOURCE MACHINERY board is treated as a **proposal**, not a law:

| Pipeline stage (ASCII) | Proposal for shelf / steward order |
|---|---|
| Trajectory / logs | Observe what is actually on the shelf (files, receipts, claim state) before rewriting |
| Instrumented steps | Name artifacts and passes in recorded order; do not invent arrival order |
| ReAG + ISR | Build a **diagnostic picture** of relations (clipboard ↔ mechanisms ↔ ASCII ↔ map); **FORBID** as a second store beside Graphic D |
| Neural / stamp checks | Run KEEP/FORBID / FM-A* / P3-M teeth as steward checks — not Core proofs |
| Candidate failures + evidence | List `[OPEN]` / drift / CONFLICT with evidence; do not auto-blame |
| Decisive-error judgment | Choose commit / release / defer / stop — **not** POINTER when / Φ fill |

**Stamps on the proposal itself:**

- **KEEP-read** as steward hygiene for the AgentScope shelf  
- **FORBID-as-Core-write** — must not become array rewrite, second store, or emission rule  
- **FORBID-ReAG-in-array** retained  
- **SILENT-for-Lace-append** — proposal does not schedule Lace append  
- **PROPOSAL** — optional; Hands may refuse without breach of Core  

This is how the reference’s diagnosis machinery **proposes** repo/shelf order. It does not own Lace.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| M-A1…M-A9 | Pass 3 — algorithms stand |
| P-A1…P-A9 + overview | Pass 4 — diagrams stand |
| P3-M mesh | CONFIRM×9 |
| PAGE A–F, X · D1–D6 · FM-A* | Stand |

Pass 5 does **not** invent Wave B unless accuracy is green **and** a ticket explicitly opens it (default: **skip** Wave B).

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Promote order proposal to Core / AGENTS.md law | Shadow only; Hands may refuse |
| Soften FORBID-ReAG-in-array or neural≠proof | Pass 3–4 confirmed |
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Merge into `systems-manifest-ascii.md` | Core board stays separate |
| Rewrite M-A* algorithms or PAGE morals for style | only P5-F drift fixes with matrix row cite |
| Full Wave B–D | default skip |
| Git-add PDF · push without ask · invent `src/` | standing rules |

---

## Pass 5 products

### P5-X — Accuracy matrix (required)

On clipboard **or** at top of ascii-machinery (prefer clipboard Pass 5 section + pointer from ascii file).

Columns: `node (overview)` · `P-A*` · `M-A*` · `stamp` · `Implements OK` · `Fence OK` · `Overview OK` · `Pin OK` · `PASS/FIX`

Rows: all **M-A1…M-A9 / P-A1…P-A9** (9) plus overview spine nodes.

Scoring: every cell Y → **PASS**; else **FIX** with exact edit target.

### P5-F — Fix drift (required if any FIX)

Only matrix-cited edits to ascii panels / overview / cross-walk (and mechanisms only if Implements text truly drifted — prefer fixing the panel). No moral softening.

### P5-O — Canonical SOURCE MACHINERY (required)

Exactly **one** canonical overview fence in `agentscope-ascii-machinery.md`. Remove or demote duplicates. Overview must match P5-X PASS rows and keep Core/Φ off-board.

### P5-P — Order Proposal (required · the distinctive ask)

Add a section (clipboard and/or ascii file; prefer **both**: full text on clipboard under Pass 5, short mirror under ascii overview):

**Title:** `Order proposal (Shadow) — SOURCE MACHINERY as shelf hygiene`

Must include:

1. Statement that this is a **proposal**, not Core / not AGENTS.md amendment.  
2. Stage→hygiene table (trajectory → … → decisive judgment) as above.  
3. Recommended **shelf file order** for readers (proposal only), e.g.:

   `clipboard (admit) → mechanisms (algorithms) → ascii-machinery (diagrams) → pass plans (history)`

4. Recommended **steward loop** when touching this shelf: observe → name → diagram-check → stamp-check → list OPENs → decide commit/release/stop.  
5. Explicit **FORBID**: using this loop as Φ scheduler, ReAG-in-array, or Hands override.  
6. Link to P5-X (accuracy is how the proposal stays honest).

### P5-R — Receipt

`## Pass 5 receipt` on book clipboard: matrix verdict, fixes landed, order-proposal locus, Wave B skipped (default), Φ still `[GAP]`.

### Optional P5-B — Wave B

**Default skip.** Only if P5-X all PASS and human asks in-execute. Otherwise one-line “Wave B still deferred.”

---

## Tickets

### T0 — Claim / base

Held at plan. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §5 → M-A* → P-A* → overview → Pass 1–4 receipts → five-stakes PAGE 5/5.

### T2 — PDF gate

Confirm refs/local; never stage PDF. Re-sight only if a FIX needs a pin.

### T3 — Build P5-X matrix

### T4 — P5-F fixes + P5-O canonical overview

### T5 — Write P5-P Order Proposal (clipboard + ascii mirror)

### T6 — P5-R receipt · mark plan EXECUTED

### T7 — Check / commit / release

```
bash docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: matrix + fixes + order proposal + receipt + plan EXECUTED
bash docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. P5-X covers all M-A1…A9 / P-A1…A9 (+ overview); each row PASS or fixed to PASS.  
2. Exactly one canonical SOURCE MACHINERY overview; Core/Φ off-board.  
3. P5-P Order Proposal present, stamped proposal/Shadow, FORBID-as-Core-write + FORBID-ReAG-in-array + SILENT-for-Lace-append explicit.  
4. Stage→hygiene table and shelf file-order proposal present.  
5. No stamp invents POINTER when/arity/adjacency; Φ still `[GAP]`.  
6. reason-model-map / law / graphics / systems-manifest / Core ASCII / `src/` untouched except clipboard + ascii (+ mechanisms only if P5-F requires).  
7. PDF never staged.  
8. Steward test: shoe in hands — can you refuse “make the ASCII board Graphic D” and still follow the order proposal as hygiene?

---

## Execution (when you say go pass 5)

1. Refresh claim.  
2. T1–T6 (T5 Order Proposal is mandatory; Wave B skip).  
3. Mark plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
The ASCII board **proposes** how to keep this shelf honest — observe, name, check, decide.  
It does not become the lace.  
ReAG stays a photograph.  
Decisive judgment is release/stop — not Φ.  
`[GAP]` remains.
