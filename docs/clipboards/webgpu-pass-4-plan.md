# WebGPU CRD 2026-09-15 — Pass 4 PLAN (HOST/GPU ASCII source-machinery / Wave A P-G* + P4-X)

**Status:** **HELD / NOT EXECUTED** · plan turn 2026-09-18 ~21:03 PT  
**Station:** clipboards (HELD `webgpu Pass 4 PLAN (HOST/GPU ASCII machinery HELD)` · claim BASE `74881b1` = `origin/main` at claim)  
**Tip at plan:** `c604ee6` (local Pass 1–3 tip; **ahead of origin**; also **behind** unrelated gearing/RESYNC commits — rebase before execute)  
**Spec:** *WebGPU* — **W3C Candidate Recommendation Draft, 15 September 2026** · editors Kai Ninomiya, Brandon Jones, Jim Blandy (et al.)  
**Clipboard under test:** `docs/clipboards/webgpu-clipboard.md` (Pass 1–3 EXECUTED · PAGE A–H, X · D1–D6 + FM-Wgpu1…9 + D7 · **P3-M CONFIRM×9 · CONFLICT×0**)  
**Mechanisms (done):** `docs/clipboards/webgpu-mechanisms.md` — Wave A **M-G1…M-G9** (algorithms stand; do not rewrite)  
**New artifact (execute only):** `docs/clipboards/webgpu-ascii-machinery.md` — **do not create at plan time**  
**Ancestors:** [`webgpu-pass-1-plan.md`](webgpu-pass-1-plan.md) · [`webgpu-pass-2-plan.md`](webgpu-pass-2-plan.md) · [`webgpu-pass-3-plan.md`](webgpu-pass-3-plan.md)  
**Templates mirrored (adapt HOST/GPU; don’t clone ISA or LLM morals):** [`xiao-zhu-pass-4-plan.md`](xiao-zhu-pass-4-plan.md) · [`agentscope-pass-4-plan.md`](agentscope-pass-4-plan.md) · [`petersen-zech-pass-4-plan.md`](petersen-zech-pass-4-plan.md) · [`wasm-spec-3-pass-4-plan.md`](wasm-spec-3-pass-4-plan.md) (rigor / P4-X spirit only — **not** fidelity-steward-of-already-drawn; this shelf’s ASCII is **first draw**)  
**ASCII schema samples (read-only):** [`agentscope-ascii-machinery.md`](agentscope-ascii-machinery.md) · [`wasm-spec-3-ascii-machinery.md`](wasm-spec-3-ascii-machinery.md) (opening contract + one panel)  
**Mesh (read-only / cite-only):** [`pointer-emission.md`](../pointer-emission.md) = **DRAFT / `[GAP]`** · [`clock/README.md`](../clock/README.md) (`lace-projection.html` WebGPU 3D) · [`clock/wasm/README.md`](../clock/wasm/README.md) (different layer) · [`hologram/`](../hologram/README.md) · [`hologram/visual-primitives.md`](../hologram/visual-primitives.md) (WGSL live path) · wasm-spec-3 Pass 1–6 (**wasm64** — cite-only; different layer)  
**Sibling (default skip):** `docs/clipboards/clipboard-five-stakes.md` — not this shelf’s anti-model PAGE  
**Hands / law:** shoe → graphics A–D → systems-manifest → staking / law-why → AGENTS.md Implementation Law  
**Kebab strand:** stick to `webgpu-*` for all products of this shelf.

**PDF (this machine):**

| Field | Value |
|---|---|
| Working link (gitignored) | `refs/local/webgpu-crd-20260915.pdf` |
| Attachment SHA-256 | `6bc31a9e21074a039a5c854f1bfc33bdbf282cd8b48e22a4977ca406ca64afe1` |
| Pages | **321** · text layer yes · PDF one-based primary (Pass 1–3 certified) |
| Git | **Never `git add` the PDF** |

Law: WebGPU = **HOST/GPU Shadow only** · GPUBuffer ≠ strand · GPUDevice ≠ Core/Graphic D · `queue.submit`/draw/dispatch ≠ Φ · texture/bind-group ≠ second store · WGSL ≠ WORD · canvas ≠ accept emission · WebGPU ≠ wasm64 compile target · validation/device-lost ≠ Lace cut · `mapAsync`/CPU readback ≠ append authority · Hands win · `[GAP]` sealed · **representation correctness of CRD procedures in ASCII > new art**.

```
pull/ff (or rebase onto origin/main) → claim clipboards → Hands + host/GPU mesh skim
        → open PDF (refs/local; no git add) light only if panel needs locus pin
        → do NOT rewrite M-G* algorithms (cite them)
        → forge SOURCE MACHINERY overview (API-side only; Core/Φ off-board)
        → for each P-G*: ASCII panel implementing M-G* steps + matching stamp under fence
        → P4-X rigor matrix (fidelity · stamp · false-friend fence · no POINTER in fences)
        → cross-walk M↔P · Pass 4 receipt on clipboard
        → mark plan EXECUTED · check · commit · release · STOP
```

---

## SPECIAL BANNER — HOST/GPU API reference · Shadow · not Core · not Lace store · not emission

```
+======================================================================+
|  HOST / GPU API REFERENCE — WebGPU W3C CRD 2026-09-15                |
|  Pass 4 = ASCII SOURCE MACHINERY + Wave A panels P-G1…P-G9 + P4-X.   |
|  NOT Xiao–Zhu / Petersen / AgentScope morals (those are anti-model). |
|  NOT wasm-spec-3 compile-target ISA panels (P-T* / M-T* / FM-W* stay).|
|  NOT Lace Core.  NOT Graphic D.  NOT pointer-emission fill.          |
|  FORBID: GPUDevice/Queue/Buffer/Texture as Lace strand or store.     |
|  FORBID: queue.submit / draw / dispatch as POINTER emission / Phi.   |
|  FORBID: WGSL as WORD · canvas as emission accept · WebGPU as wasm64.|
|  FORBID: validation/device-lost as Lace cut · mapAsync as append.    |
|  KEEP-read: host/GPU mechanics for Shadow projection / hologram.     |
|  ASCII fences = Spec/API-side glyphs ONLY; NO Lace POINTER word      |
|  inside fences; stamps live UNDER fences.                            |
|  No src/ until pointer-emission accepted.  One strand.               |
|  Ascii file = execute only.  Status this commit: HELD / NOT EXECUTED.|
+======================================================================+
```

This is **not** another anti-model LLM/theory ASCII pass.  
This is **not** wasm-spec-3 **compile-target ISA** ASCII (P-T\* / M-T\* / FM-W\* live on that shelf).  
It is **host/GPU Web API hygiene in diagrams**: CRD **constructive procedures** drawn as SOURCE MACHINERY overview + **P-G1…P-G9** 1:1 with **M-G1…M-G9**, with hard FORBID stamps under fences, plus **P4-X** rigor that each panel implements its card without smuggling Lace tokens into fences.

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 (PDF) | **Admission** — PAGE A–H, X stamped against CRD 2026-09-15 |
| 2 | **Leftovers** — D1–D6 + FM-Wgpu1…9 + D7 encoder→submit miniature |
| 3 | **System mechanisms** — M-G1…M-G9 + P3-M mesh (CONFIRM×9) |
| **4** | **Rigorous analysis + ASCII map** of those mechanisms — one SOURCE MACHINERY board + panels **P-G1…P-G9** 1:1 with **M-G\***, cross-walk, **P4-X** rigor matrix |

Sibling shelves (Xiao–Zhu / Petersen / AgentScope): Pass 4 drew ASCII of Wave A.  
wasm-spec-3 Pass 4 was fidelity-steward of **already-drawn** ascii — **different shape**; this shelf’s ascii is still **absent** (Pass 3 deferred it). Mirror **Xiao–Zhu / AgentScope Pass 4** product shape + wasm-spec-3 **P4-X rigor spirit**, retargeted to **HOST/GPU**.

Still **Shadow / Layer III**. Not Core. Not `src/`. **No Wave B.** **No Order Proposal** (Pass 5). Ascii file created **only at execute**.

---

## What “rigorous ASCII of system mechanisms” means here

Each panel is a **diagram of the CRD’s host/GPU procedure**, not of Lace:

1. Cites its **M-G\*** card and implements that card’s numbered algorithm steps (reverse-walkable)  
2. Uses **API/spec-side glyphs only inside the fence** (`navigator.gpu`, `GPUAdapter`, `GPUDevice`, `GPUQueue`, `GPUBuffer`, `mapAsync`, `GPUTexture`, `GPUBindGroup`, WGSL `code`, `createCommandEncoder`, `finish`, `queue.submit`, `configure`, `device.lost`, …)  
3. Carries the **same Lace stamp** as the card (hard FORBIDs carried **under** the fence)  
4. Does **not** place Lace `POINTER` / `WORD` / Graphic D / Φ / strand rows **inside** the fence as if they were WebGPU objects — **FORBID the word POINTER inside diagram fences**  
5. Does **not** close Φ / invent emission / swap wasm64 / birth `src/`  
6. Survives **P4-X**: Implements OK · Fence OK (API-side; no POINTER word) · Stamp OK · False-friend fence OK · Overview attach OK · PASS/FIX

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–H, X | Filled; Pass 1 KEEP-read-as-host-API / FORBID-* / PROPOSAL-only / SILENT stamps stand |
| D1–D6 + FM-Wgpu1…Wgpu9 + D7 | Pass 2 stands (D7 encoder→submit Shadow miniature; cross-link from M-G7/P-G7) |
| M-G1…M-G9 + P3-M | Pass 3 EXECUTED · CONFIRM×9 · CONFLICT×0 — **algorithms stand** |
| Five-stakes | **Out of scope** (default skip) |
| Ascii Wave A / `webgpu-ascii-machinery.md` | **Absent** — create at **execute only** |
| wasm-spec-3 products | Untouched (cite-only; Pass 1–6 EXECUTED · wasm64 seal) |
| pointer-emission.md | Still DRAFT / `[GAP]` — untouched |
| clock / hologram | Shadow surfaces citing WebGPU — not amended |
| `src/` / Cargo | Absent; stay absent |
| Order Proposal | **Not this pass** (Pass 5 if wanted) |

Pass 4 **diagrams** Wave A; it does not rewrite card morals, reopen Pass 2 leftovers wholesale, invent Wave B panels, or amend wasm-spec-3 / emission / Core boards.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Soften Pass 1–3 FORBID-as-Lace-store / FORBID-as-Lace-strand / FORBID-as-emission-rule / PROPOSAL-only / FORBID-as-compile-target-swap / SILENT | Pass 1–3 confirmed |
| Promote GPUBuffer / GPUTexture / bind-group / Adapter / Device / Queue into Lace Core or store | off-shoe / wrong ontology |
| Treat `queue.submit` / draw / dispatch / encoder finish as POINTER emission | emission sealed |
| Treat WGSL as Lace WORD / Core operator | PAGE E · FM-Wgpu5 |
| Treat canvas configure / present as accepting `pointer-emission.md` | GAP remains · FM-Wgpu6 |
| Claim WebGPU replaces wasm64 / wasm-spec-3 compile target | different layer · FM-Wgpu7 |
| Treat validation / device-lost as Lace cut / append rule / Φ gate | SILENT · FM-Wgpu8 |
| Treat `mapAsync` / CPU readback as Lace append authority | FM-Wgpu9 |
| Put Lace `POINTER` word **inside** any diagram fence | Spec/API-side fences only; stamps under fences |
| Put Lace Core ASCII on this board | Core board = `systems-manifest-ascii.md` — do not conflate |
| Rewrite M-G* algorithm bodies (except cited typo/pin fix) | cards are algorithm source |
| Wave B full panels | deferred seeds only |
| Order Proposal (Pass 5 shape) | deferred — Pass 5 if wanted |
| Create `webgpu-ascii-machinery.md` at **plan** time | execute-only artifact |
| Amend wasm-spec-3 clipboard/ascii/mechanisms/pass plans | cite-only; wrong shelf |
| Amend rust-nostd maps / law / graphics / systems-manifest / five-stakes / clock / hologram | wrong stations / default skip |
| Add `src/`, `Cargo.toml`, or invent emission via host GPU story | Implementation Law |
| Git-add PDF · push without ask | standing rules |
| Clone Xiao–Zhu / Petersen / AgentScope LLM morals or wasm M-T* / FM-W* ISA morals onto this shelf | wrong false-friend class |
| Clone wasm-spec-3 Pass 4 “already-drawn fidelity steward” as if ascii already exists | this shelf’s ascii is **first draw** |
| Execute diagrams / P4-X / clipboard receipt in the **plan** commit | plan = HELD / NOT EXECUTED |

---

## Artifact — `webgpu-ascii-machinery.md` (execute only — **not created at plan time**)

### Opening contract (≤8 lines · required)

1. Models **WebGPU CRD host/GPU procedures**, not Lace Core.  
2. Companion to `webgpu-mechanisms.md` + `webgpu-clipboard.md`.  
3. Lace Core ASCII = `docs/systems-manifest-ascii.md` — **do not conflate**.  
4. Emission Φ = `[GAP]`. No panel closes it. Shadow / Layer III only.  
5. Inside fences: **API/spec-side glyphs only** — **no Lace POINTER / WORD / Graphic D as WebGPU objects**; **never the word `POINTER` inside diagram fences**.  
6. Stamps live **under** fences (not inside) and match Wave A M-G* teeth.  
7. KEEP-read host/GPU for Shadow projection / hologram; FORBID Buffer/Device as Lace store/strand; FORBID submit as emission; FORBID WGSL as WORD; FORBID WebGPU as wasm64.  
8. Hands win; steward refuses store/emission/WORD/wasm64 lookalikes.

### Stamp legend

Reuse Pass 3 legend from `webgpu-mechanisms.md` (KEEP-read-as-host-API, FORBID-as-Core-write, FORBID-as-Lace-store, FORBID-as-Lace-strand, FORBID-as-emission-rule, SILENT-for-Lace-append, PROPOSAL-only, FORBID-as-compile-target-swap, FORBID-as-Core, FORBID fill-GAP).

### Panel schema (mandatory · P-G* ↔ M-G*)

| Field | Required |
|---|---|
| Mechanism cite | M-G* + PDF locus (Pass 1–3 pin reuse or light `pdftotext`) |
| Sighting | method note |
| Lace stamp | **same tooth as M-G\*** (hard FORBIDs explicit under fence) |
| PAGE cite | C–H as per P3-M |
| False friend | FM-Wgpu* |
| Projection / hologram | cite-only (clock / hologram) |
| wasm64 | cite-only · ≠ this shelf (**FM-Wgpu7**) |
| Implements | which M-G* algorithm steps the diagram shows |
| Fenced ASCII | ≤76 cols; **API glyphs only** inside fence; **no POINTER word** in fence |
| Stamp line | **under** fence |

### SOURCE MACHINERY overview (required · API side only)

One top-level board of the CRD constructive spine matching **M-G1…M-G9** (exact art free; Core/Φ/wasm64 ISA off-board). Spine shape (plan seed — execute may refine glyphs, not morals):

```
  [navigator.gpu / requestAdapter] ------- M-G1 / P-G1   FORBID-as-Lace-strand
           |
           v
  [requestDevice · device.queue] --------- M-G2 / P-G2   FORBID-as-Lace-strand
           |
           +--> [createBuffer · mapAsync] ---- M-G3 / P-G3   FORBID-as-Lace-store
           |                                  FORBID map as append
           +--> [createTexture · createView] - M-G4 / P-G4   FORBID-as-Lace-store
           |
           v
  [bindGroupLayout · bindGroup] ---------- M-G5 / P-G5   FORBID-as-Lace-store
           |
           v
  [shaderModule + pipeline · WGSL] ------- M-G6 / P-G6   FORBID WGSL as WORD
           |
           v
  [commandEncoder · passes · finish] ----- M-G7 / P-G7   FORBID-as-emission-rule
           |                                  (D7 cross-link)
           v
  [queue.submit · canvas configure] ------ M-G8 / P-G8   FORBID submit as Phi
           |                                  canvas PROPOSAL-only · FORBID fill-GAP
           +--> [errors · device.lost] ------- M-G9 / P-G9   SILENT-for-Lace-append
           |
           x --> Lace Core / Graphic D / Phi / wasm64 ISA swap
                 FORBIDDEN off this board

  Lace Core OFF this board --> docs/systems-manifest-ascii.md
  Phi = [GAP]
  WebGPU = HOST/GPU Shadow only (not wasm64 compile target)
```

Core, Φ, Lace strand, and wasm64 ISA stay **off-board**.

### Wave A panels (blocking · 1:1 with cards · titles must match M-G*)

| Panel | Mechanism (must match M-G* title) | Stamp seed (carry hard FORBIDs under fence) |
|---|---|---|
| **P-G1** | M-G1 Adapter request (`navigator.gpu` → `requestAdapter`) | KEEP-read-as-host-API · **FORBID-as-Lace-strand** · FORBID-as-Core-write · FM-Wgpu2 |
| **P-G2** | M-G2 Device / Queue (`requestDevice` · `device.queue`) | KEEP-read-as-host-API · **FORBID-as-Lace-strand** · FORBID-as-Core-write · FM-Wgpu2 |
| **P-G3** | M-G3 Buffer alloc / map (`createBuffer` · `mapAsync` / `getMappedRange` / `unmap`) | KEEP-read · **FORBID-as-Lace-store** · **FORBID-as-emission-rule** (map/readback) · SILENT · FM-Wgpu1 · FM-Wgpu9 |
| **P-G4** | M-G4 Texture / view (create / view / usages) | KEEP-read · **FORBID-as-Lace-store** · FM-Wgpu4 |
| **P-G5** | M-G5 Bind group layout (`createBindGroupLayout` / `createBindGroup`) | KEEP-read · **FORBID-as-Lace-store** · FM-Wgpu4 |
| **P-G6** | M-G6 Render / Compute pipeline + WGSL boundary | KEEP-read · **FORBID-as-Core-write** (WGSL ≠ WORD; pipeline ≠ Core ops) · FM-Wgpu5 |
| **P-G7** | M-G7 Command encoder / passes (encode · compute/render pass · finish) | KEEP-read · **FORBID-as-emission-rule** · **FORBID-as-Core** · SILENT · FM-Wgpu3 · **D7** cross-link |
| **P-G8** | M-G8 Submit / present (`queue.submit` · canvas configure / `getCurrentTexture`) | KEEP-read submit · **FORBID-as-emission-rule** · canvas **PROPOSAL-only** · **FORBID fill-GAP** · FM-Wgpu3 · FM-Wgpu6 |
| **P-G9** | M-G9 Error / device-lost vs Lace append | KEEP-read · **SILENT-for-Lace-append** · FORBID-as-emission-rule · FM-Wgpu8 |

Hard fence rules (execute must enforce):

- **P-G1 / P-G2** fences: Adapter/Device/Queue are host handles — not Graphic D / lace cord; FORBID-as-Lace-strand explicit under fence; no POINTER word in fence.  
- **P-G3** fence: `GPUBuffer` / linear GPU memory / `mapAsync` are host transfer — **not** Lace strand / append; FORBID-as-Lace-store + map FORBID-as-emission-rule under fence.  
- **P-G4 / P-G5** fences: texture / bind-group are GPU resources — **not** second Lace store; FORBID-as-Lace-store under fence.  
- **P-G6** fence: WGSL `code` / pipelines are host shaders — **not** Lace WORD / Core ops; FORBID-as-Core-write under fence; no WORD/POINTER as if CRD objects inside fence.  
- **P-G7 / P-G8** fences: encode / draw / dispatch / `queue.submit` are GPU schedule — **not** Φ / POINTER when; stamps under fence; **no POINTER word inside fence**; P-G7 cites **D7**; P-G8 canvas path PROPOSAL-only · FORBID fill-GAP.  
- **P-G9** fence: validation / `device.lost` are host error — **not** Lace cut / Φ gate; SILENT under fence.  
- **Overview** fence: Core / Φ / wasm64 ISA swap marked FORBIDDEN off-board; emission `[GAP]`.

### P4-X — Rigor matrix (required · primary steward deliverable)

Prefer locus: **clipboard** Pass 4 section (+ one-line pointer from ascii). Columns:

`overview node · P-G* · M-G* · stamp · Implements OK · Fence OK (API-only; no POINTER word) · Stamp OK · False-friend fence OK · Overview attach OK · PASS/FIX`

**Rows (blocking):**

- All **M-G1…M-G9 / P-G1…P-G9** (9)  
- Overview spine nodes (adapter → device/queue → buffer/map → texture → bind → pipeline+WGSL → encoder → submit/present → error/lost · forbidden Core/Φ/wasm64 exit)

**Special gates (must not rubber-stamp):**

| Gate | Why |
|---|---|
| **P-G3 / M-G3** · **FM-Wgpu1 · FM-Wgpu9** | Buffer/map ≠ strand/append — highest store false-friend risk |
| **P-G1–P-G2 / M-G1–M-G2** · **FM-Wgpu2** | Adapter/Device/Queue ≠ Graphic D / lace cord |
| **P-G7–P-G8 / M-G7–M-G8** · **FM-Wgpu3 · FM-Wgpu6** | encode/submit ≠ Φ; canvas ≠ accept emission |
| **P-G6 / M-G6** · **FM-Wgpu5** | WGSL ≠ WORD |
| **P-G4–P-G5 / M-G4–M-G5** · **FM-Wgpu4** | texture/bind ≠ second store |
| **P-G9 / M-G9** · **FM-Wgpu8** | lost/validation ≠ Lace cut |
| **Overview wasm64 exit** · **FM-Wgpu7** | WebGPU ≠ compile-target swap |
| **No POINTER-in-fence** | every fence audited |

Scoring: every cell Y → **PASS**; else **FIX** with exact edit target (ascii fence / stamp line / caption / overview). Any FIX must be resolved in the execute pass. P3-M YES×9 is **baseline only** — Pass 4 re-walks Implements and fence hygiene.

#### Plan-time contested seeds (execute must score; do not fix in plan commit)

| Seed | Contested finding (plan skim) | Likely FIX locus |
|---|---|---|
| Ascii absent | No `webgpu-ascii-machinery.md` yet | create at execute (whole artifact) |
| Panel cite | Mechanisms still say “future P-G*” | mechanisms status one-liner + panel cites → P-G* |
| Fence POINTER | Risk of Lace tokens leaking into fences if steward copies stamp text inside | hard audit all 9 fences + overview |
| P-G7 / D7 | Must cross-link Pass 2 D7 without rewriting D7 | P-G7 caption / Implements |
| P-G8 canvas | PROPOSAL-only + FORBID fill-GAP must appear under fence, not as Core accept | P-G8 stamp line |
| Overview attach | Each P-G* must appear on singular SOURCE MACHINERY board | overview + P4-X Overview column |
| FM naming | Authority is **FM-Wgpu1…9** (not FM-T* / FM-A*) | panel false-friend rows |

These seeds are **not** executed fixes — they seed P4-X rows.

### Cross-walk table (required)

`P-G* | M-G* | stamp | PAGE | FM-Wgpu* | projection/hologram | wasm64 | emission`

Suggested (execute confirms from P3-M):

| P | M | PAGE | FM | Emission |
|---|---|---|---|---|
| P-G1 | M-G1 | C | FM-Wgpu2 | `[GAP]` |
| P-G2 | M-G2 | C | FM-Wgpu2 | `[GAP]` |
| P-G3 | M-G3 | D | FM-Wgpu1 · FM-Wgpu9 | `[GAP]` |
| P-G4 | M-G4 | D | FM-Wgpu4 | `[GAP]` |
| P-G5 | M-G5 | D | FM-Wgpu4 | `[GAP]` |
| P-G6 | M-G6 | E | FM-Wgpu5 | `[GAP]` |
| P-G7 | M-G7 | F | FM-Wgpu3 (+ D7) | `[GAP]` |
| P-G8 | M-G8 | F+G | FM-Wgpu3 · FM-Wgpu6 | `[GAP]` (FORBID fill) |
| P-G9 | M-G9 | H | FM-Wgpu8 | `[GAP]` |

### Waves B–D

One-line deferred seeds only (same as mechanisms file). **No** full panels this pass. **No** Pass 5 Order Proposal yet.

### ASCII standard

Monospace fences; ≤76 cols; reverse-walkable to M-G algorithm steps; stamp **under** fence; **API/spec labels only inside fence**; **no POINTER word inside any diagram fence**; Core/Φ/wasm64 ISA off overview board.

---

## Clipboard / mechanisms thin edits (execute only)

On `webgpu-clipboard.md`:

1. **Pass 4 receipt** — EXECUTED, path to ascii file, panel count P-G1…P-G9, P4-X verdict, emission `[GAP]`  
2. **P4-X matrix** (if not only in ascii)  
3. Optional one-liner under P3-M: “Pass 4 ASCII panels P-G1…P-G9 → webgpu-ascii-machinery.md”  
4. Do **not** rewrite PAGE morals, D1–D6, FM-Wgpu*, D7, or P3-M teeth  

On `webgpu-mechanisms.md`:

- Optional status one-liner — “Pass 4 ASCII: see webgpu-ascii-machinery.md (P-G1…P-G9). Algorithms unchanged.”  
- Optional panel-cite field updates: **future P-G\*** → **P-G\***  
- Do **not** rewrite algorithm bodies

---

## Tickets

### T0 — Claim / base

Held at plan (`webgpu Pass 4 PLAN …`). Before execute: reconcile **ahead/behind** with `origin/main` (local tip `c604ee6` was ahead of `74881b1` and behind unrelated gearing/RESYNC at plan time) · `git pull --ff-only` or rebase as steward chooses · refresh claim · agent id e.g. `webgpu Pass 4 EXECUTE`.

### T1 — Hands + host/GPU mesh skim

Shoe → graphics A–D → systems-manifest → staking / law-why → AGENTS Implementation Law → pointer-emission DRAFT/`[GAP]` → clock/README + lace-projection → clock/wasm (different layer) → hologram + visual-primitives (WGSL cite) → wasm-spec-3 banner (**cite-only**) → Pass 1–3 clipboard (A–H/X + FM-Wgpu* + D7 + P3-M) → M-G1…M-G9 → sibling ASCII sample (AgentScope / wasm-spec-3 opening + one panel).

### T2 — PDF gate

Confirm `refs/local/webgpu-crd-20260915.pdf` resolves; `git check-ignore`; abort if PDF staged. Light re-sight only if a panel needs a locus pin (§4 adapter/device · §5 buffer/map · §6 texture · §8 bind · §9 WGSL · §12–13 encoder · §19 submit · §21 canvas · §22 errors).

### T3 — Scaffold `webgpu-ascii-machinery.md`

Opening contract, HOST/GPU banner, stamp legend, overview placeholder, P-G1…P-G9 stubs, cross-walk stub, P4-X stub. **Create only at execute.**

### T4 — Draw SOURCE MACHINERY overview (API-side; Core/Φ/wasm64 off-board)

Spine: adapter → device/queue → buffer/map → texture → bind → pipeline+WGSL → encoder → submit/present → error/lost.

### T5 — Draw panels P-G1…P-G9 (Implements match M-G*; stamps match; hard FORBIDs under fences; no POINTER in fences)

### T6 — P4-X rigor matrix + fix any FIX rows (incl. false-friend fences + POINTER-in-fence audit)

### T7 — Cross-walk + Pass 4 receipt · thin mechanisms status · mark plan EXECUTED

### T8 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "webgpu Pass 4 EXECUTE"
# commit: webgpu-ascii-machinery.md + clipboard receipt/P4-X (+ mechanisms one-liner) + plan EXECUTED
# never stage refs/local/*.pdf
./docs/coord/coord.sh release clipboards "webgpu Pass 4 EXECUTE"
```

Ask before push. Do **not** push unless asked.

---

## Execute checklist (when steward says go)

1. Reconcile with `origin/main` · refresh clipboards claim.  
2. T1 Hands + host/GPU mesh skim (no Core edits).  
3. T2 PDF gate (no `git add` PDF).  
4. T3 create `docs/clipboards/webgpu-ascii-machinery.md` scaffold.  
5. T4 draw SOURCE MACHINERY overview.  
6. T5 draw **P-G1…P-G9** 1:1 with M-G* titles + stamps under fences.  
7. T6 P4-X matrix · fix FIX rows · audit no POINTER in fences.  
8. T7 cross-walk + clipboard Pass 4 receipt · mechanisms thin status.  
9. Mark this plan **EXECUTED**.  
10. `coord.sh check` · commit · release.  
11. **STOP.** Do not push. Do not invent Wave B. Do not write Order Proposal. Do not touch `src/` / Cargo / wasm-spec-3 / emission / law / graphics / clock / hologram / systems-manifest-ascii.

---

## Acceptance tests / success criteria (execute must pass)

1. `webgpu-ascii-machinery.md` exists with opening contract, HOST/GPU banner, stamp legend, SOURCE MACHINERY overview, **P-G1…P-G9**, cross-walk, P4-X.  
2. Each P-G* cites matching M-G*, implements that card’s steps, matching stamp **under** fence, ≤76-col fence, **API glyphs only** inside fence; **no POINTER word inside any diagram fence**.  
3. Panel titles 1:1 with M-G*: Adapter request · Device/Queue · Buffer alloc/map · Texture/view · Bind group layout · Pipeline+WGSL · Command encoder/passes · Submit/present · Error/device-lost.  
4. Hard stamps present under fences: P-G3 FORBID-as-Lace-store + map FORBID-as-emission-rule; P-G1/P-G2 FORBID-as-Lace-strand; P-G4/P-G5 FORBID-as-Lace-store; P-G6 FORBID-as-Core-write; P-G7/P-G8 FORBID-as-emission-rule; P-G8 canvas PROPOSAL-only · FORBID fill-GAP; P-G9 SILENT; P-G7 **D7** cross-link.  
5. Overview keeps Lace Core, Φ, and wasm64 ISA swap off-board; spine matches M-G1…M-G9.  
6. P4-X all PASS (or fixed to PASS this pass) including false-friend fence + no-POINTER-in-fence columns.  
7. Cross-walk M↔P present; P3-M CONFIRM×9 not reopened unless stamp-conflict noted.  
8. No stamp invents POINTER when/arity/adjacency; Φ / emission still `[GAP]`.  
9. M-G* algorithms not rewritten; Pass 1–2 morals / FM-Wgpu* / D7 stand; wasm-spec-3 / law / graphics / systems-manifest / five-stakes / clock / hologram / `src/` untouched.  
10. PDF never staged.  
11. Pass 4 receipt on webgpu clipboard.  
12. Steward refuse tests (below) pass using only panels + stamps + P3-M.

---

## Steward refuse tests

Shoe in hands, or no. With only P-G* + stamps (+ P3-M / M-G* cites), steward must refuse:

1. “GPUBuffer is the Lace strand” → **P-G3 / M-G3** + **FM-Wgpu1** + FORBID-as-Lace-store.  
2. “GPUDevice / Adapter is Graphic D / Core door” → **P-G1/P-G2** + **FM-Wgpu2** + FORBID-as-Lace-strand.  
3. “`queue.submit` emits POINTER” / “draw/dispatch fills Φ” → **P-G7/P-G8** + **FM-Wgpu3** + FORBID-as-emission-rule; emission `[GAP]`.  
4. “Texture / bind-group is a second Lace store” → **P-G4/P-G5** + **FM-Wgpu4**.  
5. “WGSL is Lace WORD” → **P-G6** + **FM-Wgpu5** + FORBID-as-Core-write.  
6. “Canvas configure accepts pointer-emission.md” → **P-G8** + **FM-Wgpu6** + PROPOSAL-only; GAP remains.  
7. “WebGPU replaces wasm64 / wasm-spec-3 compile target” → overview off-board + **FM-Wgpu7** + FORBID-as-compile-target-swap.  
8. “Validation / device-lost is a Lace cut / Φ gate” → **P-G9** + **FM-Wgpu8** + SILENT.  
9. “`mapAsync` / CPU readback is Lace append authority” → **P-G3** + **FM-Wgpu9** + FORBID-as-emission-rule.  
10. “Calling WebGPU / configuring canvas accepts pointer-emission.md” → **P-G8** + D1/D6; GAP remains.  
11. “This fence contains Lace POINTER as a WebGPU object” → opening contract · Fence OK fail · stamps belong **under** fences.

These panels are CRD **host/GPU** procedures drawn for Shadow projection / hologram / canvas embed surfaces.  
They do not write Graphic D, do not birth `src/`, do not fill Φ, and do not swap wasm64.

---

## Success criteria (plan turn — this commit)

1. This file exists at `docs/clipboards/webgpu-pass-4-plan.md`.  
2. Status **HELD / NOT EXECUTED**; HOST/GPU SPECIAL banner present.  
3. Opening contract requirements · SOURCE MACHINERY spine · **P-G1…P-G9** 1:1 with M-G* · cross-walk · P4-X · tickets · acceptance · steward refuse specified.  
4. **No** `webgpu-ascii-machinery.md` created; **no** clipboard / mechanisms / src product deltas in this commit (plan only + station claim/release).  
5. Clipboards station released after plan commit.  
6. No push. No `src/`. PDF unstaged. Emission `[GAP]`. wasm-spec-3 untouched.

---

## Steward test (standing)

Shoe in hands, or no.  
WebGPU is the **host GPU API** Shadow projection / hologram may call, not the lace.  
ASCII maps **CRD procedures**, not Core.  
Adapter/Device/Queue are not Graphic D. GPUBuffer/Texture/bind-group are not the strand or a second store.  
Encoder → submit / draw / dispatch do not emit POINTER.  
WGSL is not WORD. Canvas does not accept emission.  
Validation / device-lost is not a Lace cut. `mapAsync` is not append authority.  
WebGPU is not the wasm64 compile target.  
Fences stay API-side; **no POINTER word inside fences**; stamps under fences.  
Φ stays `[GAP]`. No Order Proposal until Pass 5. No `src/` until human-accepted emission.

---

## Plan receipt (this turn)

| Field | Value |
|---|---|
| Status | **HELD / NOT EXECUTED** |
| When | 2026-09-18 ~21:03 PT |
| Station | clipboards · agent `webgpu Pass 4 PLAN (HOST/GPU ASCII machinery HELD)` |
| Claim BASE | `74881b1` (= `origin/main` at claim) |
| Tip at plan | `c604ee6` (local; ahead of origin; behind unrelated gearing/RESYNC — rebase before execute) |
| Artifact created | this plan only |
| Ascii created | **No** (execute only: `webgpu-ascii-machinery.md`) |
| Mechanisms / clipboard product edits | **No** |
| Emission | still `[GAP]` |
| PDF staged | **No** |
| Push | **No** |
