# Xiao & Zhu pass 4 — rigorous ASCII map of Wave A system mechanisms

**Status:** **HELD** / **NOT EXECUTED**. Emission `[GAP]`. Tokenizer `[GAP]`.  
**Station:** clipboards (HELD `xiao-zhu-pass-4-plan` at plan; execute as `reference-Mapper` or equivalent)  
**Base at plan:** `6b5aa03` (Pass 3 tip after last push) · refresh to current `origin/main` before execute  
**Book:** Tong Xiao & Jingbo Zhu, *Foundations of Large Language Models*  
**arXiv:** `2501.09223v2` [cs.CL] · 15/17 Jun 2025  
**License:** CC BY-NC 4.0 — cite; do not fold body into Apache Core  
**Mechanisms (done):** `docs/clipboards/xiao-zhu-foundations-mechanisms.md` — Wave A M-A1…M-A9  
**Clipboard:** `docs/clipboards/xiao-zhu-foundations-clipboard.md` (Pass 1–3 EXECUTED; P3-M CONFIRM×9 · CONFLICT×0)  
**New artifact (execute only):** `docs/clipboards/xiao-zhu-foundations-ascii-machinery.md` — **do not create at plan time**  
**Sibling (read-only):** five-stakes PAGE 3/5 · reason-model-map §3 X1–X7  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → map §3 → mechanisms Wave A → Pass 3 clipboard  
**Ancestors:** [`xiao-zhu-pass-3-plan.md`](xiao-zhu-pass-3-plan.md) · Pass 1–2 plans  
**Mirrors:** [`agentscope-pass-4-plan.md`](agentscope-pass-4-plan.md) · [`petersen-zech-pass-4-plan.md`](petersen-zech-pass-4-plan.md)  
**ASCII schema sample:** [`agentscope-ascii-machinery.md`](agentscope-ascii-machinery.md) or [`petersen-zech-ascii-machinery.md`](petersen-zech-ascii-machinery.md) (opening contract, stamp legend, overview, one P-A* panel)

**PDF (gitignored):** `refs/local/xiao-zhu-foundations-llm.pdf` → attachment `0f5d145d298a90bbe1648b365d97b8833e86be3c31bedd7db900a932ba50bc1a` · **277** pp. · text layer · printed ≈ PDF − 7 · **never `git add`**

Law: arrival order kin only · intern / MASK / embed / next-token ≠ Core · soft/RLHF steward-only · decode/KV ≠ POINTER · Hands win · Φ `[GAP]` sealed · no Lace tokenizer invent.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add) light only if panel needs locus pin
        → rigorously re-read each M-A* algorithm (do NOT rewrite unless typo/pin)
        → forge SOURCE MACHINERY overview (book side only; Core/Φ off-board)
        → for each P-A*: ASCII panel implementing M-A* steps with matching stamp
        → P4-X implements/fence/overview rigor matrix
        → cross-walk P↔M↔stamp↔X#↔PAGE; Pass 4 receipt on clipboard
        → mark plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 | PDF-sighted PAGE A–F, X |
| 2 | Leftovers D1–D6 + FM-X1…X9 + D7 |
| 3 | Wave A **mechanism cards** M-A1…M-A9 + P3-M mesh |
| **4** | **Rigorous analysis + ASCII map** of those mechanisms — one SOURCE MACHINERY board + panels P-A1…P-A9 tied 1:1 to M-A*, with P4-X implements/fence rigor check |

Pass 3 deferred ASCII. Pass 4 delivers diagrams **and** a first rigor check that each panel actually implements its card. Still **Shadow / Layer III**. Not Core. Not `src/`. Not `systems-manifest-ascii.md`. This book remains the **anti-model of ingest**.

---

## What “rigorous ASCII of system mechanisms” means here

Each panel is a **diagram of the book’s procedure**, not of Lace:

1. Cites its M-A* card and implements that card’s numbered algorithm steps (reverse-walkable)  
2. Uses **book-side glyphs only inside the fence** (corpus \(D\), vocab \(V\), \(e=x+e_{pos}+e_{seg}\), MASK, soft prompt, SFT, RLHF/DPO, prefilling, KV, \(\arg\max Pr\), …)  
3. Carries the **same Lace stamp** as the card (hard FORBIDs carried into panels)  
4. Does **not** place `WORD` / `POINTER` / Graphic D rows inside the fence as if they were book objects — **FORBID the word POINTER inside diagram fences** (book labels only)  
5. Does **not** close Φ or invent a Lace tokenizer  
6. Survives P4-X: Implements OK · Fence OK · Stamp OK · Overview attach OK · false-friend fence OK

---

## Already done (do not redo)

| Layer | State |
|---|---|
| M-A1…M-A9 cards | Pass 3 EXECUTED — algorithms stand |
| P3-M mesh | CONFIRM×9 · REFINE×0 · CONFLICT×0 |
| PAGE A–F, X + Pass 2 D1–D6 / FM-X* / D7 | Stand |
| Five-stakes PAGE 3/5 | Agrees; default skip |
| reason-model-map §3 X1–X7 | Untouched |

Pass 4 **diagrams** Wave A; it does not rewrite card morals or invent Wave B panels (seeds only).

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Invent a Lace tokenizer (close PAGE F) | opening ≠ filling |
| Soften M-A2 FORBID-as-Lace-identity · M-A3 FORBID-as-store · M-A4/M-A9 next-token/decode FORBIDs · M-A5 MASK≠cut · M-A9 FORBID-as-POINTER-when | Pass 3 confirmed |
| Put Lace Core ASCII on this board | Core board = `systems-manifest-ascii.md` — do not conflate |
| Rewrite M-A* algorithm bodies (except cited typo/pin fix) | cards are algorithm source |
| Wave B–D full panels | deferred seeds only |
| Pass 5 order proposal | not this pass |
| Amend reason-model-map / law / graphics / systems-manifest | wrong stations |
| Rewrite five-stakes PAGE 3 KEEP/FORBID | default skip |
| Create `xiao-zhu-foundations-ascii-machinery.md` at plan time | execute only |
| `src/` or other-shelf mechanism edits | wrong Shadow shelf |
| Fold CC BY-NC body into Apache Core | license |
| Git-add PDF · push without ask | standing rules |

---

## Artifact — `xiao-zhu-foundations-ascii-machinery.md` (execute only)

### Opening contract (≤8 lines)

1. Models **Xiao–Zhu book procedures** (LLM pipeline), not Lace Core.  
2. Companion to `xiao-zhu-foundations-mechanisms.md` + book clipboard.  
3. Lace Core ASCII = `docs/systems-manifest-ascii.md` — **do not conflate**.  
4. Emission Φ = `[GAP]`. Tokenizer = `[GAP]`. No panel closes either.  
5. Stamps match Wave A cards (intern≠identity · MASK≠cut · embed≠store · next-token/decode≠Core continue · soft/SFT/RLHF≠Core rows · KV≠POINTER).  
6. **Book-side glyphs only inside fences**; never the word `POINTER` inside diagram fences.  
7. Shadow / Layer III only. Anti-model of ingest.

### Stamp legend

Reuse Pass 3 legend from mechanisms file (KEEP-read, FORBID-as-Core-write, FORBID-as-Lace-identity, FORBID-as-store, FORBID-as-Lace-history, FORBID-as-POINTER-when, FORBID next-token/decode as Core continue, FORBID corpus→DOCUMENT fuel, SILENT-for-Lace-append, FORBID as Core rows).

### Panel schema (mandatory · P-A* ↔ M-A*)

| Field | Required |
|---|---|
| Mechanism cite | M-A* + PDF locus |
| Sighting | `pdftotext` and/or Pass 1–3 pin reuse |
| Lace stamp | same tooth as M-A* (hard FORBIDs explicit under fence) |
| Map cite | X1–X7 |
| False friend | FM-X* |
| Implements | which M-A* algorithm steps the diagram shows |
| Fenced ASCII | ≤76 cols; **book glyphs only** inside fence; **no POINTER word** in fence |
| Stamp line | under fence |

### SOURCE MACHINERY overview (required · book side only)

One top-level board of the book’s constructive LLM pipeline spine matching M-A1…M-A9 (exact art free; Core/Φ/tokenizer off-board), e.g.:

```
  [corpus D + self-sup task] -------- M-A1 / P-A1   FORBID D as DOCUMENT
           |
           v
  [tokenize → shared vocab V] ---- M-A2 / P-A2   FORBID-as-Lace-identity
           |                          tokenizer [GAP]
           v
  [e = x + e_pos (+ e_seg)] --------- M-A3 / P-A3   FORBID-as-store
           |
           +--> [causal next-token train] -- M-A4 / P-A4   FORBID as Core continue
           |
           +--> [BERT MASK 80/10/10] ------- M-A5 / P-A5   FORBID MASK as Lace cut
           |
           v
  [soft prompt / SFT / RLHF|DPO] ---- M-A6 / M-A7 / M-A8
           |                          P-A6 / P-A7 / P-A8
           |                          FORBID as Core rows / write / history
           v
  [prefill → decode / KV / argmax] -- M-A9 / P-A9   FORBID Core continue / POINTER when
           |
           x --> Lace Core / Graphic D / Phi / Lace tokenizer
                 FORBIDDEN off this board

  Lace Core OFF this board --> docs/systems-manifest-ascii.md
  Phi = [GAP] · tokenizer = [GAP]
```

Core, Φ, and Lace tokenizer stay **off-board**.

### Wave A panels (blocking · 1:1 with cards)

| Panel | Mechanism (must match M-A* title) | Stamp seed (carry hard FORBIDs) |
|---|---|---|
| **P-A1** | M-A1 Self-supervised pre-training / foundation-model adaptation pipeline | KEEP-read pipeline; **FORBID-as-Core-write**; **FORBID corpus→DOCUMENT fuel** (FM-X9) |
| **P-A2** | M-A2 Tokenization → shared-vocabulary intern | KEEP-read book primitive; **FORBID-as-Lace-identity** (X2); no Lace tokenizer invent (FM-X8) |
| **P-A3** | M-A3 Token + position (+ segment) embedding construction | KEEP-read book vectors; **FORBID-as-store** (X5 · FM-X5) |
| **P-A4** | M-A4 Decoder-only causal next-token training (prefix LM loss) | KEEP-read objective; **FORBID next-token as Core continue** (X3 · FM-X3); SILENT-for-Lace-append |
| **P-A5** | M-A5 Masked LM / BERT 80/10/10 corruption recipe | KEEP-read paper corruption; **FORBID-as-Core-write** (MASK ≠ Lace cut) (X4 · FM-X4); SILENT; cross-link D7 |
| **P-A6** | M-A6 Soft-prompt / prompt-tuning adaptation | KEEP-read steward; **FORBID as Core rows** (X6 · FM-X6); SILENT |
| **P-A7** | M-A7 Instruction fine-tuning (SFT) | KEEP-read steward; **FORBID-as-Core-write**; SILENT-for-Lace-append (X6) |
| **P-A8** | M-A8 RLHF / human-preference alignment (reward + policy / DPO family) | KEEP-read steward; **FORBID as Core write** (X6 · FM-X6); **FORBID-as-Lace-history**; not Graphic D |
| **P-A9** | M-A9 Prefilling / decoding / KV-cache inference (two-phase + \(\arg\max Pr\)) | KEEP-read projection; **FORBID as Core continue / POINTER when** (X7 · FM-X7); FORBID-as-store for KV; SILENT; Φ `[GAP]` |

Hard fence rules (execute must enforce):

- **P-A2** fence: no Lace WORD/Star identity glyphs; stamp FORBID-as-Lace-identity explicit; tokenizer remains `[GAP]` off-fence.  
- **P-A3** fence: embed tables are book vectors — not Graphic D rows; FORBID-as-store explicit.  
- **P-A4** / **P-A9** fences: \(\arg\max Pr\) is book projection — not Piece 2 continue; no POINTER word in fence.  
- **P-A5** fence: MASK/replace/leave are corruption — not Lace cut; FORBID-as-Core-write explicit.  
- **P-A9** fence: prefilling / KV / decode are inference memory — not POINTER when/arity/adjacency.

### P4-X — Rigor matrix (required · on clipboard or ascii)

For each P-A1…P-A9: `P · M · Stamp match · Implements reverse-walk · Fence book-only (no POINTER word) · False-friend fence · Overview attach · PASS/FIX`

Any FIX must be resolved in this pass (prefer panel edits). Do not inherit Pass 3 blindly — re-walk Implements.

False-friend fence column: panel must not depict the FM-X* confusion (e.g. P-A2 must not look like Lace identity; P-A5 must not look like a strand cut).

### Cross-walk table (required)

`P-A* | M-A* | stamp | Map X# | clipboard PAGE | FM-X*`

Suggested (execute confirms from P3-M):

| P | M | Map | PAGE | FM |
|---|---|---|---|---|
| P-A1 | M-A1 | X / FM-X9 | X | FM-X9 |
| P-A2 | M-A2 | X2 | B | FM-X2 / FM-X8 |
| P-A3 | M-A3 | X5 | D | FM-X5 |
| P-A4 | M-A4 | X3 | E / A | FM-X3 |
| P-A5 | M-A5 | X4 | C / D7 | FM-X4 |
| P-A6 | M-A6 | X6 | F | FM-X6 |
| P-A7 | M-A7 | X6 | F | FM-X6 |
| P-A8 | M-A8 | X6 | F | FM-X6 |
| P-A9 | M-A9 | X7 | E / F | FM-X7 |

### Waves B–D

One-line deferred seeds only (same as mechanisms file). No full panels this pass. No Pass 5 order proposal yet.

### ASCII standard

Monospace fences; ≤76 cols; reverse-walkable to M-A steps; stamp under fence; no Core tokens / no POINTER word inside fence; book labels only.

---

## Clipboard / mechanisms thin edits (execute)

On `xiao-zhu-foundations-clipboard.md`:

1. **Pass 4 receipt** — EXECUTED, path to ascii file, panel count, P4-X verdict, emission `[GAP]`, tokenizer `[GAP]`  
2. **P4-X matrix** (if not only in ascii file)  
3. Optional one-liner under P3-M pointing to ascii file  
4. Do **not** rewrite PAGE morals, D1–D6, FM-X*, D7, or P3-M teeth  

Mechanisms file: optional one-liner under status — “Pass 4 ASCII: see xiao-zhu-foundations-ascii-machinery.md (P-A1…A9). Algorithms unchanged.”

---

## Tickets

### T0 — Claim / base

Held at plan (`xiao-zhu-pass-4-plan`). Before execute: `git pull --ff-only` · refresh claim if clock turned · agent id `reference-Mapper` (or equivalent).

### T1 — Hands skim

Shoe → A–D → manifest → law → map §3 → M-A1…M-A9 → Pass 3 clipboard (P3-M) → five-stakes PAGE 3/5 → sibling ASCII sample (AgentScope/Petersen panel schema).

### T2 — PDF gate

Confirm `refs/local/xiao-zhu-foundations-llm.pdf` resolves; `git check-ignore`; abort if PDF staged. Light re-sight only if a panel needs a locus pin (§1.1 / shared-vocab / embed / next-token / BERT MASK / soft / SFT / RLHF / Ch.5 prefilling-decode / §2.3.3 KV).

### T3 — Scaffold `xiao-zhu-foundations-ascii-machinery.md`

Opening contract, stamp legend, overview placeholder, P-A1…P-A9 stubs, cross-walk stub, P4-X stub. **Create only at execute.**

### T4 — Draw SOURCE MACHINERY overview (book-side; Core/Φ/tokenizer off-board)

Pipeline spine matching M-A1…M-A9.

### T5 — Draw panels P-A1…P-A9 (Implements match M-A*; stamps match; hard FORBIDs under fences)

### T6 — P4-X rigor matrix + fix any FIX rows (incl. false-friend fences)

### T7 — Cross-walk + Pass 4 receipt · mark plan EXECUTED

### T8 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: ascii file + clipboard receipt/P4-X (+ mechanisms one-liner) + plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push. Do **not** push unless asked.

---

## Acceptance tests (execute must pass)

1. `xiao-zhu-foundations-ascii-machinery.md` exists with opening contract, stamp legend, SOURCE MACHINERY overview, P-A1…P-A9, cross-walk, P4-X.  
2. Each P-A* cites M-A*, implements that card’s steps, matching stamp, ≤76-col fence, book glyphs only inside fence; **no POINTER word inside any diagram fence**.  
3. P-A2 fence does **not** depict vocab id as Lace WORD/Star; FORBID-as-Lace-identity explicit; tokenizer still `[GAP]`.  
4. P-A3 fence does **not** depict embed tables as Graphic D; FORBID-as-store explicit.  
5. P-A4 / P-A9 fences do **not** depict \(\arg\max Pr\) / decode as Core continue; P-A9 FORBID-as-POINTER-when explicit (stamp under fence, not inside).  
6. P-A5 fence does **not** depict MASK as Lace cut; FORBID-as-Core-write explicit.  
7. Overview keeps Lace Core, Φ, and Lace tokenizer off-board; pipeline spine matches M-A1…M-A9.  
8. P4-X all PASS (or fixed to PASS this pass) including false-friend fence column.  
9. No stamp invents POINTER when/arity/adjacency; Φ and tokenizer still `[GAP]`.  
10. M-A* algorithms not rewritten; reason-model-map / law / graphics / systems-manifest / Core ASCII / `src/` / five-stakes untouched; PDF never staged; CC BY-NC body not pasted into Core.  
11. Pass 4 receipt on book clipboard.  
12. Steward test: shoe in hands — refuse “intern vocab id as Lace WORD” and “MASK the strand” and “decode as POINTER when” using only P-A2 + P-A5 + P-A9 + stamps.

---

## Execution (when you say go pass 4)

1. Refresh claim.  
2. T1–T7.  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop. Do **not** push unless asked. Do **not** invent Wave B panels or Pass 5 order proposal.

---

## Steward test

Shoe in hands, or no.  
These diagrams are photographs of **book LLM machinery**, not Lace machinery.  
Pre-training boxes mutate weights — not DOCUMENT append.  
Vocab-intern boxes are not Lace WORD identity.  
Embed boxes are a second store — not Graphic D.  
Next-token and MASK boxes are training/corruption — not continue / cut.  
Soft / SFT / RLHF boxes are steward adaptation — not Core rows.  
Prefilling / decode / KV boxes project language — they do not emit POINTER.  
Opening a tokenizer gap is not filling Φ.  
Φ stays `[GAP]`. Tokenizer stays `[GAP]`.

---

## Plan receipt (HELD — NOT EXECUTED)

| Field | Value |
|---|---|
| Status | **HELD** / **NOT EXECUTED** |
| When (plan) | 2026-09-18 ~19:27 PT |
| Station | clipboards · claim `xiao-zhu-pass-4-plan` |
| Base | `6b5aa03` (Pass 3 tip) |
| Artifact (execute only) | `docs/clipboards/xiao-zhu-foundations-ascii-machinery.md` — **not created at plan time** |
| Panels planned | P-A1…P-A9 1:1 with M-A1…M-A9 |
| P4-X | planned rigor matrix (execute fills) |
| ASCII file | **not created** |
| Emission | `[GAP]` |
| Tokenizer | `[GAP]` |
| Push | **not** performed |
