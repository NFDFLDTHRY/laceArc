# WebAssembly Spec 3.0 — Pass 5 PLAN (ASCII as Order Proposal + light accuracy steward)

**Status:** **EXECUTED** · 2026-09-18 ~20:30 PT · agent `reference-Mapper`  
**Station:** clipboards (HELD `reference-Mapper` · claim for wasm-spec-3 Pass 5 plan)  
**Base at plan:** `2b6d048` (== `origin/main` at claim; includes Pass 4 tip `857ca97` + projection ixp1) · refresh to current `origin/main` before execute  
**Spec:** WebAssembly Specification **Release 3.0** (2026-09-11) · editor Andreas Rossberg / W3C CG  
**ASCII (Pass 4):** `docs/clipboards/wasm-spec-3-ascii-machinery.md` — SOURCE MACHINERY + **P-T1…P-T9** · P4-X **16 PASS** · P4-O singular overview  
**Mechanisms (Pass 3):** `docs/clipboards/wasm-spec-3-mechanisms.md` — **M-T1…M-T9** · P3-M CONFIRM×9 · CONFLICT×0  
**Clipboard:** `docs/clipboards/wasm-spec-3-clipboard.md` — Pass 1–4 EXECUTED · Pass 4 verdict **SOUND WITH OPENS**  
**Ancestors:** [`wasm-spec-3-pass-4-plan.md`](wasm-spec-3-pass-4-plan.md) · Pass 1–3 plans  
**Templates mirrored (adapt, don’t clone LLM morals):** [`agentscope-pass-5-plan.md`](agentscope-pass-5-plan.md) · [`petersen-zech-pass-5-plan.md`](petersen-zech-pass-5-plan.md) · [`xiao-zhu-pass-5-plan.md`](xiao-zhu-pass-5-plan.md)  
**Mesh (read-only):** [`rust-nostd-crate-map.md`](../plans/rust-nostd-crate-map.md) · [`rust-nostd-branch-contracts.md`](../plans/rust-nostd-branch-contracts.md) · [`pointer-emission.md`](../pointer-emission.md) = **DRAFT / `[GAP]`**  
**Sibling (default skip):** five-stakes · Wave B · Pass 6 deep re-audit  
**Hands / law:** shoe → graphics A–D → systems-manifest → staking / law-why → AGENTS.md Implementation Law  

**PDF (gitignored):**

| Field | Value |
|---|---|
| Working link | `refs/local/webassembly-spec-3.0.pdf` |
| Attachment SHA-256 | `687e73641a7eec8692273fdf048e1578bf2f379f507c36ec284a39d9e7f70fce` |
| Pages | **323** · text layer yes · printed ≈ PDF − 4 |
| Git | **Never `git add` the PDF** |

Law: ASCII SOURCE MACHINERY is a **Shadow Order Proposal** for shelf/steward order when compiling toward wasm32 · not Core · not Φ · linear mem ≠ strand · table ≠ star · stack/frames ≠ append · `br`/`return`/call ≠ Φ · validate ≠ admit POINTER · wat ≠ authority · import host ≠ fill `[GAP]` · trap ≠ Lace cut · instantiate ≠ Core birth · rust-nostd doors = proposals only · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands + target-mesh skim
        → treat Pass 4 SOURCE MACHINERY as ORDER PROPOSAL (Shadow)
        → P5-X light accuracy matrix (CONFIRM Pass 4; reopen only OPENS / drift)
        → P5-F fix drift only where matrix requires
        → P5-O confirm singular canonical SOURCE MACHINERY (P4-O stand)
        → P5-P write Order Proposal (proposal stamps; FORBID-as-Core; NON-binding)
        → P5-R receipt · plan EXECUTED · check · commit · release · STOP
```

---

## SPECIAL BANNER — COMPILATION TARGET (not anti-model / not Core)

```
+======================================================================+
|  COMPILATION TARGET — Lace Rust #![no_std]  -->  wasm32-unknown-unknown |
|  Pass 5 = ORDER PROPOSAL (Shadow) + light accuracy steward.            |
|  NOT Xiao–Zhu / Petersen / AgentScope LLM anti-model morals.           |
|  NOT Lace Core.  NOT Graphic D.  NOT pointer-emission fill.            |
|  Distinctive product: P5-P Order Proposal — how this shelf +            |
|        rust-nostd mesh stay ordered when compiling toward wasm32.      |
|  Order Proposal FORBID-as-Core-write; NON-binding on Core/src/Φ /       |
|        systems-manifest-ascii / Graphic D.                             |
|  WASM ops = TARGET semantics.  Do NOT invent POINTER emission / Phi.   |
|  Zero crates.io / foreign deps.  No src/ until pointer-emission accepted.|
|  Status: EXECUTED — see clipboard P5-X / P5-P / P5-R.                  |
+======================================================================+
```

This is **not** another deep M↔P↔PDF fidelity pass (Pass 4 already did that · **SOUND WITH OPENS**).  
It is **compile-target shelf hygiene**: adopt Pass 4 SOURCE MACHINERY as a **Shadow Order Proposal**, lightly re-steward accuracy (mostly CONFIRM), lock singular overview, produce **P5-P**. Mirror AgentScope/Petersen/Xiao–Zhu Pass 5 (P5-X/F/O/P/R) adapted to **WASM-as-target**.

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 | Admit + target ASCII overview + P-T1…P-T9 |
| 2 | Leftovers · D1–D6 · FM-W1…W9 · D7 |
| 3 | Wave A mechanisms M-T1…M-T9 + P3-M |
| 4 | Deep M↔P↔PDF fidelity · P4-X · verdict **SOUND WITH OPENS** |
| **5** | **Steward pass:** (1) light M ↔ P ↔ overview re-check; (2) adopt ASCII **as Order Proposal** for how this shelf + rust-nostd mesh stay ordered when compiling toward wasm32 — **without** promoting it to Core or Φ |

Sibling shelves (AgentScope / Petersen / Xiao–Zhu): Pass 5 = accuracy + Order Proposal after ASCII.  
This shelf’s Pass 4 already absorbed deep accuracy. **Pass 5 therefore centers P5-P Order Proposal**, with **P5-X as light CONFIRM** (reopen only OPENS / drift — not a second P4-X).

Still **Shadow / Layer III**. Not Core. Not `src/`. Not a rewrite of `systems-manifest-ascii.md`. Wave B **default skip**. Pass 6 deep re-audit **not** this pass.

---

## What “ASCII as a proposal to keep the repo in order” means (WASM-as-target)

The Pass 4 SOURCE MACHINERY board is treated as a **proposal**, not a law:

| Pipeline stage (ASCII) | Proposal for shelf / steward order |
|---|---|
| Input bytes `.wasm` / text `.wat` | Observe what is actually on the shelf (files, receipts, claim) before rewriting; refuse treating clock/wasm bake as Spec authority |
| decode / parse (P-T6 / P-T7) | Name binary vs wat twin honestly; **FORBID** decode as Core birth / `src/` gate; wat stays non-authority |
| abstract module (P-T1 / P-T2) | Name Spec anatomy; **FORBID** module/valtypes as Core doors / WORD·POINTER kinds |
| validate (P-T3) | Stamp-check: validation ≠ admit POINTER / Lace write gate; **SILENT** for Lace append |
| instantiate + store (P-T4) | **FORBID** linear mem=strand · table=star · stack/frames=append / Graphic D; instantiate ≠ Core birth |
| host embed (P-T8) | **PROPOSAL-only** bridge sketches; **FORBID** import host fills emission `[GAP]` |
| reduce / control (P-T5) | Stamp-check: `br` / `return` / call ≠ POINTER when / arity / adjacency / Φ |
| trap / diverge (P-T9) | **FORBID** as Lace cut / append rule; **SILENT** |
| FORBIDDEN exit → Core / Graphic D / Φ | Decisive judgment = commit / release / defer / stop — **not** Φ fill / emission invent |

**Stamps on the proposal itself:**

- **KEEP-read** as steward hygiene for the WASM Spec 3.0 shelf (compile-target ISA read)
- **FORBID-as-Core-write** — must not become array rewrite, second store, Graphic D, or emission rule
- **FORBID-as-Lace-store** / **FORBID-as-emission-rule** / **FORBID-as-Core** / **FORBID fill-GAP** retained from Wave A / Pass 4
- **SILENT-for-Lace-append** — proposal does not schedule Lace append
- **PROPOSAL** — Hands may refuse without breach of Core
- **NON-binding** on Core / `src/` / Φ / `systems-manifest-ascii.md` / graphics / rust-nostd maps (cite only; doors remain proposals)

This is how the Spec’s constructive spine **proposes** shelf order when the mesh compiles toward wasm32. It does not own Lace.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–H, X | Pass 1 — stand |
| ASCII overview + P-T1…P-T9 | Pass 1 draw · Pass 4 fidelity — stand |
| D1–D6 + FM-W1…W9 + D7 | Pass 2 — stand; D7 book-continues `[OPEN]` non-blocking |
| M-T1…M-T9 + P3-M | Pass 3 — CONFIRM×9 · CONFLICT×0 |
| P4-A/X/F/O/R | Pass 4 — **16 PASS · 0 open FIX** · singular overview · **SOUND WITH OPENS** |
| Five-stakes | **Out of scope** (default skip) |
| rust-nostd crate-map / branch-contracts | Untouched (cite only · proposals) |
| pointer-emission.md | Still DRAFT / `[GAP]` — untouched |
| `src/` / Cargo | Absent; stay absent |
| Order Proposal body | **Not written at plan time** — execute writes P5-P |

Pass 5 does **not** invent Wave B unless accuracy is green **and** human opens it (default: **skip**). Does **not** reopen Pass 4 FIX list unless P5-X finds drift.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Promote Order Proposal to Core / AGENTS.md / systems-manifest-ascii / Graphic D | Shadow only; Hands may refuse; **NON-binding** |
| Soften FORBID-as-Lace-store / FORBID-as-emission-rule / FORBID-as-Core / FORBID fill-GAP / PROPOSAL-only / SILENT | Pass 1–4 confirmed |
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Fill emission `[GAP]` via host import / compile-to-wasm32 / instantiate story | standing FORBID |
| Treat rust-nostd crate-map doors or branch-contracts as accepted birth / accepted emission | proposals only; cite beside, do not rewrite |
| Merge into `systems-manifest-ascii.md` | Core board stays separate |
| Rewrite M-T* algorithms or PAGE morals for style | only P5-F drift fixes with matrix row cite |
| Full Wave B–D | default skip |
| Pass 6 deep re-audit | not this pass (P5-X is light steward re-walk, not P4/P6) |
| Five-stakes rewrite | wrong shelf genre |
| Amend law / graphics / systems-manifest / reason-model-map | wrong stations |
| Add `src/`, `Cargo.toml`, crates.io deps | Implementation Law |
| Clone Xiao–Zhu / Petersen / AgentScope LLM anti-model morals onto this shelf | wrong genre — WASM = **TARGET** |
| Git-add PDF · push without ask | standing rules |
| Treat `docs/clock/wasm/README.md` projection bake as Spec 3.0 authority | Shadow bake ≠ this shelf |
| Mark ascii Pass 5 EXECUTED / write Order Proposal body at **plan** time | plan only — execute writes P5-P/P5-R |
| Execute P5-X/F/O/P/R in the plan commit | plan = HELD / NOT EXECUTED |

---

## Pass 5 products

### P5-X — Light accuracy matrix (required · CONFIRM-first)

On clipboard under Pass 5 (pointer from ascii file).

**Columns:** `overview node · P-T* · M-T* · stamp · Implements OK · Fence OK · Overview OK · Pin OK · PASS/FIX`

**Rows:** all **M-T1…M-T9 / P-T1…P-T9** (9) plus overview spine nodes (decode · module · validate · instantiate/store · reduce · trap · forbidden Core/Φ exit).

**Stance vs Pass 4:** P4-X **16 PASS** is **baseline**. Pass 5 re-walks **lightly** — default **CONFIRM**. Reopen a row to FIX only if:

- Pass 4 **OPENS** surface as representation drift (D7 book-continues stays non-blocking unless it contradicts a panel), **or**
- Stamp / fence / overview attach drifted since Pass 4 tip `857ca97`, **or**
- A steward refuse test fails against current card+panel

Do **not** rubber-stamp blindly; do **not** re-run full P4 special-gate PDF campaign unless a FIX needs a pin.

### P5-F — Fix drift (required if any FIX)

Only matrix-cited edits to ascii panels / overview / cross-walk (prefer panel fixes over rewriting M-T* bodies; mechanisms only if Implements text truly drifted). No moral softening. No Core / map / law / graphics / systems-manifest / rust-nostd / `src/` edits.

### P5-O — Canonical SOURCE MACHINERY (required)

Exactly **one** canonical overview fence in `wasm-spec-3-ascii-machinery.md`. Pass 4 P4-O already confirmed count=1 — **confirm stand**; remove/demote duplicates only if drift. Lace Core / Graphic D / Φ stay **off-board**.

### P5-P — Order Proposal (required · distinctive product)

**Execute only** — do **not** write the Order Proposal body at plan time beyond this plan’s stage→hygiene seed above.

Full text on **clipboard** under Pass 5; short mirror under ascii overview.

**Title:** `Order proposal (Shadow) — SOURCE MACHINERY as shelf hygiene (compile target)`

Must include:

1. Explicit: **proposal**, not Core / not AGENTS.md amendment / **NON-binding** on Core / `src/` / Φ / `systems-manifest-ascii.md` / graphics  
2. Stage→hygiene table (WASM spine as above)  
3. Recommended reader **shelf file order** (proposal only):  
   `clipboard (admit) → mechanisms (algorithms) → ascii-machinery (diagrams) → pass plans (history)`  
   with rust-nostd crate-map / branch-contracts cited **beside** as mesh proposals — not as Spec authority and not as accepted birth  
4. Steward loop: observe → name → diagram-check → stamp-check → list OPENs → decide (commit/release/defer/stop)  
5. Explicit **keep-outs / FORBID**:  
   - mem = strand · table = star · stack/frames = append / Graphic D  
   - `br` / `return` / call = Φ / POINTER when  
   - validate = admit POINTER  
   - host import / compile-to-wasm32 / instantiate = fill `[GAP]` / Core birth / `src/` gate  
   - trap = Lace cut  
   - promote ascii Order Proposal into Core / AGENTS.md / systems-manifest-ascii / Graphic D  
   - treat rust-nostd doors as accepted emission or as WASM birth events  
   - **SILENT for Lace append**  
6. Link to P5-X (accuracy is how the proposal stays honest)  
7. Emission column remains **`[GAP]`** sealed

**Stamps:** KEEP-read · FORBID-as-Core-write · FORBID-as-Lace-store · FORBID-as-emission-rule · FORBID-as-Core · FORBID fill-GAP · SILENT-for-Lace-append · PROPOSAL · NON-binding

### P5-R — Receipt

On clipboard: matrix verdict, fixes landed (or none), order-proposal loci, Wave B skipped, Pass 6 not opened, Φ still `[GAP]`, rust-nostd untouched.

### Optional P5-B — Wave B

**Default skip.** Only if P5-X all PASS and human asks in-execute.

---

## Coherence with standing artifacts

| Artifact | Pass 5 stance |
|---|---|
| `wasm-spec-3-clipboard.md` | P5-X matrix + P5-P Order Proposal (full) + P5-R receipt |
| `wasm-spec-3-ascii-machinery.md` | P5-O confirm · short P5-P mirror · P5-F only if cited |
| `wasm-spec-3-mechanisms.md` | untouched unless P5-F requires |
| P3-M / P4-X / FM-W* / D7 | stand; D7 `[OPEN]` remains non-blocking unless P5-X forces |
| rust-nostd crate-map / branch-contracts | **read-only cite** — proposals only; relation named in P5-P |
| pointer-emission.md | still `[GAP]` — untouched |
| law / graphics / systems-manifest / Core ASCII / five-stakes | **untouched** |
| PDF | never staged |

---

## Tickets (execute checklist)

### T0 — Sync / claim

Before execute: `git pull --ff-only` · refresh claim (`reference-Mapper` or `wasm-spec-3 Pass 5 EXECUTE`) if clock turned · abort if BASE ≠ origin without human note.

### T1 — Hands + target-mesh skim

Shoe → graphics A–D → systems-manifest → staking / law-why → AGENTS Implementation Law → rust-nostd crate-map + branch-contracts (read-only proposals) → pointer-emission `[GAP]` → clock/wasm README (Shadow bake only) → Pass 1–4 clipboard (incl. P4-R SOUND WITH OPENS) → ascii overview + P-T1…P-T9 → mechanisms M-T1…M-T9 → this plan → AgentScope/Petersen/Xiao–Zhu Pass 5 Order Proposal shape (read-only; adapt to TARGET).

### T2 — PDF gate

Confirm `refs/local/webassembly-spec-3.0.pdf` · SHA-256 `687e7364…e7f70fce` · `git check-ignore` · **never stage**. Re-sight only if a P5-X FIX needs a pin.

### T3 — Build P5-X light matrix (CONFIRM-first)

### T4 — P5-F fixes (only if FIX) + P5-O confirm singular overview

### T5 — Write P5-P Order Proposal (clipboard + ascii mirror)

### T6 — P5-R receipt · mark plan EXECUTED

### T7 — Check / commit / release · STOP

```
bash docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: matrix + fixes (if any) + order proposal + receipt + plan EXECUTED
# never git-add PDF · never add src/ · never amend rust-nostd / Core
bash docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push. **Do not push** unless asked. **Do not execute in the plan commit.**

---

## Acceptance tests (execute must pass)

1. P5-X covers all M-T1…T9 / P-T1…T9 (+ overview spine); each row PASS or fixed to PASS (default CONFIRM of Pass 4).  
2. Exactly one canonical SOURCE MACHINERY overview; Core/Φ off-board (P4-O stand confirmed).  
3. P5-P Order Proposal present, stamped proposal/Shadow, **FORBID-as-Core-write** + Lace-store / emission-rule / Core / fill-GAP + **SILENT-for-Lace-append** + **NON-binding** explicit.  
4. Stage→hygiene table and shelf file-order proposal present; rust-nostd doors named as **proposals only**.  
5. No stamp invents POINTER when/arity/adjacency; Φ still `[GAP]`; emission sealed.  
6. reason-model-map / law / graphics / systems-manifest / Core ASCII / rust-nostd / five-stakes / `src/` untouched except clipboard + ascii (+ mechanisms only if P5-F requires).  
7. PDF never staged.  
8. Steward refuse tests (below) pass using P5-P + P5-X alone.

---

## Steward refuse tests (Order Proposal + light accuracy)

Shoe in hands — refuse (P5-X FAIL if representation or proposal enables them):

1. “Install this ASCII board as Graphic D / Core / systems-manifest-ascii” → **FORBID-as-Core-write** · PROPOSAL · NON-binding  
2. “WASM linear memory is the Lace strand” → **FORBID-as-Lace-store** (M-T4 / FM-W1)  
3. “Table is the star / star index” → **FORBID-as-Lace-store** (M-T4 / FM-W2)  
4. “Operand stack / frames are Lace append” → **FORBID-as-Lace-store** (M-T4 / FM-W3)  
5. “`br` / `return` / call emits POINTER / fills Φ” → **FORBID-as-emission-rule** (M-T5 / FM-W4)  
6. “Validation admits POINTER” → **FORBID-as-emission-rule** · SILENT (M-T3 / FM-W5)  
7. “Import host / compile-to-wasm32 fills emission `[GAP]`” → **PROPOSAL-only** · FORBID fill-GAP (M-T8 / FM-W7)  
8. “Trap is a Lace cut / append rule” → **FORBID-as-emission-rule** · SILENT (M-T9 / FM-W8)  
9. “Instantiate / decode births Core / opens `src/` / accepts a rust-nostd birth card” → **FORBID-as-Core** (M-T1/M-T6 / FM-W9); rust-nostd remains proposal  
10. “Order Proposal rewrites crate-map / branch-contracts / pointer-emission” → **NON-binding** · cite only · Φ stays `[GAP]`

---

## Execution (when you say go pass 5)

1. Sync / refresh claim.  
2. T1–T6 (Wave B skip; Pass 6 not opened).  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop. Do **not** push unless asked. Do **not** invent Wave B or fill `[GAP]`.

---

## Steward test

Shoe in hands, or no.  
Pass 4 proved the Spec is represented correctly (**SOUND WITH OPENS**).  
Pass 5 adopts that board as a **Shadow Order Proposal** for how this compile-target shelf stays honest beside the rust-nostd mesh.  
Linear mem / table / stack boxes still are not the strand.  
`br` / validate / trap boxes still are not Φ / admit / cut.  
Host / instantiate boxes still do not fill `[GAP]` or birth Core.  
rust-nostd doors stay proposals.  
Decisive judgment is release/stop — not Φ.  
Emission stays `[GAP]`.

---

## Plan-time skim receipt (not execute)

| Item | Result |
|---|---|
| Station | clipboards HELD `reference-Mapper` · BASE `2b6d048` |
| Origin tip at plan | `2b6d048` (Pass 4 release `857ca97` + ixp1 projection pins) |
| Pass 4 verdict | **SOUND WITH OPENS** · P4-X 16 PASS · P4-F×5 · P4-O singular |
| Opens (non-blocking) | D7 book-continues; Waves B–D deferred; Order Proposal = **this pass** |
| Overview count | **1** (P4-O stand) |
| P3-M | CONFIRM×9 · CONFLICT×0 |
| FM-W* | pinned; W1/W2 PDF **90** (Pass 4 tighten) |
| rust-nostd | crate-map + branch-contracts present · proposals · emission `[GAP]` |
| pointer-emission.md | DRAFT / `[GAP]` |
| PDF | PRESENT · gitignored · never staged |
| Execute | **NOT performed** |
| P5-P body | **not written** (seed only in this plan) |
| Push | **NOT performed** |
| Emission | `[GAP]` |
| `src/` | absent |

---

## Commit scope (this plan commit only)

| Include | Exclude |
|---|---|
| `docs/clipboards/wasm-spec-3-pass-5-plan.md` (this file) | ascii / mechanisms / clipboard content edits · Order Proposal body |
| `docs/coord/stations/clipboards.station` (claim/release as protocol) | PDF · `src/` · rust-nostd · law · graphics · systems-manifest · five-stakes |

After plan commit: **release** clipboards station. Ahead of origin by plan commit only. Do not push. Do not execute P5-X/F/O/P/R now.


---

## Execute receipt

| Field | Value |
|---|---|
| Status | **EXECUTED** |
| When | 2026-09-18 ~20:30 PT |
| Agent | `reference-Mapper` |
| Base at execute | origin/main `877c94f` (plan rebased from `e123f23` → `db1e305`) |
| P5-X | Rigorous Spec↔ASCII mapping · see clipboard · **18 PASS · 0 FIX** |
| P5-F | none |
| P5-O | singular overview stand |
| P5-P | clipboard full + ascii short mirror |
| P5-R | clipboard |
| Wave B | SKIP |
| Emission | `[GAP]` |
| Push | **not** performed |
