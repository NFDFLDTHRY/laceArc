# Rowlands pass 4 — rigorous mechanism map + ASCII source-machinery

**Status:** EXECUTED (2026-09-18 ~16:58 PT). Wave A complete; Waves B–D deferred with seeds. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Peter Rowlands, *Zero to Infinity: The Foundations of Physics* · Series on Knots and Everything Vol. 41 · World Scientific 2007  
**Title in file:** *Zero to Infinity* (ch.1 head “Zero”)  
**PDF:** `refs/local/rowlands-zero-to-infinity.pdf` · **738** pp. · text layer yes · **never git-add**  
**Offset:** printed p ≈ PDF p − 25 after front matter (Pass 1 certificate; ch.1 printed p.1 ≈ PDF 26)  
**Primary artifacts (new):**
- `docs/clipboards/rowlands-zero-to-infinity-mechanisms.md` — constructive mechanism cards  
- `docs/clipboards/rowlands-zero-to-infinity-ascii-machinery.md` — SOURCE MACHINERY overview + panels  
**Companion (receipt only):** `docs/clipboards/rowlands-zero-to-infinity-clipboard.md`  
**Read-only:** graphics A–D · systems-manifest · systems-manifest-ascii · law · reason-model-map §2 · five-stakes PAGE 2/5 · Passes 1–3  

Law: empty start · no extra rooms · “rewrite” ≠ mutate-WORD · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
       → open PDF (gitignored)
       → inventory Wave A from TOC + Pass 1–3 pins
       → for each M-A*: pin → extract rule → write constructive algorithm + Lace stamp
       → for each P-A*: draw ASCII panel tied to M-A* (book glyphs only inside fence)
       → forge one SOURCE MACHINERY overview (book side only; Core off-board)
       → receipt on book clipboard
       → check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 | PDF-sighted admission (A–F, X) |
| 2 | Leftovers / FM-R / D7 miniature |
| 3 | Coherence mesh book ↔ five-stakes ↔ map §2 |
| **4** | **Machinery of the book** — rigorous cards **and** ASCII diagram of the **system mechanism** Rowlands actually runs |

Kauffman split algorithms (pass 4) and ASCII (pass 5).  
This pass **combines** both for Rowlands in one execute, because the ask is to map/analyze **and** forge the ASCII of the system mechanism together.

Still **Shadow / Layer III**. Not Lace Core. Not `systems-manifest-ascii.md`. Not `src/`. Not Φ.

---

## What “system mechanism” means here

From ch.1–3 + later forks (TOC + Pass 1–3), the book’s spine is:

```
zero totality
    → rewrite / generate alphabet from empty (conjugation · complexification · dimensionalization)
    → dual / STMC group readings (why physics works)
    → Dirac algebra → nilpotent Dirac state
    → vacuum / particle tables (physics recovery)
    → dimensionality explained (not stored rooms)
    → Nature’s code / DNA–UG rewrite metaphors (biology/language fork)
```

Pass 4 must diagram and algorithm **that generative spine**, with FORBID stamps where the book’s physics/biology tables must not become Core.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Add `src/` or executable Core | Implementation law |
| Treat “rewrite” as mutate-WORD / Lace append scheduler | R3 clash; Pass 2–3 |
| Import nilpotent Dirac / vacuum / TOE / DNA / UG as Core schema | R4–R6 FORBID |
| Quietly amend reason-model-map / law / graphics / systems-manifest-ascii | wrong stations |
| Redraw Core ASCII as if Rowlands *is* Lace | wrong object |
| Soften Pass 3 mesh morals | mesh stands |
| Git-add PDF | gitignore |
| Claim full 738-page re-OCR as done | honesty; pin what you sight |
| Push without ask | standing rule |

---

## Artifact A — Mechanism cards

### File

`docs/clipboards/rowlands-zero-to-infinity-mechanisms.md`

### Card schema (mandatory)

Same rigor as Kauffman pass 4:

Kind · Book locus (printed + PDF) · Sighting · Eye-quote ≤3 lines · Inputs · Outputs · Invariants · Algorithm (numbered, constructive) · Complexity/termination · Worked miniature · Lace stamp · Hands cite · Map cite (R1–R6 or none) · False friend · Open gaps.

Algorithm standard: preconditions · state · numbered steps · choice points as `[OPEN]` · halt · output certificate. Book vocabulary inside the algorithm; Lace vocabulary only in stamp / false friend.

### Wave A — generative spine (blocking)

| ID | Mechanism | Seed (printed / PDF ≈ +25) | Expected stamp |
|---|---|---|---|
| M-A1 | Zero totality / empty start | ch.1 head p.1 / PDF 26; Preface PDF 10 | KEEP-read (R1 kin) |
| M-A2 | Universal rewrite: alphabet from empty (act on subalphabet → new zero-totality alphabet) | ch.1.2–1.5 · PDF ~27–45 | FORBID-as-mutate-WORD; SILENT for Lace append (R3) |
| M-A3 | Conjugation · complexification · dimensionalization trilogy | ch.1 (after rewrite setup) | KEEP-read as book algebra; FORBID as Core operators |
| M-A4 | Why physics works / dualities / where dimensionality comes from | ch.2 · esp. 2.6 PDF ~70 / p.45 | KEEP-read “explain not store”; FORBID N-D rooms (R2) |
| M-A5 | Emergence: Dirac algebra from rewrite stack | ch.3.3–3.4 · PDF ~94–97 / p.69–72 | FORBID-as-Core-schema (R4 neighborhood) |
| M-A6 | Nilpotent Dirac equation / nilpotent state | ch.3.5 · ch.6 · PDF ~102+ / p.77+; ch.6 PDF ~162+ | FORBID-as-Core-write / meaning table |
| M-A7 | Vacuum structures (weak/strong/electric/gravitational sketches) | ch.6.4 · ch.12 TOC | FORBID-as-Core rooms (R4) |
| M-A8 | Dimensionality / strings-without-strings (explain ≠ store) | ch.18 · PDF ~509 / p.484 | KEEP-read R2; FORBID extra rooms |
| M-A9 | Nature’s rewrite / DNA–UG fork | ch.19–20 · §19.7+ · §20.16–20.18 (Pass 1 pins PDF 615/621) | FORBID R5/R6; opened ≠ imported |

### Waves B–D (deferrable)

| Wave | Seeds | Default |
|---|---|---|
| B | Groups/reps (ch.4); breaking Dirac code (ch.5); QFT/nilpotent methods (ch.10–11) | Deferred table with seeds |
| C | Fermion/boson charge tables (ch.13–14); GUT/masses (ch.15) | Deferred |
| D | Gravity/cosmology (ch.17–18, 21); Nature’s rules / infinity (ch.20–21); App. A summary | Deferred |

Pass 4 success = **Wave A cards + Wave A ASCII + overview**. B–D may be deferred with seeds.

---

## Artifact B — ASCII source-machinery

### File

`docs/clipboards/rowlands-zero-to-infinity-ascii-machinery.md`

### Opening contract (≤8 lines)

1. Models **Rowlands’ book procedures**, not Lace Core.  
2. Companion to mechanisms cards + book clipboard.  
3. Lace Core ASCII = `docs/systems-manifest-ascii.md` — **do not conflate**.  
4. Emission Φ = `[GAP]`. No panel closes it.

### Panel schema (P-A* ↔ M-A*)

Mechanism cite · Sighting · Lace stamp · Map cite · False friend · Implements (M-A steps) · fenced ASCII ≤76 cols · stamp line under fence.

**Inside fence:** book glyphs only. **No** `WORD` / `POINTER` as if they were book objects.

### SOURCE MACHINERY overview (required)

One top-level ASCII board, book side only, e.g.:

```
  [zero totality]
        |
        v
  [rewrite: subalphabet → new zero alphabet]
        |  conjugation / complexification / dimensionalization
        v
  [dual / STMC readings] ----> (why physics works)
        |
        v
  [Dirac algebra] → [nilpotent Dirac state]
        |
        +--> [vacuum tables]          FORBID Core rooms
        |
        +--> [dimension explained]    KEEP-read; FORBID store
        |
        +--> [DNA / UG rewrite fork]  FORBID R5/R6

  Lace Core OFF this board → systems-manifest-ascii.md
  Phi = [GAP]
```

Exact art free; Core must stay off-board.

### ASCII standard

Monospace fences; ≤76 cols; figure-authoritative pages marked; panel labels must reverse-walk to M-A algorithm steps; stamp under fence.

---

## Tickets

### T0 — Claim / base

Held at plan. Before execute: pull · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §2 → five-stakes PAGE 2 → Passes 1–3 clipboard · plan.

### T2 — PDF gate

Confirm `refs/local/rowlands-zero-to-infinity.pdf`; `git check-ignore`; abort if PDF staged.

### T3 — Scaffold both artifacts

Headers, stamp legend, empty M-A1…A9 / P-A1…A9, overview placeholder, links to clipboard/map/five-stakes + non-merge with Core ASCII.

### T4 — Wave A mechanism cards (blocking)

Fill M-A1…M-A9 to schema. Prefer worked miniature on M-A2 (one rewrite generation) and M-A6 (nilpotent form statement).

### T5 — SOURCE MACHINERY overview + Wave A panels (blocking)

Draw overview; fill P-A1…P-A9; cross-walk table P↔M↔stamp↔R#↔clipboard PAGE/FM.

### T6 — Waves B–D

Fill what sighting allows **or** Deferred table with seeds in **both** files.

### T7 — Book clipboard receipt only

`## Pass 4 receipt` pointing at both artifacts. Do not rewrite A–X or Pass 2–3 morals.

### T8 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: mechanisms + ascii + clipboard receipt + this plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Every M-A1…A9 has all schema fields (or `[OPEN]` with cause).  
2. Every P-A1…A9 has schema + fenced ASCII + stamp; implements cited M-A steps.  
3. SOURCE MACHINERY overview present; Lace Core **outside**; Φ `[GAP]`.  
4. No card/panel stamps KEEP-as-Core-write for rewrite/Dirac/vacuum/DNA/UG.  
5. No fence body contains Lace `WORD`/`POINTER`.  
6. No card claims to close Φ; D7 OPENs from Pass 2 remain ≠ Φ fill.  
7. PDF never staged; `systems-manifest-ascii.md` untouched.  
8. Steward test: walk M-A2 steps on P-A2 alone; refuse “rewrite” as array verb using stamp+false friend.  
9. Steward test: overview shows generative spine without looking like Core tape.

---

## Relation to other ASCII / mechanism work

| File | Object |
|---|---|
| `docs/systems-manifest-ascii.md` | Lace Core spine |
| `docs/clipboards/kauffman-4ed-mechanisms.md` / `…-ascii-machinery.md` | Kauffman Shadow |
| **`rowlands-zero-to-infinity-mechanisms.md` + `…-ascii-machinery.md`** | **Rowlands Shadow** |

Do not import H×C×W train oil or Core piece numbers into panel bodies.

---

## Execution (when you say go pass 4)

1. Refresh claim.  
2. T1–T7 with Wave A cards + panels + overview complete.  
3. Mark this plan EXECUTED; list deferred B–D IDs.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
Zero totality is kin of Graphic A1 — not a second cosmos.  
Book “rewrite” generates; Lace never mutates WORD @ i.  
Nilpotent Dirac on paper ≠ Core schema.  
ASCII gears ≠ Lace append.  
Φ stays `[GAP]`.

---

## Execution receipt

| Field | Value |
|---|---|
| When | 2026-09-18 ~16:58 PT |
| Agent | `reference-Mapper` |
| Wave A | M-A1…M-A9 + P-A1…P-A9 + SOURCE MACHINERY overview |
| Deferred | B–D (seed tables in both artifacts) |
| Clipboard | Pass 4 receipt only (no A–X rewrite) |
| Untouched | `src/` · systems-manifest* · systems-manifest-ascii · law · graphics · reason-model-map (read §2 only) · PDF (never staged) |
| Commits | see git log this station release |
