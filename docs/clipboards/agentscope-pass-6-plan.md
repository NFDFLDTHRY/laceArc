# AgentScope pass 6 — rigorous analysis & check of the ASCII systems-diagram proposal

**Status:** PLANNED — not executed. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute (Pass 5 commits may still be local — sync first)  
**Paper:** Jiayi Bi et al. · arXiv `2609.02371v1` · **AGENTSCOPE**  
**Under test:** `docs/clipboards/agentscope-ascii-machinery.md` — SOURCE MACHINERY + P-A1…P-A9 + Pass 5 Order Proposal mirror  
**Proposal text:** clipboard Pass 5 P5-P (full) · ascii short mirror  
**Prior accuracy:** P5-X = 11 PASS / 0 FIX (standing baseline; Pass 6 goes **deeper**)  
**Companions:** `agentscope-mechanisms.md` · `agentscope-clipboard.md` · map §5 · five-stakes PAGE 5/5  
**Hands:** shoe → graphics A–D → systems-manifest → law-why → map §5 → M-A* → P-A* → overview → P5-P → this plan  

**PDF (gitignored):** `refs/local/bi-et-al-agentscope-diagnosing-insights.pdf` · **never `git add`**

Law: ASCII board + Order Proposal remain **Shadow proposal** · not Core · not Φ · ReAG ↛ array · Hands may refuse · `[GAP]` sealed.

```
pull/ff (incl. Pass 5 if unpushed) → claim → Hands skim
        → P6-A structural analysis of SOURCE MACHINERY as systems diagram
        → P6-P rigor check of Order Proposal claims (stage→hygiene + FORBIDs)
        → P6-X deep accuracy re-audit (stricter than P5-X)
        → P6-F fix only matrix/analysis-cited drift
        → P6-R verdict + receipt · plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 4 | Draw SOURCE MACHINERY + P-A1…P-A9 |
| 5 | First M↔P accuracy + establish Order Proposal (Shadow) |
| **6** | **Rigorously analyze and check** that ASCII systems-diagram proposal: structure, safety stamps, proposal claims, deep fence/Implements fidelity |

Pass 5 answered “do cards and panels roughly match?”  
Pass 6 answers “is the **systems diagram proposal** coherent, complete, non-leaking, and steward-refusable under Hands?”

Still Shadow. Not Core. Not Wave B. Not a rewrite of `systems-manifest-ascii.md`.

---

## What “rigorous analysis & check” means

### A. Systems-diagram structure (P6-A)

Walk the canonical SOURCE MACHINERY fence and prove:

1. **Spine completeness** — every diagnosis stage the paper’s §3.3 Fig.2 needs appears once (trajectory → steps → ReAG+ISR → neural checks → candidates → decisive judgment).  
2. **No orphan nodes** — every overview box cites ≥1 P-A*/M-A* (or is explicitly marked input/off-board).  
3. **No missing Wave A coverage** — every M-A1…A9 appears on overview or is explicitly “labels only” (taxonomy).  
4. **Off-board integrity** — Lace Core / 1D array / Graphic D / Φ appear only as **FORBIDDEN exits**, never as on-board stores.  
5. **Glyph discipline** — overview fence contains **no** `WORD` / `POINTER` / Graphic-D row art as if paper objects.  
6. **Singularity** — still exactly one canonical overview (reconfirm P5-O).

Record a checklist table: item · evidence (line/fence cite) · PASS/OPEN.

### B. Order Proposal rigor (P6-P)

Analyze the P5-P proposal text (clipboard full + ascii mirror) against the diagram:

| Check | Question |
|---|---|
| Claim–diagram bind | Does each stage→hygiene row point at a real overview node / P-A*? |
| Overclaim | Does any sentence imply Core write, AGENTS.md law, or Φ schedule? |
| FORBID completeness | Are FORBID-as-Core-write · FORBID-ReAG-in-array · SILENT-for-Lace-append · PROPOSAL · Hands-refusal all explicit and consistent with panel stamps? |
| Steward loop soundness | observe→name→diagram-check→stamp-check→OPEN list→decide — does “decide” exclude POINTER-when / Φ fill? |
| File-order proposal | clipboard→mechanisms→ascii→plans — still proposal-only (not mandatory Core path)? |
| Mirror fidelity | Ascii short mirror does not **strengthen** claims beyond clipboard P5-P |

Failures → OPEN rows; fix by tightening proposal wording (not by softening FORBIDs).

### C. Deep accuracy (P6-X) — stricter than P5-X

For **each** P-A1…P-A9:

1. **Cite match** — Mechanism cite / PDF pin agrees with M-A* (re-sight with `pdftotext` if doubtful).  
2. **Stamp match** — panel stamp ≡ card stamp family.  
3. **Implements reverse-walk** — every “Implements” step id exists as a numbered step on the card; steward can walk card→fence.  
4. **Fence fidelity** — glyphs depict those steps; no Core tokens inside fence; P-A3 is DAG-shaped not 1D row strip.  
5. **False-friend / FM link** — present or explicitly N/A.  
6. **Overview attach** — panel’s stage appears on canonical overview.

Columns: `P · M · Cite · Stamp · Implements · Fence · Overview · FM · Verdict (PASS/FIX)`.

P5-X PASS is **not** inherited blindly — Pass 6 must re-walk.

### D. Fixes (P6-F)

Allowed: align Implements/stamps/pins; tighten fences; tighten proposal wording; demote duplicate overviews; add OPEN rows with cause.  
Disallowed: soften FORBIDs for beauty; invent Φ; merge into Core ASCII; invent Wave B/C.

### E. Receipt (P6-R)

Verdict: **PROPOSAL SOUND** / **PROPOSAL SOUND WITH OPENS** / **PROPOSAL BLOCKED**.  
Φ still `[GAP]`. Wave B still deferred unless human opens it (default skip).

---

## Already done (baseline — re-check, don’t assume)

| Layer | Pass 5 claim |
|---|---|
| P5-X | 11 PASS / 0 FIX |
| P5-O | one canonical overview |
| P5-P | Order Proposal on clipboard + ascii mirror |
| Wave B | skipped |

Pass 6 may **confirm** or **reopen** those claims with deeper evidence.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Promote proposal to Core / AGENTS.md | Hands / Shadow only |
| Soften FORBID-ReAG-in-array or neural≠proof | morals stand |
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Redraw Core `systems-manifest-ascii.md` | wrong object |
| Quiet rewrite of P3-M / five-stakes / map §5 | wrong scope |
| Wave B–D invent from TOC | default skip |
| Git-add PDF · push without ask | standing rules |

---

## Products (execute)

1. **P6-A** checklist table (structure) — on clipboard Pass 6 section; pointer from ascii.  
2. **P6-P** proposal-rigor table + any wording fixes.  
3. **P6-X** deep accuracy matrix (9 panels + overview attach).  
4. **P6-F** edits only if cited.  
5. **P6-R** receipt with overall verdict.  
6. Plan marked **EXECUTED**.

---

## Tickets

### T0 — Sync / claim

Before execute: ensure Pass 5 is on the branch (`git status`; push/rebase if needed per human) · `git pull --ff-only` · refresh claim `reference-Mapper`.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §5 → M-A* → ascii overview+panels → clipboard P5-P/P5-X → five-stakes PAGE 5/5.

### T2 — PDF gate

Confirm refs/local; never stage. Re-sight §3.1–3.3 / Fig.2 / Table 1 where Cite match is doubtful.

### T3 — P6-A structural analysis of SOURCE MACHINERY

### T4 — P6-P Order Proposal rigor check (+ tighten if overclaim)

### T5 — P6-X deep panel re-walk (all P-A1…A9)

### T6 — P6-F fixes; reconfirm singular overview

### T7 — P6-R receipt · plan EXECUTED · check · commit · release

```
bash docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: P6 tables + any fixes + receipt + plan EXECUTED
bash docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. P6-A checklist complete; every item PASS or OPEN-with-cause.  
2. P6-P shows proposal does not overclaim Core/Φ; FORBIDs explicit; mirror ≤ clipboard strength.  
3. P6-X covers all P-A1…A9 with Implements reverse-walk + fence fidelity; P-A3 still FORBID-ReAG-in-array and non-array-shaped.  
4. Overall verdict recorded (SOUND / SOUND WITH OPENS / BLOCKED).  
5. Exactly one canonical overview; Core/Φ off-board.  
6. No Φ invent; map/law/graphics/Core ASCII/`src/` untouched except cited Shadow edits.  
7. PDF never staged.  
8. Steward test: shoe in hands — refuse “install this systems diagram as Graphic D / Core ASCII” using P6-A + P6-P alone.

---

## Execution (when you say go pass 6)

1. Sync Pass 5 / refresh claim.  
2. T1–T6.  
3. Mark plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
Pass 5 proposed order.  
Pass 6 **cross-examines** that proposal and its diagram.  
If the board leaks toward Core, the proposal is not sound.  
ReAG stays a photograph.  
Decisive judgment stays release/stop — not Φ.  
`[GAP]` remains.
