# Petersen & Zech pass 5 — ASCII as order proposal + accuracy steward

**Status:** PLANNED — not executed. Emission `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute (Pass 4 may still be local — sync/rebase first)  
**Book:** Philipp Petersen & Jakob Zech, *Mathematical theory of deep learning*  
**arXiv:** `2407.18384v4` [cs.LG]  
**ASCII (Pass 4):** `docs/clipboards/petersen-zech-ascii-machinery.md` — SOURCE MACHINERY + P-A1…P-A9 · P4-X 9/9 PASS  
**Mechanisms (Pass 3):** `docs/clipboards/petersen-zech-mechanisms.md` — M-A1…M-A9  
**Clipboard:** `docs/clipboards/petersen-zech-clipboard.md`  
**Sibling (read-only default):** five-stakes PAGE 4/5 · reason-model-map §4 · `systems-manifest-ascii.md` (Core — **do not conflate**)  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → map §4 → Wave A cards + ASCII → Pass 1–4 receipts  

**PDF (gitignored):** `refs/local/petersen-zech-mathematical-theory-deep-learning.pdf` · **never `git add`**

Law: ASCII SOURCE MACHINERY is a **Shadow proposal** for shelf/steward order · not Core · not Φ · train-w ↛ Lace history · NTK ↛ store · Hands win · `[GAP]` sealed.

```
pull/ff (incl. Pass 4 if unpushed) → claim → Hands skim
        → treat Pass 4 SOURCE MACHINERY as ORDER PROPOSAL (Shadow)
        → P5-X accuracy matrix every M-A* ↔ P-A* ↔ overview node
        → P5-F fix drift only where matrix requires
        → P5-O one canonical SOURCE MACHINERY (if duplicates/drift)
        → P5-P write Order Proposal (proposal stamps; FORBID-as-Core)
        → P5-R receipt · plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1–2 | Admit + leftover fences |
| 3 | Wave A mechanism cards |
| 4 | ASCII panels + P4-X rigor |
| **5** | **Steward pass:** (1) keep M ↔ P ↔ overview **in order** (accuracy); (2) adopt the ASCII diagram **as a proposal** for how this shelf stays ordered — **without** promoting it to Core or Φ |

Sibling shelf AgentScope used the same pass-5 shape after ASCII. Wave B remains **default skip**.

Still **Shadow / Layer III**. Not Core. Not `src/`. Not a rewrite of `systems-manifest-ascii.md`.

---

## What “ASCII as a proposal to keep the repo in order” means

The Pass 4 SOURCE MACHINERY board is treated as a **proposal**, not a law:

| Pipeline stage (ASCII) | Proposal for shelf / steward order |
|---|---|
| Input x / observe | Observe what is actually on the shelf (files, receipts, claim) before rewriting |
| Feedforward Φ(x;w) | Name the constructive object under study; do not confuse map with Lace strand |
| Same Φ, many (arch,w) | Keep function≠construction when comparing artifacts / digests |
| Approx / ReLU / depth / high-d | Diagram-check constructions against cards; do not import as Core rooms |
| Interpolation | List reconstruction claims as Shadow only — not POINTER when-rules |
| Train GD/SGD/backprop | Stamp-check: parameter updates are **not** Lace history; refuse history-rewrite proposals |
| NTK / ∞-width | Stamp-check: analysis ≠ second store; refuse Graphic D substitutes |
| FORBIDDEN exit → Core / Φ | Decisive judgment = commit / release / defer / stop — **not** Φ fill |

**Stamps on the proposal itself:**

- **KEEP-read** as steward hygiene for the Petersen–Zech shelf  
- **FORBID-as-Core-write** — must not become array rewrite, second store, or emission rule  
- **FORBID-as-Lace-history** / **FORBID-as-store** retained from M-A8 / M-A9  
- **SILENT-for-Lace-append** — proposal does not schedule Lace append  
- **PROPOSAL** — Hands may refuse without breach of Core  

---

## Already done (do not redo)

| Layer | State |
|---|---|
| M-A1…M-A9 | Pass 3 — algorithms stand |
| P-A1…P-A9 + overview | Pass 4 — diagrams stand; P4-X 9/9 PASS |
| P3-M mesh | CONFIRM×7 · REFINE×2 · CONFLICT×0 |
| PAGE A–F, X · D1–D6 · FM-P* · D7 | Stand |

Pass 5 does **not** invent Wave B unless accuracy is green **and** human opens it (default: **skip**).

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Promote order proposal to Core / AGENTS.md law | Shadow only; Hands may refuse |
| Soften FORBID-as-Lace-history / FORBID-as-store / star≠neuron | Pass 3–4 confirmed |
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Merge into `systems-manifest-ascii.md` | Core board stays separate |
| Rewrite M-A* algorithms or PAGE morals for style | only P5-F drift fixes with matrix row cite |
| Full Wave B–D | default skip |
| Git-add PDF · push without ask · invent `src/` | standing rules |

---

## Pass 5 products

### P5-X — Accuracy matrix (required)

On book clipboard under Pass 5 (pointer from ascii file).

Columns: `overview node · P-A* · M-A* · stamp · Implements OK · Fence OK · Overview OK · Pin OK · PASS/FIX`

Rows: all **M-A1…M-A9 / P-A1…P-A9** (9) plus overview spine nodes (input, forbidden Core/Φ exit).

P4-X PASS is **baseline only** — Pass 5 re-walks (lighter than Pass 6 deep audit, but not rubber-stamp).

### P5-F — Fix drift (required if any FIX)

Only matrix-cited edits to ascii panels / overview / cross-walk (prefer panel fixes over rewriting M-A* bodies). No moral softening.

### P5-O — Canonical SOURCE MACHINERY (required)

Exactly **one** canonical overview fence in `petersen-zech-ascii-machinery.md`. Remove/demote duplicates. Core/Φ stay off-board.

### P5-P — Order Proposal (required · distinctive ask)

Full text on **clipboard** under Pass 5; short mirror under ascii overview.

**Title:** `Order proposal (Shadow) — SOURCE MACHINERY as shelf hygiene`

Must include:

1. Explicit: proposal, not Core / not AGENTS.md amendment  
2. Stage→hygiene table (as above)  
3. Recommended reader file order (proposal only): `clipboard → mechanisms → ascii-machinery → pass plans`  
4. Steward loop: observe → name → diagram-check → stamp-check → list OPENs → decide (commit/release/defer/stop)  
5. Explicit FORBID: Φ scheduler, Lace-history rewrite via training, NTK-as-store, Hands override; SILENT for Lace append  
6. Link to P5-X  

Stamps: KEEP-read · FORBID-as-Core-write · FORBID-as-Lace-history · FORBID-as-store · SILENT-for-Lace-append · PROPOSAL

### P5-R — Receipt

On clipboard: matrix verdict, fixes landed, order-proposal loci, Wave B skipped, Φ still `[GAP]`.

### Optional P5-B — Wave B

**Default skip.**

---

## Tickets

### T0 — Sync / claim

Before execute: ensure Pass 4 is on the branch (rebase/push as human directs) · `git pull --ff-only` · refresh `reference-Mapper`.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §4 → M-A* → P-A* + overview → Pass 1–4 receipts → five-stakes PAGE 4/5.

### T2 — PDF gate

Confirm refs/local; never stage PDF. Re-sight only if a FIX needs a pin.

### T3 — Build P5-X matrix

### T4 — P5-F fixes + P5-O canonical overview

### T5 — Write P5-P Order Proposal (clipboard + ascii mirror)

### T6 — P5-R receipt · mark plan EXECUTED

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: matrix + fixes + order proposal + receipt + plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. P5-X covers all M-A1…A9 / P-A1…A9 (+ overview); each row PASS or fixed to PASS.  
2. Exactly one canonical SOURCE MACHINERY overview; Core/Φ off-board.  
3. P5-P Order Proposal present, stamped proposal/Shadow, FORBID-as-Core-write + FORBID-as-Lace-history + FORBID-as-store + SILENT-for-Lace-append explicit.  
4. Stage→hygiene table and shelf file-order proposal present.  
5. No stamp invents POINTER when/arity/adjacency; Φ still `[GAP]`.  
6. reason-model-map / law / graphics / systems-manifest / Core ASCII / `src/` untouched except clipboard + ascii (+ mechanisms only if P5-F requires).  
7. PDF never staged.  
8. Steward test: shoe in hands — refuse “install this ASCII board as Graphic D / Core” using P5-P + P5-X alone.

---

## Execution (when you say go pass 5)

1. Sync Pass 4 / refresh claim.  
2. T1–T6 (Wave B skip).  
3. Mark plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
Pass 4 drew the board.  
Pass 5 adopts it as **shelf hygiene proposal** and checks the map still matches the cards.  
Training boxes still do not rewrite Lace history.  
NTK boxes still are not a second store.  
Decisive judgment is release/stop — not Φ.  
`[GAP]` remains.
