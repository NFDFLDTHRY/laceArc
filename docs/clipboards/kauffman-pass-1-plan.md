# Kauffman pass 1 plan — dokumen.pub *Knots and Physics* 4ed clipboard

**Status:** EXECUTED — `docs/clipboards/kauffman-4ed-clipboard.md` tightened. Emission `[GAP]`. PDF missing; prior-OCR baseline accepted.  
**Station:** clipboards (released after pass 1 · was `reference-Mapper`)  
**Source slug:** `dokumen.pub_knots-and-physics-4ed-9814383007`  
**Book:** L.H. Kauffman, *Knots and Physics*, 4ed · ISBN family `9814383007`  
**Clipboard to write / verify:** `docs/clipboards/kauffman-4ed-clipboard.md`  
**Prompt:** `docs/prompts/kauffman-clipboard-prompt.md`  
**Hands map already present:** `docs/reason-model-map.md` §1 (K1–K9)  
**Sibling:** `docs/clipboards/clipboard-five-stakes.md` PAGE 1/5 (do not silently amend map from this pass)  
**PDF:** local path expected `artifacts/refs/kauffman-knots-and-physics-4ed.pdf` — **MISSING on this computer as of plan time**. Never `git add` the PDF.

Law on this clip: diagram ≠ rope · hitch kin · grab ≠ emission · `[GAP]` sealed.

```
pull/ff main → shoe → graphics A–D → systems-manifest → staking / law-why
                 → reason-model-map §1
                 → Kauffman 4ed (attachment / local only; no git add)
                 → PAGE A → B → C → D → E → F → X
                 → kauffman-4ed-clipboard.md
                 → STOP
```

---

## What already exists (do not pretend blank)

| Artifact | State |
|---|---|
| reason-model-map §1 K1–K9 | Filled. Hitch kin; closed-loop / Reidemeister / slide / virtual conflict; emission silent. |
| Pass 1 Pieces×five-texts table | Kauffman column **Done** (map § Pass 1). This plan is the *book clipboard* pass, not a rewrite of that table. |
| `kauffman-4ed-clipboard.md` | Filled PAGE A–F, X from prior OCR recovery. Image-only PDF (`pypdf`/`pypdfium2` = 0 chars on sample). |
| five-stakes PAGE 1/5 | Anchored; notes transcription conflict on `T2 ≤ κ T1` vs strict `<`. |
| PDF on disk here | **Absent.** Pass 1 execute needs the PDF restored under `artifacts/refs/` (gitignored) or an attached OCR dump. |

Pass 1 is therefore a **verify-and-tighten** of the Kauffman clipboard against Hands, not a greenfield invent.

---

## Pass 1 product

Update (or confirm unchanged) `docs/clipboards/kauffman-4ed-clipboard.md` only:

- Overview strip: Shadow, Hands win, emission `[GAP]`, never git-add PDF
- PAGE A Hitches — KEEP hitch image; FORBID friction → array fields
- PAGE B Diagram ≠ rope — projection only
- PAGE C Reidemeister / closed loop — CONFLICT with open strand / Rule Zero
- PAGE D Slide equivalence — CONFLICT (implies RII/RIII class)
- PAGE E Virtual / 1-handles — CONFLICT (extra rooms)
- PAGE F “When to point” — NONE FOUND; false friends listed; do not invent when/arity/adjacency
- PAGE X Cross-cut — temptations vs Hands deadbolts; Φ still `[GAP]`

Each page: locus · quote/eye · ON SHOE · KEEP · FORBID · EMISSION? · vs reason-model-map (confirms|refines|conflicts|silent).

Stop. Do not edit `reason-model-map.md`, law, graphics, or five-stakes unless a later ticket names them and the owning station is claimed.

---

## Tickets

### T0 — Claim / base (done at plan time)

- `git pull --ff-only origin main`
- `./docs/coord/coord.sh claim clipboards "reference-Mapper"`
- BASE recorded on station file

### T1 — Hands first

Read in order: shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §1.  
Cite graphic or map id on every KEEP/FORBID. If the picture is silent → `[GAP]`.

### T2 — Source availability

| Need | Action |
|---|---|
| PDF under `artifacts/refs/` | Human places file (or attach OCR). Blocked without it if re-walk required. |
| Image-only scan | Loci may stay prior-OCR; label “not a fresh byte-read of every page.” |
| Text layer appears later | Re-walk; do not pretend this pass re-OCR’d. |

### T3 — Page walk A→X

Fill or verify each PAGE against §1 K1–K9:

| PAGE | Map ids | Expected stamp |
|---|---|---|
| A | K1, K7 | kin / CONFIRMS |
| B | K3 | CONFIRMS diagram≠rope |
| C | K2, K4 | CONFLICT |
| D | K8 | CONFLICT |
| E | K9 | CONFLICT |
| F | (none) | silent + false friends (grab, RII, Gauss, DNA) |
| X | cross-cut | Φ `[GAP]` |

Record the five-stakes transcription note if still true: printed `T2 ≤ κ T1` vs clipboard strict `<` — log, do not “fix” the map.

### T4 — False friends

| Looks like | Must not become |
|---|---|
| clove “grab” / later tension | POINTER emission / when-rule |
| Reidemeister II | append / pointer write |
| slide axioms | milder Core write |
| virtual 1-handles | second store / extra rooms |
| Gauss / bracket / Jones | history of wraps on Graphic D |
| DNA strand passage | cut-join on the lace |
| friction \(T_2,\kappa,e^\theta\) | array fields |

### T5 — Forbidden fills

- Invent when / arity / adjacency for POINTER
- Add `src/`
- Git-add the PDF
- Treat knot diagram as Hands authority
- Amend reason-model-map or law from clipboard convenience
- Schedule a routine
- Push unless human says push

---

## Execution (when you say go pass 1)

1. Confirm PDF or accepted prior-OCR baseline.  
2. Re-read Hands chain (T1).  
3. Rewrite only `docs/clipboards/kauffman-4ed-clipboard.md` to match product above.  
4. `./docs/coord/coord.sh check clipboards "reference-Mapper"`  
5. Commit docs under claim (ask before push).  
6. `./docs/coord/coord.sh release clipboards "reference-Mapper"`  
7. STOP.

---

## Steward test

If you cannot do it to a shoe lace in your hands, it stays out of Core.  
If Kauffman never wrote the missing emission sentence, PAGE F stays silent — do not finish it from a clove hitch.  
Shoe in hands, or no.


---

## Execution result

- Hands chain re-read (graphics README, systems-manifest Pieces 1/5/13, staking, law-why, reason-model-map §1).
- Local PDF still missing; prior-OCR baseline accepted on human go.
- Only `docs/clipboards/kauffman-4ed-clipboard.md` rewritten (pass-1 receipt, K5 false friend, ≤/&lt; note, diagram-authority deadbolt).
- reason-model-map / law / graphics / five-stakes untouched.
- Φ / POINTER when·arity·adjacency remain `[GAP]`.
- Station `clipboards` released after check.

