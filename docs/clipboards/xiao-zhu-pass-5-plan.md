# Xiao & Zhu pass 5 — ASCII as order proposal + accuracy steward

**Status:** **HELD** / **NOT EXECUTED** — ready for execute. Emission `[GAP]`. Tokenizer `[GAP]`.  
**Station:** clipboards (HELD `xiao-zhu pass-5 plan` at plan time; refresh claim before execute)  
**Base at plan:** `44a3af4` (origin/main after Pass 4 tip `6270270` + clock pins) · refresh to current `origin/main` before execute  
**Book:** Tong Xiao & Jingbo Zhu, *Foundations of Large Language Models*  
**arXiv:** `2501.09223v2` [cs.CL] · 15/17 Jun 2025  
**License:** CC BY-NC 4.0 — cite; do not fold body into Apache Core  
**ASCII (Pass 4):** `docs/clipboards/xiao-zhu-foundations-ascii-machinery.md` — SOURCE MACHINERY + P-A1…P-A9 · P4-X **9/9 PASS**  
**Mechanisms (Pass 3):** `docs/clipboards/xiao-zhu-foundations-mechanisms.md` — M-A1…M-A9  
**Clipboard:** `docs/clipboards/xiao-zhu-foundations-clipboard.md` (Pass 1–4 EXECUTED; P3-M CONFIRM×9 · CONFLICT×0)  
**Sibling (read-only default):** five-stakes PAGE 3/5 · reason-model-map §3 X1–X7 · `systems-manifest-ascii.md` (Core — **do not conflate**)  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → map §3 → Wave A cards + ASCII → Pass 1–4 receipts  
**Ancestors:** [`xiao-zhu-pass-4-plan.md`](xiao-zhu-pass-4-plan.md) · Pass 1–3 plans  
**Mirrors:** [`agentscope-pass-5-plan.md`](agentscope-pass-5-plan.md) · [`petersen-zech-pass-5-plan.md`](petersen-zech-pass-5-plan.md)

**PDF (gitignored):** `refs/local/xiao-zhu-foundations-llm.pdf` → attachment `0f5d145d298a90bbe1648b365d97b8833e86be3c31bedd7db900a932ba50bc1a` · **277** pp. · printed ≈ PDF − 7 · **never `git add`**

Law: ASCII SOURCE MACHINERY is a **Shadow proposal** for shelf/steward order · not Core · not Φ · intern/MASK/embed/next-token/decode ≠ Core · soft/SFT/RLHF steward-only · KV ≠ POINTER when · tokenizer `[GAP]` · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → treat Pass 4 SOURCE MACHINERY as ORDER PROPOSAL (Shadow)
        → P5-X accuracy matrix every M-A* ↔ P-A* ↔ overview node
        → P5-F fix drift only where matrix requires
        → P5-O one canonical SOURCE MACHINERY (if duplicates/drift)
        → P5-P write Order Proposal (proposal stamps; FORBID-as-Core)
        → P5-R receipt · plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1–2 | Admit + leftover fences |
| 3 | Wave A mechanism cards |
| 4 | ASCII panels + P4-X rigor |
| **5** | **Steward pass:** (1) keep M ↔ P ↔ overview **in order** (accuracy); (2) adopt the ASCII diagram **as a proposal** for how this shelf stays ordered — **without** promoting it to Core or Φ |

Sibling shelves AgentScope and Petersen–Zech used the same pass-5 shape after ASCII. Wave B remains **default skip**. Pass 6 deep re-audit is **not** this pass.

Still **Shadow / Layer III**. Not Core. Not `src/`. Not a rewrite of `systems-manifest-ascii.md`. This book remains the **anti-model of ingest**.

---

## What “ASCII as a proposal to keep the repo in order” means

The Pass 4 SOURCE MACHINERY board is treated as a **proposal**, not a law:

| Pipeline stage (ASCII) | Proposal for shelf / steward order |
|---|---|
| corpus D + self-sup task | Observe what is actually on the shelf (files, receipts, claim) before rewriting; refuse corpus→DOCUMENT fuel |
| tokenize → shared vocab V | Name book primitives honestly; **FORBID** vocab id as Lace WORD/Star identity; tokenizer stays `[GAP]` |
| e = x + e_pos (+ e_seg) | Diagram-check constructions against cards; refuse embed tables as second store beside Graphic D |
| causal next-token / BERT MASK | Stamp-check training/corruption objectives — not Piece 2 continue / not Lace cut |
| soft / SFT / RLHF\|DPO | Steward adaptation only — **FORBID** as Core rows / Core write / Lace history |
| prefill → decode / KV | Projection / inference memory only — **FORBID** as Core continue / POINTER when; SILENT for Lace append |
| FORBIDDEN exit → Core / Φ / tokenizer | Decisive judgment = commit / release / defer / stop — **not** Φ fill / Lace tokenizer invent |

**Stamps on the proposal itself:**

- **KEEP-read** as steward hygiene for the Xiao–Zhu shelf  
- **FORBID-as-Core-write** — must not become array rewrite, second store, or emission rule  
- **FORBID-as-Lace-identity** / **FORBID-as-store** / **FORBID next-token/decode as Core continue** / **FORBID MASK as Lace cut** / **FORBID-as-POINTER-when** / **FORBID corpus→DOCUMENT fuel** / **FORBID as Core rows** / **FORBID-as-Lace-history** retained from Wave A  
- **SILENT-for-Lace-append** — proposal does not schedule Lace append  
- **PROPOSAL** — Hands may refuse without breach of Core  

This is how the reference’s LLM pipeline **proposes** repo/shelf order. It does not own Lace.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| M-A1…M-A9 | Pass 3 — algorithms stand |
| P-A1…P-A9 + overview | Pass 4 — diagrams stand; P4-X 9/9 PASS |
| P3-M mesh | CONFIRM×9 · REFINE×0 · CONFLICT×0 |
| PAGE A–F, X · D1–D6 · FM-X* · D7 | Stand |
| Five-stakes PAGE 3/5 | Agrees; default skip unless CONFLICT |

Pass 5 does **not** invent Wave B unless accuracy is green **and** human opens it (default: **skip**).

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Promote order proposal to Core / AGENTS.md law | Shadow only; Hands may refuse |
| Soften FORBID-as-Lace-identity / FORBID-as-store / next-token/decode / MASK≠cut / POINTER-when / corpus→DOCUMENT / Core rows / Lace-history | Pass 3–4 confirmed |
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Invent a Lace tokenizer (close PAGE F) | tokenizer `[GAP]`; opening ≠ filling |
| Merge into `systems-manifest-ascii.md` | Core board stays separate |
| Rewrite M-A* algorithms or PAGE morals for style | only P5-F drift fixes with matrix row cite |
| Full Wave B–D | default skip |
| Pass 6 deep re-audit | not this pass (P5-X is steward re-walk, not P6) |
| Five-stakes PAGE 3 rewrite | unless P5-X finds CONFLICT (default skip) |
| Amend reason-model-map / law / graphics / systems-manifest | wrong stations |
| `src/` or other-shelf edits | wrong Shadow shelf |
| Fold CC BY-NC body into Apache Core | license |
| Git-add PDF · push without ask | standing rules |
| Mark ascii Pass 5 EXECUTED / write Order Proposal body at **plan** time | plan only — execute writes P5-P/P5-R |

---

## Pass 5 products

### P5-X — Accuracy matrix (required)

On book clipboard under Pass 5 (pointer from ascii file).

Columns: `overview node · P-A* · M-A* · stamp · Implements OK · Fence OK · Overview OK · Pin OK · PASS/FIX`

Rows: all **M-A1…M-A9 / P-A1…P-A9** (9) plus overview spine nodes (corpus input; FORBIDDEN Core/Φ/tokenizer exit).

Checks (re-walk; do not rubber-stamp P4-X):

- **Stamp match** — panel stamp equals M-A* hard FORBIDs  
- **Algorithm fidelity** — Implements reverse-walks M-A* steps  
- **Fence book-only** — book glyphs only; **no POINTER word** inside any diagram fence  
- **False-friend** — panel does not depict FM-X* confusion  
- **Overview attach** — every P-A* attaches to the singular SOURCE MACHINERY spine  

P4-X 9/9 PASS is **baseline only**.

### P5-F — Fix drift (required if any FIX)

Only matrix-cited edits to ascii panels / overview / cross-walk (prefer panel fixes over rewriting M-A* bodies; mechanisms only if Implements text truly drifted). No moral softening. No Core / map / law / graphics / systems-manifest / `src/` edits.

### P5-O — Canonical SOURCE MACHINERY (required)

Exactly **one** canonical overview fence in `xiao-zhu-foundations-ascii-machinery.md`. Remove/demote duplicates. Overview must match P5-X PASS rows. Lace Core, Φ, and Lace tokenizer stay **off-board**.

### P5-P — Order Proposal (required · distinctive ask)

**Execute only** — do **not** write the Order Proposal body at plan time beyond this plan’s stage→hygiene seed.

Full text on **clipboard** under Pass 5; short mirror under ascii overview.

**Title:** `Order proposal (Shadow) — SOURCE MACHINERY as shelf hygiene`

Must include:

1. Explicit: proposal, not Core / not AGENTS.md amendment  
2. Stage→hygiene table (as above — Xiao–Zhu spine)  
3. Recommended reader file order (proposal only):  
   `clipboard (admit) → mechanisms (algorithms) → ascii-machinery (diagrams) → pass plans (history)`  
4. Steward loop: observe → name → diagram-check → stamp-check → list OPENs → decide (commit/release/defer/stop)  
5. Explicit FORBID: Φ scheduler; Lace tokenizer invent; vocab-as-identity; embed/KV-as-store; next-token/decode as Core continue; MASK as Lace cut; soft/SFT/RLHF as Core rows/write/history; Hands override; promoting proposal into Core / AGENTS.md / systems-manifest-ascii / Graphic D. **SILENT for Lace append**  
6. Link to P5-X  

Stamps: KEEP-read · FORBID-as-Core-write · FORBID-as-Lace-identity · FORBID-as-store · FORBID next-token/decode as Core continue · FORBID MASK as Lace cut · FORBID-as-POINTER-when · FORBID corpus→DOCUMENT · FORBID as Core rows · FORBID-as-Lace-history · SILENT-for-Lace-append · PROPOSAL

**NON-binding:** Order Proposal does **not** rewrite Core, `systems-manifest-ascii.md`, graphics, or `src/`.

### P5-R — Receipt

On clipboard: matrix verdict, fixes landed, order-proposal loci, Wave B skipped, Φ still `[GAP]`, tokenizer still `[GAP]`.

### Optional P5-B — Wave B

**Default skip.** Only if P5-X all PASS and human asks in-execute.

---

## Tickets

### T0 — Sync / claim

Before execute: `git pull --ff-only` · refresh claim (`reference-Mapper` or equivalent) if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §3 (X1–X7) → M-A* → P-A* + overview → Pass 1–4 receipts → five-stakes PAGE 3/5 (read-only).

### T2 — PDF gate

Confirm `refs/local/xiao-zhu-foundations-llm.pdf` · SHA-256 `0f5d145d…ba50bc1a` · 277 pp · `git check-ignore` · never stage. Re-sight only if a FIX needs a pin.

### T3 — Build P5-X matrix

### T4 — P5-F fixes + P5-O canonical overview

### T5 — Write P5-P Order Proposal (clipboard + ascii mirror)

### T6 — P5-R receipt · mark plan EXECUTED

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: matrix + fixes + order proposal + receipt + plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push. Do **not** push unless asked.

---

## Acceptance tests (execute must pass)

1. P5-X covers all M-A1…A9 / P-A1…A9 (+ overview spine); each row PASS or fixed to PASS.  
2. Exactly one canonical SOURCE MACHINERY overview; Core/Φ/tokenizer off-board.  
3. P5-P Order Proposal present, stamped proposal/Shadow, FORBID-as-Core-write + identity/store/continue/MASK/POINTER-when/DOCUMENT/Core-rows/Lace-history + SILENT-for-Lace-append explicit.  
4. Stage→hygiene table and shelf file-order proposal present.  
5. No stamp invents POINTER when/arity/adjacency; Φ and tokenizer still `[GAP]`.  
6. reason-model-map / law / graphics / systems-manifest / Core ASCII / `src/` / five-stakes untouched except clipboard + ascii (+ mechanisms only if P5-F requires).  
7. PDF never staged; CC BY-NC body not pasted into Core.  
8. Steward test: shoe in hands — refuse “install this ASCII board as Graphic D / Core” and “intern vocab as Lace WORD” and “MASK the strand” and “decode as POINTER when” using P5-P + P5-X alone.

---

## Execution (when you say go pass 5)

1. Sync / refresh claim.  
2. T1–T6 (Wave B skip).  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop. Do **not** push unless asked. Do **not** invent Wave B or Pass 6.

---

## Steward test

Shoe in hands, or no.  
Pass 4 drew the board.  
Pass 5 adopts it as **shelf hygiene proposal** and checks the map still matches the cards.  
Corpus boxes still do not fuel DOCUMENT.  
Vocab-intern boxes still are not Lace WORD identity.  
Embed / KV boxes still are not a second store.  
Next-token / MASK / decode boxes still are not continue / cut / POINTER when.  
Soft / SFT / RLHF boxes still are steward-only.  
Decisive judgment is release/stop — not Φ.  
Tokenizer stays `[GAP]`. Emission stays `[GAP]`.

---

## Plan receipt (HELD — NOT EXECUTED)

| Field | Value |
|---|---|
| Status | **HELD** / **NOT EXECUTED** |
| When (plan) | 2026-09-18 ~19:35 PT |
| Station | clipboards · claim `xiao-zhu pass-5 plan` |
| Base (plan) | `44a3af4` |
| Artifact at plan | plan file only — **no** Order Proposal body · **no** ascii Pass 5 EXECUTED mark |
| P5 scope | P5-X matrix · P5-F if FIX · P5-O singular overview · P5-P Order Proposal (Shadow) · P5-R receipt |
| Wave B | **SKIP** (default) |
| Pass 6 | not this pass |
| Emission | `[GAP]` |
| Tokenizer | `[GAP]` |
| Push | **not** performed |
