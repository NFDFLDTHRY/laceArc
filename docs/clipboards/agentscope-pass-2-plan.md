# AgentScope pass 2 — leftovers / failure modes / coherence (PDF present)

**Status:** EXECUTED — 2026-09-18 ~17:33 PT. Emission still `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Paper:** Jiayi Bi et al., *Diagnosing with Insights: Structured Analysis of Agent Failures via Behavioral Abstractions*  
**arXiv:** `2609.02371v1` (cs.AI) · tool name **AGENTSCOPE**  
**Clipboard under test:** `docs/clipboards/agentscope-clipboard.md`  
**Ancestor:** [`agentscope-pass-1-pdf-plan.md`](agentscope-pass-1-pdf-plan.md) (EXECUTED · PDF-sighted A–F, X)  
**Sibling (read-only unless T5 opens):** `docs/clipboards/clipboard-five-stakes.md` PAGE 5/5  
**Hands / map:** shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §5 (A1–A5)  

**PDF (this machine):**

| Field | Value |
|---|---|
| Working link (gitignored) | `refs/local/bi-et-al-agentscope-diagnosing-insights.pdf` |
| Attachment | `6464d69d81d4265aff0cab1e1affe160ef7af4c77ccc142436528f75ea3fd1e9.pdf` |
| Pages | **22** · text layer yes · printed ↔ PDF coincide at cited loci |
| Git | **Never `git add` the PDF** |

Law: failure taxonomy is **holder** vocabulary · ReAG ↛ 1D array · neural invariants ≠ Core proofs · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add)
        → do NOT redo A–X fill or invent emission
        → Pass 2 deltas only: D1–D6 (+ optional D7)
        → receipt on book clipboard
        → check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 (PDF) | **Admission + locus bridges** — A–F, X stamped against this attachment |
| **2** | **Leftovers** — what the clipboard is not; KEEP/FORBID strip; failure modes with PDF pins; interface coherence; source gate cite; sealed |

Same shape as Rowlands / Kauffman pass 2 (D1–D6), now that the PDF is already present (Pass 1 gate is done).

Still **Shadow / Layer III**. Not Core. Not `src/`. Not Kauffman / Rowlands shelves.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–F, X morals | Filled; Pass 1 CONFIRM/REFINE/SILENT stamps |
| PDF gate | Present; 22 pp.; text layer; gitignored; SHA matches header |
| Per-page KEEP/FORBID | Already on A–E (and X cross-cut) |
| Five-stakes PAGE 5/5 | Agrees; T5 skipped in Pass 1 |
| reason-model-map §5 A1–A5 | Untouched (stand) |

Pass 2 does **not** re-walk every page unless a delta hunt needs a fresh pin.

---

## What is still flat / missing

1. **No “what this clipboard is not” strip** — readers can mistake it for Core / emission law / sixth graphic / AGENTS.md replacement.  
2. **No compact ON-SHOE / KEEP / FORBID / MAP strip** mirroring five-stakes PAGE 5/5 on the book clipboard itself (per-page cards exist; spine card does not).  
3. **No numbered failure-mode table (FM-A\*)** with PDF pins (false friends are narrative only on F/X).  
4. **No interface-coherence paragraph** stating which PAGE outputs may touch Hands inputs (holder diagnosis only; nothing writes Graphic D).  
5. **Source gate** is in Pass 1 receipt — Pass 2 should **cite** it, not claim PDF absent.  
6. **Optional:** one Shadow constructive miniature of ReAG build or diagnosis pipeline stamped FORBID-as-Core-write — algorithm on paper, not Lace append.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Soften A2 FORBID (ReAG-in-array) or A3/A4 holder boundary | Pass 1 confirmed |
| Import AGENTSCOPE runtime / AgentErrata as Core schema | off-shoe |
| Amend reason-model-map / law / graphics | wrong stations |
| Rewrite five-stakes PAGE 5 morals | default skip; optional one-line receipt only if T5 opens |
| Add `src/` or edit Kauffman/Rowlands mechanism files | wrong Shadow shelf |
| Git-add PDF | gitignore |
| Push without ask | standing rule |
| Confuse this paper with other products named AgentScope | clipboard + map §5 bind to **this** arXiv id |

---

## Pass 2 product (deltas D1–D6)

Edit **only** `docs/clipboards/agentscope-clipboard.md` unless T5 opens five-stakes.

### D1 — What this clipboard is not

Short sealed list:

- Not Core / not a sixth graphic  
- Not `pointer-emission.md` / not a Φ fill  
- Not a rewrite of reason-model-map §5  
- Not a replacement for AGENTS.md or agent-control  
- Not permission to store ReAG / ISR / neural-invariant text in Graphic D  
- Not an import of AgentErrata / Who&When scores as Lace correctness  

### D2 — ON-SHOE / KEEP / FORBID / MAP strip

Mirror five-stakes PAGE 5/5 on the book clipboard (compact card). Must match Pass 1 morals:

| KEEP | FORBID |
|---|---|
| failure taxonomy for the HOLDER (A4) | taxonomy / flags as Core types |
| trajectory as steward observation (A1) | trace order as Lace arrival order |
| ReAG as diagnostic picture (A2) | **ReAG rows in the 1D array** |
| neural predicates over holder traces (A3) | invariant text as stored meaning / infallible proof |

Map arrows: A4 → [H] steward review · A1 → observation only · A2 ──[X]──► Piece 5/6 · A3 → adjacent diagnosis · emission `[GAP]`.

### D3 — Failure modes (FM-A1…A9)

Table with columns: ID · Misread · Hands deadbolt · PDF pin (from Pass 1 or fresh `pdftotext`).

Minimum rows:

| ID | Misread seed |
|---|---|
| FM-A1 | failure taxonomy ⇒ Core types / WORD kinds |
| FM-A2 | trajectory step order ⇒ Lace arrival order |
| FM-A3 | ReAG vertex ⇒ WORD; ReAG edge ⇒ POINTER |
| FM-A4 | ISR / trajectory memory ⇒ Lace store |
| FM-A5 | neural invariant text ⇒ Core proof / stored meaning |
| FM-A6 | LLM judge verdict ⇒ acceptance / Hands override |
| FM-A7 | AgentErrata / Who&When metrics ⇒ Lace correctness score |
| FM-A8 | “decisive error” / first-mistake selection ⇒ POINTER when-rule |
| FM-A9 | appendix / 22-page remainder ⇒ hidden Core piece / Φ fill |

Each row needs a PDF/printed pin (reuse Pass 1 bridges where valid).

### D4 — Interface coherence

One short paragraph: only A1/A4 outputs are kin to Hands inputs (observe the steward; label breaches with evidence). A2/A3 are diagnostic pictures and predicates over the holder — they do not match any Core write. Nothing in the paper supplies POINTER when/arity/adjacency — `[GAP]` remains. Diagnosis ↛ strand operation.

### D5 — Source gate (cite Pass 1)

Do **not** narrate PDF-absent. Cite Pass 1 receipt: PDF present, 22 pp., text layer, printed↔PDF coincide, path `refs/local/bi-et-al-agentscope-diagnosing-insights.pdf`, attachment `6464d69d…fd1e9`, never git-add.

### D6 — Sealed

Repeat sealed list: invent Φ · amend map/law/graphics · `src/` · git-add PDF · treat AGENTSCOPE tooling as Lace Core · pretend OCR without the file.

### Optional D7 — Constructive miniature (Shadow only)

If time: one numbered paper algorithm for ReAG construction (§3.1 / §3.3) or staged diagnosis (§3.3), stamped **FORBID-as-Core-write** and **SILENT for Lace append**. Not a Graphic D scheduler. Mark choice points `[OPEN]`. Skip entirely rather than invent steps the paper does not state.

---

## Tickets

### T0 — Claim / base

Held at plan by `reference-Mapper`. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → reason-model-map §5 → Pass 1 clipboard → five-stakes PAGE 5/5.

### T2 — PDF gate

Confirm `refs/local/bi-et-al-agentscope-diagnosing-insights.pdf` resolves; `git check-ignore`; abort if PDF staged.

### T3 — Write D1–D2–D4–D5–D6

Prose deltas; no A–X moral rewrite. Prefer append after Pass 1 receipt / before PAGE A (or a dedicated Pass 2 section at top of body — match Rowlands/Kauffman clipboard layout if one exists; else after Pass 1 receipt).

### T4 — FM-A\* hunt (D3)

Reuse Pass 1 pins; `pdftotext` only where a row lacks a pin. ≤2-line eye-quote per new pin.

### T5 — Five-stakes (default skip)

Only if human asks: one receipt line “book clipboard Pass 2 deltas landed”; do not rewrite PAGE 5 KEEP/FORBID.

### T6 — Optional D7 miniature

Default **include if ≤30 min**; else defer with note under Pass 2 receipt.

### T7 — Check / commit / release

```
bash docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: clipboard D1–D6 (+ D7 if any) + this plan EXECUTED
bash docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Pass 2 receipt on the AgentScope clipboard.  
2. D1–D6 present; D5 says PDF **present** (not absent).  
3. FM-A1…A9 each have Hands deadbolt + PDF/printed pin (or `[OPEN]` with cause).  
4. No stamp invents POINTER when/arity/adjacency.  
5. A2 FORBID (ReAG-in-array) and A1/A3/A4 holder boundaries not softened.  
6. reason-model-map / law / graphics / systems-manifest untouched; PDF never staged.  
7. If D7 present: miniature is Shadow-only with FORBID-as-Core-write.  
8. Steward test: shoe in hands — can you refuse “store the ReAG” using only D2+D3?

---

## Execution receipt

| Field | Value |
|---|---|
| Status | **EXECUTED** |
| When | 2026-09-18 ~17:33 PT |
| Agent | `reference-Mapper` |
| Station | clipboards (refreshed → check OK → release after commit) |
| Product | D1–D6 + D7 on `agentscope-clipboard.md`; A–X morals untouched |
| T5 | skip (default) |
| FM pins | FM-A1…A9 all pinned (Pass 1 reuse; no `[OPEN]`) |
| Amended | reason-model-map · law · graphics · five-stakes · src/ · systems-manifest | **No** |
| PDF | present; never staged |
| Emission | still `[GAP]` |
| Push | **not** pushed |

Steps done: refresh claim → Hands skim → PDF gate → D1–D7 write → this plan EXECUTED → check · commit · release · STOP.

---

## Steward test

Shoe in hands, or no.  
Failure labels diagnose the **holder**, not the lace.  
ReAG is a photograph of steward steps — not Graphic D.  
Neural “invariants” are LLM-judged checks — not Core proofs.  
Metrics measure diagnosis tools — not Lace correctness.  
Φ stays `[GAP]`.
