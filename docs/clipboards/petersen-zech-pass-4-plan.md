# Petersen & Zech pass 4 — rigorous ASCII map of Wave A system mechanisms

**Status:** **EXECUTED** — Pass 4 complete. Emission `[GAP]`.
**Execution receipt:** artifact `docs/clipboards/petersen-zech-ascii-machinery.md` (overview + P-A1…P-A9); P4-X 9/9 PASS · FIX 0; clipboard Pass 4 receipt; M-A* algorithms unchanged; Core ASCII / map / law / graphics / src untouched; PDF never staged.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Philipp Petersen & Jakob Zech, *Mathematical theory of deep learning*  
**arXiv:** `2407.18384v4` [cs.LG] · cover 16 Jan 2026  
**Mechanisms (done):** `docs/clipboards/petersen-zech-mechanisms.md` — Wave A M-A1…M-A9  
**Clipboard:** `docs/clipboards/petersen-zech-clipboard.md` (Pass 1–3 EXECUTED; P3-M CONFIRM×7 · REFINE×2 · CONFLICT×0)  
**New artifact:** `docs/clipboards/petersen-zech-ascii-machinery.md` — SOURCE MACHINERY overview + panels P-A1…P-A9  
**Sibling (read-only):** five-stakes PAGE 4/5 · reason-model-map §4 P1–P7  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → map §4 → mechanisms Wave A → Pass 3 clipboard  

**PDF (gitignored):** `refs/local/petersen-zech-mathematical-theory-deep-learning.pdf` → SHA `a8505f3de280cb1c6f9742161471fadc52c21340afc05efe4549bd58c84383fa` · **333** pp. · text layer · **never `git add`**

Law: function ≠ construction · star ≠ neuron · train-w ≠ Lace history · NTK ↛ store · Hands win · Φ `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
        → open PDF (refs/local; no git add)
        → rigorously re-read each M-A* algorithm (do NOT rewrite unless typo/pin)
        → forge SOURCE MACHINERY overview (book side only; Core/Φ off-board)
        → for each P-A*: ASCII panel implementing M-A* steps with matching stamp
        → P4-X implements/fence/overview check table (rigor)
        → cross-walk P↔M↔stamp↔P#↔PAGE; Pass 4 receipt on clipboard
        → mark plan EXECUTED · check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 | PDF-sighted PAGE A–F, X |
| 2 | Leftovers D1–D6 + FM-P* + D7 |
| 3 | Wave A **mechanism cards** M-A1…M-A9 + P3-M mesh |
| **4** | **Rigorous analysis + ASCII map** of those mechanisms — one SOURCE MACHINERY board + panels P-A1…P-A9 tied 1:1 to M-A*, with an implements/fence rigor check |

Pass 3 deferred ASCII. Pass 4 delivers diagrams **and** a first rigor check that each panel actually implements its card. Still **Shadow / Layer III**. Not Core. Not `src/`. Not `systems-manifest-ascii.md`.

---

## What “rigorous ASCII of system mechanisms” means here

Each panel is a **diagram of the book’s procedure**, not of Lace:

1. Cites its M-A* card and implements that card’s numbered algorithm steps (reverse-walkable)  
2. Uses **book-side glyphs only inside the fence** (Φ, layers, ReLU, pieces, width, GD step, NTK, …)  
3. Carries the **same Lace stamp** as the card  
4. Does **not** place `WORD` / `POINTER` / Graphic D rows inside the fence as if they were book objects  
5. Does **not** close Φ  
6. Survives P4-X: Implements OK · Fence OK · Stamp OK · Overview attach OK

---

## Already done (do not redo)

| Layer | State |
|---|---|
| M-A1…M-A9 cards | Pass 3 EXECUTED — algorithms stand |
| P3-M mesh | CONFIRM×7 · REFINE×2 · CONFLICT×0 |
| PAGE A–F, X + Pass 2 D1–D6 / FM-P* / D7 | Stand |
| Five-stakes PAGE 4/5 | Agrees; default skip |

Pass 4 **diagrams** Wave A; it does not rewrite card morals or invent Wave B panels (seeds only).

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ `[GAP]` |
| Soften M-A8 FORBID-as-Lace-history or M-A9 FORBID-as-store | Pass 3 confirmed |
| Soften star≠neuron / Φ≠build | Pass 1–3 confirmed |
| Put Lace Core ASCII on this board | Core board = `systems-manifest-ascii.md` — do not conflate |
| Rewrite M-A* algorithm bodies (except cited typo/pin fix) | cards are algorithm source |
| Wave B–D full panels | deferred seeds only |
| Amend reason-model-map / law / graphics | wrong stations |
| Git-add PDF · invent `src/` · push without ask | standing rules |

---

## Artifact — `petersen-zech-ascii-machinery.md`

### Opening contract (≤8 lines)

1. Models **Petersen–Zech book procedures**, not Lace Core.  
2. Companion to `petersen-zech-mechanisms.md` + book clipboard.  
3. Lace Core ASCII = `docs/systems-manifest-ascii.md` — **do not conflate**.  
4. Emission Φ = `[GAP]`. No panel closes it.  
5. Stamps match Wave A cards (function≠construction · FORBID train-w-as-history · FORBID NTK-as-store · …).

### Stamp legend

Reuse Pass 3 legend from mechanisms file (KEEP-read, FORBID-as-Core-types, FORBID-as-Lace-history, FORBID-as-store, FORBID-as-Core-write, SILENT-for-Lace-append, FORBID-as-POINTER-when, …).

### Panel schema (mandatory · P-A* ↔ M-A*)

| Field | Required |
|---|---|
| Mechanism cite | M-A* + PDF locus |
| Sighting | `pdftotext` and/or Pass 1–3 pin reuse |
| Lace stamp | same tooth as M-A* |
| Map cite | P1–P7 |
| False friend | FM-P* |
| Implements | which M-A* algorithm steps the diagram shows |
| Fenced ASCII | ≤76 cols; book glyphs only inside fence |
| Stamp line | under fence |

### SOURCE MACHINERY overview (required · book side only)

One top-level board of the book’s constructive spine (exact art free; Core/Φ off-board), e.g.:

```
  [input x]
       |
       v
  [feedforward Phi(x;w)] ----------- M-A1 / P-A1
       |
       +--> [same Phi, many (arch,w)] -- M-A2 / P-A2   KEEP fn≠construction
       |
       v
  [approx / ReLU pieces / depth] --- M-A3 / M-A4 / M-A5
       |                             P-A3 / P-A4 / P-A5
       v
  [high-d / compositionality] ------ M-A6 / P-A6
       |
       v
  [interpolation / reconstruction] - M-A7 / P-A7
       |
       v
  [train: GD/SGD/backprop on w] ---- M-A8 / P-A8   FORBID as Lace history
       |
       v
  [NTK / infinite-width limit] ----- M-A9 / P-A9   FORBID as store
       |
       x --> Lace Core / Graphic D / Phi
             FORBIDDEN off this board

  Lace Core OFF this board --> docs/systems-manifest-ascii.md
  Phi = [GAP]
```

### Wave A panels (blocking · 1:1 with cards)

| Panel | Mechanism | Stamp seed |
|---|---|---|
| **P-A1** | M-A1 Feedforward Def 2.1 | KEEP-read; FORBID star=neuron (P1) |
| **P-A2** | M-A2 Remark 2.2 fn≠construction | KEEP-read (P5); FORBID Φ-sameness as Lace identity |
| **P-A3** | M-A3 Universal approximation | KEEP-read existence; FORBID approx⇒possession |
| **P-A4** | M-A4 ReLU / CPWL calculus | KEEP-read; FORBID as Core ops |
| **P-A5** | M-A5 Affine pieces / depth | KEEP-read; FORBID depth/width⇒Lace rooms |
| **P-A6** | M-A6 High-d / Barron / compositionality | KEEP-read; FORBID as Core dimension axis |
| **P-A7** | M-A7 Interpolation / reconstruction | KEEP-read; FORBID as POINTER when-rule |
| **P-A8** | M-A8 Training GD/SGD/backprop | **FORBID-as-Lace-history**; SILENT-for-Lace-append |
| **P-A9** | M-A9 NTK / infinite width | **FORBID-as-store**; width≠Piece 12 |

### P4-X — Rigor matrix (required · on clipboard or ascii)

For each P-A1…P-A9: `P · M · Stamp match · Implements reverse-walk · Fence book-only · Overview attach · PASS/FIX`

Any FIX must be resolved in this pass (prefer panel edits). Do not inherit Pass 3 blindly — re-walk Implements.

### Cross-walk table (required)

`P-A* | M-A* | stamp | Map P# | clipboard PAGE | FM-P*`

### Waves B–D

One-line deferred seeds only (same as mechanisms file). No full panels this pass.

### ASCII standard

Monospace fences; ≤76 cols; reverse-walkable to M-A steps; stamp under fence; no Core tokens inside fence.

---

## Clipboard / mechanisms thin edits

On `petersen-zech-clipboard.md`:

1. **Pass 4 receipt** — EXECUTED, path to ascii file, panel count, P4-X verdict, emission `[GAP]`  
2. **P4-X matrix** (if not only in ascii file)  
3. Optional one-liner under P3-M pointing to ascii file  
4. Do **not** rewrite PAGE morals, D1–D6, FM-P*, D7, or P3-M teeth  

Mechanisms file: optional one-liner under status — “Pass 4 ASCII: see petersen-zech-ascii-machinery.md (P-A1…A9). Algorithms unchanged.”

---

## Tickets

### T0 — Claim / base

Held at plan. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → map §4 → M-A1…M-A9 → Pass 3 clipboard → five-stakes PAGE 4/5 → sibling ASCII sample (AgentScope/Rowlands panel schema).

### T2 — PDF gate

Confirm refs/local; `git check-ignore`; abort if PDF staged. Re-sight Def 2.1 / Remark 2.2 / Ch.3/5/10/11 as needed for diagram fidelity.

### T3 — Scaffold `petersen-zech-ascii-machinery.md`

Opening contract, stamp legend, overview placeholder, P-A1…P-A9 stubs, cross-walk stub.

### T4 — Draw SOURCE MACHINERY overview (book-side; Core/Φ off-board)

### T5 — Draw panels P-A1…P-A9 (Implements match M-A*; stamps match)

### T6 — P4-X rigor matrix + fix any FIX rows

### T7 — Cross-walk + Pass 4 receipt · mark plan EXECUTED

### T8 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: ascii file + clipboard receipt/P4-X (+ mechanisms one-liner) + plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. `petersen-zech-ascii-machinery.md` exists with opening contract, stamp legend, SOURCE MACHINERY overview, P-A1…P-A9, cross-walk, P4-X.  
2. Each P-A* cites M-A*, implements that card’s steps, matching stamp, ≤76-col fence, book glyphs only inside fence.  
3. P-A8 fence does **not** depict training as Lace append/history rewrite; FORBID-as-Lace-history explicit.  
4. P-A9 fence does **not** depict NTK as Graphic D / second store; FORBID-as-store explicit.  
5. Overview keeps Lace Core and Φ off-board.  
6. P4-X all PASS (or fixed to PASS this pass).  
7. No stamp invents POINTER when/arity/adjacency; Φ still `[GAP]`.  
8. M-A* algorithms not rewritten; reason-model-map / law / graphics / systems-manifest / Core ASCII / `src/` untouched; PDF never staged.  
9. Pass 4 receipt on book clipboard.  
10. Steward test: shoe in hands — refuse “store trained w as Lace history” using only P-A8 + stamp.

---

## Execution (when you say go pass 4)

**Done.** Refresh → T1–T7 → plan EXECUTED → check · commit · release · STOP. Do not push.

---

## Steward test

Shoe in hands, or no.  
These diagrams are photographs of **book mathematics**, not Lace machinery.  
Forward Φ boxes are not the strand.  
Training boxes update parameters — not occurrence history.  
NTK boxes are analysis — not a second store.  
Φ stays `[GAP]`.
