# Rowlands pass 2 — leftovers / failure modes / coherence (PDF present)

**Status:** EXECUTED — 2026-09-18 ~16:40 PT. Emission still `[GAP]`.  
**Station:** clipboards (HELD `reference-Mapper`)  
**Base at plan:** refresh to current `origin/main` before execute  
**Book:** Peter Rowlands, *Zero to Infinity: The Foundations of Physics* · K&E Vol. 41 · World Scientific 2007  
**Clipboard:** `docs/clipboards/rowlands-zero-to-infinity-clipboard.md`  
**Ancestor:** [rowlands-pass-1-pdf-plan.md](rowlands-pass-1-pdf-plan.md) (EXECUTED · PDF-sighted A–F, X)  
**Sibling (default read-only):** `docs/clipboards/clipboard-five-stakes.md` PAGE 2/5  
**Hands:** shoe → graphics A–D → systems-manifest → staking / law-why → reason-model-map §2 (R1–R6)  
**PDF:** `refs/local/rowlands-zero-to-infinity.pdf` (738 pp., text layer, gitignored). **Never `git add`.**  
**Offset:** printed p ≈ PDF p + 25 after front matter (Pass 1 certificate)

Law: empty start · no extra rooms · “rewrite” ≠ mutate-WORD · Hands win · `[GAP]` sealed.

```
pull/ff → claim clipboards → Hands skim
       → open PDF (refs/local; no git add)
       → do NOT redo A–X fill or invent emission
       → Pass 2 deltas only: D1–D6 (+ optional constructive miniature)
       → receipt on book clipboard
       → check · commit · release · STOP
```

---

## Why this pass exists

| Pass | Job |
|---|---|
| 1 (PDF) | **Admission + locus bridges** — A–F, X stamped against this attachment |
| **2** | **Leftovers** — what the clipboard is not; KEEP/FORBID strip; failure modes with PDF pins; interface coherence; source gate cite; sealed |

Same shape as early Kauffman pass 2 (D1–D6), now that the PDF is already present (Pass 1 D5-class gate is done).

Still **Shadow / Layer III**. Not Core. Not `src/`. Not Kauffman mechanism/ASCII files.

---

## Already done (do not redo)

| Layer | State |
|---|---|
| PAGE A–F, X morals | Filled; Pass 1 CONFIRM/SILENT stamps |
| Offset certificate | printed ≈ PDF+25 |
| PDF gate | Present; text layer; gitignored |
| Five-stakes PAGE 2/5 | Agrees; T5 skipped |
| reason-model-map §2 | Untouched (R1–R6 stand) |

Pass 2 does **not** re-walk every page unless a delta hunt needs a fresh pin.

---

## What is still flat / missing

1. **No “what this clipboard is not” strip** — readers can mistake it for Core / emission law / sixth graphic.  
2. **No compact ON-SHOE / KEEP / FORBID / MAP strip** mirroring five-stakes PAGE 2/5 on the book clipboard itself.  
3. **No numbered failure-mode table (FM-R\*)** with PDF pins (false friends are narrative only on F/X).  
4. **No interface-coherence paragraph** stating which PAGE outputs may touch Hands inputs (only R1/R2 kin).  
5. **Source gate** is in Pass 1 receipt — Pass 2 should **cite** it, not claim PDF absent.  
6. **Optional:** one Shadow constructive miniature of generate-from-empty (ch.1) stamped FORBID-as-mutate-WORD — algorithm on paper, not Lace append.

---

## Non-goals (sealed)

| Forbidden | Why |
|---|---|
| Invent POINTER when / arity / adjacency | Φ stays `[GAP]` |
| Soften R3 clash or R4–R6 FORBID | Pass 1 confirmed |
| Import Dirac / DNA / UG / TOE as Core schema | off-shoe |
| Amend reason-model-map / law / graphics | wrong stations |
| Rewrite five-stakes PAGE 2 morals | default skip; optional one-line receipt only if T5 opens |
| Add `src/` or edit Kauffman mechanisms/ASCII | wrong Shadow shelf |
| Git-add PDF | gitignore |
| Push without ask | standing rule |

---

## Pass 2 product (deltas D1–D6)

Edit **only** `docs/clipboards/rowlands-zero-to-infinity-clipboard.md` unless T5 opens five-stakes.

### D1 — What this clipboard is not

Short sealed list:

- Not Core / not a sixth graphic  
- Not `pointer-emission.md`  
- Not a rewrite of reason-model-map §2  
- Not permission to say “rewrite” in standing orders as mutate-WORD  
- Not an import of §20 DNA/UG into Piece 9  

### D2 — ON-SHOE / KEEP / FORBID / MAP strip

Mirror five-stakes PAGE 2/5 on the book clipboard (compact card). Must match Pass 1 morals:

| KEEP | FORBID |
|---|---|
| empty start (R1) | rewrite-as-mutate-WORD (R3 name) |
| no extra rooms (R2) | TOE / Dirac / vacuum tables (R4) |
| dimension explained not stored | DNA rewrite / UG tables (R5, R6) |

Map arrows: R1→A1/Piece1 · R2→Graphic D · R3→[X] Piece2/6 · emission `[GAP]`.

### D3 — Failure modes (FM-R1…R9)

Table with columns: ID · Misread · Hands deadbolt · PDF pin (from Pass 1 or fresh `pdftotext`).

Minimum rows:

| ID | Misread seed |
|---|---|
| FM-R1 | empty start ⇒ second cosmos / second store |
| FM-R2 | strings/membranes math ⇒ N-D Lace rooms |
| FM-R3 | English “rewrite” ⇒ mutate WORD @ i |
| FM-R4 | TOE / Dirac nilpotent tables ⇒ Core schema |
| FM-R5 | DNA create/conserve ⇒ WORD append types |
| FM-R6 | UG / Chomsky ⇒ stored lexicon / Piece 9 field |
| FM-R7 | concatenation / new alphabet ⇒ POINTER emission |
| FM-R8 | “one process” ⇒ scheduler for Φ |
| FM-R9 | unified-theory aim ⇒ license to fill `[GAP]` |

Each row needs a PDF/printed pin (reuse Pass 1 bridges where valid).

### D4 — Interface coherence

One short paragraph: only R1/R2 outputs are kin to Hands inputs (empty strand; one carrier). R3 is a name clash, not an operator. R4–R6 outputs do not match any Core write. Nothing in the book supplies POINTER when/arity/adjacency — `[GAP]` remains.

### D5 — Source gate (cite Pass 1)

Do **not** narrate PDF-absent. Cite Pass 1 receipt: PDF present, 738 pp., text layer, offset +25, path `refs/local/rowlands-zero-to-infinity.pdf`, never git-add.

### D6 — Sealed

Repeat sealed list: invent Φ · amend map/law/graphics · `src/` · git-add PDF · pretend OCR without the file.

### Optional D7 — Constructive miniature (Shadow only)

If time: one numbered paper algorithm for ch.1 generate-from-empty / concatenation (PDF 30–32), stamped **FORBID-as-mutate-WORD** and **SILENT for Lace append**. Not a Core scheduler. Mark choice points `[OPEN]`. Skip entirely rather than invent steps the book does not state.

---

## Tickets

### T0 — Claim / base

Held at plan. Before execute: `git pull --ff-only` · refresh if clock turned.

### T1 — Hands skim

Shoe → A–D → manifest → law → reason-model-map §2 → Pass 1 clipboard → five-stakes PAGE 2/5.

### T2 — PDF gate

Confirm `refs/local/rowlands-zero-to-infinity.pdf` resolves; `git check-ignore`; abort if PDF staged.

### T3 — Write D1–D2–D4–D5–D6

Prose deltas; no A–X moral rewrite.

### T4 — FM-R\* hunt (D3)

Reuse Pass 1 pins; `pdftotext` only where a row lacks a pin. ≤2-line eye-quote per new pin.

### T5 — Five-stakes (default skip)

Only if human asks: one receipt line “book clipboard Pass 2 deltas landed YYYY-MM-DD”; do not rewrite PAGE 2 KEEP/FORBID.

### T6 — Optional D7 miniature

Default **include if ≤30 min**; else defer with note under Pass 2 receipt.

### T7 — Check / commit / release

```
./docs/coord/coord.sh check clipboards "reference-Mapper"
# commit: clipboard D1–D6 (+ D7 if any) + this plan EXECUTED
./docs/coord/coord.sh release clipboards "reference-Mapper"
```

Ask before push.

---

## Acceptance tests (execute must pass)

1. Pass 2 receipt on the Rowlands clipboard.  
2. D1–D6 present; D5 says PDF **present** (not absent).  
3. FM-R1…R9 each have Hands deadbolt + PDF/printed pin (or `[OPEN]` with cause).  
4. No stamp invents POINTER when/arity/adjacency.  
5. R1/R2 KEEP and R3–R6 FORBID/clash not softened.  
6. reason-model-map / law / graphics / systems-manifest untouched; PDF never staged.  
7. If D7 present: miniature is Shadow-only with FORBID-as-mutate-WORD.  
8. Steward test: shoe in hands — can you refuse “rewrite” as array verb using only D2+D3?

---

## Execution (when you say go pass 2)

1. Refresh claim.  
2. T1–T4 (+ T6 if time; T5 only if asked).  
3. Mark this plan EXECUTED.  
4. Check · commit · release.  
5. Stop.

---

## Steward test

Shoe in hands, or no.  
Empty start ≠ second cosmos.  
“Rewrite” in this book ≠ mutate-WORD.  
§20 opened ≠ §20 imported.  
Failure modes are fences, not new Core ops.  
Φ stays `[GAP]`.

---

## Execution receipt

| Field | Value |
|---|---|
| Status | EXECUTED |
| When | 2026-09-18 ~16:40 PT |
| Agent | `reference-Mapper` |
| Product | D1–D6 on book clipboard; D7 included (PDF 30–32 miniature); T5 skip |
| Commit target | clipboard + this plan; then release clipboards |
| Push | not requested |

