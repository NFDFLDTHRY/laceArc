# History recovery — how the ChatGPT extract sits

Source: workspace attachment `Lace_Memory_Recovery_2026-09-18.md` (Caleb Dumitry; recovered 18 September 2026).

This file is a **reading of that extract against current law**.
It does not amend staking law.
It does not settle pointer-emission.
It does not revive a walker.
It does not grant `src/`.

The extract itself already says the same. Honor that.

Attribution in the extract is the rule here too: user-then, assistant-then, and document-summary are not Core axioms.

---

## What the extract is

A historical reading note from nine searches. Not an exhaustive transcript. Not independently audited original files. Dates UTC. Quotation marks are retrieved wording.

Most consequential disposition it records:

> 29 August — all three walker candidates rejected; no clean authoritative file remained.

Earlier “successful run” reports cannot establish Lace. The machine under the run was later rejected.

---

## What survives as intent (not as code)

These aims appear repeatedly. They match current graphics and clock. They are not implementations.

- One strand. Two ends. Everything that counts ends on the lace.
- Arrival sequence is law. No sorting of touches into a tidy order.
- STAR is a lens / forming view. Not a serialized Star table. Recurrence revisits a bearing; it does not copy a spoke.
- Inspection was wanted from the start (3D, traces). Projection is not a second store.
- HCC-A, Coffee Cup, Water are conceptual ancestry (26 August). That is why they are gears now, not fence posts.
- “Nothing can touch lace without growing lace.” Intent: contact is not free. It does **not** write today’s read/emission contract.
- June 14–15: “Absolutely everything is the same lace.” “Any and all input ends on the lace.”
- September lab: heterogeneous-machine map is experimental lab, not a merge of architectures.

---

## Dead as foundation

Do not open these as Core, as branches-to-port, or as “the old code almost worked.”

| Dead thing | Why |
|---|---|
| Walker `be16b05f…` (legacy) | Rejected 29 Aug |
| Walker `e1aa242e…` (state-machine rebuild) | Rejected 29 Aug |
| Walker `558058af…` (substrate-lock) | Rejected 29 Aug |
| Loose `lace_walk.py` | Rejected candidate |
| Invented recursion guard `any(frame.focus == unit_id for frame in self.frames)` | Assistant-added; results are not the user’s mechanism |
| “Local fidelity mirror” used for R0.2 numbers | Substitute implementation |
| External spelling-to-position lookup | Bypassed traversal |
| BFS / frontier / global seen / halt when no unseen word | Closure probe, not continuing lace |
| Reconstruct / replay / cache Touches as scalar pulses | User rejected |
| Sort / canonicalize Touch argument order | “No sorting!!! Arrival sequence!” |
| Closed ring, wraparound circuits | Current law forbids closed loop |
| Python/C walkers, browser ladders as Core | Implementation law is Rust `#![no_std]` after emission |
| ATOM / unary POINT(target) as current schema | Law names WORD/POINTER; arity `[GAP]` |
| One persistent spelling slot that dedupes arrivals | Conflicts with no-intern + occurrence order |
| Proof-carrying compiler / Lace A* / Looped PointString / RP2350 ladder as contract | Historical pivot; not today’s contract |

The extract’s lesson, which matches AgentScope-on-the-holder:

Assistants introduced familiar machinery, tested the *altered* machine, and attributed the result to Lace.

That is grind counted as a tick. W pallet must reject it.

---

## Conflicts that stay visible

Do not reconcile these into one smooth story.

### “The relationships are the object” (8 June) vs clock law

Clock law now: objects do not *have* relationships; relationships cluster as the train turns; a variable is a label on index geometry.

June 8: “The relationships are the object.” One strand underlay presented objects and dimensions.

**Do not pick a winner by deleting a sentence.**

Lawful reading that keeps both:

- June: what you are looking *at* is the cluster / the participation — not a bag of thing-nodes. The strand is the object; the “objects” you thought you had were presentations of relation-on-the-strand.
- Clock: a WORD mark does not own `relations[]`. The cluster is authored by ticks. The label names geometry.

Unlawful reading: WORD grows an edge list because “relationships are the object.”

If the hologram hears those two sentences as the same machine, say so in speech. Do not encode a graph to make them look the same.

### “Nothing can touch without growing” vs emission `[GAP]`

Survives as a physical question: is a read a pass of cord, or a look at cord already there?

Current law does not answer. A look at Graphic D is not automatically a POINTER row. Treating the slogan as emission would close the `[GAP]` from a sentence instead of from the four graphics.

### Old OBS/DELTA/UNK/INVALID as lace statuses

June used those labels over admitted material. They now live in Gear W (talk / clutch). They are not row types and not ingest gates.

### Old Touch language vs POINTER

Ordered Touch is historical. It does not choose today’s pointer fields or arity.

### Alive / continuously processing (11 August)

“This structure is alive.” When input was not being laid down, a block was picked up.  
Current tape does not run itself. Continuous processing is historical intent about a walker that was later rejected. Do not start a scheduler to honor the sentence.

### Visualizer (7 September)

X = entry index, Y = pointer index, height = convergence, pond surface not spikes.  
Inspection preference. Not Core geometry. Not a Star table.

---

## Named artifacts (leads only)

The extract lists files and conversation titles. Names are for finding sources. They have no authority until opened under current law.

Including: `pointer_ball_system.md`, `lace-manifest.md`, `LACE_STRUCTURE_SPEC_v2.md`, hologram / circulation / corpus captures of 11–15 August, September `.docx` “canonical” files, `ONE_SYSTEM_THREE_ALTITUDES.md` (already the three gears), `lace_walk.py`.

A title that says Canonical does not override later law.

---

## Clock reading of the failure record

| Historical failure | Clock slot |
|---|---|
| Mirror / substitute machine | grind counted as tick |
| External word lookup | second array beside the tape |
| BFS closure | halt invented; lace “Continue.” has no such halt |
| Sort arrival | file tooth 4; erase tape geometry |
| Replay cached Touches | aftermath as physics |
| Recursion guard | PFC inside the walker, not RIC from the strand |
| Closed loop | Reidemeister / Kauffman forbid |

---

## Steward rule for this extract

Use it to refuse revival.
Do not use it to fill emission.
Do not paste old Python into a first-party branch.
If a later file from the lead table is opened, map it here first; do not implement it.
