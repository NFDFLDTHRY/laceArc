# Systems-manifest ASCII pass 4 plan — source panels as systems

**Status:** EXECUTED on `docs/systems-manifest-ascii.md`. Emission `[GAP]`.  
**Station:** law  
**Source:** four graphics in `docs/graphics/` (the project definition). There is **no Core source PDF**. If “PDF” meant those four images, this pass is that mapping.  
**Cite:** `docs/systems-manifest.md` piece locations (A1–A13, B1–B12, C1–C12, D1–D6); `docs/graphics-close-reading.md` for D1 rows only.  
**Companion:** `docs/systems-manifest-ascii.md`

Do not git-add other PDFs. Do not import Kauffman into this diagram.

---

## Job

Passes 1–3 drew Core I/II/III, pieces, interfaces, open board. They never drew **the source’s own panel systems**.

Pass 4: one ASCII per graphic, panel list as the graphic systems them. Lossless = every panel the manifest cites appears; D1’s eleven rows appear as already transcribed; no extra row.

---

## Tickets

### T1 — Graphic D (only store)

ASCII of D1–D6 jobs + D1 eleven-row tape as close-reading wrote it.

```
0000 WORD  PIE
0001 WORD  DESSERT
0002 PTR   0000 → 0001
...
0010 PTR   0009 → 0002
```

Stamp: illustrative mix, not an emission trace. Adjacent-pair rule already false.

### T2 — Graphic A (growth story)

A1–A13 as a left-to-right growth, not a second store.

### T3 — Graphic B (mechanism)

B1–B12 including governing rule B12. Same operator as D footer.

### T4 — Graphic C (what dimensions are not)

C1 Rule Zero through C12 governing rule. C6 four families named; still `[GAP]` vs D schema (open 11).

### T5 — Cross-walk

One small table: panel → Piece # already on the pass-2 board. No new pieces.

### T6 — Sealed

G2. Kauffman. Train oil inside D. Relabeling D1 into a rule.

---

## Execution (when you say go pass 4)

Edit `docs/systems-manifest-ascii.md` only (or `docs/systems-manifest-ascii-panels.md` if the companion would exceed one screen — prefer one file unless crowding lies). STOP.

---

## Steward test

If a panel is not in A–D or the close-reading, it stays out.  
If D1 becomes “the” emission algorithm, delete that sentence.
