# Rust target clipboard — current routing ticket for the tenth source shelf

**Classification:** BOUNDED FUTURE TICKET with historical source receipts. Not Hands. Not law. Not Core. Do not replay the original staking/mapping work over the existing products.

**Current routing, 2026-09-22:** the product is [`docs/clipboards/rust-target-clipboard.md`](../clipboards/rust-target-clipboard.md), with the companion source machinery, locator extracts, Cargo A4 shelf and executed Pass-4/5/6 receipts indexed from [the clipboard shelf](../clipboards/README.md). Read those products before proposing any new source or probe task.

**Current target verdict:** **TARGET UNDECIDED.** The former repository-wide sealed `wasm64-unknown-unknown` instruction is historical. wasm64 remains a conditional Tier-3 capability candidate; wasm32 is a lawful lower-burden candidate but is not yet proved sufficient. Platform/toolchain evidence cannot select the target without the required implementation envelope and actual build/device evidence.

**A4 status:** the supplied Cargo Book is **A4 AVAILABLE / BOUNDED SOURCE MAPPING COMPLETE**. Its `build-std` syntax, crate-list mechanism, nightly/rust-src requirements and bounded lockfile evidence are mapped. It did **not** settle exact build-std lock representation, `compiler_builtins` participation, exact toolchain/component identity, or actual wasm32/wasm64 build closure.

**Implementation gate:** `docs/pointer-emission.md` is a reconciled but unaccepted draft; RM-A retain-when remains open / MISSING-A; G1 and R3 remain open; no `src/`.

---

## Source identity and custody

The rustc source was staked by the human as *The rustc book* text conversion. A source sighting is identified by exact bytes/hash/line locators; a different conversion is a different sighting. Source material remains outside git unless the repository explicitly carries a derived locator/product.

Historical Pass 0 and Pass 1 remain preserved in the product:

- **Pass 0:** staking receipt / source sighting.
- **Pass 1:** target/toolchain mapping.

Subsequent source work already exists and must not be recreated from this prompt:

- A4 Cargo source mapping;
- rustc and Cargo locator/forge work;
- Pass 4 ASCII source machinery;
- Pass 5 steward/order reconciliation;
- Pass 6 deep diagram audit.

Those are executed records at their stated evidence strength. Their old “A4 next” lines are dated provenance from before A4 arrived.

---

## What a future task may be

A future task must be one named, bounded source/probe question, for example:

```
SOURCE ACQUISITION
  missing host/embedder primary source
  missing exact toolchain/component identity source
  missing build-std lock / compiler_builtins evidence

PROBE
  actual wasm32 build closure
  actual wasm64 build closure
  artifact imports/exports/features
  Pixel/browser admission and practical capacity

COURT
  compare evidence only after the semantic/domain/carrier requirements
  are sufficiently fixed to discriminate a target
```

It may **not** start with “make wasm64 work” or “switch to wasm32.” A target-helping result does not become a requirement.

## Required reading for target work

1. `AGENTS.md`
2. [`context-pass.md`](context-pass.md)
3. current [rust-target clipboard](../clipboards/rust-target-clipboard.md)
4. current [Cargo A4 clipboard](../clipboards/cargo-build-std-clipboard.md)
5. [Wasm source clipboard](../clipboards/wasm-spec-3-clipboard.md)
6. Pass-5 [target requirements](../plans/source-lab-lace-alignment-pass-5-target-requirements.md) and [target audit](../plans/source-lab-lace-alignment-pass-5-target-audit.md)
7. Pass-6 [domain/carrier/working-set findings](../plans/source-lab-lace-alignment-pass-6-findings.md)

## Evidence discipline

- SOURCE says what a supplied document/specification says.
- SOURCE-SPECIFIED capability does not prove browser/device execution.
- NOT_RUN stays NOT_RUN.
- wasm64 capability does not imply wasm64 selection.
- wasm32 lower burden does not imply wasm32 sufficiency.
- build-std syntax does not prove an actual accepted artifact.
- target choice does not define Lace semantics.

## Refuse

- “A4 is next” as current status.
- sealed wasm64 as current law.
- compiling ⇒ accepted.
- a custom target merely to preserve the old seal.
- `std`, crates.io, wasm-bindgen or foreign glue as convenience.
- treating WebGPU/WGSL/WebNN as target-selection authority.
- creating `src/` or implementation code from this prompt.

**Product of this prompt:** a bounded source/probe plan or evidence note inside an explicitly authorized task. Existing clipboard/source-machinery products remain the record; do not overwrite them from historical instructions.
