# History recovery pass 2 — how the second extract sits

Source: workspace attachment `Lace_Memory_Recovery_Pass_2_2026-09-18.md` (18 September 2026).

Supplements [docs/history-recovered-2026-09-18.md](history-recovered-2026-09-18.md).

This is a **reading of that extract against current law**.
It does not amend staking law.
It does not settle pointer-emission.
It does not revive a walker, scheduler, codec, or C machine.
It does not grant `src/`.

The extract itself already says the same. Honor that.

D01/D02 cautions apply here too: silence is not assent; `EW` / `RULING` / `LOCKED` can preserve apparent agreement while the parties imagine different behavior.

---

## What pass 2 adds that pass 1 did not have

- `no_containers.md` (D06) read in full.
- Eleven scheduler rules reproduced from D01 §7 (original July thread still missing).
- Later allocation correction: strict starvation is not the last historical instruction.
- Direct reads of ten documents (D01–D10) with coverage limits named.
- Card-number citations in D01 do not match recovered `axioms.html` ordinals.
- Contact/admission rules conflict across artifacts.
- Codec round-trip ≠ intelligence.
- Frozen C append-machine counts are not Lace evidence.
- Provenance of the four current graphics is not a verified identity chain.

29 August rejection of the three walkers is **not reversed**.

---

## No Containers (D06) — ancestry, not a rewrite key

Thesis recovered:

- Words do not carry a semantic payload between minds.
- Understanding depends on overlap between separately developed structures.
- Objects are formations of relationships, not primitives that then acquire relationships.
- A communication failure: the reader answers a familiar adjacent claim and misses the inversion.

That last item is Gear H (PFC-wins) and Gear W (grind): heat on a lookalike, no mesh on the proposed inversion.

### Clock reading (keep both sentences)

| Sentence | Lawful reading | Unlawful reading |
|---|---|---|
| 8 Jun: “The relationships are the object.” | What you are looking at is the cluster / participation. | WORD owns `relations[]`. |
| Clock: objects do not have relationships; clusters form as ticks. | A WORD is a mark. Geometry thickens only on append. | There are no objects, so nothing to append. |
| D06: objects are formations of relationships, not primitives with attachments. | No payload-container. Formation = cluster-as-seen. | Substitute/relabel history in place. |

Do not pick a winner by deleting a sentence. The inversion is: **there is no container**. The tape still grows. The label still names index geometry.

### Substitution demonstration — theoretical, not a write

D06 replaces a phrase everywhere in its complete occurrence history while preserving pattern of use.

That is a **thought experiment about labels**. It is **not** permission to:

- replace arriving WORDs
- mutate history
- intern
- rewrite the strand
- file tooth 4

The file also uses the later-retired word “cuts.” Preserve the thesis. Do not revive cuts.

Ten-year conceptual ancestry in D01 is a duration claim in a capture, not a dated Lace repository.

---

## Scheduler — recovered, then corrected, still not Core

D01 §7 reproduces a 25 July eleven-rule handoff. Four seams: resolution selection, lap order, preemption, conflict with N+ P-09. A proof on one branch does not settle another by analogy.

| Rule (historical) | Current law |
|---|---|
| 1 Recursion of unknowns | Talk/hologram UNK, not a Core operator |
| 2 Shells as queues, not storage | Do not become a second array |
| 3 Strict shallowest-first / starvation | Later user correction (11 Aug) rejects fairness framing |
| 4 Three lanes DEADLINE / LEDGER / COMPREHENSION | Not Graphic D types |
| 5 Inward cascade | Historical; not emission |
| 6 Learning as touch/insert/cascade/gate | Several of these were subsequently retired |
| 7 Gate feedback into deadline templates | PFC leak if stored as rows |
| 8 No quiescence / work between inputs | D03: stronger permanence not mathematically secured; 29 Aug walker died |
| 9 Insertion: logical near origin, physical append; old mutable slot | Mutable continuation **forbidden**. Append-only has no slot to rewrite |
| 10 LLM in a reasoning socket, not orchestrator | Old branch. Not Core. Implementation law is not “call an LLM” |
| 11 Exact identity, no similarity-scored matching | Kin of no-embed. Theorem language is not a verified proof |

Later record:

- 11 Aug 19:15 user: descending allocation by degree of separation; nearer gets more; not “fairness.”
- 11 Aug 19:16 assistant: withdraws strict-starvation; illustrates 4:3:2:1.

The ratio is the **assistant’s illustration**. It is not the user’s established formula. No current scheduler is authorized. N+ concurrent-shell vs strict-starvation remains an unreconciled historical conflict.

---

## Card numbers are not rulings

D01 cites `axioms.html` by card number. Recovered D09 has 55 articles and **no** explicit card-number identifiers. Document-order ordinals disagree with D01’s numbered citations (e.g. D01 “Card 13 = tie block” is ordinal 30 in the recovered HTML: “A tie is a block”).

A bare card number cannot identify a rule. Need title, actual text, and version. Do not equate D01 “Axiom 10” with the tenth card.

The deck records its own missing machinery: trajectory function NOT BUILT; several tie/output questions undecided. Decode claims do not close those gaps, and they do not close pointer-emission.

---

## Contact rules conflict — still `[GAP]`

| Record | Historical position | Why it is not emission |
|---|---|---|
| D04 `pointer_ball_system.md` | Pointability/crossing; INVALID never enters; multiple typed laces | Conflicts with one-lace + append-first |
| D07 `lace-manifest.md` 1 Aug | New element in a populated window triggers a tie; admission as change | Differs from later exact-recurrence |
| D01 / D09 | Arriving figure matching earlier figure appends a tie | Historical exact-match, not accepted contract |
| D02 | Unequal figures, identity, partial formation unsettled | Equality rule cannot settle these |
| D05 Sep v3 | Unary backward POINT as candidate; ingress, prior target, traversal still open | Even the strong unary form did not fully determine execution |

D07 also combined append-only with a write-once continuation field on an already created block. D02/D03 reject reviving that mutable slot. “Write once” is not an exception to Rule Zero.

June fidelity audit (D08) found real terminology mismatches with HCC-A / Cup / Water, then proposed `LaceId`, typed proof-intake, rejection route, traversal masking. Those repairs are incompatible with present prohibitions. An audit may keep the discrepancy and still propose the wrong fix.

---

## STAR, codec, geometry — do not merge

Preserved-interior: a formation may compact to a seed only if interior remains accessible (D01 §3.4; D05). That is a **positive constraint on views**. It does not choose storage schema, pointer arity, or a formation-birth operator.

Three geometric experiments are not one star: radius-as-character, angle-as-sequence, paragraph-slide radius. D02 names them distinct. All drawing stars does not make them interchangeable.

| Claim | Qualification |
|---|---|
| Codec round-trips | Preserves enough for its own decoder. Not selection, grammar, contact, or learning |
| `legal()` as learned legality | Whitelist of previously observed figures. Does not say why this assembly now |
| 60,002 / 104,510 sentences at 100% | Reported runs; D02 not reproducible (dataset absent); this pass did not rerun |
| Semantic “breathing” | Animation of computed stars, not two semantic transition laws |
| Signed-radius repairs absolute-radius | Changes coordinate frame; cancels current index; does not settle intended geometry |

D03: append frontier is where new history is committed; an evaluative focus may move over old history. A STAR around an old focus is labeled a **strong synthesis, not an established ruling**. The word “frontier” must not move the append location backward.

---

## September and graphics provenance

D05 rejects yarn tension, manufactured adjacency edges, semantic sidecars, fixed coordinates, rigid dimensional staircase, destructive summary of interiors. Those rejections match current law.

D05 still leaves open: ingress identity; prior target on recurrence; formation boundary; interior access; dimensional measure; subextent naming; write granularity; traversal; crash/commit; self-training criterion. “Canonical” in the filename did not mean those were proved.

14 September “communication barrier passing outputs” / `ref_A`/`ref_B` is **not** a verified identity chain to current Graphics A–D. Current four graphics are source of record **by present staking**, not by recovered provenance. Do not merge image sets. Do not infer emission from them.

---

## False attribution: the C machine

1 September conversation returned large C verification/ablation counts. Follow-up identified `append_machine_verification_report.md` / a frozen append-only C machine. Resemblance to Lace was considered **after** investigating that machine. It was not established as Lace. **Exclude those counts from Lace evidence.**

Codec reports and this C experiment do not replace the rejected August walkers.

---

## Dead additions (pass 2)

| Dead thing | Why |
|---|---|
| Eleven-rule scheduler as Core | Historical; later corrected; not authorized |
| 4:3:2:1 shares | Assistant illustration |
| Mutable / write-once continuation slot | Rejected by D02/D03; Rule Zero |
| Substitution rewrite of occurrence history | D06 thought experiment, not a write |
| Typed laces / linking maps (D04) | One-lace law |
| `LaceId` / typed intake / rejection route / traversal masking (D08 repairs) | Wrong repair |
| Similarity-scored matching as Core | Historical exclusion is not a license to add embeddings either |
| LLM reasoning socket as Core | Old branch |
| Card-N as a ruling | Numbering mismatch |
| Codec `legal()` as learning | Whitelist, not why-now |
| Frozen C append-machine metrics | Not Lace |
| Trajectory function from the deck | Marked NOT BUILT |

---

## Clock reading of pass 2

| Historical item | Clock slot |
|---|---|
| No Containers / no payload | WORD is a mark, not a vessel |
| Substitution demo | aftermath wanting to file the tape |
| Strict starvation vs graded shells | two clocks claimed; neither is Core |
| Mutable continuation | rewrite of an old tooth |
| Exact-match tie on arrival | candidate tick; emission still `[GAP]` |
| Codec round-trip | hands that redraw; not the mainspring |
| Evaluative focus vs append frontier | looking at old tape ≠ moving the write head |
| C machine counts | grind counted as tick |
| Card-number citations | pointing without mesh |

---

## Steward rule

Use pass 2 to refuse: schedulers, continuation slots, substitution writes, C-metrics, card-number law, and emission-by-exact-match.

If `no_containers.md` is opened again, map it here first. Do not implement it.
Keep D06’s inversion and the clock’s “objects do not have relationships” both visible.
