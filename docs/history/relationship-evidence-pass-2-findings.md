# Relationship evidence — Pass 2 findings

**Status: EXECUTED.** Canonical graphics A–D: **VIEWED**. Independent adjudication by a third agent: **NOT_RUN**. Maps response to proposals A/B: **NOT_RUN**. Emission: **unaccepted**.

**Verdict:** Pass 1’s *textual* account of the word / definition-route / retained-touch coupling is **CONFIRMED**. The four Hands pictures show the same two phenomena Pass 1 joined from prose (definition as a route through ordinary stars; later wire threading structure that remains). They do **not** draw one labeled composite arrow “definition-route becomes this later touch.” The ASCII companion still underexpresses that join; star caption `write: the WORD rows only` stays ambiguous. Construction (when / targets / arity / slots / adjacency-as-relation) stays unspecified. Φ stays `[GAP]`.

Executed by **Grok-evidence** under “Proceed with pass 2”, following [`relationship-evidence-pass-2-plan.md`](relationship-evidence-pass-2-plan.md). Not recovery pass 8. Not quiet-door #5. Not global ASCII Pass 26. Companion, law, graphics bytes, and pointer draft were not edited.

```
PASS 1 TEXT VERDICT              HANDS PICTURES OPENED
Pieces 7–11, 15 + law            A definition-route
companion underexpresses         B thread / touch remains
star caption ambiguous           C participation ≠ meaning
construction open                D WORD|POINTER specimen
              |                               |
              +---------------+---------------+
                              |
                              v
                 CONFIRM the relationship
                 NARROW “the diagram”:
                   pictures show the parts;
                   companion still does not
                   exhibit the join
                              |
                              v
                 Construction and A/B apply
                 remain off this ticket
```

These arrows describe the review, not an emitter.

## 1. Freeze

**Run pin:** `32ab7242b87bafc91843b996049e066a7b5e27c5` (origin/main at execute check).  
**Holder:** Grok-evidence. History HELD.

| Object | Blob / SHA at execute |
|---|---|
| Packet | `6a1896b0b9f1c6319a0f7329a167562a47185961` |
| Pass 1 findings | `4dc3d7ab343f6a6cd585839092113d38991b084b` |
| Manifest (tip = A = B) | `ca8b7d7bc4a93eea81510693424d450d66c6851a` |
| ASCII companion | `2628df45e644f56b27e03c4af4460e89b47f905d` |
| Pointer draft | `6399a1680cb5f8327f093fc2425c944a47113c98` |
| Live law | `5539a405a79925b0c49f6e00c55067a44071a862` |
| Graphic A PNG | `403d4754ede54eccf8defbc55d27d22fc6b4d8ed` |
| Graphic B PNG | `794e11b2f609fdf3927a961726f228013aec3cad` |
| Graphic C PNG | `554360f3765904652e947c5b497108a347788ac5` |
| Graphic D PNG | `681384934bc98064b99fd78fe2cc31f9de64995c` |

Samples A `f3b554c` / B `856e91d` not redrawn. Manifest blob unchanged from Pass 1’s A=B pair. Packet and Pass 1 findings bytes were not rewritten this tick.

Pass 1 image blob for D was recorded as `681384934bc98064b99fd78fe2cc31f9de64995c` with decode failure. This run opened that same object from the clone.

## 2. Image register

Physical lace: **NOT_PERFORMED**.

| Graphic | Status | What was read off the picture (not inferred as Φ) |
|---|---|---|
| A `from-words-to-worlds.png` | VIEWED | Header: one strand; every word a centimetre of wire; meaning does not exist. A5 later occurrence threads existing star. A6 dictionary = words defining words; definition = route through other word-stars; example path PIE → dessert → slice → whole → food; nothing stored as a definition. A8 later documents reuse the same stars through new routes; distinct passes, not collapsed edges. A10 later input can thread existing 3D structure. A11 dictionary and documents wired in one Lace. |
| B `mechanisms-in-3d.png` | VIEWED | B3 later occurrence = more wire through the same star, new direction. B4 definition is a physical path, not a stored field. B7 new wire threads existing structure (not attach-to-outside). B8 the touch remains; can participate later. B10 no stored definition. B11 2D flatten is a shadow and must not write back. |
| C `n-dimensional-relationships.png` | VIEWED | More dimensions = more independent participation, not more meaning. C4 stars from repeated passages. C6 names dictionary / document / conversation / provenance as families / axes over the same stars. C7 relations among relations. C11 flatten cannot write D. |
| D `data-structure-1d-array.png` | VIEWED | One array. Types WORD and POINTER only. D1 eleven rows match the close-reading transcription used in Pass 1 (PIE / DESSERT / POINTER 0000→0001 / PIE / POINTER 0003→0000 / WHOLE / POINTER 0003→0005 / POINTER 0006→0004 / CUSTOMER / POINTER 0008→0007 / POINTER 0009→0002). Refs backward. Two slots drawn. POINTER rows name POINTER rows. D4 3D view not stored. D5 star = repeated passages through the same variable. D6 later section can participate in earlier participation. |

**PNG vs transcription:** no disagreement found on D1’s eleven rows or the six pointer pairs. Adjacent *WORD* pairs in the word stream: PIE–DESSERT linked by 0002; DESSERT–PIE (0001 then 0003) has no dedicated pointer; PIE–WHOLE linked by 0006; WHOLE–CUSTOMER has none. That still rules out “every consecutive word pair emits.”

The image tool labeled the files `image/jpeg` while the repo stores them as `.png`. That is a viewer MIME sniff, not a finding that Hands bytes were swapped this tick. Graphics station was not claimed. Bytes were not rewritten.

## 3. Q1 — What connects the two historical passages?

**Pass 2: CONFIRM Pass 1’s connected account. NARROW the exhibit.**

Directly stated (manifest blob `ca8b7d7`, same at A, B, and tip):

- Piece 7: star = accumulated passages; not a separate stored object; implementation view includes WORD rows of that value **plus POINTER chains that thread them**.
- Piece 8: word order is the physical route through stars.
- Piece 9: definition is a route through ordinary word-stars; later documents reuse those stars through new routes.
- Piece 10: document feed is the same operator on a longer stream; passages remain.
- Piece 11: new wire threads existing structure; the touch remains; it can participate later.
- Piece 15 / composition: stars, dictionary routes, documents, touches are formations of one line.

Graphics now add picture-level support Pass 1 lacked:

- A6 / B4 state definition-as-route on the source images.
- A8 / A10 / B7 / B8 state reuse and remaining touch on the source images.
- D6 / D1 show participation including POINTER→POINTER.

**Inference (named):** “A definition route can be among the structures a later touch re-enters” follows from (definition route is Lace structure) + (later wire may thread existing structure). The pictures and pieces state both premises. They do **not** mark a particular D1 pointer as “the definition-route of PIE.” Shared vocabulary is not the whole warrant; the warrant is those two stated mechanisms on one strand.

**Not established:** an automatic walk that expands every definition word; a required POINTER at each reuse; a runtime relation between git commits A and B.

## 4. Q2 — Where do definition words' own definitions participate?

**Pass 2: CONFIRM.**

Graphic A6 and Piece 9: definition stars are ordinary word-stars. Live law:

> A word is defined by words. Those words are defined by words. … Definition = route through stars. … Recursion = Continue. Not a closed splice. Seeing the route is a view. Writing the POINTER that would cut the route is still [GAP].

When further definition text is fed, it is more arrivals through ordinary stars on the same line (A8, Piece 10). That is retained routes and possible later touches, not a glossary field on the WORD row.

**Not established:** lookup, recursive expansion operator, transitive closure, splice of the strand’s ends. The missing premise for those would be an operation the sources do not give.

**Diagram split:** Hands A/B show the positive recursive *relationship* as route-through-stars. The ASCII companion names dictionary wiring and forbids stored definition; it still does not walk “definition words have definitions” as Continue on the same wire. Law supplies that sentence.

## 5. Q3 — Membership vs participation?

**Pass 2: CONFIRM Pass 1’s two-view result. Pictures agree.**

Companion at tip (same ASCII blob as Pass 1 review pin’s current companion), Pass-5 panel:

```
view:  star(V) = those passes
write: the WORD rows only
```

Piece 7 still includes POINTER chains in the star view. Graphic D5 says a star is repeated passages through the same variable; D1 records both WORD repeats and POINTER threads; D4 says the 3D view is not stored.

Same-record test, now against the **opened** D1 as well as the transcription:

- View A: WORD rows 0000, 0001, 0003, 0005, 0008.
- View B: those plus POINTER rows 0002, 0004, 0006, 0007, 0009, 0010.

Both views show two PIE occurrences. Only View B shows 0004 (second PIE to first), 0006 (that PIE to WHOLE), 0007 (those relations participating), and 0009/0010 carrying that participation onward.

**Refuted:** “the occurrence listing alone fully exhibits the recorded participation.”  
**Not refuted:** a read-only view may show only occurrences.  
**Caption:** `the WORD rows only` remains able to mean “no Star row” (charitable, and D4 supports that) or “formation is occurrences alone” (too strong vs Piece 7 and D1). Pass 2 does not pick a hidden author intent. It keeps the ambiguity.

## 6. Q4 — What is still open?

**Pass 2: CONFIRM Pass 1’s separation. Images do not close construction.**

| Claim | Pass 2 | Remaining boundary |
|---|---|---|
| Definition routes through ordinary stars; later text reuses them; touches can remain | CONFIRMED by Pieces 7–11, 15, law, Graphics A/B/D | Companion join still underexpressed |
| When to append a POINTER, including on a repeat | OPEN | Draft Q1 / Q5; pictures show mixed WORD/POINTER, not a scheduler |
| Which earlier indices to select | OPEN | Equality of `value` is not a target rule |
| Arity as law | OPEN | D1/D2: six of six are two-slot; source never writes “exactly two” |
| `ref_A` vs `ref_B` | OPEN | PNG matches close-reading: A is not always earlier (0004 is 0003→0000) |
| Adjacency as relation without POINTER | OPEN | D1 still has unlinked consecutive WORDs; “always emit” stays contradicted, not replaced |
| POINTER may name POINTER | PERMITTED in AGENTS, Piece 5, D6, D1 rows 0007/0009/0010; draft §4 still asks | Documentation tension. Not silence. Not acceptance |
| C6 families vs D schema | OPEN | C names four families; D has no family field. Three readings in close-reading §4 stay unchosen |
| Word identity / query | Separate gaps | Not smuggled into this coupling |
| Every touch must be used later | Not established | Piece 11 / B8: *can* participate |
| Emission accepted | No | Draft still DRAFT. This execute is not acceptance |

## 7. Q5 — Can the current diagram answer without a new mechanism?

**Pass 2: NARROW.**

Split “the diagram”:

- **Hands A–D:** already show definition-as-route, reuse through the same stars, remaining touch, and POINTER participation. No new Core mechanism is required for those statements. No new drawing is required to *state* them; they are on the source pictures.
- **ASCII companion:** still does not exhibit the *join* Pass 1 asked for. Neighboring POINTER panel and D1 block show participation in general. The local star write-caption does not scope itself. A source-linked coupling note plus a scoped caption remain the smallest *presentation* response.

Pass 1 proposals A and B are still the right size for that companion work. They are still **NOT_APPLIED**. They are maps work. This ticket does not apply them.

What a correction would preserve: Piece 7–11 wording, D1 as specimen not scheduler, draft unaccepted, no Star table, no definition field, no new entry type.

## 8. Propose / attack / resolve

**Propose:** Pass 1 was right that the sources already describe the coupling, and that the companion should either show it or point at it.

**Attack:** A and B are still one document. Opening the PNGs could have been used to declare the companion problem “solved because Hands already speak.” That would erase the packet’s actual target (does *the current companion / diagram-as-used* exhibit the join?). D1 still is not a recursive dictionary specimen. This agent is a second reader, not maps, not a third adjudicator.

**Resolve:** Confirm the relationship. Narrow Q5 to Hands-vs-companion. Keep construction open. Leave A/B unapplied. Do not treat VIEWED pictures as accepted emission.

Roles: Pass 1 three-role review = Astra-evidence. Pass 2 reread = Grok-evidence. Maps = `NOT_RUN`. Third adjudicator = absent.

## 9. Execution receipt

| Planned step | Outcome |
|---|---|
| 1 Freeze | Run pin `32ab724`; blobs recorded; A/B/R retained |
| 2 Reread claims | Packet, Pieces 7–11/15, law MEANING block, Pass 1 findings as candidate, draft unread-as-answers |
| 3 Open A–D | All four VIEWED. D1 matches prior transcription. MIME-sniff noted, bytes untouched |
| 4 Two-view attack | Occurrence-only completeness REFUTED. Lawful partial view not refuted |
| 5 Separate gaps | Q4 table confirmed; images add no emit-when |
| 6 Bounded result | This file |

**Must-not:** companion, manifest, law, pointer draft, PNG bytes, `src/` — untouched.

**Stop:** Pass 2 execute complete under the plan’s stop test. Campaign remains HELD until the human frees it. Next legal objects, if any: maps sit of A/B, or a third reader. Not Pass 3 by default.

Shoe in hands. The pictures show the parts. The write that would cut a route remains `[GAP]`.
