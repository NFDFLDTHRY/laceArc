# Relationship evidence — Pass 3 plan

**Status:** EXECUTED (handoff published). A/B still NOT_APPLIED. Maps unclaimed.  
**Station:** history (HELD Grok-evidence).  
**Object:** Pointable maps-handoff for proposals A/B after Pass 1+2. Read-only companion search. No companion edit.  
**Parents:** [`relationship-evidence-pass-1-findings.md`](relationship-evidence-pass-1-findings.md) proposals A/B; [`relationship-evidence-pass-2-findings.md`](relationship-evidence-pass-2-findings.md) Q5 NARROW (Hands show the parts; companion does not exhibit the join).  
**Not:** recovery pass 8; quiet-door #5; global ASCII pass; maps claim; apply A/B; emission answers; Pass 4 by default.  
**Emission:** `[GAP]`. No `src/`. Packet and Pass 1–2 findings stay.

```
pull/ff → refresh history
        → write this plan
        → index
        → check · commit
        → STOP
execute (later, one authorized tick):
        → six steps below
        → write docs/history/relationship-evidence-pass-3-handoff.md
        → stamp this plan EXECUTED
        → index
        → check · commit
        → STOP
        → do not claim maps
        → do not edit docs/systems-manifest-ascii.md
```

---

## 0. One question

After Pass 2 split Hands pictures from the ASCII companion, what exact companion loci would a maps editor need in order to sit proposals A/B — and has any later companion section already exhibited the join so that A/B would be stale?

Pass 3 produces a **handoff**. It does not apply the handoff. Maps stays a different door.

---

## 1. Inherited pins (do not redraw)

| Name | SHA / blob | Role |
|---|---|---|
| Samples A / B | `f3b554c` / `856e91d` | Persistence pair; same manifest blob |
| Manifest | `ca8b7d7bc4a93eea81510693424d450d66c6851a` | Unchanged through Pass 2 |
| Pass 1 findings | first publish `9019506` | Wording of A/B |
| Pass 2 findings | publish `2414b8b` | Hands VIEWED; Q5 NARROW |
| ASCII companion at Pass 2 freeze | `2628df45e644f56b27e03c4af4460e89b47f905d` | Target of the search |
| Pointer draft | `6399a1680cb5f8327f093fc2425c944a47113c98` | Stays questions |

If the companion blob moved before execute, record the new blob and search *that* file. Do not pretend Pass 2’s “underexpressed” still holds without a check.

---

## 2. Why this is not maps and not Φ

| Work | Station | Pass 3 |
|---|---|---|
| Sit / apply A/B on `docs/systems-manifest-ascii.md` | maps | Forbidden on this ticket |
| Fill pointer-emission answers | law / human | Forbidden |
| Point at companion lines and say what A/B would preserve | history | Allowed, as a handoff file |
| Re-open Q1–Q5 from scratch | — | Not this pass. Pass 2 dispositions stand unless the companion blob changed the exhibit |

---

## 3. Six execute steps (local; not run on this tick)

### Step 1. Freeze

Refresh main. Record companion blob vs Pass 2’s `2628df45`. Record whether maps is still FREE. History must still be HELD by Grok-evidence.

### Step 2. Carry A/B as proposals, not as law

Copy Pass 1’s two replacement paragraphs into the handoff **as quoted proposals**. Do not tighten them into an emit-when. Do not invent a third proposal unless the companion search finds a *different* ambiguous caption that A/B do not cover. A third item must be labeled EXTRA and smaller than a new mechanism.

### Step 3. Read-only companion search

Search `docs/systems-manifest-ascii.md` at the frozen blob for whether the join is already explicit.

Look for, in context, not as isolated words:

- definition route through ordinary stars **and** later reuse / retained touch in the same passage or adjacent panel
- star write/view captions (`write: the WORD rows only` and neighbors)
- Piece 7–11 / 15 callouts that already tell the reader to read those pieces *together* as this coupling
- any later global-pass verdict that already records this exact presentation gap as closed

Record hits with line ranges. Two outcomes are both legal:

- **Still missing:** handoff proceeds with loci for A (star panel) and B (dictionary / star / participation neighborhood).
- **Already exhibited:** handoff says A/B are stale and maps owes no caption work from this campaign.

Do not treat “dictionary” appearing somewhere as the join.

### Step 4. Attack the handoff

Opposite errors to refuse in the handoff itself:

- “Companion silence means the relationship is absent from Hands.”
- “Hands pictures mean the companion needs no caption.”
- “Apply A/B from history.”
- “A scoped star caption decides Φ.”
- “Search found the words ‘definition’ and ‘touch’; therefore the join is shown.”

### Step 5. Separate doors

The handoff table must keep three columns:

| Door | Owner | Status after Pass 3 execute |
|---|---|---|
| Evidence campaign (Q1–Q5) | history | Closed as of Pass 2 unless companion blob refutes the NARROW |
| Presentation A/B | maps | Handoff ready or stale; still `NOT_APPLIED` |
| Construction / Φ | human + `pointer-emission.md` | Untouched |

### Step 6. Bounded artifact

Write [`relationship-evidence-pass-3-handoff.md`](relationship-evidence-pass-3-handoff.md) with:

- companion blob + line loci
- A/B quoted
- search outcome (missing / already exhibited)
- refuse list
- “maps may claim; history must not apply”
- Φ `[GAP]`

Then stamp this plan EXECUTED. Do not claim maps. Do not edit the companion.

---

## 4. Execute may write

| Path | Why |
|---|---|
| `docs/history/relationship-evidence-pass-3-handoff.md` | Handoff |
| `docs/history/relationship-evidence-pass-3-plan.md` | Stamp + receipt |
| `docs/history/README.md` | One index line |
| `docs/coord/stations/history.station` | Claim / refresh |

---

## 5. Execute must not write

- `docs/systems-manifest-ascii.md` and other maps paths
- manifest, law, `AGENTS.md`, pointer draft
- `docs/graphics/**` bytes
- `src/`
- Quiet-door #5
- A new Q1–Q5 findings rewrite unless the companion blob forces a correction note

---

## 6. Stop test

**This planning tick:** plan exists, indexed, Pass 1–2 files untouched, no handoff file yet.

**Execute:** handoff names loci or names the later section that already exhibits the join; A/B still unapplied by this agent; maps unclaimed by this agent; Φ `[GAP]`.

Release of history is not automatic.

---

## 7. Planning receipt — 19 September 2026

**Performed:** Pass 2 execute `2414b8b`; human instruction “Plan pass 3”; `git pull --ff-only`; `coord.sh refresh` + `check` history Grok-evidence at `93a2bc088bb955928820e52a50f65bf80e551917`; maps observed FREE; this plan written.

**Not performed:** companion search execute; handoff file; maps claim; A/B apply; emission answers.

**Stations:** history HELD Grok-evidence. maps FREE. graphics FREE. law FREE. Door #5 CLOSED. Φ `[GAP]`.

## 8. Execute receipt — 19 September 2026

**Performed:** refresh/check at `fbc4e5ad17569a781c03a9189edffcfaafb02240`; companion blob still `2628df45`; search recorded in [`relationship-evidence-pass-3-handoff.md`](relationship-evidence-pass-3-handoff.md); join still missing; A/B loci named; maps left FREE.

**Not performed:** maps claim; companion edit; Φ answers; Pass 4.

Shoe in hands. Ticket closed as execute. Campaign hold remains until the human says otherwise.

