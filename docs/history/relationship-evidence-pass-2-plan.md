# Relationship evidence — Pass 2 plan (takeover)

**Status:** PLAN only. Not run.  
**Station:** history (HELD Grok-evidence).  
**Campaign:** relationship evidence. Not recovery pass 8. Not quiet-door #5. Not global ASCII Pass 26.  
**Emission:** `[GAP]`. No `src/`. No companion edit. Proposals A/B stay `NOT_APPLIED` unless a later maps tick is separately authorized.

```
pull/ff → which → claim history "Grok-evidence"
        → write this plan
        → index link
        → check · commit
        → STOP
execute (later, one authorized tick, named below):
        → independent reread of Q1–Q5 against pinned sources + four PNGs
        → write findings or sit-note
        → stamp this plan EXECUTED
        → check · commit
        → STOP
        → release only when the human says the campaign is done
```

---

## 0. Why this plan exists

Astra-evidence ran Pass 1 text review, published findings, and released History (`c4fa5d5`). The human asked this editor to take over that campaign.

Pass 1 completion test was met **for text**. Three rows were left open on purpose:

| Leftover | Pass 1 stamp | Who can close it |
|---|---|---|
| Canonical PNG verification | `BLOCKED` (contents-API / decode; A–D `NOT_VIEWED`) | A later history tick that actually opens the four files |
| Independent adjudication | `NOT_RUN` (same agent played propose / attack / resolve) | A different agent rereading the same pins |
| Maps response to proposals A/B | `NOT_APPLIED` / mapper `NOT_RUN` | **maps** station, not this file |

This plan authorizes preparation and, if later told to proceed, the first two leftovers only. It does not authorize applying A/B, editing `docs/systems-manifest-ascii.md`, filling `docs/pointer-emission.md`, or starting Pass 3.

---

## 1. Inherited pins (do not redraw)

From [`relationship-evidence-pass-1.md`](relationship-evidence-pass-1.md) and [`relationship-evidence-pass-1-findings.md`](relationship-evidence-pass-1-findings.md):

| Name | SHA / blob | Role |
|---|---|---|
| Sample A | `f3b554c2cff033d3033a5d05f15224bcbd612a36` | Manifest Piece 9 passage |
| Sample B | `856e91d1b7faa0ec62ea2ff2bcc5b95f98acc3aa` | Manifest Piece 11 passage |
| Manifest blob A=B | `ca8b7d7bc4a93eea81510693424d450d66c6851a` | Same document; not two witnesses |
| Review pin R | `a0d7e6aea0267e63ebe8ac7a5febdef85275d832` | Pass 1 execution pin |
| Findings first publish | `901950641eaa2f75e632f86fcadf1efc7b96ea73` | Q1–Q5 answers |
| Release | `c4fa5d55e27f135cf5302c43991a1c66e6d9cfa8` | History FREE after text review |

A and B remain a persistence pair, not independent corroboration. Sampling is not rerun.

Takeover claim pin (this tick): origin/main at claim time `46d2a24665eff0be5805824fa93cfa8e7c8cf14f`.

---

## 2. One question for Pass 2

Does an independent reader, with the four canonical PNGs actually opened, confirm, narrow, or refuse Pass 1’s text verdict — without treating that verdict as emission, and without editing the companion?

Pass 1 verdict to test, not to rubber-stamp:

- Word / definition-route / retained-touch coupling is supported in pinned manifest + live law.
- The ASCII companion keeps the component names and general participation mechanism but does not make this coupling explicit.
- Star caption `write: the WORD rows only` is ambiguous about membership vs Piece 7 threading.
- Same-record WORD-only vs WORD+POINTER views: occurrence listing is not a complete exhibit of recorded participation. A lawful partial view is not refuted.
- Construction (when / arity / adjacency / slots) stays unspecified. Proposals A/B are presentation only and unapplied.

A confirmation is allowed. A refusal is allowed. A “text holds; image row still blocked” is allowed only if this editor also cannot view the PNGs.

---

## 3. What this editor can do that Pass 1 could not

Astra’s image path: GitHub contents fetch returned empty / failed PNG decode. Graphics `NOT_VIEWED`.

This editor has a working clone and opened Graphic D (`docs/graphics/data-structure-1d-array.png`) during preparation. The four files exist on disk:

- `docs/graphics/from-words-to-worlds.png`
- `docs/graphics/mechanisms-in-3d.png`
- `docs/graphics/n-dimensional-relationships.png`
- `docs/graphics/data-structure-1d-array.png`

That unblocks **attempting** the image row. It does not execute it. Execute still needs a later “proceed” tick and a findings/sit file. Graphics station stays unclaimed unless a later tick only *reads* the PNGs (read does not require a graphics claim). Bytes of the PNGs are not to be rewritten.

---

## 4. Execute may write (only after proceed)

| Path | Why |
|---|---|
| `docs/history/relationship-evidence-pass-2-findings.md` | Independent sit of Q1–Q5 + image row |
| `docs/history/relationship-evidence-pass-2-plan.md` | This file: stamp EXECUTED + receipt |
| `docs/history/README.md` | One index line |

Preserve: original packet, Pass 1 plan, Pass 1 findings. Do not rewrite Astra’s answers in place. Disagreement is recorded in the Pass 2 findings, as confirm / narrow / refuse per question.

---

## 5. Execute must not write

- `docs/systems-manifest-ascii.md` and other maps paths (proposals A/B live there)
- `docs/systems-manifest.md`, law, `AGENTS.md`, `docs/pointer-emission.md`
- `docs/graphics/**` bytes
- `src/`, Cargo, emit stubs
- Quiet-door #5 files
- A new recovery extract (not pass 8)

Maps remains FREE at plan time. A/B are a **different door**. History will not sneak them in.

---

## 6. Independent adjudication rule

Pass 2 is the second reader. It is still one agent. It does **not** count as a third independent adjudicator, and it must not invent a mapper reply.

Label the roles in the findings:

- Pass 1 propose/attack/resolve: Astra-evidence
- Pass 2 reread: Grok-evidence
- Maps response: `NOT_RUN` until maps claims
- Human acceptance of emission: still absent

---

## 7. Stop test

Preparation (this tick) is complete when:

- history is HELD by Grok-evidence
- this plan exists
- History-index links it
- Pass 1 files are untouched
- no findings file yet

Execute is complete only when each of Q1–Q5 has a Pass 2 disposition (confirm / narrow / refuse / still blocked), the image row is OBSERVED or still BLOCKED with the actual failure named, and A/B remain unapplied.

Release is not automatic. The campaign stays claimed until the human frees it or names the next object.

---

## 8. Preparation receipt — 19 September 2026

**Performed:** `git pull --ff-only`; `coord.sh which` on packet / findings / this plan / README / ASCII companion / Graphic A path; `coord.sh claim history "Grok-evidence"`; local open of Graphic D; read of Pass 1 packet, plan stop-boundary, findings §7–§8, History-index.

**Not performed:** Pass 2 Q1–Q5 reread write-up; full A–C panel walk; maps claim; A/B apply; emission answers.

**Stations at claim:** history HELD Grok-evidence `BASE=46d2a24665eff0be5805824fa93cfa8e7c8cf14f`. maps FREE. graphics FREE. law FREE. coord FREE. Door #5 CLOSED. Φ `[GAP]`.

**Operational:** this environment can fetch and push GitHub. `coord.sh` ran. That is the difference from Astra’s connector-only path, not a proof of their text.

Shoe in hands. Takeover is ownership of the leftover rows, not a new machine.
