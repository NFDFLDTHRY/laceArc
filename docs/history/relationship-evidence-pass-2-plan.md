# Relationship evidence — Pass 2 plan

**Status:** EXECUTED (findings published this campaign). Image row VIEWED. Maps A/B still NOT_RUN.  
**Station:** history (HELD Grok-evidence).  
**Object:** Independent reread of the Pass 1 packet and findings, with Graphics A–D opened.  
**Authority:** [`relationship-evidence-pass-1.md`](relationship-evidence-pass-1.md) (packet, frozen samples); [`relationship-evidence-pass-1-findings.md`](relationship-evidence-pass-1-findings.md) (text verdict to test); [`relationship-evidence-pass-1-plan.md`](relationship-evidence-pass-1-plan.md) §6 stop test (form); four files under [`../graphics/`](../graphics/README.md).  
**Not:** recovery pass 8; quiet-door #5; global ASCII Pass 26; maps apply of proposals A/B; emission answers; Pass 3.  
**Emission:** `[GAP]`. No `src/`. Packet and Pass 1 findings stay. Companion stays.

```
pull/ff → refresh history
        → write / thicken this plan
        → index
        → check · commit
        → STOP
execute (later, one authorized tick):
        → six steps below
        → write docs/history/relationship-evidence-pass-2-findings.md
        → stamp this plan EXECUTED
        → index
        → check · commit
        → STOP
        → release only when the human frees the campaign
```

---

## 0. One question

Does a second reader, with the four canonical PNGs actually opened, **confirm / narrow / refuse** Pass 1’s text verdict on the word / definition-route / retained-touch coupling — without treating that verdict as Φ, and without editing the companion?

Pass 1 verdict under test (do not rubber-stamp):

1. Coupling is supported in pinned manifest Pieces 7–11, 15 and live law.
2. ASCII companion keeps names and general participation; it does not make *this* coupling explicit.
3. Star caption `write: the WORD rows only` is ambiguous (no Star row vs occurrence-only formation).
4. Same-record WORD-only view vs WORD+POINTER view: occurrence listing does not fully exhibit recorded participation. A lawful partial view is not refuted.
5. Construction (when / arity / adjacency / slots / family discriminator) stays unspecified. Proposals A/B are presentation only and `NOT_APPLIED`.

Allowed outcomes: confirm; narrow; refuse; text-holds + image-row still BLOCKED (only if this editor also cannot view the PNGs).

---

## 1. Inherited pins (do not redraw)

| Name | SHA / blob | Role |
|---|---|---|
| Sample A | `f3b554c2cff033d3033a5d05f15224bcbd612a36` | Piece 9 definition-route passage |
| Sample B | `856e91d1b7faa0ec62ea2ff2bcc5b95f98acc3aa` | Piece 11 retained-touch passage |
| Manifest blob A=B | `ca8b7d7bc4a93eea81510693424d450d66c6851a` | Same document; not two witnesses |
| Review pin R | `a0d7e6aea0267e63ebe8ac7a5febdef85275d832` | Pass 1 execution pin |
| Findings first publish | `901950641eaa2f75e632f86fcadf1efc7b96ea73` | Q1–Q5 answers |
| Astra release | `c4fa5d55e27f135cf5302c43991a1c66e6d9cfa8` | History freed after text review |
| Takeover plan first publish | `ae43f88e35b5900fb613ca4a57a129b774ec25e3` | Thin plan |
| This thicken pin | origin/main at refresh, recorded in §9 |

Sampling is not rerun. A/B remain a persistence pair.

---

## 2. Roles (do not impersonate)

| Role | Who | Status after this plan |
|---|---|---|
| Pass 1 propose / attack / resolve | Astra-evidence | Done (text). Image BLOCKED. |
| Pass 2 reread | Grok-evidence | PLAN. Execute later. |
| Maps response to A/B | maps holder | `NOT_RUN`. Not this station. |
| Third independent adjudicator | — | Still absent. Pass 2 is a second agent, not a third. |
| Human acceptance of emission | human, in-repo | Absent. Unchanged. |

Do not invent a mapper reply. Do not count self-review as maps.

---

## 3. Six execute steps (local to this plan)

Not global-diagram pass numbers. Not run on this tick.

### Step 1. Freeze

Refresh main. Record run commit, blob identities for: packet, Pass 1 findings, manifest at R and at tip, ASCII companion at R and at tip, pointer draft, four PNG blobs. Confirm history still HELD by Grok-evidence. If packet or findings bytes moved, say so; do not silently rebase the questions.

### Step 2. Reread the load-bearing claims

Read, in this order, before assembling a disposition:

1. Packet Q1–Q5.
2. Manifest Pieces 7–11 and 15 at pin R (same blob as A/B).
3. Live law MEANING / Continue passages cited by Pass 1.
4. Pass 1 findings §§2–6 as *candidate*, not as source.
5. Pointer draft: questions only. Do not fill answers.

Per link, keep Pass 1’s claim card:

```
CLAIM
  exact pinned passage / graphic panel
  directly stated, or inference with named premises
  what the passage does NOT establish
  Pass 2: CONFIRM / NARROW / REFUSE / BLOCKED
```

Recursion of definition-by-words is not lookup, expansion, closure, or an emit schedule. Absence of those algorithms does not erase a relationship the sources state.

### Step 3. Open the four graphics

This is the row Pass 1 had BLOCKED.

Open, do not rewrite:

| File | What the reread is allowed to ask |
|---|---|
| `from-words-to-worlds.png` | Does the growth story show definition as a route through ordinary stars, or only as later documents? |
| `mechanisms-in-3d.png` | Repeat / thread / no-collapse. Does a later pass look like more wire, a stored gloss, or a collapsed node? |
| `n-dimensional-relationships.png` | Independent participation vs new meaning. C6 family names: drawn as axes, or as talk over one store? |
| `data-structure-1d-array.png` | D1 rows vs Pass 1 transcription. Backward refs. Two slots drawn. POINTER→POINTER drawn. Adjacent WORD pairs with and without a POINTER between them. |

Record for each graphic: `VIEWED` + what was read off the picture, or `BLOCKED` + the actual failure.

Do not derive Φ from D1. Pass 1 already used the close-reading transcription to refute “every adjacent pair emits.” Pass 2 may confirm that reading **from the PNG**. If the PNG and the transcription disagree, name the disagreement. Do not “fix” D by editing pixels.

Physical cord: `NOT_PERFORMED` unless the human is holding one and says so.

### Step 4. Attack both directions

Reuse Pass 1’s same-record two-view test on D1 (transcription + PNG if viewed):

- View A: WORD rows only.
- View B: those WORDs plus the six POINTER rows.
- Record unchanged.

Ask: does Pass 1’s explanation identify exactly what View A omits, without forbidding View A as a lawful partial read?

Opposite attack: does any Pass 1 sentence entail a stored gloss, Star table, automatic expansion, or invented emit-when? Cite the sentence. Do not repair it silently.

### Step 5. Separate diagram gaps from construction gaps

Walk Pass 1’s Q4 table. Each row: CONFIRM the disposition, NARROW it, or REFUSE it. Do not dump every leftover into G2.

Keep separate:

- Relationship supported and shown (source + picture or companion location).
- Relationship supported, companion underexpresses it.
- Construction unspecified (when / targets / arity / slots / adjacency-as-relation).
- Documentation CONFLICT (e.g. Piece 5 / AGENTS permit POINTER→POINTER; draft §4 still asks).
- Evidence unavailable (image still blocked; mapper silent).
- Human acceptance: still absent.

### Step 6. Bounded result

Write [`relationship-evidence-pass-2-findings.md`](relationship-evidence-pass-2-findings.md) with:

- Per-question CONFIRM / NARROW / REFUSE / BLOCKED.
- Image register (A–D).
- Counterexample outcome.
- Whether proposals A/B still look like the smallest *presentation* fix — still unapplied.
- Roles named. Mapper `NOT_RUN`. Emission unaccepted.

Do not edit the companion. Do not rewrite Pass 1 findings in place.

---

## 4. Execute may write

| Path | Why |
|---|---|
| `docs/history/relationship-evidence-pass-2-findings.md` | Second-reader sit |
| `docs/history/relationship-evidence-pass-2-plan.md` | Stamp EXECUTED + §10 receipt |
| `docs/history/README.md` | One index line |
| `docs/coord/stations/history.station` | Claim / refresh / later release |

---

## 5. Execute must not write

- `docs/systems-manifest-ascii.md` and other maps paths (A/B live there)
- `docs/systems-manifest.md`, law, `AGENTS.md`, `docs/pointer-emission.md`
- `docs/graphics/**` bytes
- `src/`, Cargo, emit stubs
- Quiet-door #5 files
- A new recovery extract

---

## 6. Stop test

**This planning tick** is complete when the six steps exist on this file, the index points here, Pass 1 files are untouched, and no findings file exists yet.

**Execute** is complete when each of Q1–Q5 has a Pass 2 disposition, the image row is VIEWED or BLOCKED with the failure named, A/B remain unapplied, and Φ is still `[GAP]`.

Release is not automatic.

---

## 7. Image capability noted in preparation (not an execute result)

Pass 1 image path: GitHub contents fetch empty / PNG decode failed. A–D `NOT_VIEWED`.

This editor opened Graphic A and Graphic D from the local clone during planning. Files present:

- `docs/graphics/from-words-to-worlds.png`
- `docs/graphics/mechanisms-in-3d.png`
- `docs/graphics/n-dimensional-relationships.png`
- `docs/graphics/data-structure-1d-array.png`

That unblocks *attempting* Step 3. Opening a file in a plan tick is not the findings image register. Step 3 still waits on **proceed**.

---

## 8. What Pass 2 will not decide

- When a POINTER is written.
- Arity as law.
- Slot assignment rule.
- Whether adjacency is already a relation.
- Which C6 family reading holds.
- Whether maps should apply A/B.
- Whether `src/` may exist.

Those stay on the Hands gap board. Pass 2 may only say whether Pass 1 *described that board honestly*.

---

## 9. Planning receipt — 19 September 2026

**Performed:** takeover claim `ae43f88`; first thin plan; human instruction “Plan pass 2”; `git pull --ff-only`; `coord.sh refresh` + `check` history Grok-evidence; thicken this file to a six-step execute ticket; local open of Graphics A and D as capability note only.

**Not performed:** Steps 1–6 execute; findings file; maps claim; A/B apply; emission answers; physical lace.

**Stations at this receipt:** history HELD Grok-evidence. maps FREE. graphics FREE. law FREE. Door #5 CLOSED. Φ `[GAP]`.

**BASE at refresh for this thicken:** `74527e5a25ddc37d641d961ee532faa8933e29a7` (`== origin/main` at check).

## 10. Execute receipt — 19 September 2026

**Performed:** `git pull --ff-only`; `coord.sh refresh` + `check` history Grok-evidence at `32ab7242b87bafc91843b996049e066a7b5e27c5`; opened Graphics A–D from the clone; reread packet Q1–Q5, Pieces 7–11 and 15, law MEANING block, Pass 1 findings as candidate, pointer draft as questions; wrote [`relationship-evidence-pass-2-findings.md`](relationship-evidence-pass-2-findings.md); stamped this plan EXECUTED; index update.

**Dispositions:** Q1 CONFIRM + NARROW exhibit; Q2 CONFIRM; Q3 CONFIRM; Q4 CONFIRM open board; Q5 NARROW Hands-vs-companion. A/B unapplied. Φ `[GAP]`.

**Not performed:** maps claim; companion edit; third adjudication; physical lace; emission answers; Pass 3.

Shoe in hands. Ticket closed as execute. Campaign hold remains until the human says otherwise.

