# Quiet door #5 (History) — Pass 1 PLAN

**Status:** PLAN only. Not run.  
**Station:** history (HELD Grok-history).  
**Object:** quiet door #5 — [`docs/history/README.md`](README.md).  
**Why history, not kit:** history OWNS `docs/history/**`. The companion can live next to the door without a maps catch-all file at `docs/`. Kit stays FREE.  
**Not:** history pass 8. Not a new extract. Not a walker revival. Not live-law rewrite. Not door #4 pass 7.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the history door. Do not rewrite extracts or sit-notes.

```
pull/ff → refresh history
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/history/quiet-door-5-ascii.md spine
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the history door be one ASCII spine of what it names (readings, not Core; extract vs sit-note; passes 1–7 bound) and what it refuses (source of record, emitter, revived walkers, `src/`) — without becoming a new extract?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| History pass 6 | HCC / Cup / Water originals vs surfaces; extract + sit-note |
| History pass 7 | bound; cites pass 6; evidence JSON |
| Register row #5 | LIVE; `which` = history |

This campaign makes #5 activity WORKED, then CLOSED at pass 6 of *this door iteration*. That is not history pass 8. That does not flip LIVE.

---

## 2. Spine execute will draw (shape only)

Off the history door only. Do not paste extracts.

```
  HISTORY DOOR
  docs/history/README.md
  readings against current law
  not Core; not an emitter; not source of record
  four graphics remain Hands
  rejected walkers stay rejected

  Extract vs sit-note
    extract  = what a pass recovered (evidence)
    sit-note = how that extract sits against law now (ruling)
    separate files on purpose

  Bound
    pass 1 sit
    pass 2 sit
    pass 3 extract
    pass 4 extract + sit
    pass 5 extract + sit
    pass 6 plan + extract + sit
    pass 7 extract + evidence JSON

  [X]  new extract this iteration
  [X]  revive a walker
  [X]  history amends live law
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

If a heading cannot be read off the door, `[GAP]`. Do not invent pass 8.

---

## 3. What execute may write

History only.

- Create [`docs/history/quiet-door-5-ascii.md`](quiet-door-5-ascii.md).
- Mark this plan EXECUTED.

Do not edit [`README.md`](README.md). The door is the object.

---

## 4. What execute must not do

- Edit the history door, any extract, any sit-note, or pass 6/7 plans.
- Revive a walker named in AGENTS.
- Claim kit / law / maps.
- Fill G2.
- Start pass 2 fields.
- Open history pass 8.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read `docs/history/README.md`. Quote headings, do not add extracts.
2. Draw the spine on the history companion. Stop.

Success: one spine; door bytes unchanged; extracts untouched; G2 `[GAP]`.  
Failure: new extract; walker revived; door rewritten.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain: extract vs sit-note vs law |
| 4 | mechanism: readings do not amend law |
| 5 | seam vs live law and vs court |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
History reads. Law stores.
Walkers stay dead.
This is not pass 8.
Φ stays [GAP].
```
