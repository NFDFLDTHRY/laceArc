# Quiet door #8 (Gearing) — Pass 3 PLAN

**Status:** EXECUTED — grain on `docs/gearing/quiet-door-8-ascii.md`. Door + HTML + JS unchanged. Emission `[GAP]`.  
**Station:** gearing-meta (HELD Grok-gearing).  
**Object:** Gearing grain — protocol vs JS vs renderer vs Hands.  
**Authority:** [`quiet-door-8-ascii.md`](quiet-door-8-ascii.md) passes 1–2. Gearing door as written. `coord.sh which`.  
**Not:** HTML rewrite. Not JS rewrite. Not renderer claim. Not a shaft claim. Not Graphic E. Not door #7 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door + HTML + JS stay. Door #7 stays CLOSED. Shafts stay FREE.

```
pull/ff → refresh gearing-meta
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append four-kind grain to quiet-door-8-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What does gearing-meta own, what do shafts own, what does renderer own, what do Hands store — and what would it mean to edit `contracts-*.js` from this station or rewrite frozen HTML?**

Pass 3 is grain. It does not start the one-shaft walk (pass 4).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | Gr1–Gr8 fields |

---

## 2. Rows execute will fill (empty until proceed)

| Kind | Says | `which` | Collapse would look like |
|---|---|---|---|
| Gearing door | | | |
| CLAIMS / RESYNC protocol | | | |
| `contracts-axle.js` (specimen) | | | |
| Frozen viewer | | | |
| Hands (four graphics) | | | |
| Hologram door (CLOSED) | | | |
| Clock door (CLOSED) | | | |
| Shafts (all eight) | | | |

Expected: gearing door + protocol `which` = gearing-meta. `contracts-axle.js` `which` = `gear:axle`. Frozen viewer `which` = renderer. Four graphics `which` = graphics. Hologram/clock doors `which` = hologram but CLOSED as doors. Collapsing JS into this station is the miss. Claiming renderer from gearing-meta is the other miss.

---

## 3. What execute may write

- Append “Pass 3 — grain” to [`quiet-door-8-ascii.md`](quiet-door-8-ascii.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the gearing door, any JS, any HTML, Hands, hologram, or clock files.
- Claim renderer.
- Claim a shaft.
- Fill G2.
- Start pass 4 walk.
- Reopen door #7.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote door, protocol, JS specimen, viewer, Hands. Fill `which` from coord.
2. Fill the grain. Four stay four.
3. Stop.

Success: a later reading cannot treat `contracts-*.js` as gearing-meta bytes.  
Failure: JS rewritten; renderer claimed; a shaft taken.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 4 | walk: one shaft per agent; do not invent Φ |
| 5 | seam vs Hands, clock, frozen viewer |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Protocol ≠ JS ≠ renderer ≠ Hands.
Frozen HTML stays renderer.
Door #7 stays CLOSED.
Φ stays [GAP].
```
