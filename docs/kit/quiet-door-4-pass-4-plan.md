# Quiet door #4 (live law) — Pass 4 PLAN

**Status:** EXECUTED — walk on `docs/kit/quiet-door-4-ascii.md`. Pair untouched. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** live-law **walk** — one mechanism on [`quiet-door-4-ascii.md`](quiet-door-4-ascii.md).  
**Authority:** live-law passes 1–3. Live-law file as a *named door*, not bytes to edit.  
**Not:** live-law rewrite. Not the pair. Not Hands verbs. Not G2 fill.  
**Emission:** `[GAP]`. No `src/`.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append walk mechanism to quiet-door-4-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What single walk does live law run — this file stores, copies lag — how does every L-block apply it, and what does it refuse versus Hands verbs and versus resolving the pair?**

The store’s mechanism is not arrive / through-star / continue. Those three verbs belong to Hands. This walk **wins where a copy disagrees, and does not accept emission**.

Pass 4 is the mechanism map. It does not start the seam (pass 5).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | L1–L8 fields |
| 3 | exists ≠ accepted; copies lag |

---

## 2. Operator as live law states it (T1)

Working sentence (the door wins; execute quotes, does not invent a Core verb):

> This file wins where copies lag. Existence is not acceptance.

Walk:

```
 reader arrives at docs/law-why-these-documents.md
   if a copy disagrees          → this file wins
   if emission is asked         → DRAFT; not accepted
   if the pair is asked         → human; do not edit
   never: exists ⇒ accepted
   never: transport ⇒ winner
   never: kit reading ⇒ store
```

Execute draws **one walk box** on the kit companion. Not on live law. Not “emit POINTER.”

---

## 3. Application register (T2)

Every L-block. `New machine?` must be `no`.

Rows execute will fill:

| # | Role | What arrives | What is walked | What continues | New machine? |
|---|---|---|---|---|---|
| L1 this file | | | | | |
| L2 banner | | | | | |
| L3 pair | | | | | |
| L4 Why | | | | | |
| L5 ASCII | | | | | |
| L6 IMPLEMENTATION LAW | | | | | |
| L7 Binding files | | | | | |
| L8 transports | | | | | |

Expected: all `New machine?` = no. This file is not Core. Pair stays human.

---

## 4. Live law versus Hands versus kit reading (T3)

```
 quiet-door-4-ascii.md       live law                 Hands
 --------------------       --------                 -----
 kit reading                the store                A13 verbs
 names the store            wins                     not this walk

 wrapper-as-Core            [X]
 Hands verbs pasted here    [X]
 pair resolved              [X]
 G2                         [GAP]
```

---

## 5. What execute may write

- Append “Pass 4 — walk” to [`quiet-door-4-ascii.md`](quiet-door-4-ascii.md).
- One README status tick.
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Edit live law, AGENTS, staking, transports, or `pointer-emission.md`.
- Copy A13 onto this walk.
- Resolve the pair.
- Fill G2.
- Start pass 5 seam.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read spine + grain. Quote those, not A13.
2. Draw one walk box. Fill the register. Every `New machine?` = no.
3. Draw live law vs Hands vs kit reading seam. Stop.

Success: one walk; this file still the store; pair still human.  
Failure: Hands verbs here; pair edited; DRAFT accepted.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 5 | seam vs court, package door, AGENTS |
| 6 | receipt |

---

## 8. Governing check

```text
One pass, one act.
This file stores. Copies lag.
Hands keeps its verbs.
The pair is human.
Φ stays [GAP].
```
