# Quiet door #1 (package) — Pass 1 PLAN

**Status:** PLAN only. Not run.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #1 — `docs/kit/READ_ME_FIRST.md` (package door).  
**Not:** cold-doors pass 7. The register is closed 6/6. This is pass 1 of a six-pass iteration **on that door**.  
**Authority:** [`READ_ME_FIRST.md`](READ_ME_FIRST.md). Then [`cold-doors.md`](cold-doors.md) pass 5 pin table (THEN/NOW already scored). Live `AGENTS.md` and live law as **targets the door names**, not as files this pass edits.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the package door. Do not retarget THEN/NOW.

```
pull/ff → claim kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/kit/quiet-door-1-ascii.md spine from the package door
        → one pointer line in docs/kit/README.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the package door be drawn as one ASCII systems diagram of what it routes, what it refuses to be, and what it leaves to live law — without becoming a second law, without moving THEN/NOW, and without filling G2?**

Pass 1 is the **spine**. Later passes hang fields, THEN/NOW/tip grain (already scored — do not redo the pin table), routing mechanism, companion seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Cold-doors 1–6 | register closed; door #1 stamp LIVE |
| Pass 5 pin table | THEN `4fe984b` / NOW `044257c` resolve as objects; four claims hold |
| Package door bytes | routing to live AGENTS + live law; DRAFT emission; no `src/` |
| Remainder | other stations; not this door |

Lossy if any of these happen: rewrite `READ_ME_FIRST.md`; retarget pins; treat this file as live law; fill `docs/pointer-emission.md`; start R1 repair.

---

## 2. Inventory (working; execute re-reads the door)

| Block on the door | Job |
|---|---|
| Opening | kit, not Hands; live law + AGENTS at tip win |
| THEN | dated pin; emission absent *at that pin*; old STALE banner was a warning, not current stamp |
| NOW | re-read pin; routing holds; DRAFT not accepted; no `src/`; not the tip |
| Integration paragraph | package is in-repo; does not establish enforcement |
| File table | agent-control / task-template / downstream-audit |
| Routing block | paste text for harnesses that cannot load the repo |
| Audit paragraph | THEN pin; F10 absence then, DRAFT now; body not rewritten |
| Ready / not-ready | what the package contains vs what it does not |

**[GAP]** Any printed heading execute cannot read. PNG-style: the markdown wins.

---

## 3. Spine execute will draw (shape only; not frozen)

```
  READ_ME_FIRST          kit, not Hands
          |
          v
  live AGENTS.md  +  live law     <-- door routes here
          |
          +-- THEN 4fe984b   emission absent in that tree
          +-- NOW  044257c   re-read, not the tip
          |
          v
  companions (DATED / LIVE)
          agent-control
          task-template
          downstream-audit (THEN snapshot)
          |
          v
  [X]  second law
  [X]  acceptance record
  [X]  src/
  [X]  enforcement
  [GAP] pointer-emission answers
```

---

## 4. What execute may write

Kit only.

- Create [`docs/kit/quiet-door-1-ascii.md`](quiet-door-1-ascii.md).
- One pointer sentence in [`docs/kit/README.md`](README.md).
- Mark this plan EXECUTED.

---

## 5. What execute must not do

- Edit `READ_ME_FIRST.md`.
- Retarget THEN / NOW / `e21cf39`.
- Edit live law, AGENTS, transports, audit body.
- Claim law / hologram / maps / graphics.
- Fill G2.
- Reopen the cold-doors register as pass 7.
- Add `src/`.

---

## 6. Single execute act

When the human says proceed (not this tick):

1. Re-read `READ_ME_FIRST.md`. PNG/markdown wins.
2. Draw the spine. Dual-bind: this door routes; live law is the store of law.
3. Leave G2 `[GAP]`. Do not move pins.
4. Point from the kit README. Stop.

Success: a reader can walk the door with the file open and never think kit is live law.  
Failure: the door was rewritten, pins moved, or emission filled.

---

## Queued — later passes on this door, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain (THEN tree vs NOW tree vs tip) — cite pass 5, do not retarget |
| 4 | routing mechanism vs live law |
| 5 | companion seam |
| 6 | receipt |

---

## 7. Governing check

```text
One pass, one act.
This is door #1, not cold-doors pass 7.
The door routes. Live law wins.
THEN/NOW stay. Φ stays [GAP].
```
