# Cold doors — Pass 6 PLAN

**Status:** EXECUTED — remainder table on `docs/kit/cold-doors.md`. No foreign file edited.  
**Station:** kit.  
**Agent:** Grok-kit.  
**Plan BASE:** `fa7c84266c8a257a90631b86095562009173219c`.  
**Emission:** `[GAP]`. No `src/`. Renderer stays paused. Seven Rust doors stay shut.

Pass 5 executed the package-door pins. It queued this remainder table. Do not reopen THEN/NOW.

```
pull/ff → claim kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append one off-kit remainder table to docs/kit/cold-doors.md
        → check · commit · release · STOP
```

---

## 0. Question (one act)

**What off-kit remainders did passes 1–5 notice and refuse to repair, and which station owns each — without claiming those stations, without repairing R1, without resuming the renderer, and without opening Rust?**

Pass 6 is a kit register of other people's doors. It is not a law pass, not a hologram resume, not a maps birth.

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1–2 | ten quiet doors LIVE; companions LIVE or DATED; none STALE |
| 3 | transport fences identical; pin `e21cf39` UNREACHABLE |
| 4 | R1 sit: live law LIVE, staking ORIGINAL, AGENTS POINTER, two transports DATED |
| 5 | THEN `4fe984b` / NOW `044257c` resolve as objects; four claims hold; door unchanged |

---

## 2. Rows execute will fill (empty until proceed)

Stamp only. Do not change the named object's bytes.

| Remainder | Station that would own a repair | Stamp already on register | Pass 6 sit (hold / moved / `[GAP]`) | Kit may |
|---|---|---|---|---|
| R1 size split (four law-bearing files) | law | noticed, not repaired | | name the station |
| Live-law header pair (exists ≠ accepted) | law | noticed pass 1 / 3 | | name the station |
| Pin `e21cf39` UNREACHABLE | kit (label only) | DATED companions | | keep the label; do not retarget |
| Graphics door row says station `law` | graphics + coord | door 10 | | notice; do not rewrite OWNS |
| Projection HTML campaign | hologram | PAUSED | | do not resume |
| Seven Rust doors | maps | SHUT | | do not open |
| Emission | law | `[GAP]` | | do not fill |
| Graphics ASCII i3 (D companion) | graphics | queued, no plan | | do not start |

Hold = still true at execute tip. Moved = another station already changed it. `[GAP]` = kit cannot see from this station.

Not a row: rewrite live law; retarget THEN/NOW; claim hologram / maps / law / graphics.

---

## 3. What execute may write

Kit only.

- Append “Pass 6 — off-kit remainder” to [`docs/kit/cold-doors.md`](cold-doors.md).
- One README line to mark this plan EXECUTED.

---

## 4. What execute must not do

- Claim law, hologram, maps, graphics, or coord.
- Edit live law, AGENTS, staking, transports, projection HTML, or Rust cards.
- Retarget THEN / NOW / `e21cf39`.
- Fill G2 or `docs/pointer-emission.md`.
- Start graphics iteration 3.
- Add `src/`.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read the register rows named in §2. Confirm stamps still match the tree.
2. Fill Sit. If kit cannot see a station's file without crossing OWNS, `[GAP]`.
3. Stop. Release kit.

Success: every remainder has a station and a stamp; no foreign file edited.  
Failure: R1 “fixed,” renderer resumed, Rust opened, or emission filled.

---

## Queued — after pass 6, not this file

A later named pass on **law** / **hologram** / **maps** / **graphics**. Kit does not invent those tickets.

---

## 6. Governing check

```text
One pass, one act.
Kit names other doors. It does not walk through them.
PAUSED stays paused. SHUT stays shut.
Φ stays [GAP].
```
