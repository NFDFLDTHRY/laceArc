# Quiet door #4 (live law) — Pass 3 PLAN

**Status:** EXECUTED — grain on `docs/kit/quiet-door-4-ascii.md`. Pair untouched. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** live-law grain — header pair vs IMPLEMENTATION LAW vs transports.  
**Authority:** [`quiet-door-4-ascii.md`](quiet-door-4-ascii.md) passes 1–2. Live-law file as written. `coord.sh which`.  
**Not:** live-law rewrite. Not the pair. Not transport regen.  
**Emission:** `[GAP]`. No `src/`. Door bytes stay.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append three-source grain to quiet-door-4-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What does the header pair say, what does the IMPLEMENTATION LAW block say, what do the transports carry, and what would it mean to collapse those into one sentence an agent may “fix”?**

Pass 3 is grain. It does not start the stores-vs-lags walk (pass 4).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | L1–L8 fields |

---

## 2. Rows execute will fill (empty until proceed)

| Source | Says | `which` | Collapse would look like |
|---|---|---|---|
| Header pair | | | |
| Line 10 (“exists”) | | | |
| IMPLEMENTATION LAW (“accepted”) | | | |
| staking-the-workspace.md | | | |
| AGENTS.md | | | |
| kit agent-control | | | |
| kit grok-bot-profile | | | |
| pointer-emission.md | | | |

Expected: pair names both sentences and forbids agent edit. Line 10 = exists. Block = accepted. Transports `which` = kit and may lag. Emission file exists as DRAFT, not accepted. Collapsing exists-into-accepted is the miss this grain stops.

---

## 3. What execute may write

- Append “Pass 3 — grain” to [`quiet-door-4-ascii.md`](quiet-door-4-ascii.md).
- One README status tick.
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit live law, AGENTS, staking, transports, or `pointer-emission.md`.
- Resolve the pair.
- Claim law.
- Fill G2.
- Start pass 4 walk.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote pair, block, `which`. Do not pick a winner.
2. Fill the grain. Three stay three.
3. Stop.

Success: a later agent cannot “fix” exists into accepted without ignoring this grain.  
Failure: pair edited; transport regenerated; emission accepted.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 4 | mechanism: this file stores; copies lag |
| 5 | seam vs court, package door, AGENTS |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Exists is not accepted.
Copies lag. This file wins.
The pair is human.
Φ stays [GAP].
```
