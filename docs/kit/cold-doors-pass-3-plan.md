# Cold doors — Pass 3 PLAN

**Status:** EXECUTED. Inventory is the Pass 3 section of [`cold-doors.md`](cold-doors.md).
**Station:** kit.
**Agent:** Grok-kit.
**Plan BASE:** `6dd12634189df98065cf73b00da69e625e235a6b`.
**Execute BASE:** recorded at commit time against `origin/main`.
**Emission:** `[GAP]`. No `src/`. Renderer stays paused. Seven Rust doors stay shut.

Pass 2 said stop. The human named pass 3.

```
pull/ff → claim kit
        → write this plan
        → STOP
execute (this tick):
        → append one lag-inventory section to docs/kit/cold-doors.md
        → check · commit · release · STOP
```

---

## 0. Question

**What, exactly, lags in the two DATED transport copies against the live-law fenced block — and does pin `e21cf39` still resolve on this repo?**

Not: regenerate `agent-control.md` or `grok-bot-profile.md`.
Not: edit live law or staking ASCII.
Not: close R1 by making the four files the same size.
Not: unpause the renderer. Not: open Rust doors.

---

## 1. Why pass 3 exists

Pass 1 stamped doors. Pass 2 stamped companions. Both transport copies are **DATED** at `e21cf39` and already say live law wins.

What is still unmeasured on this shelf: *which blocks* differ, not only that the files have different byte counts (R1 sizes).

| Object | Role in pass 3 |
|---|---|
| `docs/law-why-these-documents.md` fenced ASCII | Read-only source. Law station owns the file. |
| `docs/kit/agent-control.md` law block | Compare. Do not paste over it. |
| `docs/kit/grok-bot-profile.md` Explanation block | Compare. Do not paste over it. |
| Commit `e21cf39` | Does the declared regen pin resolve? Yes/no + locator. |

Downstream audit, task forms, githack, and `.claude/` stay at their pass-2 stamps. No re-walk.

R1 repair remains a **law-station** pass if the human names that. Pass 3 only inventories.

---

## 2. What execute may write

Kit only.

- Append “Pass 3 — transport lag inventory” to [`docs/kit/cold-doors.md`](cold-doors.md).
- Rows: pin resolvable?; block present/absent; existence-vs-acceptance sentence; any other named KEEP/FORBID drift found by reading the fenced block against the two copies.
- One README line only if the inventory changes how the index should speak (it should not retarget `e21cf39`).

No new register file. No copy rewrite.

---

## 3. What execute must not do

- Replace a transport block with live-law bytes (restructure phase 3).
- Edit `docs/law-why-these-documents.md`, `docs/staking-the-workspace.md`, or `AGENTS.md`.
- Treat a missing line in a copy as license to delete it from live law, or a extra line in a copy as license to add it to live law.
- Touch renderer, emission, history, hologram, maps, clipboards.

---

## 4. Single execute act

When the human says proceed:

1. Resolve `e21cf39` (`git cat-file` / `git log -1`). Record full SHA or “unreachable.”
2. Read the live-law fenced `STAKING THE WORKSPACE` block.
3. Read the corresponding blocks in the two kit copies.
4. Write the inventory table. Mark `[GAP]` if a difference cannot be named without guessing.
5. Stop. No pass 4.

Success: pin fate recorded; at least the existence-vs-acceptance pair is located in live law vs each copy or marked absent. Failure: a copy or live law was rewritten.

---

## 5. Governing check

```text
Size difference is not a block inventory.
A named lag is not a repair.
Law owns the law. Kit owns the register.
Φ stays [GAP].
```
