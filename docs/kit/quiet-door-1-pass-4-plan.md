# Quiet door #1 (package) — Pass 4 PLAN

**Status:** PLAN only. Not run.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #1 — routing mechanism vs live law.  
**Authority:** [`READ_ME_FIRST.md`](READ_ME_FIRST.md). Then [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md) passes 1–3. Live law and `AGENTS.md` as **targets the door names**, not files this pass edits.  
**Not authority:** live-law fence dump. R1 repair. Graphic A/B/C application registers copied in.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the package door. Do not claim law.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append mechanism map to quiet-door-1-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What single mechanism does the package door state, how does every block apply it (or forbid a fake), and what does it refuse to add — especially versus live law?**

The door's mechanism is not A13/B12/C12. Those three verbs belong to Hands. This door **routes**. It does not append WORD.

Pass 4 is the mechanism map. It does not start the companion seam (pass 5).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | Spine: door routes; live law wins; `[X]` second law / src / enforcement |
| 2 | D1–D8 fields |
| 3 | THEN / NOW / tip stay three times |

---

## 2. Operator as the door states it (T1)

Working sentence (door wins; execute quotes, does not invent a fourth verb):

> Live law is AGENTS.md and docs/law-why-these-documents.md at the current tip. No wrapper here substitutes for those.

Plus the harness block's order: read live law → read kit controls → follow the law's source-reading order. Emission stays a gap unless accepted.

Execute draws **one route box**. Not “emit POINTER.” Not “accept the DRAFT.” Not “append to D.”

---

## 3. Application register (T2)

Every D-block is precondition, pin, constraint, index, route, or statement.

Rows execute will fill. Door wins. `New machine?` must be `no` or the block is misread.

| # | Role | What arrives | What is routed | What continues | New machine? |
|---|---|---|---|---|---|
| D1 | | | | | |
| D2 | | | | | |
| D3 | | | | | |
| D4 | | | | | |
| D5 | | | | | |
| D6 | | | | | |
| D7 | | | | | |
| D8 | | | | | |

Expected: all `New machine?` = no. Kit is not Core.

---

## 4. Door versus live law (T3)

```
 door names                    live law stores
 ----------                    ---------------
 AGENTS.md at tip          \ 
 law-why-these-documents    }→  the enforced text
 harness paste-block       /

                           kit does not store law
                           D still stores Hands

 wrapper-as-law                 [X]
 existence-as-acceptance        [X]
 NOW-as-tip                     [X]
 G2 when / arity / adjacency    [GAP]
```

---

## 5. What execute may write

- Append “Pass 4 — mechanism map” to [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md).
- One README status tick.
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Redraw spine, fields, or grain.
- Edit `READ_ME_FIRST.md`, live law, or AGENTS.
- Fill G2.
- Claim law.
- Copy A13's three verbs onto this door.
- Start pass 5 companion seam.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read the door opening + harness block. Quote those, not A13.
2. Fill eight rows. Every `New machine?` = no.
3. Draw the door↔law seam. Stop.

Success: one route; live law still wins; G2 still `[GAP]`.  
Failure: a second law; Hands operator pasted here; emission filled.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 5 | companion seam |
| 6 | receipt |

---

## 8. Governing check

```text
One pass, one act.
This door routes. Live law wins.
Hands keeps its three verbs.
Φ stays [GAP].
```
