# Quiet door #4 (live law) — Pass 1 PLAN

**Status:** EXECUTED — spine on `docs/kit/quiet-door-4-ascii.md`. Live-law bytes unchanged. Pair untouched. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #4 — [`docs/law-why-these-documents.md`](../law-why-these-documents.md).  
**Why kit, not law:** law OWNS the door. Claiming law also locks AGENTS / emission / court. A new `docs/*.md` hits the maps catch-all. This campaign is a **reading**. Kit holds the reading. Live law still wins on the door.  
**Not:** live-law rewrite. Not AGENTS. Not `pointer-emission.md` answers. Not the header pair. Not door #3 pass 7.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite live law. Do not claim law.

```
pull/ff → claim/refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/kit/quiet-door-4-ascii.md spine
        → one pointer in docs/kit/README.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can live law be one ASCII spine of what it stores (LIVE LAW header, unresolved pair, staking ASCII, implementation gate, binding files) and what it refuses (agent edit of the pair, accepted emission, `src/`) — without the reading becoming the store?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Door #3 CLOSED 6/6 | court indexes; law stores |
| Door #1 CLOSED 6/6 | package routes here |
| Register row #4 | LIVE; `which` = law |

This campaign makes #4 activity WORKED, then CLOSED at pass 6. That does not flip LIVE.

---

## 2. Spine execute will draw (shape only)

Off the live-law file only. Do not paste the staking ASCII. Name its blocks.

```
  LIVE LAW
  docs/law-why-these-documents.md
  this file wins where copies lag
  kit reads; law wins; door bytes stay

  Header
    LIVE LAW banner
    staking-the-workspace.md = original
    kit transports may lag
    unresolved pair (existence vs acceptance)
      [X] agent edits the pair

  Why
    graphics remain source
    five texts adjacent, not Core
    three gears, not Core rows
    history does not amend this law
    no walkers

  ASCII (the prompt)
    Hands / Shadow
    five KEEP/FORBID stakes
    gears H C W
    IMPLEMENTATION LAW block
      exists ≠ accepted
      no_std when ever allowed

  Binding files
    this file / AGENTS / grok-bot-profile
    CONTRIBUTING / README

  [X]  paraphrase the ASCII into a friendlier Bot
  [X]  accepted emission
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

If a block cannot be read off the door, `[GAP]`. Do not invent a sixth stake.

---

## 3. What execute may write

Kit only.

- Create [`docs/kit/quiet-door-4-ascii.md`](quiet-door-4-ascii.md).
- One pointer in [`docs/kit/README.md`](README.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit `docs/law-why-these-documents.md`, AGENTS, staking, or `pointer-emission.md`.
- Resolve the header pair.
- Claim law or maps.
- Fill G2.
- Start pass 2 fields.
- Reopen door #3.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read live law headings + header. Quote names, do not paste the ASCII.
2. Draw the spine on the kit companion. Stop.

Success: one spine; live-law bytes unchanged; pair untouched; G2 `[GAP]`.  
Failure: live law rewritten; pair “fixed”; maps catch-all file at `docs/`.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain: header pair vs IMPLEMENTATION LAW vs transports |
| 4 | mechanism: this file stores; copies lag |
| 5 | seam vs court, package door, AGENTS |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
This file stores. Kit reads.
The pair is human. Do not edit it.
Φ stays [GAP].
```
