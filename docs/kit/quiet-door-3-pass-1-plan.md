# Quiet door #3 (Court) — Pass 1 PLAN

**Status:** EXECUTED — spine on `docs/kit/quiet-door-3-ascii.md`. Court door unchanged. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #3 — [`docs/README.md`](../README.md) (the court).  
**Why kit, not law:** law OWNS the door. A new `docs/*.md` hits the maps catch-all. This campaign is a **reading**. Kit holds the reading. Law still wins on the door.  
**Not:** live-law rewrite. Not AGENTS. Not `pointer-emission.md` answers. Not cold-doors pass 7. Not door #1 pass 7. Not scanner pass 7.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the court door. Do not claim law.

```
pull/ff → claim/refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/kit/quiet-door-3-ascii.md spine
        → one pointer in docs/kit/README.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the court door be one ASCII spine of what it names (Hands / gate / law / maps / operations / shelves) and what it refuses (second store, accepted emission, `src/`) — without becoming live law?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Scanner CLOSED 6/6 | stamp ≠ activity; #3 LIVE + QUIET *as door last campaign* |
| Door #1 CLOSED 6/6 | package route; not this door |
| Court register row | LIVE; `which` = law |

This campaign makes #3 activity WORKED, then CLOSED at pass 6. That does not flip LIVE.

---

## 2. Spine execute will draw (shape only)

Off the court page only. Sections as the door titles them:

```
  THE COURT
  docs/README.md
  shoe in hands is the court
  kit reads; law wins; door bytes stay

  Hands          four PNGs + manifest + close-reading
  Open gate      pointer-emission DRAFT; existence ≠ acceptance
  Law            live law + staking + references
  Maps           readings; never a second source
  Operations     audit append-only; defrag complete
  Not prose      shadow-clock HTML; Layer III; ≠ Graphic D
  Shelves        clock clipboards coord gearing history
                 hologram kit plans prompts

  [X]  second Hands store
  [X]  accepted emission
  [X]  src/
  [GAP] pointer-emission answers (G2)
```

If a heading cannot be read off the door, `[GAP]`. Do not invent a shelf.

---

## 3. What execute may write

Kit only.

- Create [`docs/kit/quiet-door-3-ascii.md`](quiet-door-3-ascii.md).
- One pointer in [`docs/kit/README.md`](README.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit `docs/README.md`, live law, AGENTS, or `pointer-emission.md`.
- Claim law or maps.
- Fill G2 or treat DRAFT as accepted.
- Open Rust.
- Start pass 2 fields.
- Reopen scanner or door #1.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read `docs/README.md`. Quote headings, do not add shelves.
2. Draw the spine on the kit companion. Stop.

Success: one spine; court bytes unchanged; G2 `[GAP]`; law unclaimed.  
Failure: court rewritten; emission filled; maps catch-all file created at `docs/`.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields (In / Does / Break / Gap) |
| 3 | grain: Hands vs law vs maps on this page |
| 4 | mechanism: court indexes, law stores |
| 5 | seam vs live law and vs kit package door |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
The court names. It does not store law.
Kit reads. Law wins.
Φ stays [GAP].
```
