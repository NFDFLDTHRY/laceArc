# Cold doors — Pass 2 PLAN

**Status:** PLAN only. Not run.
**Station:** kit.
**Agent:** Grok-kit.
**Plan BASE:** `44c2237eaaae4c6bc316a7322f15da90df7f01ae`.
**Emission:** `[GAP]`. No `src/`. Renderer stays paused. Seven Rust doors stay shut.

Pass 1 said stop. The human named pass 2.

```
pull/ff → claim kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append one section to docs/kit/cold-doors.md
        → check · commit · release · STOP
```

---

## 0. Question

**Behind the package door, are the kit-owned companions LIVE, DATED, or STALE against live law — without regenerating a copy and without editing live law?**

Not: rewrite `docs/law-why-these-documents.md` or `docs/staking-the-workspace.md`.
Not: regenerate `agent-control.md` / `grok-bot-profile.md`.
Not: unpause the renderer. Not: open the seven Rust doors.
Not: the R1 size repair. That stays a law-station pass if the human names it.

---

## 1. Why pass 2 exists

Pass 1 stamped ten *shelf doors*. All LIVE. None STALE.

What it did not stamp are the files the package door *points at* on this shelf:

| # | Companion | Path | Why it is this pass |
|---|---|---|---|
| 1 | Standing prompt | `docs/kit/agent-control.md` | Transport copy of live-law blocks. Kit-owned. Size ≠ live law. |
| 2 | Grok paste | `docs/kit/grok-bot-profile.md` | Transport copy. Kit-owned. Size ≠ live law. |
| 3 | Downstream audit | `docs/kit/downstream-audit.md` | THEN snapshot at `4fe984b`. Package door already says the body is not rewritten. |
| 4 | Task forms | `docs/kit/agent-task-template.md` | Kit index calls it live. Confirm or narrow. |
| 5 | GitHack deploy | `docs/kit/githack-pwa-deploy.md` | Kit index marks **LAW**. Confirm it still matches AGENTS rule 9. |
| 6 | Hook door | `.claude/README.md` | Kit-owned. Not in the ten-door table. |

Pass 1 already recorded the four-file size split as R1. Pass 2 *uses* that notice. It does not close it by rewriting law.

---

## 2. What execute may write

Kit only.

- Append one section to [`docs/kit/cold-doors.md`](cold-doors.md): one row per companion in §1, same stamps as pass 1.
- One README status line if a companion's stamp changes how the index should speak (DATED vs LIVE). Do not retarget pins.

No second register file.

---

## 3. What execute must not do

- Edit live law, staking ASCII, AGENTS.md, or the court door.
- Paste live-law blocks over the transport copies (restructure phase 3, not this tick).
- Rewrite the downstream-audit body to match NOW.
- Touch renderer HTML, githack URLs in clock, pointer-emission, history, hologram, maps, or clipboards.
- Claim law. A later human line “do R1 on law” is a different plan.

---

## 4. Single execute act

When the human says proceed:

1. Read live law header + IMPLEMENTATION LAW block (read-only).
2. Read each path in §1 at the execute BASE.
3. For each: stamp LIVE / DATED / STALE with pin or “none” and one-line evidence (what lags, what already says it lags).
4. Append that table under a “Pass 2 — kit companions” heading in `cold-doors.md`.
5. Stop. No pass 3.

Success: six rows stamped; live law bytes unchanged. Failure: a copy was regenerated, a pin was silently moved, or law was edited.

---

## 5. Governing check

```text
A lagging copy that says it may lag is DATED, not STALE.
A lagging copy that pretends to be the tip is STALE.
Repair of four law-bearing files is not this station.
Φ stays [GAP].
```

Kit owns the register. Law owns the law.
