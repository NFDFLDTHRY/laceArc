# Quiet door #1 (package) — Pass 2 PLAN

**Status:** PLAN only. Not run.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #1 — `docs/kit/READ_ME_FIRST.md`. ASCII spine already on [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md).  
**This pass:** In / Does / Break / Gap for the door's blocks.  
**Authority:** the package door. Then pass-1 spine. Cold-doors pass 5 pin table (cite, do not redo).  
**Not authority:** live-law fence dump. R1 repair. Graphics ASCII tables copied in.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the package door. Do not retarget THEN/NOW. Do not reopen cold-doors 7.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append fields table to quiet-door-1-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**For each block on the package door, what arrives, what does the block do, what would break the dual-bind (door routes / live law wins), and what stays silent?**

Pass 2 is fields. It does not redraw the spine. It does not start THEN/NOW/tip grain (pass 3).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | Spine: kit not Hands; routes to live AGENTS + live law; THEN/NOW labeled; companions named; `[X]` second law / src / enforcement / G2 |

---

## 2. Rows execute will fill (empty until proceed)

Off the door only. Not live-law piece fields. Not Graphic C's board.

| # | Block | In | Does | Break | Gap | Role |
|---|---|---|---|---|---|---|
| D1 | Opening (kit, not Hands) | | | | | |
| D2 | THEN pin | | | | | |
| D3 | NOW pin | | | | | |
| D4 | Integration / no enforcement | | | | | |
| D5 | Companion table | | | | | |
| D6 | Harness paste-block | | | | | |
| D7 | Audit paragraph | | | | | |
| D8 | Ready / not-ready | | | | | |

Expected shape (not frozen): D1 precondition; D2/D3 dated pins; D4 constraint; D5 index; D6 route for harnesses; D7 constraint (THEN snapshot); D8 statement. `New machine?` is not this table — that is pass 4.

If execute cannot defend a cell from the door, `[GAP]`.

---

## 3. What execute may write

- Append “Pass 2 — panel fields” to [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md).
- Add pass 2 to that file's Executed line.
- One README status tick.
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Redraw the pass-1 spine.
- Edit `READ_ME_FIRST.md`, live law, AGENTS, transports, or the audit body.
- Retarget THEN / NOW / `e21cf39`.
- Fill G2.
- Start pass 3 grain.
- Claim law / hologram / maps / graphics.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read the door. Markdown wins.
2. Fill eight In / Does / Break / Gap / Role rows.
3. Stop.

Success: every block has a job; none is live law.  
Failure: the door was rewritten; kit became a second law; G2 filled.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 3 | grain (THEN tree vs NOW tree vs tip) |
| 4 | routing mechanism vs live law |
| 5 | companion seam |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Fields hang under the spine.
The door still routes. Live law still wins.
Φ stays [GAP].
```
