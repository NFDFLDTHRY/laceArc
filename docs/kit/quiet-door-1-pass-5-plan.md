# Quiet door #1 (package) — Pass 5 PLAN

**Status:** EXECUTED — companion seam on `docs/kit/quiet-door-1-ascii.md`. Package door unchanged. Companion bodies untouched. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** quiet door #1 — companion seam.  
**Authority:** companion table on [`READ_ME_FIRST.md`](READ_ME_FIRST.md). Cold-doors pass 2 companion stamps (cite; do not redo). Pass 1–4 on [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md).  
**Not authority:** live-law fence dump. R1 repair. Regen of transports.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the package door. Do not retarget `e21cf39`. Do not edit companion bodies.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append companion seam to quiet-door-1-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Which kit files does the package door name, which does it leave off the table, and what happens if a named companion is treated as live law?**

Pass 5 is the seam. It does not start the receipt (pass 6). It does not regenerate copies.

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| Cold-doors 2 | six companions stamped LIVE or DATED; none STALE |
| Door #1 p2 D5 | indexes three files; Gap → this pass (githack, grok paste not on the door table) |
| Door #1 p4 | one route; wrapper-as-law `[X]` |

---

## 2. Rows execute will fill (empty until proceed)

Door table first, then kit files the door does not name. Stamps from cold-doors pass 2 unless the tree moved.

| File | On the door table? | Stamp | Job | Collapse would look like |
|---|---|---|---|---|
| `agent-control.md` | | | | |
| `agent-task-template.md` | | | | |
| `downstream-audit.md` | | | | |
| `grok-bot-profile.md` | | | | |
| `githack-pwa-deploy.md` | | | | |
| `.claude/README.md` | | | | |
| `quiet-door-1-ascii.md` | | | | |
| `cold-doors.md` | | | | |

Expected: three yes on the door; others no. None is live law. ASCII companion is a reading, not a fourth named control.

If execute cannot defend a cell from the door + pass 2 stamps, `[GAP]`.

---

## 3. What execute may write

- Append “Pass 5 — companion seam” to [`quiet-door-1-ascii.md`](quiet-door-1-ascii.md).
- One README status tick.
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Redraw spine / fields / grain / mechanism.
- Edit `READ_ME_FIRST.md` or any companion body.
- Retarget THEN / NOW / `e21cf39`.
- Fill G2 or promote a companion to live law.
- Claim law.
- Start pass 6 receipt.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Re-read the door's three-row table. Markdown wins.
2. Fill the seam. Door-named vs kit-present. Stamps cited, not changed.
3. Stop.

Success: three named routes; extras exist and stay off the door table; none is live law.  
Failure: a companion rewritten; `e21cf39` moved; G2 filled.

---

## Queued — later pass, not this one

| Pass | Act |
|---|---|
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
The door names three. Kit holds more.
None of them is live law.
Φ stays [GAP].
```
