# Quiet door #3 (Court) — Pass 4 PLAN

**Status:** EXECUTED — walk on `docs/kit/quiet-door-3-ascii.md`. Court door unchanged. Emission `[GAP]`.  
**Station:** kit (HELD Grok-kit).  
**Object:** Court **walk** — one mechanism on [`quiet-door-3-ascii.md`](quiet-door-3-ascii.md).  
**Authority:** Court passes 1–3. Court page as a *named door*, not bytes to edit.  
**Adjacent, not this file:** live law stores. Hands keeps A13. Core ASCII stays maps.  
**Not:** `docs/README.md` rewrite. Not AGENTS. Not G2 fill.  
**Emission:** `[GAP]`. No `src/`.

```
pull/ff → refresh kit
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append walk mechanism to quiet-door-3-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What single walk does the court run — it indexes, it does not store law — how does every Ct-block apply it, and what does it refuse versus Hands verbs and versus live law?**

The court's mechanism is not arrive / through-star / continue. Those three verbs belong to Hands. This walk **names a file, then sends the reader**.

Pass 4 is the mechanism map. It does not start the seam vs package door (pass 5).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine from court headings |
| 2 | Ct1–Ct8 fields |
| 3 | page label ≠ `which` |

---

## 2. Operator as the court states it (T1)

Working sentence (court wins; execute quotes, does not invent a Core verb):

> The court names. Law stores. Kit reads.

Walk:

```
 reader arrives at docs/README.md
   read the heading          → page label
   follow the named path     → file
   do not copy that file here
   do not become live law
   never: DRAFT ⇒ accepted
   never: map ⇒ Hands
   never: HTML ⇒ Graphic D
```

Execute draws **one walk box** on the kit companion. Not on the court door. Not “emit POINTER.”

---

## 3. Application register (T2)

Every Ct-block. `New machine?` must be `no`.

Rows execute will fill:

| # | Role | What arrives | What is walked | What continues | New machine? |
|---|---|---|---|---|---|
| Ct1 court page | | | | | |
| Ct2 Hands | | | | | |
| Ct3 open gate | | | | | |
| Ct4 Law | | | | | |
| Ct5 Maps | | | | | |
| Ct6 Operations | | | | | |
| Ct7 Not prose | | | | | |
| Ct8 Shelves | | | | | |

Expected: all `New machine?` = no. Court is not Core. Live law stays the store.

---

## 4. Court versus live law versus Hands (T3)

```
 quiet-door-3-ascii.md       docs/README.md           live law
 --------------------       ---------------           --------
 kit reading                the index                 the store
 names + sends              names + sends             wins

 wrapper-as-Core            [X]
 Hands verbs pasted here    [X]
 court page as live law     [X]
 G2                         [GAP]
```

---

## 5. What execute may write

- Append “Pass 4 — walk” to [`quiet-door-3-ascii.md`](quiet-door-3-ascii.md).
- One README status tick.
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Edit `docs/README.md`, live law, AGENTS, maps ASCII, or the HTML.
- Copy A13 onto this walk.
- Fill G2.
- Start pass 5 seam.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read court spine + grain. Quote those, not A13.
2. Draw one walk box. Fill the register. Every `New machine?` = no.
3. Draw court vs live law vs Hands seam. Stop.

Success: one walk; court still an index; live law still the store.  
Failure: Hands verbs here; court rewritten; DRAFT accepted.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 5 | seam vs live law and vs kit package door |
| 6 | receipt |

---

## 8. Governing check

```text
One pass, one act.
Court indexes. Law stores. Kit reads.
Hands keeps its verbs.
Φ stays [GAP].
```
