# Graphics ASCII — Iteration 2, Pass 6 PLAN

**Status:** PLAN only. Not run. Iteration 2 open (this pass closes it). Iteration 1 stays closed.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic C mechanism map + iteration-2 correctness receipt.  
**Authority:** C12 on `docs/graphics/n-dimensional-relationships.png`. Then [`graphic-c-ascii.md`](graphic-c-ascii.md) (i2p3–p5). A13 / B12 only as the already-stated operator quote. Graphic D types as the type seam.  
**Not authority:** A’s or B’s application-register cells (cite “same three verbs,” do not paste). Manifest Piece dumps. HCC/Cup.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A or B maps. Do not start Graphic D ASCII. Do not invent pass 7.

Human asked to rigorously analyze and ensure the ASCII systems diagram is correct. On this iteration that is: one C12 operator applied across C1–C12, then a receipt that checks A/B/C companions against Hands law. Correctness is “does the diagram still say what the graphics say.” It is not filling G2 to make the diagram feel finished.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append C mechanism map to graphic-c-ascii.md
        → write ascii-iteration-2-receipt.md
        → close i2 at 6/6 in ascii-iterations.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Does Graphic C state one operator, do all twelve panels apply / view / constrain / forbid that operator without adding machinery, and do the three ASCII companions still match Hands — one strand, D as store, feeds and families not types, G2 still silent?**

If a companion contradicts a PNG, execute marks `[GAP]` or `[X]` off the PNG. It does not “fix” by filling emission.

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| i2p1–p2 | B grain + B mechanism map |
| i2p3 | C1–C12 spine; C12 three verbs; C11 `[X]` |
| i2p4 | In / Does / Break / Gap / Role |
| i2p5 | C6 families ≠ D types; C11 write-back `[X]`; label-how `[GAP]` |

This pass hangs a C mechanism map and a receipt under those.

---

## 2. Operator as C states it (T1)

C12 (same sentence as A13 / B12):

> Input word arrives. Route new wire through that word’s star. Continue.

C10 punch-card: constraint on arrival, not a second machine.  
C11 is not a verb. C6’s four names are not verbs.

Execute draws **one operator box**. Three verbs only. No “emit POINTER.” No “allocate a dimension.” No “write family as type.”

---

## 3. Application register (T2)

Every C panel is precondition, application, specimen, view, constraint, statement, or anti-model.

Rows execute will fill. PNG wins. `New machine?` must be `no` or the panel is misread.

| # | Role | What arrives | What is routed | What continues | New machine? |
|---|---|---|---|---|---|
| C1 | | | | | |
| C2 | | | | | |
| C3 | | | | | |
| C4 | | | | | |
| C5 | | | | | |
| C6 | | | | | |
| C7 | | | | | |
| C8 | | | | | |
| C9 | | | | | |
| C10 | | | | | |
| C11 | | | | | |
| C12 | | | | | |

Expected shape (not frozen; i2p4 Role is the prior reading):

- C1 precondition.
- C2–C4 views of applications already on the wire (1D / 2D / 3D).
- C5 statement: more axes ≠ more meaning.
- C6 view of four families; not types.
- C7 view of nested participation.
- C8 view of N-D symptoms.
- C9 constraint + same operator on definition text.
- C10 constraint on arrival.
- C11 anti-model.
- C12 statement of the operator.

C7 “touches of touches” is this view of applications already done, not G2.

---

## 4. Correctness board (T3)

Execute marks each row Pass / Fail / `[GAP]` off the companions + PNGs. Fail means the ASCII added a machine Hands does not draw. `[GAP]` means Hands is silent — leave it.

| Check | Hands says | ASCII must still say |
|---|---|---|
| One operator | A13 = B12 = C12 three verbs | one box, no fourth verb |
| One store | Graphic D; WORD \| POINTER | no second array; no family types |
| Rule Zero | strand only grows | C1 / B1 / A header |
| Meaning | does not exist | no stored gloss; B10 / C9 / C11 |
| Dimensions | views of participation | C2–C8 stay views |
| C6 families | routes / views | type? = no (already i2p5) |
| Projection | not the Lace | C11 / B11 `[X]` |
| Emission | unstated | G2 `[GAP]` |
| Printed panel numbers | on artwork | `[GAP]` if still unread |
| Document-as-entry | unstated | `[GAP]` |
| Family-label-how | unstated (Piece 12) | `[GAP]` |

A Fail on a companion is a note on the receipt, not a rewrite of A or B this tick. Only C’s new section may be written.

---

## 5. Receipt (T4)

Execute writes [`ascii-iteration-2-receipt.md`](ascii-iteration-2-receipt.md) in the i1 receipt form:

- Status CLOSED. Six of six. Pin = this execute SHA.
- What landed per i2 pass.
- What this iteration did not do → iteration 3 queue (not pass 7).
- Coverage: 3 / 4 graphics have ASCII companions (A, B, C). D companion later if named. G2 still `[GAP]`.

Expected leftovers for i3 (placeholders; a later plan may rename):

| i3 | Likely act |
|---|---|
| 1 | Graphic D ASCII spine (store reading, not a second store) |
| 2 | D fields |
| 3 | printed A/B/C numbers if recovered |
| 4–6 | leftover `[GAP]`s that are still Hands-silent — point, do not fill G2 |

Do not start D from this plan tick.

---

## 6. What execute may write

- Append “Iteration 2 pass 6 — mechanism map” to [`graphic-c-ascii.md`](graphic-c-ascii.md).
- Create the i2 receipt.
- Close the i2 table in [`ascii-iterations.md`](ascii-iterations.md). Open an i3 queue stub if the leftovers list is ready.
- Mark this plan EXECUTED.

---

## 7. What execute must not do

- Redraw i2p3–p5 tables.
- Edit A or B ASCII.
- Fill G2, document-as-entry, or family-label-how.
- Add extra types or a fourth verb.
- Start Graphic D ASCII.
- Treat a Fail on the correctness board as license to invent Core.
- Add `src/` or retouch PNGs.

---

## 8. Single execute act

When the human says proceed (not this tick):

1. Re-read C12 on the PNG. Operator quote must match A13/B12.
2. Fill the twelve-row register. Every `New machine?` defended from C.
3. Mark the correctness board. Failures noted, not patched with emission.
4. Write the i2 receipt. Queue leftovers to i3. Stop.

Success: one operator on C; twelve roles; A/B/C still say Hands; iteration 2 closed 6/6; G2 still `[GAP]`.  
Failure: a second rule; C6 becomes schema; receipt fills emission; A/B rewritten; pass 7 invented.

---

## 9. Governing check

```text
One pass, one act.
C states one rule. Panels apply it, view it, constrain it, or forbid a fake.
The three companions are correct if they still say that.
Six closes the iteration. Leftovers are i3.
Φ stays [GAP].
```
