# Graphics ASCII — Iteration 2, Pass 5 PLAN

**Status:** PLAN only. Not run. Iteration 2 open. Iteration 1 stays closed.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic C.  
**This pass:** C6 four families vs Graphic D types; C11 projection anti-model grain.  
**Authority:** C6 and C11 on `docs/graphics/n-dimensional-relationships.png`. Then spine + fields on [`graphic-c-ascii.md`](graphic-c-ascii.md). Graphic D types only as the type seam (WORD | POINTER).  
**Not authority:** A11 three-feed list copied as schema. B4/B6 feed table copied as C6. Manifest Piece 12 field dump. HCC/Cup.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A or B. Do not start the C mechanism register (i2p6).

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append C6-vs-D + C11 grain to graphic-c-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What are C6’s four named families as C draws them, and what happens if they are treated as array types? What does C11 forbid a flattened picture from doing — without closing how a family would be encoded on D, and without filling G2?**

Pass 5 is grain of two panels. It does not redo In/Does/Break. It does not write the C application register (i2p6).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| i2p3 | C6 four names as views; vs D `[GAP]`. C11 `[X]`. Stacked D1–D4 on C ≠ Graphic D panels |
| i2p4 | C6 Role = view; Break = promote to types; Gap → this pass. C10 Gap = document-as-entry → this pass. C11 Does = prohibition. C7 Gap = G2 |

Do not redraw those lines. This pass hangs under them.

---

## 2. C6 grain — four families, not four types

C6 names (working; PNG wins):

| Family on C6 | Kind of thing on C | On Graphic D as a type? | Collapse would look like |
|---|---|---|---|
| dictionary route | | no | |
| document route | | no | |
| conversation route | | no | |
| provenance route | | no | |

Execute fills Kind from C’s caption only (`route`, `family`, `axis`, `view`, or `—`).  
`On Graphic D as a type?` stays **no**. D’s types remain WORD | POINTER.

If the PNG prints fewer or different names, inventory **as drawn**. Do not invent a fifth family. Do not drop a printed name.

Seam execute will draw (shape only):

```
 C6 names                         D stores
 --------                         --------
 dictionary route   \
 document route      }→           more WORD rows
 conversation route  /            (POINTER rows only when that rule exists)
 provenance route   /

                                  types on D: WORD | POINTER only

 family-as-entry-type             [X]
 document-boundary-as-entry       [GAP]
 how a family is labelled on D    [GAP]
 G2 when / arity / adjacency      [GAP]
```

Families are names of participation. They are not array types.  
Closing “how an implementer labels an axis” is Piece 12’s `[GAP]`. This pass points at it. It does not fill it.

---

## 3. C11 grain — projection is not Lace

C11’s own job: a flattened picture is not the Lace and cannot write back as store.

| Picture | Is Lace? | May write the array? | Collapse would look like |
|---|---|---|---|
| C2 1D sequence view | | | |
| C3 2D crossings view | | | |
| C4 3D stars view | | | |
| C stacked planes D1–D4 | | | |
| a 2D node-edge graph | | | |
| Graphic D 1D array | | | |

Expected: views are not Lace-as-store; Graphic D is the store; write-back from a view is `[X]`.  
If execute cannot defend a cell from C11 + D dual-bind already on the spine, `[GAP]` — do not invent a write rule.

C11 is the same anti-model family as B11. Cite the kin. Do not paste B11’s table.

---

## 4. What execute may write

- Append “Iteration 2 pass 5 — C6 / C11 grain” to [`graphic-c-ascii.md`](graphic-c-ascii.md).
- Add i2p5 to that file’s Executed line.
- Tick i2 pass 5 in [`ascii-iterations.md`](ascii-iterations.md).
- Mark this plan EXECUTED.

---

## 5. What execute must not do

- Redraw i2p3 spine or rewrite i2p4 cells.
- Edit A or B ASCII.
- Add DOCUMENT / CONVERSATION / PROVENANCE as types.
- Fill G2 or “how a family is labelled on D.”
- Start i2p6 mechanism register.
- Treat C11 as a constructive machine.
- Add `src/` or retouch PNGs.

---

## 6. Single execute act

When the human says proceed (not this tick):

1. Re-read C6 and C11 on the PNG. PNG wins on family names.
2. Fill the four-family table. Every family `type? = no`.
3. Draw the C6↔D seam. Family-as-type `[X]`. Document boundary `[GAP]`. Label-how `[GAP]`.
4. Fill the C11 picture table. Stop.

Success: four names stay views; D still has two types; a flatten cannot write the array; G2 still `[GAP]`.  
Failure: four extra types; C11 becomes the store; G2 filled; A/B edited.

---

## Queued — later i2 pass, not this one

| i2 | Act |
|---|---|
| 6 | C mechanism map or iteration-2 receipt |

---

## 7. Governing check

```text
One pass, one act.
C6 names views. D names two types.
C11 stays [X].
How a family is labelled on D stays [GAP].
Φ stays [GAP].
```
