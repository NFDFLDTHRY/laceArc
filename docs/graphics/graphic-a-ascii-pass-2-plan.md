# Graphic A → ASCII systems diagram — Pass 2 PLAN

**Status:** PLAN only. Not run. One act. Remainder queued.  
**Station:** graphics (HELD Grok-graphics).  
**Authority:** `docs/graphics/from-words-to-worlds.png`. Then the pass-1 spine [`graphic-a-ascii.md`](graphic-a-ascii.md).  
**Not authority:** `docs/systems-manifest.md` piece-field dumps (wrong grain). Maps-station ASCII. OCR.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Spine from pass 1 stays.

Pass 1 drew A1→A13. The human named pass 2. Execute is not this tick.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → hang per-panel fields on graphic-a-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**For each of A's thirteen panels, what does that panel consume, what does it do to the strand, and what would break the panel — using only language that is on Graphic A?**

Pass 2 hangs a field board on the existing spine. It does not redraw the Core-looking boxes. It does not start A8 grain or A11-vs-D (those are pass 3 / 4).

---

## 1. What pass 1 already flattened

On [`graphic-a-ascii.md`](graphic-a-ascii.md):

- Header / footer as A's language
- A1→A13 spine + PIE specimen box
- A8 kept split
- 3D = reading of D
- A13 checklist compact
- G2 and A3 grain left `[GAP]`

Pass 2 does not touch those drawings except to add a “Executed: pass 1 · pass 2” line.

---

## 2. Field grain (fixed)

One row per panel. Four columns only. Source = that panel's title, feed line, and caption on the PNG.

| Field | Means | Must not become |
|---|---|---|
| In | what the panel is handed (empty strand, a feed line, a star that already exists) | a new entry type |
| Does | what grows on the strand | a stored meaning / definition |
| Break | what would make this panel lie | a POINTER-when rule |
| Gap | silence on A for this panel, or `—` | an invented answer |

No Purpose / Preconditions / Boundary & Coupling / Implementation implications. Those are manifest-piece fields. Wrong station, wrong grain.

---

## 3. Rows execute will fill (empty until proceed)

Do not score these in the plan tick. Execute re-reads the PNG; if a caption differs, the PNG wins.

| # | Title | In | Does | Break | Gap |
|---|---|---|---|---|---|
| A1 | Start | | | | |
| A2 | First word | | | | |
| A3 | More words | | | | |
| A4 | A sentence in 3D | | | | |
| A5 | Repetition strengthens stars | | | | |
| A6 | The word “PIE” in context | | | | |
| A7 | Later words connect | | | | |
| A8 | Ambiguity forms in 3D | | | | |
| A9 | Responsibility chain | | | | |
| A10 | Higher-dimensional touch | | | | |
| A11 | A growing lexical Lace | | | | |
| A12 | Zoom in: The PIE star | | | | |
| A13 | What this model gives you | | | | |

Likely `[GAP]` already named, not filled: A3 word/phrase grain; A9/A11 document boundary vs D types (point at pass 4, do not settle); A10 “touch” vs POINTER emission (point at G2, do not settle).

A8 Break must include “collapse distinct passes into one edge.” Pass 2 records that as the panel's own failure. Pass 3 still owns the grain.

---

## 4. What execute may write

Graphics station only.

- Append a “Pass 2 — panel fields” section to [`graphic-a-ascii.md`](graphic-a-ascii.md).
- Add pass 2 to that file's Executed line.
- Mark this plan EXECUTED.

---

## 5. What execute must not do

- Redraw the pass-1 spine.
- Rewrite or re-encode PNGs.
- Copy Pieces 1–15 field blocks onto A panels.
- Answer G2 or edit `docs/pointer-emission.md`.
- Start pass 3 (A8 grain + A12 traffic) or pass 4 (A11 vs D types).
- Edit `docs/systems-manifest-ascii.md`.
- Add `src/`.

---

## 6. Single execute act

When the human says proceed (not this tick):

1. Re-read each panel on the PNG.
2. Fill the thirteen rows. Quote A's caption language in Does / Break where a distinction must be exact (`distinct passes, not collapsed edges`; `no meanings stored`; punch-card / governing rule at A13).
3. Leave G2 `[GAP]`. Point, do not fill.
4. Stop. Spine bytes above the new section stay.

Success: every panel has In / Does / Break; every invented type is absent; A8 is still split.  
Failure: the spine was redrawn, a POINTER rule appeared, or A8 was collapsed into one Does.

---

## Queued — next named pass, not this one

Pass 3: A8 grain + A12 zoom as star-internal traffic.  
Pass 4: A11 dictionary/document wiring vs D's two types.  
Pass 5+: B or C — other graphics.

---

## 7. Governing check

```text
One pass, one act.
Planning names the rows. Execute fills them.
Fields come off A, not off the manifest piece list.
A8 kept split is still success.
Φ stays [GAP].
```
