# Graphics ASCII — Iteration 2, Pass 4 PLAN

**Status:** PLAN only. Not run. Iteration 2 open. Iteration 1 stays closed.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic C.  
**This pass:** rigorous per-panel map of C onto the ASCII spine — In / Does / Break / Gap off C only, plus a Role map (view / constraint / anti-model / operator).  
**Authority:** `docs/graphics/n-dimensional-relationships.png`. Then the i2p3 spine [`graphic-c-ascii.md`](graphic-c-ascii.md).  
**Not authority:** Graphic A or B field boards (kin; do not copy cells). Manifest piece-field dumps. Maps-station ASCII. HCC/Cup C*.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A or B. Do not start C6-vs-D grain (i2p5) or the C mechanism register (i2p6).

Human asked for rigorous analysis and mapping. On this iteration that is C’s field board plus Role. It is not a new A/B pass and not a fourth store.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → hang fields + Role map on graphic-c-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**For each of C’s twelve panels, what does the panel take, what does it do to the strand (or forbid), what would break it, and how does it map onto the one operator already stated — using only language that is on Graphic C?**

Rigorous here means: quote C; classify each panel; refuse extra types; leave silent what C does not draw. It does not mean filling G2 or closing C6 vs D.

---

## 1. Already flat (do not redo)

On [`graphic-c-ascii.md`](graphic-c-ascii.md):

- Header as C’s title language
- C1→C12 spine
- C2–C4 presentation ladder as views
- C5 more dimensions ≠ more meaning
- C6 four families as views; vs D `[GAP]`
- C11 `[X]`
- C12 same three verbs as A13/B12
- stacked-plane D1–D4 on C ≠ Graphic D panels
- printed C-numbers `[GAP]`

Do not redraw those lines. This pass hangs under them.

---

## 2. Field grain (fixed)

One row per panel. Four columns + Role. Source = that panel’s title and caption on the PNG. Working titles from i2p3 until the PNG contradicts them.

| Field | Means | Must not become |
|---|---|---|
| In | what the panel is handed (strand, a presentation of the same wire, a family name) | a new entry type |
| Does | what the view shows, or what the anti-model forbids | stored meaning; a dimension column |
| Break | what would make this panel lie | a POINTER-when rule; C6-as-schema |
| Gap | silence on C, or `—` | an invented answer |
| Role | precondition / view / constraint / statement / anti-model / operator | a second machine |

No Purpose / Preconditions / Boundary & Coupling / Implementation implications. Those are manifest-piece fields.

C11 is anti-model. Its Does is the prohibition. C6 Role is view. Do not promote C6 to types.

---

## 3. Rows execute will fill (empty until proceed)

Do not score these in the plan tick. Execute re-reads the PNG; if a caption differs, the PNG wins.

| # | Title | In | Does | Break | Gap | Role |
|---|---|---|---|---|---|---|
| C1 | Rule Zero | | | | | |
| C2 | 1D presentation | | | | | |
| C3 | 2D crossings / adjacency | | | | | |
| C4 | 3D looping / stars | | | | | |
| C5 | More dimensions ≠ more meaning | | | | | |
| C6 | Four named families | | | | | |
| C7 | 5D+ touches of touches | | | | | |
| C8 | Symptoms of N-D | | | | | |
| C9 | Dictionary / no collapse | | | | | |
| C10 | Input treatment | | | | | |
| C11 | Projection ≠ lace | | | | | |
| C12 | Governing rule | | | | | |

Expected Role shape (not frozen):

- C1 precondition (one strand)
- C2–C4 views of the same wire at 1D / 2D / 3D
- C5 statement (≠ meaning)
- C6 view (four families); Gap = vs D schema
- C7 view / statement of nested participation
- C8 view (symptoms)
- C9 constraint + same operator on definition text
- C10 constraint on arrival
- C11 anti-model
- C12 operator statement

Likely `[GAP]` already named, not filled: printed C-numbers; C6 vs D schema (point at i2p5); when-POINTER (G2); stacked-plane D1–D4 vs Graphic D panel D1 (already dual-bound — do not reopen as types).

---

## 4. Mapping fence (what “map” may not do)

| May map | Must not map into |
|---|---|
| C12 → same three verbs as A13/B12 | a second operator |
| C2–C4 → presentation of D | a second array per dimension |
| C6 four names → views of participation | WORD / POINTER / DOCUMENT / CONVERSATION / PROVENANCE |
| C11 → `[X]` | projection as store |
| C stacked D1–D4 → axes on C | Graphic D’s D1 tape |

Do not paste A’s or B’s In/Does/Break cells. Kin language may be cited (“same operator”) without copying rows.

---

## 5. What execute may write

- Append “Iteration 2 pass 4 — panel fields + Role” to [`graphic-c-ascii.md`](graphic-c-ascii.md).
- Add i2p4 to that file’s Executed line.
- Tick i2 pass 4 in [`ascii-iterations.md`](ascii-iterations.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Redraw the i2p3 spine.
- Edit `graphic-a-ascii.md` or `graphic-b-ascii.md`.
- Fill G2 or close C6 vs D.
- Add extra array types.
- Start i2p5 grain or i2p6 mechanism register.
- Add `src/` or retouch PNGs.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read each C panel on the PNG. PNG wins on titles.
2. Fill twelve In / Does / Break / Gap / Role rows off C only.
3. C11 Does = prohibition. C6 Gap = vs D. G2 pointed, not filled.
4. Stop. Spine bytes above the new section stay.

Success: every panel mapped; dimensions stay views; C6 not schema; one operator.  
Failure: a third store; C6 types; A/B edited; emission filled.

---

## Queued — later i2 passes, not this one

| i2 | Act |
|---|---|
| 5 | C grain / anti-model (C6 families vs D; C11) |
| 6 | C mechanism map or iteration-2 receipt |

---

## 8. Governing check

```text
One pass, one act.
Fields and Role come off C, not off A/B and not off the manifest piece list.
C6 stays [GAP] vs D. C11 stays [X].
Φ stays [GAP].
```
