# Graphics ASCII — Iteration 2, Pass 3 PLAN

**Status:** PLAN only. Not run. Iteration 2 open. Iteration 1 stays closed.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic C.  
**This pass:** spine of C — what more dimensions is (and is not).  
**Authority:** `docs/graphics/n-dimensional-relationships.png` (original `13286.png`). Then `docs/graphics/README.md`. Bound panel names in `docs/systems-manifest.md` / maps ASCII C ladder are **working inventory**, not a substitute for the PNG.  
**Not authority:** Graphic A ASCII. Graphic B ASCII (cite “same operator / D is the store” only). Maps-station `docs/systems-manifest-ascii.md` as product. Layer III viewers. HCC/Cup C*.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A or B maps. Do not start C fields.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/graphics/graphic-c-ascii.md from the PNG
        → one pointer line in docs/graphics/README.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can Graphic C’s twelve panels be drawn as one ASCII systems diagram of how more dimensions present themselves — the same operator A and B already stated — without promoting N-D to a store, without copying A’s or B’s spine, and without answering G2 or filling C6 vs D schema?**

Pass 3 is the **spine of C**. Later i2 passes hang C fields, C grain / anti-model, and a C mechanism map or receipt. A C spine that pastes B7–B9 grain will lie by substituting the wrong graphic. A C spine that allocates four array types for C6’s families will lie by filling the `[GAP]` this pass must only point at.

---

## 1. What already exists (do not redo)

| Object | What it is | What it is not |
|---|---|---|
| The PNG | Source of record for C | Not a fifth store |
| `docs/graphics/graphic-a-ascii.md` | A’s closed four-pass map | Not C. Do not edit it. |
| `docs/graphics/graphic-b-ascii.md` | B’s four-pass map | Not C. Do not edit it. |
| Maps ASCII “Graphic C — what more dimensions is not” | Twelve working labels | Not this product |
| Piece cites (C1…C12) | Working titles | Execute re-reads the PNG |

Lossy if any of these happen: merge C into A or B; treat C6 families as D types; treat C11’s projection as the store; fill when-POINTER; import HCC/Cup; restyle the PNG.

---

## 2. Panel inventory (working; execute re-reads)

Titles as bound on the Hands / maps ladder. Execute must look at `n-dimensional-relationships.png`. If a title on the PNG differs, **the PNG wins**; mark the plan row `[GAP]` only if the panel is unreadable.

| # | Working title | Job (bound) |
|---|---|---|
| C1 | Rule Zero | one continuous strand; only grows |
| C2 | 1D presentation | sequence only; no crossings visible |
| C3 | 2D crossings / adjacency | crossings / adjacency appear (close-reading ladder; confirm on PNG) |
| C4 | 3D looping / stars | true structure begins to appear; stars as formations |
| C5 | More dimensions ≠ more meaning | more axes of participation, not new content |
| C6 | Four named families | dictionary / document / conversation / provenance as **views**, not D types. Schema vs D remains `[GAP]` |
| C7 | 5D+ touches of touches | relations among relations; still the same strand |
| C8 | Symptoms of N-D | denser structure / more recoverable routes — a **view** |
| C9 | Dictionary / no collapse | definition is a route; do not collapse to a gloss |
| C10 | Input treatment | punch-card; raw arrival; no interpretation |
| C11 | Projection ≠ lace | **anti-model** — a flattened picture is not the Lace |
| C12 | Governing rule | Input word arrives. Route new wire through that word’s star. Continue. |

**[CONFLICT-light]** C speaks stacked dimensions / N-D. Graphic D: N-D is a projection of the same 1D array. Pass 3 draws both. It does not pick a second store.

**[GAP]** Printed C-numbers and any column grouping on the PNG. Do not invent headers.

C6’s four names, if printed, may be quoted as C6’s specimen. They stay families of participation. They do not become WORD | POINTER | DOCUMENT | CONVERSATION.

---

## 3. What “lossless” means here

Every structural fact **on Graphic C** has a box, arrow, or labeled gap:

- C1 one continuous strand (Rule Zero)
- C2–C4 presentation ladder (1D → 2D → 3D) as **views** of the same wire
- C5 more dimensions ≠ more meaning
- C6 four families named, schema vs D `[GAP]`
- C7 touches of touches
- C8 N-D symptoms as view
- C9 dictionary route / no collapse
- C10 punch-card input
- C11 projection anti-model
- C12 the same mechanism sentence as A13 / B12
- N-D language on C = how participation looks, not a second array

Pass 3 does **not** need: per-panel In/Does/Break (i2p4); C6-vs-D grain (i2p5); C mechanism register (i2p6).

---

## 4. Spine execute will draw (shape only; not frozen)

```
 C1   Rule Zero — one strand, only grows
      |
      v
 C2   1D  sequence only
 C3   2D  crossings appear
 C4   3D  stars / looping
      |
      v
 C5   more dimensions ≠ more meaning
 C6   four named families (views, not types)   [GAP] vs D
 C7   5D+ touches of touches
 C8   N-D symptoms (view)
      |
      v
 C9   dictionary = route; no collapse
 C10  punch-card input
      |
      +-- C11  [X] projection as the thing
      |
      v
 C12  Input word arrives.
      Route new wire through that word's star.
      Continue.

 Graphic D is the store of this presentation.
 C does not append a second line.
 G2 / C6-vs-D schema = [GAP]
```

---

## 5. What execute may write

Graphics station only.

- Create [`docs/graphics/graphic-c-ascii.md`](graphic-c-ascii.md).
- One pointer sentence in [`docs/graphics/README.md`](README.md).
- Tick i2 pass 3 in [`ascii-iterations.md`](ascii-iterations.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Rewrite or re-encode the four PNGs.
- Edit `graphic-a-ascii.md` or `graphic-b-ascii.md` (except no edits at all).
- Answer G2 or close C6 vs D schema.
- Add DOCUMENT / CONVERSATION / PROVENANCE as array types.
- Copy A’s PIE box or B’s B7–B9 grain tables.
- Start i2p4 fields.
- Add `src/`.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read the PNG. Confirm C1–C12 titles against §2. PNG wins.
2. Draw the spine. Dual-bind N-D talk to D (projection), not to a new store.
3. Mark C11 anti-model. Leave G2 and C6-vs-D `[GAP]`.
4. Point from the graphics README. Stop.

Success: a reader can walk C1→C12 with the PNG open, sees one operator, sees dimensions as views, and never thinks C6 is four types.  
Failure: a second array appears, C6 becomes schema, C11 is treated as the store, A/B files are edited, or emission is filled.

---

## Queued — later i2 passes, not this one

| i2 | Act |
|---|---|
| 4 | C fields (In / Does / Break / Gap) |
| 5 | C grain / anti-model (C6 families vs D; C11) |
| 6 | C mechanism map or iteration-2 receipt |

---

## 8. Governing check

```text
One pass, one act.
This is C, not a fifth B pass.
Planning names the panels. Execute draws them.
N-D is how participation looks. D is the store.
C6 stays [GAP] vs D. C11 stays [X].
Φ stays [GAP].
```
