# Graphic B → ASCII systems diagram — Pass 1 PLAN

**Status:** PLAN only. Not run. One act. Remainder queued.  
**Station:** graphics (HELD Grok-graphics).  
**This ticket:** graphics ASCII **pass 5** = Graphic B **pass 1**. Graphic A’s four-pass map is closed. Do not reopen A.  
**Authority:** `docs/graphics/mechanisms-in-3d.png` (original `13285.png`). Then `docs/graphics/README.md`. Bound panel names in `docs/systems-manifest.md` are **working inventory**, not a substitute for the PNG.  
**Not authority:** Graphic A ASCII (`graphic-a-ascii.md`). Maps-station `docs/systems-manifest-ascii.md`. Layer III viewers.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put.

A pass 4 said next graphic is a new ticket. The human named pass 5. Execute is not this tick.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/graphics/graphic-b-ascii.md from the PNG
        → one pointer line in docs/graphics/README.md
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can Graphic B’s twelve panels be drawn as one ASCII systems diagram of how the wire behaves — the same operator A already stated — without promoting 3D to a store, without copying A’s spine, and without answering G2?**

Pass 1 is the **spine of B**. Later B passes hang fields, touch/traffic grain, and a mechanism map. A pass 1 that letters every anti-model X will lie by crowding; B pass 1 that copies A’s PIE specimen will lie by substituting the wrong graphic.

---

## 1. What already exists (do not redo)

| Object | What it is | What it is not |
|---|---|---|
| The PNG | Source of record for B | Not a fifth store |
| `docs/graphics/graphic-a-ascii.md` | A’s closed four-pass map | Not B. Do not edit it. |
| `docs/systems-manifest-ascii.md` “Graphic B — mechanism” | Eight labels, maps station | Not this product |
| Piece cites (B1…B12) | Working titles | Execute re-reads the PNG |

Lossy if any of these happen: merge B into A’s file; draw a Star table beside D; treat B10’s stored definition as Core; fill when-POINTER; import H/C/W; restyle the PNG.

---

## 2. Panel inventory (working; execute re-reads)

Titles as bound in the Hands map. Execute must look at `mechanisms-in-3d.png`. If a title on the PNG differs, **the PNG wins**; mark the plan row `[GAP]` only if the panel is unreadable.

| # | Working title | Growth / mechanism job (bound) |
|---|---|---|
| B1 | Continuous wire | one strand; only grows |
| B2 | Wire through star / arrival | word arrives; routes through a star |
| B3 | Star | persistent formation of passes, not a point |
| B4 | Dictionary | definition is a route through other stars, not a gloss |
| B5 | Sequence is physical | word order = physical route of the wire |
| B6 | Document feed | punch-card input; raw sequence, no interpretation |
| B7 | Touch (thread, not attach) | new occurrence threads existing 3D structure |
| B8 | Touch remains | a touch is permanent Lace; later wire can re-enter it |
| B9 | Star-inside-star traffic | interior of a star: many strands, many crossings |
| B10 | Collapse / stored definition | **anti-model** — PIE = “a dessert…” is forbidden |
| B11 | Graph is a shadow | **anti-model** — 2D node-edge is projection, not Lace |
| B12 | Governing rule | Input word arrives. Route new wire through that word’s star. Continue. |

**[CONFLICT-light]** B speaks “3D structure / real wire.” Graphic D + Piece 13: the 3D picture is a projection. Pass 1 draws both. It does not pick a third store.

**[GAP]** Whether B also carries a three-column grouping (wire / star / touch) is for execute to confirm on the PNG. Do not invent column headers that are not printed.

B4’s path PIE → dessert → slice → whole → food is **on B** (unlike A11). Execute may quote it as B4’s specimen. It is still a route, not a stored definition (B10).

---

## 3. What “lossless” means here

Every structural fact **on Graphic B** has a box, arrow, or labeled gap:

- B1 one continuous wire (Rule Zero, geometric)
- B2–B3 arrive and pass through a star
- B4 dictionary = route
- B5 sequence = the wire’s path
- B6 punch-card document feed
- B7–B8 thread / touch remains
- B9 interior traffic
- B10–B11 anti-model (collapse, flat graph)
- B12 the same mechanism sentence as A13
- 3D language on B = how the wire behaves, not a second array

Pass 1 does **not** need: per-panel In/Does/Break (B pass 2); B7–B9 grain (B pass 3); A’s PIE two-pass table; D1 tape; POINTER when.

---

## 4. Rows execute will fill (empty until proceed)

Do not score these in the plan tick.

**Spine (T1)** — one box-drawing. Suggested shape only; this plan does not freeze glyphs:

```
 B1   continuous wire (only grows)
      |
      |  B2–B3  word arrives → through star
      v
 B4   dictionary = route through other stars
 B5   sequence is that route
 B6   document feed (punch-card)
      |
      |  B7–B8  thread existing structure; touch remains
      v
 B9   interior traffic (same star, many strands)
      |
      +-- B10  [X] stored definition / collapse
      +-- B11  [X] flat graph as the thing
      |
      v
 B12  Input word arrives.
      Route new wire through that word's star.
      Continue.

 Graphic D is the store of this behaviour. B does not append a second line.
 G2 when / arity / adjacency = [GAP]
```

**Anti-model (T2)** — B10 and B11 stay marked fail. They are not extra operators.

**Fence (T3)** — no Star table; no HCC/Cup/Water; no POINTER answers; do not paste A’s A6–A9 PIE box into B.

**Pointer (T4)** — one line in `docs/graphics/README.md` to the executed B file. No PNG rewrite. No edit of `graphic-a-ascii.md` or `docs/systems-manifest-ascii.md`.

---

## 5. What execute may write

Graphics station only.

- Create [`docs/graphics/graphic-b-ascii.md`](graphic-b-ascii.md).
- One pointer sentence in [`docs/graphics/README.md`](README.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Rewrite or re-encode the four PNGs.
- Edit `docs/graphics/graphic-a-ascii.md` (A map is closed).
- Answer G2 or edit `docs/pointer-emission.md`.
- Add `src/`.
- Draw A or C panels except as “same operator as A13 / store is D.”
- Treat B10’s gloss as a Core field.
- Treat B11’s graph as the store.
- Claim maps, law, hologram, or renderer.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read the PNG. Confirm B1–B12 titles against §2. PNG wins.
2. Draw the spine. Dual-bind 3D talk to Piece 13 (projection), not to a new store.
3. Mark B10 and B11 as anti-model. Leave G2 `[GAP]`.
4. Point from the graphics README. Stop.

Success: a reader can walk B1→B12 on the ASCII with the PNG open, sees one operator, and never thinks the boxes are Graphic D.  
Failure: a second array appears, B10 is treated as a definition store, A’s file is edited, or emission is filled.

---

## Queued — next named pass, not this one

B pass 2: per-panel In / Does / Break / Gap off B only.  
B pass 3: B7–B9 thread / touch-remains / interior traffic.  
B pass 4: mechanism map (one B12 operator; feeds vs types; seam to D).  
Graphic C is a later ticket.

---

## 8. Governing check

```text
One pass, one act.
This is B, not a fifth A pass.
Planning names the panels. Execute draws them.
B is how the wire behaves. D is the store.
B10/B11 stay [X].
Φ stays [GAP].
```

If origin moved during work, pull --ff-only, refresh, then commit.
