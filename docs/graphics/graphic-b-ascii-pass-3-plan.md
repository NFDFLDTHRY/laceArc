# Graphic B → ASCII systems diagram — Pass 3 PLAN

**Status:** EXECUTED as alias of [`ascii-iteration-2-pass-1-plan.md`](ascii-iteration-2-pass-1-plan.md). Same ticket. Iteration 1 stays closed. Emission `[GAP]`.  
**Station:** graphics (HELD Grok-graphics). No separate `graphic-b` station — `docs/graphics/**` already maps here. A second station would split OWNS.  
**Claim:** Graphic B is the active object on this station. Pass 1 spine and pass 2 fields already on main (iteration 1 passes 5–6). This ticket is B7–B9 grain = B map pass 3 = **i2p1**.  
**Authority:** Graphic B panels B7, B8, B9 on `docs/graphics/mechanisms-in-3d.png`. Then pass-1 spine + pass-2 rows on [`graphic-b-ascii.md`](graphic-b-ascii.md).  
**Not authority:** Graphic A’s A8/A12 tables (kin, other graphic). Manifest Pieces 11 / 15 field dumps. D1 tape.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Spine and pass-2 board stay. A’s file stays closed.

Register: [`ascii-iterations.md`](ascii-iterations.md). Do not execute this plan as iteration-1 pass 7.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append B7–B9 grain to graphic-b-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**On B7, what is the difference between threading and attaching? On B8, what stays after the pass? On B9, what is inside the star as B draws it — without a Star table and without filling G2?**

Pass 3 is grain of three panels. It does not redo In/Does/Break. It does not settle B6/B4 vs D types (B pass 4).

---

## 1. What is already flat

| Pass | Holds |
|---|---|
| 1 | B7 thread not attach. B8 touch remains; later wire can re-enter. B9 interior: many strands, many crossings, none merged or lost |
| 2 | B7 Break = attach-only. B8 Break = drop the touch / write POINTER-when here. B9 Break = flatten to a hub. Grain pointed here |

Do not redraw those lines. Pass 3 hangs under them.

---

## 2. B7 grain — thread, not attach

B7’s own job: a new occurrence does not sit on the outside of a star. It threads the existing 3D structure.

Rows execute will fill (empty until proceed):

| Move | On B7 | Looks like | Must not become |
|---|---|---|---|
| Attach | | | |
| Thread | | | |

Execute quotes B7’s caption. Do not import “POINTER chains” as the difference. That is G2.

---

## 3. B8 grain — the touch remains

B8’s own job: the newly created touch remains in Lace. Later wire can re-enter it.

Rows execute will fill:

| After the pass | Stays? | Re-enter later? | Collapse would look like |
|---|---|---|---|
| the touch | | | |
| the sentence-as-unit | | | |
| a stored edge A—B | | | |

B8 names permanence and later participation. It does not name when a POINTER row is appended. Point G2. Do not fill.

---

## 4. B9 grain — interior traffic

B9 title (working): “Star inside star traffic.” Caption job: many distinct strands through the same formation; internal crossings; no meaning stored — only accumulated structure.

Rows execute will fill from the PNG. If B9 lists incoming sources, inventory them **as drawn**. If it does not, do not copy A12’s six labels.

| Source label on B9 | Kind of pass (B’s word, or `—`) | Merged with any other? |
|---|---|---|
| (one row per label on the PNG, or a single row `—` if none printed) | | no |

Interior rule, already on the B spine, to be drawn once:

```
 same star
 many strands
 many crossings
 all remain in Lace
 none merged or lost
```

That is the traffic. Not coordinates. Not a row type.

Do not paste A12’s definition / “This says pie.” list unless those strings are printed on B9.

---

## 5. What execute may write

Graphics station only.

- Append “Pass 3 — B7–B9 grain” to [`graphic-b-ascii.md`](graphic-b-ascii.md).
- Add pass 3 to that file’s Executed line.
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Redraw pass 1 or rewrite pass 2 cells.
- Edit `docs/graphics/graphic-a-ascii.md`.
- Create a `graphic-b` station or edit `coord.sh`.
- Answer G2. B8 permanence is not the POINTER-when rule.
- Start B pass 4 (feeds vs D types).
- Allocate a Star table.
- Add `src/` or retouch PNGs.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read B7, B8, B9 on the PNG. PNG wins on titles and source labels.
2. Fill attach-vs-thread. Quote “thread / not attach.”
3. Fill what remains. Point G2 from B8. Do not fill it.
4. Inventory B9 labels if printed; otherwise one `—` row. Draw the five-line interior rule. Stop.

Success: a reader can tell thread from attach, see a touch that stays, and see interior traffic without a second array.  
Failure: attach becomes the model; B8 becomes emission; B9 becomes A12 copied; a B station is invented.

---

## Queued — next named pass, not this one

B pass 4: mechanism map (one B12 operator; feeds vs types; seam to D).  
Graphic C is a later ticket.

---

## 8. Governing check

```text
One pass, one act.
B is already claimed under graphics.
This is grain, not a new station.
B8 is not G2.
Φ stays [GAP].
```
