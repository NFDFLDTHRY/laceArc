# Graphic B → ASCII systems diagram — Pass 2 PLAN

**Status:** EXECUTED — panel fields on `docs/graphics/graphic-b-ascii.md`. Emission `[GAP]`.  
**Station:** graphics (HELD Grok-graphics).  
**This ticket:** graphics ASCII **pass 6** = Graphic B **pass 2**.  
**Authority:** `docs/graphics/mechanisms-in-3d.png`. Then the pass-1 spine [`graphic-b-ascii.md`](graphic-b-ascii.md).  
**Not authority:** Graphic A field board. Manifest piece-field dumps. Maps-station ASCII.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Spine from pass 1 stays. A’s file stays closed.

Execute landed: twelve In / Does / Break / Gap rows. Remainder still queued.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → hang per-panel fields on graphic-b-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**For each of B’s twelve panels, what does that panel consume, what does it do to the strand, and what would break the panel — using only language that is on Graphic B?**

Pass 2 hangs a field board on the existing spine. It does not redraw B1–B12. It does not start B7–B9 grain or the B↔D mechanism map (those are B pass 3 / 4).

---

## 1. What pass 1 already flattened

On [`graphic-b-ascii.md`](graphic-b-ascii.md):

- Header as B’s title language
- B1→B12 spine
- B10 / B11 marked `[X]`
- 3D = how the wire behaves; D is the store
- B12 same three verbs as A13
- G2 left `[GAP]`
- printed B-numbers on the PNG not recovered; working titles from the Hands map
- three-column grouping `[GAP]`

Pass 2 does not touch those drawings except to add a “Executed: pass 1 · pass 2” line.

---

## 2. Field grain (fixed)

One row per panel. Four columns only. Source = that panel’s title and caption on the PNG. Working titles from pass 1 until the PNG contradicts them.

| Field | Means | Must not become |
|---|---|---|
| In | what the panel is handed (strand, arriving word, existing star, a feed) | a new entry type |
| Does | what grows on the strand, or what the anti-model forbids | a stored meaning / definition |
| Break | what would make this panel lie | a POINTER-when rule |
| Gap | silence on B for this panel, or `—` | an invented answer |

No Purpose / Preconditions / Boundary & Coupling / Implementation implications. Those are manifest-piece fields. Wrong station, wrong grain.

B10 and B11 are anti-model. Their Does is the prohibition. Their Break is treating the forbidden picture as Core. Do not give them a constructive operator.

---

## 3. Rows execute will fill (empty until proceed)

Do not score these in the plan tick. Execute re-reads the PNG; if a caption differs, the PNG wins.

| # | Title | In | Does | Break | Gap |
|---|---|---|---|---|---|
| B1 | Continuous wire | | | | |
| B2 | Wire through star / arrival | | | | |
| B3 | Star | | | | |
| B4 | Dictionary | | | | |
| B5 | Sequence is physical | | | | |
| B6 | Document feed | | | | |
| B7 | Touch (thread, not attach) | | | | |
| B8 | Touch remains | | | | |
| B9 | Star-inside-star traffic | | | | |
| B10 | Collapse / stored definition | | | | |
| B11 | Graph is a shadow | | | | |
| B12 | Governing rule | | | | |

Likely `[GAP]` already named, not filled: printed B-numbers; three-column headers; B7–B9 grain (point at B pass 3); when a POINTER is appended (point at G2 from B8/B12, do not settle); document-as-entry (point at D types, do not settle).

B4 Does must keep “definition is a route.” B4 Break must include “store PIE = dessert.” B10 is that failure drawn as a panel, not a second rule.

---

## 4. What execute may write

Graphics station only.

- Append a “Pass 2 — panel fields” section to [`graphic-b-ascii.md`](graphic-b-ascii.md).
- Add pass 2 to that file’s Executed line.
- Mark this plan EXECUTED.

---

## 5. What execute must not do

- Redraw the pass-1 spine.
- Edit `docs/graphics/graphic-a-ascii.md`.
- Rewrite or re-encode PNGs.
- Copy A’s In/Does/Break cells onto B.
- Answer G2 or edit `docs/pointer-emission.md`.
- Start B pass 3 (B7–B9 grain) or B pass 4 (mechanism map vs D).
- Treat B10 / B11 as constructive machines.
- Add `src/`.

---

## 6. Single execute act

When the human says proceed (not this tick):

1. Re-read each panel on the PNG. If a working title was wrong, correct the row title; do not silently keep the Hands-map name.
2. Fill the twelve rows. Quote B’s caption language in Does / Break where a distinction must be exact (`thread, not attach`; `definition is a route`; punch-card / no interpretation; governing rule at B12).
3. Leave G2 `[GAP]`. Point, do not fill.
4. Stop. Spine bytes above the new section stay.

Success: every panel has In / Does / Break; B10/B11 stay anti-model; no invented type.  
Failure: the spine was redrawn, a POINTER rule appeared, B10 became a definition store, or A’s file was edited.

---

## Queued — next named pass, not this one

B pass 3: B7–B9 thread / touch-remains / interior traffic.  
B pass 4: mechanism map (one B12 operator; feeds vs types; seam to D).  
Graphic C is a later ticket.

---

## 7. Governing check

```text
One pass, one act.
Planning names the rows. Execute fills them.
Fields come off B, not off A and not off the manifest piece list.
B10/B11 stay [X].
Φ stays [GAP].
```
