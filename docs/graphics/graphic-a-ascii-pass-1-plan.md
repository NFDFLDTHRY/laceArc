# Graphic A → ASCII systems diagram — Pass 1 PLAN

**Status:** EXECUTED — `docs/graphics/graphic-a-ascii.md`. Emission `[GAP]`.  
**Station:** graphics (HELD Grok-graphics).  
**Authority:** `docs/graphics/from-words-to-worlds.png` (original `13284.png`). Then `docs/graphics/README.md`. Then panel cites already bound in `docs/systems-manifest.md`.  
**Not authority:** OCR of this file. The existing eight-line stub under `docs/systems-manifest-ascii.md` (maps station). Layer III viewers.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put.

Execute landed: spine + README pointer. Remainder still queued.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/graphics/graphic-a-ascii.md from the PNG
        → one pointer line in docs/graphics/README.md
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can Graphic A's thirteen panels be drawn as one ASCII systems diagram that keeps A as a growth reading of Graphic D — without promoting the 3D picture to a store, and without answering G2?**

Pass 1 is the **spine of A**. Later passes hang per-panel fields, A8 ambiguity grain, and A11 dictionary/document wiring. A pass 1 that letters every A13 checkmark will lie by crowding.

---

## 1. What already exists (do not redo)

| Object | What it is | What it is not |
|---|---|---|
| The PNG | Source of record for A | Not a fifth store |
| `docs/graphics/README.md` | Title + lossless law | Not a panel map |
| `docs/graphics-close-reading.md` §5 | Header scale clause (1 cm / word) | Not A1–A13 |
| `docs/systems-manifest.md` Pieces 1–15 | Panel → piece cites (A1…A13) | Not an A diagram |
| `docs/systems-manifest-ascii.md` “Graphic A — growth story” | Eight labels, maps station | Not this product. Do not edit it from here. |

Lossy if any of these happen: merge A into the Hands-manifest spine; draw stars as a table beside D; treat “persistent 3D object / 3D star” as a row type; fill when-POINTER; import H/C/W oil; restyle the PNG.

---

## 2. Panel inventory (read off the PNG; execute re-reads)

Titles as they sit on Graphic A. Feed lines are the panel's own specimen, not a second corpus. Execute must look at the PNG. Do not copy OCR junk (`Continure`, merged words) into the product.

| # | Title on A | Feed / specimen on A | Growth job |
|---|---|---|---|
| A1 | Start | empty Lace | strand exists; no words yet |
| A2 | First word | Feed “The” | first occurrence begins a star |
| A3 | More words | Feed “Golden Skillet” | each word through its star (or a new one); wire continues |
| A4 | A sentence in 3D | Feed “The Golden Skillet had pancakes.” | sentence = physical route through stars in arrival order |
| A5 | Repetition strengthens stars | Later, “The” appears again | same star, new direction; star grows in 3D |
| A6 | The word “PIE” in context | Feed “The order said: PIE.” | PIE star created or reinforced |
| A7 | Later words connect | Feed “Is that a slice of pie?” | sequence links stars; PIE participates in a larger formation |
| A8 | Ambiguity forms in 3D | Feed “Or is it the whole pie?” | same PIE star, different path; distinct passes, not collapsed edges |
| A9 | Responsibility chain | Feed “Courier… customer… support…” | document = chain of participation; no meanings stored |
| A10 | Higher-dimensional touch | new input through a star that already contains history | new strand touches prior structure; that structure participates later |
| A11 | A growing lexical Lace (partial view) | dictionary + document wired together | one Lace; each star is a word |
| A12 | Zoom in: The PIE star | many distinct strands through the same star | same star; many passes; different directions/contexts; none merged or lost |
| A13 | What this model gives you | punch-card; governing rule | checklist + “Input word arrives. Route new wire through that word's star. Continue.” |

Header on A (close-reading §5 already bound):

> One continuous strand. Every word a centimetre of wire. Every word's star a persistent 3D formation. Meaning does not exist. Only structure, sequence, and participation.

Footer on A: Lace is not a database. It is a growing 3D history of language in motion. The result is a single, continuously growing 3D object.

**[CONFLICT-light]** A13 and the footer speak “3D star / 3D object / 3D history.” Graphic D + Piece 13: the 3D picture is a projection. Pass 1 draws both sentences. It does not pick a third store.

**[GAP]** Token / phrase grain at A3 (“Golden Skillet”) is already flagged in the manifest as word-level unless later source says otherwise. Pass 1 does not decide it.

---

## 3. What “lossless” means here

Every structural fact **on Graphic A** has a box, arrow, or labeled gap:

- A1 empty strand (Rule Zero precondition)
- A2–A5 arrival → first star → more stars → sentence-as-route → repeat pass
- A6–A9 PIE specimen: context → link → ambiguity kept → participation chain
- A10 touch of a star that already has history
- A11 dictionary and document are the same Lace
- A12 star-internal traffic (zoom)
- A13 punch-card + no stored meaning + governing rule (mechanism sentence only)
- Header: meaning does not exist
- 3D language on A = how the wire looks, not a second array

Pass 1 does **not** need: Piece field dumps, D1 tape, B/C panels, slot A vs B, arity.

---

## 4. Rows execute will fill (empty until proceed)

Do not score these in the plan tick.

**Spine (T1)** — one box-drawing. Suggested shape only; this plan does not freeze glyphs:

```
 A1 empty strand
      |
      |  A2–A4  word arrives → through star → continue
      v
 A5   same star, new direction
      |
      |  A6–A9  PIE specimen (context / link / A8 ambiguity / chain)
      v
 A10  touch of prior formation
 A11  dictionary + document = one Lace
 A12  zoom: many passes, none merged
 A13  punch-card · no meaning · governing rule
      |
      v
 Graphic D is the store of this growth. A does not append a second line.
 G2 when / arity / adjacency = [GAP]
```

**Header / footer (T2)** — four header clauses + footer “not a database,” marked as A's language.

**Fence (T3)** — inside the A diagram: no Star table, no HCC/Cup/Water, no POINTER emission answers. A8 stays “distinct passes,” not a stored sense-list.

**Pointer (T4)** — one line in `docs/graphics/README.md` to the executed file. No change to PNG files. No edit of `docs/systems-manifest-ascii.md` (wrong station).

---

## 5. What execute may write

Graphics station only.

- Create [`docs/graphics/graphic-a-ascii.md`](graphic-a-ascii.md).
- One pointer sentence in [`docs/graphics/README.md`](README.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Rewrite or re-encode the four PNGs.
- Answer G2 or edit `docs/pointer-emission.md`.
- Add `src/`.
- Draw B, C, or D1 tape except as “store is D.”
- Collapse A8 into one edge / one definition.
- Claim maps, law, hologram, or renderer.
- Treat “persistent 3D star” as a row type next to WORD | POINTER.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read the PNG. Confirm A1–A13 titles against §2. If a title differs, the PNG wins; mark the plan row `[GAP]` only if the panel is unreadable.
2. Draw the spine. Dual-bind 3D talk to Piece 13 (projection), not to a new store.
3. Leave G2 `[GAP]`.
4. Point from the graphics README. Stop.

Success: a reader can walk A1→A13 on the ASCII with the PNG open, and never thinks the boxes are Graphic D.  
Failure: a second array appears, A8 is collapsed, or emission is filled.

---

## Queued — next named pass, not this one

Pass 2: per-panel fields (inputs / effects / failure) off A only.  
Pass 3: A8 grain + A12 zoom as star-internal traffic.  
Pass 4: A11 dictionary/document wiring vs D's two types (document boundary still `[GAP]`).  
Pass 5+: B or C maps — other graphics, other tickets.

---

## 8. Governing check

```text
One pass, one act.
Planning names the panels. Execute draws them.
A is a reading of D. D is the store.
A8 kept split is success.
Φ stays [GAP].
```
