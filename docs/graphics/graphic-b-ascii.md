# Graphic B — ASCII spine

**Not Graphic B.** [`mechanisms-in-3d.png`](mechanisms-in-3d.png) is the source. This is an ASCII reading of how the wire behaves and settles nothing the PNG does not.

**Executed:** [pass 1](graphic-b-ascii-pass-1-plan.md).  
**Station:** graphics.  
**Store:** Graphic D. B is the mechanism reading of the same strand.  
**Not this file:** Graphic A ASCII (`graphic-a-ascii.md` is closed), C, D1 tape, Hands-manifest spine (`docs/systems-manifest-ascii.md`), H×C×W oil, `src/`, POINTER emission answers.

Emission remains `[GAP]`.

Printed panel numbers on the PNG were not recovered this tick. Working titles B1–B12 are the Hands-map inventory. If a later pass reads a different title on the artwork, the PNG wins.

Three-column grouping (wire / star / touch) as printed headers: `[GAP]`. Not invented here.

---

## Header (on B)

```
 Lace Mechanisms in 3D
 How the Wire Actually Behaves
```

3D on B is how the wire looks while it behaves. The store of that behaviour is Graphic D. Not a second line. Not a Star table.

---

## Spine

```
                         B1  CONTINUOUS WIRE
                         one strand
                         only grows
                         no cuts / no resets
                                   |
                                   |  word arrives
                                   v
                         B2  WIRE THROUGH STAR
                         arrival routes through a star
                         (or begins one)
                                   |
                                   v
                         B3  STAR
                         not a point
                         persistent formation of passes
                         same word, accumulated structure
                                   |
                                   v
                         B4  DICTIONARY
                         a definition is a route
                         through other word stars
                         specimen: PIE → dessert → slice → whole → food
                         nothing stored as a gloss
                                   |
                                   v
                         B5  SEQUENCE IS PHYSICAL
                         exact word order
                         = exact physical route of the wire
                         the sentence is this piece of wire
                                   |
                                   v
                         B6  DOCUMENT FEED
                         punch-card input
                         raw sequence
                         no interpretation
                                   |
                                   |  new occurrence meets prior formation
                                   v
                         B7  TOUCH (THREAD, NOT ATTACH)
                         does not sit on the outside
                         threads the existing 3D structure
                                   |
                                   v
                         B8  TOUCH REMAINS
                         a touch is permanent Lace
                         later wire can re-enter it
                                   |
                                   v
                         B9  STAR-INSIDE-STAR TRAFFIC
                         interior of one star
                         many strands
                         many crossings
                         none merged or lost
                                   |
              +--------------------+--------------------+
              |                                         |
              v                                         v
    B10  [X] COLLAPSE                          B11  [X] GRAPH AS THING
    stored definition                          2D node-edge
    PIE = "a dessert …"                        is a shadow
    forbidden                                  not Lace
              |                                         |
              +--------------------+--------------------+
                                   |
                                   v
                         B12 GOVERNING RULE
                         (same operator as A13)

                           Input word arrives.
                           Route new wire through that word's star.
                           Continue.


  B is this behaviour seen in 3D.
  The store of the behaviour is Graphic D — one 1D array.
  3D star / 3D structure / real wire = how the wire looks.
  Not a second line. Not a Star table. Not a row type.

  G2  POINTER when / arity / adjacency     = [GAP]
      (B does not answer it. This diagram does not fill it.)
```

---

## Operator (B12)

Same three verbs as A13. Punch-card at B6 is how arrival is allowed to look, not a second machine.

B10 and B11 are anti-model. They are not extra operators.

---

## Fence

| On B | Not on this spine |
|---|---|
| B10 / B11 marked `[X]` | stored gloss as Core; graph as store |
| dictionary = route (B4) | definition table beside the strand |
| 3D behaviour language | Star table beside D |
| same mechanism sentence as A13 | array-layer POINTER answers |
| one continuous wire | A's PIE specimen box copied in |

HCC π, Cup C*, Water OBS do not appear here. A's file is not this file.

---

## Pass 1 deltas

| Ticket | Action |
|---|---|
| T1 | Spine B1→B12 drawn |
| T2 | B10 / B11 anti-model |
| T3 | Fence: no Star table; 3D ≠ store; G2 left `[GAP]` |
| T4 | Pointer from `docs/graphics/README.md` |

Queued (not this file): per-panel fields; B7–B9 grain; mechanism map vs D types.
