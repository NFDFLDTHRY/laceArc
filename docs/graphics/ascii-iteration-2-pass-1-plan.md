# Graphics ASCII — Iteration 2, Pass 1 PLAN

**Status:** PLAN only. Not run. Iteration 2 is **open**. Iteration 1 stays closed.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic B.  
**This pass:** B7–B9 grain (thread / remains / interior). Same leftover named in [`ascii-iterations.md`](ascii-iterations.md). Companion grain sheet: [`graphic-b-ascii-pass-3-plan.md`](graphic-b-ascii-pass-3-plan.md) (alias; do not execute twice).  
**Authority:** B7, B8, B9 on `docs/graphics/mechanisms-in-3d.png`. Then [`graphic-b-ascii.md`](graphic-b-ascii.md) spine + fields from iteration 1.  
**Not authority:** Graphic A A8/A12 tables. Manifest Pieces 11 / 15. D1 tape. Iteration-1 receipt (closed).  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A. Do not start C.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append B7–B9 grain to graphic-b-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**On B7, what is threading versus attaching? On B8, what stays after the pass? On B9, what is inside the star as B draws it — without a Star table and without filling G2?**

This is iteration 2 pass 1, not iteration-1 pass 7. It does not redo B’s spine or fields. It does not settle feeds vs D types (i2 pass 2).

---

## 1. Already flat (iteration 1)

| i1 pass | Holds |
|---|---|
| 5 | B7 thread not attach. B8 touch remains; later wire can re-enter. B9 many strands, many crossings, none merged or lost |
| 6 | B7 Break = attach-only. B8 Break = drop the touch / write POINTER-when here. B9 Break = flatten to a hub |

Do not redraw those lines. This pass hangs under them.

---

## 2. B7 — thread, not attach

Rows execute will fill (empty until proceed). PNG wins.

| Move | On B7 | Looks like | Must not become |
|---|---|---|---|
| Attach | | | |
| Thread | | | |

Do not import POINTER chains as the difference. That is G2.

---

## 3. B8 — the touch remains

| After the pass | Stays? | Re-enter later? | Collapse would look like |
|---|---|---|---|
| the touch | | | |
| the sentence-as-unit | | | |
| a stored edge A—B | | | |

B8 names permanence and later participation. Point G2. Do not fill.

---

## 4. B9 — interior traffic

If B9 lists incoming sources, inventory them as drawn. If not, one `—` row. Do not copy A12’s six labels.

| Source label on B9 | Kind of pass (B’s word, or `—`) | Merged with any other? |
|---|---|---|
| (one row per label on the PNG, or `—`) | | no |

Draw once:

```
 same star
 many strands
 many crossings
 all remain in Lace
 none merged or lost
```

Not coordinates. Not a row type.

---

## 5. What execute may write

- Append “Iteration 2 pass 1 — B7–B9 grain” to [`graphic-b-ascii.md`](graphic-b-ascii.md).
- Add i2p1 to that file’s Executed line.
- Mark this plan EXECUTED. Mark the alias EXECUTED too if still open.

---

## 6. What execute must not do

- Redraw i1 spine or rewrite i1 field cells.
- Edit Graphic A ASCII.
- Fill G2.
- Start i2 pass 2 (B mechanism map) or Graphic C.
- Allocate a Star table.
- Add `src/` or retouch PNGs.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read B7–B9 on the PNG.
2. Fill attach vs thread. Quote B7.
3. Fill what remains. Point G2 from B8.
4. Inventory B9 labels if printed. Draw the interior rule. Stop.

Success: thread ≠ attach; a touch stays; interior traffic with no second array.  
Failure: attach is the model; B8 is emission; B9 is A12 copied; this is treated as pass 7.

---

## Queued — later i2 passes, not this one

| i2 | Act |
|---|---|
| 2 | B mechanism map (B12 operator; feeds vs D types) |
| 3–6 | Graphic C (placeholders) |

---

## 8. Governing check

```text
Six passes per iteration.
This is i2p1 on Graphic B.
Grain, not a new station.
B8 is not G2.
Φ stays [GAP].
```
