# Prefix device on Hands — Pass 1

**Status:** EXECUTED. Not accepted. Not emission.  
**Station:** graphics.  
**Plan:** [`prefix-device-pass-1-plan.md`](prefix-device-pass-1-plan.md)  
**Authority:** four PNGs. Picture wins.  
**Lens (not law):** next section is Φ of the current prefix. Prefix *is* the dictionary. Recursion is a POINTER naming an earlier cell.  
**Emission:** `[GAP]`. No `src/`. WORD rows on D1 were not deleted.

Pass 2 sheet (no winner): [`prefix-device-pass-2.md`](prefix-device-pass-2.md).

---

## Receipt

| Stamp | Panels |
|---|---|
| DEVICE | D1 D2 D3 D5 D6 D-footer; B1 B2 B3 B5 B7 B8 B12; A1 A3 A4 A5 A6 A7 A8 A9 A10 A13; C1 C7 C12 |
| VIEW | D4; B9; A12; C2 C3 C4 C5 C6 C8 |
| SEED | B4 B6; A2 A11; C9 C10 |
| FORBID | B10; B11-left; C11-left |
| split VIEW | B11-right; C11-right |
| `[GAP]` under this lens | no panel refused a stamp; G2 pointed from B8 and A10 and not filled; D1 mix is not Φ's schedule; C6 families are not D types |

Recursion on the substrate is **D3 + D6**. Everywhere else that looks N-D is a view of those chains, or a later cell participating in the prefix.

---

## Graphic D — store

`data-structure-1d-array.png`

| Panel | Drawn mechanism | Stamp | One-line reading | Extra if misread |
|---|---|---|---|---|
| D1 | 1D append-only array | DEVICE | The only store. Each row is a section of the same strand. | Reading the eleven rows as Φ's complete when-schedule. Mix is illustrative. `[GAP]` |
| D2 | WORD / POINTER shape | DEVICE | Two section kinds. WORD = variable. POINTER = refs into earlier cells. Minimal structure. | A third type; discarding WORD to "save text." |
| D3 | later refs earlier, incl. pointers | DEVICE | Φ as drawn: a new cell names what already exists. | Forward refs; an edge table beside the line. |
| D4 | same data in 3D | VIEW | Projection generated from the array and its refs. Caption: not storage. | Treating the star picture as the store. |
| D5 | star from repeated WORD | DEVICE | Same variable at new indices. Star is accumulated passage, not a row. | Star table; intern one row + count. |
| D6 | pointer-to-pointer chain | DEVICE | Recursion on the substrate. Word to word, relation to relation, later relation to earlier relation. Still 1D. | A stack, a span array, N-D storage. |
| D footer | Input arrives. Append a new section. Use pointers to participate in what already exists. | DEVICE | Storage sentence of the same rule. | A second rule after ingest. |

Key properties box on D restates DEVICE: one line; both kinds are entries; refs only earlier; pointers may name pointers; 3D is projection.

---

## Graphic B — wire

`mechanisms-in-3d.png`

| Panel | Drawn mechanism | Stamp | One-line reading | Extra if misread |
|---|---|---|---|---|
| B1 | continuous wire | DEVICE | One strand. Only grows. Past already on L. | Cut, reset, second strand. |
| B2 | occurrence routes through star | DEVICE | Arrive. Route through that word's star (or begin one). Wire continues. | Parse the word away; skip the star. |
| B3 | repetition builds the star | DEVICE | Later occurrence is another WORD on the line through the same variable. | Collapse passes to one node. |
| B4 | definition is a route | SEED | Definition text is more wire through ordinary word stars. PIE → dessert → slice → whole → food is prefix other writes may name. | Gloss table; lexicon engine beside D. |
| B5 | sequence is physical | DEVICE | Word order = route on the line. Sentence is this wire, not a unit object. | Sentence record; reorder. |
| B6 | punch-card document feed | SEED | Raw sequence into the same wire. No interpretation at the hopper. Sampler, not a second machine. | Per-document graph; interpret-then-route. |
| B7 | thread, not attach | DEVICE | New section goes through existing formation (the prefix). | Attach-only; treating thread as a when-rule. |
| B8 | touch remains | DEVICE | The pass stays on L. Later Φ may name it. G2 pointed, not filled. | Drop at sentence end; write emit-when here. |
| B9 | star-inside-star traffic | VIEW | Interior of one variable's passages. Many crossings. No new row type. | Star table; coordinates as store. |
| B10 | no collapse | FORBID | Not a lookup. Not one flat edge. Not PIE = "a dessert…". Not semantic collapse. | Any of those four as Core. |
| B11 | projection vs Lace | FORBID / VIEW | Left: 2D graph as the thing — FORBID. Right: 3D lace as actual structure — VIEW of D. | Graph DB as authority. |
| B12 | arrive / route / continue | DEVICE | Geometry sentence of the same rule. Three verbs. No fourth. | "Emit POINTER" as a fourth verb on B. |

B4 and B6 are one line: seed text and later text both append into L.

---

## Graphic A — growth

`from-words-to-worlds.png`

| Panel | Drawn mechanism | Stamp | One-line reading | Extra if misread |
|---|---|---|---|---|
| A1 | empty lace | DEVICE | L is empty. Strand exists. No words. No structure yet. | Starting from a schema instead of an empty line. |
| A2 | first word | SEED | First WORD begins a star. Prefix now has something later cells can name. | Intern table at birth. |
| A3 | more words | DEVICE | Each new word through its star. Wire continues in arrival order. | Phrase object ("Golden Skillet" as one row) — picture splits the words. |
| A4 | sentence in 3D | DEVICE | One route through several stars in arrival order. | Sentence-as-unit store. |
| A5 | repetition, new direction | DEVICE | Same variable, new WORD, new passage. Star grows. | Merge with first occurrence. |
| A6 | PIE in context | DEVICE | Each word through its star. PIE begins or threads the PIE star. | Stored sense of PIE. |
| A7 | later words connect | DEVICE | slice and pie are more line. PIE star now sits in a larger formation. | One collapsed PIE–slice edge. |
| A8 | ambiguity in 3D | DEVICE | Same star, different path. Distinct passes. Collapse would lie. | One edge for two questions. |
| A9 | responsibility chain | DEVICE | Document is participation in arrival order. No meanings stored. | A role graph beside the wire. |
| A10 | higher-D touch | DEVICE | New wire through a star that already holds history. That formation can be named later. G2 pointed, not filled. | Touch-grows-lace as emit-when. |
| A11 | growing lexical lace | SEED | Dictionary + document on one object. Each star is a word. Every feed adds wire. | Two laces; dictionary as side table. |
| A12 | zoom PIE star | VIEW | Many feeds through one variable. None merged or lost. Reading of D5 + many routes. | Six source labels as row types. |
| A13 | what the model gives | DEVICE | Checklist + the same three verbs. Punch-card named as how input is treated. | Turning the checklist into extra operators. |

---

## Graphic C — dimensions

`n-dimensional-relationships.png`

| Panel | Drawn mechanism | Stamp | One-line reading | Extra if misread |
|---|---|---|---|---|
| C1 | rule zero | DEVICE | One strand. Always growing. No cuts / resets / rewrites. | Second store; tidy history. |
| C2 | 1D presentation | VIEW | Line in time and order. Sequence only. Structure not visible. | 1D as a lesser lace. |
| C3 | 2D presentation | VIEW | Flat projection. Crossings appear. Over/under and depth collapse. | Paper graph as the thing. |
| C4 | 3D presentation | VIEW | Looping, over/under, threading. Same word from different directions. | 3D as a second store. |
| C5 | what a dimension adds | VIEW | Another independent way the same wire participates in existing L. Not new meaning. Not new content. | Dimension as extra array storage. Picture is stacked axes of reading. |
| C6 | 4D / relation families | VIEW | Dictionary / document / conversation / provenance: four axes through the same stars. Same wire. | Four stores; family names as D entry types. That schema `[GAP]`. |
| C7 | 5D+ touches in later touches | DEVICE | A touch remains and can be named by a later touch. That is D6 on the line. | Nested storage; a new array per depth. |
| C8 | how N-D presents | VIEW | Symptoms at one star: more passes, denser routes, revisit without rewrite. | Treating symptoms as operators. |
| C9 | dictionary wiring example | SEED | Same as B4: definition is a route through other stars. Later documents reuse those stars. | Gloss; definition field on PIE. |
| C10 | punch-card input | SEED | Same as B6: raw arrival order into the dictionary wiring already on L. | Parse/discard; sentence object. |
| C11 | projection vs actual | FORBID / VIEW | Left flat graph — FORBID. Right 3D+ formation — VIEW of the one line. | Node-edge model as Lace. |
| C12 | governing rule | DEVICE | Same three verbs. No stored meaning — routes, touches, structure. | Fourth verb. |

Header on C is the dimension law in one sentence: more dimensions do not add meaning; they add independent ways the same wire participates in existing structure.

---

## What this pass did not do

- Did not fill when / arity / adjacency on `docs/pointer-emission.md`.
- Did not treat session speech as in-repo acceptance.
- Did not add span stores.
- Did not delete WORD from D to match "don't save the text."

That collision is filed as pass 2: [`prefix-device-pass-2.md`](prefix-device-pass-2.md). No winner. Still not acceptance of emission.

---

## Governing check

```text
D is the store.
Φ as lens held: every DEVICE panel is arrive / append / participate-in-prefix.
Recursion is D3/D6.
Seed is B4/B6, A2/A11, C9/C10 — same line.
Views are A–C presentations and D4/B9/A12.
Forbids are collapse and graph-as-thing.
G2 stays [GAP].
No second array.
```
