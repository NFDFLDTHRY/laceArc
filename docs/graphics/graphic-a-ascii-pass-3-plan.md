# Graphic A → ASCII systems diagram — Pass 3 PLAN

**Status:** PLAN only. Not run. One act. Remainder queued.  
**Station:** graphics (HELD Grok-graphics).  
**Authority:** Graphic A panels A8 and A12 on `docs/graphics/from-words-to-worlds.png`. Then pass-1 spine + pass-2 rows on [`graphic-a-ascii.md`](graphic-a-ascii.md).  
**Not authority:** B9 (other graphic). Manifest Pieces 14–15 field dumps. D1 tape.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Spine and pass-2 board stay.

Pass 2 hung fields. The human named pass 3. Execute is not this tick.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append A8 grain + A12 traffic to graphic-a-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**On A8, what stays uncollapsed when two later feeds touch the same PIE star? On A12, what is inside that star — as Graphic A draws it — without turning the zoom into a Star table?**

Pass 3 is grain of two panels. It does not redo In/Does/Break. It does not settle A11 vs D types.

---

## 1. What is already flat

| Pass | Holds |
|---|---|
| 1 | A8 caption: distinct passes, not collapsed edges. A12: same star; many passes; different directions / contexts; none merged or lost |
| 2 | A8 Break = collapse distinct passes into one edge. A12 Break = merge or drop passes. Grain pointed here |

Do not redraw those lines. Pass 3 hangs under them.

---

## 2. A8 grain (what execute must make sayable)

Two feeds already on A, both through PIE:

| Panel | Feed on A | Path job |
|---|---|---|
| A7 | “Is that a slice of pie?” | PIE participates in a larger formation |
| A8 | “Or is it the whole pie?” | same PIE star, different path |

A8's own words: “these are distinct passes, not collapsed edges.”

Execute draws **one star, two passes**. Not two PIE stars. Not one PIE edge. Not a sense inventory (`slice` vs `whole` as stored meanings — A header: meaning does not exist).

Rows execute will fill (empty until proceed):

| Pass | Feed | Through | Must remain | Collapse would look like |
|---|---|---|---|---|
| A7 | | PIE star | | |
| A8 | | PIE star | | |

`Must remain` and `Collapse would look like` come off A8's caption, not off a definition table.

---

## 3. A12 traffic (what execute must inventory)

A12 title on A: “Zoom in: The PIE star.” Caption: many distinct strands pass through the same star.

The zoom lists incoming strands by source label. Execute re-reads the PNG and writes the labels **as drawn**. Do not invent extras. A prior OCR pass saw (verify on execute; PNG wins):

- from dictionary / definition of pie
- from “This says pie.”
- from “slice of pie”
- from “whole pie”
- from liability discussion
- from future conversations

Rows execute will fill:

| Source label on A12 | Kind of pass (A's word, or `—`) | Merged with any other? |
|---|---|---|
| (one row per label on the PNG) | | no |

Kind may only use A's language (dictionary, document, conversation, future). If A12 does not name a kind, write `—`. Do not import C6's four families.

Interior rule, already on A12, to be drawn once:

```
 same star
 many passes
 different directions
 different contexts
 all remain in Lace
 none merged or lost
```

That is the traffic. Not coordinates. Not a row type.

---

## 4. What execute may write

Graphics station only.

- Append “Pass 3 — A8 grain + A12 traffic” to [`graphic-a-ascii.md`](graphic-a-ascii.md).
- Add pass 3 to that file's Executed line.
- Mark this plan EXECUTED.

---

## 5. What execute must not do

- Redraw pass 1 or rewrite pass 2 cells.
- Copy B9 “star inside star” as if it were A.
- Answer G2. Touch at A10 stays pointed, not filled.
- Start pass 4 (A11 vs D types).
- Allocate a Star table or a sense list for PIE.
- Edit maps / law / `docs/systems-manifest-ascii.md`.
- Add `src/` or retouch PNGs.

---

## 6. Single execute act

When the human says proceed (not this tick):

1. Re-read A8 and A12 on the PNG.
2. Fill the A7/A8 two-pass table. Quote “distinct passes, not collapsed edges.”
3. Inventory A12 source labels from the zoom. If a label is unreadable, `[GAP]` that row; do not guess.
4. Draw the six-line interior rule. Stop.

Success: a reader sees two PIE passes and many named strands inside one star, and cannot find a stored meaning or a second array.  
Failure: slice/whole become definitions; A12 becomes a node with edges; emission is filled.

---

## Queued — next named pass, not this one

Pass 4: A11 dictionary/document wiring vs D's two types.  
Pass 5+: B or C — other graphics.

---

## 7. Governing check

```text
One pass, one act.
A8 kept split is the grain.
A12 is interior of the same star, not a table.
Φ stays [GAP].
```
