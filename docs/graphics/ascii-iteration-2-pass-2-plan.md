# Graphics ASCII — Iteration 2, Pass 2 PLAN

**Status:** EXECUTED — mechanism map on `docs/graphics/graphic-b-ascii.md`. Iteration 2 remains open (pass 2 of 6). Iteration 1 stays closed. Emission `[GAP]`.  
**Station:** graphics (HELD Grok-graphics).  
**Object:** Graphic B.  
**This pass:** mechanism map — one B12 operator; every B panel as application / anti-model / statement; B feeds vs D types.  
**Authority:** `docs/graphics/mechanisms-in-3d.png` — B4 dictionary route, B6 punch-card, B10/B11 anti-model, B12 governing rule. Then [`graphic-b-ascii.md`](graphic-b-ascii.md) (i1 spine/fields + i2p1 grain).  
**Not authority:** Graphic A’s application register (kin, other graphic — may cite “same three verbs,” not copy rows). Manifest Pieces 9–11 dumps. C6 families.  
**Emission:** `[GAP]`. No `src/`. PNG bytes stay put. Do not reopen A. Do not start C.

```
pull/ff → refresh graphics
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append mechanism map to graphic-b-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What is the mechanism Graphic B actually states, how does every panel apply that one mechanism (or forbid a fake one), and what does B refuse to add as extra machinery — especially dictionary / document versus Graphic D’s two entry types?**

Rigorous: quote B12; classify each panel; mark names on B that are not types on D; leave silent what B does not draw.

This pass does not invent a second operator. It does not fill G2. It does not start Graphic C. It does not copy A’s thirteen rows onto B.

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| i1p5 | B1→B12 spine; B12 three verbs; B10/B11 `[X]`; 3D = behaviour, D = store |
| i1p6 | In / Does / Break / Gap; B4 definition is a route; B6 punch-card |
| i2p1 | thread ≠ attach; touch remains; interior rule; B9 labels `[GAP]` |

This pass hangs a mechanism map under those.

---

## 2. Operator as B states it (T1)

B12 (same sentence as A13):

> Input word arrives. Route new wire through that word’s star. Continue.

B6 punch-card: raw sequence, no interpretation — constraint on arrival, not a second machine.

B10/B11 are not verbs. They are `[X]`.

Execute draws **one operator box**. Three verbs only. No “emit POINTER.” No “write definition.” No “build graph.”

---

## 3. Application register (T2)

Every panel is precondition, application, specimen, view, statement, or anti-model.

Rows execute will fill. PNG wins. `New machine?` must be `no` or the panel is misread. If execute cannot defend `no` from B’s caption, `[GAP]` — do not invent a machine.

| # | Role | What arrives | What is routed | What continues | New machine? |
|---|---|---|---|---|---|
| B1 | | | | | |
| B2 | | | | | |
| B3 | | | | | |
| B4 | | | | | |
| B5 | | | | | |
| B6 | | | | | |
| B7 | | | | | |
| B8 | | | | | |
| B9 | | | | | |
| B10 | | | | | |
| B11 | | | | | |
| B12 | | | | | |

Expected shape (not frozen):

- B1 precondition (one continuous wire).
- B2–B3 application (arrive through star; star as formation).
- B4 application of the same operator to definition text (route, not gloss).
- B5 statement: sequence = the route.
- B6 constraint on arrival (punch-card) plus application to a longer feed.
- B7–B8 application onto prior formation (thread; remains).
- B9 view of accumulated applications inside one star.
- B10 anti-model (stored gloss).
- B11 anti-model (flat graph as the thing).
- B12 statement of the operator.

B7’s name “touch” is this application, not a new opcode and not G2.

---

## 4. Feeds versus D types (T3)

B names at least:

- dictionary (B4) — a route through other stars; specimen PIE → dessert → slice → whole → food
- document feed (B6) — punch-card

B does not draw CONVERSATION as a third feed unless the PNG prints it. If it does not, do not import A11’s three-feed list.

Graphic D names two **types**: WORD, POINTER.

```
 B names          D stores
 --------         --------
 dictionary  \ 
 document     }→  more WORD rows
              /   (POINTER rows only when that rule exists)

                  types on D: WORD | POINTER only

 document-boundary-as-entry     [GAP]
 definition-as-stored-gloss     [X]     B10 / B4
 graph-as-store                 [X]     B11
```

Feeds are names of incoming text. They are not array types.

---

## 5. Seam table (T4)

| Name | On Graphic B? | Mechanism status |
|---|---|---|
| arrive / route through star / continue | B12 | the operator |
| punch-card / no interpretation | B6 | constraint on arrival |
| star as formation, not a point | B3 | view of passes |
| definition as route | B4 | same operator on definition text |
| sequence is physical | B5 | the route is the wire |
| thread, not attach | B7 | how later application meets prior formation |
| touch remains | B8 | permanence; not G2 |
| interior traffic | B9 | view of many applications |
| stored definition | B10 | `[X]` |
| 2D graph as the thing | B11 | `[X]` |
| WORD / POINTER / index / arity / when | not on B | D / G2. Point. Do not fill. |

---

## 6. What execute may write

- Append “Iteration 2 pass 2 — mechanism map” to [`graphic-b-ascii.md`](graphic-b-ascii.md).
- Add i2p2 to that file’s Executed line.
- Mark this plan EXECUTED.
- Tick the i2 pass-2 row in [`ascii-iterations.md`](ascii-iterations.md).

---

## 7. What execute must not do

- Redraw i1 spine, i1 fields, or i2p1 grain tables.
- Edit Graphic A ASCII.
- Fill G2.
- Add DOCUMENT / DEFINITION as array types.
- Copy A’s application-register cells.
- Start Graphic C.
- Treat B10/B11 as constructive machines.
- Add `src/` or retouch PNGs.

---

## 8. Single execute act

When the human says proceed (not this tick):

1. Re-read B4, B6, B10–B12 on the PNG. Operator quote must match B12.
2. Fill the twelve-row register. Every `New machine?` defended from that panel.
3. Draw the B↔D seam. Document boundary `[GAP]`. Gloss `[X]`. Graph-as-store `[X]`.
4. Fill the seam table. Stop.

Success: one operator; twelve roles; dictionary/document are feeds; D still has two types; G2 still `[GAP]`.  
Failure: a second rule; a third array type; B8/B7 become emission; A’s rows pasted onto B.

---

## Queued — later i2 passes, not this one

| i2 | Act |
|---|---|
| 3–6 | Graphic C (placeholders) |

---

## 9. Governing check

```text
One pass, one act.
B states one rule. Panels apply it, view it, or forbid a fake.
Feeds are not types.
D remains the store.
Φ stays [GAP].
```
