# Quiet door #9 (Coord) — Pass 1 PLAN

**Status:** EXECUTED — spine on `docs/coord/quiet-door-9-ascii.md`. Door + `coord.sh` unchanged. Emission `[GAP]`.  
**Station:** coord (HELD Grok-coord).  
**Object:** quiet door #9 — [`docs/coord/README.md`](README.md).  
**Why coord, not kit / not gearing-meta:** coord OWNS `docs/coord/**`. The companion can live next to the door. This campaign does not rewrite `coord.sh`, does not rewrite station files of other holders, and does not claim renderer.  
**Not:** Core. Not Graphic E. Not Hands. Not door #8 pass 7. Not projection iter 5. Not a shaft claim. Not a rewrite of `coord.sh`.  
**Emission:** `[GAP]`. No `src/`. Do not rewrite the coord door. Do not rewrite `coord.sh`. Do not reopen door #8.

```
pull/ff → refresh coord
        → write this plan
        → STOP
execute (later, one authorized tick):
        → write docs/coord/quiet-door-9-ascii.md spine
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**Can the coord door be one ASCII spine of what it names (Layer III umbrella; entrypoint; standing rule; doc stations; gear stations as delegates; fail closed) and what it refuses (Core, inventing Φ, rewriting `coord.sh` this pass, treating this shelf as Hands) — without becoming the claim protocol or a shaft drop?**

Pass 1 is the spine. Later passes hang fields, grain, mechanism, seam, receipt.

---

## 1. Already flat (do not redo)

| Object | Holds |
|---|---|
| Register row #9 | LIVE; `which` = coord |
| Door #8 (Gearing) | CLOSED 6/6; not this iteration |
| Renderer | FREE; HTML not this claim |
| Shafts | all FREE; not claimed this tick |
| Hands | four graphics still source of record |

This campaign makes #9 activity WORKED, then CLOSED at pass 6 of *this door iteration*. That does not flip LIVE. That does not reopen Gearing. That does not rewrite `coord.sh`.

---

## 2. Spine execute will draw (shape only)

Off the coord door headings only. Do not paste `coord.sh`. Do not paste station files.

```
  COORD DOOR
  docs/coord/README.md
  coordination umbrella (Layer III)
  not Core; POINTER emission remains [GAP]
  this shelf does not authorize src/
  fail closed: unknown path is held

  Entrypoint
    coord.sh — status / which / claim / release / check / refresh / gate / doctor
    stations/ — per-station FREE/HELD
    gear:shaft strips prefix and calls docs/gearing/claim.sh
    do not rewrite the shaft protocol

  Standing rule (before edit)
    ff-only main → which → claim → edit owned files → check → commit → release

  Doc stations
    clipboards prompts maps history hologram renderer
    kit law graphics coord gearing-meta

  Gear stations (delegate)
    axle gears trainA trainB trainC cells escape audits

  Station file shape
    STATUS AGENT BASE SINCE NOTE
    force-free is override, not expiry

  [X]  this shelf as Hands
  [X]  this shelf as Core
  [X]  invent Φ here
  [X]  rewrite coord.sh this pass
  [X]  claim a shaft
  [X]  claim renderer
  [X]  src/
  [X]  door #8 reopened
  [GAP] pointer-emission answers (G2)
```

Empty until proceed. Execute quotes headings. It does not invent a new station.

---

## 3. What execute may write

- `docs/coord/quiet-door-9-ascii.md` — spine only.
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the coord door, `coord.sh`, other stations' `.station` files, any HTML, or any `contracts-*.js`.
- Claim renderer.
- Claim a shaft.
- Fill G2.
- Reopen door #8.
- Start pass 2 fields.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote the door headings. Draw the spine. Stop.
2. Do not hang In / Does / Break / Gap (pass 2).
3. Do not rewrite `coord.sh`.

Success: one page that can take later passes without becoming Hands.  
Failure: `coord.sh` rewritten; a shaft taken; door #8 reopened.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 2 | fields: In / Does / Break / Gap / Role |
| 3 | grain: umbrella vs coord.sh vs stations vs Hands |
| 4 | walk: fail closed; which then claim; do not invent Φ |
| 5 | seam vs Hands, gearing, kit |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
This umbrella is Layer III.
This shelf is not Hands.
coord.sh stays unedited this pass.
Door #8 stays CLOSED.
Φ stays [GAP].
```
