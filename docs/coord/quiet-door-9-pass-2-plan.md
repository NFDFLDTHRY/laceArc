# Quiet door #9 (Coord) — Pass 2 PLAN

**Status:** PLAN only. Not run.  
**Station:** coord (HELD Grok-coord).  
**Object:** Coord fields — In / Does / Break / Gap / Role.  
**Authority:** [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md) pass-1 spine. Coord door as written.  
**Not:** Core. Not Graphic E. Not a rewrite of `coord.sh`. Not renderer claim. Not a shaft claim. Not door #8 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door + `coord.sh` stay. Door #8 stays CLOSED. Shafts stay FREE.

```
pull/ff → refresh coord
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append fields to quiet-door-9-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What does each coord-door block take in, what does it do, what would collapse the umbrella into Hands or treat `coord.sh` as bytes to rewrite this pass, and what stays a gap?**

Pass 2 is fields under the spine. It does not start umbrella-vs-`coord.sh`-vs-stations grain (pass 3).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine: umbrella / entrypoint / standing rule / stations / fail closed |

---

## 2. Rows execute will fill (empty until proceed)

Off the coord door headings only.

| # | Block | In | Does | Break | Gap | Role |
|---|---|---|---|---|---|---|
| Co1 | the coord door | | | | | |
| Co2 | Layer III / not Core | | | | | |
| Co3 | entrypoint `coord.sh` | | | | | |
| Co4 | standing rule | | | | | |
| Co5 | doc stations | | | | | |
| Co6 | gear stations (delegate) | | | | | |
| Co7 | fail closed / force-free | | | | | |
| Co8 | Φ / do not invent | | | | | |

Expected: Co3 Break = rewrite `coord.sh` this pass. Co6 Break = take a shaft from coord. Co7 Break = treat unknown path as FREE. Co8 Break = fill G2. G2 stays `[GAP]`.

---

## 3. What execute may write

- Append “Pass 2 — fields” to [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the coord door, `coord.sh`, other stations' `.station` files, any HTML, or any `contracts-*.js`.
- Claim renderer.
- Claim a shaft.
- Fill G2.
- Start pass 3 grain.
- Reopen door #8.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote the eight blocks off the door headings.
2. Fill In / Does / Break / Gap / Role. Stop.

Success: eight rows; `coord.sh` still named not rewritten.  
Failure: `coord.sh` rewritten; a shaft taken; door #8 reopened.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 3 | grain: umbrella vs `coord.sh` vs stations vs Hands |
| 4 | walk: fail closed; which then claim; do not invent Φ |
| 5 | seam vs Hands, gearing, kit |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
This umbrella is Layer III.
This shelf is not Hands.
coord.sh stays unedited this pass.
Door #8 stays CLOSED.
Φ stays [GAP].
```
