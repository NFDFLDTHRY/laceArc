# Quiet door #9 (Coord) — Pass 4 PLAN

**Status:** EXECUTED — walk on `docs/coord/quiet-door-9-ascii.md`. Door + `coord.sh` unchanged. Emission `[GAP]`.  
**Station:** coord (HELD Grok-coord).  
**Object:** Coord **walk** — one mechanism on [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md).  
**Authority:** Coord door passes 1–3. Coord door as a *named door*, not bytes to edit.  
**Not:** Hands verbs. Not a rewrite of `coord.sh`. Not renderer claim. Not a shaft claim. Not Graphic E. Not door #8 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door #8 stays CLOSED. Shafts stay FREE.

```
pull/ff → refresh coord
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append walk mechanism to quiet-door-9-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What single walk does the coord door run — fail closed; which then claim; do not invent Φ — how does every Co-block apply it, and what does it refuse versus Hands verbs, versus rewriting `coord.sh`, versus a claimed renderer?**

The coord mechanism is not arrive / through-star / continue. Those three verbs belong to Hands. This walk **names the owner or holds the unknown and does not emit POINTER**.

Pass 4 is the mechanism map. It does not start the seam (pass 5). This pass does **not** rewrite `coord.sh`.

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | Co1–Co8 fields |
| 3 | umbrella ≠ script ≠ station object ≠ Hands |

---

## 2. Operator as the coord door states it (T1)

Working sentence (the door wins; execute quotes, does not invent a Core verb):

> If you cannot tell which station owns a path, it is held. Fail closed. Before edit: ff-only main → which → claim → edit owned files → check → release. POINTER emission remains `[GAP]`.

Walk:

```
 reader arrives at docs/coord/README.md
   if a path is asked          → which; unknown is held
   if an edit is asked         → claim the owner; edit only owned files
   if Hands is asked           → send to the four graphics
   if frozen HTML is asked     → renderer; do not claim it
   if a shaft is asked         → delegate to claim.sh; this pass takes none
   if Φ is asked               → leave G2 open
   never: this shelf ⇒ Hands
   never: coord ⇒ rewrite coord.sh this pass
   never: coord ⇒ renderer
   never: door #8 ⇒ reopened
```

Execute draws **one walk box** on the coord companion. Not on the door. Not “emit POINTER.” Not A13.

---

## 3. Application register (T2)

Every Co-block. `New machine?` must be `no`.

Rows execute will fill:

| # | Role | What arrives | What is walked | What continues | New machine? |
|---|---|---|---|---|---|
| Co1 door | | | | | |
| Co2 Layer III | | | | | |
| Co3 coord.sh | | | | | |
| Co4 standing rule | | | | | |
| Co5 doc stations | | | | | |
| Co6 gear delegates | | | | | |
| Co7 fail closed | | | | | |
| Co8 Φ / do not invent | | | | | |

Expected: all `New machine?` = no. `coord.sh` stays unedited. Shafts stay FREE. Renderer stays unclaimed. Door #8 stays CLOSED.

---

## 4. Coord versus Hands versus renderer (T3)

```
 quiet-door-9-ascii.md       coord door              Hands
 --------------------       -------------           -----
 Layer III reading          the index               the store
 names stations             names stations          four graphics

 wrapper-as-Core            [X]
 Hands verbs pasted here    [X]
 coord.sh rewritten         [X]
 viewer writes L            [X]
 renderer claimed           [X]
 shaft claimed this pass    [X]
 door #8 reopened           [X]
 G2                         [GAP]
```

---

## 5. What execute may write

- Append “Pass 4 — walk” to [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md).
- Mark this plan EXECUTED.

---

## 6. What execute must not do

- Edit the door, `coord.sh`, HTML, JS, Hands, gearing, or kit files.
- Copy A13 onto this walk.
- Claim renderer.
- Claim a shaft.
- Fill G2.
- Reopen door #8.
- Start pass 5 seam.

---

## 7. Single execute act

When the human says proceed (not this tick):

1. Re-read spine + grain. Quote those, not A13.
2. Draw one walk box. Fill the register. Every `New machine?` = no.
3. Draw coord vs Hands vs renderer. Stop.

Success: one walk; `coord.sh` unedited.  
Failure: Hands verbs here; script rewritten; a shaft claimed.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 5 | seam vs Hands, gearing, kit |
| 6 | receipt |

---

## 8. Governing check

```text
One pass, one act.
Fail closed. Which then claim.
coord.sh stays unedited this pass.
Door #8 stays CLOSED.
Φ stays [GAP].
```
