# Quiet door #9 — Coord ASCII

**Object:** `docs/coord/README.md`  
**Station:** coord.  
**Executed:** [quiet-door-9-pass-1-plan.md](quiet-door-9-pass-1-plan.md) · [quiet-door-9-pass-2-plan.md](quiet-door-9-pass-2-plan.md).  
**Not:** Core. Not Graphic E. Not Hands. Not door #8 pass 7. Not projection iter 5. Not a shaft claim. Not a rewrite of `coord.sh`.  
**Emission:** `[GAP]`. No `src/`. Coord door bytes untouched. `coord.sh` untouched. Renderer unclaimed. Shafts FREE. Door #8 CLOSED.

This diagram is a reading of the coord door. The umbrella is Layer III. This shelf is not Hands. `coord.sh` stays unedited this pass.

---

## Spine (pass 1)

```
                         COORD DOOR
                         docs/coord/README.md
                         coordination umbrella (Layer III)
                         not Core; POINTER emission remains [GAP]
                         this shelf does not authorize src/
                         fail closed: unknown path is held

  Entrypoint
    coord.sh — status / which / claim / release / check / refresh / gate / doctor
    stations/ — per-station FREE/HELD
    gear:shaft strips prefix and calls docs/gearing/claim.sh
    do not rewrite the shaft protocol

  Standing rule (before edit)
    ff-only main → which → claim → edit owned files → check → commit → release

  Doc stations
    clipboards prompts maps history hologram renderer
    kit law graphics coord gearing-meta

  Gear stations (delegate)
    axle gears trainA trainB trainC cells escape audits

  Station file shape
    STATUS AGENT BASE SINCE NOTE
    force-free is override, not expiry

  [X]  this shelf as Hands
  [X]  this shelf as Core
  [X]  invent Φ here
  [X]  rewrite coord.sh this pass
  [X]  claim a shaft
  [X]  claim renderer
  [X]  src/
  [X]  door #8 reopened
  [GAP] pointer-emission answers (G2)
```

The umbrella names stations. It does not store Hands. `coord.sh` stays unedited this pass.

---

## Fence

| On this door | Not this spine |
|---|---|
| Layer III umbrella | Core |
| entrypoint `coord.sh` | rewrite `coord.sh` this pass |
| standing rule | a shaft drop |
| doc stations named | Hands store |
| gear stations as delegates | rewrite claim.sh |
| fail closed | invent Φ |
| no `src/` | open Rust |
| door #8 CLOSED | reopen Gearing |

---

## Pass 1 deltas

| Ticket | Action |
|---|---|
| T1 | Spine drawn from `docs/coord/README.md` headings |
| T2 | `coord.sh` named, not rewritten |
| T3 | Door + `coord.sh` + shafts + Gearing untouched |

## Pass 2 — fields

Off the coord door headings. Door wins over invention.

| # | Block | In | Does | Break | Gap | Role |
|---|---|---|---|---|---|---|
| Co1 | the coord door | a reader at `docs/coord/` | names the umbrella, not Hands | treat this page as Hands | — | index |
| Co2 | Layer III / not Core | a bid to store | keeps the umbrella off Core | treat this shelf as Hands | — | fence |
| Co3 | entrypoint `coord.sh` | a status / which / claim bid | names the script; does not rewrite it | rewrite `coord.sh` this pass | — | entrypoint |
| Co4 | standing rule | an agent about to edit | ff-only → which → claim → check → release | skip `which` and edit anyway | — | protocol |
| Co5 | doc stations | a docs path | names the owning station | treat stations as Hands | — | catalog |
| Co6 | gear stations (delegate) | a `gear:<shaft>` bid | strip prefix; call `claim.sh` | take a shaft from coord | — | delegate |
| Co7 | fail closed / force-free | an unknown path, or a stale hold | unknown is held; force-free is override | treat unknown as FREE | — | fence |
| Co8 | Φ / do not invent | a missing emission rule | leave G2 open | fill G2 here | G2 `[GAP]` | fence |

G2 stays `[GAP]`.

## Pass 2 deltas

| Ticket | Action |
|---|---|
| T1 | Eight In / Does / Break / Gap / Role rows |
| T2 | Co3 Break = rewrite `coord.sh`; Co6 Break = take a shaft |
| T3 | Spine left in place |
| T4 | Door + `coord.sh` + shafts + Gearing untouched |
