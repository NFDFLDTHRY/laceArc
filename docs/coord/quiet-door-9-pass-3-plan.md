# Quiet door #9 (Coord) — Pass 3 PLAN

**Status:** EXECUTED — grain on `docs/coord/quiet-door-9-ascii.md`. Door + `coord.sh` unchanged. Emission `[GAP]`.  
**Station:** coord (HELD Grok-coord).  
**Object:** Coord grain — umbrella vs `coord.sh` vs stations vs Hands.  
**Authority:** [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md) passes 1–2. Coord door as written. `coord.sh which`.  
**Not:** rewrite of `coord.sh`. Not renderer claim. Not a shaft claim. Not Graphic E. Not door #8 pass 7. Not projection iter 5.  
**Emission:** `[GAP]`. No `src/`. Door + `coord.sh` stay. Door #8 stays CLOSED. Shafts stay FREE.

```
pull/ff → refresh coord
        → write this plan
        → STOP
execute (later, one authorized tick):
        → append four-kind grain to quiet-door-9-ascii.md
        → mark this plan EXECUTED
        → check · commit · STOP
```

---

## 0. Question (one act)

**What does the coord door name, what does `coord.sh` run, what do station files record, what do Hands store — and what would it mean to rewrite `coord.sh` this pass or treat a `.station` file as Hands?**

Pass 3 is grain. It does not start the fail-closed walk (pass 4).

---

## 1. Already flat (do not redo)

| Pass | Holds |
|---|---|
| 1 | spine |
| 2 | Co1–Co8 fields |

---

## 2. Rows execute will fill (empty until proceed)

| Kind | Says | `which` | Collapse would look like |
|---|---|---|---|
| Coord door | | | |
| `coord.sh` | | | |
| `stations/kit.station` (specimen) | | | |
| Kit door | | | |
| Gearing door (CLOSED) | | | |
| Frozen viewer | | | |
| Hands (four graphics) | | | |
| Shafts (all eight) | | | |

Expected: coord door + `coord.sh` + station *files* `which` = coord. Kit door `which` = kit. Gearing door `which` = gearing-meta. Frozen viewer `which` = renderer. Four graphics `which` = graphics. `contracts-axle.js` `which` = `gear:axle`. Collapsing `coord.sh` into rewritten bytes this pass is the miss. Treating a `.station` file as Hands is the other miss. Editing another holder’s station object from coord is a third miss — the file lives here; the object does not.

---

## 3. What execute may write

- Append “Pass 3 — grain” to [`quiet-door-9-ascii.md`](quiet-door-9-ascii.md).
- Mark this plan EXECUTED.

---

## 4. What execute must not do

- Edit the coord door, `coord.sh`, other holders’ objects, any HTML, or any JS.
- Claim renderer.
- Claim a shaft.
- Fill G2.
- Start pass 4 walk.
- Reopen door #8.

---

## 5. Single execute act

When the human says proceed (not this tick):

1. Quote door, script, station file, kit, gearing, viewer, Hands. Fill `which` from coord.
2. Fill the grain. Four stay four.
3. Stop.

Success: a later reading cannot treat `coord.sh` as bytes to rewrite this pass.  
Failure: `coord.sh` rewritten; renderer claimed; a shaft taken.

---

## Queued — later passes, not this one

| Pass | Act |
|---|---|
| 4 | walk: fail closed; which then claim; do not invent Φ |
| 5 | seam vs Hands, gearing, kit |
| 6 | receipt |

---

## 6. Governing check

```text
One pass, one act.
Umbrella ≠ script ≠ station object ≠ Hands.
coord.sh stays unedited this pass.
Door #8 stays CLOSED.
Φ stays [GAP].
```
